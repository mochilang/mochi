//go:build ignore

// Generated by Mochi v0.10.39 on 2025-07-24 20:12:11 GMT+7
package main

import (
	"fmt"
)

var grid [][]string

func flood(x int, y int, repl string) {
	var target string = grid[y][x]
	_ = target
	if target == repl {
		return
	}
	var ff func(int, int)
	ff = func(px int, py int) {
		if (((px < 0) || (py < 0)) || (py >= len(grid))) || (px >= len(grid[0])) {
			return
		}
		if grid[py][px] != target {
			return
		}
		grid[py][px] = repl
		ff((px - 1), py)
		ff((px + 1), py)
		ff(px, (py - 1))
		ff(px, (py + 1))
	}
	ff(x, y)
}

func main() {
	grid = [][]string{[]string{".", ".", ".", ".", "."}, []string{".", "#", "#", "#", "."}, []string{".", "#", ".", "#", "."}, []string{".", "#", "#", "#", "."}, []string{".", ".", ".", ".", "."}}
	flood(2, 2, "o")
	for _, row := range grid {
		var line string = ""
		_ = line
		for _, _ch := range row {
			ch := string(_ch)
			line = (line + ch)
		}
		fmt.Println(line)
	}
}
