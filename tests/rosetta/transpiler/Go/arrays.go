//go:build ignore

// Generated by Mochi v0.10.38 on 2025-07-24 00:09:58 GMT+7
package main

import (
	"fmt"
)

func listStr(xs []int) string {
	var s string = "["
	_ = s
	var i int = 0
	_ = i
	for i < len(xs) {
		s = (s + fmt.Sprint(xs[i]))
		if (i + 1) < len(xs) {
			s = (s + " ")
		}
		i = (i + 1)
	}
	s = (s + "]")
	return s
}

var a []int = []int{0, 0, 0, 0, 0}

var s []int = a[0:4]

var cap_s int = 5

func main() {
	fmt.Println(("len(a) = " + fmt.Sprint(len(a))))
	fmt.Println(("a = " + listStr(a)))
	a[0] = 3
	fmt.Println(("a = " + listStr(a)))
	fmt.Println(("a[0] = " + fmt.Sprint(a[0])))
	fmt.Println(("s = " + listStr(s)))
	fmt.Println(((("len(s) = " + fmt.Sprint(len(s))) + "  cap(s) = ") + fmt.Sprint(cap_s)))
	s = a[0:5]
	fmt.Println(("s = " + listStr(s)))
	a[0] = 22
	s[0] = 22
	fmt.Println(("a = " + listStr(a)))
	fmt.Println(("s = " + listStr(s)))
	s = append(s, 4)
	s = append(s, 5)
	s = append(s, 6)
	cap_s = 10
	fmt.Println(("s = " + listStr(s)))
	fmt.Println(((("len(s) = " + fmt.Sprint(len(s))) + "  cap(s) = ") + fmt.Sprint(cap_s)))
	a[4] = (0 - 1)
	fmt.Println(("a = " + listStr(a)))
	fmt.Println(("s = " + listStr(s)))
	s = []int{}
	for i := 0; i < 8; i++ {
		s = append(s, 0)
	}
	cap_s = 8
	fmt.Println(("s = " + listStr(s)))
	fmt.Println(((("len(s) = " + fmt.Sprint(len(s))) + "  cap(s) = ") + fmt.Sprint(cap_s)))
}
