//go:build ignore

// Generated by Mochi v0.10.37 on 2025-07-23 11:51:45 GMT+7
package main

import (
	"bufio"
	"fmt"
	math "math"
	"os"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

var _scanner = bufio.NewScanner(os.Stdin)

func _input() string {
	if !_scanner.Scan() {
		return ""
	}
	return _scanner.Text()
}

func randDigit() int {
	return ((_now() % 9) + 1)
}

func mochiMain() {
	var digits []any = []any{}
	for i := 0; i < 4; i++ {
		digits = append(digits, randDigit())
	}
	var numstr string = ""
	for i := 0; i < 4; i++ {
		numstr = (numstr + fmt.Sprint(digits[i]))
	}
	fmt.Println((("Your numbers: " + numstr) + "\n"))
	fmt.Println("Enter RPN: ")
	var expr string = _input()
	if len(expr) != 7 {
		fmt.Println("invalid. expression length must be 7. (4 numbers, 3 operators, no spaces)")
		return
	}
	var stack []any = []any{}
	var i int = 0
	var valid bool = true
	for i < len(expr) {
		var ch string = string([]rune(expr)[i:(i + 1)])
		if (ch >= "0") && (ch <= "9") {
			if len(digits) == 0 {
				fmt.Println("too many numbers.")
				return
			}
			var j int = 0
			for digits[j] != (func() int { n, _ := strconv.Atoi(ch); return n }() - func() int { n, _ := strconv.Atoi("0"); return n }()) {
				j = (j + 1)
				if j == len(digits) {
					fmt.Println("wrong numbers.")
					return
				}
			}
			digits = func() []any {
				res := make([]any, len(digits[:j]))
				copy(res, digits[:j])
				res = append(res, digits[(j+1):]...)
				return res
			}()
			stack = append(stack, float64((func() int { n, _ := strconv.Atoi(ch); return n }() - func() int { n, _ := strconv.Atoi("0"); return n }())))
		} else {
			if len(stack) < 2 {
				fmt.Println("invalid expression syntax.")
				valid = false
				break
			}
			b := stack[(len(stack) - 1)]
			a := stack[(len(stack) - 2)]
			if ch == "+" {
				stack[(len(stack) - 2)] = (a.(float64) + b.(float64))
			} else {
				if ch == "-" {
					stack[(len(stack) - 2)] = (a.(float64) - b.(float64))
				} else {
					if ch == "*" {
						stack[(len(stack) - 2)] = (a.(float64) * b.(float64))
					} else {
						if ch == "/" {
							stack[(len(stack) - 2)] = (a.(float64) / b.(float64))
						} else {
							fmt.Println((ch + " invalid."))
							valid = false
							break
						}
					}
				}
			}
			stack = stack[:(len(stack) - 1)]
		}
		i = (i + 1)
	}
	if valid {
		if math.Abs((stack[0].(float64) - 24.0)) > 1e-06 {
			fmt.Println((("incorrect. " + fmt.Sprint(stack[0])) + " != 24"))
		} else {
			fmt.Println("correct.")
		}
	}
}

func main() {
	mochiMain()
}
