//go:build ignore

// Generated by Mochi v0.10.42 on 2025-07-27 22:23:02 GMT+7
package main

import (
	"fmt"
)

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

var depth int

var seq string

var i int

func main() {
	depth = 10
	seq = "F"
	i = 0
	for i < depth {
		var rev string = ""
		var j int = (len(seq) - 1)
		for j >= 0 {
			var c string = _substr(seq, j, (j + 1))
			if c == "L" {
				rev = (rev + "R")
			} else {
				if c == "R" {
					rev = (rev + "L")
				} else {
					rev = (rev + c)
				}
			}
			j = (j - 1)
		}
		seq = ((seq + "L") + rev)
		i = (i + 1)
	}
	fmt.Println(seq)
}
