//go:build ignore

// Generated by Mochi v0.10.52 on 2025-08-01 19:05:57 GMT+7
package main

import (
	"fmt"
	"os"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

var fs map[string]int

func mochiMain() {
	var old int = fs["input.txt"]
	_ = old
	fmt.Println(("mod time was: " + fmt.Sprint(old)))
	var mtime int = _now()
	_ = mtime
	mtime = _now()
	fs["input.txt"] = int(mtime)
	fmt.Println(("mod time now: " + fmt.Sprint(mtime)))
}

func main() {
	fs = map[string]int{"input.txt": 0}
	mochiMain()
}
