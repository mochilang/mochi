//go:build ignore

// Generated by Mochi v0.10.38 on 2025-07-24 00:43:09 GMT+7
package main

import (
	"fmt"
	"sort"
)

func mochiMain() {
	var m map[string]int = map[string]int{"hello": 13, "world": 31, "!": 71}
	_ = m
	for _, k := range func() []string {
		keys := make([]string, 0, len(m))
		for kx := range m {
			keys = append(keys, kx)
		}
		sort.Strings(keys)
		return keys
	}() {
		fmt.Println(((("key = " + k) + ", value = ") + fmt.Sprint(m[k])))
	}
	for _, k := range func() []string {
		keys := make([]string, 0, len(m))
		for kx := range m {
			keys = append(keys, kx)
		}
		sort.Strings(keys)
		return keys
	}() {
		fmt.Println(("key = " + k))
	}
	for _, k := range func() []string {
		keys := make([]string, 0, len(m))
		for kx := range m {
			keys = append(keys, kx)
		}
		sort.Strings(keys)
		return keys
	}() {
		fmt.Println(("value = " + fmt.Sprint(m[k])))
	}
}

func main() {
	mochiMain()
}
