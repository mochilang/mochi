//go:build ignore

// Generated by Mochi v0.10.42 on 2025-07-28 00:15:09 GMT+7
package main

import (
	"fmt"
)

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

var FASTA string

func splitLines(s string) []string {
	var lines []string = []string{}
	_ = lines
	var start int = 0
	_ = start
	var i int = 0
	_ = i
	for i < len(s) {
		if _substr(s, i, (i+1)) == "\n" {
			lines = append(lines, _substr(s, start, i))
			i = (i + 1)
			start = i
		} else {
			i = (i + 1)
		}
	}
	lines = append(lines, _substr(s, start, len(s)))
	return lines
}

func parseFasta(text string) []string {
	var key string = ""
	_ = key
	var val string = ""
	_ = val
	var out []string = []string{}
	_ = out
	for _, _ch := range splitLines(text) {
		line := string(_ch)
		if line == "" {
			continue
		}
		if _substr(line, 0, 1) == ">" {
			if key != "" {
				out = append(out, ((key + ": ") + val))
			}
			var hdr string = _substr(line, 1, len(line))
			_ = hdr
			var idx int = 0
			_ = idx
			for (idx < len(hdr)) && (_substr(hdr, idx, (idx+1)) != " ") {
				idx = (idx + 1)
			}
			key = _substr(hdr, 0, idx)
			val = ""
		} else {
			if key == "" {
				fmt.Println("missing header")
				return []string{}
			}
			val = (val + line)
		}
	}
	if key != "" {
		out = append(out, ((key + ": ") + val))
	}
	return out
}

func mochiMain() {
	var res []string = parseFasta(FASTA)
	_ = res
	for _, _ch := range res {
		line := string(_ch)
		fmt.Println(line)
	}
}

func main() {
	FASTA = (((((">Rosetta_Example_1\n" + "THERECANBENOSPACE\n") + ">Rosetta_Example_2\n") + "THERECANBESEVERAL\n") + "LINESBUTTHEYALLMUST\n") + "BECONCATENATED")
	mochiMain()
}
