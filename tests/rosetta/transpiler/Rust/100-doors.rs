// Generated by Mochi transpiler v0.10.37 on 2025-07-23 10:09 +0700
static mut g_doors: Vec<bool> = Vec::new();
fn main() {
    unsafe {
        g_doors = vec![];
        for i in 0..100 {
            g_doors = { let mut v = g_doors.clone(); v.push(false); v };
        }
        for pass in 1..101 {
            let mut idx: usize = (pass - 1);
            while (idx < 100) {
                g_doors[idx] = !g_doors[idx];
                idx = (idx + pass);
            }
        }
        for row in 0..10 {
            let mut line = String::from("");
            for col in 0..10 {
                let idx: usize = ((row * 10) + col);
                if g_doors[idx] {
                    line = format!("{}{}", line, "1");
                } else {
                    line = format!("{}{}", line, "0");
                }
                if (col < 9) {
                    line = format!("{}{}", line, " ");
                }
            }
            println!("{}", line);
        }
    }
}
