// Generated by Mochi transpiler v0.10.37 on 2025-07-22 23:21 +0700
fn main() {
    let mut doors = vec![];
    for i in 0..100 {
        doors = { let mut v = doors.clone(); v.push(false); v };
    }
    for pass in 1..101 {
        let mut idx: usize = (pass - 1);
        while (idx < 100) {
            doors[idx] = !doors[idx];
            idx = (idx + pass);
        }
    }
    for row in 0..10 {
        let mut line = String::from("");
        for col in 0..10 {
            let idx: usize = ((row * 10) + col);
            if doors[idx] {
                line = format!("{}{}", line, "1");
            } else {
                line = format!("{}{}", line, "0");
            }
            if (col < 9) {
                line = format!("{}{}", line, " ");
            }
        }
        println!("{}", line);
    }
}
