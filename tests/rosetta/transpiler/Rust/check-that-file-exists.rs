// Generated by Mochi transpiler v0.10.50 on 2025-07-31 08:00 +0700
use std::collections::HashMap;
fn printStat(fs: &HashMap<String, bool>, path: &str) {
    if fs.contains_key(path) {
        if fs[path] {
            println!("{}", format!("{}{}", path, " is a directory"));
        } else {
            println!("{}", format!("{}{}", path, " is a file"));
        }
    } else {
        println!("{}", format!("{}{}", format!("{}{}", "stat ", path), ": no such file or directory"));
    }
}

fn mochi_main() {
    let mut fs: HashMap<String, bool> = HashMap::from([]);
    fs.insert(String::from("docs"), true);
    for p in vec![String::from("input.txt"), String::from("/input.txt"), String::from("docs"), String::from("/docs")] {
        printStat(&fs, &p);
    }
}

fn main() {
    mochi_main();
}
