-- Generated by Mochi v0.10.37 on 2025-07-23 13:57 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function abs(x)
  if (x < 0) then
    return (0 - x)
  end
  return x
end;

function sqrtApprox(x)
  local guess = x
  local i = 0
  while (i < 20) do
    guess = ((guess + (x / guess)) / 2)
    i = (i + 1)
  end
  return guess
end;

function agmPi()
  local a = 1
  local g = (1 / sqrtApprox(2))
  local sum = 0
  local pow = 2
  while (abs((a - g)) > 1e-15) do
    local t = ((a + g) / 2)
    local u = sqrtApprox((a * g))
    a = t
    g = u
    pow = (pow * 2)
    local diff = ((a * a) - (g * g))
    sum = (sum + (diff * pow))
  end
  local pi = (((4 * a) * a) / (1 - sum))
  return pi
end;

function main()
  print(tostring(agmPi()))
end;

main();
