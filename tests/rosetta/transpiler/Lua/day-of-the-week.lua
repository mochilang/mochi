-- Generated by Mochi v0.10.40 on 2025-07-25 21:10 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function weekday(y, m, d)
  local yy = y
  local mm = m
  if (mm < 3) then
    mm = (mm + 12)
    yy = (yy - 1)
  end
  local k = (yy % 100)
  local j = math.floor((yy // 100))
  local a = math.floor(((13 * (mm + 1)) // 5))
  local b = math.floor((k // 4))
  local c = math.floor((j // 4))
  return ((((((d + a) + k) + b) + c) + (5 * j)) % 7)
end;

for year = 2008, 2122 - 1 do
  if (weekday(year, 12, 25) == 1) then
    print((("25 December " .. tostring(year)) .. " is Sunday"))
  end
end;
