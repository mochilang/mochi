-- Generated by Mochi v0.10.40 on 2025-07-25 21:10 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function main()
  print("enter rows cols: ")
  local row = math.floor(input())
  local col = math.floor(input())
  local a = {}
  local i = 0
  while (i < row) do
    local rowArr = {}
    local j = 0
    while (j < col) do
      rowArr = (function(lst, item)
      local res = {table.unpack(lst)}
      table.insert(res, item)
      return res
    end)(rowArr, 0)
    j = (j + 1)
  end
  a = (function(lst, item)
  local res = {table.unpack(lst)}
  table.insert(res, item)
  return res
end)(a, rowArr)
i = (i + 1)
end
print(("a[0][0] = " .. tostring(a[0 + 1][0 + 1])))
a[math.floor((row - 1)) + 1][math.floor((col - 1)) + 1] = 7
print(((((("a[" .. tostring((row - 1))) .. "][") .. tostring((col - 1))) .. "] = ") .. tostring(a[math.floor((row - 1)) + 1][math.floor((col - 1)) + 1])))
a = _nil
end;

main();
