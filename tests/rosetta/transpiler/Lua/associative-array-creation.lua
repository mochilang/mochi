-- Generated by Mochi v0.10.37 on 2025-07-23 14:12 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function removeKey(m, k)
  local out = {}
  for key in pairs(m) do
    if (key ~= k) then
      out[key] = m[key]
    end
  end
  return out
end;

function main()
  local x = nil
  x = {}
  x.foo = 3
  local y1 = x.bar
  local ok = (x["bar"] ~= nil)
  print(y1)
  print(((ok) and (1) or (0)))
  x = removeKey(x, "foo")
  x = {__name = "GenType1", __order = {"foo", "bar", "baz"}, foo = 2, bar = 42, baz = (0 - 1)}
  print((string.gsub(string.format("%d %d %d", x.foo, x.bar, x.baz), "%s+$", "")))
end;

main();
