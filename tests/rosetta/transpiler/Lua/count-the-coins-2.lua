-- Generated by Mochi v0.10.40 on 2025-07-25 21:10 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function countChange(amount)
  local ways = {}
  local i = 0
  while (i <= amount) do
    ways = (function(lst, item)
    local res = {table.unpack(lst)}
    table.insert(res, item)
    return res
  end)(ways, 0)
  i = (i + 1)
end
ways[0 + 1] = 1
for _, coin in ipairs({100, 50, 25, 10, 5, 1}) do
  local j = coin
  while (j <= amount) do
    ways[j + 1] = (ways[j + 1] + ways[(j - coin) + 1])
    j = (j + 1)
  end
end
return ways[amount + 1]
end;

amount = (1000 * 100);

print(((("amount, ways to make change: " .. tostring(amount)) .. " ") .. tostring(countChange(amount))));
