-- Generated by Mochi v0.10.38 on 2025-07-24 08:13 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _padStart(s, len, ch)
if ch == nil or ch == '' then ch = ' ' end
if #s >= len then return s end
local fill = string.sub(ch, 1, 1)
return string.rep(fill, len - #s) .. s
end
function main()
  local ss = {runtimeFields = {}}
  print("Create two fields at runtime: \n")
  local i = 1
  while (i <= 2) do
    print((("  Field #" .. tostring(i)) .. ":\n"))
    print("       Enter name  : ")
    local name = input()
    print("       Enter value : ")
    local value = input()
    local fields = ss.runtimeFields
    fields[name] = value
    ss.runtimeFields = fields
    print("\n")
    i = (i + 1)
  end
  while true do
    print("Which field do you want to inspect ? ")
    local name = input()
    if (ss.runtimeFields[name] ~= nil) then
      local value = ss.runtimeFields[name]
      print((("Its value is '" .. value) .. "'"))
      return
    else
      print("There is no field of that name, try again\n")
    end
  end
end;

main();
