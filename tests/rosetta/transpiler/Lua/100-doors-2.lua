-- Generated by Mochi v0.10.37 on 2025-07-22 21:48 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
door = 1;

incrementer = 0;

for current = 1, 101 - 1 do
  line = (("Door " .. tostring(current)) .. " ")
  if (current == door) then
    line = (line .. "Open")
    incrementer = (incrementer + 1)
    door = ((door + (2 * incrementer)) + 1)
  else
    line = (line .. "Closed")
  end
  print(line)
end;
