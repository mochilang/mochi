-- Generated by Mochi v0.10.38 on 2025-07-24 08:13 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _padStart(s, len, ch)
if ch == nil or ch == '' then ch = ' ' end
if #s >= len then return s end
local fill = string.sub(ch, 1, 1)
return string.rep(fill, len - #s) .. s
end
function pfacSum(i)
  local sum = 0
  local p = 1
  while (p <= (i // 2)) do
    if ((i % p) == 0) then
      sum = (sum + p)
    end
    p = (p + 1)
  end
  return sum
end;

function main()
  local d = 0
  local a = 0
  local pnum = 0
  local i = 1
  while (i <= 20000) do
    local j = pfacSum(i)
    if (j < i) then
      d = (d + 1)
    end
    if (j == i) then
      pnum = (pnum + 1)
    end
    if (j > i) then
      a = (a + 1)
    end
    i = (i + 1)
  end
  print((("There are " .. tostring(d)) .. " deficient numbers between 1 and 20000"))
  print((("There are " .. tostring(a)) .. " abundant numbers  between 1 and 20000"))
  print((("There are " .. tostring(pnum)) .. " perfect numbers between 1 and 20000"))
end;

main();
