-- Generated by Mochi v0.10.37 on 2025-07-22 19:34 GMT+7

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
doors = {};

for i = 0, 100 - 1 do
  doors = (function(lst, item)
  local res = {table.unpack(lst)}
  table.insert(res, item)
  return res
end)(doors, false)
end;

for pass = 1, 101 - 1 do
  idx = (pass - 1)
  while (idx < 100) do
    doors[idx + 1] = ((doors[idx + 1]) and (0) or (1))
    idx = (idx + pass)
  end
end;

for row = 0, 10 - 1 do
  line = ""
  for col = 0, 10 - 1 do
    idx = ((row * 10) + col)
    if doors[idx + 1] then
      line = (line .. "1")
    else
      line = (line .. "0")
    end
    if (col < 9) then
      line = (line .. " ")
    end
  end
  print(line)
end;
