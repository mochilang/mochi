-- Generated by Mochi v0.10.37 on 2025-07-23 13:23 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function countDivisors(n)
  if (n < 2) then
    return 1
  end
  local count = 2
  local i = 2
  while (i <= (n // 2)) do
    if ((n % i) == 0) then
      count = (count + 1)
    end
    i = (i + 1)
  end
  return count
end;

function main()
  print("The first 20 anti-primes are:")
  local maxDiv = 0
  local count = 0
  local n = 1
  local line = ""
  while (count < 20) do
    local d = countDivisors(n)
    if (d > maxDiv) then
      line = ((line .. tostring(n)) .. " ")
      maxDiv = d
      count = (count + 1)
    end
    n = (n + 1)
  end
  line = string.sub(line, 0 + 1, ((function(v)
  if type(v) == 'table' and v.items ~= nil then
    return #v.items
  elseif type(v) == 'table' and (v[1] == nil) then
      local c = 0
      for _ in pairs(v) do c = c + 1 end
      return c
    elseif type(v) == 'string' or type(v) == 'table' then
        return #v
      else
        return 0
      end
    end)(line) - 1))
    print(line)
  end;
  
  main();
