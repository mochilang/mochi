-- Generated by Mochi v0.10.37 on 2025-07-23 13:51 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function each(xs, f)
  for _, x in ipairs(xs) do
    f(x)
  end
end;

function Map(xs, f)
  local r = {}
  for _, x in ipairs(xs) do
    r = (function(lst, item)
    local res = {table.unpack(lst)}
    table.insert(res, item)
    return res
  end)(r, f(x))
end
return r
end;

function main()
  local s = {1, 2, 3, 4, 5}
  each(s, function(i)
  return print(tostring((i * i)))
end)
print(tostring(Map(s, function(i)
return (i * i)
end)))
end;

main();
