-- Generated by Mochi v0.10.37 on 2025-07-23 13:52 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function abs(x)
  if (x < 0) then
    return (0 - x)
  end
  return x
end;

function maxf(a, b)
  if (a > b) then
    return a
  end
  return b
end;

function isClose(a, b)
  local relTol = 1e-09
  local t = abs((a - b))
  local u = (relTol * maxf(abs(a), abs(b)))
  return (t <= u)
end;

function sqrtApprox(x)
  local guess = x
  local i = 0
  while (i < 10) do
    guess = ((guess + (x / guess)) / 2)
    i = (i + 1)
  end
  return guess
end;

function main()
  local root2 = sqrtApprox(2)
  local pairs = {{1.0000000000000002e+14, 1.0000000000000002e+14}, {100.01, 100.011}, {(1.0000000000000002e+13 / 10000), 1.0000000000000001e+09}, {0.001, 0.0010000001}, {1.01e-22, 0}, {(root2 * root2), 2}, {((0 - root2) * root2), (0 - 2)}, {1e+17, 1e+17}, {3.141592653589793, 3.141592653589793}}
  for _, pair in ipairs(pairs) do
    local a = pair[0 + 1]
    local b = pair[1 + 1]
    local s = ((isClose(a, b)) and ("≈") or ("≉"))
    print(((((tostring(a) .. " ") .. s) .. " ") .. tostring(b)))
  end
end;

main();
