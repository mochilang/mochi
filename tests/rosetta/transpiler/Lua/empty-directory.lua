-- Generated by Mochi v0.10.41 on 2025-07-27 15:57 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function isEmptyDir(fs, name)
  if (fs[name] ~= nil) then
    return ((function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' or type(v) == 'table' then
          return #v
        else
          return 0
        end
      end)(fs[name]) == 0)
    end
    return true
  end;
  
  function main()
    local fs = {}
    fs["/tmp"] = {}
    fs["/var"] = {"log"}
    if isEmptyDir(fs, "/tmp") then
      print("/tmp is empty")
    else
      print("/tmp is not empty")
    end
  end;
  
  main();
