-- Generated by Mochi v0.10.37 on 2025-07-23 13:52 GMT+7
function input()
  return io.read('*l')
end

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
  return _now_seed
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
function pow_int(base, exp)
  local result = 1
  local b = base
  local e = exp
  while (e > 0) do
    if ((e % 2) == 1) then
      result = (result * b)
    end
    b = (b * b)
    e = (e // 2)
  end
  return result
end;

function pow_big(base, exp)
  local result = 1
  local b = base
  local e = exp
  while (e > 0) do
    if ((e % 2) == 1) then
      result = (result * b)
    end
    b = (b * b)
    e = (e // 2)
  end
  return result
end;

e1 = pow_int(3, 2);

e2 = pow_int(4, e1);

base = 5;

x = pow_big(base, e2);

s = tostring(x);

print((string.gsub(string.format("5^(4^(3^2)) has %s digits: %s ... %s", (function(v)
if type(v) == 'table' and v.items ~= nil then
  return #v.items
elseif type(v) == 'table' and (v[1] == nil) then
    local c = 0
    for _ in pairs(v) do c = c + 1 end
    return c
  elseif type(v) == 'string' or type(v) == 'table' then
      return #v
    else
      return 0
    end
  end)(s), string.sub(s, 0 + 1, 20), string.sub(s, ((function(v)
  if type(v) == 'table' and v.items ~= nil then
    return #v.items
  elseif type(v) == 'table' and (v[1] == nil) then
      local c = 0
      for _ in pairs(v) do c = c + 1 end
      return c
    elseif type(v) == 'string' or type(v) == 'table' then
        return #v
      else
        return 0
      end
    end)(s) - 20) + 1, (function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' or type(v) == 'table' then
          return #v
        else
          return 0
        end
      end)(s))), "%s+$", "")));
