run: exit status 1
    warning: variable "seed" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 14 │               seed = v
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:14:15: Main._now/0

    warning: variable "seeded" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 15 │               seeded = true
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:15:15: Main._now/0

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 53 │       {arr, i} = try do
    │             ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:53:13: Main.shuffleStr/1

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 89 │       {i, out} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:89:8: Main.shuffleStr/1

    warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 72 │       {arr, j} = try do
    │             ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:72:13: Main.shuffleStr/1

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 140 │           {j, line} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:140:12: Main.createPolybius/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 153 │       {i, p} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:153:8: Main.createPolybius/0

     warning: variable "li" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 114 │       {labels, li} = try do
     │                ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:114:16: Main.createPolybius/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 183 │       {i, key, pool} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:183:8: Main.createKey/1

     warning: variable "pool" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 183 │       {i, key, pool} = try do
     │                ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:183:16: Main.createKey/1

     warning: variable "pairs" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 224 │                 pairs = List.replace_at(pairs, j + 1, tmp)
     │                 ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:224:17: Main.orderKey/1

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 232 │           {j, pairs} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:232:12: Main.orderKey/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 208 │       {i, pairs} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:208:8: Main.orderKey/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 261 │       {i, res} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:261:8: Main.orderKey/1

     warning: variable "m" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 244 │       {m, pairs} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:244:8: Main.orderKey/1

     warning: variable "temp" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 302 │                     temp = ((temp <> Enum.slice(labels, r, r + 1 - r)) <> Enum.slice(labels, c, c + 1 - c))
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:302:21: Main.encrypt/3

     warning: variable "c" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 310 │               {c, temp} = try do
     │                ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:310:16: Main.encrypt/3

     warning: variable "r" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 322 │           {r, temp} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:322:12: Main.encrypt/3

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 362 │           {j, row} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:362:12: Main.encrypt/3

     warning: variable "ri" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 415 │           {colStr, ri} = try do
     │                    ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:415:20: Main.encrypt/3

     warning: variable "result" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 440 │             result = (result <> " ")
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:440:13: Main.encrypt/3

     warning: variable "idx" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 393 │       {idx, table} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:393:8: Main.encrypt/3

     warning: variable "ci" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 448 │       {ci, result} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:448:8: Main.encrypt/3

     warning: variable "ci" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 428 │       {ci, cols} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:428:8: Main.encrypt/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 334 │       {i, temp} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:334:8: Main.encrypt/3

     warning: variable "rIdx" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 375 │       {rIdx, table} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:375:8: Main.encrypt/3

     warning: variable "li" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 285 │       {labels, li} = try do
     │                ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:285:16: Main.encrypt/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 473 │       i = try do
     │       ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:473:7: Main.indexOf/2

     warning: variable "colStrs" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 492 │             colStrs = (colStrs ++ [String.slice(cipherText, start, i - start)])
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:492:13: Main.decrypt/3

     warning: variable "start" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 493 │             start = i + 1
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:493:13: Main.decrypt/3

     warning: variable "maxColLen" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 512 │             maxColLen = String.length(Enum.at(colStrs, i))
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:512:13: Main.decrypt/3

     warning: variable "cols" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 568 │             cols = (cols ++ [ls])
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:568:13: Main.decrypt/3

     warning: variable "cols" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 566 │             cols = (cols ++ [pad])
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:566:13: Main.decrypt/3

     warning: variable "k" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 560 │             {k, pad} = try do
     │              ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:560:14: Main.decrypt/3

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 542 │           {j, ls} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:542:12: Main.decrypt/3

     warning: variable "c" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 597 │           {c, row} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:597:12: Main.decrypt/3

     warning: variable "c" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 630 │           {c, table} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:630:12: Main.decrypt/3

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 662 │           {j, temp} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:662:12: Main.decrypt/3

     warning: variable "idx" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 693 │       {idx, plainText} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:693:8: Main.decrypt/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 520 │       {i, maxColLen} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:520:8: Main.decrypt/3

     warning: variable "r" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 642 │       {r, table} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:642:8: Main.decrypt/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 501 │       {colStrs, i, start} = try do
     │                 ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:501:17: Main.decrypt/3

     warning: variable "r" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 674 │       {r, temp} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:674:8: Main.decrypt/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 576 │       {cols, i} = try do
     │              ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:576:14: Main.decrypt/3

     warning: variable "start" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 501 │       {colStrs, i, start} = try do
     │                    ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:501:20: Main.decrypt/3

     warning: variable "r" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 610 │       {r, table} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:610:8: Main.decrypt/3

    warning: function _lookup_host/1 is unused
    │
 31 │   defp _lookup_host(host) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:31:8: Main (module)

6 x 6 Polybius square:

  | A D F G V X
---------------
** (Protocol.UndefinedError) protocol Enumerable not implemented for type BitString

Got value:

    "HVTP98JUWS6LOCID5YMR17AN2K0XZFGBEQ43"

    (elixir 1.18.3) lib/enum.ex:1: Enumerable.impl_for!/1
    (elixir 1.18.3) lib/enum.ex:230: Enumerable.slice/1
    (elixir 1.18.3) lib/enum.ex:4579: Enum.slice_forward/4
    /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:127: anonymous fn/5 in Main.createPolybius/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:154: Main.createPolybius/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:707: Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:723: Main.bench_main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/adfgvx-cipher.exs:729: (file)
