run: exit status 1
    warning: variable "seed" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 14 │               seed = v
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:14:15: Main._now/0

    warning: variable "seeded" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 15 │               seeded = true
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:15:15: Main._now/0

     warning: variable "bit" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 134 │       {bit, res, x, y} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:134:8: Main.xor/2

     warning: variable "x" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 134 │       {bit, res, x, y} = try do
     │                  ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:134:18: Main.xor/2

     warning: variable "y" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 134 │       {bit, res, x, y} = try do
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:134:21: Main.xor/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 158 │       {i, v} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:158:8: Main.rshift/2

     warning: variable "v" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 213 │       {out, v} = try do
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:213:13: Main.toHex/1

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 241 │           {j, word} = try do
     │            ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:241:12: Main.crc32Table/0

     warning: variable "table" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 247 │           table = (table ++ [word])
     │           ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:247:11: Main.crc32Table/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 254 │       i = try do
     │       ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:254:7: Main.crc32Table/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 280 │       {crc, i} = try do
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:280:13: Main.crc32/1

     warning: function _sha256/1 is unused
     │
 106 │   defp _sha256(bs) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:106:8: Main (module)

    warning: function _lookup_host/1 is unused
    │
 31 │   defp _lookup_host(host) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:31:8: Main (module)

    warning: function _bigrat_sub/2 is unused
    │
 87 │   defp _bigrat_sub(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:87:8: Main (module)

     warning: function _bigrat_neg/1 is unused
     │
 102 │   defp _bigrat_neg(a) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:102:8: Main (module)

    warning: function _bigrat_mul/2 is unused
    │
 92 │   defp _bigrat_mul(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:92:8: Main (module)

    warning: function _bigrat_div/2 is unused
    │
 97 │   defp _bigrat_div(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:97:8: Main (module)

    warning: function _bigrat_add/2 is unused
    │
 82 │   defp _bigrat_add(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:82:8: Main (module)

    warning: function _bigrat/2 is unused
    │
 70 │   defp _bigrat(n, d) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:70:8: Main (module)

    warning: function _bigrat/1 is unused
    │
 67 │   defp _bigrat(v) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:67:8: Main (module)

** (ArithmeticError) bad argument in arithmetic expression: rem(nil, 2)
    :erlang.rem(nil, 2)
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:118: anonymous fn/5 in Main.xor/2
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:135: Main.xor/2
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:273: anonymous fn/4 in Main.crc32/1
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:281: Main.crc32/1
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:294: Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:304: Main.bench_main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/crc-32-1.exs:311: (file)
