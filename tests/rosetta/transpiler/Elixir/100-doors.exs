# Code generated by Mochi transpiler 2025-07-22 17:27 +0700
doors = []
Enum.each((0..(100 - 1)), fn i ->
  doors = (doors ++ [false])
end)
Enum.each((1..(101 - 1)), fn pass ->
  idx = pass - 1
  while_fun = fn while_fun, doors, idx ->
    if idx < 100 do
      doors = List.replace_at(doors, idx, !Enum.at(doors, idx))
      idx = idx + pass
      while_fun.(while_fun, doors, idx)
    else
      {doors, idx}
    end
  end
  {doors, idx} = while_fun.(while_fun, doors, idx)
end)
Enum.each((0..(10 - 1)), fn row ->
  line = ""
  Enum.each((0..(10 - 1)), fn col ->
    idx = row * 10 + col
    if Enum.at(doors, idx) do
      line = (line <> "1")
    else
      line = (line <> "0")
    end
    if col < 9 do
      line = (line <> " ")
    end
  end)
  IO.puts(line)
end)
