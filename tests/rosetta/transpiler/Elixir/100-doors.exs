# Code generated by Mochi transpiler 2025-07-26 17:59 +0700
defmodule Main do
  defp _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      seed
    else
      System.os_time(:nanosecond)
    end
  end
  defp _mem() do
    :erlang.memory(:total)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) -> String.slice(base, start, len)
      len == 1 -> Enum.at(base, start)
      true -> Enum.slice(base, start, len)
    end
  end
  def main() do
    doors = []
    {doors} = Enum.reduce((0..(100 - 1)), {doors}, fn i, {doors} ->
      doors = (doors ++ [false])
      {doors}
    end)
    {doors} = Enum.reduce((1..(101 - 1)), {doors}, fn pass, {doors} ->
      idx = pass - 1
      while_fun_2 = fn while_fun_2, doors, idx ->
        if idx < 100 do
          doors = List.replace_at(doors, idx, !Enum.at(doors, idx))
          idx = idx + pass
          while_fun_2.(while_fun_2, doors, idx)
        else
          {doors, idx}
        end
      end
      {doors, idx} = try do
          while_fun_2.(while_fun_2, doors, idx)
        catch
          :break -> {doors, idx}
        end

      {doors}
    end)
    Enum.each((0..(10 - 1)), fn row ->
      line = ""
      {line} = Enum.reduce((0..(10 - 1)), {line}, fn col, {line} ->
        idx = row * 10 + col
        line = if Enum.at(doors, idx) do
  (line <> "1")
else
  (line <> "0")
end
        if col < 9 do
          line = (line <> " ")
        end
        {line}
      end)
      IO.puts(line)
    end)
  end
end
Main.main()
