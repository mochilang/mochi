run: exit status 1
    warning: variable "seed" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 14 │               seed = v
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:14:15: Main._now/0

    warning: variable "seeded" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 15 │               seeded = true
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:15:15: Main._now/0

    warning: variable "arr" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 76 │                 arr = List.replace_at(arr, j + 1, tmp)
    │                 ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:76:17: Main.sortRunes/1

    warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 84 │           {arr, j} = try do
    │                 ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:84:17: Main.sortRunes/1

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 60 │       {arr, i} = try do
    │             ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:60:13: Main.sortRunes/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 113 │       {i, out} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:113:8: Main.sortRunes/1

    warning: variable "m" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 96 │       {arr, m} = try do
    │             ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:96:13: Main.sortRunes/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 141 │       i = try do
     │       ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:141:7: Main.deranged/2

     warning: variable "m" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 167 │               m = Map.put(m, k, [w])
     │               ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:167:15: Main.main/0

     warning: variable "bestLen" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 174 │                     bestLen = length(w)
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:174:21: Main.main/0

     warning: variable "w1" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 175 │                     w1 = c
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:175:21: Main.main/0

     warning: variable "w2" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 176 │                     w2 = w
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:176:21: Main.main/0

     warning: variable "m" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 186 │             m = Map.put(m, k, (m[k] ++ [w]))
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:186:13: Main.main/0

    warning: function _slice/3 is unused
    │
 40 │   defp _slice(base, start, len) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:40:8: Main (module)

    warning: function _now/0 is unused
    │
  3 │   defp _now() do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:3:8: Main (module)

    warning: function _mem/0 is unused
    │
 28 │   defp _mem() do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:28:8: Main (module)

    warning: function _lookup_host/1 is unused
    │
 31 │   defp _lookup_host(host) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:31:8: Main (module)

** (ArgumentError) errors were found at the given arguments:

  * 1st argument: not a list

    :erlang.length("constitutionalism")
    /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:162: anonymous fn/1 in Main.main/0
    (elixir 1.18.3) lib/enum.ex:1714: Enum."-map/2-lists^map/1-1-"/2
    /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:160: Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/anagrams-deranged-anagrams.exs:200: (file)
