# Code generated by Mochi transpiler 2025-07-22 17:27 +0700
door = 1
incrementer = 0
Enum.each((1..(101 - 1)), fn current ->
  line = (("Door " <> to_string(current)) <> " ")
  if current == door do
    line = (line <> "Open")
    incrementer = incrementer + 1
    door = door + 2 * incrementer + 1
  else
    line = (line <> "Closed")
  end
  IO.inspect(line)
end)
