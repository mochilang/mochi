run: exit status 1
    warning: variable "seed" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 14 │               seed = v
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:14:15: Main._now/0

    warning: variable "seeded" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 15 │               seeded = true
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:15:15: Main._now/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 142 │       i = try do
     │       ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:142:7: Main.sliceEqual/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 169 │       {i, j, xs} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:169:8: Main.reverse/1

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 169 │       {i, j, xs} = try do
     │           ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:169:11: Main.reverse/1

     warning: variable "xs" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 169 │       {i, j, xs} = try do
     │              ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:169:14: Main.reverse/1

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 222 │               {c, j} = try do
     │                   ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:222:19: Main.perimEqual/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 235 │           {c, i} = try do
     │               ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:235:15: Main.perimEqual/2

     warning: variable "c" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 248 │       {c, r} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:248:8: Main.perimEqual/2

     warning: variable "r" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 248 │       {c, r} = try do
     │           ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:248:11: Main.perimEqual/2

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 284 │           {arr, j} = try do
     │                 ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:284:17: Main.sortEdges/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 296 │       {arr, i} = try do
     │             ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:296:13: Main.sortEdges/1

     warning: variable "e" is unused (if the variable is not meant to be used, prefix it with an underscore)
     │
 333 │           e = Enum.at(face, i)
     │           ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:333:11: Main.faceToPerim/1

     warning: variable "e" is unused (if the variable is not meant to be used, prefix it with an underscore)
     │
 364 │               e = Enum.at(edges, idx)
     │               ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:364:15: Main.faceToPerim/1

     warning: variable "idx" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 407 │           {done, edges, found, idx, last, le, perim} = try do
     │                                ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:407:32: Main.faceToPerim/1

     warning: variable "done" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 421 │       {done, edges, last, le, perim} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:421:8: Main.faceToPerim/1

     warning: variable "edges" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 421 │       {done, edges, last, le, perim} = try do
     │              ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:421:14: Main.faceToPerim/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 344 │       {edges, i} = try do
     │               ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:344:15: Main.faceToPerim/1

     warning: variable "last" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 421 │       {done, edges, last, le, perim} = try do
     │                     ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:421:21: Main.faceToPerim/1

     warning: variable "le" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 421 │       {done, edges, last, le, perim} = try do
     │                           ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:421:27: Main.faceToPerim/1

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 451 │       {i, s} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:451:8: Main.listStr/1

     warning: function _sha256/1 is unused
     │
 106 │   defp _sha256(bs) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:106:8: Main (module)

    warning: function _now/0 is unused
    │
  3 │   defp _now() do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:3:8: Main (module)

    warning: function _mem/0 is unused
    │
 28 │   defp _mem() do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:28:8: Main (module)

    warning: function _lookup_host/1 is unused
    │
 31 │   defp _lookup_host(host) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:31:8: Main (module)

     warning: function _getenv/1 is unused
     │
 110 │   defp _getenv(name) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:110:8: Main (module)

     warning: function _environ/0 is unused
     │
 113 │   defp _environ() do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:113:8: Main (module)

    warning: function _bigrat_sub/2 is unused
    │
 87 │   defp _bigrat_sub(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:87:8: Main (module)

     warning: function _bigrat_neg/1 is unused
     │
 102 │   defp _bigrat_neg(a) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:102:8: Main (module)

    warning: function _bigrat_mul/2 is unused
    │
 92 │   defp _bigrat_mul(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:92:8: Main (module)

    warning: function _bigrat_div/2 is unused
    │
 97 │   defp _bigrat_div(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:97:8: Main (module)

    warning: function _bigrat_add/2 is unused
    │
 82 │   defp _bigrat_add(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:82:8: Main (module)

    warning: function _bigrat/2 is unused
    │
 70 │   defp _bigrat(n, d) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:70:8: Main (module)

    warning: function _bigrat/1 is unused
    │
 67 │   defp _bigrat(v) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:67:8: Main (module)

Perimeter format equality checks:
  Q == R is true
  U == V is true

Edge to perimeter format translations:
** (KeyError) key :b not found in: [%{b: 11, a: 7}, %{b: 11, a: 1}, %{b: 7, a: 1}]

If you are using the dot syntax, such as map.field, make sure the left-hand side of the dot is a map
    /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:334: anonymous fn/5 in Main.faceToPerim/1
    /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:345: Main.faceToPerim/1
    /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:477: anonymous fn/1 in Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:490: Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/faces-from-a-mesh.exs:497: (file)
