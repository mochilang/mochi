// Generated by Mochi transpiler
void main() {
  final Map<String, int> m = {"hello": 13, "world": 31, "!": 71};
  for (var k in keys(m)) {
    print("key = " + k + ", value = " + (m[k]!).toString());
  }
  for (var k in keys(m)) {
    print("key = " + k);
  }
  for (var k in keys(m)) {
    print("value = " + (m[k]!).toString());
  }
}

void _start() {
  main();
}
