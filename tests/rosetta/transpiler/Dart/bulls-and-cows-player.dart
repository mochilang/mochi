// Generated by Mochi transpiler
import 'dart:io';

int indexOf(String s, String ch) {
  int i = 0;
  while (i < s.length) {
    if (s.substring(i, i + 1) == ch) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

List<String> fields(String s) {
  List<String> words = [];
  String cur = "";
  int i = 0;
  while (i < s.length) {
    final String ch = s.substring(i, i + 1);
    if (ch == " " || ch == "	" || ch == "\n") {
    if (cur.length > 0) {
    words = [...words, cur];
    cur = "";
  };
  } else {
    cur = cur + ch;
  }
    i = i + 1;
  }
  if (cur.length > 0) {
    words = [...words, cur];
  }
  return words;
}

List<String> makePatterns() {
  final List<String> digits = ["1", "2", "3", "4", "5", "6", "7", "8", "9"];
  List<String> pats = [];
  int i = 0;
  while (i < digits.length) {
    int j = 0;
    while (j < digits.length) {
    if (j != i) {
    int k = 0;
    while (k < digits.length) {
    if (k != i && k != j) {
    int l = 0;
    while (l < digits.length) {
    if (l != i && l != j && l != k) {
    pats = [...pats, digits[i] + digits[j] + digits[k] + digits[l]];
  }
    l = l + 1;
  };
  }
    k = k + 1;
  };
  }
    j = j + 1;
  }
    i = i + 1;
  }
  return pats;
}

void main() {
  print("Cows and bulls/player\n" + "You think of four digit number of unique digits in the range 1 to 9.\n" + "I guess.  You score my guess:\n" + "    A correct digit but not in the correct place is a cow.\n" + "    A correct digit in the correct place is a bull.\n" + "You give my score as two numbers separated with a space.");
  List<String> patterns = makePatterns();
  while (true) {
    if (patterns.length == 0) {
    print("Oops, check scoring.");
    return;
  }
    final String guess = patterns[0];
    patterns = patterns.sublist(1, patterns.length);
    int cows = 0;
    int bulls = 0;
    while (true) {
    print("My guess: " + guess + ".  Score? (c b) ");
    final String line = stdin.readLineSync() ?? '';
    final List<String> toks = fields(line);
    if (toks.length == 2) {
    final int c = (toks[0]).codeUnitAt(0);
    final int b = (toks[1]).codeUnitAt(0);
    if (c >= 0 && c <= 4 && b >= 0 && b <= 4 && c + b <= 4) {
    cows = c;
    bulls = b;
    break;
  };
  }
    print("Score guess as two numbers: cows bulls");
  }
    if (bulls == 4) {
    print("I did it. :)");
    return;
  }
    List<String> next = [];
    int idx = 0;
    while (idx < patterns.length) {
    final String pat = patterns[idx];
    int c = 0;
    int b = 0;
    int i = 0;
    while (i < 4) {
    final String cg = guess.substring(i, i + 1);
    final String cp = pat.substring(i, i + 1);
    if (cg == cp) {
    b = b + 1;
  } else {
    if (pat.indexOf(cg) >= 0) {
    c = c + 1;
  };
  }
    i = i + 1;
  }
    if (c == cows && b == bulls) {
    next = [...next, pat];
  }
    idx = idx + 1;
  }
    patterns = next;
  }
}

void _start() {
  main();
}
