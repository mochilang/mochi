// Generated by Mochi transpiler
String beastKind(dynamic b) {
  return b == Dog(k, _) ? k : b == Cat(k, _) ? k : null;
}

String beastName(dynamic b) {
  return b == Dog(_, n) ? n : b == Cat(_, n) ? n : null;
}

String beastCry(dynamic b) {
  return b == Dog(_, _) ? "Woof" : b == Cat(_, _) ? "Meow" : null;
}

void bprint(dynamic b) {
  print(beastName(b) + ", who's a " + beastKind(b) + ", cries: \"" + beastCry(b) + "\".");
}

void main() {
  final d = {"kind": "labrador", "name": "Max"};
  final c = {"kind": "siamese", "name": "Sammy"};
  bprint(d);
  bprint(c);
}

void _start() {
  main();
  main();
}
