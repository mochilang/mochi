// Generated by Mochi transpiler
List<List<String>> grid = [[".", ".", ".", ".", "."], [".", "#", "#", "#", "."], [".", "#", ".", "#", "."], [".", "#", "#", "#", "."], [".", ".", ".", ".", "."]];
void flood(int x, int y, String repl) {
  final String target = grid[y]![x];
  if (target == repl) {
    return;
  }
  void ff(int px, int py) {
  if (px < 0 || py < 0 || py >= grid.length || px >= grid[0].length) {
    return;
  }
  if (grid[py]![px] != target) {
    return;
  }
  grid[py]![px] = repl;
  ff(px - 1, py);
  ff(px + 1, py);
  ff(px, py - 1);
  ff(px, py + 1);
}
  ff(x, y);
}

void main() {
  flood(2, 2, "o");
  for (var row in grid) {
    String line = "";
    for (var ch in row) {
    line = line + ch;
  }
    print(line);
  }
}
