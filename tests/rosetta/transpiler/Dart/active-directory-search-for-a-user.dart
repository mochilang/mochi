// Generated by Mochi transpiler
List<String> search_user(Map<String, List<String>> directory, String username) {
  return directory[username]!;
}

void main() {
  final Map<String, dynamic> client = {"Base": "dc=example,dc=com", "Host": "ldap.example.com", "Port": 389, "GroupFilter": "(memberUid=%s)"};
  final Map<String, List<String>> directory = {"username": ["admins", "users"], "john": ["users"]};
  final List<String> groups = search_user(directory, "username");
  if (groups.length > 0) {
    String out = "Groups: [";
    int i = 0;
    while (i < groups.length) {
    out = out + "\"" + groups[i] + "\"";
    if (i < groups.length - 1) {
    out = out + ", ";
  }
    i = i + 1;
  };
    out = out + "]";
    print(out);
  } else {
    print("User not found");
  }
}

void _start() {
  main();
  main();
}
