// Generated by Mochi transpiler
num abs(num x) {
  if (x < 0.0) {
    return -x;
  }
  return x;
}

num sqrtApprox(num x) {
  num guess = x;
  int i = 0;
  while (i < 20) {
    guess = (guess + x / guess) / 2.0;
    i = i + 1;
  }
  return guess;
}

num agmPi() {
  num a = 1.0;
  num g = 1.0 / sqrtApprox(2.0);
  num sum = 0.0;
  num pow = 2.0;
  while ((a - g).abs().toString().compareTo(0.000000000000001.toString()) > 0) {
    num t = (a + g) / 2.0;
    num u = sqrtApprox(a * g);
    a = t;
    g = u;
    pow = pow * 2.0;
    num diff = a * a - g * g;
    sum = sum + diff * pow;
  }
  num pi = 4.0 * a * a / (1.0 - sum);
  return pi;
}

void main() {
  print((agmPi()).toString());
}

void _start() {
  main();
}
