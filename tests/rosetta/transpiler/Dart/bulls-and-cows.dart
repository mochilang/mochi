// Generated by Mochi transpiler
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

int indexOf(String s, String ch) {
  int i = 0;
  while (i < s.length) {
    if (s.substring(i, i + 1) == ch) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

List<String> shuffle(List<String> xs) {
  List<String> arr = xs;
  int i = arr.length - 1;
  while (i > 0) {
    final int j = _now() % (i + 1);
    final String tmp = arr[i];
    arr[i] = arr[j];
    arr[j] = tmp;
    i = i - 1;
  }
  return arr;
}

void main() {
  print("Cows and Bulls");
  print("Guess four digit number of unique digits in the range 1 to 9.");
  print("A correct digit but not in the correct place is a cow.");
  print("A correct digit in the correct place is a bull.");
  List<String> digits = ["1", "2", "3", "4", "5", "6", "7", "8", "9"];
  digits = shuffle(digits);
  String pat = digits[0] + digits[1] + digits[2] + digits[3];
  final String valid = "123456789";
  while (true) {
    print("Guess: ");
    final String guess = stdin.readLineSync() ?? '';
    if (guess.length != 4) {
    print("Please guess a four digit number.");
    continue;
  }
    int cows = 0;
    int bulls = 0;
    String seen = "";
    int i = 0;
    bool malformed = false;
    while (i < 4) {
    final String cg = guess.substring(i, i + 1);
    if (seen.indexOf(cg) != -1) {
    print("Repeated digit: " + cg);
    malformed = true;
    break;
  }
    seen = seen + cg;
    final int pos = pat.indexOf(cg);
    if (pos == -1) {
    if (valid.indexOf(cg) == -1) {
    print("Invalid digit: " + cg);
    malformed = true;
    break;
  };
  } else {
    if (pos == i) {
    bulls = bulls + 1;
  } else {
    cows = cows + 1;
  };
  }
    i = i + 1;
  }
    if (malformed) {
    continue;
  }
    print("Cows: " + (cows).toString() + ", bulls: " + (bulls).toString());
    if (bulls == 4) {
    print("You got it.");
    break;
  }
  }
}

void _start() {
  _initNow();
  main();
}
