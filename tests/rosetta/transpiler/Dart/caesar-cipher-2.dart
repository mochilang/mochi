// Generated by Mochi transpiler
int indexOf(String s, String ch) {
  int i = 0;
  while (i < s.length) {
    if (s.substring(i, i + 1) == ch) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

int ord(String ch) {
  final String upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  final String lower = "abcdefghijklmnopqrstuvwxyz";
  int idx = upper.indexOf(ch);
  if (idx >= 0) {
    return 65 + idx;
  }
  idx = lower.indexOf(ch);
  if (idx >= 0) {
    return 97 + idx;
  }
  return 0;
}

String chr(int n) {
  final String upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  final String lower = "abcdefghijklmnopqrstuvwxyz";
  if (n >= 65 && n < 91) {
    return upper.substring(n - 65, n - 64);
  }
  if (n >= 97 && n < 123) {
    return lower.substring(n - 97, n - 96);
  }
  return "?";
}

String shiftRune(String r, int k) {
  if (r.compareTo("a") >= 0 && r.compareTo("z") <= 0) {
    return chr((ord(r) - 97 + k) % 26 + 97);
  }
  if (r.compareTo("A") >= 0 && r.compareTo("Z") <= 0) {
    return chr((ord(r) - 65 + k) % 26 + 65);
  }
  return r;
}

String encipher(String s, int k) {
  String out = "";
  int i = 0;
  while (i < s.length) {
    out = out + shiftRune(s.substring(i, i + 1), k);
    i = i + 1;
  }
  return out;
}

String decipher(String s, int k) {
  return encipher(s, (26 - k % 26) % 26);
}

void main() {
  final String pt = "The five boxing wizards jump quickly";
  print("Plaintext: " + pt);
  for (var key in [0, 1, 7, 25, 26]) {
    if (key.toString().compareTo(1.toString()) < 0 || key.toString().compareTo(25.toString()) > 0) {
    print("Key " + (key).toString() + " invalid");
    continue;
  }
    final String ct = encipher(pt, key);
    print("Key " + (key).toString());
    print("  Enciphered: " + ct);
    print("  Deciphered: " + decipher(ct, key));
  }
}

void _start() {
  main();
}
