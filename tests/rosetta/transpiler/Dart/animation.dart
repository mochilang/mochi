// Generated by Mochi transpiler
final String msg = "Hello World! ";
int shift = 0;
int inc = 1;
int clicks = 0;
int frames = 0;
void main() {
  while (clicks < 5) {
    String line = "";
    int i = 0;
    while (i < msg.length) {
    final int idx = (shift + i) % msg.length;
    line = line + msg.substring(idx, idx + 1);
    i = i + 1;
  }
    print(line);
    shift = (shift + inc) % msg.length;
    frames = frames + 1;
    if (frames % msg.length == 0) {
    inc = msg.length - inc;
    clicks = clicks + 1;
  }
  }
}
