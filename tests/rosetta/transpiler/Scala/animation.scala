// Generated by Mochi v0.10.39 on 2025-07-25 00:26:14 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
object Main {
  val msg: String = "Hello World! "
  
  var shift: Int = 0
  
  var inc: Int = 1
  
  var clicks: Int = 0
  
  var frames: Int = 0
  
  def main(args: Array[String]): Unit = {
    while (clicks < 5) {
      var line: String = ""
      var i: Int = 0
      while (i < (msg).size) {
        val idx = (shift + i) % (msg).size
        line = (line + msg.slice(idx, idx + 1)).asInstanceOf[String]
        i = (i + 1).asInstanceOf[Int]
      }
      println(line)
      shift = ((shift + inc) % (msg).size).asInstanceOf[Int]
      frames = (frames + 1).asInstanceOf[Int]
      if (frames % (msg).size == 0) {
        inc = ((msg).size - inc).asInstanceOf[Int]
        clicks = (clicks + 1).asInstanceOf[Int]
      }
    }
  }
}
