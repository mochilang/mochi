// Generated by Mochi v0.10.52 on 2025-08-02 10:48:54 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
object Main {
  private def _repeat(s: String, n: BigInt): String = s * n.toInt
  
  def repeat(s: String, n: BigInt): String = {
    var out: String = ""
    var i: BigInt = BigInt(0)
    while (i < n) {
      out = (out + s).toString
      i = i + BigInt(1)
    }
    return out
  }
  
  def reverseStr(s: String): String = {
    var out: String = ""
    var i: BigInt = (s).size - BigInt(1).toInt
    while (i >= BigInt(0)) {
      out = out + s.slice((i).toInt, (i + BigInt(1)).toInt)
      i = i - BigInt(1)
    }
    return out
  }
  
  var records: ArrayBuffer[String] = ArrayBuffer(_repeat("abcdefgh", BigInt(10)), _repeat("ijklmnop", BigInt(10)), _repeat("qrstuvwx", BigInt(10)))
  
  def main(args: Array[String]): Unit = {
    for (r <- records) {
      println(reverseStr(r))
    }
  }
}
