// Generated by Mochi v0.10.40 on 2025-07-26 11:01:14 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
object Main {
  def printStat(fs: scala.collection.mutable.Map[String,Boolean], path: String): Any = {
    if ((fs.contains(path)).asInstanceOf[Boolean]) {
      if (fs.getOrElse(path, null.asInstanceOf[Boolean])) {
        println(path + " is a directory")
      } else {
        println(path + " is a file")
      }
    } else {
      println("stat " + path + ": no such file or directory")
    }
  }
  
  def main(): Any = {
    var fs: scala.collection.mutable.Map[String,Boolean] = scala.collection.mutable.Map()
    fs.update("docs", true)
    for (p <- ArrayBuffer("input.txt", "/input.txt", "docs", "/docs")) {
      printStat(fs, p.toString)
    }
  }
  
  def main(args: Array[String]): Unit = {
    main()
  }
}
