// Generated by Mochi v0.10.38 on 2025-07-24 08:28:27 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs(System.nanoTime().toInt)
  }
}

def randDigit(): Int = {
  return (_now() % 9 + 1).asInstanceOf[Int]
}

def main(): Unit = {
  var digits: ArrayBuffer[Any] = ArrayBuffer()
  for (i <- 0 until 4) {
    digits = digits :+ randDigit()
  }
  var numstr: String = ""
  for (i <- 0 until 4) {
    numstr = (numstr + String.valueOf(digits(i))).asInstanceOf[String]
  }
  println("Your numbers: " + numstr + "\n")
  println("Enter RPN: ")
  var expr = Option(scala.io.StdIn.readLine()).getOrElse("q")
  if ((expr).size != 7) {
    println("invalid. expression length must be 7. (4 numbers, 3 operators, no spaces)")
    return
  }
  var stack: ArrayBuffer[Any] = ArrayBuffer()
  var i: Int = 0
  var valid: Boolean = true
  val _br0 = new Breaks
  _br0.breakable {
    while (i < (expr).size) {
      val ch: String = expr.substring(i, i + 1)
      if ((ch >= "0" && ch <= "9").asInstanceOf[Boolean]) {
        if ((digits).size == 0) {
          println("too many numbers.")
          return
        }
        var j: Int = 0
        while (digits(j) != ch.asInstanceOf[Int] - "0".asInstanceOf[Int]) {
          j = (j + 1).asInstanceOf[Int]
          if (j == (digits).size) {
            println("wrong numbers.")
            return
          }
        }
        digits = (digits.slice(0, j) + digits.slice(j + 1, (digits).size)).asInstanceOf[ArrayBuffer[Any]]
        stack = stack :+ float(ch.asInstanceOf[Int] - "0".asInstanceOf[Int])
      } else {
        if ((stack).size < 2) {
          println("invalid expression syntax.")
          valid = false
          _br0.break()
        }
        var b = stack((stack).size - 1)
        var a = stack((stack).size - 2)
        if (ch == "+") {
          stack((stack).size - 2) = a + b
        } else {
          if (ch == "-") {
            stack((stack).size - 2) = a - b
          } else {
            if (ch == "*") {
              stack((stack).size - 2) = a * b
            } else {
              if (ch == "/") {
                stack((stack).size - 2) = a / b
              } else {
                println(ch + " invalid.")
                valid = false
                _br0.break()
              }
            }
          }
        }
        stack = (stack.slice(0, (stack).size - 1)).asInstanceOf[ArrayBuffer[Any]]
      }
      i = (i + 1).asInstanceOf[Int]
    }
  }
  if (valid) {
    if (abs((stack(0)).asInstanceOf[double] - 24.0) > 0.000001) {
      println("incorrect. " + String.valueOf(stack(0)) + " != 24")
    } else {
      println("correct.")
    }
  }
}

def main(args: Array[String]): Unit = {
  main()
}
}
