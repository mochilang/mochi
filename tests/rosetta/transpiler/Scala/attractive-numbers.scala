// Generated by Mochi v0.10.39 on 2025-07-25 01:20:17 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
object Main {
  def isPrime(n: Int): Boolean = {
    if (n < 2) {
      return false
    }
    if (Math.floorMod(n, 2) == 0) {
      return n == 2
    }
    if (Math.floorMod(n, 3) == 0) {
      return n == 3
    }
    var d: Int = 5
    while ((d * d).asInstanceOf[Int] <= n) {
      if (Math.floorMod(n, d) == 0) {
        return false
      }
      d = (d + 2).asInstanceOf[Int]
      if (Math.floorMod(n, d) == 0) {
        return false
      }
      d = (d + 4).asInstanceOf[Int]
    }
    return true
  }
  
  def countPrimeFactors(_n: Int): Int = {
    var n: Int = _n
    if (n == 1) {
      return 0
    }
    if (isPrime(n)) {
      return 1
    }
    var count: Int = 0
    var f: Int = 2
    while (true) {
      if (Math.floorMod(n, f) == 0) {
        count = (count + 1).asInstanceOf[Int]
        n = (n / f).asInstanceOf[Int]
        if (n == 1) {
          return count
        }
        if (isPrime(n)) {
          f = n
        }
      } else {
        if (f >= 3) {
          f = (f + 2).asInstanceOf[Int]
        } else {
          f = 3
        }
      }
    }
    return count
  }
  
  def pad4(n: Int): String = {
    var s = String.valueOf(n)
    while ((s).size < 4) {
      s = " " + s
    }
    return s
  }
  
  def main() = {
    val max: Int = 120
    println("The attractive numbers up to and including " + String.valueOf(max) + " are:")
    var count: Int = 0
    var line: String = ""
    var lineCount: Int = 0
    var i: Int = 1
    while (i <= max) {
      val c: Int = countPrimeFactors(i)
      if (isPrime(c)) {
        line = (line + pad4(i)).asInstanceOf[String]
        count = (count + 1).asInstanceOf[Int]
        lineCount = (lineCount + 1).asInstanceOf[Int]
        if (lineCount == 20) {
          println(line)
          line = ""
          lineCount = 0
        }
      }
      i = (i + 1).asInstanceOf[Int]
    }
    if (lineCount > 0) {
      println(line)
    }
  }
  
  def main(args: Array[String]): Unit = {
    main()
  }
}
