;; Generated by Mochi 0.10.38 on 2025-07-23 16:59 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (current-inexact-milliseconds)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))


(define (poolPut p x)
  (let/ec _return (begin
(_return (append p (list x)))
))
)
(define (poolGet p)
  (let/ec _return (begin
(if (equal? (cond [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) 0) (let ()
(displayln "pool empty")
(_return (hash "pool" p "val" 0))
) (void))
(define idx (- (cond [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) 1))
(define v (list-ref p idx))
(set! p (sublist p 0 idx))
(_return (hash "pool" p "val" v))
))
)
(define (clearPool p)
  (let/ec _return (begin
(_return (list))
))
)
(define (main)
  (let/ec _return (begin
(define pool (list))
(define i 1)
(define j 2)
(displayln (format "~a" (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(set! pool (poolPut pool i))
(set! pool (poolPut pool j))
(set! i 0)
(set! j 0)
(define res1 (poolGet pool))
(set! pool (hash-ref res1 "pool"))
(set! i (int (hash-ref res1 "val")))
(define res2 (poolGet pool))
(set! pool (hash-ref res2 "pool"))
(set! j (int (hash-ref res2 "val")))
(set! i 4)
(set! j 5)
(displayln (format "~a" (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(set! pool (poolPut pool i))
(set! pool (poolPut pool j))
(set! i 0)
(set! j 0)
(set! pool (clearPool pool))
(define res3 (poolGet pool))
(set! pool (hash-ref res3 "pool"))
(set! i (int (hash-ref res3 "val")))
(define res4 (poolGet pool))
(set! pool (hash-ref res4 "pool"))
(set! j (int (hash-ref res4 "val")))
(set! i 7)
(set! j 8)
(displayln (format "~a" (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(main)
