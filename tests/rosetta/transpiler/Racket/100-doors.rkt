;; Generated by Mochi 0.10.37 on 2025-07-22 23:20 +0700
#lang racket/base
(require racket/list racket/string racket/math json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (current-inexact-milliseconds)))))


(define doors (list))
(for ([i (in-range 0 100)])
(set! doors (append doors (list #f)))
)
(for ([pass (in-range 1 101)])
(define idx (- pass 1))
(let/ec _break (let loop ()
  (if (< idx 100) (let ()
    (set! doors (list-set doors idx (not (list-ref doors idx))))
    (set! idx (+ idx pass))
    (loop)) (void))))
)
(for ([row (in-range 0 10)])
(define line "")
(for ([col (in-range 0 10)])
(define idx (+ (* row 10) col))
(if (list-ref doors idx) (let ()
(set! line (string-append line "1"))
) (let ()
(set! line (string-append line "0"))
))
(if (< col 9) (let ()
(set! line (string-append line " "))
) (void))
)
(displayln line)
)
