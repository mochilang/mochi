;; Generated by Mochi 0.10.39 on 2025-07-24 13:15 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (current-inexact-milliseconds)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))


(define (merge base update)
  (let/ec _return (begin
(define result (hash))
(for ([k (in-hash-keys base)])
(set! result (hash-set result k (hash-ref base k #f)))
)
(for ([k (in-hash-keys update)])
(set! result (hash-set result k (hash-ref update k #f)))
)
(_return result)
))
)
(define (main)
  (let/ec _return (begin
(define base (hash "name" "Rocket Skates" "price" 12.75 "color" "yellow"))
(define update (hash "price" 15.25 "color" "red" "year" 1974))
(define result (merge base update))
(displayln result)
))
)
(main)
