;; Generated by Mochi 0.10.37 on 2025-07-23 10:24 +0700
#lang racket/base
(require racket/list racket/string racket/math json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (current-inexact-milliseconds)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (input) (read-line))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))


(define OP_NUM 0)
(define OP_ADD 1)
(define OP_SUB 2)
(define OP_MUL 3)
(define OP_DIV 4)
(define (newNum n)
  (let/ec _return (begin
(_return (hash "op" OP_NUM "value" (hash "num" n "denom" 1)))
))
)
(define (exprEval x)
  (let/ec _return (begin
(if (equal? (hash-ref x "op") OP_NUM) (let ()
(_return (hash-ref x "value"))
) (void))
(define l (exprEval (hash-ref x "left")))
(define r (exprEval (hash-ref x "right")))
(if (equal? (hash-ref x "op") OP_ADD) (let ()
(_return (hash "num" (+ (* (hash-ref l "num") (hash-ref r "denom")) (* (hash-ref l "denom") (hash-ref r "num"))) "denom" (* (hash-ref l "denom") (hash-ref r "denom"))))
) (void))
(if (equal? (hash-ref x "op") OP_SUB) (let ()
(_return (hash "num" (- (* (hash-ref l "num") (hash-ref r "denom")) (* (hash-ref l "denom") (hash-ref r "num"))) "denom" (* (hash-ref l "denom") (hash-ref r "denom"))))
) (void))
(if (equal? (hash-ref x "op") OP_MUL) (let ()
(_return (hash "num" (* (hash-ref l "num") (hash-ref r "num")) "denom" (* (hash-ref l "denom") (hash-ref r "denom"))))
) (void))
(_return (hash "num" (* (hash-ref l "num") (hash-ref r "denom")) "denom" (* (hash-ref l "denom") (hash-ref r "num"))))
))
)
(define (exprString x)
  (let/ec _return (begin
(if (equal? (hash-ref x "op") OP_NUM) (let ()
(_return (format "~a" (hash-ref (hash-ref x "value") "num")))
) (void))
(define ls (exprString (hash-ref x "left")))
(define rs (exprString (hash-ref x "right")))
(define opstr "")
(if (equal? (hash-ref x "op") OP_ADD) (let ()
(set! opstr " + ")
) (let ()
(if (equal? (hash-ref x "op") OP_SUB) (let ()
(set! opstr " - ")
) (let ()
(if (equal? (hash-ref x "op") OP_MUL) (let ()
(set! opstr " * ")
) (let ()
(set! opstr " / ")
))
))
))
(_return (string-append (string-append (string-append (string-append "(" ls) opstr) rs) ")"))
))
)
(define n_cards 4)
(define goal 24)
(define digit_range 9)
(define (solve xs)
  (let/ec _return (begin
(if (equal? (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)]) 1) (let ()
(define f (exprEval (list-ref xs 0)))
(if (and (not (equal? (hash-ref f "denom") 0)) (equal? (hash-ref f "num") (* (hash-ref f "denom") goal))) (let ()
(displayln (exprString (list-ref xs 0)))
(_return #t)
) (void))
(_return #f)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let ()
    (define j (+ i 1))
    (let/ec _break (let loop ()
  (if (< j (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let ()
    (define rest (list))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let ()
    (if (and (not (equal? k i)) (not (equal? k j))) (let ()
(set! rest (append rest (list (list-ref xs k))))
) (void))
    (set! k (+ k 1))
    (loop)) (void))))
    (define a (list-ref xs i))
    (define b (list-ref xs j))
    (for ([op (list OP_ADD OP_SUB OP_MUL OP_DIV)])
(define node (hash "op" op "left" a "right" b))
(if (solve (append rest (list node))) (let ()
(_return #t)
) (void))
)
    (define node (hash "op" OP_SUB "left" b "right" a))
    (if (solve (append rest (list node))) (let ()
(_return #t)
) (void))
    (set! node (hash "op" OP_DIV "left" b "right" a))
    (if (solve (append rest (list node))) (let ()
(_return #t)
) (void))
    (set! j (+ j 1))
    (loop)) (void))))
    (set! i (+ i 1))
    (loop)) (void))))
(_return #f)
))
)
(define (main)
  (let/ec _return (begin
(define iter 0)
(let/ec _break (let loop ()
  (if (< iter 10) (let ()
    (define cards (list))
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i n_cards) (let ()
    (define n (+ (modulo (now) (- digit_range 1)) 1))
    (set! cards (append cards (list (newNum n))))
    (displayln (string-append " " (format "~a" n)))
    (set! i (+ i 1))
    (loop)) (void))))
    (displayln ":  ")
    (if (not (solve cards)) (let ()
(displayln "No solution")
) (void))
    (set! iter (+ iter 1))
    (loop)) (void))))
))
)
(main)
