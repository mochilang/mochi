;; Generated by Mochi 0.10.42 on 2025-07-28 10:03 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (pow10 n)
  (let/ec _return (begin
(define r 1.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (set! r (* r 10.0))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return r)
))
)
(define (formatFloat f prec)
  (let/ec _return (begin
(define scale (pow10 prec))
(define scaled (let ([__l (* f scale)] [__r 0.5]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define n (int scaled))
(define digits (format "~a" n))
(let/ec _break (let loop ()
  (if (<= (cond [(string? digits) (string-length digits)] [(hash? digits) (hash-count digits)] [else (length digits)]) prec) (let ()
    (set! digits (string-append "0" digits))
    (loop)) (void))))
(define intPart (substring digits 0 (- (cond [(string? digits) (string-length digits)] [(hash? digits) (hash-count digits)] [else (length digits)]) prec)))
(define fracPart (substring digits (- (cond [(string? digits) (string-length digits)] [(hash? digits) (hash-count digits)] [else (length digits)]) prec) (cond [(string? digits) (string-length digits)] [(hash? digits) (hash-count digits)] [else (length digits)])))
(_return (string-append (string-append intPart ".") fracPart))
))
)
(define (padLeft s w)
  (let/ec _return (begin
(define res "")
(define n (- w (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])))
(let/ec _break (let loop ()
  (if (> n 0) (let ()
    (set! res (string-append res " "))
    (set! n (- n 1))
    (loop)) (void))))
(_return (string-append res s))
))
)
(define (averageSquareDiff f preds)
  (let/ec _return (begin
(define av 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? preds) (string-length preds)] [(hash? preds) (hash-count preds)] [else (length preds)])) (let ()
    (set! av (let ([__l av] [__r (* (- (if preds (list-ref preds (int i)) #f) f) (- (if preds (list-ref preds (int i)) #f) f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! av (/ av (exact->inexact (cond [(string? preds) (string-length preds)] [(hash? preds) (hash-count preds)] [else (length preds)]))))
(_return av)
))
)
(define (diversityTheorem truth preds)
  (let/ec _return (begin
(define av 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? preds) (string-length preds)] [(hash? preds) (hash-count preds)] [else (length preds)])) (let ()
    (set! av (let ([__l av] [__r (if preds (list-ref preds (int i)) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! av (/ av (exact->inexact (cond [(string? preds) (string-length preds)] [(hash? preds) (hash-count preds)] [else (length preds)]))))
(define avErr (averageSquareDiff truth preds))
(define crowdErr (* (- truth av) (- truth av)))
(define div (averageSquareDiff av preds))
(_return (list avErr crowdErr div))
))
)
(define (main)
  (let/ec _return (begin
(define predsArray (list (list 48.0 47.0 51.0) (list 48.0 47.0 51.0 42.0)))
(define truth 49.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? predsArray) (string-length predsArray)] [(hash? predsArray) (hash-count predsArray)] [else (length predsArray)])) (let ()
    (define preds (if predsArray (list-ref predsArray (int i)) #f))
    (define res (diversityTheorem truth preds))
    (displayln (string-append "Average-error : " (padLeft (formatFloat (if res (list-ref res (int 0)) #f) 3) 6)))
    (displayln (string-append "Crowd-error   : " (padLeft (formatFloat (if res (list-ref res (int 1)) #f) 3) 6)))
    (displayln (string-append "Diversity     : " (padLeft (formatFloat (if res (list-ref res (int 2)) #f) 3) 6)))
    (displayln "")
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
