;; Generated by Mochi 0.10.38 on 2025-07-23 16:59 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (current-inexact-milliseconds)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))


(define (main)
  (let/ec _return (begin
(define a 12)
(define b 8)
(displayln (string-append (string-append (string-append (string-append (format "~a" a) " + ") (format "~a" b)) " = ") (format "~a" (let ([__l a] [__r b]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(displayln (string-append (string-append (string-append (string-append (format "~a" a) " - ") (format "~a" b)) " = ") (format "~a" (- a b))))
(displayln (string-append (string-append (string-append (string-append (format "~a" a) " * ") (format "~a" b)) " = ") (format "~a" (* a b))))
(displayln (string-append (string-append (string-append (string-append (format "~a" a) " / ") (format "~a" b)) " = ") (format "~a" (int (quotient a b)))))
(displayln (string-append (string-append (string-append (string-append (format "~a" a) " % ") (format "~a" b)) " = ") (format "~a" (modulo a b))))
))
)
(main)
