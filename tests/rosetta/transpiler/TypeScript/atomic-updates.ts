// Generated by Mochi v0.10.38 on 2025-07-23 16:17:23 GMT+7

function randOrder(seed: number, n: number): number[] {
  const next: number = (((seed * 1664525) + 1013904223) % 2147483647);
  return [next, (next % n)];
}
function randChaos(seed: number, n: number): number[] {
  const next: number = (((seed * 1103515245) + 12345) % 2147483647);
  return [next, (next % n)];
}
function main() {
  const nBuckets: number = 10;
  const initialSum: number = 1000;
  let buckets: number[] = [];
  for (let i = 0; i < nBuckets; i++) {
    buckets = [...buckets, 0];
  }
  let i: number = nBuckets;
  let dist: number = initialSum;
  while ((i > 0)) {
    const v: number = Math.trunc(dist / i);
    i = (i - 1);
    buckets[i] = v;
    dist = (dist - v);
  }
  let tc0: number = 0;
  let tc1: number = 0;
  let total: number = 0;
  let nTicks: number = 0;
  let seedOrder: number = 1;
  let seedChaos: number = 2;
  console.log("sum  ---updates---    mean  buckets");
  let t: number = 0;
  while ((t < 5)) {
    let r: number[] = randOrder(seedOrder, nBuckets);
    seedOrder = r[0];
    let b1: number = r[1];
    let b2: number = ((b1 + 1) % nBuckets);
    const v1: number = buckets[b1];
    const v2: number = buckets[b2];
    if ((v1 > v2)) {
      let a: number = ((v1 - v2) / 2);
      if ((a > buckets[b1])) {
        a = buckets[b1];
      }
      buckets[b1] = (buckets[b1] - a);
      buckets[b2] = (buckets[b2] + a);
    } else {
      let a: number = ((v2 - v1) / 2);
      if ((a > buckets[b2])) {
        a = buckets[b2];
      }
      buckets[b2] = (buckets[b2] - a);
      buckets[b1] = (buckets[b1] + a);
    }
    tc0 = (tc0 + 1);
    r = randChaos(seedChaos, nBuckets);
    seedChaos = r[0];
    b1 = r[1];
    b2 = ((b1 + 1) % nBuckets);
    r = randChaos(seedChaos, (buckets[b1] + 1));
    seedChaos = r[0];
    let amt: number = r[1];
    if ((amt > buckets[b1])) {
      amt = buckets[b1];
    }
    buckets[b1] = (buckets[b1] - amt);
    buckets[b2] = (buckets[b2] + amt);
    tc1 = (tc1 + 1);
    let sum = 0;
    let idx: number = 0;
    while ((idx < nBuckets)) {
      sum = (sum + buckets[idx]);
      idx = (idx + 1);
    }
    total = ((total + tc0) + tc1);
    nTicks = (nTicks + 1);
    console.log(((((((((String(sum) + " ") + String(tc0)) + " ") + String(tc1)) + " ") + String(Math.trunc(total / nTicks))) + "  ") + String(buckets)));
    tc0 = 0;
    tc1 = 0;
    t = (t + 1);
  }
}
main();
