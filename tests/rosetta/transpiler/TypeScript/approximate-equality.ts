// Generated by Mochi v0.10.37 on 2025-07-23 15:52:25 GMT+7

function abs(x: number): number {
  if ((x < 0.0)) {
    return -x;
  }
  return x;
}
function maxf(a: number, b: number): number {
  if ((a > b)) {
    return a;
  }
  return b;
}
function isClose(a: number, b: number): boolean {
  const relTol: number = 1e-09;
  const t = abs((a - b));
  const u: number = (relTol * maxf(abs(a), abs(b)));
  return (t <= u);
}
function sqrtApprox(x: number): number {
  let guess: number = x;
  let i: number = 0;
  while ((i < 10)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function main() {
  const root2: number = sqrtApprox(2.0);
  const pairs: number[][] = [[1.0000000000000002e+14, 1.0000000000000002e+14], [100.01, 100.011], [(1.0000000000000002e+13 / 10000.0), 1.0000000000000001e+09], [0.001, 0.0010000001], [1.01e-22, 0.0], [(root2 * root2), 2.0], [(-root2 * root2), -2.0], [100000000000000000.0, 100000000000000000.0], [3.141592653589793, 3.141592653589793]];
  for (const pair of pairs) {
    const a = pair[0];
    const b = pair[1];
    const s: string = (isClose(a, b) ? "≈" : "≉");
    console.log(((((String(a) + " ") + s) + " ") + String(b)));
  }
}
main();
