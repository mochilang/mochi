// Generated by Mochi v0.10.38 on 2025-07-23 16:04:19 GMT+7

function abs(x: number): number {
  if ((x < 0.0)) {
    return -x;
  }
  return x;
}
function sqrtApprox(x: number): number {
  let guess: number = x;
  let i: number = 0;
  while ((i < 20)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function agmPi(): number {
  let a: number = 1.0;
  let g: number = (1.0 / sqrtApprox(2.0));
  let sum = 0.0;
  let pow: number = 2.0;
  while ((abs((a - g)) > 1e-15)) {
    let t: number = ((a + g) / 2.0);
    let u: number = sqrtApprox((a * g));
    a = t;
    g = u;
    pow = (pow * 2.0);
    let diff: number = ((a * a) - (g * g));
    sum = (sum + (diff * pow));
  }
  let pi = (((4.0 * a) * a) / (1.0 - sum));
  return pi;
}
function main() {
  console.log(String(agmPi()));
}
main();
