// Generated by Mochi v0.10.39 on 2025-07-24 18:23:14 GMT+7

let seed: number = 1;
function rnd(): number {
  seed = (((seed * 214013) + 2531011) % 2147483648);
  return Math.trunc(seed / 65536);
}
function deal(game: number): number[] {
  seed = game;
  let deck: number[] = [];
  let i: number = 0;
  while ((i < 52)) {
    deck = [...deck, (51 - i)];
    i = (i + 1);
  }
  i = 0;
  while ((i < 51)) {
    const j: number = (51 - (rnd() % (52 - i)));
    const tmp: number = deck[i];
    deck[i] = deck[j];
    deck[j] = tmp;
    i = (i + 1);
  }
  return deck;
}
const suits: string = "CDHS";
const nums: string = "A23456789TJQK";
function show(cards: number[]) {
  let i: number = 0;
  while ((i < (Array.isArray(cards) || typeof cards === 'string' ? cards.length : Object.keys(cards ?? {}).length))) {
    const c: number = cards[i];
    stdout.write(((" " + (nums).substring(Math.trunc(c / 4), (Math.trunc(c / 4) + 1))) + (suits).substring((c % 4), ((c % 4) + 1))));
    if (((((i + 1) % 8) == 0) || ((i + 1) == (Array.isArray(cards) || typeof cards === 'string' ? cards.length : Object.keys(cards ?? {}).length)))) {
      console.log("");
    }
    i = (i + 1);
  }
}
console.log("");
console.log("Game #1");
show(deal(1));
console.log("");
console.log("Game #617");
show(deal(617));
