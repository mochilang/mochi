// Generated by Mochi v0.10.38 on 2025-07-23 19:12:17 GMT+7

function f(): any[] {
  return [0, 0.0];
}
function g(a: number, b: number): number {
  return 0;
}
function h(s: string, nums: number[]) {
}
function main() {
  const ab: any[] = f();
  const a = ab[Math.trunc(0)];
  const b = ab[Math.trunc(1)];
  const cb = f()[Math.trunc(1)];
  const d: number = g(a, cb);
  const e: number = g(d, b);
  let i: number = g(d, 2.0);
  let list: number[] = [];
  list = [...list, a];
  list = [...list, d];
  list = [...list, e];
  list = [...list, i];
  i = (Array.isArray(list) || typeof list === 'string' ? list.length : Object.keys(list ?? {}).length);
}
main();
