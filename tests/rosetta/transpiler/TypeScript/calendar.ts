// Generated by Mochi v0.10.40 on 2025-07-25 19:45:16 GMT+7

var _nowSeed = 1;
var _nowSeeded = true;
function _now(): number {
  _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
  return _nowSeed;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
{
  const _startMem = _mem()
  const _start = _now()
  const daysInMonth: number[] = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
  const start: number[] = [3, 6, 6, 2, 4, 0, 2, 5, 1, 3, 6, 1];
  const months: string[] = [" January ", " February", "  March  ", "  April  ", "   May   ", "   June  ", "   July  ", "  August ", "September", " October ", " November", " December"];
  const days: string[] = ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"];
  console.log("                                [SNOOPY]\n");
  console.log("                                  1969\n");
  let qtr: number = 0;
  while ((qtr < 4)) {
    let mi: number = 0;
    while ((mi < 3)) {
      console.log((("      " + months[Math.trunc(((qtr * 3) + mi))]) + "           "), false);
      mi = (mi + 1);
    }
    console.log("");
    mi = 0;
    while ((mi < 3)) {
      let d: number = 0;
      while ((d < 7)) {
        console.log((" " + days[d]), false);
        d = (d + 1);
      }
      console.log("     ", false);
      mi = (mi + 1);
    }
    console.log("");
    let week: number = 0;
    while ((week < 6)) {
      mi = 0;
      while ((mi < 3)) {
        let day: number = 0;
        while ((day < 7)) {
          const m: number = ((qtr * 3) + mi);
          const val: number = ((((week * 7) + day) - start[m]) + 1);
          if (((val >= 1) && (val <= daysInMonth[m]))) {
            let s: string = String(val);
            if (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) == 1)) {
              s = (" " + s);
            }
            console.log((" " + s), false);
          } else {
            console.log("   ", false);
          }
          day = (day + 1);
        }
        console.log("     ", false);
        mi = (mi + 1);
      }
      console.log("");
      week = (week + 1);
    }
    console.log("");
    qtr = (qtr + 1);
  }
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
}

