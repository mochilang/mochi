// Generated by Mochi v0.10.38 on 2025-07-23 17:27:01 GMT+7

function padLeft(s: string, w: number): string {
  let res: string = "";
  let n: number = (w - (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length));
  while ((n > 0)) {
    res = (res + " ");
    n = (n - 1);
  }
  return (res + s);
}
function padRight(s: string, w: number): string {
  let out: string = s;
  let i: number = (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length);
  while ((i < w)) {
    out = (out + " ");
    i = (i + 1);
  }
  return out;
}
function indexOf(s: string, ch: string): number {
  let i: number = 0;
  while ((i < (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    if (((s).substring(i, (i + 1)) == ch)) {
      return i;
    }
    i = (i + 1);
  }
  return -1;
}
function format2(f: number): string {
  let s: string = String(f);
  const idx: number = s.indexOf(".");
  if ((idx < 0)) {
    s = (s + ".00");
  } else {
    let need: number = (idx + 3);
    if (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) > need)) {
      s = (s).substring(0, need);
    } else {
      while (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) < need)) {
        s = (s + "0");
      }
    }
  }
  return s;
}
function cpx(h: number): number {
  let x: number = Math.trunc(((h / 11.25) + 0.5));
  x = (x % 32);
  if ((x < 0)) {
    x = (x + 32);
  }
  return x;
}
const compassPoint: string[] = ["North", "North by east", "North-northeast", "Northeast by north", "Northeast", "Northeast by east", "East-northeast", "East by north", "East", "East by south", "East-southeast", "Southeast by east", "Southeast", "Southeast by south", "South-southeast", "South by east", "South", "South by west", "South-southwest", "Southwest by south", "Southwest", "Southwest by west", "West-southwest", "West by south", "West", "West by north", "West-northwest", "Northwest by west", "Northwest", "Northwest by north", "North-northwest", "North by west"];
function degrees2compasspoint(h: number): string {
  return compassPoint[Math.trunc(cpx(h))];
}
const headings: number[] = [0.0, 16.87, 16.88, 33.75, 50.62, 50.63, 67.5, 84.37, 84.38, 101.25, 118.12, 118.13, 135.0, 151.87, 151.88, 168.75, 185.62, 185.63, 202.5, 219.37, 219.38, 236.25, 253.12, 253.13, 270.0, 286.87, 286.88, 303.75, 320.62, 320.63, 337.5, 354.37, 354.38];
console.log("Index  Compass point         Degree");
let i: number = 0;
while ((i < (Array.isArray(headings) || typeof headings === 'string' ? headings.length : Object.keys(headings ?? {}).length))) {
  const h: number = headings[Math.trunc(i)];
  const idx: number = ((i % 32) + 1);
  const cp: string = degrees2compasspoint(h);
  console.log((((((padLeft(String(idx), 4) + "   ") + padRight(cp, 19)) + " ") + format2(h)) + "°"));
  i = (i + 1);
}
