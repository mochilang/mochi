// Generated by Mochi v0.10.38 on 2025-07-23 16:20:08 GMT+7

var _nowSeed = 0;
var _nowSeeded = false;
{
  const s = typeof Deno !== "undefined" ? Deno.env.get("MOCHI_NOW_SEED") : (process.env.MOCHI_NOW_SEED || "");
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return Date.now() * 1000;
}
function qsel(a: number[], k: number): number {
  let arr: number[] = a;
  while (((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) > 1)) {
    let px: number = (_now() % (Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length));
    let pv: number = arr[px];
    const last: number = ((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) - 1);
    const tmp: number = arr[px];
    arr[px] = arr[last];
    arr[last] = tmp;
    px = 0;
    let i: number = 0;
    while ((i < last)) {
      const v: number = arr[i];
      if ((v < pv)) {
        const tmp2: number = arr[px];
        arr[px] = arr[i];
        arr[i] = tmp2;
        px = (px + 1);
      }
      i = (i + 1);
    }
    if ((px == k)) {
      return pv;
    }
    if ((k < px)) {
      arr = arr.slice(0, px);
    } else {
      const tmp2: number = arr[px];
      arr[px] = pv;
      arr[last] = tmp2;
      arr = arr.slice((px + 1));
      k = (k - (px + 1));
    }
  }
  return arr[0];
}
function median(list: number[]): number {
  let arr: number[] = list;
  const half: number = Math.trunc((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) / 2);
  const med: number = qsel(arr, half);
  if ((((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) % 2) == 0)) {
    return ((med + qsel(arr, (half - 1))) / 2.0);
  }
  return med;
}
console.log(String(median([3.0, 1.0, 4.0, 1.0])));
console.log(String(median([3.0, 1.0, 4.0, 1.0, 5.0])));
