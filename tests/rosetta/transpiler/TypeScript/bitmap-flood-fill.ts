// Generated by Mochi v0.10.40 on 2025-07-25 17:41:43 GMT+7

let grid: string[][] = [[".", ".", ".", ".", "."], [".", "#", "#", "#", "."], [".", "#", ".", "#", "."], [".", "#", "#", "#", "."], [".", ".", ".", ".", "."]];
function flood(x: number, y: number, repl: string) {
  const target: string = grid[y][x];
  if ((target == repl)) {
    return;
  }
  function ff(px: number, py: number) {
    if (((((px < 0) || (py < 0)) || (py >= (Array.isArray(grid) || typeof grid === 'string' ? grid.length : Object.keys(grid ?? {}).length))) || (px >= (Array.isArray(grid[Math.trunc(0)]) || typeof grid[Math.trunc(0)] === 'string' ? grid[Math.trunc(0)].length : Object.keys(grid[Math.trunc(0)] ?? {}).length)))) {
      return;
    }
    if ((grid[py][px] != target)) {
      return;
    }
    grid[py][px] = repl;
    ff((px - 1), py);
    ff((px + 1), py);
    ff(px, (py - 1));
    ff(px, (py + 1));
  }
  ff(x, y);
}
flood(2, 2, "o");
for (const row of grid) {
  let line: string = "";
  for (const ch of row) {
    line = (line + ch);
  }
  console.log(line);
}
