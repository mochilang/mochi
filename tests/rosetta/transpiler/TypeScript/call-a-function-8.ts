// Generated by Mochi v0.10.38 on 2025-07-23 19:12:15 GMT+7

function mapString(s: string, f): string {
  let out: string = "";
  let i: number = 0;
  while ((i < (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    out = (out + f(s.slice(i, (i + 1))));
    i = (i + 1);
  }
  return out;
}
function main() {
  const fn = (r) => ((r == " ") ? "" : r);
  mapString("Spaces removed", fn);
  mapString("Test", (r) => r.toLowerCase());
  mapString("shift", (r) => r);
}
main();
