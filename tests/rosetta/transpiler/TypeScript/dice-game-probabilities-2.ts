// Generated by Mochi v0.10.41 on 2025-07-26 17:34:27 GMT+7

var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function roll(nDice: number, nSides: number): number {
  let sum: number = 0;
  let i: number = 0;
  while ((i < nDice)) {
    sum = ((sum + (_now() % nSides)) + 1);
    i = (i + 1);
  }
  return sum;
}
function beats(n1: number, s1: number, n2: number, s2: number, trials: number): number {
  let wins: number = 0;
  let i: number = 0;
  while ((i < trials)) {
    if ((roll(n1, s1) > roll(n2, s2))) {
      wins = (wins + 1);
    }
    i = (i + 1);
  }
  return (wins / trials);
}
console.log(String(beats(9, 4, 6, 6, 1000)));
console.log(String(beats(5, 10, 7, 6, 1000)));
