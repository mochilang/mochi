// Generated by Mochi v0.10.39 on 2025-07-24 16:53:54 GMT+7

function countChange(amount: number): number {
  let ways: number[] = [];
  let i: number = 0;
  while ((i <= amount)) {
    ways = [...ways, 0];
    i = (i + 1);
  }
  ways[0] = 1;
  for (const coin of [100, 50, 25, 10, 5, 1]) {
    let j = coin;
    while ((j <= amount)) {
      ways[j] = (ways[j] + ways[Math.trunc((j - coin))]);
      j = (j + 1);
    }
  }
  return ways[amount];
}
const amount: number = (1000 * 100);
console.log(((("amount, ways to make change: " + String(amount)) + " ") + String(countChange(amount))));
