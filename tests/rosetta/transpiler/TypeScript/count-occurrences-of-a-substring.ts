// Generated by Mochi v0.10.39 on 2025-07-24 16:53:22 GMT+7

function countOccurrences(s: string, sub: string): number {
  if (((Array.isArray(sub) || typeof sub === 'string' ? sub.length : Object.keys(sub ?? {}).length) == 0)) {
    return ((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) + 1);
  }
  let cnt: number = 0;
  let i: number = 0;
  let step: number = (Array.isArray(sub) || typeof sub === 'string' ? sub.length : Object.keys(sub ?? {}).length);
  while (((i + step) <= (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    if (((s).substring(i, (i + step)) == sub)) {
      cnt = (cnt + 1);
      i = (i + step);
    } else {
      i = (i + 1);
    }
  }
  return cnt;
}
function main() {
  console.log(String(countOccurrences("the three truths", "th")));
  console.log(String(countOccurrences("ababababab", "abab")));
}
main();
