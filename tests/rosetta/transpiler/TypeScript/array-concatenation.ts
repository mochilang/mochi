// Generated by Mochi v0.10.42 on 2025-07-27 18:45:27 GMT+7

function concatInts(a: number[], b: number[]): number[] {
  let out: number[] = [];
  for (const v of a) {
    out.push(v);
  }
  for (const v of b) {
    out.push(v);
  }
  return out;
}
function concatAny(a: any[], b: any[]): any[] {
  let out: any[] = [];
  for (const v of a) {
    out.push(v);
  }
  for (const v of b) {
    out.push(v);
  }
  return out;
}
let a: number[] = [1, 2, 3];
let b: number[] = [7, 12, 60];
let i: any[] = [1, 2, 3];
let j: any[] = ["Crosby", "Stills", "Nash", "Young"];
let l: number[] = [1, 2, 3];
let m: number[] = [7, 12, 60];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  console.log(String(concatInts(a, b)));
  console.log(String(concatAny(i, j)));
  console.log(String(concatInts(l, m)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

