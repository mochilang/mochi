// Generated by Mochi v0.10.39 on 2025-07-24 18:23:27 GMT+7

function listToStringInts(xs: number[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < (Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length))) {
    s = (s + String(Math.trunc(xs[i])));
    if ((i < ((Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length) - 1))) {
      s = (s + " ");
    }
    i = (i + 1);
  }
  return (s + "]");
}
function deconv(g: number[], f: number[]): number[] {
  let h: number[] = [];
  let n: number = 0;
  const hn: number = (((Array.isArray(g) || typeof g === 'string' ? g.length : Object.keys(g ?? {}).length) - (Array.isArray(f) || typeof f === 'string' ? f.length : Object.keys(f ?? {}).length)) + 1);
  while ((n < hn)) {
    let v: number = g[n];
    let lower: number = 0;
    if ((n >= (Array.isArray(f) || typeof f === 'string' ? f.length : Object.keys(f ?? {}).length))) {
      lower = ((n - (Array.isArray(f) || typeof f === 'string' ? f.length : Object.keys(f ?? {}).length)) + 1);
    }
    let i = lower;
    while ((i < n)) {
      v = (v - (h[i] * f[Math.trunc((n - i))]));
      i = (i + 1);
    }
    v = Math.trunc(v / f[Math.trunc(0)]);
    h = [...h, v];
    n = (n + 1);
  }
  return h;
}
function main() {
  const h: number[] = [-8.0, -9.0, -3.0, -1.0, -6.0, 7.0];
  const f: number[] = [-3.0, -6.0, -1.0, 8.0, -6.0, 3.0, -1.0, -9.0, -9.0, 3.0, -2.0, 5.0, 2.0, -2.0, -7.0, -1.0];
  const g: number[] = [24.0, 75.0, 71.0, -34.0, 3.0, 22.0, -45.0, 23.0, 245.0, 25.0, 52.0, 25.0, -67.0, -96.0, 96.0, 31.0, 55.0, 36.0, 29.0, -43.0, -7.0];
  console.log(listToStringInts(h));
  console.log(listToStringInts(deconv(g, f)));
  console.log(listToStringInts(f));
  console.log(listToStringInts(deconv(g, h)));
}
main();
