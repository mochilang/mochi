// Generated by Mochi v0.10.37 on 2025-07-23 13:07:23 GMT+7

function isPrime(n: number): boolean {
  if ((n < 2)) {
    return false;
  }
  if (((n % 2) == 0)) {
    return (n == 2);
  }
  if (((n % 3) == 0)) {
    return (n == 3);
  }
  let d: number = 5;
  while (((d * d) <= n)) {
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 2);
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 4);
  }
  return true;
}
function sumDigits(n: number): number {
  let s: number = 0;
  let x: number = n;
  while ((x > 0)) {
    s = (s + (x % 10));
    x = Math.trunc(x / 10);
  }
  return s;
}
function pad(n: number): string {
  if ((n < 10)) {
    return ("  " + String(n));
  }
  if ((n < 100)) {
    return (" " + String(n));
  }
  return String(n);
}
function main() {
  console.log("Additive primes less than 500:");
  let count = 0;
  let line: string = "";
  let lineCount: number = 0;
  let i: number = 2;
  while ((i < 500)) {
    if ((isPrime(i) && isPrime(sumDigits(i)))) {
      count = (count + 1);
      line = ((line + pad(i)) + "  ");
      lineCount = (lineCount + 1);
      if ((lineCount == 10)) {
        console.log((line).substring(0, ((Array.isArray(line) || typeof line === 'string' ? line.length : Object.keys(line ?? {}).length) - 2)));
        line = "";
        lineCount = 0;
      }
    }
    if ((i > 2)) {
      i = (i + 2);
    } else {
      i = (i + 1);
    }
  }
  if ((lineCount > 0)) {
    console.log((line).substring(0, ((Array.isArray(line) || typeof line === 'string' ? line.length : Object.keys(line ?? {}).length) - 2)));
  }
  console.log((String(count) + " additive primes found."));
}
main();
