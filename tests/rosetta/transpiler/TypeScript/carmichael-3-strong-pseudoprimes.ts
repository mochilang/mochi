// Generated by Mochi v0.10.38 on 2025-07-23 19:58:52 GMT+7

function mod(n: number, m: number): number {
  return (((n % m) + m) % m);
}
function isPrime(n: number): boolean {
  if ((n < 2)) {
    return false;
  }
  if (((n % 2) == 0)) {
    return (n == 2);
  }
  if (((n % 3) == 0)) {
    return (n == 3);
  }
  let d: number = 5;
  while (((d * d) <= n)) {
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 2);
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 4);
  }
  return true;
}
function pad(n: number, width: number): string {
  let s: string = String(n);
  while (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) < width)) {
    s = (" " + s);
  }
  return s;
}
function carmichael(p1: number) {
  for (let h3 = 2; h3 < p1; h3++) {
    for (let d = 1; d < (h3 + p1); d++) {
      if ((((((h3 + p1) * (p1 - 1)) % d) == 0) && (mod((-p1 * p1), h3) == (d % h3)))) {
        const p2 = (1 + (((p1 - 1) * (h3 + p1)) / d));
        if (!isPrime(p2)) {
          continue
        }
        const p3 = (1 + ((p1 * p2) / h3));
        if (!isPrime(p3)) {
          continue
        }
        if ((((p2 * p3) % (p1 - 1)) != 1)) {
          continue
        }
        const c = ((p1 * p2) * p3);
        console.log(((((((pad(p1, 2) + "   ") + pad(p2, 4)) + "   ") + pad(p3, 5)) + "     ") + String(c)));
      }
    }
  }
}
console.log("The following are Carmichael munbers for p1 <= 61:\n");
console.log("p1     p2      p3     product");
console.log("==     ==      ==     =======");
for (let p1 = 2; p1 < 62; p1++) {
  if (isPrime(p1)) {
    carmichael(p1);
  }
}
