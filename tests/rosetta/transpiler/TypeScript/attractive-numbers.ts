// Generated by Mochi v0.10.38 on 2025-07-23 16:17:45 GMT+7

function isPrime(n: number): boolean {
  if ((n < 2)) {
    return false;
  }
  if (((n % 2) == 0)) {
    return (n == 2);
  }
  if (((n % 3) == 0)) {
    return (n == 3);
  }
  let d: number = 5;
  while (((d * d) <= n)) {
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 2);
    if (((n % d) == 0)) {
      return false;
    }
    d = (d + 4);
  }
  return true;
}
function countPrimeFactors(n: number): number {
  if ((n == 1)) {
    return 0;
  }
  if (isPrime(n)) {
    return 1;
  }
  let count = 0;
  let f: number = 2;
  while (true) {
    if (((n % f) == 0)) {
      count = (count + 1);
      n = Math.trunc(n / f);
      if ((n == 1)) {
        return count;
      }
      if (isPrime(n)) {
        f = n;
      }
    } else {
      if ((f >= 3)) {
        f = (f + 2);
      } else {
        f = 3;
      }
    }
  }
  return count;
}
function pad4(n: number): string {
  let s: string = String(n);
  while (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) < 4)) {
    s = (" " + s);
  }
  return s;
}
function main() {
  const max = 120;
  console.log((("The attractive numbers up to and including " + String(max)) + " are:"));
  let count = 0;
  let line: string = "";
  let lineCount: number = 0;
  let i: number = 1;
  while ((i <= max)) {
    const c: number = countPrimeFactors(i);
    if (isPrime(c)) {
      line = (line + pad4(i));
      count = (count + 1);
      lineCount = (lineCount + 1);
      if ((lineCount == 20)) {
        console.log(line);
        line = "";
        lineCount = 0;
      }
    }
    i = (i + 1);
  }
  if ((lineCount > 0)) {
    console.log(line);
  }
}
main();
