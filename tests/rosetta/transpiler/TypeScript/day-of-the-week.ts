// Generated by Mochi v0.10.39 on 2025-07-24 18:23:11 GMT+7

function weekday(y: number, m: number, d: number): number {
  let yy: number = y;
  let mm: number = m;
  if ((mm < 3)) {
    mm = (mm + 12);
    yy = (yy - 1);
  }
  const k: number = (yy % 100);
  const j: number = Math.trunc(Math.trunc(yy / 100));
  const a: number = Math.trunc(Math.trunc((13 * (mm + 1)) / 5));
  const b: number = Math.trunc(Math.trunc(k / 4));
  const c: number = Math.trunc(Math.trunc(j / 4));
  return ((((((d + a) + k) + b) + c) + (5 * j)) % 7);
}
for (let year = 2008; year < 2122; year++) {
  if ((weekday(year, 12, 25) == 1)) {
    console.log((("25 December " + String(year)) + " is Sunday"));
  }
}
