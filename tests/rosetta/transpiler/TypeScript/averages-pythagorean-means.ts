// Generated by Mochi v0.10.40 on 2025-07-25 17:41:06 GMT+7

function powf(base: number, exp: number): number {
  let result: number = 1.0;
  let i: number = 0;
  while ((i < exp)) {
    result = (result * base);
    i = (i + 1);
  }
  return result;
}
function nthRoot(x: number, n: number): number {
  let low: number = 0.0;
  let high: number = x;
  let i: number = 0;
  while ((i < 60)) {
    const mid: number = ((low + high) / 2.0);
    if ((powf(mid, n) > x)) {
      high = mid;
    } else {
      low = mid;
    }
    i = (i + 1);
  }
  return low;
}
function main() {
  let sum: number = 0.0;
  let sumRecip: number = 0.0;
  let prod: number = 1.0;
  let n: number = 1;
  while ((n <= 10)) {
    const f: number = n;
    sum = (sum + f);
    sumRecip = (sumRecip + (1.0 / f));
    prod = (prod * f);
    n = (n + 1);
  }
  const count = 10.0;
  const a = (sum / count);
  const g: number = nthRoot(prod, 10);
  const h = (count / sumRecip);
  console.log(((((("A: " + String(a)) + " G: ") + String(g)) + " H: ") + String(h)));
  console.log(("A >= G >= H: " + String(((a >= g) && (g >= h)))));
}
main();
