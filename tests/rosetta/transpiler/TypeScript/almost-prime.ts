// Generated by Mochi v0.10.37 on 2025-07-23 12:50:54 GMT+7

function kPrime(n: number, k: number): boolean {
  let nf: number = 0;
  let i: number = 2;
  while ((i <= n)) {
    while (((n % i) == 0)) {
      if ((nf == k)) {
        return false;
      }
      nf = (nf + 1);
      n = Math.trunc(n / i);
    }
    i = (i + 1);
  }
  return (nf == k);
}
function gen(k: number, count: number): number[] {
  let r: number[] = [];
  let n: number = 2;
  while (((Array.isArray(r) || typeof r === 'string' ? r.length : Object.keys(r ?? {}).length) < count)) {
    if (kPrime(n, k)) {
      r = [...r, n];
    }
    n = (n + 1);
  }
  return r;
}
function main() {
  let k: number = 1;
  while ((k <= 5)) {
    console.log(((String(k) + " ") + String(gen(k, 10))));
    k = (k + 1);
  }
}
main();
