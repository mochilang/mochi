// Generated by Mochi v0.10.37 on 2025-07-23 15:52:12 GMT+7

function countDivisors(n: number): number {
  if ((n < 2)) {
    return 1;
  }
  let count = 2;
  let i: number = 2;
  while ((i <= Math.trunc(n / 2))) {
    if (((n % i) == 0)) {
      count = (count + 1);
    }
    i = (i + 1);
  }
  return count;
}
function main() {
  console.log("The first 20 anti-primes are:");
  let maxDiv: number = 0;
  let count = 0;
  let n: number = 1;
  let line: string = "";
  while ((count < 20)) {
    const d: number = countDivisors(n);
    if ((d > maxDiv)) {
      line = ((line + String(n)) + " ");
      maxDiv = d;
      count = (count + 1);
    }
    n = (n + 1);
  }
  line = (line).substring(0, ((Array.isArray(line) || typeof line === 'string' ? line.length : Object.keys(line ?? {}).length) - 1));
  console.log(line);
}
main();
