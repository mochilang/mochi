// Generated by Mochi v0.10.39 on 2025-07-24 18:23:08 GMT+7

var _nowSeed = 0;
var _nowSeeded = false;
{
  const s = typeof Deno !== "undefined" ? Deno.env.get("MOCHI_NOW_SEED") : (process.env.MOCHI_NOW_SEED || "");
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return Date.now() * 1000;
}
function pad2(n: number): string {
  if ((n < 10)) {
    return ("0" + String(n));
  }
  return String(n);
}
function weekdayName(z: number): string {
  const names: string[] = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
  return names[Math.trunc(((z + 4) % 7))];
}
function main() {
  const ts: number = Math.trunc(Math.trunc(_now() / 1000000000));
  let days: number = Math.trunc(Math.trunc(ts / 86400));
  let z: number = (days + 719468);
  let era: number = Math.trunc(Math.trunc(z / 146097));
  let doe: number = (z - (era * 146097));
  let yoe: number = Math.trunc((((doe - Math.trunc(doe / 1460)) + Math.trunc(doe / 36524)) - Math.trunc(doe / 146096)) / Math.trunc(365));
  let y: number = (yoe + (era * 400));
  let doy: number = (doe - (((365 * yoe) + Math.trunc(yoe / 4)) - Math.trunc(yoe / 100)));
  let mp: number = Math.trunc(((5 * doy) + 2) / Math.trunc(153));
  let d: number = Math.trunc(((doy - Math.trunc(((153 * mp) + 2) / Math.trunc(5))) + 1));
  let m: number = Math.trunc((mp + 3));
  if ((m > 12)) {
    y = (y + 1);
    m = (m - 12);
  }
  const iso: string = ((((String(y) + "-") + pad2(m)) + "-") + pad2(d));
  console.log(iso);
  const months: string[] = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
  const line: string = ((((((weekdayName(days) + ", ") + months[Math.trunc((m - 1))]) + " ") + String(d)) + ", ") + String(y));
  console.log(line);
}
main();
