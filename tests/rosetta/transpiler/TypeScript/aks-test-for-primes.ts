// Generated by Mochi v0.10.37 on 2025-07-23 12:50:48 GMT+7

function poly(p: number): string {
  let s: string = "";
  let coef: number = 1;
  let i: number = p;
  if ((coef != 1)) {
    s = (s + String(coef));
  }
  while ((i > 0)) {
    s = (s + "x");
    if ((i != 1)) {
      s = ((s + "^") + String(i));
    }
    coef = ((coef * i) / ((p - i) + 1));
    let d: number = coef;
    if ((((p - (i - 1)) % 2) == 1)) {
      d = -d;
    }
    if ((d < 0)) {
      s = ((s + " - ") + String(-d));
    } else {
      s = ((s + " + ") + String(d));
    }
    i = (i - 1);
  }
  if ((s == "")) {
    s = "1";
  }
  return s;
}
function aks(n: number): boolean {
  if ((n < 2)) {
    return false;
  }
  let c: number = n;
  let i: number = 1;
  while ((i < n)) {
    if (((c % n) != 0)) {
      return false;
    }
    c = ((c * (n - i)) / (i + 1));
    i = (i + 1);
  }
  return true;
}
function main() {
  let p: number = 0;
  while ((p <= 7)) {
    console.log(((String(p) + ":  ") + poly(p)));
    p = (p + 1);
  }
  let first: boolean = true;
  p = 2;
  let line: string = "";
  while ((p < 50)) {
    if (aks(p)) {
      if (first) {
        line = (line + String(p));
        first = false;
      } else {
        line = ((line + " ") + String(p));
      }
    }
    p = (p + 1);
  }
  console.log(line);
}
main();
