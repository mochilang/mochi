// Generated by Mochi v0.10.40 on 2025-07-25 17:41:05 GMT+7

let arr1: number[] = [2, 7, 1, 8, 2];
let counts1: Record<number, number> = {};
let keys1: number[] = [];
let i: number = 0;
while ((i < (Array.isArray(arr1) || typeof arr1 === 'string' ? arr1.length : Object.keys(arr1 ?? {}).length))) {
  const v: number = arr1[i];
  if ((v in counts1)) {
    counts1[v] = (counts1[v] + 1);
  } else {
    counts1[v] = 1;
    keys1 = [...keys1, v];
  }
  i = (i + 1);
}
let max1: number = 0;
i = 0;
while ((i < (Array.isArray(keys1) || typeof keys1 === 'string' ? keys1.length : Object.keys(keys1 ?? {}).length))) {
  const k: number = keys1[i];
  const c: number = counts1[k];
  if ((c > max1)) {
    max1 = c;
  }
  i = (i + 1);
}
let modes1: number[] = [];
i = 0;
while ((i < (Array.isArray(keys1) || typeof keys1 === 'string' ? keys1.length : Object.keys(keys1 ?? {}).length))) {
  const k: number = keys1[i];
  if ((counts1[k] == max1)) {
    modes1 = [...modes1, k];
  }
  i = (i + 1);
}
console.log(String(modes1));
let arr2: number[] = [2, 7, 1, 8, 2, 8];
let counts2: Record<number, number> = {};
let keys2: number[] = [];
i = 0;
while ((i < (Array.isArray(arr2) || typeof arr2 === 'string' ? arr2.length : Object.keys(arr2 ?? {}).length))) {
  const v: number = arr2[i];
  if ((v in counts2)) {
    counts2[v] = (counts2[v] + 1);
  } else {
    counts2[v] = 1;
    keys2 = [...keys2, v];
  }
  i = (i + 1);
}
let max2: number = 0;
i = 0;
while ((i < (Array.isArray(keys2) || typeof keys2 === 'string' ? keys2.length : Object.keys(keys2 ?? {}).length))) {
  const k: number = keys2[i];
  const c: number = counts2[k];
  if ((c > max2)) {
    max2 = c;
  }
  i = (i + 1);
}
let modes2: number[] = [];
i = 0;
while ((i < (Array.isArray(keys2) || typeof keys2 === 'string' ? keys2.length : Object.keys(keys2 ?? {}).length))) {
  const k: number = keys2[i];
  if ((counts2[k] == max2)) {
    modes2 = [...modes2, k];
  }
  i = (i + 1);
}
console.log(String(modes2));
