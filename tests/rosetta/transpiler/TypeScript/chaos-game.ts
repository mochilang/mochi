// Generated by Mochi v0.10.38 on 2025-07-23 20:04:19 GMT+7

const width: number = 60;
const height: number = Math.trunc((width * 0.86602540378));
const iterations: number = 5000;
let grid: string[][] = [];
let y: number = 0;
while ((y < height)) {
  let line: string[] = [];
  let x: number = 0;
  while ((x < width)) {
    line = [...line, " "];
    x = (x + 1);
  }
  grid = [...grid, line];
  y = (y + 1);
}
function randInt(s: number, n: number): number[] {
  const next: number = (((s * 1664525) + 1013904223) % 2147483647);
  return [next, (next % n)];
}
let seed: number = 1;
const vertices: number[][] = [[0, (height - 1)], [(width - 1), (height - 1)], [Math.trunc(Math.trunc(width / 2)), 0]];
let px: number = Math.trunc(Math.trunc(width / 2));
let py: number = Math.trunc(Math.trunc(height / 2));
let i: number = 0;
while ((i < iterations)) {
  let r: number[] = randInt(seed, 3);
  seed = r[Math.trunc(0)];
  const idx: number = Math.trunc(r[Math.trunc(1)]);
  const v: number[] = vertices[idx];
  px = Math.trunc(((px + v[Math.trunc(0)]) / 2));
  py = Math.trunc(((py + v[Math.trunc(1)]) / 2));
  if (((((px >= 0) && (px < width)) && (py >= 0)) && (py < height))) {
    grid[py][px] = "*";
  }
  i = (i + 1);
}
y = 0;
while ((y < height)) {
  let line: string = "";
  let x: number = 0;
  while ((x < width)) {
    line = (line + grid[y][x]);
    x = (x + 1);
  }
  console.log(line);
  y = (y + 1);
}
