// Generated by Mochi v0.10.39 on 2025-07-24 18:25:01 GMT+7

const c: string = ((((("Character,Speech\n" + "The multitude,The messiah! Show us the messiah!\n") + "Brians mother,<angry>Now you listen here! He's not the messiah; he's a very naughty boy! Now go away!</angry>\n") + "The multitude,Who are you?\n") + "Brians mother,I'm his mother; that's who!\n") + "The multitude,Behold his mother! Behold his mother!");
let rows: string[][] = [];
for (const line of c.split("\n")) {
  rows = [...rows, line.split(",")];
}
const headings: boolean = true;
console.log("<table>");
if (headings) {
  if (((Array.isArray(rows) || typeof rows === 'string' ? rows.length : Object.keys(rows ?? {}).length) > 0)) {
    let th: string = "";
    for (const h of rows[Math.trunc(0)]) {
      th = (((th + "<th>") + h) + "</th>");
    }
    console.log("   <thead>");
    console.log((("      <tr>" + th) + "</tr>"));
    console.log("   </thead>");
    console.log("   <tbody>");
    let i: number = 1;
    while ((i < (Array.isArray(rows) || typeof rows === 'string' ? rows.length : Object.keys(rows ?? {}).length))) {
      let cells: string = "";
      for (const cell of rows[i]) {
        cells = (((cells + "<td>") + cell) + "</td>");
      }
      console.log((("      <tr>" + cells) + "</tr>"));
      i = (i + 1);
    }
    console.log("   </tbody>");
  }
} else {
  for (const row of rows) {
    let cells: string = "";
    for (const cell of row) {
      cells = (((cells + "<td>") + cell) + "</td>");
    }
    console.log((("    <tr>" + cells) + "</tr>"));
  }
}
console.log("</table>");
