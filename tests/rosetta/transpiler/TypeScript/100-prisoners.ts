// Generated by Mochi v0.10.37 on 2025-07-22 21:04:24 GMT+7

var _nowSeed = 0;
var _nowSeeded = false;
{
  const s = typeof Deno !== "undefined" ? Deno.env.get("MOCHI_NOW_SEED") : (process.env.MOCHI_NOW_SEED || "");
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return Date.now() * 1000;
}
function shuffle(xs: number[]): number[] {
  let arr = xs;
  let i = 99;
  while ((i > 0)) {
    const j = (_now() % (i + 1));
    const tmp = arr[i];
    arr[i] = arr[j];
    arr[j] = tmp;
    i = (i - 1);
  }
  return arr;
}
function doTrials(trials: number, np: number, strategy: string) {
  let pardoned = 0;
  let t = 0;
  while ((t < trials)) {
    let drawers = [];
    let i = 0;
    while ((i < 100)) {
      drawers = [...drawers, i];
      i = (i + 1);
    }
    drawers = shuffle(drawers);
    let p = 0;
    let success = true;
    while ((p < np)) {
      let found = false;
      if ((strategy == "optimal")) {
        let prev = p;
        let d = 0;
        while ((d < 50)) {
          const _this = drawers[prev];
          if ((_this == p)) {
            found = true;
            break
          }
          prev = _this;
          d = (d + 1);
        }
      } else {
        let opened = [];
        let k = 0;
        while ((k < 100)) {
          opened = [...opened, false];
          k = (k + 1);
        }
        let d = 0;
        while ((d < 50)) {
          let n = (_now() % 100);
          while (opened[n]) {
            n = (_now() % 100);
          }
          opened[n] = true;
          if ((drawers[n] == p)) {
            found = true;
            break
          }
          d = (d + 1);
        }
      }
      if (!found) {
        success = false;
        break
      }
      p = (p + 1);
    }
    if (success) {
      pardoned = (pardoned + 1);
    }
    t = (t + 1);
  }
  const rf = ((pardoned / trials) * 100.0);
  console.log((((((("  strategy = " + strategy) + "  pardoned = ") + String(pardoned)) + " relative frequency = ") + String(rf)) + "%"));
}
function main() {
  const trials = 1000;
  for (const np of [10, 100]) {
    console.log((((("Results from " + String(trials)) + " trials with ") + String(np)) + " prisoners:\n"));
    for (const strat of ["random", "optimal"]) {
      doTrials(trials, np, strat);
    }
  }
}
main();
