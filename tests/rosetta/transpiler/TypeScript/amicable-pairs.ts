// Generated by Mochi v0.10.37 on 2025-07-23 15:51:39 GMT+7

function pfacSum(i: number): number {
  let sum = 0;
  let p: number = 1;
  while ((p <= Math.trunc(i / 2))) {
    if (((i % p) == 0)) {
      sum = (sum + p);
    }
    p = (p + 1);
  }
  return sum;
}
function pad(n: number, width: number): string {
  let s: string = String(n);
  while (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) < width)) {
    s = (" " + s);
  }
  return s;
}
function main() {
  let sums: number[] = [];
  let i: number = 0;
  while ((i < 20000)) {
    sums = [...sums, 0];
    i = (i + 1);
  }
  i = 1;
  while ((i < 20000)) {
    sums[i] = pfacSum(i);
    i = (i + 1);
  }
  console.log("The amicable pairs below 20,000 are:");
  let n: number = 2;
  while ((n < 19999)) {
    const m: number = sums[n];
    if ((((m > n) && (m < 20000)) && (n == sums[m]))) {
      console.log(((("  " + pad(n, 5)) + " and ") + pad(m, 5)));
    }
    n = (n + 1);
  }
}
main();
