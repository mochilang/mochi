// Generated by Mochi v0.10.38 on 2025-07-23 20:04:22 GMT+7

function removeName(names: string[], name: string): string[] {
  let out: string[] = [];
  for (const n of names) {
    if ((n != name)) {
      out = [...out, n];
    }
  }
  return out;
}
function main() {
  let clients: string[] = [];
  function broadcast(msg: string) {
    console.log(msg);
  }
  function add(name: string) {
    clients = [...clients, name];
    broadcast((("+++ \"" + name) + "\" connected +++\n"));
  }
  function send(name: string, msg: string) {
    broadcast((((name + "> ") + msg) + "\n"));
  }
  function remove(name: string) {
    clients = removeName(clients, name);
    broadcast((("--- \"" + name) + "\" disconnected ---\n"));
  }
  add("Alice");
  add("Bob");
  send("Alice", "Hello Bob!");
  send("Bob", "Hi Alice!");
  remove("Bob");
  remove("Alice");
  broadcast("Server stopping!\n");
}
main();
