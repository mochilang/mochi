// Generated by Mochi v0.10.39 on 2025-07-24 17:00:16 GMT+7

function createFile(fs: Record<string, boolean>, fn: string) {
  if ((fn in fs)) {
    console.log((("open " + fn) + ": file exists"));
  } else {
    fs[fn] = false;
    console.log((("file " + fn) + " created!"));
  }
}
function createDir(fs: Record<string, boolean>, dn: string) {
  if ((dn in fs)) {
    console.log((("mkdir " + dn) + ": file exists"));
  } else {
    fs[dn] = true;
    console.log((("directory " + dn) + " created!"));
  }
}
function main() {
  let fs: Record<string, boolean> = {};
  fs.docs = true;
  createFile(fs, "input.txt");
  createFile(fs, "/input.txt");
  createDir(fs, "docs");
  createDir(fs, "/docs");
}
main();
