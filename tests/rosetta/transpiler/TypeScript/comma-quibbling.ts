// Generated by Mochi v0.10.39 on 2025-07-24 15:22:21 GMT+7

function quibble(items: string[]): string {
  const n: number = (Array.isArray(items) || typeof items === 'string' ? items.length : Object.keys(items ?? {}).length);
  if ((n == 0)) {
    return "{}";
  } else {
    if ((n == 1)) {
      return (("{" + items[Math.trunc(0)]) + "}");
    } else {
      if ((n == 2)) {
        return (((("{" + items[Math.trunc(0)]) + " and ") + items[Math.trunc(1)]) + "}");
      } else {
        let prefix: string = "";
        for (let i = 0; i < (n - 1); i++) {
          if ((i == (n - 1))) {
            break
          }
          if ((i > 0)) {
            prefix = (prefix + ", ");
          }
          prefix = (prefix + items[i]);
        }
        return (((("{" + prefix) + " and ") + items[Math.trunc((n - 1))]) + "}");
      }
    }
  }
}
function main() {
  console.log(quibble([]));
  console.log(quibble(["ABC"]));
  console.log(quibble(["ABC", "DEF"]));
  console.log(quibble(["ABC", "DEF", "G", "H"]));
}
main();
