// Generated by Mochi v0.10.40 on 2025-07-25 17:41:02 GMT+7

function sel(list: number[], k: number): number {
  let i: number = 0;
  while ((i <= k)) {
    let minIndex: number = i;
    let j: number = (i + 1);
    while ((j < (Array.isArray(list) || typeof list === 'string' ? list.length : Object.keys(list ?? {}).length))) {
      if ((list[j] < list[minIndex])) {
        minIndex = j;
      }
      j = (j + 1);
    }
    const tmp: number = list[i];
    list[i] = list[minIndex];
    list[minIndex] = tmp;
    i = (i + 1);
  }
  return list[k];
}
function median(a: number[]): number {
  let arr: number[] = a;
  const half: number = Math.trunc(Math.trunc((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) / 2));
  const med: number = sel(arr, half);
  if ((((Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length) % 2) == 0)) {
    return ((med + arr[Math.trunc((half - 1))]) / 2.0);
  }
  return med;
}
console.log(String(median([3.0, 1.0, 4.0, 1.0])));
console.log(String(median([3.0, 1.0, 4.0, 1.0, 5.0])));
