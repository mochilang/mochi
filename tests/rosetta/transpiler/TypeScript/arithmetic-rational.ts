// Generated by Mochi v0.10.39 on 2025-07-25 00:21:23 GMT+7

function intSqrt(x: number): number {
  if ((x < 2)) {
    return x;
  }
  let left: number = 1;
  let right: number = Math.trunc(x / 2);
  let ans: number = 0;
  while ((left <= right)) {
    const mid: number = (left + Math.trunc((right - left) / 2));
    const sq: number = (mid * mid);
    if ((sq == x)) {
      return mid;
    }
    if ((sq < x)) {
      left = (mid + 1);
      ans = mid;
    } else {
      right = (mid - 1);
    }
  }
  return ans;
}
function sumRecip(n: number): number {
  let s: number = 1;
  const limit: number = intSqrt(n);
  let f: number = 2;
  while ((f <= limit)) {
    if (((n % f) == 0)) {
      s = (s + Math.trunc(n / f));
      const f2: number = Math.trunc(n / f);
      if ((f2 != f)) {
        s = (s + f);
      }
    }
    f = (f + 1);
  }
  return s;
}
function main() {
  const nums: number[] = [6, 28, 120, 496, 672, 8128, 30240, 32760, 523776];
  for (const n of nums) {
    const s = sumRecip(n);
    if (((s % n) == 0)) {
      const val = Math.trunc(s / n);
      let perfect: string = "";
      if ((val == 1)) {
        perfect = "perfect!";
      }
      console.log(((((("Sum of recipr. factors of " + String(n)) + " = ") + String(val)) + " exactly ") + perfect));
    }
  }
}
main();
