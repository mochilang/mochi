// Generated by Mochi v0.10.37 on 2025-07-23 15:52:31 GMT+7

const PI: number = 3.141592653589793;
function sinApprox(x: number): number {
  let term: number = x;
  let sum = x;
  let n: number = 1;
  while ((n <= 10)) {
    const denom = ((2 * n) * ((2 * n) + 1));
    term = (((-term * x) * x) / denom);
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}
function cosApprox(x: number): number {
  let term: number = 1.0;
  let sum = 1.0;
  let n: number = 1;
  while ((n <= 10)) {
    const denom = (((2 * n) - 1) * (2 * n));
    term = (((-term * x) * x) / denom);
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}
const degreesIncr: number = ((0.1 * PI) / 180.0);
const turns: number = 2.0;
const stop: number = (((360.0 * turns) * 10.0) * degreesIncr);
const width: number = 600.0;
const centre: number = (width / 2.0);
const a: number = 1.0;
const b: number = 20.0;
let theta: number = 0.0;
let count: number = 0;
while ((theta < stop)) {
  const r: number = (a + (b * theta));
  const x: number = (r * cosApprox(theta));
  const y: number = (r * sinApprox(theta));
  if (((count % 100) == 0)) {
    console.log(((String((centre + x)) + ",") + String((centre - y))));
  }
  theta = (theta + degreesIncr);
  count = (count + 1);
}
