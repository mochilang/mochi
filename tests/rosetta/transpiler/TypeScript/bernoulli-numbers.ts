// Generated by Mochi v0.10.38 on 2025-07-23 17:07:52 GMT+7

function num(x: number): number { return x; }
function denom(_x: number): number { return 1; }
function bernoulli(n: number): number {
  let a: number[] = [];
  let m: number = 0;
  while ((m <= n)) {
    a = [...a, (1 / (m + 1))];
    let j: number = m;
    while ((j >= 1)) {
      a[(j - 1)] = (j * (a[(j - 1)] - a[j]));
      j = (j - 1);
    }
    m = (m + 1);
  }
  return a[0];
}
for (let i = 0; i < 61; i++) {
  const b = bernoulli(i);
  if ((num(b) != 0)) {
    const numStr = String(num(b));
    const denStr = String(denom(b));
    console.log(((((("B(" + String(i).padStart(2, " ")) + ") =") + numStr.padStart(45, " ")) + "/") + denStr));
  }
}
