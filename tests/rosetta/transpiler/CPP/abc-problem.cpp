// Generated by Mochi transpiler v0.10.39 on 2025-07-24 15:20:57 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <optional>



std::vector<std::string> fields(std::string s);
bool canSpell(std::string word, std::vector<std::string> blks);
auto newSpeller(std::string blocks);
int main();

std::vector<std::string> fields(std::string s) {
    std::vector<std::string> res = {};
    std::string cur = std::string("");
    int i = 0;
    while ((i < s.size())) {
        std::string c = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(i, (i + 1) - i); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+i, c.begin()+(i + 1)); })(s);
        if ((c == std::string(" "))) {
            if ((cur.size() > 0)) {
                res = ([&]{ auto __tmp = res; __tmp.push_back(cur); return __tmp; }());
                cur = std::string("");
            }
        } else {
            cur = (cur + c);
        }
        i = (i + 1);
    }
    if ((cur.size() > 0)) {
        res = ([&]{ auto __tmp = res; __tmp.push_back(cur); return __tmp; }());
    }
    return res;
}

bool canSpell(std::string word, std::vector<std::string> blks) {
    if ((word.size() == 0)) {
        return true;
    }
    std::string c = ([&]{ std::string __s = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(0, 1 - 0); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+0, c.begin()+1); })(word); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }());
    int i = 0;
    while ((i < blks.size())) {
        std::string b = blks[i];
        if (((c == ([&]{ std::string __s = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(0, 1 - 0); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+0, c.begin()+1); })(b); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }())) || (c == ([&]{ std::string __s = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(1, 2 - 1); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+1, c.begin()+2); })(b); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }())))) {
            std::vector<std::string> rest = {};
            int j = 0;
            while ((j < blks.size())) {
                if ((j != i)) {
                    rest = ([&]{ auto __tmp = rest; __tmp.push_back(blks[j]); return __tmp; }());
                }
                j = (j + 1);
            }
            if (canSpell(([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(1, word.size() - 1); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+1, c.begin()+word.size()); })(word), rest)) {
                return true;
            }
        }
        i = (i + 1);
    }
    return false;
}

auto newSpeller(std::string blocks) {
    std::vector<std::string> bl = fields(blocks);
    return [&](std::string w) { return canSpell((std::string)(w), bl); };
}

int main() {
    auto sp = newSpeller(std::string("BO XK DQ CP NA GT RE TG QD FS JW HU VI AN OB ER FS LY PC ZM"));
    for (std::string word : std::vector{std::string("A"), std::string("BARK"), std::string("BOOK"), std::string("TREAT"), std::string("COMMON"), std::string("SQUAD"), std::string("CONFUSE")}) {
        std::cout << ((word + std::string(" ")) + ([&]{ std::ostringstream ss; ss<<sp(word); return ss.str(); }()));
        std::cout << std::endl;
    }
    return 0;
}
