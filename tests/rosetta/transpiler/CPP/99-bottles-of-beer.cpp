// Generated by Mochi transpiler v0.10.39 on 2025-07-24 16:22:00 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <optional>
#include <vector>



std::string bottles(int n);
int main();

std::string bottles(int n) {
    if ((n == 0)) {
        return std::string("No more bottles");
    }
    if ((n == 1)) {
        return std::string("1 bottle");
    }
    return (([&]{ std::ostringstream ss; ss<<n; return ss.str(); }()) + std::string(" bottles"));
}

int main() {
    int i = 99;
    while ((i > 0)) {
        std::cout << (bottles(i) + std::string(" of beer on the wall"));
        std::cout << std::endl;
        std::cout << (bottles(i) + std::string(" of beer"));
        std::cout << std::endl;
        std::cout << std::string("Take one down, pass it around");
        std::cout << std::endl;
        std::cout << (bottles((i - 1)) + std::string(" of beer on the wall"));
        std::cout << std::endl;
        i = (i - 1);
    }
    return 0;
}
