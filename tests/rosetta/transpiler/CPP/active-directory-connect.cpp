// Generated by Mochi transpiler v0.10.39 on 2025-07-24 18:49:42 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <optional>


template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; os << vec[i]; }
    os << "]";
    return os;
}



struct LDAPClient {
    std::string Base;
    std::string Host;
    int Port;
    bool UseSSL;
    std::string BindDN;
    std::string BindPassword;
    std::string UserFilter;
    std::string GroupFilter;
    std::vector<std::string> Attributes;
    auto operator<=>(const LDAPClient&) const = default;
};

std::ostream& operator<<(std::ostream& os, const LDAPClient& v) {
    os << '{' << "'Base': "<< "'" << v.Base << "'"
 << ", " << "'Host': "<< "'" << v.Host << "'"
 << ", " << "'Port': "<< v.Port
 << ", " << "'UseSSL': "<< v.UseSSL
 << ", " << "'BindDN': "<< "'" << v.BindDN << "'"
 << ", " << "'BindPassword': "<< "'" << v.BindPassword << "'"
 << ", " << "'UserFilter': "<< "'" << v.UserFilter << "'"
 << ", " << "'GroupFilter': "<< "'" << v.GroupFilter << "'"
 << ", " << "'Attributes': "<< "["; for(size_t i=0;i<v.Attributes.size();++i){ if(i>0) os << ", "; os << v.Attributes[i]; } os << "]"
 << '}';
    return os;
}

bool connect(LDAPClient client);
int main();

bool connect(LDAPClient client) {
    return ((client.Host != std::string("")) && (client.Port > 0));
}

int main() {
    LDAPClient client = LDAPClient{std::string("dc=example,dc=com"), std::string("ldap.example.com"), 389, false, std::string("uid=readonlyuser,ou=People,dc=example,dc=com"), std::string("readonlypassword"), std::string("(uid=%s)"), std::string("(memberUid=%s)"), std::vector{std::string("givenName"), std::string("sn"), std::string("mail"), std::string("uid")}};
    if (connect(client)) {
        std::cout << (std::string("Connected to ") + client.Host);
        std::cout << std::endl;
    } else {
        std::cout << std::string("Failed to connect");
        std::cout << std::endl;
    }
    return 0;
}
