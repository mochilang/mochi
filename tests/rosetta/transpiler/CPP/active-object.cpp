// Generated by Mochi transpiler v0.10.39 on 2025-07-24 18:53:04 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>



double sinApprox(double x);
int main();

double PI = 3.141592653589793;
double dt = 0.01;
double s = 0.0;
double t1 = 0.0;
double k1 = sinApprox(0.0);
int i = 1;
int i2 = 1;

double sinApprox(double x) {
    double term = x;
    double sum = x;
    int n = 1;
    while ((n <= 12)) {
        double denom = (double)(((2 * n) * ((2 * n) + 1)));
        term = ((double)(((-term * x) * x)) / (denom));
        sum = (sum + term);
        n = (n + 1);
    }
    return sum;
}

int main() {
    while ((i <= 200)) {
        double t2 = ((double)(i) * dt);
        double k2 = sinApprox((t2 * PI));
        s = (s + (((k1 + k2) * 0.5) * (t2 - t1)));
        t1 = t2;
        k1 = k2;
        i = (i + 1);
    }
    while ((i2 <= 50)) {
        double t2 = (2.0 + ((double)(i2) * dt));
        double k2 = 0.0;
        s = (s + (((k1 + k2) * 0.5) * (t2 - t1)));
        t1 = t2;
        k1 = k2;
        i2 = (i2 + 1);
    }
    { std::ostringstream __ss; double __dv = s; if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
    std::cout << std::endl;
    return 0;
}
