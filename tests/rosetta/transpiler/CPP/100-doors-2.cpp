// Generated by Mochi transpiler v0.10.39 on 2025-07-25 10:09:55 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long _mem() {
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
#ifdef __APPLE__
    return usage.ru_maxrss;
#else
    return usage.ru_maxrss * 1024;
#endif
}

int main();

int door = 1;
int incrementer = 0;

int main() {
    {
        auto __bench_start = _now();
        auto __bench_mem_start = _mem();
        for (int current = 1; current < 101; current++ ) {
            std::string line = ((std::string("Door ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << current; return ss.str(); }())) + std::string(" "));
            if ((current == door)) {
                line = (line + std::string("Open"));
                incrementer = (incrementer + 1);
                door = ((door + (2 * incrementer)) + 1);
            } else {
                line = (line + std::string("Closed"));
            }
            std::cout << line;
            std::cout << std::endl;
        }
        auto __bench_end = _now();
        auto __bench_mem_end = _mem();
        auto __bench_dur = __bench_end - __bench_start;
        auto __bench_mem = __bench_mem_end;
        std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
    }
    return 0;
}
