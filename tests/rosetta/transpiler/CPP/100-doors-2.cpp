// Generated by Mochi transpiler v0.10.37 on 2025-07-23 09:22:17 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <optional>
#include <vector>



int door = 1;
int incrementer = 0;

int main() {
    for (int current = 1; current < 101; current++ ) {
        std::string line = ((std::string("Door ") + ([&]{ std::ostringstream ss; ss<<current; return ss.str(); }())) + std::string(" "));
        if ((current == door)) {
            line = (line + std::string("Open"));
            incrementer = (incrementer + 1);
            door = ((door + (2 * incrementer)) + 1);
        } else {
            line = (line + std::string("Closed"));
        }
        std::cout << line;
        std::cout << std::endl;
    }
    return 0;
}
