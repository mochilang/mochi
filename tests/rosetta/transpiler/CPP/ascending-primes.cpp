// Generated by Mochi transpiler v0.10.40 on 2025-07-26 05:21:49 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <map>
#include <type_traits>


static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int>)) { const auto& v = std::any_cast<const std::vector<int>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

bool isPrime(int n);
void gen(int first, int cand, int digits);
std::string pad(int n, int width);
int main();

std::vector<int> asc = {};

bool isPrime(int n) {
    if ((n < 2)) {
        return false;
    }
    if (((n % 2) == 0)) {
        return (n == 2);
    }
    if (((n % 3) == 0)) {
        return (n == 3);
    }
    int d = 5;
    while (((d * d) <= n)) {
        if (((n % d) == 0)) {
            return false;
        }
        d = (d + 2);
        if (((n % d) == 0)) {
            return false;
        }
        d = (d + 4);
    }
    return true;
}

void gen(int first, int cand, int digits) {
    if ((digits == 0)) {
        if (isPrime(cand)) {
            asc = ([&]{ auto __lhs = asc; auto __rhs = std::vector<int>{cand}; __lhs.insert(__lhs.end(), __rhs.begin(), __rhs.end()); return __lhs; }());
        }
        return;
    }
    int i = first;
    while ((i < 10)) {
        gen((i + 1), ((cand * 10) + i), (digits - 1));
        i = (i + 1);
    }
}

std::string pad(int n, int width) {
    std::string s = ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }());
    while ((s.size() < width)) {
        s = (std::string(" ") + s);
    }
    return s;
}

int main() {
    int digits = 1;
    while ((digits < 10)) {
        gen(1, 0, digits);
        digits = (digits + 1);
    }
    std::cout << ((std::string("There are ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << asc.size(); return ss.str(); }())) + std::string(" ascending primes, namely:"));
    std::cout << std::endl;
    int i = 0;
    std::string line = std::string("");
    while ((i < asc.size())) {
        line = ((line + pad(asc[i], 8)) + std::string(" "));
        if ((((i + 1) % 10) == 0)) {
            std::cout << line.substr(0, (line.size() - 1) - 0);
            std::cout << std::endl;
            line = std::string("");
        }
        i = (i + 1);
    }
    if ((line.size() > 0)) {
        std::cout << line.substr(0, (line.size() - 1) - 0);
        std::cout << std::endl;
    }
    return 0;
}
