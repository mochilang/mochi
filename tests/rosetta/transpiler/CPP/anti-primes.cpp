// Generated by Mochi transpiler v0.10.40 on 2025-07-25 19:51:18 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>



int countDivisors(int n);
int main();

int countDivisors(int n) {
    if ((n < 2)) {
        return 1;
    }
    int count = 2;
    int i = 2;
    while ((i <= (n / 2))) {
        if (((n % i) == 0)) {
            count = (count + 1);
        }
        i = (i + 1);
    }
    return count;
}

int main() {
    std::cout << std::string("The first 20 anti-primes are:");
    std::cout << std::endl;
    int maxDiv = 0;
    int count = 0;
    int n = 1;
    std::string line = std::string("");
    while ((count < 20)) {
        int d = countDivisors(n);
        if ((d > maxDiv)) {
            line = ((line + ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }())) + std::string(" "));
            maxDiv = d;
            count = (count + 1);
        }
        n = (n + 1);
    }
    line = line.substr(0, (line.size() - 1) - 0);
    std::cout << line;
    std::cout << std::endl;
    return 0;
}
