#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1]).

% Generated by Mochi transpiler v0.10.37 (fe5a498481) on 2025-07-23 00:33 +0700

main(_) ->
    Door = 1,
    Incrementer = 0,
    Fun = fun Fun_loop(List, Door, Incrementer) ->
    case List of
        [] -> {Door, Incrementer};
        [Current|Rest] ->
            Line = (("Door " ++ integer_to_list(Current)) ++ " "),
            case (Current == Door) of
        true -> Line2 = (Line ++ "Open"),
            Incrementer2 = (Incrementer + 1),
            Door2 = ((Door + (2 * Incrementer2)) + 1),
            Door3 = Door2,
            Incrementer3 = Incrementer2,
            Line4 = Line2;
        _ -> Line3 = (Line ++ "Closed"),
            Door3 = Door,
            Incrementer3 = Incrementer,
            Line4 = Line3
    end,
            io:format("~ts~n", [Line4]),
            Fun_loop(Rest, Door3, Incrementer3)
    end
end,
{Door3, Incrementer3} = Fun(lists:seq(1, (101) - 1), Door, Incrementer).
