#!/usr/bin/env escript
-module(main).
-export([main/1]).

% Generated by Mochi transpiler v0.10.37 (e0abb15f5) on 2025-07-22 19:10 +0700

main(_) ->
    Door = 1,
    Incrementer = 0,
    lists:foreach(fun(Current) ->
    Line = (("Door " ++ integer_to_list(Current)) ++ " "),
    case (Current == Door) of
        true -> Line2 = (Line ++ "Open"),
            Incrementer2 = (Incrementer + 1),
            Door2 = ((Door + (2 * Incrementer2)) + 1),
            Incrementer2 = Incrementer2,
            Line2 = Line2,
            Door2 = Door2;
        _ -> Line2 = (Line ++ "Closed"),
            Incrementer2 = Incrementer,
            Line2 = Line2,
            Door2 = Door
    end,
    io:format("~ts~n", [Line2])
end, lists:seq(1, (101) - 1)).
