#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, bar/3, main/0]).

% Generated by Mochi transpiler v0.10.41 (228dd6653b) on 2025-07-26 21:45 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.

bar(A, B, C) ->
    try
        io:format("~ts~n", [((((lists:flatten(io_lib:format("~p", [A])) ++ ", ") ++ lists:flatten(io_lib:format("~p", [B]))) ++ ", ") ++ lists:flatten(io_lib:format("~p", [C])))]),
        nil
    catch {return, Ret} -> Ret end.

main() ->
    try
        Args = #{},
        Args_2 = maps:put("a", 3, Args),
        Args_3 = maps:put("b", 2, Args_2),
        Args_4 = maps:put("c", 1, Args_3),
        bar(maps:get("a", Args_4, nil), maps:get("b", Args_4, nil), maps:get("c", Args_4, nil)),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    Start = mochi_now(),
    StartMem = erlang:memory(total),
    main(),
    End = mochi_now(),
    EndMem = erlang:memory(total),
    DurationUs = (End - Start) div 1000,
    MemBytes = erlang:abs(EndMem - StartMem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [DurationUs, MemBytes]).
