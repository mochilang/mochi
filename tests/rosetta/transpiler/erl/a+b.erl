#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, main/0]).

% Generated by Mochi transpiler v0.10.37 (13adf36e48) on 2025-07-23 14:04 +0700


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.

main() ->
    try
        A = mochi_to_int(string:trim(io:get_line(""))),
        B = mochi_to_int(string:trim(io:get_line(""))),
        io:format("~p~n", [(A + B)]),
        nil
    catch {return, V} -> V end.

main(_) ->
    main().
