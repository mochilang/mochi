#!/usr/bin/env escript
-module(main).
-export([main/1]).

% Generated by Mochi transpiler v0.10.37 (471b8e5ea) on 2025-07-22 22:49 +0700

main(_) ->
    Result = "",
    Fun = fun F(List, Result) ->
    case List of
        [] -> {Result};
        [I|Rest] ->
            J = 1,
            Fun2 = fun F(I, J, Result) ->
    case ((J * J) < I) of
        true ->
            J2 = (J + 1),
            F(I, J2, Result);
        _ -> {I, J, Result}
    end
end,
{I, J2, Result} = Fun2(I, J, Result),
            case ((J2 * J2) == I) of
        true -> Result2 = (Result ++ "O"),
            Result2 = Result2;
        _ -> Result2 = (Result ++ "-"),
            Result2 = Result2
    end,
            F(Rest, Result2)
    end
end,
{Result2} = Fun(lists:seq(1, (101) - 1), Result),
    io:format("~ts~n", [Result2]).
