#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, anglediff/2]).

% Generated by Mochi transpiler v0.10.55 (1df91402aa) on 2025-08-02 23:18 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.

anglediff(B1, B2) ->
    try
        D = (B2 - B1),
        case (D < (0 - 180)) of
        true -> throw({return, (D + 360)});
        _ -> ok
    end,
        case (D > 180) of
        true -> throw({return, (D - 360)});
        _ -> ok
    end,
        D
    catch {return, Ret} -> Ret end.

main(_) ->
    BenchStart = mochi_now(),
    BenchStartMem = erlang:memory(total),
    erlang:put('testCases', [[20, 45], [(0 - 45), 45], [(0 - 85), 90], [(0 - 95), 90], [(0 - 45), 125], [(0 - 45), 145], [29.4803, (0 - 88.6381)], [(0 - 78.3251), (0 - 159.036)]]),
    Fun = fun Fun_loop(List) ->
    case List of
        [] -> {};
        [Tc|Tc_rest] ->
            io:format("~p~n", [anglediff((case erlang:is_map(Tc) of true -> maps:get(0, Tc, nil); _ -> lists:nth(0 + 1, Tc) end), (case erlang:is_map(Tc) of true -> maps:get(1, Tc, nil); _ -> lists:nth(1 + 1, Tc) end))]),
            Fun_loop(Tc_rest)
    end
end,
{} = Fun(erlang:get('testCases')),
    BenchEnd = mochi_now(),
    BenchEndMem = erlang:memory(total),
    DurationUs = (BenchEnd - BenchStart) div 1000,
    MemBytes = erlang:abs(BenchEndMem - BenchStartMem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [DurationUs, MemBytes]).
