#!/usr/bin/env escript
-module(main).
-export([main/1, connect/1, main/0]).

% Generated by Mochi transpiler v0.10.37 (cdf73e20a) on 2025-07-22 21:24 +0700

connect(Client) ->
    try
        ((maps:get("Host", Client) /= "") andalso (maps:get("Port", Client) > 0))
    catch {return, V} -> V end.

main() ->
    try
        Client = #{"Base" => "dc=example,dc=com", "Host" => "ldap.example.com", "Port" => 389, "UseSSL" => false, "BindDN" => "uid=readonlyuser,ou=People,dc=example,dc=com", "BindPassword" => "readonlypassword", "UserFilter" => "(uid=%s)", "GroupFilter" => "(memberUid=%s)", "Attributes" => ["givenName", "sn", "mail", "uid"]},
        case connect(Client) of
        true -> io:format("~ts~n", [("Connected to " ++ maps:get("Host", Client))]);
        _ -> io:format("~ts~n", ["Failed to connect"])
    end,
        nil
    catch {return, V} -> V end.

main(_) ->
    main().
