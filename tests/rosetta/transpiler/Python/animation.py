# Code generated by Mochi transpiler.
# Version 0.10.37, generated on 2025-07-23 11:36 +0700
import sys
sys.set_int_max_str_digits(0)

msg = "Hello World! "
shift = 0
inc = 1
clicks = 0
frames = 0
while clicks < 5:
    line = ""
    i = 0
    while i < len(msg):
        idx = (shift + i) % len(msg)
        line = line + "".join(msg[idx:idx + 1])
        i = i + 1
    print(line)
    shift = (shift + inc) % len(msg)
    frames = frames + 1
    if frames % len(msg) == 0:
        inc = len(msg) - inc
        clicks = clicks + 1
