# Code generated by Mochi transpiler.
# Version 0.10.41, generated on 2025-07-27 12:17 +0700
import os

import sys
sys.set_int_max_str_digits(0)

def hasPrefix(s, p):
    if len(p) > len(s):
        sys.exit(False)
    sys.exit(s[0:len(p)] == p)
name = "SHELL"
prefix = name + "="
for v in os.environ:
    if hasPrefix(v, prefix):
        print(name + " has value " + "".join(v[len(prefix):len(v)]))
        sys.exit()
print(name + " not found")
