# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-27 22:24 +0700
import sys
sys.set_int_max_str_digits(0)

nextPID = 1
def run(hasArg):
    global nextPID
    pid = nextPID
    nextPID = nextPID + 1
    print("PID: " + str(pid))
    if not hasArg:
        print("Done.")
        return
    childPID = nextPID
    print("Child's PID: " + str(childPID))
    run(False)
run(True)
