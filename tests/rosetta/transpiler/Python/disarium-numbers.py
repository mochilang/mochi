# Code generated by Mochi transpiler.
# Version 0.10.41, generated on 2025-07-26 19:54 +0700
import sys
sys.set_int_max_str_digits(0)

def pow(base, exp):
    result = 1
    i = 0
    while i < exp:
        result = result * base
        i = i + 1
    return result
def isDisarium(n):
    digits = []
    x = n
    if x == 0:
        digits = digits + [0]
    while x > 0:
        digits = digits + [x % 10]
        x = int((x // 10))
    sum = 0
    pos = 1
    i = len(digits) - 1
    while i >= 0:
        sum = sum + pow(digits[i], pos)
        pos = pos + 1
        i = i - 1
    return sum == n
def main():
    count = 0
    n = 0
    while count < 19 and n < 30000000:
        if isDisarium(n):
            print(str(n))
            count = count + 1
        n = n + 1
    print("\nFound the first " + str(count) + " Disarium numbers.")
main()
