# Code generated by Mochi transpiler.
# Version 0.10.40, generated on 2025-07-25 13:13 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict

import sys
sys.set_int_max_str_digits(0)

@dataclass
class Dog:
    kind: str
    name: str

@dataclass
class Cat:
    kind: str
    name: str

def beastKind(b):
    return (b.kind if isinstance(b, Dog) else (b.kind if isinstance(b, Cat) else None))
def beastName(b):
    return (b.name if isinstance(b, Dog) else (b.name if isinstance(b, Cat) else None))
def beastCry(b):
    return ("Woof" if isinstance(b, Dog) else ("Meow" if isinstance(b, Cat) else None))
def bprint(b):
    print(beastName(b) + ", who's a " + beastKind(b) + ", cries: \"" + beastCry(b) + "\".")
def main():
    d = Dog(kind="labrador", name="Max")
    c = Cat(kind="siamese", name="Sammy")
    bprint(d)
    bprint(c)
main()
