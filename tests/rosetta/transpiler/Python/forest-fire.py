# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-27 22:23 +0700
import sys
sys.set_int_max_str_digits(0)

rows = 5
cols = 5
board = []
r = 0
while r < rows:
    row = []
    c = 0
    while c < cols:
        row = row + [(r + c) % 2]
        c = c + 1
    board = board + [row]
    r = r + 1
def step(b):
    nb = b
    r = 0
    while r < rows:
        c = 0
        while c < cols:
            nb[r][c] = 1 - b[r][c]
            c = c + 1
        r = r + 1
    return nb
print("start")
for row in board:
    print(str(row))
board = step(board)
print("after")
for row in board:
    print(str(row))
