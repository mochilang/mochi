# Code generated by Mochi transpiler.
# Version 0.10.37, generated on 2025-07-23 09:25 +0700
def fields(s):
    res = []
    cur = ""
    i = 0
    while i < len(s):
        c = s[i:i + 1]
        if c == " ":
            if len(cur) > 0:
                res = res + [cur]
                cur = ""
        else:
            cur = cur + c
        i = i + 1
    if len(cur) > 0:
        res = res + [cur]
    return res
def canSpell(word, blks):
    if len(word) == 0:
        return True
    c = word[0:1].lower()
    i = 0
    while i < len(blks):
        b = blks[i]
        if c == b[0:1].lower() or c == b[1:2].lower():
            rest = []
            j = 0
            while j < len(blks):
                if j != i:
                    rest = rest + [blks[j]]
                j = j + 1
            if canSpell(word[1:], rest):
                return True
        i = i + 1
    return False
def newSpeller(blocks):
    bl = fields(blocks)
    return lambda w: canSpell(w, bl)
def main():
    sp = newSpeller("BO XK DQ CP NA GT RE TG QD FS JW HU VI AN OB ER FS LY PC ZM")
    for word in ["A", "BARK", "BOOK", "TREAT", "COMMON", "SQUAD", "CONFUSE"]:
        print(word + " " + str(sp(word)))
main()
