# Code generated by Mochi transpiler.
# Version 0.10.37, generated on 2025-07-23 04:18 +0000
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
import dataclasses

import sys
sys.set_int_max_str_digits(0)

@dataclass
class SomeStruct:
    runtimeFields: {string: string}

def main():
    ss = SomeStruct(runtimeFields={})
    print("Create two fields at runtime: \n")
    i = 1
    while i <= 2:
        print("  Field #" + str(i) + ":\n")
        print("       Enter name  : ")
        name = input()
        print("       Enter value : ")
        value = input()
        fields = ss.runtimeFields
        fields[name] = value
        ss = dataclasses.replace(ss, runtimeFields=fields)
        print("\n")
        i = i + 1
    while True:
        print("Which field do you want to inspect ? ")
        name = input()
        if name in ss.runtimeFields:
            value = ss.runtimeFields[name]
            print("Its value is '" + value + "'")
            return
        else:
            print("There is no field of that name, try again\n")
main()
