// Generated by Mochi 0.10.32 on 2025-07-22 18:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

int door = 1;
int incrementer = 0;

int main(void) {
    for (int current = 1; current < 101; current++) {
        const char* line = str_concat(str_concat("Door ", str_int(current)), " ");
        if (current == door) {
            line = str_concat(line, "Open");
            incrementer = incrementer + 1;
            door = (door + (2 * incrementer)) + 1;
        } else {
            line = str_concat(line, "Closed");
        }
        puts(line);
    }
    return 0;
}
