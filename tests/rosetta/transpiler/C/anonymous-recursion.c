// Generated by Mochi 0.10.32 on 2025-07-26 05:05 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

int fib(int n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int user_main() {
    int i = -1;
    while (i <= 10) {
        if (i < 0) {
            puts(str_concat(str_concat("fib(", str_int(i)), ") returned error: negative n is forbidden"));
        } else {
            puts(str_concat(str_concat(str_concat("fib(", str_int(i)), ") = "), str_int(fib(i))));
        }
        i = i + 1;
    }
}

int main(void) {
    user_main();
    return 0;
}
