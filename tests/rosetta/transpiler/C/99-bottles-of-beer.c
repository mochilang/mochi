// Generated by Mochi 0.10.32 on 2025-07-24 18:26 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

const char* bottles(int n) {
    if (n == 0) {
        return "No more bottles";
    }
    if (n == 1) {
        return "1 bottle";
    }
    return str_concat(str_int(n), " bottles");
}

int user_main() {
    int i = 99;
    while (i > 0) {
        puts(str_concat(bottles(i), " of beer on the wall"));
        puts(str_concat(bottles(i), " of beer"));
        puts("Take one down, pass it around");
        puts(str_concat(bottles(i - 1), " of beer on the wall"));
        i = i - 1;
    }
}

int main(void) {
    user_main();
    return 0;
}
