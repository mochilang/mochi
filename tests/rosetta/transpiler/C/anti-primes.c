// Generated by Mochi 0.10.32 on 2025-07-26 05:05 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int countDivisors(int n) {
    if (n < 2) {
        return 1;
    }
    int count = 2;
    int i = 2;
    while (i <= (n / 2)) {
        if ((n % i) == 0) {
            count = count + 1;
        }
        i = i + 1;
    }
    return count;
}

int user_main() {
    puts("The first 20 anti-primes are:");
    int maxDiv = 0;
    int count = 0;
    int n = 1;
    const char* line = "";
    while (count < 20) {
        int d = countDivisors(n);
        if (d > maxDiv) {
            line = str_concat(str_concat(line, str_int(n)), " ");
            maxDiv = d;
            count = count + 1;
        }
        n = n + 1;
    }
    line = _substring(line, 0, strlen(line) - 1);
    puts(line);
}

int main(void) {
    user_main();
    return 0;
}
