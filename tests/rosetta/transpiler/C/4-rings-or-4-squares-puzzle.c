// Generated by Mochi 0.10.32 on 2025-07-24 15:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

typedef struct Anon1 Anon1;

struct Anon1 {
    int count;
    int** list;
};

int validComb(int a, int b, int c, int d, int e, int f, int g) {
    int square1 = a + b;
    int square2 = (b + c) + d;
    int square3 = (d + e) + f;
    int square4 = f + g;
    return ((square1 == square2) && (square2 == square3)) && (square3 == square4);
}

int isUnique(int a, int b, int c, int d, int e, int f, int g) {
    int nums[] = {a, b, c, d, e, f, g};
    size_t nums_len = 7;
    int i = 0;
    while (i < 7) {
        int j = i + 1;
        while (j < 7) {
            if (nums[i] == nums[j]) {
                return 0;
            }
            j = j + 1;
        }
        i = i + 1;
    }
    return 1;
}

Anon1 getCombs(int low, int high, int unique) {
    int **valid = NULL;
    size_t valid_len = 0;
    int count = 0;
    for (int b = low; b < high + 1; b++) {
        for (int c = low; c < high + 1; c++) {
            for (int d = low; d < high + 1; d++) {
                int s = (b + c) + d;
                for (int e = low; e < high + 1; e++) {
                    for (int f = low; f < high + 1; f++) {
                        int a = s - b;
                        int g = s - f;
                        if ((a < low) || (a > high)) {
                            continue;
                        }
                        if ((g < low) || (g > high)) {
                            continue;
                        }
                        if (((d + e) + f) != s) {
                            continue;
                        }
                        if ((f + g) != s) {
                            continue;
                        }
                        if (!(unique) || isUnique(a, b, c, d, e, f, g)) {
                            valid = list_append_intptr(valid, &valid_len, (int[]){ a, b, c, d, e, f, g });
                            count = count + 1;
                        }
                    }
                }
            }
        }
    }
    return (Anon1){.count = count, .list = valid};
}

int main(void) {
    Anon1 r1 = getCombs(1, 7, 1);
    puts(str_concat(str_int(r1.count), " unique solutions in 1 to 7"));
    printf("%d\n", r1.list);
    Anon1 r2 = getCombs(3, 9, 1);
    puts(str_concat(str_int(r2.count), " unique solutions in 3 to 9"));
    printf("%d\n", r2.list);
    Anon1 r3 = getCombs(0, 9, 0);
    puts(str_concat(str_int(r3.count), " non-unique solutions in 0 to 9"));
    return 0;
}
