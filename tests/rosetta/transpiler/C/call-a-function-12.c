// Generated by Mochi 0.10.32 on 2025-08-02 20:52 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct MkAddClosure MkAddClosure;
typedef struct PartialSumClosure PartialSumClosure;

struct MkAddClosure {
    long long a;
};

struct PartialSumClosure {
    long long x;
};

MkAddClosure mkAdd(long long a);
long long MkAddClosure_call(long long a, long long b);
long long mysum(long long x, long long y);
PartialSumClosure partialSum(long long x);
long long PartialSumClosure_call(long long x, long long y);
void user_main();
int main(void);

MkAddClosure mkAdd(long long a) {
    return (MkAddClosure){.a = a};
}

long long MkAddClosure_call(long long a, long long b) {
    return a + b;
}

long long mysum(long long x, long long y) {
    return x + y;
}

PartialSumClosure partialSum(long long x) {
    return (PartialSumClosure){.x = x};
}

long long PartialSumClosure_call(long long x, long long y) {
    return mysum(x, y);
}

void user_main() {
    MkAddClosure add2 = mkAdd(2LL);
    MkAddClosure add3 = mkAdd(3LL);
    puts(str_concat(str_concat(str_int(MkAddClosure_call(add2.a, 5LL)), " "), str_int(MkAddClosure_call(add3.a, 6LL))));
    PartialSumClosure partial = partialSum(13LL);
    puts(str_int(PartialSumClosure_call(partial.x, 5LL)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
