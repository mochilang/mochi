// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t stringSearch_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int indexOfStr(const char* h, const char* n);
int stringSearchSingle(const char* h, const char* n);
int * stringSearch(const char* h, const char* n);
const char* display(int * nums, size_t nums_len);
int user_main();
int main(void);

int indexOfStr(const char* h, const char* n) {
    int hlen = strlen(h);
    int nlen = strlen(n);
    if (nlen == 0LL) {
        return 0LL;
    }
    int i = 0LL;
    while (i <= (hlen - nlen)) {
        if (strcmp(_substring(h, i, i + nlen), n) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

int stringSearchSingle(const char* h, const char* n) {
    return indexOfStr(h, n);
}

int * stringSearch(const char* h, const char* n) {
    int *result = NULL;
    size_t result_len = 0;
    int start = 0LL;
    int hlen = strlen(h);
    int nlen = strlen(n);
    while (start < hlen) {
        int idx = indexOfStr(_substring(h, start, hlen), n);
        if (idx >= 0LL) {
            result = list_append_int(result, &result_len, start + idx);
            start = (start + idx) + nlen;
        } else {
            break;
        }
    }
    return stringSearch_len = result_len, result;
}

const char* display(int * nums, size_t nums_len) {
    const char* s = "[";
    int i = 0LL;
    while (i < nums_len) {
        if (i > 0LL) {
            s = str_concat(s, ", ");
        }
        s = str_concat(s, str_int(nums[(int)(i)]));
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

int user_main() {
    const char* texts[6] = {"GCTAGCTCTACGAGTCTA", "GGCTATAATGCGTA", "there would have been a time for such a word", "needle need noodle needle", "DKnuthusesandprogramsanimaginarycomputertheMIXanditsassociatedmachinecodeandassemblylanguages", "Nearby farms grew an acre of alfalfa on the dairy's behalf, with bales of that alfalfa exchanged for milk."};
    size_t texts_len = 6;
    const char* patterns[6] = {"TCTA", "TAATAAA", "word", "needle", "and", "alfalfa"};
    size_t patterns_len = 6;
    int i = 0LL;
    while (i < 6LL) {
        puts(str_concat(str_concat(str_concat("text", str_int(i + 1LL)), " = "), texts[(int)(i)]));
        i = i + 1LL;
    }
    puts("");
    int j = 0LL;
    while (j < 6LL) {
        int *idxs = stringSearch(texts[(int)(j)], patterns[(int)(j)]);
        size_t idxs_len = stringSearch_len;
        puts(str_concat(str_concat(str_concat(str_concat(str_concat("Found \"", patterns[(int)(j)]), "\" in 'text"), str_int(j + 1LL)), "' at indexes "), display(idxs, idxs_len)));
        j = j + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
