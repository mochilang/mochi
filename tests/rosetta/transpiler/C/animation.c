// Generated by Mochi 0.10.32 on 2025-07-26 05:05 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* msg = "Hello World! ";
int shift = 0;
int inc = 1;
int clicks = 0;
int frames = 0;

int main(void) {
    while (clicks < 5) {
        const char* line = "";
        int i = 0;
        while (i < strlen(msg)) {
            int idx = (shift + i) % strlen(msg);
            line = str_concat(line, _substring(msg, idx, idx + 1));
            i = i + 1;
        }
        puts(line);
        shift = (shift + inc) % strlen(msg);
        frames = frames + 1;
        if ((frames % strlen(msg)) == 0) {
            inc = strlen(msg) - inc;
            clicks = clicks + 1;
        }
    }
    return 0;
}
