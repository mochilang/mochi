// Generated by Mochi 0.10.32 on 2025-08-01 18:59 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* toBase(int n, int b);
int parseIntStr(const char* str);
int parseIntBase(const char* s, int b);
const char* reverseStr(const char* s);
int isPalindrome(const char* s);
int isPalindromeBin(int n);
int myMin(int a, int b);
int myMax(int a, int b);
int reverse3(int n);
int show(int n);
int user_main();
int main(void);

const char* toBase(int n, int b) {
    if (n == 0LL) {
        return "0";
    }
    const char* s = "";
    int x = n;
    while (x > 0LL) {
        s = str_concat(str_int(x % b), s);
        x = x / b;
    }
    return s;
}

int parseIntStr(const char* str) {
    int i = 0LL;
    int neg = 0LL;
    if ((strlen(str) > 0LL) && (strcmp((const char[]){str[0LL], 0}, "-") == 0)) {
        neg = 1LL;
        i = 1LL;
    }
    int n = 0LL;
    while (i < strlen(str)) {
        n = ((n * 10LL) + (int)(_atoi(_substring(str, i, i + 1LL)))) - 0LL;
        i = i + 1LL;
    }
    if (neg) {
        n = -(n);
    }
    return n;
}

int parseIntBase(const char* s, int b) {
    int n = 0LL;
    int i = 0LL;
    while (i < strlen(s)) {
        n = (n * b) + parseIntStr(_substring(s, i, i + 1LL));
        i = i + 1LL;
    }
    return n;
}

const char* reverseStr(const char* s) {
    const char* out = "";
    int i = strlen(s) - 1LL;
    while (i >= 0LL) {
        out = str_concat(out, _substring(s, i, i + 1LL));
        i = i - 1LL;
    }
    return out;
}

int isPalindrome(const char* s) {
    return strcmp(s, reverseStr(s)) == 0;
}

int isPalindromeBin(int n) {
    const char* b = toBase(n, 2LL);
    return isPalindrome(b);
}

int myMin(int a, int b) {
    if (a < b) {
        return a;
    }
    return b;
}

int myMax(int a, int b) {
    if (a > b) {
        return a;
    }
    return b;
}

int reverse3(int n) {
    int x = 0LL;
    int y = n;
    while (y != 0LL) {
        x = (x * 3LL) + (y % 3LL);
        y = y / 3LL;
    }
    return x;
}

int show(int n) {
    puts(str_concat("Decimal : ", str_int(n)));
    puts(str_concat("Binary  : ", toBase(n, 2LL)));
    puts(str_concat("Ternary : ", toBase(n, 3LL)));
    puts("");
}

int user_main() {
    puts("The first 6 numbers which are palindromic in both binary and ternary are :\n");
    show(0LL);
    int count = 1LL;
    int lo = 0LL;
    int hi = 1LL;
    int pow2 = 1LL;
    int pow3 = 1LL;
    while (1LL) {
        int i = lo;
        while (i < hi) {
            int n = (((i * 3LL) + 1LL) * pow3) + reverse3(i);
            if (isPalindromeBin(n)) {
                show(n);
                count = count + 1LL;
                if (count >= 6LL) {
                    return 0;
                }
            }
            i = i + 1LL;
        }
        if (i == pow3) {
            pow3 = pow3 * 3LL;
        } else {
            pow2 = pow2 * 4LL;
        }
        while (1LL) {
            while (pow2 <= pow3) {
                pow2 = pow2 * 4LL;
            }
            int lo2 = ((pow2 / pow3) - 1LL) / 3LL;
            int hi2 = ((((pow2 * 2LL) / pow3) - 1LL) / 3LL) + 1LL;
            int lo3 = pow3 / 3LL;
            int hi3 = pow3;
            if (lo2 >= hi3) {
                pow3 = pow3 * 3LL;
            } else {
                if (lo3 >= hi2) {
                    pow2 = pow2 * 4LL;
                } else {
                    lo = myMax(lo2, lo3);
                    hi = myMin(hi2, hi3);
                    break;
                }
            }
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
