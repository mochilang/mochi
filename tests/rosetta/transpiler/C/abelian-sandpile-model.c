// Generated by Mochi 0.10.32 on 2025-07-25 00:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

size_t newPile_len;
size_t handlePile_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

int dim = 16;

int * * newPile(int d) {
    int **b = NULL;
    size_t b_len = 0;
    int y = 0;
    while (y < d) {
        int *row = NULL;
        size_t row_len = 0;
        int x = 0;
        while (x < d) {
            row = list_append_int(row, &row_len, 0);
            x = x + 1;
        }
        b = list_append_intptr(b, &b_len, row);
        y = y + 1;
    }
    return newPile_len = b_len, b;
}

int * * handlePile(int * * pile, size_t pile_len, int x, int y) {
    if (pile[y][x] >= 4) {
        pile[y][x] = pile[y][x] - 4;
        if (y > 0) {
            pile[y - 1][x] = pile[y - 1][x] + 1;
            if (pile[y - 1][x] >= 4) {
                pile = handlePile(pile, pile_len, x, y - 1);
                pile_len = handlePile_len;
            }
        }
        if (x > 0) {
            pile[y][x - 1] = pile[y][x - 1] + 1;
            if (pile[y][x - 1] >= 4) {
                pile = handlePile(pile, pile_len, x - 1, y);
                pile_len = handlePile_len;
            }
        }
        if (y < (dim - 1)) {
            pile[y + 1][x] = pile[y + 1][x] + 1;
            if (pile[y + 1][x] >= 4) {
                pile = handlePile(pile, pile_len, x, y + 1);
                pile_len = handlePile_len;
            }
        }
        if (x < (dim - 1)) {
            pile[y][x + 1] = pile[y][x + 1] + 1;
            if (pile[y][x + 1] >= 4) {
                pile = handlePile(pile, pile_len, x + 1, y);
                pile_len = handlePile_len;
            }
        }
        pile = handlePile(pile, pile_len, x, y);
        pile_len = handlePile_len;
    }
    return handlePile_len = pile_len, pile;
}

int drawPile(int * * pile, size_t pile_len, int d) {
    const char* chars[] = {" ", "░", "▓", "█"};
    size_t chars_len = 4;
    int row = 0;
    while (row < d) {
        const char* line = "";
        int col = 0;
        while (col < d) {
            int v = pile[row][col];
            if (v > 3) {
                v = 3;
            }
            line = str_concat(line, chars[v]);
            col = col + 1;
        }
        puts(line);
        row = row + 1;
    }
}

int user_main() {
    int **pile = newPile(16);
    size_t pile_len = newPile_len;
    int hdim = 7;
    pile[hdim][hdim] = 16;
    pile = handlePile(pile, pile_len, hdim, hdim);
    pile_len = handlePile_len;
    drawPile(pile, pile_len, 16);
}

int main(void) {
    user_main();
    return 0;
}
