// Generated by Mochi 0.10.32 on 2025-08-01 18:59 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int limit = 42LL;

int isPrime(int n);
const char* commatize(int n);
const char* padLeft(const char* s, int w);
const char* padRight(const char* s, int w);
int user_main();
int main(void);

int isPrime(int n) {
    if (n < 2LL) {
        return 0LL;
    }
    if ((n % 2LL) == 0LL) {
        return n == 2LL;
    }
    if ((n % 3LL) == 0LL) {
        return n == 3LL;
    }
    int d = 5LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 2LL;
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 4LL;
    }
    return 1LL;
}

const char* commatize(int n) {
    const char* s = str_int(n);
    int i = strlen(s) - 3LL;
    while (i >= 1LL) {
        s = str_concat(str_concat(_substring(s, 0LL, i), ","), _substring(s, i, strlen(s)));
        i = i - 3LL;
    }
    return s;
}

const char* padLeft(const char* s, int w) {
    const char* out = s;
    while (strlen(out) < w) {
        out = str_concat(" ", out);
    }
    return out;
}

const char* padRight(const char* s, int w) {
    const char* out = s;
    while (strlen(out) < w) {
        out = str_concat(out, " ");
    }
    return out;
}

int user_main() {
    int i = limit;
    int n = 0LL;
    while (n < limit) {
        if (isPrime(i)) {
            n = n + 1LL;
            const char* nStr = padRight(str_int(n), 2LL);
            const char* pStr = padLeft(commatize(i), 19LL);
            puts(str_concat(str_concat(str_concat("n = ", nStr), "  "), pStr));
            i = (i + i) - 1LL;
        }
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
