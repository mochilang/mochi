// Generated by Mochi 0.10.32 on 2025-07-27 16:06 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t getBins_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int * getBins(int * limits, size_t limits_len, int * data, size_t data_len);
const char* padLeft(int n, int width);
int printBins(int * limits, size_t limits_len, int * bins, size_t bins_len);
int user_main();
int main(void);

int * getBins(int * limits, size_t limits_len, int * data, size_t data_len) {
    size_t n = limits_len;
    int *bins = NULL;
    size_t bins_len = 0;
    int i = 0;
    while (i < (n + 1)) {
        bins = list_append_int(bins, &bins_len, 0);
        i = i + 1;
    }
    int j = 0;
    while (j < data_len) {
        int d = data[(int)(j)];
        int index = 0;
        while (index < limits_len) {
            if (d < limits[(int)(index)]) {
                break;
            }
            if (d == limits[(int)(index)]) {
                index = index + 1;
                break;
            }
            index = index + 1;
        }
        bins[(int)(index)] = bins[(int)(index)] + 1;
        j = j + 1;
    }
    return getBins_len = bins_len, bins;
}

const char* padLeft(int n, int width) {
    const char* s = str_int(n);
    int pad = width - strlen(s);
    const char* out = "";
    int i = 0;
    while (i < pad) {
        out = str_concat(out, " ");
        i = i + 1;
    }
    return str_concat(out, s);
}

int printBins(int * limits, size_t limits_len, int * bins, size_t bins_len) {
    size_t n = limits_len;
    puts(str_concat(str_concat(str_concat("           < ", padLeft(limits[(int)(0)], 3)), " = "), padLeft(bins[(int)(0)], 2)));
    int i = 1;
    while (i < n) {
        puts(str_concat(str_concat(str_concat(str_concat(str_concat(">= ", padLeft(limits[(int)(i - 1)], 3)), " and < "), padLeft(limits[(int)(i)], 3)), " = "), padLeft(bins[(int)(i)], 2)));
        i = i + 1;
    }
    puts(str_concat(str_concat(str_concat(">= ", padLeft(limits[(int)(n - 1)], 3)), "           = "), padLeft(bins[(int)(n)], 2)));
    puts("");
}

int user_main() {
    int limitsList[2][6] = {{23, 37, 43, 53, 67, 83}, {14, 18, 249, 312, 389, 392, 513, 591, 634, 720}};
    size_t limitsList_len = 2;
    size_t limitsList_lens[] = {6, 10};
    size_t limitsList_lens_len = 2;
    int dataList[2][50] = {{95, 21, 94, 12, 99, 4, 70, 75, 83, 93, 52, 80, 57, 5, 53, 86, 65, 17, 92, 83, 71, 61, 54, 58, 47, 16, 8, 9, 32, 84, 7, 87, 46, 19, 30, 37, 96, 6, 98, 40, 79, 97, 45, 64, 60, 29, 49, 36, 43, 55}, {445, 814, 519, 697, 700, 130, 255, 889, 481, 122, 932, 77, 323, 525, 570, 219, 367, 523, 442, 933, 416, 589, 930, 373, 202, 253, 775, 47, 731, 685, 293, 126, 133, 450, 545, 100, 741, 583, 763, 306, 655, 267, 248, 477, 549, 238, 62, 678, 98, 534, 622, 907, 406, 714, 184, 391, 913, 42, 560, 247, 346, 860, 56, 138, 546, 38, 985, 948, 58, 213, 799, 319, 390, 634, 458, 945, 733, 507, 916, 123, 345, 110, 720, 917, 313, 845, 426, 9, 457, 628, 410, 723, 354, 895, 881, 953, 677, 137, 397, 97, 854, 740, 83, 216, 421, 94, 517, 479, 292, 963, 376, 981, 480, 39, 257, 272, 157, 5, 316, 395, 787, 942, 456, 242, 759, 898, 576, 67, 298, 425, 894, 435, 831, 241, 989, 614, 987, 770, 384, 692, 698, 765, 331, 487, 251, 600, 879, 342, 982, 527, 736, 795, 585, 40, 54, 901, 408, 359, 577, 237, 605, 847, 353, 968, 832, 205, 838, 427, 876, 959, 686, 646, 835, 127, 621, 892, 443, 198, 988, 791, 466, 23, 707, 467, 33, 670, 921, 180, 991, 396, 160, 436, 717, 918, 8, 374, 101, 684, 727, 749}};
    size_t dataList_len = 2;
    size_t dataList_lens[] = {50, 200};
    size_t dataList_lens_len = 2;
    int i = 0;
    while (i < limitsList_len) {
        puts(str_concat(str_concat("Example ", str_int(i + 1)), "\n"));
        int *bins = getBins(limitsList[(int)(i)], limitsList_lens[i], dataList[(int)(i)], dataList_lens[i]);
        size_t bins_len = getBins_len;
        printBins(limitsList[(int)(i)], limitsList_lens[i], bins, bins_len);
        i = i + 1;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
