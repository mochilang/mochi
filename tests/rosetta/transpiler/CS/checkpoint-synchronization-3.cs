// Generated by Mochi 0.10.52 on 2025-07-31 08:37 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static string[] partList_4 = new string[]{"A", "B", "C", "D"};
    static long nAssemblies_5 = 3;
    static string lower(string ch_0) {
        string up_1 = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        string low_2 = "abcdefghijklmnopqrstuvwxyz";
        long i_3 = 0;
        while ((Convert.ToDouble(i_3) < Convert.ToDouble(up_1.Length))) {
            if ((ch_0 == up_1.Substring((int)(i_3), (int)((i_3 + 1) - i_3)))) {
                return low_2.Substring((int)(i_3), (int)((i_3 + 1) - i_3));
            }
            i_3 = (i_3 + 1);
        };
        return ch_0;
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            foreach (var p_6 in partList_4) {
                Console.WriteLine(_fmtTop((p_6 + " worker running")));
            }
            for (var cycle_7 = 1; cycle_7 < (nAssemblies_5 + 1); cycle_7++) {
                Console.WriteLine(_fmtTop(("begin assembly cycle " + _fmt(cycle_7))));
                string a_8 = "";
                foreach (var p_9 in partList_4) {
                    Console.WriteLine(_fmtTop((p_9 + " worker begins part")));
                    Console.WriteLine(_fmtTop(((p_9 + " worker completed ") + lower(p_9))));
                    a_8 = (a_8 + lower(p_9));
                }
                Console.WriteLine(_fmtTop((((a_8 + " assembled.  cycle ") + _fmt(cycle_7)) + " complete")));
            }
            foreach (var p_10 in partList_4) {
                Console.WriteLine(_fmtTop((p_10 + " worker stopped")));
            }
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
