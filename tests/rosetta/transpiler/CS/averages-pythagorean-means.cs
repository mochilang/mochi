// Generated by Mochi 0.10.40 on 2025-07-26 09:54 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static double powf(double _base, long exp) {
        double result_0 = 1;
        long i_1 = 0;
        while ((i_1 < exp)) {
            result_0 = (result_0 * _base);
            i_1 = (i_1 + 1);
        };
        return result_0;
    }

    static double nthRoot(double x, long n) {
        double low_2 = 0;
        double high_3 = x;
        long i_4 = 0;
        while ((i_4 < 60)) {
            double mid_5 = ((low_2 + high_3) / 2);
            if ((powf(mid_5, n) > x)) {
                high_3 = mid_5;
            } else {
                low_2 = mid_5;
            }
            i_4 = (i_4 + 1);
        };
        return low_2;
    }

    static void main() {
        double sum_6 = 0;
        double sumRecip_7 = 0;
        double prod_8 = 1;
        long n_9 = 1;
        while ((n_9 <= 10)) {
            var f_10 = Convert.ToDouble(n_9);
            sum_6 = (((dynamic)sum_6) + ((dynamic)f_10));
            sumRecip_7 = (sumRecip_7 + (((dynamic)1) / ((dynamic)f_10)));
            prod_8 = (((dynamic)prod_8) * ((dynamic)f_10));
            n_9 = (n_9 + 1);
        };
        double count_11 = 10;
        double a_12 = (sum_6 / count_11);
        double g_13 = nthRoot(prod_8, 10);
        double h_14 = (count_11 / sumRecip_7);
        Console.WriteLine(_fmtTop(((((("A: " + (a_12).ToString()) + " G: ") + (g_13).ToString()) + " H: ") + (h_14).ToString())));
        Console.WriteLine(_fmtTop(("A >= G >= H: " + (((a_12 >= g_13) && (g_13 >= h_14))).ToString())));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
