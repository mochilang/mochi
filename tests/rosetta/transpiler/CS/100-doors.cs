// Generated by Mochi 0.10.37 on 2025-07-23 00:38 +0700
using System;
using System.Linq;

class Program {
    static bool[] doors = new bool[]{};
    static void Main() {
        for (var i = 0; i < 100; i++) {
    doors = (doors.Append(false).ToArray());
}
        for (var pass = 1; pass < 101; pass++) {
    int idx = (pass - 1);
    while ((idx < 100)) {
    doors[idx] = (!doors[idx]);
    idx = (idx + pass);
}
}
        for (var row = 0; row < 10; row++) {
    string line = "";
    for (var col = 0; col < 10; col++) {
    int idx = ((row * 10) + col);
    if (doors[idx]) {
    line = (line + "1");
} else {
    line = (line + "0");
}
    if ((col < 9)) {
    line = (line + " ");
}
}
    Console.WriteLine(line);
}
    }
}
