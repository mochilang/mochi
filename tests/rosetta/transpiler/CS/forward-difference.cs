// Generated by Mochi 0.10.50 on 2025-07-31 02:10 UTC
using System;
using System.Linq;
using System.Numerics;
using System.Collections;
using System.Collections.Generic;

class Program {
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static long[] a_4 = new long[]{90, 47, 58, 29, 22, 32, 55, 5, 55, 73};
    static long[] fd(long[] a_0, long ord_1) {
        long i_2 = 0;
        while ((i_2 < ord_1)) {
            long j_3 = 0;
            while ((j_3 < ((((dynamic)(a_0.Length)) - ((dynamic)(i_2))) - 1))) {
                a_0[j_3] = (a_0[(int)((j_3 + 1))] - a_0[(int)(j_3)]);
                j_3 = (j_3 + 1);
            }
            i_2 = (i_2 + 1);
        };
        return a_0.Skip((int)(0)).Take((int)(((((dynamic)(a_0.Length)) - ((dynamic)(ord_1))) - 0))).ToArray();
    }

    static void Main() {
        Console.WriteLine(_fmtTop((a_4).ToString()));
        Console.WriteLine(_fmtTop((fd(a_4, 9)).ToString()));
    }
}
