// Generated by Mochi 0.10.39 on 2025-07-24 16:14 +0700
using System;
using System.Linq;
using System.Collections;
using System.Collections.Generic;

class Program {
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static int[] divisors(int n) {
        int[] divs = new int[]{1};
        int[] divs2 = new int[]{};
        int i = 2;
        while (((i * i) <= n)) {
            if (((n % i) == 0)) {
                var j = Convert.ToInt32((n / i));
                divs = (divs.Append(i).ToArray());
                if ((i != j)) {
                    divs2 = (divs2.Append(j).ToArray());
                }
            }
            i = (i + 1);
        };
        int j_0 = (divs2.Length - 1);
        while ((j_0 >= 0)) {
            divs = (divs.Append(divs2[j_0]).ToArray());
            j_0 = (j_0 - 1);
        };
        return divs;
    }

    static int sum(int[] xs) {
        int tot = 0;
        foreach (var v_1 in xs) {
            tot = (tot + v_1);
        };
        return tot;
    }

    static string sumStr(int[] xs) {
        string s = "";
        int i_2 = 0;
        while ((i_2 < xs.Length)) {
            s = ((s + xs[i_2].ToString()) + " + ");
            i_2 = (i_2 + 1);
        };
        return s.Substring(0, ((s.Length - 3) - 0));
    }

    static string pad2(int n) {
        string s_3 = n.ToString();
        if ((s_3.Length < 2)) {
            return (" " + s_3);
        };
        return s_3;
    }

    static string pad5(int n) {
        string s_4 = n.ToString();
        while ((s_4.Length < 5)) {
            s_4 = (" " + s_4);
        };
        return s_4;
    }

    static int abundantOdd(int searchFrom, int countFrom, int countTo, bool printOne) {
        int count = countFrom;
        int n = searchFrom;
        while ((count < countTo)) {
            int[] divs_5 = divisors(n);
            int tot_6 = (divs_5.Sum());
            if ((tot_6 > n)) {
                count = (count + 1);
                if ((printOne && (count < countTo))) {
                    n = (n + 2);
                    continue;
                }
                string s_7 = sumStr(divs_5);
                if ((!printOne)) {
                    Console.WriteLine(_fmtTop(((((((pad2(count) + ". ") + pad5(n)) + " < ") + s_7) + " = ") + tot_6.ToString())));
                } else {
                    Console.WriteLine(_fmtTop(((((n.ToString() + " < ") + s_7) + " = ") + tot_6.ToString())));
                }
            }
            n = (n + 2);
        };
        return n;
    }

    static void main() {
        int max = 25;
        Console.WriteLine(_fmtTop((("The first " + max.ToString()) + " abundant odd numbers are:")));
        int n_8 = abundantOdd(1, 0, max, false);
        Console.WriteLine(_fmtTop("\nThe one thousandth abundant odd number is:"));
        abundantOdd(n_8, max, 1000, true);
        Console.WriteLine(_fmtTop("\nThe first abundant odd number above one billion is:"));
        abundantOdd(1000000001, 0, 1, true);
    }

    static void Main() {
        main();
    }
}
