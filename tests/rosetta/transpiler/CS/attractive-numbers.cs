// Generated by Mochi 0.10.40 on 2025-07-25 14:26 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static bool isPrime(long n) {
        if ((n < 2)) {
            return false;
        };
        if (((n % 2) == 0)) {
            return (n == 2);
        };
        if (((n % 3) == 0)) {
            return (n == 3);
        };
        long d_0 = 5;
        while (((d_0 * d_0) <= n)) {
            if (((n % d_0) == 0)) {
                return false;
            }
            d_0 = (d_0 + 2);
            if (((n % d_0) == 0)) {
                return false;
            }
            d_0 = (d_0 + 4);
        };
        return true;
    }

    static long countPrimeFactors(long n) {
        if ((n == 1)) {
            return 0;
        };
        if (isPrime(n)) {
            return 1;
        };
        long count_1 = 0;
        long f_2 = 2;
        while (true) {
            if (((n % f_2) == 0)) {
                count_1 = (count_1 + 1);
                n = (n / f_2);
                if ((n == 1)) {
                    return count_1;
                }
                if (isPrime(n)) {
                    f_2 = n;
                }
            } else {
                if ((f_2 >= 3)) {
                    f_2 = (f_2 + 2);
                } else {
                    f_2 = 3;
                }
            }
        };
        return count_1;
    }

    static string pad4(long n) {
        string s_3 = (n).ToString();
        while ((s_3.Length < 4)) {
            s_3 = (" " + s_3);
        };
        return s_3;
    }

    static void main() {
        long max_4 = 120;
        Console.WriteLine(_fmtTop((("The attractive numbers up to and including " + (max_4).ToString()) + " are:")));
        long count_5 = 0;
        string line_6 = "";
        long lineCount_7 = 0;
        long i_8 = 1;
        while ((i_8 <= max_4)) {
            long c_9 = countPrimeFactors(i_8);
            if (isPrime(c_9)) {
                line_6 = (line_6 + pad4(i_8));
                count_5 = (count_5 + 1);
                lineCount_7 = (lineCount_7 + 1);
                if ((lineCount_7 == 20)) {
                    Console.WriteLine(_fmtTop(line_6));
                    line_6 = "";
                    lineCount_7 = 0;
                }
            }
            i_8 = (i_8 + 1);
        };
        if ((lineCount_7 > 0)) {
            Console.WriteLine(_fmtTop(line_6));
        };
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
