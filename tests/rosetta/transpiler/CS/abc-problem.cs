// Generated by Mochi 0.10.40 on 2025-07-25 19:02 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static string[] fields(string s) {
        string[] res_0 = new string[]{};
        string cur_1 = "";
        long i_2 = 0;
        while ((i_2 < s.Length)) {
            string c_3 = s.Substring((int)(i_2), (int)((i_2 + 1) - i_2));
            if ((c_3 == " ")) {
                if ((cur_1.Length > 0)) {
                    res_0 = (Enumerable.ToArray(Enumerable.Append(res_0, cur_1)));
                    cur_1 = "";
                }
            } else {
                cur_1 = (cur_1 + c_3);
            }
            i_2 = (i_2 + 1);
        };
        if ((cur_1.Length > 0)) {
            res_0 = (Enumerable.ToArray(Enumerable.Append(res_0, cur_1)));
        };
        return res_0;
    }

    static bool canSpell(string word, string[] blks) {
        if ((word.Length == 0)) {
            return true;
        };
        var c_4 = word.Substring((int)(0), (int)(1 - 0)).ToLower();
        long i_5 = 0;
        while ((i_5 < blks.Length)) {
            string b_6 = blks[i_5];
            if (((c_4 == b_6.Substring((int)(0), (int)(1 - 0)).ToLower()) || (c_4 == b_6.Substring((int)(1), (int)(2 - 1)).ToLower()))) {
                string[] rest_7 = new string[]{};
                long j_8 = 0;
                while ((j_8 < blks.Length)) {
                    if ((j_8 != i_5)) {
                        rest_7 = (Enumerable.ToArray(Enumerable.Append(rest_7, blks[j_8])));
                    }
                    j_8 = (j_8 + 1);
                }
                if (canSpell(word.Substring((int)(1), (int)(word.Length - 1)), rest_7)) {
                    return true;
                }
            }
            i_5 = (i_5 + 1);
        };
        return false;
    }

    static Func<string, bool> newSpeller(string blocks) {
        string[] bl_9 = fields(blocks);
        return (string w) => canSpell(w, bl_9);
    }

    static void main() {
        Func<string, bool> sp_10 = newSpeller("BO XK DQ CP NA GT RE TG QD FS JW HU VI AN OB ER FS LY PC ZM");
        foreach (var word_11 in new string[]{"A", "BARK", "BOOK", "TREAT", "COMMON", "SQUAD", "CONFUSE"}) {
            Console.WriteLine(_fmtTop(((word_11 + " ") + (sp_10(word_11)).ToString())));
        };
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
