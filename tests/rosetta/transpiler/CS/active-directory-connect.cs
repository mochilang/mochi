// Generated by Mochi 0.10.39 on 2025-07-24 15:41 +0700
using System;
using System.Collections;
using System.Collections.Generic;

struct LDAPClient {
    public string Base;
    public string Host;
    public int Port;
    public bool UseSSL;
    public string BindDN;
    public string BindPassword;
    public string UserFilter;
    public string GroupFilter;
    public string[] Attributes;
    public override string ToString() => $"LDAPClient {{Base = \"{Base}\", Host = \"{Host}\", Port = {Port}, UseSSL = {UseSSL}, BindDN = \"{BindDN}\", BindPassword = \"{BindPassword}\", UserFilter = \"{UserFilter}\", GroupFilter = \"{GroupFilter}\", Attributes = {Attributes}}}";
}
class Program {
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static bool connect(LDAPClient client) {
        return ((client.Host != "") && (client.Port > 0));
    }

    static void main() {
        LDAPClient client = new LDAPClient{Base = "dc=example,dc=com", Host = "ldap.example.com", Port = 389, UseSSL = false, BindDN = "uid=readonlyuser,ou=People,dc=example,dc=com", BindPassword = "readonlypassword", UserFilter = "(uid=%s)", GroupFilter = "(memberUid=%s)", Attributes = new string[]{"givenName", "sn", "mail", "uid"}};
        if (connect(client)) {
            Console.WriteLine(_fmtTop(("Connected to " + client.Host)));
        } else {
            Console.WriteLine(_fmtTop("Failed to connect"));
        };
    }

    static void Main() {
        main();
    }
}
