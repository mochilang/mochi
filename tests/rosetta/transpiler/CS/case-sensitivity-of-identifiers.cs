// Generated by Mochi 0.10.42 on 2025-07-27 16:36 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Numerics;
using System.Collections;

class Program {
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static void main() {
        string pkg_dog_0 = "Salt";
        string Dog_1 = "Pepper";
        string pkg_DOG_2 = "Mustard";
        Func<string, string, string, Dictionary<string, bool>> packageSees = null;
        packageSees = (string d1_3, string d2_4, string d3_5) => {Console.WriteLine(_fmtTop(((((("Package sees: " + d1_3) + " ") + d2_4) + " ") + d3_5))); return new Dictionary<string, bool>{{"pkg_dog", true}, {"Dog", true}, {"pkg_DOG", true}};};
        Dictionary<string, bool> d_6 = packageSees(pkg_dog_0, Dog_1, pkg_DOG_2);
        Console.WriteLine(_fmtTop((("There are " + (d_6.Count).ToString()) + " dogs.\n")));
        string dog_7 = "Benjamin";
        d_6 = packageSees(pkg_dog_0, Dog_1, pkg_DOG_2);
        Console.WriteLine(_fmtTop(((((("Main sees:   " + dog_7) + " ") + Dog_1) + " ") + pkg_DOG_2)));
        d_6["dog"] = true;
        d_6["Dog"] = true;
        d_6["pkg_DOG"] = true;
        Console.WriteLine(_fmtTop((("There are " + (d_6.Count).ToString()) + " dogs.\n")));
        Dog_1 = "Samba";
        d_6 = packageSees(pkg_dog_0, Dog_1, pkg_DOG_2);
        Console.WriteLine(_fmtTop(((((("Main sees:   " + dog_7) + " ") + Dog_1) + " ") + pkg_DOG_2)));
        d_6["dog"] = true;
        d_6["Dog"] = true;
        d_6["pkg_DOG"] = true;
        Console.WriteLine(_fmtTop((("There are " + (d_6.Count).ToString()) + " dogs.\n")));
        string DOG_8 = "Bernie";
        d_6 = packageSees(pkg_dog_0, Dog_1, pkg_DOG_2);
        Console.WriteLine(_fmtTop(((((("Main sees:   " + dog_7) + " ") + Dog_1) + " ") + DOG_8)));
        d_6["dog"] = true;
        d_6["Dog"] = true;
        d_6["pkg_DOG"] = true;
        d_6["DOG"] = true;
        Console.WriteLine(_fmtTop((("There are " + (d_6.Count).ToString()) + " dogs.")));
    }

    static void Main() {
        main();
    }
}
