// Generated by Mochi 0.10.40 on 2025-07-25 14:15 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static long[] a_2 = new long[]{0, 0, 0, 0, 0};
    static long[] s_3 = new long[]{};
    static long cap_s_4 = 5;
    static string listStr(long[] xs) {
        string s_0 = "[";
        long i_1 = 0;
        while ((i_1 < xs.Length)) {
            s_0 = (s_0 + (xs[(int)(i_1)]).ToString());
            if (((i_1 + 1) < xs.Length)) {
                s_0 = (s_0 + " ");
            }
            i_1 = (i_1 + 1);
        };
        s_0 = (s_0 + "]");
        return s_0;
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(_fmtTop(("len(a) = " + (a_2.Length).ToString())));
            Console.WriteLine(_fmtTop(("a = " + listStr(a_2))));
            a_2[0] = 3;
            Console.WriteLine(_fmtTop(("a = " + listStr(a_2))));
            Console.WriteLine(_fmtTop(("a[0] = " + (a_2[(int)(0)]).ToString())));
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            Console.WriteLine(_fmtTop(((("len(s) = " + (s_3.Length).ToString()) + "  cap(s) = ") + (cap_s_4).ToString())));
            s_3 = a_2.Skip((int)(0)).Take((int)((5 - 0))).ToArray();
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            a_2[0] = 22;
            s_3[0] = 22;
            Console.WriteLine(_fmtTop(("a = " + listStr(a_2))));
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            s_3 = (Enumerable.ToArray(Enumerable.Append(s_3, 4)));
            s_3 = (Enumerable.ToArray(Enumerable.Append(s_3, 5)));
            s_3 = (Enumerable.ToArray(Enumerable.Append(s_3, 6)));
            cap_s_4 = 10;
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            Console.WriteLine(_fmtTop(((("len(s) = " + (s_3.Length).ToString()) + "  cap(s) = ") + (cap_s_4).ToString())));
            a_2[4] = -1;
            Console.WriteLine(_fmtTop(("a = " + listStr(a_2))));
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            s_3 = new long[]{};
            for (var i_5 = 0; i_5 < 8; i_5++) {
                s_3 = (Enumerable.ToArray(Enumerable.Append(s_3, 0)));
            }
            cap_s_4 = 8;
            Console.WriteLine(_fmtTop(("s = " + listStr(s_3))));
            Console.WriteLine(_fmtTop(((("len(s) = " + (s_3.Length).ToString()) + "  cap(s) = ") + (cap_s_4).ToString())));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
