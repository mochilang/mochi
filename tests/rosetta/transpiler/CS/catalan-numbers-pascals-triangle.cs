// Generated by Mochi 0.10.42 on 2025-07-27 16:46 UTC
using System;
using System.Linq;
using System.Numerics;
using System.Collections;
using System.Collections.Generic;

class Program {
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static long n_0 = 15;
    static long[] t_1 = new long[]{};
    static void Main() {
        for (var __2 = 0; __2 < (n_0 + 2); __2++) {
            t_1 = (Enumerable.ToArray(Enumerable.Append(t_1, 0)));
        }
        t_1[1] = 1;
        for (var i_3 = 1; i_3 < (n_0 + 1); i_3++) {
            long j_4 = i_3;
            while ((j_4 > 1)) {
                t_1[j_4] = (t_1[(int)(j_4)] + t_1[(int)((j_4 - 1))]);
                j_4 = (j_4 - 1);
            }
            t_1[Convert.ToInt64((i_3 + 1))] = t_1[(int)(i_3)];
            j_4 = (i_3 + 1);
            while ((j_4 > 1)) {
                t_1[j_4] = (t_1[(int)(j_4)] + t_1[(int)((j_4 - 1))]);
                j_4 = (j_4 - 1);
            }
            long cat_5 = (t_1[(int)((i_3 + 1))] - t_1[(int)(i_3)]);
            if ((i_3 < 10)) {
                Console.WriteLine(_fmtTop((((" " + (i_3).ToString()) + " : ") + (cat_5).ToString())));
            } else {
                Console.WriteLine(_fmtTop((((i_3).ToString() + " : ") + (cat_5).ToString())));
            }
        }
    }
}
