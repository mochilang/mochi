(* Generated by Mochi transpiler v0.10.42 on 2025-07-27 17:37 UTC *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0

exception Return

let rec doIt p =
  let __ret = ref 0 in
  (try
  let b = ref (0) in
  if (List.mem_assoc "b" p) then (
  b := (try List.assoc ("b") p with Not_found -> (Obj.magic 0));
  );
  __ret := (Obj.magic ((((try List.assoc ("a") p with Not_found -> (Obj.magic 0)) + !b) + (try List.assoc ("c") p with Not_found -> (Obj.magic 0)))) : int); raise Return
  with Return -> !__ret)

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let p = ref ([]) in
  p := (("a", 1) :: List.remove_assoc "a" !p);
  p := (("c", 9) :: List.remove_assoc "c" !p);
  print_endline ((string_of_int (doIt (!p))));
    !__ret
  with Return -> !__ret)

let () =
  ignore (main ());
