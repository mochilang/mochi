(* Generated by Mochi transpiler v0.10.40 on 2025-07-26 05:05 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec angleDiff b1 b2 =
  let __ret = ref 0.0 in
  (try
  let b1 = (Obj.magic b1 : float) in
  let b2 = (Obj.magic b2 : float) in
  let d = (b2 -. b1) in
  if (d < (float_of_int (0) -. 180.0)) then (
  __ret := (Obj.magic ((d +. 360.0)) : float); raise Return
  );
  if (d > 180.0) then (
  __ret := (Obj.magic ((d -. 360.0)) : float); raise Return
  );
  __ret := (Obj.magic (d) : float); raise Return
  with Return -> !__ret)

let testCases = ref ([[20.0; 45.0]; [(float_of_int (0) -. 45.0); 45.0]; [(float_of_int (0) -. 85.0); 90.0]; [(float_of_int (0) -. 95.0); 90.0]; [(float_of_int (0) -. 45.0); 125.0]; [(float_of_int (0) -. 45.0); 145.0]; [29.4803; (float_of_int (0) -. 88.6381)]; [(float_of_int (0) -. 78.3251); (float_of_int (0) -. 159.036)]])
let () =
  let mem_start = _mem () in
  let start = _now () in
  (try List.iter (fun tc ->
    try
  print_endline (string_of_float (angleDiff (List.nth (tc) (0)) (List.nth (tc) (1))));
    with Continue -> ()) !testCases with Break -> ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
