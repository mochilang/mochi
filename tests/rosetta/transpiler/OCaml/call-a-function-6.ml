(* Generated by Mochi transpiler v0.10.42 on 2025-07-27 17:37 UTC *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0

exception Return

let rec bar a b c =
  let __ret = ref (Obj.magic 0) in
  (try
  let a = (Obj.magic a : int) in
  let b = (Obj.magic b : int) in
  let c = (Obj.magic c : int) in
  print_endline ((((((string_of_int (a)) ^ ", ") ^ (string_of_int (b))) ^ ", ") ^ (string_of_int (c))));
    !__ret
  with Return -> !__ret)

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let args = ref ([]) in
  args := (("a", 3) :: List.remove_assoc "a" !args);
  args := (("b", 2) :: List.remove_assoc "b" !args);
  args := (("c", 1) :: List.remove_assoc "c" !args);
  ignore (bar ((try List.assoc ("a") !args with Not_found -> (Obj.magic 0))) ((try List.assoc ("b") !args with Not_found -> (Obj.magic 0))) ((try List.assoc ("c") !args with Not_found -> (Obj.magic 0))));
    !__ret
  with Return -> !__ret)

let () =
  ignore (main ());
