(* Generated by Mochi transpiler v0.10.42 on 2025-07-27 17:37 UTC *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0

exception Return

let rec mkAdd a =
  let __ret = ref (Obj.magic 0) in
  (try
  let a = (Obj.magic a : int) in
  __ret := (Obj.magic ((fun b ->
  let b = (Obj.magic !b : int) in
  (a + b))) : Obj.t); raise Return
  with Return -> !__ret)

let rec mysum x y =
  let __ret = ref 0 in
  (try
  let x = (Obj.magic x : int) in
  let y = (Obj.magic y : int) in
  __ret := (Obj.magic ((x + y)) : int); raise Return
  with Return -> !__ret)

let rec partialSum x =
  let __ret = ref (Obj.magic 0) in
  (try
  let x = (Obj.magic x : int) in
  __ret := (Obj.magic ((fun y ->
  let y = (Obj.magic !y : int) in
  mysum (x) (y))) : Obj.t); raise Return
  with Return -> !__ret)

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let add2 = mkAdd (2) in
  let add3 = mkAdd (3) in
  print_endline ((((string_of_int (add2 (Obj.repr (5)))) ^ " ") ^ (string_of_int (add3 (Obj.repr (6))))));
  let partial = partialSum (13) in
  print_endline ((string_of_int (partial (Obj.repr (5)))));
    !__ret
  with Return -> !__ret)

let () =
  ignore (main ());
