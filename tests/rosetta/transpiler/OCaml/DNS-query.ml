(* Generated by Mochi transpiler v0.10.37 on 2025-07-23 15:05 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0

let _lookup_host _host =
  [Obj.repr ([] : string list); nil]

exception Return

let res = _lookup_host ("www.kame.net")
let addrs = List.nth (res) (0)
let err = List.nth (res) (1)
let () =
  if (err = nil) then (
  print_endline (__show (addrs));
  ) else (
  print_endline (__show err);
  );
