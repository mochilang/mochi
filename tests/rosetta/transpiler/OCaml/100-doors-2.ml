(* Generated by Mochi transpiler v0.10.37 on 2025-07-22 20:39 +0700 *)

exception Break
exception Continue

let () =
  let door = ref 1 in
  let incrementer = ref 0 in
  (try for current = 1 to (101 - 1) do
    try
  let line = ref (("Door " ^ string_of_int current) ^ " ") in
  if (current == !door) then (
  line := (!line ^ "Open");
  incrementer := (!incrementer + 1);
  door := ((!door + (2 * !incrementer)) + 1);
  ) else (
  line := (!line ^ "Closed");
  );
  print_endline !line;
    with Continue -> ()
  done with Break -> ());
