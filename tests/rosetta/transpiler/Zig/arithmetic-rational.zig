// Generated by Mochi Zig transpiler on 2025-07-28 08:20 +0700
const std = @import("std");

fn intSqrt(x: i64) i64 {
    if (x < 2) {
        return x;
    }
    var left: i64 = 1;
    var right: i64 = @divTrunc(x, 2);
    var ans: i64 = 0;
    while (left <= right) {
        const mid: i64 = left + @divTrunc(right - left, 2);
        const sq: i64 = mid * mid;
        if (sq == x) {
            return mid;
        }
        if (sq < x) {
            left = mid + 1;
            ans = mid;
        } else {
            right = mid - 1;
        }
    }
    return ans;
}

fn sumRecip(n: i64) i64 {
    var s: i64 = 1;
    const limit: i64 = intSqrt(n);
    var f: i64 = 2;
    while (f <= limit) {
        if (@mod(n, f) == 0) {
            s = s + @divTrunc(n, f);
            const f2: i64 = @divTrunc(n, f);
            if (f2 != f) {
                s = s + f;
            }
        }
        f = f + 1;
    }
    return s;
}

fn mochi_main() void {
    const nums: []i64 = @constCast(&[_]i64{6, 28, 120, 496, 672, 8128, 30240, 32760, 523776})[0..];
    for (nums) |__it0| {
        const n = __it0;
        const s: i64 = sumRecip(n);
        if (@mod(s, n) == 0) {
            const val: i64 = @divTrunc(s, n);
            var perfect: []const u8 = "";
            if (val == 1) {
                perfect = "perfect!";
            }
            _print(_concat_string(_concat_string(_concat_string(_concat_string(_concat_string("Sum of recipr. factors of ", _str(n)), " = "), _str(val)), " exactly "), perfect));
        }
    }
}

pub fn main() void {
    mochi_main();
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    return std.mem.concat(alloc, u8, &[_][]const u8{ lhs, rhs }) catch unreachable;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
