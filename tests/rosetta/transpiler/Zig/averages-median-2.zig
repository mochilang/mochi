// Generated by Mochi Zig transpiler on 2025-08-02 18:10 +0700
const std = @import("std");

fn sel(list_param: []f64, k: i64) f64 {
    var list_var: []f64 = list_param;
    var i: i64 = 0;
    while (i <= k) {
        var min_index: i64 = i;
        var j: i64 = i + 1;
        while (j < @as(i64, @intCast(list_var.len))) {
            if (list_var[@as(usize, @intCast(j))] < list_var[@as(usize, @intCast(min_index))]) {
                min_index = j;
            }
            j = j + 1;
        }
        const tmp: f64 = list_var[@as(usize, @intCast(i))];
        list_var[@as(usize, @intCast(i))] = list_var[@as(usize, @intCast(min_index))];
        list_var[@as(usize, @intCast(min_index))] = tmp;
        i = i + 1;
    }
    return list_var[@as(usize, @intCast(k))];
}

fn median(a: []f64) f64 {
    const arr: []f64 = a;
    const half: i64 = @divTrunc(@as(i64, @intCast(arr.len)), 2);
    const med: f64 = sel(arr, half);
    if (@mod(@as(i64, @intCast(arr.len)), 2) == 0) {
        return (med + arr[@as(usize, @intCast(half - 1))]) / 2.0;
    }
    return med;
}

pub fn main() void {
    std.debug.print("{s}\n", .{_str(median(@constCast(&[_]f64{3.0, 1.0, 4.0, 1.0})[0..]))});
    std.debug.print("{s}\n", .{_str(median(@constCast(&[_]f64{3.0, 1.0, 4.0, 1.0, 5.0})[0..]))});
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
