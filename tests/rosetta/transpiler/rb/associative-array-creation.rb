# Generated by Mochi transpiler v0.10.37 on 2025-07-23 13:37 +0700

def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end

def removeKey(m, k)
  out = {}
  m.each do |key|
    if key != k
      out[key] = m[key]
    end
  end
  return out
end
def main()
  x = nil
  x = {}
  x["foo"] = 3
  y1 = x["bar"]
  ok = x.include?("bar")
  puts(y1)
  puts(ok)
  x = removeKey(x, "foo")
  x = {"foo" => 2, "bar" => 42, "baz" => -1}
  puts(([x["foo"], x["bar"], x["baz"]]).map{ |x| if x.nil? then 'None' elsif x == true then 'True' elsif x == false then 'False' elsif x.respond_to?(:to_h) then '{' + x.to_h.map{ |k,v| "'#{k}': #{v.is_a?(String) ? '\'' + v + '\'' : v.to_s}" }.join(', ') + '}' else x end }.join(' ').rstrip())
end
main()
