# Generated by Mochi transpiler v0.10.42 on 2025-07-28 07:49 +0700

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

def shuffle(xs)
  arr = xs
  i = arr.length - 1
  while i > 0
    j = _now() % (_add(i, 1))
    tmp = arr[i]
    arr[i] = arr[j]
    arr[j] = tmp
    i = i - 1
  end
  return arr
end
shuffle(["Enjoy", "Rosetta", "Code"]).each do |w|
  puts(w)
end
