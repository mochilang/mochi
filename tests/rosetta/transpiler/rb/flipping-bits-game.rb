# Generated by Mochi transpiler v0.10.42 on 2025-07-27 16:02 +0000
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def flipRow(b, r)
    row = 0
    while row < b[r].length
      b[r][row] = 1 - b[r][row]
      row = _add(row, 1)
    end
    return b
  end
  def flipCol(b, c)
    i = 0
    while i < b.length
      b[i][c] = 1 - b[i][c]
      i = _add(i, 1)
    end
    return b
  end
  $board = [[1, 0, 1], [0, 1, 0], [1, 1, 0]]
  $board = flipRow($board, 1)
  $board = flipCol($board, 2)
  puts(($board).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
