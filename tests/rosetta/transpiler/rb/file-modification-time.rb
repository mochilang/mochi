# Generated by Mochi transpiler v0.10.50 on 2025-07-30 21:05 +0700

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

def main()
  old = $fs["input.txt"]
  puts(_add("mod time was: ", (old).to_s))
  mtime = _now()
  mtime = _now()
  $fs["input.txt"] = (mtime).to_i
  puts(_add("mod time now: ", (mtime).to_s))
end
$fs = {"input.txt" => 0}
main()
