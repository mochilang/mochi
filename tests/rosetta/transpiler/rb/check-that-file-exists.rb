# Generated by Mochi transpiler v0.10.39 on 2025-07-24 20:52 +0700

def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.rjust(len, ch)
end

def printStat(fs, path)
  if fs.key?(path)
    if fs[path]
      puts(_add(path, " is a directory"))
    else
      puts(_add(path, " is a file"))
    end
  else
    puts(_add(_add("stat ", path), ": no such file or directory"))
  end
end
def main()
  fs = {}
  fs["docs"] = true
  ["input.txt", "/input.txt", "docs", "/docs"].each do |p|
    printStat(fs, p)
  end
end
main()
