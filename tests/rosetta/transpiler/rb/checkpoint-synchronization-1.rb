# Generated by Mochi transpiler v0.10.39 on 2025-07-24 20:52 +0700

def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.rjust(len, ch)
end

$partList = ["A", "B", "C", "D"]
$nAssemblies = 3
(1...(_add($nAssemblies, 1))).each do |cycle|
  puts(_add("begin assembly cycle ", (cycle).to_s))
  $partList.each do |p|
    puts(_add(p, " worker begins part"))
  end
  $partList.each do |p|
    puts(_add(p, " worker completes part"))
  end
  puts(_add(_add("assemble.  cycle ", (cycle).to_s), " complete"))
end
