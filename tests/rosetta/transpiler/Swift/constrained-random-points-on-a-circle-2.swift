// Generated by Mochi transpiler v0.10.52 on 2025-08-01 22:11:23 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
let nPts = 100
let rMin = 10
let rMax = 15
let span = ((rMax + 1) + rMax)
var poss: [[Int]] = ([] as! [[Int]])
let min2 = (rMin * rMin)
let max2 = (rMax * rMax)
var y: Int = -rMax
while (y <= rMax) {
    var x: Int = -rMax
    while (x <= rMax) {
        let r2 = ((x * x) + (y * y))
        if ((r2 >= min2) && (r2 <= max2)) {
            poss = (_append(poss, ([(x as! Any), (y as! Any)] as! [Int])) as! [[Int]])
        }
        x = ((x + 1) as! Int)
    }
    y = ((y + 1) as! Int)
}
print(_p((_p(Int(((poss).count))) + " possible points")))
var rows: [[String]] = ([] as! [[String]])
var r: Int = 0
while (r < span) {
    var row: [String] = ([] as! [String])
    var c: Int = 0
    while (c < (span * 2)) {
        row = (_append(row, " ") as! [String])
        c = ((c + 1) as! Int)
    }
    rows = (_append(rows, row) as! [[String]])
    r = ((r + 1) as! Int)
}
var u: Int = 0
var seen: [String: Bool] = ([:] as! [String: Bool])
var n: Int = 0
while (n < nPts) {
    var i: Int = (Int(_now()) % Int(((poss).count)))
    let x = (poss[i][0] as! Int)
    let yy = (poss[i][1] as! Int)
    let row = (yy + rMax)
    let col = (Int((x + rMax)) * 2)
    rows[row][col] = "*"
    let key = ((_p(row) + ",") + _p(col))
    if (!seen[key]!) {
        seen[key] = true
        u = ((u + 1) as! Int)
    }
    n = ((n + 1) as! Int)
}
var i2: Int = 0
while (i2 < span) {
    var line: String = ""
    var j: Int = 0
    while (j < (span * 2)) {
        line = ((line + (rows[i2][j] as! String)) as! String)
        j = ((j + 1) as! Int)
    }
    print(_p(line))
    i2 = ((i2 + 1) as! Int)
}
print(_p((_p(u) + " unique points")))
