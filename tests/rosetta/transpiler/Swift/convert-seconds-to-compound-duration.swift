// Generated by Mochi transpiler v0.10.52 on 2025-08-01 22:11:30 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

func timeStr(_ sec: Int) -> String {
    var sec = sec
    var wks: Int = (sec / 604800)
    sec = ((sec % 604800) as! Int)
    var ds: Int = (sec / 86400)
    sec = ((sec % 86400) as! Int)
    var hrs: Int = (sec / 3600)
    sec = ((sec % 3600) as! Int)
    var mins: Int = (sec / 60)
    sec = ((sec % 60) as! Int)
    var res: String = ""
    var comma: Bool = false
    if (wks != 0) {
        res = (((res + _p(wks)) + " wk") as! String)
        comma = true
    }
    if (ds != 0) {
        if comma {
            res = ((res + ", ") as! String)
        }
        res = (((res + _p(ds)) + " d") as! String)
        comma = true
    }
    if (hrs != 0) {
        if comma {
            res = ((res + ", ") as! String)
        }
        res = (((res + _p(hrs)) + " hr") as! String)
        comma = true
    }
    if (mins != 0) {
        if comma {
            res = ((res + ", ") as! String)
        }
        res = (((res + _p(mins)) + " min") as! String)
        comma = true
    }
    if (sec != 0) {
        if comma {
            res = ((res + ", ") as! String)
        }
        res = (((res + _p(sec)) + " sec") as! String)
    }
    return res
}
print(_p(String(describing: timeStr(7259))))
print(_p(String(describing: timeStr(86400))))
print(_p(String(describing: timeStr(6000000))))
