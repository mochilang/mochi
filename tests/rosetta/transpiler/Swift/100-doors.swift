// Generated by Mochi transpiler v0.10.37 on 2025-07-22 23:36:59 GMT+7
import Foundation

var doors: [Any] = ([] as! [Any])
for i in 0..<100 {
    doors = (((doors as! [Any]) + [false]) as! [Bool])
}
for pass in 1..<101 {
    var idx = (Int(pass) - 1)
    while (Int(idx) < 100) {
        doors[idx] = (!(doors[idx] as! Bool))
        idx = (Int(idx) + Int(pass))
    }
}
for row in 0..<10 {
    var line = ""
    for col in 0..<10 {
        let idx = ((Int(row) * 10) + Int(col))
        if (doors[idx] as! Bool) {
            line = (String(line) + "1")
        } else {
            line = (String(line) + "0")
        }
        if (Int(col) < 9) {
            line = (String(line) + " ")
        }
    }
    print(line)
}
