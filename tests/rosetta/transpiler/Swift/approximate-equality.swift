// Generated by Mochi transpiler v0.10.40 on 2025-07-25 10:53:33 GMT+7
import Foundation

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func abs(_ x: Double) -> Double {
        if (x < 0.0) {
            return (-x as! Double)
        }
        return (x as! Double)
    }
    func maxf(_ a: Double, _ b: Double) -> Double {
        if (a > b) {
            return (a as! Double)
        }
        return (b as! Double)
    }
    func isClose(_ a: Double, _ b: Double) -> Bool {
        let relTol = 0.000000001
        let t = Double(abs(((a - b) as! Double)))
        let u = (relTol * Double(maxf(Double(abs((a as! Double))), Double(abs((b as! Double))))))
        return ((t <= u) as! Bool)
    }
    func sqrtApprox(_ x: Double) -> Double {
        var guess = (x as! Double)
        var i = 0
        while (i < 10) {
            guess = ((Double((guess + (x / guess))) / 2.0) as! Double)
            i = ((i + 1) as! Int)
        }
        return (guess as! Double)
    }
    func main() {
        let root2 = Double(sqrtApprox(2.0))
        let pairs = ([([100000000000000.02, 100000000000000.02] as! [Double]), ([100.01, 100.011] as! [Double]), ([(10000000000000.002 / 10000.0), 1000000000.0000001] as! [Double]), ([0.001, 0.0010000001] as! [Double]), ([0.000000000000000000000101, 0.0] as! [Double]), ([(root2 * root2), 2.0] as! [Double]), ([(Double(-root2) * root2), -2.0] as! [Double]), ([100000000000000000.0, 100000000000000000.0] as! [Double]), ([3.141592653589793, 3.141592653589793] as! [Double])] as! [[Double]])
        for pair in pairs {
            let a = Double(pair[0])
            let b = Double(pair[1])
            let s = String(describing: (Bool(isClose((a as! Double), (b as! Double))) ? "≈" : "≉"))
            print(((((String(describing: a) + " ") + s) + " ") + String(describing: b)))
        }
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
