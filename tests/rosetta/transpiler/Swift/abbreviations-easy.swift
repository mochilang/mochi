// Generated by Mochi transpiler v0.10.56 on 2025-08-04 09:52:50 GMT+7
import Foundation

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func fields(_ s: String) -> [String] {
        var words: [String] = ([] as! [String])
        var cur: String = ""
        var i: Int = 0
        while (i < Int(((s).count))) {
            let ch = String(describing: String(Array(String(describing: (s as! String)))[Int(i)..<Int((i &+ 1))]))
            if (((ch == " ") || (ch == "\n")) || (ch == "\t")) {
                if (Int(((cur).count)) > 0) {
                    words = (_append(words, cur) as! [String])
                    cur = ""
                }
            } else {
                cur = ((cur + ch) as! String)
            }
            i = Int((i &+ 1))
        }
        if (Int(((cur).count)) > 0) {
            words = (_append(words, cur) as! [String])
        }
        return words
    }
    func padRight(_ s: String, _ width: Int) -> String {
        var out: String = s
        var i: Int = Int(((s).count))
        while (i < width) {
            out = ((out + " ") as! String)
            i = Int((i &+ 1))
        }
        return out
    }
    func join(_ xs: [String], _ sep: String) -> String {
        var res: String = ""
        var i: Int = 0
        while (i < Int(((xs).count))) {
            if (i > 0) {
                res = ((res + sep) as! String)
            }
            res = ((res + (xs[i] as! String)) as! String)
            i = Int((i &+ 1))
        }
        return res
    }
    func validate(_ commands: [String], _ words: [String], _ mins: [Int]) -> [String] {
        var results: [String] = ([] as! [String])
        if (Int(((words).count)) == 0) {
            return results
        }
        var wi: Int = 0
        while (wi < Int(((words).count))) {
            let w = (words[wi] as! String)
            var found: Bool = false
            let wlen = Int(((w).count))
            var ci: Int = 0
            while (ci < Int(((commands).count))) {
                let cmd = (commands[ci] as! String)
                if (((Int(mins[ci]) != 0) && (wlen >= Int(mins[ci]))) && (wlen <= Int(((cmd).count)))) {
                    let c = String(describing: ((cmd as! String).uppercased()))
                    let ww = String(describing: ((w as! String).uppercased()))
                    if (String(describing: String(Array(String(describing: (c as! String)))[0..<Int(wlen)])) == ww) {
                        results = (_append(results, c) as! [String])
                        found = true
                        break
                    }
                }
                ci = Int((ci &+ 1))
            }
            if (!found) {
                results = (_append(results, "*error*") as! [String])
            }
            wi = Int((wi &+ 1))
        }
        return results
    }
    func main() {
        let table = (((((("Add ALTer  BAckup Bottom  CAppend Change SCHANGE  CInsert CLAst COMPress Copy " + "COUnt COVerlay CURsor DELete CDelete Down DUPlicate Xedit EXPand EXTract Find ") + "NFind NFINDUp NFUp CFind FINdup FUp FOrward GET Help HEXType Input POWerinput ") + " Join SPlit SPLTJOIN  LOAD  Locate CLocate  LOWercase UPPercase  LPrefix MACRO ") + "MErge MODify MOve MSG Next Overlay PARSE PREServe PURge PUT PUTD  Query  QUIT ") + "READ  RECover REFRESH RENum REPeat  Replace CReplace  RESet  RESTore  RGTLEFT ") + "RIght LEft  SAVE  SET SHift SI  SORT  SOS  STAck STATus  TOP TRAnsfer TypeUp ")
        let commands: [String] = (fields((table as! String)) as! [String])
        var mins: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < Int(((commands).count))) {
            var count: Int = 0
            var j: Int = 0
            let cmd = (commands[i] as! String)
            while (j < Int(((cmd).count))) {
                let ch = String(describing: String(Array(String(describing: (cmd as! String)))[Int(j)..<Int((j &+ 1))]))
                if ((ch >= "A") && (ch <= "Z")) {
                    count = Int((count &+ 1))
                }
                j = Int((j &+ 1))
            }
            mins = (_append(mins, count) as! [Int])
            i = Int((i &+ 1))
        }
        let sentence = "riG   rePEAT copies  put mo   rest    types   fup.    6       poweRin"
        let words: [String] = (fields((sentence as! String)) as! [String])
        let results: [String] = (validate((commands as! [String]), (words as! [String]), (mins as! [Int])) as! [String])
        var out1: String = "user words:  "
        var k: Int = 0
        while (k < Int(((words).count))) {
            out1 = (((out1 + String(describing: padRight((words[k] as! String), Int((((results[k] as! String)).count))))) + " ") as! String)
            k = Int((k &+ 1))
        }
        print(_p(out1))
        print(_p(("full words:  " + String(describing: join((results as! [String]), " ")))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
