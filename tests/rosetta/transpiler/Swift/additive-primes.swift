// Generated by Mochi transpiler v0.10.39 on 2025-07-24 20:09:43 GMT+7
import Foundation

func isPrime(_ n: Int) -> Bool {
    if (Int(n) < 2) {
        return false
    }
    if ((Int(n) % 2) == 0) {
        return ((Int(n) == 2) as! Bool)
    }
    if ((Int(n) % 3) == 0) {
        return ((Int(n) == 3) as! Bool)
    }
    var d = 5
    while ((Int(d) * Int(d)) <= Int(n)) {
        if ((Int(n) % Int(d)) == 0) {
            return false
        }
        d = ((Int(d) + 2) as! Int)
        if ((Int(n) % Int(d)) == 0) {
            return false
        }
        d = ((Int(d) + 4) as! Int)
    }
    return true
}
func sumDigits(_ n: Int) -> Int {
    var s = 0
    var x = Int(n)
    while (Int(x) > 0) {
        s = ((Int(s) + (Int(x) % 10)) as! Int)
        x = Int((Int(x) / 10))
    }
    return Int(s)
}
func pad(_ n: Int) -> String {
    if (Int(n) < 10) {
        return (("  " + String(String(describing: Int(n)))) as! String)
    }
    if (Int(n) < 100) {
        return ((" " + String(String(describing: Int(n)))) as! String)
    }
    return String(String(describing: Int(n)))
}
func main() {
    print("Additive primes less than 500:")
    var count = 0
    var line = ""
    var lineCount = 0
    var i = 2
    while (Int(i) < 500) {
        if (Bool(isPrime(Int(i))) && Bool(isPrime(Int(sumDigits(Int(i)))))) {
            count = ((Int(count) + 1) as! Int)
            line = (((String(line) + String(pad(Int(i)))) + "  ") as! String)
            lineCount = ((Int(lineCount) + 1) as! Int)
            if (Int(lineCount) == 10) {
                print(String(String(Array(String(line))[0..<((Int(((String(line)).count)) - 2) as! Int)])))
                line = ""
                lineCount = 0
            }
        }
        if (Int(i) > 2) {
            i = ((Int(i) + 2) as! Int)
        } else {
            i = ((Int(i) + 1) as! Int)
        }
    }
    if (Int(lineCount) > 0) {
        print(String(String(Array(String(line))[0..<((Int(((String(line)).count)) - 2) as! Int)])))
    }
    print((String(String(describing: Int(count))) + " additive primes found."))
}
_ = main()
