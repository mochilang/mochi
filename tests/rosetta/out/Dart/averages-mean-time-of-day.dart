// Generated by Mochi compiler v0.10.26 on 2025-07-16T12:35:54Z
import 'dart:io';
import 'dart:convert';

var PI = 3.141592653589793;

double sinApprox(double x) {
  var term = x;
  var sum = x;
  var n = 1;
  while (n <= 8) {
    var denom = double.parse(((2 * n) * (2 * n + 1)));
    term = -term * x * x / (denom as num);
    sum = (sum as num) + term;
    n = n + 1;
  }
  return sum;
}

double cosApprox(double x) {
  var term = 1;
  var sum = 1;
  var n = 1;
  while (n <= 8) {
    var denom = double.parse(((2 * n - 1) * (2 * n)));
    term = -term * x * x / (denom as num);
    sum = (sum as num) + term;
    n = n + 1;
  }
  return sum;
}

double atanApprox(double x) {
  if (x > 1) {
    return PI / 2 - x / (x * x + 0.28);
  }
  if (x < (-1)) {
    return -PI / 2 - x / (x * x + 0.28);
  }
  return x / (1 + 0.28 * x * x);
}

double atan2Approx(double y, double x) {
  if (x > 0) {
    return atanApprox(y / x);
  }
  if (x < 0) {
    if (y >= 0) {
      return atanApprox(y / x) + PI;
    }
    return atanApprox(y / x) - PI;
  }
  if (y > 0) {
    return PI / 2;
  }
  if (y < 0) {
    return -PI / 2;
  }
  return 0;
}

int digit(String ch) {
  var digits = '0123456789';
  var i = 0;
  while (i < digits.length) {
    if (digits.toString().substring(i, i + 1) == ch) {
      return i;
    }
    i = i + 1;
  }
  return 0;
}

int parseTwo(String s, int idx) {
  return digit(s.toString().substring(idx, idx + 1)) * 10 + digit(s.toString().substring(idx + 1, idx + 2));
}

double parseSec(String s) {
  var h = parseTwo(s, 0);
  var m = parseTwo(s, 3);
  var sec = parseTwo(s, 6);
  return double.parse(((h * 60 + m) * 60 + sec));
}

String pad(int n) {
  if (n < 10) {
    return '0' + n.toString();
  }
  return n.toString();
}

String meanTime(List<String> times) {
  var ssum = 0;
  var csum = 0;
  var i = 0;
  while (i < times.length) {
    var sec = parseSec(times[i]);
    var ang = sec * 2 * PI / 86400;
    ssum = ssum + sinApprox(ang);
    csum = csum + cosApprox(ang);
    i = i + 1;
  }
  var theta = atan2Approx(ssum, csum);
  var frac = theta / (2 * PI);
  while (frac < 0) {
    frac = frac + 1;
  }
  var total = frac * 86400;
  var si = int.parse(total);
  var h = int.parse((si / 3600));
  var m = int.parse(((si % 3600) / 60));
  var s = int.parse((si % 60));
  return pad(h) + ':' + pad(m) + ':' + pad(s);
}

void _main() {
  var inputs = ['23:00:17', '23:40:20', '00:12:45', '00:17:19'];
  print(meanTime(inputs));
}

void main() {
  _main();
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

num _sum(dynamic v) {
    Iterable<dynamic>? list;
    if (v is Iterable) list = v;
    else if (v is Map && v['items'] is Iterable) list = (v['items'] as Iterable);
    else if (v is Map && v['Items'] is Iterable) list = (v['Items'] as Iterable);
    else { try { var it = (v as dynamic).items; if (it is Iterable) list = it; } catch (_) {} }
    if (list == null) return 0;
    num s = 0;
    for (var n in list) s += (n as num);
    return s;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
