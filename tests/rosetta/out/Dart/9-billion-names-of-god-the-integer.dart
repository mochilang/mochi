// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:33:24Z
List<int> bigTrim(List<int> a) {
  var n = a.length;
  while ((n as num) > 1 && a[(n as num) - 1] == 0) {
    a = a.sublist(0, (n as num) - 1);
    n = (n as num) - 1;
  }
  return a;
}

List<int> bigFromInt(int x) {
  if (x == 0) {
    return [0];
  }
  List<int> digits = [];
  var n = x;
  while ((n as num) > 0) {
    digits = List.from(digits)..add((n as num) % 10);
    n = (n as num) / 10;
  }
  return digits;
}

List<int> bigAdd(List<int> a, List<int> b) {
  List<int> res = [];
  num carry = 0;
  num i = 0;
  while ((i as num) < a.length || (i as num) < b.length || (carry as num) > 0) {
    num av = 0;
    if ((i as num) < a.length) {
      av = a[i];
    }
    num bv = 0;
    if ((i as num) < b.length) {
      bv = b[i];
    }
    var s = ((av as num) + (bv as num) as num) + (carry as num);
    res = List.from(res)..add((s as num) % 10);
    carry = (s as num) / 10;
    i = (i as num) + 1;
  }
  return bigTrim(res);
}

List<int> bigSub(List<int> a, List<int> b) {
  List<int> res = [];
  num borrow = 0;
  num i = 0;
  while ((i as num) < a.length) {
    var av = a[i];
    num bv = 0;
    if ((i as num) < b.length) {
      bv = b[i];
    }
    var diff = ((av as num) - (bv as num) as num) - (borrow as num);
    if ((diff as num) < 0) {
      diff = (diff as num) + 10;
      borrow = 1;
    }
    else {
      borrow = 0;
    }
    res = List.from(res)..add(diff);
    i = (i as num) + 1;
  }
  return bigTrim(res);
}

String bigToString(List<int> a) {
  var s = '';
  var i = a.length - 1;
  while ((i as num) >= 0) {
    s = s + a[i].toString();
    i = (i as num) - 1;
  }
  return s;
}

int minInt(int a, int b) {
  if (a < b) {
    return a;
  }
  else {
    return b;
  }
}

List<List<int>> cumu(int n) {
  List<List<List<int>>> cache = [
    [bigFromInt(1)],
  ];
  var y = 1;
  while ((y as num) <= n) {
    List<List<int>> row = [bigFromInt(0)];
    var x = 1;
    while (x <= (y as num)) {
      var val = cache[(y as num) - x][minInt(x, (y as num) - x)];
      row = List.from(row)..add(bigAdd(row[row.length - 1], val));
      x = x + 1;
    }
    cache = List.from(cache)..add(row);
    y = (y as num) + 1;
  }
  return cache[n];
}

List<String> row(int n) {
  var e = cumu(n);
  List<String> out = [];
  num i = 0;
  while ((i as num) < n) {
    var diff = bigSub(e[(i as num) + 1], e[i]);
    out = List.from(out)..add(bigToString(diff));
    i = (i as num) + 1;
  }
  return out;
}

var x = 1;

void main() {
  print('rows:');
  while (x < 11) {
    var r = row(x);
    var line = '';
    num i = 0;
    while ((i as num) < r.length) {
      line = line + ' ' + r[i] + ' ';
      i = (i as num) + 1;
    }
    print(line);
    x = x + 1;
  }
  print('');
  print('sums:');
  for (var num in [23, 123, 1234]) {
    var r = cumu(num);
    print(num.toString() + ' ' + bigToString(r[r.length - 1]));
  }
}
