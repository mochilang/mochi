// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:29:31Z
List<int> primesUpTo(int n) {
  List<bool> sieve = [];
  var i = 0;
  while (i.toString().compareTo(n.toString()) <= 0) {
    sieve = List.from(sieve)..add(true);
    i = (i as num) + 1;
  }
  var p = 2;
  while ((p as num) * (p as num).toString().compareTo(n.toString()) <= 0) {
    if (sieve[p] != null) {
      var m = (p as num) * (p as num);
      while (m.toString().compareTo(n.toString()) <= 0) {
        sieve[m] = false;
        m = (m as num) + (p as num);
      }
    }
    p = (p as num) + 1;
  }
  List<int> res = [];
  var x = 2;
  while (x.toString().compareTo(n.toString()) <= 0) {
    if (sieve[x] != null) {
      res = List.from(res)..add(x);
    }
    x = (x as num) + 1;
  }
  return res;
}

List<int> sortInts(List<int> xs) {
  List<int> res = [];
  var tmp = xs;
  while (tmp.length > 0) {
    var min = tmp[0];
    var idx = 0;
    var i = 1;
    while (i.toString().compareTo(tmp.length.toString()) < 0) {
      if (tmp[i].toString().compareTo(min.toString()) < 0) {
        min = tmp[i];
        idx = i;
      }
      i = (i as num) + 1;
    }
    res = List.from(res)..add(min);
    List<int> out = [];
    var j = 0;
    while (j.toString().compareTo(tmp.length.toString()) < 0) {
      if (j != idx) {
        out = List.from(out)..add(tmp[j]);
      }
      j = (j as num) + 1;
    }
    tmp = out;
  }
  return res;
}

String commatize(int n) {
  var s = n.toString();
  var i = s.length - 3;
  while (i.toString().compareTo(1.toString()) >= 0) {
    s = ((s is String) ? s.substring(0, i) : (s as List).sublist(0, i)) + ',' + ((s is String) ? s.substring(i, s.length) : (s as List).sublist(i, s.length));
    i = (i as num) - 3;
  }
  return s;
}

var primes = primesUpTo(3200000);

Map<String, any> getBrilliant(int digits, int limit, bool countOnly) {
  List<int> brilliant = [];
  var count = 0;
  var pow = 1;
  var next = 999999999999999;
  var k = 1;
  while (k.toString().compareTo(digits.toString()) <= 0) {
    List<int> s = [];
    for (var p in primes) {
      if (p.toString().compareTo((pow as num) * 10.toString()) >= 0) {
        break;
      }
      if (p.toString().compareTo(pow.toString()) > 0) {
        s = List.from(s)..add(p);
      }
    }
    var i = 0;
    while (i.toString().compareTo(s.length.toString()) < 0) {
      var j = i;
      while (j.toString().compareTo(s.length.toString()) < 0) {
        var prod = (s[i] as num) * (s[j] as num);
        if (prod.toString().compareTo(limit.toString()) < 0) {
          if (countOnly) {
            count = (count as num) + 1;
          }
          else {
            brilliant = List.from(brilliant)..add(prod);
          }
        }
        else {
          if (prod.toString().compareTo(next.toString()) < 0) {
            next = prod;
          }
          break;
        }
        j = (j as num) + 1;
      }
      i = (i as num) + 1;
    }
    pow = (pow as num) * 10;
    k = (k as num) + 1;
  }
  if (countOnly) {
    return {'bc': count, 'next': next};
  }
  return {'bc': brilliant, 'next': next};
}

void main() {
  print('First 100 brilliant numbers:');
  var r = getBrilliant(2, 10000, false);
  var br = sortInts(r['bc']);
  br = ((br is String) ? br.substring(0, 100) : (br as List).sublist(0, 100));
  var i = 0;
  while (i.toString().compareTo(br.length.toString()) < 0) {
    print([br[i].toString().padStart(4, ' ') + ' ', false].join(' '));
    if ((((i as num) + 1) as num) % 10 == 0) {
      print(['', true].join(' '));
    }
    i = (i as num) + 1;
  }
  print(['', true].join(' '));
  var k = 1;
  while (k.toString().compareTo(13.toString()) <= 0) {
    var limit = pow(10, k);
    var r2 = getBrilliant(k, limit, true);
    var total = r2['bc'];
    var next = r2['next'];
    var climit = commatize(limit);
    var ctotal = commatize((total as num) + 1);
    var cnext = commatize(next);
    print('First >= ' + climit['padStart'](18, ' ') + ' is ' + ctotal['padStart'](14, ' ') + ' in the series: ' + cnext['padStart'](18, ' '));
    k = (k as num) + 1;
  }
}

void main() {
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

num _sum(dynamic v) {
    Iterable<dynamic>? list;
    if (v is Iterable) list = v;
    else if (v is Map && v['items'] is Iterable) list = (v['items'] as Iterable);
    else if (v is Map && v['Items'] is Iterable) list = (v['Items'] as Iterable);
    else { try { var it = (v as dynamic).items; if (it is Iterable) list = it; } catch (_) {} }
    if (list == null) return 0;
    num s = 0;
    for (var n in list) s += (n as num);
    return s;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
