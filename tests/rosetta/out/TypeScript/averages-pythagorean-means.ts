// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:36Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/averages-pythagorean-means.mochi

function powf(base: number, exp: number): number {
  var result = 1;
  var i = 0;
  while ((i < exp)) {
    result = result * base;
    i = i + 1;
  }
  return result;
}

function nthRoot(x: number, n: number): number {
  var low = 0;
  var high = x;
  var i = 0;
  while ((i < 60)) {
    let mid = (low + high) / 2;
    if ((powf(mid, n) > x)) {
      high = mid;
    } else {
      low = mid;
    }
    i = i + 1;
  }
  return low;
}

function main(): void {
  var sum = 0;
  var sumRecip = 0;
  var prod = 1;
  var n = 1;
  while ((n <= 10)) {
    let f = n;
    sum = sum + f;
    sumRecip = sumRecip + (1 / f);
    prod = prod * f;
    n = n + 1;
  }
  let count = 10;
  let a = sum / count;
  let g = nthRoot(prod, 10);
  let h = count / sumRecip;
  console.log(`A: ${String(a)} G: ${String(g)} H: ${String(h)}`);
  return console.log(`A >= G >= H: ${String((a >= g) && (g >= h))}`);
}

function main(): void {
  main();
}
main();
