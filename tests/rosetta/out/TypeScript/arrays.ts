// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:34Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/arrays.mochi

function listStr(xs: number[]): string {
  var s = "[";
  var i = 0;
  while ((i < xs.length)) {
    s = `${s}${String(xs[i])}`;
    if (((i + 1) < xs.length)) {
      s = `${s} `;
    }
    i = i + 1;
  }
  s = `${s}]`;
  return s;
}

var a: number[];
var cap_s: number;
var s: number[];

function main(): void {
  a = [
    0,
    0,
    0,
    0,
    0,
  ];
  console.log(`len(a) = ${String(a.length)}`);
  console.log(`a = ${listStr(a)}`);
  a[0] = 3;
  console.log(`a = ${listStr(a)}`);
  console.log(`a[0] = ${String(a[0])}`);
  s = a.slice(0, 4);
  cap_s = 5;
  console.log(`s = ${listStr(s)}`);
  console.log(`len(s) = ${String(s.length)}  cap(s) = ${String(cap_s)}`);
  s = a.slice(0, 5);
  console.log(`s = ${listStr(s)}`);
  a[0] = 22;
  s[0] = 22;
  console.log(`a = ${listStr(a)}`);
  console.log(`s = ${listStr(s)}`);
  s = [...s, 4];
  s = [...s, 5];
  s = [...s, 6];
  cap_s = 10;
  console.log(`s = ${listStr(s)}`);
  console.log(`len(s) = ${String(s.length)}  cap(s) = ${String(cap_s)}`);
  a[4] = -1;
  console.log(`a = ${listStr(a)}`);
  console.log(`s = ${listStr(s)}`);
  s = [];
  for (let i: number = 0; i < 8; i++) {
    s = [...s, 0];
  }
  cap_s = 8;
  console.log(`s = ${listStr(s)}`);
  console.log(`len(s) = ${String(s.length)}  cap(s) = ${String(cap_s)}`);
}
main();
