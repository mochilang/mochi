// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:36Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/averages-median-2.mochi

function sel(list: number[], k: number): number {
  var i = 0;
  while ((i <= k)) {
    var minIndex = i;
    var j = i + 1;
    while ((j < list.length)) {
      if ((list[j] < list[minIndex])) {
        minIndex = j;
      }
      j = j + 1;
    }
    let tmp = list[i];
    list[i] = list[minIndex];
    list[minIndex] = tmp;
    i = i + 1;
  }
  return list[k];
}

function median(a: number[]): number {
  var arr = a;
  let half = Math.trunc(arr.length / 2);
  let med = sel(arr, half);
  if (((arr.length % 2) == 0)) {
    return ((med + arr[half - 1]) / 2);
  }
  return med;
}

function main(): void {
  console.log(String(median([
    3,
    1,
    4,
    1,
  ])));
  console.log(String(median([
    3,
    1,
    4,
    1,
    5,
  ])));
}
main();
