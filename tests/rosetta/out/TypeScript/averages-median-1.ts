// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:36Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/averages-median-1.mochi

function sortFloat(xs: number[]): number[] {
  var arr = xs;
  var n = arr.length;
  var i = 0;
  while ((i < n)) {
    var j = 0;
    while ((j < (n - 1))) {
      if ((arr[j] > arr[j + 1])) {
        let tmp = arr[j];
        arr[j] = arr[j + 1];
        arr[j + 1] = tmp;
      }
      j = j + 1;
    }
    i = i + 1;
  }
  return arr;
}

function median(a: number[]): number {
  var arr = sortFloat(a);
  let half = Math.trunc(arr.length / 2);
  var m = arr[half];
  if (((arr.length % 2) == 0)) {
    m = (m + arr[half - 1]) / 2;
  }
  return m;
}

function main(): void {
  console.log(String(median([
    3,
    1,
    4,
    1,
  ])));
  console.log(String(median([
    3,
    1,
    4,
    1,
    5,
  ])));
}
main();
