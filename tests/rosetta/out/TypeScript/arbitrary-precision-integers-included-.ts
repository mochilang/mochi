// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:33Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/arbitrary-precision-integers-included-.mochi

function pow_int(base: number, exp: number): number {
  var result = 1;
  var b = base;
  var e = exp;
  while ((e > 0)) {
    if (((e % 2) == 1)) {
      result = result * b;
    }
    b = b * b;
    e = Math.trunc(e / 2);
  }
  return result;
}

function pow_big(base: any, exp: number): any {
  var result: any = 1;
  var b: any = base;
  var e = exp;
  while ((e > 0)) {
    if (((e % 2) == 1)) {
      result = result * b;
    }
    b = b * b;
    e = Math.trunc(e / 2);
  }
  return result;
}

var base: any;
var e1: number;
var e2: number;
var s: string;
var x: any;

function main(): void {
  e1 = pow_int(3, 2);
  e2 = pow_int(4, e1);
  base = 5;
  x = pow_big(base, e2);
  s = String(x);
  console.log(
    `5^(4^(3^2)) has ${s.length} digits: ${s.substring(0, (0) + (20))} ... ${
      s.substring(s.length - 20, (s.length - 20) + (s.length))
    }`,
  );
}
main();
