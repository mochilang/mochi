// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:12:35Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/attractive-numbers.mochi

function isPrime(n: number): boolean {
  if ((n < 2)) {
    return false;
  }
  if (((n % 2) == 0)) {
    return (n == 2);
  }
  if (((n % 3) == 0)) {
    return (n == 3);
  }
  var d = 5;
  while (((d * d) <= n)) {
    if (((n % d) == 0)) {
      return false;
    }
    d = d + 2;
    if (((n % d) == 0)) {
      return false;
    }
    d = d + 4;
  }
  return true;
}

function countPrimeFactors(n: number): number {
  if ((n == 1)) {
    return 0;
  }
  if (isPrime(n)) {
    return 1;
  }
  var count = 0;
  var f = 2;
  while (true) {
    if (((n % f) == 0)) {
      count = count + 1;
      n = Math.trunc(n / f);
      if ((n == 1)) {
        return count;
      }
      if (isPrime(n)) {
        f = n;
      }
    } else if ((f >= 3)) {
      f = f + 2;
    } else {
      f = 3;
    }
  }
  return count;
}

function pad4(n: number): string {
  var s = String(n);
  while ((s.length < 4)) {
    s = ` ${s}`;
  }
  return s;
}

function main(): void {
  let max = 120;
  console.log(`The attractive numbers up to and including ${String(max)} are:`);
  var count = 0;
  var line = "";
  var lineCount = 0;
  var i = 1;
  while ((i <= max)) {
    let c = countPrimeFactors(i);
    if (isPrime(c)) {
      line = `${line}${pad4(i)}`;
      count = count + 1;
      lineCount = lineCount + 1;
      if ((lineCount == 20)) {
        console.log(line);
        line = "";
        lineCount = 0;
      }
    }
    i = i + 1;
  }
  return ((lineCount > 0) ? console.log(line) : undefined);
}

function main(): void {
  main();
}
main();
