//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
)

type v map[string]any

// line 4
func sel(list *[]float64, k int) float64 {
	i := 0
	for i <= k {
		minIndex := i
		j := (i + 1)
		for j < len(list) {
			if list[j] < list[minIndex] {
				minIndex = j
			}
			j = (j + 1)
		}
		tmp := list[i]
		list[i] = list[minIndex]
		list[minIndex] = tmp
		i = (i + 1)
	}
	return list[k]
}

// line 23
func median(a []float64) float64 {
	arr := a
	half := int((float64(len(arr)) / float64(2)))
	med := sel(arr, half)
	if (len(arr) % 2) == 0 {
		return ((med + arr[(half-1)]) / 2.0)
	}
	return med
}

func main() {
	fmt.Println(fmt.Sprint(median([]float64{
		3.0,
		1.0,
		4.0,
		1.0,
	})))
	fmt.Println(fmt.Sprint(median([]float64{
		3.0,
		1.0,
		4.0,
		1.0,
		5.0,
	})))
}
