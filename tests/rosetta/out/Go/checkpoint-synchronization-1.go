//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
)

type v map[string]any

func main() {
	partList := []string{
		"A",
		"B",
		"C",
		"D",
	}
	nAssemblies := 3
	for cycle := 1; cycle < (nAssemblies + 1); cycle++ {
		fmt.Println("begin assembly cycle " + fmt.Sprint(cycle))
		for _, p := range partList {
			fmt.Println(p + " worker begins part")
		}
		for _, p := range partList {
			fmt.Println(p + " worker completes part")
		}
		fmt.Println("assemble.  cycle " + fmt.Sprint(cycle) + " complete")
	}
}
