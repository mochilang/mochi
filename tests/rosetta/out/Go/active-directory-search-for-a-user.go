//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
)

type v map[string]any

// line 3
func search_user(directory map[string][]string, username string) []string {
	return directory[username]
}

// line 7
func main() {
	client := Client{
		Base:        "dc=example,dc=com",
		Host:        "ldap.example.com",
		Port:        389,
		GroupFilter: "(memberUid=%s)",
	}
	_ = client
	directory := Directory{
		Username: []string{"admins", "users"},
		John:     []string{"users"},
	}
	groups := search_user(map[string][]string(directory), "username")
	if len(groups) > 0 {
		out := "Groups: ["
		i := 0
		for i < len(groups) {
			out = out + "\"" + groups[i] + "\""
			if i < (len(groups) - 1) {
				out = out + ", "
			}
			i = (i + 1)
		}
		out = out + "]"
		fmt.Println(out)
	} else {
		fmt.Println("User not found")
	}
}

func main() {
	main()
}
