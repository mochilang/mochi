//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
)

type v map[string]any

// line 18
func setChar(s string, idx int, ch string) string {
	return string([]rune(s)[0:idx]) + ch + string([]rune(s)[(idx+1):len(s)])
}

func main() {
	width := 81
	height := 5
	var lines []string = []string{}
	for i := 0; i < height; i++ {
		row := ""
		j := 0
		for j < width {
			row = row + "*"
			j = (j + 1)
		}
		lines = append(lines, row)
	}
	var stack []map[string]int = []map[string]int{map[string]int{
		"start": 0,
		"len":   width,
		"index": 1,
	}}
	for len(stack) > 0 {
		frame := stack[(len(stack) - 1)]
		stack = stack[0:(len(stack) - 1)]
		start := frame["start"]
		lenSeg := frame["len"]
		index := frame["index"]
		seg := int((float64(lenSeg) / float64(3)))
		if seg == 0 {
			continue
		}
		i := index
		for i < height {
			j := (start + seg)
			for j < (start + (2 * seg)) {
				lines[i] = setChar(lines[i], j, " ")
				j = (j + 1)
			}
			i = (i + 1)
		}
		stack = append(stack, map[string]int{
			"start": start,
			"len":   seg,
			"index": (index + 1),
		})
		stack = append(stack, map[string]int{
			"start": (start + (seg * 2)),
			"len":   seg,
			"index": (index + 1),
		})
	}
	for _, line := range lines {
		fmt.Println(line)
	}
}
