//go:build ignore

// Generated by Mochi compiler v0.10.30 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"mochi/runtime/data"
	"reflect"
	"strings"
)

type v map[string]any

// line 1
func fields(s string) []string {
	var words []string = []string{}
	cur := ""
	i := 0
	for i < _count(s) {
		ch := string([]rune(s)[i:(i + 1)])
		if ((ch == " ") || (ch == "\n")) || (ch == "\t") {
			if _count(cur) > 0 {
				words = append(words, cur)
				cur = ""
			}
		} else {
			cur = cur + ch
		}
		i = (i + 1)
	}
	if _count(cur) > 0 {
		words = append(words, cur)
	}
	return words
}

// line 23
func padRight(s string, width int) string {
	out := s
	i := _count(s)
	for i < width {
		out = out + " "
		i = (i + 1)
	}
	return out
}

// line 33
func join(xs []string, sep string) string {
	res := ""
	i := 0
	for i < _count(_toAnySlice(xs)) {
		if i > 0 {
			res = res + sep
		}
		res = res + xs[i]
		i = (i + 1)
	}
	return res
}

// line 46
func validate(commands []string, words []string, mins []int) []string {
	var results []string = []string{}
	if _count(_toAnySlice(words)) == 0 {
		return results
	}
	wi := 0
	for wi < _count(_toAnySlice(words)) {
		w := words[wi]
		found := false
		wlen := _count(w)
		ci := 0
		for ci < _count(_toAnySlice(commands)) {
			cmd := commands[ci]
			if ((mins[ci] != 0) && (wlen >= mins[ci])) && (wlen <= _count(cmd)) {
				c := strings.ToUpper(cmd)
				ww := strings.ToUpper(w)
				if string([]rune(c)[0:wlen]) == ww {
					results = append(results, c)
					found = true
					break
				}
			}
			ci = (ci + 1)
		}
		if !(found) {
			results = append(results, "*error*")
		}
		wi = (wi + 1)
	}
	return results
}

// line 78
func mainFn() {
	table := "Add ALTer  BAckup Bottom  CAppend Change SCHANGE  CInsert CLAst COMPress Copy " + "COUnt COVerlay CURsor DELete CDelete Down DUPlicate Xedit EXPand EXTract Find " + "NFind NFINDUp NFUp CFind FINdup FUp FOrward GET Help HEXType Input POWerinput " + " Join SPlit SPLTJOIN  LOAD  Locate CLocate  LOWercase UPPercase  LPrefix MACRO " + "MErge MODify MOve MSG Next Overlay PARSE PREServe PURge PUT PUTD  Query  QUIT " + "READ  RECover REFRESH RENum REPeat  Replace CReplace  RESet  RESTore  RGTLEFT " + "RIght LEft  SAVE  SET SHift SI  SORT  SOS  STAck STATus  TOP TRAnsfer TypeUp "
	commands := fields(table)
	var mins []int = []int{}
	i := 0
	for i < _count(_toAnySlice(commands)) {
		count := 0
		j := 0
		cmd := commands[i]
		for j < _count(cmd) {
			ch := string([]rune(cmd)[j:(j + 1)])
			if (ch >= "A") && (ch <= "Z") {
				count = (count + 1)
			}
			j = (j + 1)
		}
		mins = append(mins, count)
		i = (i + 1)
	}
	sentence := "riG   rePEAT copies  put mo   rest    types   fup.    6       poweRin"
	words := fields(sentence)
	results := validate(commands, words, mins)
	out1 := "user words:  "
	k := 0
	for k < _count(_toAnySlice(words)) {
		out1 = out1 + padRight(words[k], _count(results[k])) + " "
		k = (k + 1)
	}
	fmt.Println(any(out1))
	fmt.Println(any("full words:  " + join(results, " ")))
}

func main() {
	mainFn()
}

func _count(v any) int {
	if g, ok := v.(*data.Group); ok {
		return len(g.Items)
	}
	switch s := v.(type) {
	case []any:
		return len(s)
	case []int:
		return len(s)
	case []float64:
		return len(s)
	case []string:
		return len(s)
	case []bool:
		return len(s)
	case []map[string]any:
		return len(s)
	case map[string]any:
		return len(s)
	case string:
		return len([]rune(s))
	}
	rv := reflect.ValueOf(v)
	if rv.Kind() == reflect.Slice || rv.Kind() == reflect.Array {
		return rv.Len()
	}
	panic("count() expects list or group")
}

func _toAnySlice[T any](s []T) []any {
	out := make([]any, len(s))
	for i, v := range s {
		out[i] = v
	}
	return out
}
