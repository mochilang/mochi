# Generated by Mochi compiler v0.10.26 on 2025-07-16T11:34:06Z
defmodule Main do
  def main do
    # door :: integer()
    door = 1
    _ = door
    # incrementer :: integer()
    incrementer = 0
    _ = incrementer

    {door, incrementer} =
      Enum.reduce(1..(101 - 1), {door, incrementer}, fn current, {door, incrementer} ->
        line = ("Door " <> to_string(current)) <> " "
        _ = line

        if current == door do
          line = line <> "Open"
          incrementer = incrementer + 1
          door = door + 2 * incrementer + 1
        else
          line = line <> "Closed"
        end

        IO.inspect(line)
        {door, incrementer}
      end)

    _ = door
    _ = incrementer
  end
end

Main.main()
