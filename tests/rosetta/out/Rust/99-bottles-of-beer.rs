// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
fn main() {
    fn bottles(n: i32) -> &'static str {
        if n == 0 {
            return "No more bottles";
        }
        if n == 1 {
            return "1 bottle";
        }
        return format!("{}{}", n.to_string(), " bottles");
    }
    fn main() -> () {
        let mut i = 99;
        while i > 0 {
            println!("{}", vec![format!("{}", format!("{}{}", bottles(i), " of beer on the wall"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
            println!("{}", vec![format!("{}", format!("{}{}", bottles(i), " of beer"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
            println!("Take one down, pass it around");
            println!("{}", vec![format!("{}", format!("{}{}", bottles(i - 1), " of beer on the wall"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
            i -= 1;
        }
    }
    main();
}
