// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
fn main() {
    fn printStat(fs: std::collections::HashMap<&'static str, bool>, path: &'static str) -> () {
        if fs.contains_key(&path) {
            if *fs.get(&path).unwrap() {
                println!("{}", vec![format!("{}", format!("{}{}", path, " is a directory"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
            } else {
                println!("{}", vec![format!("{}", format!("{}{}", path, " is a file"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
            }
        } else {
            println!("{}", vec![format!("{}", format!("{}{}", format!("{}{}", "stat ", path), ": no such file or directory"))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
        }
    }
    fn main() -> () {
        let mut fs: std::collections::HashMap<&'static str, bool> = { let mut m = std::collections::BTreeMap::new(); m };
        fs.insert("docs", true);
        for p in vec!["input.txt", "/input.txt", "docs", "/docs"] {
            printStat(fs, p);
        }
    }
    main();
}
