// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:31:36Z
using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        var doors = new List<dynamic>();
        for (var i = 0; i < 100; i++)
        {
            doors = new List<dynamic>(doors) { false };
        }
        for (var pass = 1; pass < 101; pass++)
        {
            var idx = (pass - 1);
            while (idx < 100)
            {
                doors[idx] = (!_indexList(doors, idx));
                idx = (idx + pass);
            }
        }
        for (var row = 0; row < 10; row++)
        {
            string line = "";
            for (var col = 0; col < 10; col++)
            {
                var idx = ((row * 10) + col);
                if (_indexList(doors, idx))
                {
                    line = string.Concat(line, "1");
                }
                else
                {
                    line = string.Concat(line, "0");
                }
                if (col < 9)
                {
                    line = string.Concat(line, " ");
                }
            }
            Console.WriteLine(line);
        }
    }
    static dynamic _indexList(dynamic l, long i)
    {
        var list = l as System.Collections.IList;
        if (list == null) throw new Exception("index() expects list");
        if (i < 0) i += list.Count;
        if (i < 0 || i >= list.Count) throw new Exception("index out of range");
        return list[(int)i];
    }

}
