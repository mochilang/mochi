# Generated by Mochi compiler v0.10.28 on 2025-07-18T09:21:25Z
from __future__ import annotations
import time


def parseIntStr(_str):
    i = 0
    neg = False
    if len(_str) > 0 and _str[0:1] == "-":
        neg = True
        i = 1
    n = 0
    digits = {
        "_": 0,
        "_": 1,
        "_": 2,
        "_": 3,
        "_": 4,
        "_": 5,
        "_": 6,
        "_": 7,
        "_": 8,
        "_": 9,
    }
    while i < len(_str):
        n = n * 10 + digits[_str[i : i + 1]]
        i = i + 1
    if neg:
        n = -n
    return n


def main():
    total = 0
    computer = time.time_ns() % 2 == 0
    print("Enter q to quit at any time\n")
    if computer:
        print("The computer will choose first")
    else:
        print("You will choose first")
    print("\n\nRunning total is now 0\n\n")
    round = 1
    done = False
    while not done:
        print("ROUND " + str(round) + ":\n\n")
        i = 0
        while i < 2 and (not done):
            if computer:
                choice = 0
                if total < 18:
                    choice = time.time_ns() % 3 + 1
                else:
                    choice = 21 - total
                total = total + choice
                print("The computer chooses " + str(choice))
                print("Running total is now " + str(total))
                if total == 21:
                    print("\nSo, commiserations, the computer has won!")
                    done = True
            else:
                while True:
                    print("Your choice 1 to 3 : ")
                    line = input()
                    if line == "q" or line == "Q":
                        print("OK, quitting the game")
                        done = True
                        break
                    num = parseIntStr(line)
                    if num < 1 or num > 3:
                        if total + num > 21:
                            print("Too big, try again")
                        else:
                            print("Out of range, try again")
                        continue
                    if total + num > 21:
                        print("Too big, try again")
                        continue
                    total = total + num
                    print("Running total is now " + str(total))
                    break
                if total == 21:
                    print("\nSo, congratulations, you've won!")
                    done = True
            print("\n")
            computer = not computer
            i = i + 1
        round = round + 1


main()
