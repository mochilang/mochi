// Generated by Mochi compiler v0.10.26 on 1970-01-01T00:00:00Z
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn doIt(p: std.AutoHashMap([]const u8, i32)) i32 {
    var b = 0; // i32
    if (p.contains("b")) {
        b = p["b"];
    }
    return ((p["a"] + b) + p["c"]);
}

fn user_main() void {
    var p = std.AutoHashMap([]const u8, i32).init(std.heap.page_allocator);
    _ = p.put("a", 1) catch |err| handleError(err);
    _ = p.put("c", 9) catch |err| handleError(err);
    std.debug.print("{s}\n", .{std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{doIt(p)}) catch |err| handleError(err)});
}

pub fn main() void {
    user_main();
}
