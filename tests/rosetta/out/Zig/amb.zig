// Generated by Mochi compiler v0.10.26 on 1970-01-01T00:00:00Z
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn _concat_string(a: []const u8, b: []const u8) []const u8 {
    return std.mem.concat(u8, &[_][]const u8{ a, b }) catch |err| handleError(err);
}

fn amb(wordsets: []const []const []const u8, res: []const []const u8, idx: i32) bool {
    if (idx == (wordsets).len) {
        return true;
    }
    var prev = ""; // []const u8
    if (idx > 0) {
        prev = res[(idx - 1)];
    }
    var i = 0; // i32
    while (i < (wordsets[idx]).len) {
        const w = wordsets[idx][i]; // []const u8
        if ((idx == 0) or (substring(prev, ((prev).len - 1), (prev).len) == substring(w, 0, 1))) {
            res.items[idx] = w;
            if (amb(wordsets, res, (idx + 1))) {
                return true;
            }
        }
        i = (i + 1);
    }
    return false;
}

fn user_main() void {
    const wordset = &[_][]const []const u8{
    &[_][]const u8{
    "the",
    "that",
    "a",
},
    &[_][]const u8{
    "frog",
    "elephant",
    "thing",
},
    &[_][]const u8{
    "walked",
    "treaded",
    "grows",
},
    &[_][]const u8{
    "slowly",
    "quickly",
},
}; // []const []const []const u8
    var res = std.ArrayList(u8).init(std.heap.page_allocator);
    var i = 0; // i32
    while (i < (wordset).len) {
        res = blk0: { var _tmp0 = std.ArrayList([]const u8).init(std.heap.page_allocator); defer _tmp0.deinit(); _tmp0.appendSlice(res) catch |err| handleError(err); _tmp0.append("") catch |err| handleError(err); break :blk0 _tmp0.items; };
        i = (i + 1);
    }
    if (amb(wordset, res, 0)) {
        var out = _concat_string("[", res[0]); // []const u8
        var j = 1; // i32
        while (j < (res).len) {
            out = _concat_string(_concat_string(out, " "), res[j]);
            j = (j + 1);
        }
        out = _concat_string(out, "]");
        std.debug.print("{s}\n", .{out});
    } else {
        std.debug.print("No amb found\n", .{});
    }
}

pub fn main() void {
    user_main();
}
