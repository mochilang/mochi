// Generated by Mochi compiler v0.10.26 on 2025-07-16T10:02:21Z
program _00Doors2;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser, fpjsonrtti;

type
  generic TArray<T> = array of T;

var
  current: integer;
  door: integer;
  incrementer: integer;
  line: string;

begin
  door := 1;
  incrementer := 0;
  for current := 1 to 101 - 1 do
  begin
    line := 'Door ' + IntToStr(current) + ' ';
    if (current = door) then
    begin
      line := line + 'Open';
      incrementer := incrementer + 1;
      door := door + 2 * incrementer + 1;
    end else
    begin
      line := line + 'Closed';
    end;
    writeln(line);
  end;
end.
