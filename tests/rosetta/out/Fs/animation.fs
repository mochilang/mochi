// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let msg: string = "Hello World! "
let mutable shift: int = 0
let mutable inc: int = 1
let mutable clicks: int = 0
let mutable frames: int = 0
while clicks < 5 do
    let mutable line: string = ""
    let mutable i: int = 0
    while i < List.length msg do
        let idx: obj = (shift + i) % List.length msg
        line <- line + msg.Substring(idx, idx + 1 - idx)
        i <- i + 1
    printfn "%s" line
    shift <- (shift + inc) % List.length msg
    frames <- frames + 1
    if frames % List.length msg = 0 then
        inc <- List.length msg - inc
        clicks <- clicks + 1
