// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let countDivisors (n) =
    if n < 2 then
        1
    let mutable count: int = 2
    let mutable i: int = 2
    while i <= n / 2 do
        if n % i = 0 then
            count <- count + 1
        i <- i + 1
    count
let main () =
    printfn "%s" "The first 20 anti-primes are:"
    let mutable maxDiv: int = 0
    let mutable count: int = 0
    let mutable n: int = 1
    let mutable line: string = ""
    while count < 20 do
        let d: obj = countDivisors n
        if d > maxDiv then
            line <- line + string n + " "
            maxDiv <- d
            count <- count + 1
        n <- n + 1
    line <- line.Substring(0, List.length line - 1 - 0)
    printfn "%s" line
printfn "%A" (main())
