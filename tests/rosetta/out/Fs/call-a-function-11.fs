// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let zeroval (ival) =
    let mutable x: obj = ival
    x <- 0
    x
let zeroptr (ref) =
    ref.[0] <- 0
let main () =
    let mutable i: int = 1
    printfn "%s" ("initial: " + string i)
    let tmp: obj = zeroval i
    printfn "%s" ("zeroval: " + string i)
    let mutable box: int list = [|i|]
    printfn "%A" (zeroptr box)
    i <- box.[0]
    printfn "%s" ("zeroptr: " + string i)
    printfn "%s" "pointer: 0"
printfn "%A" (main())
