// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let binom (n) (k) =
    if k < 0 || k > n then
        0
    let mutable kk: obj = k
    if kk > n - kk then
        kk <- n - kk
    let mutable res: int = 1
    let mutable i: int = 0
    while i < kk do
        res <- (res * (n - i))
        i <- i + 1
        res <- (res / i)
    res
let catalan (n) =
    (binom 2 * n n / (n + 1))
let main () =
    for i in 0 .. 15 do
        printfn "%A" (string catalan i)
printfn "%A" (main())
