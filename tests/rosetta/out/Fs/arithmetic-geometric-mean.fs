// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let abs (x) =
    if x < 0.0 then
        -x
    x
let sqrtApprox (x) =
    let mutable guess: obj = x
    let mutable i: int = 0
    while i < 20 do
        guess <- (guess + x / guess) / 2.0
        i <- i + 1
    guess
let agm (a) (g) =
    let eps: float = 0.00000000000001
    while abs a - g > abs a * eps do
        let newA: obj = (a + g) / 2.0
        let newG: obj = sqrtApprox a * g
        a <- newA
        g <- newG
    a
let main () =
    printfn "%A" (string agm 1.0 1.0 / sqrtApprox 2.0)
printfn "%A" (main())
