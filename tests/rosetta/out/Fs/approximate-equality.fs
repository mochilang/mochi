// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let abs (x) =
    if x < 0.0 then
        -x
    x
let maxf (a) (b) =
    if a > b then
        a
    b
let isClose (a) (b) =
    let relTol: float = 0.000000001
    let t: obj = abs a - b
    let u: float = relTol * maxf abs a abs b
    t <= u
let sqrtApprox (x) =
    let mutable guess: obj = x
    let mutable i: int = 0
    while i < 10 do
        guess <- (guess + x / guess) / 2.0
        i <- i + 1
    guess
let main () =
    let root2: obj = sqrtApprox 2.0
    let pairs: float list list = [[100000000000000.02; 100000000000000.02]; [100.01; 100.011]; [10000000000000.002 / 10000.0; 1000000000.0000001]; [0.001; 0.0010000001]; [0.000000000000000000000101; 0.0]; [root2 * root2; 2.0]; [(-root2) * root2; -2.0]; [100000000000000000.0; 100000000000000000.0]; [3.141592653589793; 3.141592653589793]]
    for pair in pairs do
        let a: float list = pair.[0]
        let b: float list = pair.[1]
        let s: string = (if isClose a b then "≈" else "≉")
        printfn "%A" (string a + " " + s + " " + string b)
printfn "%A" (main())
