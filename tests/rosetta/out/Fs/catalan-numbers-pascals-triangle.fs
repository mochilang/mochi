// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let n: int = 15
let mutable t: obj = [||]
for _ in 0 .. (n + 2) do
    t <- t @ [0]
t.[1] <- 1
for i in 1 .. (n + 1) do
    let mutable j: int = i
    while j > 1 do
        t.[j] <- t.[j] + t.[j - 1]
        j <- j - 1
    t.[i + 1] <- t.[i]
    j <- i + 1
    while j > 1 do
        t.[j] <- t.[j] + t.[j - 1]
        j <- j - 1
    let cat: obj = t.[i + 1] - t.[i]
    if i < 10 then
        printfn "%s" (" " + string i + " : " + string cat)
    else
        printfn "%A" (string i + " : " + string cat)
