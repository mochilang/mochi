// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let cart2 (a) (b) =
    let mutable p: obj = [||]
    for x in a do
        for y in b do
            p <- p @ [[x; y]]
    p
let llStr (lst) =
    let mutable s: string = "["
    let mutable i: int = 0
    while i < List.length lst do
        let mutable row: obj = lst.[i]
        s <- s + "["
        let mutable j: int = 0
        while j < List.length row do
            s <- s + string row.[j]
            if j < List.length row - 1 then
                s <- s + " "
            j <- j + 1
        s <- s + "]"
        if i < List.length lst - 1 then
            s <- s + " "
        i <- i + 1
    s <- s + "]"
    s
let main () =
    printfn "%A" (llStr cart2 [1; 2] [3; 4])
    printfn "%A" (llStr cart2 [3; 4] [1; 2])
    printfn "%A" (llStr cart2 [1; 2] [])
    printfn "%A" (llStr cart2 [] [1; 2])
printfn "%A" (main())
