// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let pow_int (base) (exp) =
    let mutable result: int = 1
    let mutable b: obj = base
    let mutable e: obj = exp
    while e > 0 do
        if e % 2 = 1 then
            result <- result * b
        b <- b * b
        e <- (e / 2)
    result
let pow_big (base) (exp) =
    let mutable result: bigint = 1
    let mutable b: bigint = base
    let mutable e: obj = exp
    while e > 0 do
        if e % 2 = 1 then
            result <- result * b
        b <- b * b
        e <- (e / 2)
    result
let mutable e1: obj = pow_int 3 2
let mutable e2: obj = pow_int 4 e1
let mutable base: bigint = 5
let mutable x: bigint = pow_big base e2
let mutable s: obj = string x
printfn "%s" (String.concat " " [string "5^(4^(3^2)) has"; string List.length s; string "digits:"; string s.Substring(0, 20 - 0); string "..."; string s.Substring(List.length s - 20, List.length s - List.length s - 20)])
