// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let listStr (xs) =
    let mutable s: string = "["
    let mutable i: int = 0
    while i < List.length xs do
        s <- s + string xs.[i]
        if i + 1 < List.length xs then
            s <- s + " "
        i <- i + 1
    s <- s + "]"
    s
let mutable a: int list = [|0; 0; 0; 0; 0|]
printfn "%s" ("len(a) = " + string List.length a)
printfn "%s" ("a = " + listStr a)
a.[0] <- 3
printfn "%s" ("a = " + listStr a)
printfn "%s" ("a[0] = " + string a.[0])
let mutable s: int list = a.[0..(4-1)]
let mutable cap_s: int = 5
printfn "%s" ("s = " + listStr s)
printfn "%s" ("len(s) = " + string List.length s + "  cap(s) = " + string cap_s)
s <- a.[0..(5-1)]
printfn "%s" ("s = " + listStr s)
a.[0] <- 22
s.[0] <- 22
printfn "%s" ("a = " + listStr a)
printfn "%s" ("s = " + listStr s)
s <- s @ [4]
s <- s @ [5]
s <- s @ [6]
cap_s <- 10
printfn "%s" ("s = " + listStr s)
printfn "%s" ("len(s) = " + string List.length s + "  cap(s) = " + string cap_s)
a.[4] <- -1
printfn "%s" ("a = " + listStr a)
printfn "%s" ("s = " + listStr s)
s <- []
for i in 0 .. 8 do
    s <- s @ [0]
cap_s <- 8
printfn "%s" ("s = " + listStr s)
printfn "%s" ("len(s) = " + string List.length s + "  cap(s) = " + string cap_s)
