// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let sortFloat (xs) =
    let mutable arr: obj = xs
    let mutable n: obj = List.length arr
    let mutable i: int = 0
    while i < n do
        let mutable j: int = 0
        while j < n - 1 do
            if arr.[j] > arr.[j + 1] then
                let tmp: obj = arr.[j]
                arr.[j] <- arr.[j + 1]
                arr.[j + 1] <- tmp
            j <- j + 1
        i <- i + 1
    arr
let median (a) =
    let mutable arr: obj = sortFloat a
    let half: obj = (List.length arr / 2)
    let mutable m: obj = arr.[half]
    if List.length arr % 2 = 0 then
        m <- (m + arr.[half - 1]) / 2.0
    m
printfn "%A" (string median [3.0; 1.0; 4.0; 1.0])
printfn "%A" (string median [3.0; 1.0; 4.0; 1.0; 5.0])
