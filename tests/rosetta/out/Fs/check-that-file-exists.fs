// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

type Anon1 = {
}
let printStat (fs) (path) =
    if List.contains path fs then
        if fs.[path] then
            printfn "%A" (path + " is a directory")
        else
            printfn "%A" (path + " is a file")
    else
        printfn "%s" "stat " + path + ": no such file or directory"
let main () =
    let mutable fs: Anon1 = {  }
    fs.["docs"] <- true
    for p in ["input.txt"; "/input.txt"; "docs"; "/docs"] do
        printfn "%A" (printStat fs p)
printfn "%A" (main())
