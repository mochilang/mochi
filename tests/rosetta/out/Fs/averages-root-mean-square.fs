// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let sqrtApprox (x) =
    let mutable guess: obj = x
    let mutable i: int = 0
    while i < 20 do
        guess <- (guess + x / guess) / 2.0
        i <- i + 1
    guess
let n: int = 10
let mutable sum: float = 0.0
let mutable x: int = 1
while x <= n do
    sum <- sum + (x) * (x)
    x <- x + 1
let rms: obj = sqrtApprox sum / (n)
printfn "%A" (string rms)
