// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let add (a) (b) =
    a + b
let sub (a) (b) =
    a - b
let mul (a) (b) =
    a * b
let fold (f) (xs) =
    let mutable r: obj = xs.[0]
    let mutable i: int = 1
    while i < List.length xs do
        r <- f r xs.[i]
        i <- i + 1
    r
let n: int list = [1; 2; 3; 4; 5]
printfn "%A" (fold fun a b -> add a b n)
printfn "%A" (fold fun a b -> sub a b n)
printfn "%A" (fold fun a b -> mul a b n)
