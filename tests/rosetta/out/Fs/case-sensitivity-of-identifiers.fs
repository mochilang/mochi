// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
open System

let main () =
    let mutable pkg_dog: string = "Salt"
    let mutable Dog: string = "Pepper"
    let mutable pkg_DOG: string = "Mustard"
    let packageSees (d1) (d2) (d3) =
        printfn "%s" ("Package sees: " + d1 + " " + d2 + " " + d3)
        dict [("pkg_dog", true); ("Dog", true); ("pkg_DOG", true)]
    let mutable d: obj = packageSees pkg_dog Dog pkg_DOG
    printfn "%s" "There are " + string List.length d + " dogs.\n"
    let mutable dog: string = "Benjamin"
    d <- packageSees pkg_dog Dog pkg_DOG
    printfn "%s" ("Main sees:   " + dog + " " + Dog + " " + pkg_DOG)
    d.["dog"] <- true
    d.["Dog"] <- true
    d.["pkg_DOG"] <- true
    printfn "%s" "There are " + string List.length d + " dogs.\n"
    Dog <- "Samba"
    d <- packageSees pkg_dog Dog pkg_DOG
    printfn "%s" ("Main sees:   " + dog + " " + Dog + " " + pkg_DOG)
    d.["dog"] <- true
    d.["Dog"] <- true
    d.["pkg_DOG"] <- true
    printfn "%s" "There are " + string List.length d + " dogs.\n"
    let mutable DOG: string = "Bernie"
    d <- packageSees pkg_dog Dog pkg_DOG
    printfn "%s" ("Main sees:   " + dog + " " + Dog + " " + DOG)
    d.["dog"] <- true
    d.["Dog"] <- true
    d.["pkg_DOG"] <- true
    d.["DOG"] <- true
    printfn "%s" "There are " + string List.length d + " dogs."
printfn "%A" (main())
