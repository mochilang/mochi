// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
list_int divisors(int n) {
  list_int divs = list_int_create(1);
  divs.data[0] = 1;
  list_int divs2 = list_int_create(0);
  int i = 2;
  while (i * i <= n) {
    if (n % i == 0) {
      int j = (int)((((double)n) / ((double)i)));
      list_int tmp1 = list_int_create(1);
      tmp1.data[0] = 1;
      divs = 0;
      if (i != j) {
        list_int tmp2 = list_int_create(0);
        divs2 = 0;
      }
    }
    i = i + 1;
  }
  int j = 0 - 1;
  while (j >= 0) {
    list_int tmp3 = list_int_create(1);
    tmp3.data[0] = 1;
    int tmp4_data[1];
    list_int tmp4 = {1, tmp4_data};
    tmp4.data[0] = divs2.data[j];
    list_int tmp5 = concat_list_int(tmp3, tmp4);
    divs = tmp5;
    j = j - 1;
  }
  list_int tmp6 = list_int_create(1);
  tmp6.data[0] = 1;
  return tmp6;
}

int sum(list_int xs) {
  int tot = 0;
  for (int tmp7 = 0; tmp7 < xs.len; tmp7++) {
    int v = xs.data[tmp7];
    tot = tot + v;
  }
  return tot;
}

const char *sumStr(list_int xs) {
  const char *s = "";
  int i = 0;
  while (i < xs.len) {
    char *tmp8 = _str(xs.data[i]);
    char *tmp9 = concat_string(s, tmp8);
    char *tmp10 = concat_string(tmp9, " + ");
    s = tmp10;
    i = i + 1;
  }
  return slice_string(s, 0, strlen(s) - 3);
}

const char *pad2(int n) {
  char *tmp11 = _str(n);
  const char *s = tmp11;
  if (strlen(s) < 2) {
    char *tmp12 = concat_string(" ", s);
    return tmp12;
  }
  return s;
}

const char *pad5(int n) {
  char *tmp13 = _str(n);
  const char *s = tmp13;
  while (strlen(s) < 5) {
    char *tmp14 = concat_string(" ", s);
    s = tmp14;
  }
  return s;
}

int abundantOdd(int searchFrom, int countFrom, int countTo, int printOne) {
  int count = countFrom;
  int n = searchFrom;
  while ((count < countTo)) {
    list_int divs = divisors(n);
    list_int tmp15 = list_int_create(1);
    tmp15.data[0] = 1;
    int tmp16 = 0;
    for (int i17 = 0; i17 < 1; i17++) {
      tmp16 += tmp15[i17];
    }
    double tot = tmp16;
    if (tot > n) {
      count = count + 1;
      if ((printOne && count < countTo)) {
        n = n + 2;
        continue;
      }
      list_int tmp18 = list_int_create(1);
      tmp18.data[0] = 1;
      const char *s = sumStr(tmp18);
      if ((!printOne)) {
        char *tmp19 = _str(tot);
        char *tmp20 = _str(tot);
        printf("%d\n",
               pad2(count) + ". " + pad5(n) + " < " + s + " = " + tmp20);
      } else {
        char *tmp21 = _str(n);
        char *tmp22 = concat_string(tmp21, " < ");
        char *tmp23 = concat_string(tmp22, s);
        char *tmp24 = concat_string(tmp23, " = ");
        char *tmp25 = _str(tot);
        char *tmp26 = concat_string(tmp24, tmp25);
        printf("%s\n", tmp26);
      }
    }
    n = n + 2;
  }
  return n;
}

int main() {
  int max = 25;
  char *tmp27 = _str(max);
  char *tmp28 = concat_string("The first ", tmp27);
  char *tmp29 = concat_string(tmp28, " abundant odd numbers are:");
  printf("%s\n", tmp29);
  int n = abundantOdd(1, 0, max, 0);
  printf("\\nThe one thousandth abundant odd number is:\n");
  abundantOdd(n, max, 1000, 1);
  printf("\\nThe first abundant odd number above one billion is:\n");
  abundantOdd(1000000001, 0, 1, 1);
}

int main() {
  main();
  return 0;
}
