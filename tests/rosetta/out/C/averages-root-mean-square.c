// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:19Z
#include <stdio.h>
#include <stdlib.h>

static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static int n = 10;
static double sum = 0.0;
static int x = 1;

double sqrtApprox(double x) {
  double guess = x;
  int i = 0;
  while (i < 20) {
    guess = (guess + x / guess) / 2.0;
    i = i + 1;
  }
  return guess;
}

int main() {
  while (x <= n) {
    sum = sum + ((double)(x)) * ((double)(x));
    x = x + 1;
  }
  double rms = sqrtApprox(sum / ((double)(n)));
  char *tmp1 = _str(rms);
  printf("%s\n", tmp1);
  return 0;
}
