// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
const char *strdup(char *s) {
  char *tmp1 = concat_string(s, "");
  return tmp1;
}

int main() {
  const char *go1 = "hello C";
  const char *c2 = strdup(go1);
  printf("%s\n", c2);
}

int main() {
  main();
  return 0;
}
