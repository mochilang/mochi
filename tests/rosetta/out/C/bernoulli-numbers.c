// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:19Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
int bernoulli(int n) {
  list_int a = list_int_create(0);
  int m = 0;
  while (m <= n) {
    list_int tmp1 = list_int_create(0);
    int tmp2_data[1];
    list_int tmp2 = {1, tmp2_data};
    tmp2.data[0] = (int)(1) / ((int)((m + 1)));
    list_int tmp3 = concat_list_int(tmp1, tmp2);
    a = tmp3;
    int j = m;
    while (j >= 1) {
      a[j - 1] = ((int)(j)) * (a.data[j - 1] - a.data[j]);
      j = j - 1;
    }
    m = m + 1;
  }
  return a.data[0];
}

int main() {
  for (int i = 0; i < 61; i++) {
    int b = bernoulli(i);
    if (num(b) != 0) {
      char *tmp4 = _str(num(b));
      const char *numStr = tmp4;
      char *tmp5 = _str(denom(b));
      const char *denStr = tmp5;
      char *tmp6 = _str(i);
      char *tmp7 = _str(i);
      printf("%d\n", "B(" + tmp7(2, " ") + ") =" + numStr.pad_start(45, " ") +
                         "/" + denStr);
    }
  }
  return 0;
}
