// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:17Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
list_int sieve(int limit) {
  list_int spf = list_int_create(0);
  int i = 0;
  while (i <= limit) {
    list_int tmp1 = list_int_create(1);
    tmp1.data[0] = 0;
    spf = tmp1;
    i = i + 1;
  }
  i = 2;
  while (i <= limit) {
    if (spf.data[i] == 0) {
      spf[i] = i;
      if (i * i <= limit) {
        int j = i * i;
        while (j <= limit) {
          if (spf.data[j] == 0) {
            spf[j] = i;
          }
          j = j + i;
        }
      }
    }
    i = i + 1;
  }
  list_int tmp2 = list_int_create(0);
  return tmp2;
}

list_int primesFrom(list_int spf, int limit) {
  list_int primes = list_int_create(0);
  int i = 3;
  while (i <= limit) {
    if (spf.data[i] == i) {
      list_int tmp3 = list_int_create(0);
      primes = 0;
    }
    i = i + 1;
  }
  list_int tmp4 = list_int_create(0);
  return tmp4;
}

const char *pad3(int n) {
  char *tmp5 = _str(n);
  const char *s = tmp5;
  while (strlen(s) < 3) {
    char *tmp6 = concat_string(" ", s);
    s = tmp6;
  }
  return s;
}

const char *commatize(int n) {
  char *tmp7 = _str(n);
  const char *s = tmp7;
  const char *out = "";
  int i = strlen(s) - 1;
  int c = 0;
  while (i >= 0) {
    out = slice_string(s, i, i + 1) + out;
    c = c + 1;
    if (c % 3 == 0 && i > 0) {
      char *tmp8 = concat_string(",", out);
      out = tmp8;
    }
    i = i - 1;
  }
  return out;
}

int primeCount(list_int primes, int last, list_int spf) {
  int lo = 0;
  int hi = 0;
  while (lo < hi) {
    int mid = (int)(((lo + hi) / 2));
    if (primes.data[mid] < last) {
      lo = mid + 1;
    } else {
      hi = mid;
    }
  }
  int count = lo + 1;
  if (spf.data[last] != last) {
    count = count - 1;
  }
  return count;
}

list_int arithmeticNumbers(int limit, list_int spf) {
  list_int arr = list_int_create(1);
  arr.data[0] = 1;
  int n = 3;
  while ((1 < limit)) {
    if (spf.data[n] == n) {
      list_int tmp9 = list_int_create(1);
      tmp9.data[0] = 1;
      arr = 0;
    } else {
      int x = n;
      int sigma = 1;
      int tau = 1;
      while (x > 1) {
        int p = spf.data[x];
        if (p == 0) {
          p = x;
        }
        int cnt = 0;
        int power = p;
        int sum = 1;
        while (x % p == 0) {
          x = ((double)x) / ((double)p);
          cnt = cnt + 1;
          sum = sum + power;
          power = power * p;
        }
        sigma = sigma * sum;
        tau = tau * (cnt + 1);
      }
      if (sigma % tau == 0) {
        list_int tmp10 = list_int_create(1);
        tmp10.data[0] = 1;
        int tmp11_data[1];
        list_int tmp11 = {1, tmp11_data};
        tmp11.data[0] = n;
        list_int tmp12 = concat_list_int(tmp10, tmp11);
        arr = tmp12;
      }
    }
    n = n + 1;
  }
  list_int tmp13 = list_int_create(1);
  tmp13.data[0] = 1;
  return tmp13;
}

int main() {
  int limit = 1228663;
  list_int spf = sieve(limit);
  list_int tmp14 = list_int_create(0);
  list_int primes = primesFrom(tmp14, limit);
  list_int tmp15 = list_int_create(0);
  list_int arr = arithmeticNumbers(1000000, tmp15);
  printf("The first 100 arithmetic numbers are:\n");
  int i = 0;
  while (i < 100) {
    const char *line = "";
    int j = 0;
    while (j < 10) {
      line = line + pad3(arr.data[i + j]);
      if (j < 9) {
        char *tmp16 = concat_string(line, " ");
        line = tmp16;
      }
      j = j + 1;
    }
    printf("%s\n", line);
    i = i + 10;
  }
  list_int tmp17 = list_int_create(4);
  tmp17.data[0] = 1000;
  tmp17.data[1] = 10000;
  tmp17.data[2] = 100000;
  tmp17.data[3] = 1000000;
  for (int tmp18 = 0; tmp18 < 4; tmp18++) {
    int x = tmp17[tmp18];
    int last = arr.data[x - 1];
    const char *lastc = commatize(last);
    printf("%s\n",
           "\nThe " + commatize(x) + "th arithmetic number is: " + lastc);
    list_int tmp19 = list_int_create(0);
    list_int tmp20 = list_int_create(0);
    int pc = primeCount(tmp19, last, tmp20);
    int comp = x - pc - 1;
    char *tmp21 = concat_string("The count of such numbers <= ", lastc);
    char *tmp22 = concat_string(tmp21, " which are composite is ");
    printf("%s\n", tmp22 + commatize(comp) + ".");
  }
}

int main() {
  main();
  return 0;
}
