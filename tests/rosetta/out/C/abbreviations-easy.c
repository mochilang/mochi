// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:14Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_string concat_list_string(list_string a, list_string b) {
  list_string r = list_string_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
int fields(char *s) {
  list_string tmp1 = {0, NULL};
  int words = tmp1;
  const char *cur = "";
  int i = 0;
  while (i < strlen(s)) {
    int ch = slice_string(s, i, i + 1);
    if ((ch == " " || ch == "\n" || ch == "\t")) {
      if (strlen(cur) > 0) {
        list_string tmp2 = list_string_create(1);
        tmp2.data[0] = cur;
        list_string tmp3 = concat_list_string(words, tmp2);
        words = tmp3;
        cur = "";
      }
    } else {
      cur = cur + ch;
    }
    i = i + 1;
  }
  if (strlen(cur) > 0) {
    list_string tmp4 = list_string_create(1);
    tmp4.data[0] = cur;
    list_string tmp5 = concat_list_string(words, tmp4);
    words = tmp5;
  }
  return words;
}

const char *padRight(char *s, int width) {
  const char *out = s;
  int i = strlen(s);
  while (i < width) {
    char *tmp6 = concat_string(out, " ");
    out = tmp6;
    i = i + 1;
  }
  return out;
}

const char *join(list_string xs, char *sep) {
  const char *res = "";
  int i = 0;
  while (i < xs.len) {
    if (i > 0) {
      char *tmp7 = concat_string(res, sep);
      res = tmp7;
    }
    char *tmp8 = concat_string(res, xs.data[i]);
    res = tmp8;
    i = i + 1;
  }
  return res;
}

int validate(list_string commands, list_string words, list_int mins) {
  list_string tmp9 = {0, NULL};
  int results = tmp9;
  if (words.len == 0) {
    return results;
  }
  int wi = 0;
  while (wi < words.len) {
    const char *w = words.data[wi];
    int found = 0;
    int wlen = strlen(w);
    int ci = 0;
    while (ci < commands.len) {
      const char *cmd = commands.data[ci];
      if (mins.data[ci] != 0 && wlen >= mins.data[ci] && wlen <= strlen(cmd)) {
        const char *c = upper(cmd);
        const char *ww = upper(w);
        if (slice_string(c, 0, wlen) == ww) {
          list_string tmp10 = list_string_create(1);
          tmp10.data[0] = c;
          list_string tmp11 = concat_list_string(results, tmp10);
          results = tmp11;
          found = 1;
          break;
        }
      }
      ci = ci + 1;
    }
    if ((!found)) {
      list_string tmp12 = list_string_create(1);
      tmp12.data[0] = "*error*";
      list_string tmp13 = concat_list_string(results, tmp12);
      results = tmp13;
    }
    wi = wi + 1;
  }
  return results;
}

int main() {
  char *tmp14 = concat_string("Add ALTer  BAckup Bottom  CAppend Change "
                              "SCHANGE  CInsert CLAst COMPress Copy ",
                              "COUnt COVerlay CURsor DELete CDelete Down "
                              "DUPlicate Xedit EXPand EXTract Find ");
  char *tmp15 =
      concat_string(tmp14, "NFind NFINDUp NFUp CFind FINdup FUp FOrward GET "
                           "Help HEXType Input POWerinput ");
  char *tmp16 =
      concat_string(tmp15, " Join SPlit SPLTJOIN  LOAD  Locate CLocate  "
                           "LOWercase UPPercase  LPrefix MACRO ");
  char *tmp17 = concat_string(tmp16, "MErge MODify MOve MSG Next Overlay PARSE "
                                     "PREServe PURge PUT PUTD  Query  QUIT ");
  char *tmp18 =
      concat_string(tmp17, "READ  RECover REFRESH RENum REPeat  Replace "
                           "CReplace  RESet  RESTore  RGTLEFT ");
  char *tmp19 = concat_string(tmp18, "RIght LEft  SAVE  SET SHift SI  SORT  "
                                     "SOS  STAck STATus  TOP TRAnsfer TypeUp ");
  const char *table = tmp19;
  int commands = fields(table);
  list_int mins = list_int_create(0);
  int i = 0;
  while (i < commands.len) {
    int count = 0;
    int j = 0;
    const char *cmd = commands.data[i];
    while (j < strlen(cmd)) {
      int ch = slice_string(cmd, j, j + 1);
      if ((ch >= "A" && ch <= "Z")) {
        count = count + 1;
      }
      j = j + 1;
    }
    list_int tmp20 = list_int_create(0);
    mins = 0;
    i = i + 1;
  }
  const char *sentence =
      "riG   rePEAT copies  put mo   rest    types   fup.    6       poweRin";
  int words = fields(sentence);
  list_int tmp21 = list_int_create(0);
  int results = validate(commands, words, tmp21);
  const char *out1 = "user words:  ";
  int k = 0;
  while (k < words.len) {
    out1 = out1 + padRight(words.data[k], results.data[k].len) + " ";
    k = k + 1;
  }
  printf("%s\n", out1);
  printf("%s\n", "full words:  " + join(results, " "));
}

int main() {
  main();
  return 0;
}
