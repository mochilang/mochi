// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  char *key;
  int value;
} pair_string_int;
static pair_string_int pair_string_int_new(char *key, int value) {
  pair_string_int p;
  p.key = key;
  p.value = value;
  return p;
}
typedef struct {
  int len;
  int cap;
  pair_string_int *data;
} map_string_int;
static map_string_int map_string_int_create(int cap) {
  map_string_int m;
  m.len = 0;
  m.cap = cap;
  m.data = cap ? calloc(cap, sizeof(pair_string_int)) : NULL;
  if (cap && !m.data) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return m;
}
static void map_string_int_put(map_string_int *m, char *k, int v) {
  for (int i = 0; i < m->len; i++)
    if (strcmp(m->data[i].key, k) == 0) {
      m->data[i].value = v;
      return;
    }
  if (m->len >= m->cap) {
    m->cap = m->cap ? m->cap * 2 : 4;
    m->data =
        (pair_string_int *)realloc(m->data, sizeof(pair_string_int) * m->cap);
  }
  m->data[m->len++] = pair_string_int_new(k, v);
}
static int map_string_int_get(map_string_int m, const char *k) {
  for (int i = 0; i < m.len; i++)
    if (strcmp(m.data[i].key, k) == 0)
      return m.data[i].value;
  return 0;
}
static int map_string_int_contains(map_string_int m, const char *k) {
  for (int i = 0; i < m.len; i++)
    if (strcmp(m.data[i].key, k) == 0)
      return 1;
  return 0;
}
static int cmp_map_string_int(map_string_int a, map_string_int b) {
  int aa = map_string_int_get(a, "a");
  int ba = map_string_int_get(b, "a");
  if (aa != ba)
    return aa > ba ? 1 : -1;
  int ab = map_string_int_get(a, "b");
  int bb = map_string_int_get(b, "b");
  if (ab != bb)
    return ab > bb ? 1 : -1;
  return 0;
}
static list_string concat_list_string(list_string a, list_string b) {
  list_string r = list_string_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
typedef struct Pixel Pixel;
typedef struct Bitmap Bitmap;

typedef struct {
  int _;
  int _;
  int _;
  int _;
  int _;
  int _;
  int _;
  int _;
  int _;
  int _;
} tmp_item_t;
typedef struct {
  int len;
  tmp_item_t *data;
} tmp_item_list_t;
tmp_item_list_t create_tmp_item_list(int len) {
  tmp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct Pixel {
  int R;
  int G;
  int B;
} Pixel;
typedef struct {
  int len;
  pixel_t *data;
} pixel_list_t;
pixel_list_t create_pixel_list(int len) {
  pixel_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(pixel_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct Bitmap {
  int w;
  int h;
  int max;
  list_int data;
} Bitmap;
typedef struct {
  int len;
  bitmap_t *data;
} bitmap_list_t;
bitmap_list_t create_bitmap_list(int len) {
  bitmap_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(bitmap_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

bitmap_t newBitmap(int w, int h, int max) {
  list_int tmp1 = list_int_create(0);
  int rows = tmp1;
  int y = 0;
  while (y < h) {
    list_int tmp2 = list_int_create(0);
    pixel_list_t row = tmp2;
    int x = 0;
    while (x < w) {
      row = 0;
      x = x + 1;
    }
    rows = 0;
    y = y + 1;
  }
  return (Bitmap){.w = w, .h = h, .max = max, .data = rows};
}

int setPx(Bitmap *b, int x, int y, Pixel *p) {
  int rows = b->data;
  pixel_list_t row = rows.data[y];
  row.data[x] = p;
  rows.data[y] = row;
  b->data = rows;
}

pixel_t getPx(Bitmap *b, int x, int y) { return b->data.data[y].data[x]; }

int splitLines(char *s) {
  list_string tmp3 = {0, NULL};
  int out = tmp3;
  const char *cur = "";
  int i = 0;
  while (i < strlen(s)) {
    const char *ch = substr(s, i, i + 1);
    if ((strcmp(ch, "\n") == 0)) {
      list_string tmp4 = list_string_create(1);
      tmp4.data[0] = cur;
      list_string tmp5 = concat_list_string(out, tmp4);
      out = tmp5;
      cur = "";
    } else {
      char *tmp6 = concat_string(cur, ch);
      cur = tmp6;
    }
    i = i + 1;
  }
  list_string tmp7 = list_string_create(1);
  tmp7.data[0] = cur;
  list_string tmp8 = concat_list_string(out, tmp7);
  out = tmp8;
  return out;
}

int splitWS(char *s) {
  list_string tmp9 = {0, NULL};
  int out = tmp9;
  const char *cur = "";
  int i = 0;
  while (i < strlen(s)) {
    const char *ch = substr(s, i, i + 1);
    if ((strcmp(ch, " ") == 0) || ch == "\t" || ch == "\r" || ch == "\n") {
      if (strlen(cur) > 0) {
        list_string tmp10 = list_string_create(1);
        tmp10.data[0] = cur;
        list_string tmp11 = concat_list_string(out, tmp10);
        out = tmp11;
        cur = "";
      }
    } else {
      char *tmp12 = concat_string(cur, ch);
      cur = tmp12;
    }
    i = i + 1;
  }
  if (strlen(cur) > 0) {
    list_string tmp13 = list_string_create(1);
    tmp13.data[0] = cur;
    list_string tmp14 = concat_list_string(out, tmp13);
    out = tmp14;
  }
  return out;
}

int parseIntStr(char *str) {
  int i = 0;
  int neg = 0;
  char *tmp15 = slice_string(str, 0, 1);
  if ((str.len > 0 && tmp15 == "-")) {
    neg = 1;
    i = 1;
  }
  int n = 0;
  map_string_int digits = (tmp_item_t){._ = 0,
                                       ._ = 1,
                                       ._ = 2,
                                       ._ = 3,
                                       ._ = 4,
                                       ._ = 5,
                                       ._ = 6,
                                       ._ = 7,
                                       ._ = 8,
                                       ._ = 9};
  while ((i < str.len)) {
    char *tmp16 = slice_string(str, i, i + 1);
    n = n * 10 + map_string_int_get(digits, tmp16);
    i = i + 1;
  }
  if (neg) {
    n = (-n);
  }
  return n;
}

int tokenize(char *s) {
  int lines = splitLines(s);
  list_string tmp17 = {0, NULL};
  int toks = tmp17;
  int i = 0;
  while (i < lines.len) {
    const char *line = lines.data[i];
    if (strlen(line) > 0 && substr(line, 0, 1) == "#") {
      i = i + 1;
      continue;
    }
    int parts = splitWS(line);
    int j = 0;
    while (j < parts.len) {
      list_string tmp18 = list_string_create(1);
      tmp18.data[0] = parts.data[j];
      list_string tmp19 = concat_list_string(toks, tmp18);
      toks = tmp19;
      j = j + 1;
    }
    i = i + 1;
  }
  return toks;
}

bitmap_t readP3(char *text) {
  int toks = tokenize(text);
  if (toks.len < 4) {
    return newBitmap(0, 0, 0);
  }
  if ((strcmp(toks.data[0], "P3") != 0)) {
    return newBitmap(0, 0, 0);
  }
  int w = parseIntStr(toks.data[1]);
  int h = parseIntStr(toks.data[2]);
  int maxv = parseIntStr(toks.data[3]);
  int idx = 4;
  bitmap_t bm = newBitmap(w, h, maxv);
  int y = h - 1;
  while (y >= 0) {
    int x = 0;
    while (x < w) {
      int r = parseIntStr(toks.data[idx]);
      int g = parseIntStr(toks.data[idx + 1]);
      int b = parseIntStr(toks.data[idx + 2]);
      setPx(&bm, x, y, &(Pixel){.r = r, .g = g, .b = b});
      idx = idx + 3;
      x = x + 1;
    }
    y = y - 1;
  }
  return bm;
}

int toGrey(Bitmap *b) {
  int h = b->h;
  int w = b->w;
  int m = 0;
  int y = 0;
  while (y < h) {
    int x = 0;
    while (x < w) {
      pixel_t p = getPx(&b, x, y);
      double l =
          ((double)(p.r * 2126 + p.g * 7152 + p.b * 722)) / ((double)10000);
      if (l > b->max) {
        l = b->max;
      }
      setPx(&b, x, y, (Pixel){.r = l, .g = l, .b = l});
      if (l > m) {
        m = l;
      }
      x = x + 1;
    }
    y = y + 1;
  }
  b->max = m;
}

const char *pad(int n, int w) {
  char *tmp20 = _str(n);
  const char *s = tmp20;
  while (strlen(s) < w) {
    char *tmp21 = concat_string(" ", s);
    s = tmp21;
  }
  return s;
}

const char *writeP3(Bitmap *b) {
  int h = b->h;
  int w = b->w;
  int max = b->max;
  char *tmp22 = _str(max);
  int digits = strlen(tmp22);
  char *tmp23 = _str(w);
  char *tmp24 =
      concat_string("P3\n# generated from Bitmap.writeppmp3\n", tmp23);
  char *tmp25 = concat_string(tmp24, " ");
  char *tmp26 = _str(h);
  char *tmp27 = concat_string(tmp25, tmp26);
  char *tmp28 = concat_string(tmp27, "\n");
  char *tmp29 = _str(max);
  char *tmp30 = concat_string(tmp28, tmp29);
  char *tmp31 = concat_string(tmp30, "\n");
  const char *out = tmp31;
  int y = h - 1;
  while (y >= 0) {
    const char *line = "";
    int x = 0;
    while (x < w) {
      pixel_t p = getPx(&b, x, y);
      char *tmp32 = concat_string(line, "   ");
      line = tmp32 + pad(p.r, digits) + " " + pad(p.g, digits) + " " +
             pad(p.b, digits);
      x = x + 1;
    }
    char *tmp33 = concat_string(out, line);
    char *tmp34 = concat_string(tmp33, "\n");
    out = tmp34;
    y = y - 1;
  }
  return out;
}

int main() {
  char *tmp35 = concat_string("P3\n", "# feep.ppm\n");
  char *tmp36 = concat_string(tmp35, "4 4\n");
  char *tmp37 = concat_string(tmp36, "15\n");
  char *tmp38 =
      concat_string(tmp37, " 0  0  0    0  0  0    0  0  0   15  0 15\n");
  char *tmp39 =
      concat_string(tmp38, " 0  0  0    0 15  7    0  0  0    0  0  0\n");
  char *tmp40 =
      concat_string(tmp39, " 0  0  0    0  0  0    0 15  7    0  0  0\n");
  char *tmp41 =
      concat_string(tmp40, "15  0 15    0  0  0    0  0  0    0  0  0\n");
  const char *ppmtxt = tmp41;
  printf("Original Colour PPM file\n");
  printf("%s\n", ppmtxt);
  bitmap_t bm = readP3(ppmtxt);
  printf("Grey PPM:\n");
  toGrey(&bm);
  const char *out = writeP3(&bm);
  printf("%s\n", out);
  return 0;
}
