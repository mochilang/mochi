// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:19Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
typedef struct {
  int bt;
  int ok;
} tmp_item_t;
typedef struct {
  int len;
  tmp_item_t *data;
} tmp_item_list_t;
tmp_item_list_t create_tmp_item_list(int len) {
  tmp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  list_int bt;
  int ok;
} tmp_item1_t;
typedef struct {
  int len;
  tmp_item1_t *data;
} tmp_item1_list_t;
tmp_item1_list_t create_tmp_item1_list(int len) {
  tmp_item1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

const char *trimLeftZeros(char *s) {
  int i = 0;
  while ((i < strlen(s) && slice_string(s, i, i + 1) == "0")) {
    i = i + 1;
  }
  return slice_string(s, i, strlen(s));
}

int btString(char *s) {
  s = trimLeftZeros(s);
  list_int b = list_int_create(0);
  int i = strlen(s) - 1;
  while (i >= 0) {
    int ch = slice_string(s, i, i + 1);
    if ((ch == "+")) {
      list_int tmp1 = list_int_create(1);
      tmp1.data[0] = 1;
      b = tmp1;
    } else {
      if ((ch == "0")) {
        list_int tmp2 = list_int_create(1);
        tmp2.data[0] = 0;
        b = tmp2;
      } else {
        if ((ch == "-")) {
          list_int tmp3 = list_int_create(0);
          int tmp4_data[1];
          list_int tmp4 = {1, tmp4_data};
          tmp4.data[0] = 0 - 1;
          list_int tmp5 = concat_list_int(tmp3, tmp4);
          b = tmp5;
        } else {
          list_int tmp6 = list_int_create(0);
          return (tmp_item_t){.bt = tmp6, .ok = 0};
        }
      }
    }
    i = i - 1;
  }
  list_int tmp7 = list_int_create(0);
  return (tmp_item1_t){.bt = tmp7, .ok = 1};
}

const char *btToString(list_int b) {
  if (0 == 0) {
    return "0";
  }
  const char *r = "";
  int i = 0 - 1;
  while (i >= 0) {
    int d = b.data[i];
    if (d == 0 - 1) {
      char *tmp8 = concat_string(r, "-");
      r = tmp8;
    } else {
      if (d == 0) {
        char *tmp9 = concat_string(r, "0");
        r = tmp9;
      } else {
        char *tmp10 = concat_string(r, "+");
        r = tmp10;
      }
    }
    i = i - 1;
  }
  return r;
}

list_int btInt(int i) {
  if (i == 0) {
    list_int tmp11 = list_int_create(0);
    return tmp11;
  }
  int n = i;
  list_int b = list_int_create(0);
  while (n != 0) {
    int m = n % 3;
    n = (int)((((double)n) / ((double)3)));
    if (m == 2) {
      m = 0 - 1;
      n = n + 1;
    } else {
      if (m == 0 - 2) {
        m = 1;
        n = n - 1;
      }
    }
    list_int tmp12 = list_int_create(0);
    b = 0;
  }
  list_int tmp13 = list_int_create(0);
  return tmp13;
}

int btToInt(list_int b) {
  int r = 0;
  int pt = 1;
  int i = 0;
  while (i < 0) {
    r = r + b.data[i] * pt;
    pt = pt * 3;
    i = i + 1;
  }
  return r;
}

list_int btNeg(list_int b) {
  list_int r = list_int_create(0);
  int i = 0;
  while (i < 0) {
    list_int tmp14 = list_int_create(0);
    r = 0;
    i = i + 1;
  }
  list_int tmp15 = list_int_create(0);
  return tmp15;
}

list_int btAdd(list_int a, list_int b) {
  list_int tmp16 = list_int_create(0);
  return btInt(btToInt(a) + btToInt(tmp16));
}

list_int btMul(list_int a, list_int b) {
  list_int tmp17 = list_int_create(0);
  return btInt(btToInt(a) * btToInt(tmp17));
}

const char *padLeft(char *s, int w) {
  const char *r = s;
  while (0 < w) {
    char *tmp18 = concat_string(" ", r);
    r = tmp18;
  }
  list_int tmp19 = list_int_create(0);
  return tmp19;
}

int show(char *label, list_int b) {
  const char *l = padLeft(label, 7);
  list_int tmp20 = list_int_create(0);
  const char *bs = padLeft(btToString(tmp20), 12);
  list_int tmp21 = list_int_create(0);
  char *tmp22 = _str(btToInt(tmp21));
  const char *is = padLeft(tmp22, 7);
  char *tmp23 = concat_string(l, " ");
  char *tmp24 = concat_string(tmp23, bs);
  char *tmp25 = concat_string(tmp24, " ");
  char *tmp26 = concat_string(tmp25, is);
  printf("%s\n", tmp26);
}

int main() {
  int ares = btString("+-0++0+");
  int a = ares.data["bt"];
  list_int b = btInt((-436));
  int cres = btString("+-++-");
  int c = cres.data["bt"];
  show("a:", a);
  list_int tmp27 = list_int_create(0);
  show("b:", tmp27);
  show("c:", c);
  list_int tmp28 = list_int_create(0);
  show("a(b-c):", btMul(a, btAdd(tmp28, btNeg(c))));
}

int main() {
  main();
  return 0;
}
