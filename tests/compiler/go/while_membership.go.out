package main

import (
	"fmt"
)

func main() {
	var set map[int]bool = map[int]bool{}
	for _, n := range []int{1, 2, 3} {
		set[n] = true
	}
	var i int = 1
	var count int = 0
	for {
		tmp0 := i
		tmp1 := set
		_, tmp2 := tmp1[tmp0]
		if !(tmp2) {
			break
		}
		i = (i + 1)
		count = (count + 1)
	}
	fmt.Println(count)
}
