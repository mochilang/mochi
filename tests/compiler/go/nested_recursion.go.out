package main

import (
	"fmt"
)

func wrapper(n int) int {
	var build func(int) int
	build = func(m int) int {
		if m <= 1 {
			return 1
		}
		return (m * build((m - 1)))
	}
	return build(n)
}

func main() {
	fmt.Println(wrapper(4))
}
