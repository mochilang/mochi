:- style_check(-singleton).
to_list(Str, L) :-
    string(Str), !,
    string_chars(Str, L).
to_list(L, L).


	main :-
	to_list("cat", _V0),
	forall(member(Ch, _V0), (
		write(Ch),
		nl,
		true
	))
	.
:- initialization(main, main).
