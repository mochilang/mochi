/*
fun id(x: int): int {
  return x
}

print(id(123))
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
/* fun id(x: int): int { */
int id(int x) {
  /* return x */
  return x;
}

int main() {
  /* print(id(123)) */
  printf("%d\n", id(123));
  return 0;
}
