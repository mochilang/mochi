/*
let struct = 1
print(struct)
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  /* let struct = 1 */
  int _struct = 1;
  /* print(struct) */
  printf("%d\n", _struct);
  return 0;
}
