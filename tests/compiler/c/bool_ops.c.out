/*
print(true && false)
print(true || false)
print(!false)
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  /* print(true && false) */
  printf("%d\n", (1 && 0));
  /* print(true || false) */
  printf("%d\n", (1 || 0));
  /* print(!false) */
  printf("%d\n", (!0));
  return 0;
}
