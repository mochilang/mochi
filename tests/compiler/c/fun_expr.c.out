/*
print((fun(a: int, b: int): int => a + b)(2, 3))
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int _lambda0(int a, int b) { return (a + b); }

int main() {
  /* print((fun(a: int, b: int): int => a + b)(2, 3)) */
  printf("%d\n", (_lambda0)(2, 3));
  return 0;
}
