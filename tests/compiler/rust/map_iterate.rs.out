fn main() {
    let mut m: std::collections::HashMap<i64, bool> = std::collections::HashMap::from([]);
    m.insert(1, true);
    m.insert(2, true);
    let mut sum = 0;
    for k_key in m.keys() {
        let k = k_key.clone();
        sum = sum + k;
    }
    println!("{}", sum);
}

