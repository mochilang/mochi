fun main() {
        // var scores = {"a": 1}
        // var scores = {"a": 1}
        var scores = mutableMapOf("a" to 1)
        // scores["b"] = 2
        // scores["b"] = 2
        run {
                val _tmp = scores.toMutableMap()
                _tmp["b"] = 2
                scores = _tmp
        }
        // print(scores["b"])
        println(scores["b"])
}
