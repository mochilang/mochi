/*
let poem = generate text {
  prompt: "echo hello"
}
print(poem)
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  /* let poem = generate text { */
  char *poem = 0;
  /* print(poem) */
  printf("%s\n", poem);
  return 0;
}
