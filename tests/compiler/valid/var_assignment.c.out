/*
var x = 1
x = 2
print(x)
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  /* var x = 1 */
  int x = 1;
  /* x = 2 */
  x = 2;
  /* print(x) */
  printf("%d\n", x);
  return 0;
}
