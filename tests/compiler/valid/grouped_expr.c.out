/*
let value = (1 + 2) * 3
print(value)  // 9
*/
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  /* let value = (1 + 2) * 3 */
  int value = (((1 + 2)) * 3);
  /* print(value)  // 9 */
  printf("%d\n", value);
  return 0;
}
