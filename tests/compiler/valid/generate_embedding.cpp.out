// Original Mochi source:
// let vec = generate embedding {
//   text: "hi"
// }
// print(len(vec))
// 

#include <bits/stdc++.h>
using namespace std;

int main() {
    // let vec = generate embedding {
    auto vec;
    // print(len(vec))
    std::cout << (vec.size()) << std::endl;
    return 0;
}
