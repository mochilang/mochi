// Generated by Mochi transpiler
void main() {
  final List<Products> products = [Products(name: "Laptop", price: 1500), Products(name: "Smartphone", price: 900), Products(name: "Tablet", price: 600), Products(name: "Monitor", price: 300), Products(name: "Keyboard", price: 100), Products(name: "Mouse", price: 50), Products(name: "Headphones", price: 200)];
  final dynamic expensive = (List.of([for (var p in products) p])..sort(((a, b) => b.price.compareTo(a.price)))).skip(1).take(3).toList();
  print("--- Top products (excluding most expensive) ---");
  for (var item in expensive) {
    print([item.name, "costs \$", item.price].join(" "));
  }
}

class Products {
  final String name;
  final int price;
  const Products({required this.name, required this.price});
}
