// Generated by Mochi transpiler
void main() {
  final List<Customers> customers = [Customers(id: 1, name: "Alice"), Customers(id: 2, name: "Bob"), Customers(id: 3, name: "Charlie")];
  final List<Orders> orders = [Orders(id: 100, customerId: 1, total: 250), Orders(id: 101, customerId: 2, total: 125), Orders(id: 102, customerId: 1, total: 300)];
  final List<Result> result = [Result(orderId: o.id, orderCustomerId: o.customerId, pairedCustomerName: c.name, orderTotal: o.total) for (var o in orders) for (var c in customers)];
  print("--- Cross Join: All order-customer pairs ---");
  for (var entry in result) {
    print(["Order", entry.orderId, "(customerId:", entry.orderCustomerId, ", total: $", entry.orderTotal, ") paired with", entry.pairedCustomerName].join(" "));
  }
}

class Customers {
  final int id;
  final String name;
  const Customers({required this.id, required this.name});
}
class Orders {
  final int id;
  final int customerId;
  final int total;
  const Orders({required this.id, required this.customerId, required this.total});
}
class Result {
  final int orderId;
  final int orderCustomerId;
  final String pairedCustomerName;
  final int orderTotal;
  const Result({required this.orderId, required this.orderCustomerId, required this.pairedCustomerName, required this.orderTotal});
}
