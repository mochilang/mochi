// Generated by Mochi transpiler
void main() {
  final List<Customers> customers = [Customers(id: 1, name: "Alice"), Customers(id: 2, name: "Bob"), Customers(id: 3, name: "Charlie")];
  final List<Orders> orders = [Orders(id: 100, customerId: 1, total: 250), Orders(id: 101, customerId: 2, total: 125), Orders(id: 102, customerId: 1, total: 300), Orders(id: 103, customerId: 4, total: 80)];
  final List<Result> result = [for (var o in orders) for (var c in customers) if ((o.customerId == c.id)) Result(orderId: o.id, customerName: c.name, total: o.total)];
  print("--- Orders with customer info ---");
  for (var entry in result) {
    print(["Order", entry.orderId, "by", entry.customerName, "- \$", entry.total].join(" "));
  }
}

class Customers {
  final int id;
  final String name;
  const Customers({required this.id, required this.name});
}
class Orders {
  final int id;
  final int customerId;
  final int total;
  const Orders({required this.id, required this.customerId, required this.total});
}
class Result {
  final int orderId;
  final String customerName;
  final int total;
  const Result({required this.orderId, required this.customerName, required this.total});
}
