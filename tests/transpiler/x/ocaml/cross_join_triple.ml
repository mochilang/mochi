(* Generated by Mochi transpiler v0.10.32 on 2025-07-21 15:41 +0700 *)

exception Break
exception Continue

let () =
  let nums = [1; 2] in
  let letters = ["A"; "B"] in
  let bools = [true; false] in
  let combos = (List.concat (List.map (fun n -> (List.concat (List.map (fun l -> (List.map (fun b -> [("n", Obj.repr (n)); ("l", Obj.repr (l)); ("b", Obj.repr (b))]) bools)) letters))) nums)) in
  print_endline (String.concat " " ["--- Cross Join of three lists ---"]);
  (try List.iter (fun c ->
    try
  print_endline (String.concat " " [string_of_int (Obj.magic (List.assoc "n" c)); (Obj.magic (List.assoc "l" c)); (Obj.magic (List.assoc "b" c))]);
    with Continue -> ()) combos with Break -> ())
