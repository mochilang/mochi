(* Generated by Mochi transpiler v0.10.31 on 2025-07-19 20:10:40 GMT+7 *)

let string_contains s sub =
  let len_s = String.length s and len_sub = String.length sub in
  let rec aux i =
    if i + len_sub > len_s then false
    else if String.sub s i len_sub = sub then true
    else aux (i + 1)
  in aux 0

let list_set lst idx value =
  List.mapi (fun i v -> if i = idx then value else v) lst

let () =
  let prefix = "fore" in
  let s1 = "forest" in
  print_endline (string_of_int (if (String.sub s1 0 (String.length prefix - 0) == prefix) then 1 else 0));
  let s2 = "desert" in
  print_endline (string_of_int (if (String.sub s2 0 (String.length prefix - 0) == prefix) then 1 else 0));
