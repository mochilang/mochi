(* Generated by Mochi transpiler v0.10.33 on 2025-07-21 18:06 +0700 *)

let () =
  let nums = [1; 2; 3] in
  let letters = ["A"; "B"] in
  let pairs = (List.concat (List.map (fun n -> (List.filter_map (fun l -> if ((n mod 2) == 0) then Some ([("n", Obj.repr (n)); ("l", Obj.repr (l))]) else None) letters)) nums)) in
  print_endline (String.concat " " ["--- Even pairs ---"]);
  (try List.iter (fun p ->
    try
  print_endline (String.concat " " [string_of_int (Obj.magic (List.assoc "n" p)); (Obj.magic (List.assoc "l" p))]);
    with Continue -> ()) pairs with Break -> ())
