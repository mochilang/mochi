// Generated by Mochi transpiler v0.10.32 on 2025-07-21 11:04:32 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <sstream>

struct CustomersItem {
    int id;
    std::string name;
};

struct OrdersItem {
    int id;
    int customerId;
    int total;
};

struct ResultItem {
    int orderId;
    int orderCustomerId;
    std::string pairedCustomerName;
    int orderTotal;
};

std::vector<CustomersItem> customers = std::vector{CustomersItem{.id = 1, .name = std::string("Alice")}, CustomersItem{.id = 2, .name = std::string("Bob")}, CustomersItem{.id = 3, .name = std::string("Charlie")}};
std::vector<OrdersItem> orders = std::vector{OrdersItem{.id = 100, .customerId = 1, .total = 250}, OrdersItem{.id = 101, .customerId = 2, .total = 125}, OrdersItem{.id = 102, .customerId = 1, .total = 300}};
std::vector<ResultItem> result = ([]{ std::vector<ResultItem> __items;
for (auto o : orders) {
for (auto c : customers) {
        __items.push_back(ResultItem{.orderId = o.id, .orderCustomerId = o.customerId, .pairedCustomerName = c.name, .orderTotal = o.total});
}
}
return __items; }());

int main() {
    { std::ostringstream __ss; __ss << std::boolalpha; if(false) __ss << " "; __ss << std::string("--- Cross Join: All order-customer pairs ---"); auto __line = __ss.str(); if(!__line.empty() && __line.back() == ' ') __line.pop_back(); std::cout << __line << std::endl; }
    for (auto entry : result) {
        { std::ostringstream __ss; __ss << std::boolalpha; if(false) __ss << " "; __ss << std::string("Order"); if(true) __ss << " "; __ss << entry.orderId; if(true) __ss << " "; __ss << std::string("(customerId:"); if(true) __ss << " "; __ss << entry.orderCustomerId; if(true) __ss << " "; __ss << std::string(", total: $"); if(true) __ss << " "; __ss << entry.orderTotal; if(true) __ss << " "; __ss << std::string(") paired with"); if(true) __ss << " "; __ss << entry.pairedCustomerName; auto __line = __ss.str(); if(!__line.empty() && __line.back() == ' ') __line.pop_back(); std::cout << __line << std::endl; }
    }
    return 0;
}
