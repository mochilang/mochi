// Generated by Mochi transpiler v0.10.31 on 2025-07-20 10:28:50 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <numeric>
#include <sstream>
#include <iomanip>

std::string join(const std::string& s) { return s; }
template<typename T>
std::string join(const std::vector<T>& v) {
    std::string o;
    for (size_t i = 0; i < v.size(); ++i) {
        if (i) o += " ";
        o += std::to_string(v[i]);
    }
    return o;
}

int main() {
    std::cout << ([&]{ std::ostringstream ss; ss<<std::fixed<<std::setprecision(1)<<([&]{ auto tmp = std::vector{1, 2, 3}; return tmp.empty() ? 0.0 : std::accumulate(tmp.begin(), tmp.end(), 0.0) / tmp.size(); }()); return ss.str(); }()) << std::endl;
    return 0;
}
