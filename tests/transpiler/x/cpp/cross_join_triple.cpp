// Generated by Mochi transpiler v0.10.33 on 2025-07-21 16:39:14 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <iomanip>

static std::string format_double(double v) {
    std::ostringstream ss;
    ss << std::fixed << std::setprecision(15) << v;
    auto s = ss.str();
    if(s.find('.') != std::string::npos) {
        while(!s.empty() && s.back()=='0') s.pop_back();
        if(!s.empty() && s.back()=='.') s.push_back('0');
    }
    return s;
}

struct CombosItem {
    int n;
    std::string l;
    bool b;
};

auto nums = std::vector{1, 2};
auto letters = std::vector{std::string("A"), std::string("B")};
auto bools = std::vector{true, false};
std::vector<CombosItem> combos = ([]{ std::vector<CombosItem> __items;
for (auto n : nums) {
for (auto l : letters) {
for (auto b : bools) {
        __items.push_back(CombosItem{.n = n, .l = l, .b = b});
}
}
}
return __items; }());

int main() {
    std::cout << std::boolalpha << std::showpoint << std::setprecision(15) << std::string("--- Cross Join of three lists ---") << std::endl;
    for (auto c : combos) {
        std::cout << std::boolalpha << std::showpoint << std::setprecision(15) << c.n << " " << c.l << " " << c.b << std::endl;
    }
    return 0;
}
