// Generated by Mochi transpiler v0.10.31 on 2025-07-20 11:32:04 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>

int main() {
    std::cout << ([&]{ auto tmp = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(1, 3 - 1); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+1, c.begin()+3); })(std::vector{1, 2, 3}); if constexpr(std::is_same_v<std::decay_t<decltype(tmp)>, std::string>) return tmp; std::string o; for(size_t i=0;i<tmp.size();++i){ if(i>0) o += " "; o += std::to_string(tmp[i]); } return o; }()) << std::endl;
    std::cout << ([&]{ auto tmp = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(0, 2 - 0); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+0, c.begin()+2); })(std::vector{1, 2, 3}); if constexpr(std::is_same_v<std::decay_t<decltype(tmp)>, std::string>) return tmp; std::string o; for(size_t i=0;i<tmp.size();++i){ if(i>0) o += " "; o += std::to_string(tmp[i]); } return o; }()) << std::endl;
    std::cout << ([&]{ auto tmp = ([&](const auto& c){ if constexpr(std::is_same_v<std::decay_t<decltype(c)>, std::string>) return c.substr(1, 4 - 1); else return std::vector<typename std::decay_t<decltype(c)>::value_type>(c.begin()+1, c.begin()+4); })("hello"); if constexpr(std::is_same_v<std::decay_t<decltype(tmp)>, std::string>) return tmp; std::string o; for(size_t i=0;i<tmp.size();++i){ if(i>0) o += " "; o += std::to_string(tmp[i]); } return o; }()) << std::endl;
    return 0;
}
