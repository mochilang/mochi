// Generated by Mochi 0.10.32 on 2025-07-22 13:10 +0700
#include <stdio.h>
#include <string.h>

typedef struct Customers Customers;
typedef struct Data2 Data2;
typedef struct Data4 Data4;
typedef struct OrderInfo OrderInfo;
typedef struct Orders Orders;

struct Customers {
    int id;
    const char* name;
};

struct Data2 {
    int id;
    const char* name;
};

struct Data4 {
    int id;
    int customerId;
    int total;
};

struct OrderInfo {
    int orderId;
    const char* customerName;
    int total;
};

struct Orders {
    int id;
    int customerId;
    int total;
};

Customers customers[] = { (Customers){.id = 1, .name = "Alice"}, (Customers){.id = 2, .name = "Bob"}, (Customers){.id = 3, .name = "Charlie"} };
Orders orders[] = { (Orders){.id = 100, .customerId = 1, .total = 250}, (Orders){.id = 101, .customerId = 2, .total = 125}, (Orders){.id = 102, .customerId = 1, .total = 300}, (Orders){.id = 103, .customerId = 4, .total = 80} };

int main(void) {
    OrderInfo result[12]; size_t result_len = 0;
    for(size_t i=0;i<4;i++){ Orders o=orders[i];
      for(size_t j=0;j<3;j++){ Customers c=customers[j]; if(o.customerId==c.id){
        result[result_len++] = (OrderInfo){o.id,c.name,o.total};
      }}
    }
    puts("--- Orders with customer info ---");
    for (size_t i = 0; i < result_len; i++) {
        OrderInfo entry = result[i];
        printf("%s %d %s %s %s %d\n", "Order", entry.orderId, "by", entry.customerName, "- $", entry.total);
    }
    return 0;
}
