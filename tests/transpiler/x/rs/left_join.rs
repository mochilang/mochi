// Generated by Mochi transpiler v0.10.33 on 2025-07-21 16:07 +0700
#[derive(Debug, Clone)]
struct CustomersItem {
    id: i64,
    name: String,
}
impl std::fmt::Display for CustomersItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"id\": {}", self.id)?;
        write!(f, ", ")?;
        write!(f, "\"name\": \"{}\"", self.name)?;
        write!(f, "}}")
    }
}

#[derive(Debug, Clone)]
struct OrdersItem {
    id: i64,
    customerId: i64,
    total: i64,
}
impl std::fmt::Display for OrdersItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"id\": {}", self.id)?;
        write!(f, ", ")?;
        write!(f, "\"customerId\": {}", self.customerId)?;
        write!(f, ", ")?;
        write!(f, "\"total\": {}", self.total)?;
        write!(f, "}}")
    }
}

#[derive(Debug, Clone)]
struct QueryItem {
    orderId: i64,
    customer: CustomersItem,
    total: i64,
}
impl std::fmt::Display for QueryItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"orderId\": {}", self.orderId)?;
        write!(f, ", ")?;
        write!(f, "\"customer\": {}", self.customer)?;
        write!(f, ", ")?;
        write!(f, "\"total\": {}", self.total)?;
        write!(f, "}}")
    }
}

fn main() {
    let customers: Vec<CustomersItem> = vec![CustomersItem {id: 1, name: String::from("Alice")}, CustomersItem {id: 2, name: String::from("Bob")}];
    let orders: Vec<OrdersItem> = vec![OrdersItem {id: 100, customerId: 1, total: 250}, OrdersItem {id: 101, customerId: 3, total: 80}];
    let result: Vec<QueryItem> = { let mut _q: Vec<QueryItem> = Vec::new(); for o in &orders { let mut _matched = false; for c in &customers { if (o.customerId == c.id) { _matched = true; let c: Option<CustomersItem> = Some(c.clone()); _q.push(QueryItem {orderId: o.clone().id, customer: c.clone(), total: o.clone().total}); } } if !_matched { let c: Option<CustomersItem> = None; _q.push(QueryItem {orderId: o.clone().id, customer: c, total: o.clone().total}); } } _q };
    println!("{}", "--- Left Join ---");
    for entry in &result {
        println!("{}", format!("{} {} {} {} {} {}", "Order", entry.orderId, "customer", entry.customer, "total", entry.total).trim_end());
    }
}
