// Generated by Mochi transpiler v0.10.35 on 2025-07-22 10:54 +0700
use std::collections::HashMap;
fn main() {
    let m = HashMap::from([("a", 1), ("b", 2)]);
    { let mut __pairs: Vec<_> = m.iter().collect(); __pairs.sort_by(|a,b| a.0.cmp(b.0));
println!("{{");
for (i, (k, v)) in __pairs.iter().enumerate() {
    if i + 1 == __pairs.len() {
        println!("  \"{}\": {}", k, v);
    } else {
        println!("  \"{}\": {},", k, v);
    }
}
println!("}}"); };
}
