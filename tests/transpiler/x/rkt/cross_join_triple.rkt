;; Generated by Mochi 0.10.32 on 2025-07-20 22:43 +0700
#lang typed/racket/no-check
(require racket/list racket/string)

(: nums (Listof Integer))
(define nums (list 1 2))
(: letters (Listof String))
(define letters (list "A" "B"))
(: bools (Listof Boolean))
(define bools (list #t #f))
(: combos (Listof (HashTable String Any)))
(define combos (let ([_res '()])
  (for ([n nums])
    (for ([l letters])
      (for ([b bools])
        (set! _res (append _res (list (hash "n" n "l" l "b" b))))
      )
    )
  )
  _res))
(displayln "--- Cross Join of three lists ---")
(for ([c combos])
(displayln (format "~a ~a ~a" (hash-ref c "n") (hash-ref c "l") (hash-ref c "b")))
)
