;; Generated by Mochi 0.10.32 on 2025-07-20 22:43 +0700
#lang typed/racket/no-check
(require racket/list racket/string)

(: nums (Listof Integer))
(define nums (list 1 2 3))
(: letters (Listof String))
(define letters (list "A" "B"))
(: pairs (Listof (HashTable String Any)))
(define pairs (let ([_res '()])
  (for ([n nums])
    (for ([l letters])
      (when (= (modulo n 2) 0)
      (set! _res (append _res (list (hash "n" n "l" l))))
      )
    )
  )
  _res))
(displayln "--- Even pairs ---")
(for ([p pairs])
(displayln (format "~a ~a" (hash-ref p "n") (hash-ref p "l")))
)
