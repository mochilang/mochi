;; Generated by Mochi 0.10.32 on 2025-07-20 22:43 +0700
#lang typed/racket/no-check
(require racket/list racket/string)

(: people (Listof (HashTable String Any)))
(define people (list (hash "name" "Alice" "age" 30) (hash "name" "Bob" "age" 15) (hash "name" "Charlie" "age" 65) (hash "name" "Diana" "age" 45)))
(: adults (Listof (HashTable String Any)))
(define adults (let ([_res '()])
  (for ([person people])
    (when (>= (hash-ref person "age") 18)
    (set! _res (append _res (list (hash "name" (hash-ref person "name") "age" (hash-ref person "age") "is_senior" (>= (hash-ref person "age") 60)))))
    )
  )
  _res))
(displayln "--- Adults ---")
(for ([person adults])
(displayln (format "~a ~a ~a ~a" (hash-ref person "name") "is" (hash-ref person "age") (if (hash-ref person "is_senior") " (senior)" "")))
)
