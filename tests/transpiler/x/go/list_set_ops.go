//go:build ignore

// Generated by Mochi v0.10.31 on 2025-07-19 23:40:38 GMT+7
package main

import (
	"fmt"
	"strings"
)

func main() {
	fmt.Println(strings.Trim(fmt.Sprint(func(a, b []int) []int {
		m := map[int]bool{}
		res := []int{}
		for _, n := range a {
			if !m[n] {
				m[n] = true
				res = append(res, n)
			}
		}
		for _, n := range b {
			if !m[n] {
				m[n] = true
				res = append(res, n)
			}
		}
		return res
	}([]int{1, 2}, []int{2, 3})), "[]"))
	fmt.Println(strings.Trim(fmt.Sprint(func(a, b []int) []int {
		m := map[int]bool{}
		for _, n := range b {
			m[n] = true
		}
		res := []int{}
		for _, n := range a {
			if !m[n] {
				res = append(res, n)
			}
		}
		return res
	}([]int{1, 2, 3}, []int{2})), "[]"))
	fmt.Println(strings.Trim(fmt.Sprint(func(a, b []int) []int {
		m := map[int]bool{}
		for _, n := range a {
			m[n] = true
		}
		res := []int{}
		for _, n := range b {
			if m[n] {
				res = append(res, n)
			}
		}
		return res
	}([]int{1, 2, 3}, []int{2, 4})), "[]"))
	fmt.Println(len(func(a, b []int) []int { res := make([]int, len(a)); copy(res, a); res = append(res, b...); return res }([]int{1, 2}, []int{2, 3})))
}
