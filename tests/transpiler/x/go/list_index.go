//go:build ignore

// Generated by Mochi v0.10.31 on 2025-07-19 20:12:33 GMT+7
package main

import (
	"fmt"
	"strings"
)

func index(v any, i int) any {
	switch x := v.(type) {
	case []int:
		return x[i]
	case string:
		r := []rune(x)
		return string(r[i])
	}
	return nil
}

func formatValue(v any) string {
	if v == nil {
		return "nil"
	}
	switch x := v.(type) {
	case bool:
		if x {
			return "1"
		}
		return "0"
	case []int:
		parts := make([]string, len(x))
		for i, n := range x {
			parts[i] = fmt.Sprint(n)
		}
		return strings.Join(parts, " ")
	default:
		return fmt.Sprint(v)
	}
}

func printValues(vals ...any) {
	out := make([]string, len(vals))
	for i, v := range vals {
		out[i] = formatValue(v)
	}
	fmt.Println(strings.Join(out, " "))
}

func main() {
	xs := []int{10, 20, 30}
	printValues(index(xs, 1))
}
