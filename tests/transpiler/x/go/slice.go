//go:build ignore

// Generated by Mochi v0.10.31 on 2025-07-19 20:12:34 GMT+7
package main

import (
	"fmt"
	"strings"
)

func slice(v any, i, j int) any {
	switch x := v.(type) {
	case []int:
		return x[i:j]
	case string:
		r := []rune(x)
		return string(r[i:j])
	}
	return nil
}

func formatValue(v any) string {
	if v == nil {
		return "nil"
	}
	switch x := v.(type) {
	case bool:
		if x {
			return "1"
		}
		return "0"
	case []int:
		parts := make([]string, len(x))
		for i, n := range x {
			parts[i] = fmt.Sprint(n)
		}
		return strings.Join(parts, " ")
	default:
		return fmt.Sprint(v)
	}
}

func printValues(vals ...any) {
	out := make([]string, len(vals))
	for i, v := range vals {
		out[i] = formatValue(v)
	}
	fmt.Println(strings.Join(out, " "))
}

func main() {
	printValues(slice([]int{1, 2, 3}, 1, 3))
	printValues(slice([]int{1, 2, 3}, 0, 2))
	printValues(slice("hello", 1, 4))
}
