//go:build ignore

// Generated by Mochi v0.10.31 on 2025-07-19 20:12:35 GMT+7
package main

import (
	"fmt"
	"strings"
)

func contains(s any, v any) any {
	switch xs := s.(type) {
	case []int:
		n, ok := v.(int)
		if !ok {
			return nil
		}
		for i, m := range xs {
			if m == n {
				return i
			}
		}
		return nil
	case string:
		str, ok := v.(string)
		if !ok {
			return 0
		}
		if strings.Contains(xs, str) {
			return 1
		}
		return 0
	}
	return nil
}

func formatValue(v any) string {
	if v == nil {
		return "nil"
	}
	switch x := v.(type) {
	case bool:
		if x {
			return "1"
		}
		return "0"
	case []int:
		parts := make([]string, len(x))
		for i, n := range x {
			parts[i] = fmt.Sprint(n)
		}
		return strings.Join(parts, " ")
	default:
		return fmt.Sprint(v)
	}
}

func printValues(vals ...any) {
	out := make([]string, len(vals))
	for i, v := range vals {
		out[i] = formatValue(v)
	}
	fmt.Println(strings.Join(out, " "))
}

func main() {
	s := "catch"
	printValues(contains(s, "cat"))
	printValues(contains(s, "dog"))
}
