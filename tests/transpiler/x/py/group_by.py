# Code generated by Mochi v0.10.34 - DO NOT EDIT
from dataclasses import dataclass
from typing import List, Dict
import json

@dataclass
class People:
    name: str
    age: int
    city: str

people = [People("Alice", 30, "Paris"), People("Bob", 15, "Hanoi"), People("Charlie", 65, "Paris"), People("Diana", 45, "Hanoi"), People("Eve", 70, "Paris"), People("Frank", 22, "Hanoi")]
@dataclass
class StatGroup:
    key: any
    items: List[dict]

_stats_groups = {}
for person in people:
    _g = _stats_groups.setdefault(person.city, StatGroup(person.city, []))
    _g.items.append(person)
@dataclass
class Stat:
    city: any
    count: int
    avg_age: float

stats = [Stat(g.key, len(g.items), (sum([p.age for p in g.items]) / len([p.age for p in g.items]) if [p.age for p in g.items] else 0.0)) for g in _stats_groups.values()]
print("--- People grouped by city ---")
for s in stats:
    print(" ".join(str(x) for x in [json.dumps(s.city), ": count =", json.dumps(s.count), ", avg_age =", json.dumps(s.avg_age)]).rstrip())
