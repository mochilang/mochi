# Generated by Mochi transpiler v0.10.31 on 2025-07-20 08:11 +0700
import os
import yaml

people = yaml.safe_load(open(os.path.join(os.environ.get('MOCHI_ROOT', ''), 'tests', "interpreter/valid/people.yaml")))
adults = [{"name": p["name"], "email": p["email"]} for p in [p for p in people if p["age"] >= 18]]
for a in adults:
    print(a["name"], a["email"])
