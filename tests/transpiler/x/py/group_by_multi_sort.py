# Code generated by Mochi transpiler.
# Version 0.10.34, generated on 2025-07-22 10:04 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import Any, List, Dict
import json

@dataclass
class Item:
    a: str
    b: int
    val: int

items = [Item("x", 1, 2), Item("x", 2, 3), Item("y", 1, 4), Item("y", 2, 1)]
@dataclass
class GroupedGroup:
    key: Dict[str, Any]
    items: list

_grouped_groups = {}
for i in items:
    _g = _grouped_groups.setdefault(tuple([i.a, i.b]), GroupedGroup(tuple([i.a, i.b]), []))
    _g.items.append(i)
@dataclass
class Grouped:
    a: str
    b: Any
    total: Any

grouped = [Grouped(g.key.a, g.key.b, sum([x.val for x in g.items])) for g in _grouped_groups.values()]
print(json.dumps(grouped))
