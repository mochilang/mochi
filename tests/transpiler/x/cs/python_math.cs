// Generated by Mochi 0.10.34 on 2025-07-22 09:58 +0700
using System;

class Program {
    static double r = 3;
    static var area = (math.pi * math.pow(r, 2));
    static var root = math.sqrt(49);
    static var sin45 = math.sin((math.pi / 4));
    static var log_e = math.log(math.e);
    static void Main() {
        Console.WriteLine(("[" + (string.Join(", ", new object[]{"Circle area with r =", r, "=>", area}) + "]")));
        Console.WriteLine(("[" + (string.Join(", ", new object[]{"Square root of 49:", root}) + "]")));
        Console.WriteLine(("[" + (string.Join(", ", new object[]{"sin(π/4):", sin45}) + "]")));
        Console.WriteLine(("[" + (string.Join(", ", new object[]{"log(e):", log_e}) + "]")));
    }
}
