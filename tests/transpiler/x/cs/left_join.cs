// Generated by Mochi
using System;
using System.Collections.Generic;
using System.Linq;

struct Customer {
    public int id;
    public string name;
}
struct Order {
    public int id;
    public int customerId;
    public int total;
}
class Program {
    static Customer[] customers = new[]{new Customer{id = 1, name = "Alice"}, new Customer{id = 2, name = "Bob"}};
    static Order[] orders = new[]{new Order{id = 100, customerId = 1, total = 250}, new Order{id = 101, customerId = 3, total = 80}};
    static Dictionary<string, object>[] result = (from o in orders join c in customers on o.customerId equals c.id into cTmp from c in cTmp.DefaultIfEmpty() select new Dictionary<string, object>{{"orderId", o.id}, {"customer", c}, {"total", o.total}}).ToArray();
    static void Main() {
        Console.WriteLine("--- Left Join ---");
        foreach (var entry in result) {
    Console.WriteLine(string.Join(" ", new[]{"Order", entry.orderId, "customer", entry.customer, "total", entry.total}).TrimEnd());
}
    }
}
