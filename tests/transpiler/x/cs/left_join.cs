// Generated by Mochi 0.10.32 on 2025-07-21 12:05 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;

struct Customer {
    public int id;
    public string name;
    public override string ToString() => JsonSerializer.Serialize(this);
}
struct Order {
    public int id;
    public int customerId;
    public int total;
    public override string ToString() => JsonSerializer.Serialize(this);
}
struct OResult {
    public int orderId;
    public Customer customer;
    public int total;
    public override string ToString() => JsonSerializer.Serialize(this);
}
class Program {
    static Customer[] customers = new Customer[]{new Customer{id = 1, name = "Alice"}, new Customer{id = 2, name = "Bob"}};
    static Order[] orders = new Order[]{new Order{id = 100, customerId = 1, total = 250}, new Order{id = 101, customerId = 3, total = 80}};
    static OResult[] result = (from o in orders join c in customers on o.customerId equals c.id into cTmp from c in cTmp.DefaultIfEmpty() select new OResult{orderId = o.id, customer = c, total = o.total}).ToArray();
    static void Main() {
        Console.WriteLine("--- Left Join ---");
        foreach (var entry in result) {
    Console.WriteLine(string.Join(" ", new object[]{"Order", entry.orderId, "customer", entry.customer, "total", entry.total}).TrimEnd());
}
    }
}
