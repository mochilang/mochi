// Generated by Mochi
using System;
using System.Collections.Generic;
using System.Linq;

class Program {
    static int[] nums = new[]{1, 2};
    static string[] letters = new[]{"A", "B"};
    static bool[] bools = new[]{true, false};
    static Dictionary<string, object>[] combos = (from n in nums from l in letters from b in bools select new Dictionary<string, object>{{"n", n}, {"l", l}, {"b", b}}).ToArray();
    static void Main() {
        Console.WriteLine("--- Cross Join of three lists ---");
        foreach (var c in combos) {
    Console.WriteLine(string.Join(" ", new[]{c.n, c.l, c.b}).TrimEnd());
}
    }
}
