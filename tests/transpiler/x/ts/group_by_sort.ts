// Generated by Mochi v0.10.32 on 2025-07-20 22:13:02 GMT+7

interface Item { cat: string; val: number };
interface Result { cat: any; total: number };
const items: Item[] = [{cat: "a", val: 3}, {cat: "a", val: 1}, {cat: "b", val: 5}, {cat: "b", val: 2}];
const grouped: Result[] = (() => {
  const result: Result[] = []
  for (const i of items) {
    result.push({k: -(() => {
  const result = []
  for (const x of g) {
    result.push(x["val"])
  }
  const out = result
  return out
})().reduce((a, b) => a + b, 0), v: {cat: g["key"], total: (() => {
  const result = []
  for (const x of g) {
    result.push(x["val"])
  }
  const out = result
  return out
})().reduce((a, b) => a + b, 0)}})
  }
  result.sort((a, b) => {const ak = JSON.stringify(a.k); const bk = JSON.stringify(b.k); return ak < bk ? -1 : ak > bk ? 1 : 0})
  const out = result.map(r => r.v)
  return out
})();
console.log(grouped.map((x) => JSON.stringify(x).replace(RegExp(":", "g"), ": ").replace(RegExp(",", "g"), ", ")).join(" "));
