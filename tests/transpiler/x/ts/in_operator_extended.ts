// Generated by Mochi v0.10.33 on 2025-07-21 18:23:27 GMT+7

export interface M { a: number }
const xs: number[] = [1, 2, 3];
const ys: number[] = (() => {
  const result = []
  for (const x of xs) {
    if (((x % 2) == 1)) {
      result.push(x)
    }
  }
  const out = result
  return out
})();
console.log([ys.includes(1)].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log([ys.includes(2)].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
const m: M = {a: 1};
console.log([("a" in m)].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log([("b" in m)].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
const s: string = "hello";
console.log([s.includes("ell")].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log([s.includes("foo")].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
