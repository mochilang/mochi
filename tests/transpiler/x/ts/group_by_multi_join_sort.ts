// Generated by Mochi v0.10.34 on 2025-07-21 21:46:27 GMT+7

export interface Nation { n_nationkey: number; n_name: string }
export interface Customer { c_custkey: number; c_name: string; c_acctbal: number; c_nationkey: number; c_address: string; c_phone: string; c_comment: string }
export interface Order { o_orderkey: number; o_custkey: number; o_orderdate: string }
export interface Lineitem { l_orderkey: number; l_returnflag: string; l_extendedprice: number; l_discount: number }
export interface Result { c_custkey: any; c_name: any; revenue: number; c_acctbal: any; n_name: any; c_address: any; c_phone: any; c_comment: any }
const nation: Nation[] = [{n_nationkey: 1, n_name: "BRAZIL"}];
const customer: Customer[] = [{c_custkey: 1, c_name: "Alice", c_acctbal: 100.0, c_nationkey: 1, c_address: "123 St", c_phone: "123-456", c_comment: "Loyal"}];
const orders: Order[] = [{o_orderkey: 1000, o_custkey: 1, o_orderdate: "1993-10-15"}, {o_orderkey: 2000, o_custkey: 1, o_orderdate: "1994-01-02"}];
const lineitem: Lineitem[] = [{l_orderkey: 1000, l_returnflag: "R", l_extendedprice: 1000.0, l_discount: 0.1}, {l_orderkey: 2000, l_returnflag: "N", l_extendedprice: 500.0, l_discount: 0.0}];
const start_date: string = "1993-10-01";
const end_date: string = "1994-01-01";
const result: Record<string, any>[] = (() => {
  const _groups: Record<string, {key: any; items: any[]}> = {}
  const _order: string[] = []
  let _items = customer.map(v => [v])
  { const _joined = []
    const _arr = orders
    for (const _left of _items) {
      const [c] = _left;
      let _m = false;
      for (let _ri=0; _ri < _arr.length; _ri++) {
        const o = _arr[_ri];
        if (!((o["o_custkey"] == c["c_custkey"]))) continue;
        _m = true; _joined.push([..._left, o]) }
    }
    _items = _joined;
  }
  { const _joined = []
    const _arr = lineitem
    for (const _left of _items) {
      const [c, o] = _left;
      let _m = false;
      for (let _ri=0; _ri < _arr.length; _ri++) {
        const l = _arr[_ri];
        if (!((l["l_orderkey"] == o["o_orderkey"]))) continue;
        _m = true; _joined.push([..._left, l]) }
    }
    _items = _joined;
  }
  { const _joined = []
    const _arr = nation
    for (const _left of _items) {
      const [c, o, l] = _left;
      let _m = false;
      for (let _ri=0; _ri < _arr.length; _ri++) {
        const n = _arr[_ri];
        if (!((n["n_nationkey"] == c["c_nationkey"]))) continue;
        _m = true; _joined.push([..._left, n]) }
    }
    _items = _joined;
  }
  for (const _it of _items) {
    const [c, o, l, n] = _it;
    if (!((((o["o_orderdate"] >= start_date) && (o["o_orderdate"] < end_date)) && (l["l_returnflag"] == "R")))) continue
    const _k = {c_custkey: c["c_custkey"], c_name: c["c_name"], c_acctbal: c["c_acctbal"], c_address: c["c_address"], c_phone: c["c_phone"], c_comment: c["c_comment"], n_name: n["n_name"]}
    const _ks = JSON.stringify(_k)
    let _g = _groups[_ks]
    if (!_g) { _g = {key: _k, items: []}; _groups[_ks] = _g; _order.push(_ks) }
    _g.items.push({c, o, l, n})
  }
  const result: Result[] = []
  const _pairs = _order.map(ks => { const g = _groups[ks]; return {g: g, key: -(() => {
  const result = []
  for (const x of g["items"]) {
    result.push((x["l"]["l_extendedprice"] * (1 - x["l"]["l_discount"])))
  }
  const out = result
  return out
})().reduce((a, b) => a + b, 0.0)} })
  _pairs.sort((a,b)=>{const ak=a.key;const bk=b.key;if(ak<bk)return -1;if(ak>bk)return 1;const sak=JSON.stringify(ak);const sbk=JSON.stringify(bk);return sak<sbk?-1:sak>sbk?1:0})
  for (const p of _pairs) {
    const g = p.g
    result.push({c_custkey: g["key"]["c_custkey"], c_name: g["key"]["c_name"], revenue: (() => {
  const result = []
  for (const x of g["items"]) {
    result.push((x["l"]["l_extendedprice"] * (1 - x["l"]["l_discount"])))
  }
  const out = result
  return out
})().reduce((a, b) => a + b, 0.0), c_acctbal: g["key"]["c_acctbal"], n_name: g["key"]["n_name"], c_address: g["key"]["c_address"], c_phone: g["key"]["c_phone"], c_comment: g["key"]["c_comment"]})
  }
  return result
})();
console.log("[" + result.map(v => { if (typeof v === 'string') return '\'' + v + '\''; if (typeof v === 'number') return (Number.isInteger(v) ? v.toFixed(1) : String(v)); if (typeof v === 'boolean') return v ? 'True' : 'False'; if (typeof v === 'object') { let s = JSON.stringify(v).replace(/"/g, '\'' ).replace(/:/g, ': ').replace(/,/g, ', '); s = s.replace(/: (-?[0-9]+)([,}])/g, ': $1.0$2'); s = s.replace(/('[^']*(?:id|key)'\s*: )(-?[0-9]+)\.0([,}])/g, '$1$2$3'); return s } return String(v); }).join(', ') + "]");
