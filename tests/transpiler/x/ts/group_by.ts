// Generated by Mochi v0.10.32 on 2025-07-21 07:25:18 GMT+7

interface People { name: string; age: number; city: string };
interface Result { city: any; count: number; avg_age: number };
const people: People[] = [{name: "Alice", age: 30, city: "Paris"}, {name: "Bob", age: 15, city: "Hanoi"}, {name: "Charlie", age: 65, city: "Paris"}, {name: "Diana", age: 45, city: "Hanoi"}, {name: "Eve", age: 70, city: "Paris"}, {name: "Frank", age: 22, city: "Hanoi"}];
const stats: Result[] = (() => {
  const result: Result[] = []
  for (const person of people) {
    result.push({city: g["key"], count: (Array.isArray(g) || typeof g === 'string' ? g.length : Object.keys(g ?? {}).length), avg_age: (() => { const arr = (() => {
  const result = []
  for (const p of g) {
    result.push(p["age"])
  }
  const out = result
  return out
})(); return arr.reduce((a, b) => a + b, 0) / arr.length; })()})
  }
  const out = result
  return out
})();
console.log("--- People grouped by city ---");
for (const s of stats) {
  console.log(s["city"], ": count =", s["count"], ", avg_age =", s["avg_age"]);
}
