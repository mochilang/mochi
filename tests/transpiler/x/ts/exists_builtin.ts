// Generated by Mochi v0.10.34 on 2025-07-21 20:38:52 GMT+7

const data: number[] = [1, 2];
const flag: boolean = ((Array.isArray((() => {
  const result = []
  for (const x of data) {
    if ((x == 1)) {
      result.push(x)
    }
  }
  const out = result
  return out
})()) || typeof (() => {
  const result = []
  for (const x of data) {
    if ((x == 1)) {
      result.push(x)
    }
  }
  const out = result
  return out
})() === 'string' ? (() => {
  const result = []
  for (const x of data) {
    if ((x == 1)) {
      result.push(x)
    }
  }
  const out = result
  return out
})().length : Object.keys((() => {
  const result = []
  for (const x of data) {
    if ((x == 1)) {
      result.push(x)
    }
  }
  const out = result
  return out
})() ?? {}).length) > 0);
console.log([flag].map(v => { if (v === null) return 'nil'; if (typeof v === 'boolean') return v ? 'True' : 'False'; if (typeof v === 'number') return Number.isInteger(v) ? v.toFixed(1) : String(v); if (typeof v === 'object') return JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', '); if (typeof v === 'string') return '\'' + v + '\''; return String(v); }).join(' ').trimEnd());
