// Generated by Mochi v0.10.33 on 2025-07-21 18:23:32 GMT+7

const math = Math;
const r: number = 3.0;
const area: number = (math["PI"] * math["pow"](r, 2.0));
const root: number = "math";
const sin45: number = math["sin"]((math["PI"] / 4.0));
const log_e: number = math["log"](math["E"]);
console.log(["Circle area with r =", r, "=>", area].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log(["Square root of 49:", root].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log(["sin(π/4):", sin45].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
console.log(["log(e):", log_e].map(v => v === null ? 'nil' : typeof v === 'object' ? JSON.stringify(v).replace(/:/g, ': ').replace(/,/g, ', ') : v).join(' ').trimEnd());
