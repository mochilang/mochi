// Generated by Mochi v0.10.35 on 2025-07-22 13:14:06 GMT+7

export interface Result { n: number; l: string; b: boolean }
const nums: number[] = [1, 2];
const letters: string[] = ["A", "B"];
const bools: boolean[] = [true, false];
const combos: Result[] = (() => {
  let _items = nums.map(v => [v])
  { const _next = []
    for (const it of _items) {
      for (const l of letters) { _next.push([...it, l]) }
    }
    _items = _next }
  { const _next = []
    for (const it of _items) {
      for (const b of bools) { _next.push([...it, b]) }
    }
    _items = _next }
  let _rows = _items
  const result: Result[] = []
  for (const r of _rows) { const [n, l, b] = r; result.push({"__name": "GenType1", n, l, b}) }
  return result
})();
console.log(["--- Cross Join of three lists ---"].map(v => typeof v === 'boolean' ? (v ? 'True' : 'False') : v).filter(v => v !== '').join(' '));
for (const c of combos) {
  console.log([c["n"], c["l"], c["b"]].map(v => typeof v === 'boolean' ? (v ? 'True' : 'False') : v).filter(v => v !== '').join(' '));
}
