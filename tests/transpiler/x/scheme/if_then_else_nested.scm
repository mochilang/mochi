;; Generated by Mochi transpiler on 2025-07-19T12:36:41Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(define x 8)
(define msg (if (> x 10) "big" (if (> x 5) "medium" "small")))
(mochi-print msg)
