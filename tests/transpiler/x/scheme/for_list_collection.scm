;; Generated by Mochi transpiler on 2025-07-19T12:36:39Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(for-each (lambda (n) (begin (mochi-print n))) (list 1 2 3))
