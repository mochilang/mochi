;; Generated by Mochi transpiler on 2025-07-19T12:37:01Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(define prefix "fore")
(define s1 "forest")
(mochi-print (if (= (if (string? s1) (substring s1 0 (if (string? prefix) (string-length prefix) (length prefix))) (take (drop s1 0) (- (if (string? prefix) (string-length prefix) (length prefix)) 0))) prefix) 1 0))
(define s2 "desert")
(mochi-print (if (= (if (string? s2) (substring s2 0 (if (string? prefix) (string-length prefix) (length prefix))) (take (drop s2 0) (- (if (string? prefix) (string-length prefix) (length prefix)) 0))) prefix) 1 0))
