;; Generated by Mochi transpiler on 2025-07-19T12:36:56Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(mochi-print (if (string<? "a" "b") 1 0))
(mochi-print (if (string<=? "a" "a") 1 0))
(mochi-print (if (string>? "b" "a") 1 0))
(mochi-print (if (string>=? "b" "b") 1 0))
