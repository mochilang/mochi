;; Generated by Mochi transpiler on 2025-07-19T12:36:55Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(define nums (list 3 1 4))
(mochi-print (apply min nums))
(mochi-print (apply max nums))
