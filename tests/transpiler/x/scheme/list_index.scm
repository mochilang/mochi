;; Generated by Mochi transpiler on 2025-07-19T12:36:46Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(define xs (list 10 20 30))
(mochi-print (if (string? xs) (string-ref xs 1) (list-ref xs 1)))
