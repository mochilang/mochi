;; Generated by Mochi transpiler on 2025-07-19T12:36:57Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(mochi-print (string-append "hello " "world"))
