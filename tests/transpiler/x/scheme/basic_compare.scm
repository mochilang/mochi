;; Generated by Mochi transpiler on 2025-07-19T12:36:38Z
(import (srfi 1))
(define (mochi-print v)
  (if (list? v)
      (begin
        (let loop ((xs v) (first #t))
          (unless (null? xs)
            (unless first (display " "))
            (display (car xs))
            (loop (cdr xs) #f)))
        (newline))
      (begin (display v) (newline))))
(define a (- 10 3))
(define b (+ 2 2))
(mochi-print a)
(mochi-print (if (= a 7) 1 0))
(mochi-print (if (< b 5) 1 0))
