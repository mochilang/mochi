#!/usr/bin/env escript
-module(main).
-export([main/1, classify/1]).

% Generated by Mochi transpiler v0.10.34 (7f20f22e3) on 2025-07-22 10:27 +0700

classify(N) ->
        (case N of
    0 -> "zero";
    1 -> "one";
    _ -> "many"
end).

main(_) ->
    X = 2,
    Label = (case 2 of
    1 -> "one";
    2 -> "two";
    3 -> "three";
    _ -> "unknown"
end),
    io:format("~s~n", [Label]),
    Day = "sun",
    Mood = (case "sun" of
    "mon" -> "tired";
    "fri" -> "excited";
    "sun" -> "relaxed";
    _ -> "normal"
end),
    io:format("~s~n", [Mood]),
    Ok = true,
    Status = (case true of
    true -> "confirmed";
    false -> "denied"
end),
    io:format("~s~n", [Status]),
    io:format("~p~n", [classify(0)]),
    io:format("~p~n", [classify(5)]).
