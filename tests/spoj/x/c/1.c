// Generated by Mochi 0.10.32 on 2025-08-26 11:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


size_t append_len;
static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

void user_main();
int main(void);

void user_main() {
    while (1) {
        const char* line = _input();
        long long n = _atoi(line);
        if (n == 42LL) {
            break;
        }
        puts(line);
    }
}

int main(void) {
    user_main();
    return 0;
}
