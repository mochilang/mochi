// Generated by Mochi v0.10.74 on 2025-08-26 14:34:52 GMT+7

function precedence(op: string): number {
  if (((op === "+") || (op === "-"))) {
    return 1;
  }
  if (((op === "*") || (op === "/"))) {
    return 2;
  }
  if ((op === "^")) {
    return 3;
  }
  return 0;
}
function popTop(stack: string[]): string {
  return stack[(()=>{const _mochi_idx = Math.trunc((_len(stack) - 1)); return _mochi_idx < 0 ? stack.length + _mochi_idx : _mochi_idx;})()];
}
function popStack(stack: string[]): string[] {
  let newStack: string[] = [];
  let i: number = 0;
  while ((i < (_len(stack) - 1))) {
    newStack.push(stack[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? stack.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return newStack;
}
function toRPN(expr: string): string {
  let out: string = "";
  let stack: string[] = [];
  let i: number = 0;
  while ((i < _len(expr))) {
    let ch: string = expr[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? expr.length + _mochi_idx : _mochi_idx;})()];
    if (((ch >= "a") && (ch <= "z"))) {
      out = (out + ch);
    } else {
      if ((ch === "(")) {
        stack.push(ch);
      } else {
        if ((ch === ")")) {
          while ((_len(stack) > 0)) {
            let top: string = popTop(stack);
            if ((top === "(")) {
              stack = popStack(stack);
              break
            }
            out = (out + top);
            stack = popStack(stack);
          }
        } else {
          let prec: number = precedence(ch);
          while ((_len(stack) > 0)) {
            let top: string = popTop(stack);
            if ((top === "(")) {
              break
            }
            let topPrec: number = precedence(top);
            if (((topPrec > prec) || ((topPrec === prec) && (ch !== "^")))) {
              out = (out + top);
              stack = popStack(stack);
            } else {
              break
            }
          }
          stack.push(ch);
        }
      }
    }
    i = (i + 1);
  }
  while ((_len(stack) > 0)) {
    let top: string = popTop(stack);
    out = (out + top);
    stack = popStack(stack);
  }
  return out;
}
function main() {
  let t: number = Math.trunc(_input());
  let i: number = 0;
  while ((i < t)) {
    let expr: string = _input();
    console.log(_str(toRPN(expr)));
    i = (i + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
let _inputData: string[] | null = null;
function _input(): string {
  if (_inputData === null) {
    let data: string;
    if (typeof Deno !== 'undefined') {
      const dec = new TextDecoder();
      const chunks: string[] = [];
      const buf = new Uint8Array(1024);
      for (;;) {
        const n = Deno.stdin.readSync(buf);
        if (n === null) break;
        chunks.push(dec.decode(buf.subarray(0, n)));
        if (n < buf.length) break;
      }
      data = chunks.join('');
    } else {
      const fs = require('fs');
      data = fs.readFileSync(0, 'utf8');
    }
    _inputData = data.split(/\r?\n/);
  }
  const v = _inputData.shift();
  return v === undefined ? '' : v;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return '[' + x.map(_str).join(', ') + ']';
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

