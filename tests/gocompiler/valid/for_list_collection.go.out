package main

import (
	"fmt"
)

func main() {
	for _, n := range _iter([]any{1, 2, 3}) {
		fmt.Println(n)
	}
}

func _iter(v any) []any {
    switch s := v.(type) {
    case []any:
        return s
    case map[string]any:
        out := make([]any, 0, len(s))
        for k := range s {
            out = append(out, k)
        }
        return out
    case string:
        runes := []rune(s)
        out := make([]any, len(runes))
        for i, r := range runes {
            out[i] = string(r)
        }
        return out
    default:
        return nil
    }
}