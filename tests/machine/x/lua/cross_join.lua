-- Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
function __to_string(v)
    local t = type(v)
    if t == 'string' then return v end
    if t == 'number' or t == 'boolean' then return tostring(v) end
    if t ~= 'table' then return tostring(v) end
    if v[1] ~= nil or #v > 0 then
        local parts = {}
        for i=1,#v do parts[#parts+1] = __to_string(v[i]) end
        return '['..table.concat(parts, ', ')..']'
    end
    local keys = {}
    for k in pairs(v) do if k ~= '__name' then keys[#keys+1]=k end end
    table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
    local parts = {}
    for _,k in ipairs(keys) do parts[#parts+1] = tostring(k)..': '..__to_string(v[k]) end
    local body = table.concat(parts, ', ')
    if v.__name then return v.__name..' {'..body..'}' end
    return '{'..body..'}'
end
customers = {{["id"]=1, ["name"]="Alice"}, {["id"]=2, ["name"]="Bob"}, {["id"]=3, ["name"]="Charlie"}};
orders = {{["id"]=100, ["customerId"]=1, ["total"]=250}, {["id"]=101, ["customerId"]=2, ["total"]=125}, {["id"]=102, ["customerId"]=1, ["total"]=300}};
result = (function()
    local _res = {}
    for _, o in ipairs(orders) do
        for _, c in ipairs(customers) do
            _res[#_res+1] = {["orderId"]=o.id, ["orderCustomerId"]=o.customerId, ["pairedCustomerName"]=c.name, ["orderTotal"]=o.total}
        end
    end
    return _res
end)();
print("--- Cross Join: All order-customer pairs ---");
for _, entry in ipairs(result) do
    print(table.concat({"Order", __to_string(entry.orderId), "(customerId:", __to_string(entry.orderCustomerId), ", total: $", __to_string(entry.orderTotal), ") paired with", __to_string(entry.pairedCustomerName)}, ' '));
end
