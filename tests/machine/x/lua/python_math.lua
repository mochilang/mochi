-- Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
function __to_string(v)
    local t = type(v)
    if t == 'string' then return v end
    if t == 'number' or t == 'boolean' then return tostring(v) end
    if t ~= 'table' then return tostring(v) end
    if v[1] ~= nil or #v > 0 then
        local parts = {}
        for i=1,#v do parts[#parts+1] = __to_string(v[i]) end
        return '['..table.concat(parts, ', ')..']'
    end
    local keys = {}
    for k in pairs(v) do if k ~= '__name' then keys[#keys+1]=k end end
    table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
    local parts = {}
    for _,k in ipairs(keys) do parts[#parts+1] = tostring(k)..': '..__to_string(v[k]) end
    local body = table.concat(parts, ', ')
    if v.__name then return v.__name..' {'..body..'}' end
    return '{'..body..'}'
end
local math = { sqrt = math.sqrt, pow = math.pow, sin = math.sin, log = math.log, pi = math.pi, e = math.exp(1) }
r = 3.0;
area = (math.pi * math.pow(r, 2.0));
root = math.sqrt(49.0);
sin45 = math.sin((math.pi / 4.0));
log_e = math.log(math.e);
print(table.concat({"Circle area with r =", __to_string(r), "=>", __to_string(area)}, ' '));
print(table.concat({"Square root of 49:", __to_string(root)}, ' '));
print(table.concat({"sin(π/4):", __to_string(sin45)}, ' '));
print(table.concat({"log(e):", __to_string(log_e)}, ' '));
