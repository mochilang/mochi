-- Generated by Mochi compiler v0.10.26 on 2025-07-16T17:58:21Z
function __str(v)
    local t = type(v)
    if t == 'table' then
        if v[1] ~= nil or #v > 0 then
            local parts = {}
            for i=1,#v do parts[#parts+1] = __str(v[i]) end
            return '['..table.concat(parts, ' ')..']'
        else
            local keys = {}
            for k in pairs(v) do keys[#keys+1] = k end
            table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
            local parts = {}
            for _,k in ipairs(keys) do parts[#parts+1] = __str(k)..':'..__str(v[k]) end
            return '{'..table.concat(parts, ',')..'}'
        end
    else
        return tostring(v)
    end
end
local math = { sqrt = math.sqrt, pow = math.pow, sin = math.sin, log = math.log, pi = math.pi, e = math.exp(1) }
r = 3.0
area = (math.pi * math.pow(r, 2.0))
root = math.sqrt(49.0)
sin45 = math.sin((math.pi / 4.0))
log_e = math.log(math.e)
print(__str("Circle area with r =") .. " " .. __str(r) .. " " .. __str("=>") .. " " .. __str(area))
print(__str("Square root of 49:") .. " " .. __str(root))
print(__str("sin(π/4):") .. " " .. __str(sin45))
print(__str("log(e):") .. " " .. __str(log_e))
