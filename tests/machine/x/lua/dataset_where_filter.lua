-- Generated by Mochi compiler v0.10.26 on 2025-07-16T17:34:52Z
function __print(v)
    if type(v) == 'table' and (v[1] ~= nil or #v > 0) then
        local parts = {}
        for i=1,#v do parts[#parts+1] = __str(v[i]) end
        print(table.concat(parts, ' '))
    else
        print(__str(v))
    end
end
function __str(v)
    local t = type(v)
    if t == 'table' then
        if v[1] ~= nil or #v > 0 then
            local parts = {}
            for i=1,#v do parts[#parts+1] = __str(v[i]) end
            return '['..table.concat(parts, ' ')..']'
        else
            local keys = {}
            for k in pairs(v) do keys[#keys+1] = k end
            table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
            local parts = {}
            for _,k in ipairs(keys) do parts[#parts+1] = __str(k)..':'..__str(v[k]) end
            return '{'..table.concat(parts, ',')..'}'
        end
    else
        return tostring(v)
    end
end
people = {{["name"]="Alice", ["age"]=30}, {["name"]="Bob", ["age"]=15}, {["name"]="Charlie", ["age"]=65}, {["name"]="Diana", ["age"]=45}}
adults = (function()
    local _res = {}
    for _, person in ipairs(people) do
        if (person.age >= 18) then
            _res[#_res+1] = {["name"]=person.name, ["age"]=person.age, ["is_senior"]=(person.age >= 60)}
        end
    end
    return _res
end)()
__print("--- Adults ---")
for _, person in ipairs(adults) do
    print(__str(person.name) .. " " .. __str("is") .. " " .. __str(person.age) .. " " .. __str((function()
    if person.is_senior then
        return " (senior)"
    else
        return ""
    end
end)()))
end
