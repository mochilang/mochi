#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.25 on 2025-07-13T05:21:18Z
% load_yaml.erl - generated from load_yaml.mochi

main(_) ->
    People = [#{"__name" => "Person", name => "Alice", age => 30, email => "alice@example.com"}, #{"__name" => "Person", name => "Bob", age => 15, email => "bob@example.com"}, #{"__name" => "Person", name => "Charlie", age => 20, email => "charlie@example.com"}],
    Adults = [#{name => maps:get(name, P), email => maps:get(email, P)} || P <- People, (maps:get(age, P) >= 18)],
    lists:foreach(fun(A) -> io:format("~p ~p~n", [maps:get(name, A), maps:get(email, A)]) end, Adults).
