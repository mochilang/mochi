#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.27 on 2025-07-17T01:32:33Z
% group_by_sort.erl - generated from group_by_sort.mochi

main(_) ->
    Items = [#{cat => "a", val => 3}, #{cat => "a", val => 1}, #{cat => "b", val => 5}, #{cat => "b", val => 2}],
    Grouped = [V || {_, V} <- lists:keysort(1, [{-lists:sum([mochi_get(val, X) || X <- Val0]), #{cat => Key0, total => lists:sum([mochi_get(val, X) || X <- Val0])}} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{mochi_get(cat, I), I} || I <- Items]))])],
    io:format("~p~n", [Grouped]).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
