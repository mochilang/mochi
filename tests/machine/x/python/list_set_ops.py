# Generated by Mochi compiler v0.10.26 on 2025-07-16T17:32:02Z
from __future__ import annotations
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")


def _fmt(v):
    if isinstance(v, list):
        return " ".join((_fmt(x) for x in v))
    if v is None:
        return "null"
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    if isinstance(v, dict) or hasattr(v, "__dataclass_fields__"):
        return "[object Object]"
    return str(v)


print(_fmt(list(dict.fromkeys([1, 2] + [2, 3]))))
print(_fmt([it for it in [1, 2, 3] if it not in [2]]))
print(_fmt(list(dict.fromkeys([it for it in [1, 2, 3] if it in [2, 4]]))))
print(_fmt(len([1, 2] + [2, 3])))
