# Generated by Mochi compiler v0.10.26 on 2025-07-16T18:04:11Z
from __future__ import annotations

testpkg = {"Add": lambda a, b: a + b, "Pi": 3.14, "Answer": 42}
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


def _fmt(v):
    if isinstance(v, list):
        return " ".join((_fmt(x) for x in v))
    if v is UNDEFINED:
        return "undefined"
    if v is None:
        return "null"
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    if isinstance(v, dict) or hasattr(v, "__dataclass_fields__"):
        return "[object Object]"
    return str(v)


print(
    _fmt(
        (testpkg.get("Add") if isinstance(testpkg, dict) else getattr(testpkg, "Add"))(
            2, 3
        )
    )
)
print(_fmt(testpkg.get("Pi") if isinstance(testpkg, dict) else getattr(testpkg, "Pi")))
print(
    _fmt(
        testpkg.get("Answer")
        if isinstance(testpkg, dict)
        else getattr(testpkg, "Answer")
    )
)
