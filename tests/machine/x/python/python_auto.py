# Generated by Mochi compiler v0.10.26 on 2025-07-16T18:29:20Z
from __future__ import annotations
import math
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


def _fmt(v):
    if isinstance(v, list):
        return " ".join((_fmt(x) for x in v))
    if v is UNDEFINED:
        return "undefined"
    if v is None:
        return "null"
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    if isinstance(v, dict) or hasattr(v, "__dataclass_fields__"):
        return "[object Object]"
    return str(v)


print(
    _fmt((math.get("sqrt") if isinstance(math, dict) else getattr(math, "sqrt"))(16.0))
)
print(_fmt(math.get("pi") if isinstance(math, dict) else getattr(math, "pi")))
