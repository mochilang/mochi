# Generated by Mochi compiler v0.10.26 on 2025-07-16T17:09:33Z
from __future__ import annotations
import dataclasses
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")


def _fmt(v):
    if isinstance(v, list):
        return " ".join((_fmt(x) for x in v))
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    return str(v)


def inc(c):
    c.n = c.n + 1


@dataclasses.dataclass
class Counter:
    n: int


c = Counter(n=0)
inc(c)
print(_fmt(c.n))
