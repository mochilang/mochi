# Generated by Mochi compiler v0.10.27 on 2025-07-17T11:46:09Z
from __future__ import annotations
import dataclasses


@dataclasses.dataclass
class Item:
    n: int
    v: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


def _sort_key(k):
    if hasattr(k, "__dataclass_fields__"):
        return str(k)
    if isinstance(k, list):
        return tuple((_sort_key(x) for x in k))
    if isinstance(k, tuple):
        return tuple((_sort_key(x) for x in k))
    if isinstance(k, dict):
        return str(k)
    return k


items = [Item(n=1, v="a"), Item(n=1, v="b"), Item(n=2, v="c")]
result = [i.v for i in sorted([i for i in items], key=lambda i: _sort_key(i.n))]
print(result)
