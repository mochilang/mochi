//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-14T03:02:01Z

package main

import (
	"fmt"
)

func main() {
	customers := []Customer{Customer{
		Id:   1,
		Name: "Alice",
	}, Customer{
		Id:   2,
		Name: "Bob",
	}, Customer{
		Id:   3,
		Name: "Charlie",
	}}
	orders := []Order{Order{
		Id:         100,
		CustomerId: 1,
		Total:      250,
	}, Order{
		Id:         101,
		CustomerId: 2,
		Total:      125,
	}, Order{
		Id:         102,
		CustomerId: 1,
		Total:      300,
	}}
	result := func() []Result {
		results := []Result{}
		for _, o := range orders {
			for _, c := range customers {
				results = append(results, Result{
					OrderId:            o.Id,
					OrderCustomerId:    o.CustomerId,
					PairedCustomerName: c.Name,
					OrderTotal:         o.Total,
				})
			}
		}
		return results
	}()
	fmt.Println("--- Cross Join: All order-customer pairs ---")
	for _, entry := range result {
		fmt.Println("Order", entry.OrderId, "(customerId:", entry.OrderCustomerId, ", total: $", entry.OrderTotal, ") paired with", entry.PairedCustomerName)
	}
}
