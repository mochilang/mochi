//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2025-07-16T15:33:57Z

package main

import (
	"fmt"
	"mochi/runtime/data"
	"sort"
	"strings"

	"golang.org/x/exp/constraints"
)

type Item struct {
	Cat  string `json:"cat"`
	Val  int    `json:"val"`
	Flag bool   `json:"flag"`
}

type Result struct {
	Cat   any     `json:"cat"`
	Share float64 `json:"share"`
}

type v = Result

func main() {
	items := []Item{Item{
		Cat:  "a",
		Val:  10,
		Flag: true,
	}, Item{
		Cat:  "a",
		Val:  5,
		Flag: false,
	}, Item{
		Cat:  "b",
		Val:  20,
		Flag: true,
	}}
	result := func() []Result {
		groups := map[string]*data.Group{}
		order := []string{}
		for _, i := range items {
			key := i.Cat
			ks := fmt.Sprint(key)
			g, ok := groups[ks]
			if !ok {
				g = &data.Group{Key: key}
				groups[ks] = g
				order = append(order, ks)
			}
			g.Items = append(g.Items, i)
		}
		items := []*data.Group{}
		for _, ks := range order {
			items = append(items, groups[ks])
		}
		type pair struct {
			item *data.Group
			key  any
		}
		pairs := make([]pair, len(items))
		for idx, it := range items {
			g := it
			pairs[idx] = pair{item: it, key: g.Key.(any)}
		}
		sort.Slice(pairs, func(i, j int) bool {
			a, b := pairs[i].key, pairs[j].key
			switch av := a.(type) {
			case int:
				switch bv := b.(type) {
				case int:
					return av < bv
				case float64:
					return float64(av) < bv
				}
			case float64:
				switch bv := b.(type) {
				case int:
					return av < float64(bv)
				case float64:
					return av < bv
				}
			case string:
				bs, _ := b.(string)
				return av < bs
			}
			return fmt.Sprint(a) < fmt.Sprint(b)
		})
		for idx, p := range pairs {
			items[idx] = p.item
		}
		results := []Result{}
		for _, g := range items {
			results = append(results, Result{
				Cat: g.Key.(any),
				Share: (_sumOrdered[int](func() []int {
					results := []int{}
					for _, xRaw := range g.Items {
						x := xRaw.(Item)
						results = append(results, func() int {
							if x.Flag {
								return x.Val
							} else {
								return 0
							}
						}())
					}
					return results
				}()) / _sumOrdered[int](func() []int {
					results := []int{}
					for _, xRaw := range g.Items {
						x := xRaw.(Item)
						results = append(results, x.Val)
					}
					return results
				}())),
			})
		}
		return results
	}()
	fmt.Println(strings.TrimSpace(strings.Join([]string{strings.Trim(strings.Trim(fmt.Sprint(result), "[]"), " ")}, " ")))
}

func _sumOrdered[T constraints.Integer | constraints.Float](s []T) float64 {
	var sum float64
	for _, v := range s {
		sum += float64(v)
	}
	return sum
}
