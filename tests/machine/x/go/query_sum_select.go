//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2025-07-16T16:37:08Z

package main

import (
	"fmt"
	"strings"

	"golang.org/x/exp/constraints"
)

type v map[string]any

func main() {
	nums := []int{1, 2, 3}
	result := _sumOrdered[int](func() []int {
		results := []int{}
		for _, n := range nums {
			if n > 1 {
				if n > 1 {
					results = append(results, n)
				}
			}
		}
		return results
	}())
	fmt.Println(strings.TrimSpace(strings.Join([]string{strings.Trim(strings.Trim(fmt.Sprint(result), "[]"), " ")}, " ")))
}

func _sum[T constraints.Integer | constraints.Float](v []T) float64 {
	var sum float64
	for _, n := range v {
		sum += float64(n)
	}
	return sum
}

func _sumOrdered[T constraints.Integer | constraints.Float](s []T) float64 {
	var sum float64
	for _, v := range s {
		sum += float64(v)
	}
	return sum
}
