# Generated by Mochi compiler v0.10.27 on 2025-07-17T08:50:48Z
defmodule Main do
  @s "mochi"
  def main do
    _print([_index_string(@s, 1)])
  end

  defp _fmt(v) do
    cond do
      is_list(v) ->
        Enum.map_join(v, " ", &_fmt/1)

      v == nil ->
        "null"

      is_float(v) ->
        if v == Float.floor(v), do: Integer.to_string(trunc(v)), else: Float.to_string(v)

      is_boolean(v) ->
        if v, do: "1", else: "0"

      is_map(v) ->
        "[object Object]"

      true ->
        to_string(v)
    end
  end

  defp _index_string(s, i) do
    chars = String.graphemes(s)
    idx = if i < 0, do: i + length(chars), else: i
    if idx < 0 or idx >= length(chars), do: raise("index out of range")
    Enum.at(chars, idx)
  end

  defp _print(args) do
    IO.puts(Enum.map_join(args, " ", &_fmt/1))
  end
end

Main.main()
