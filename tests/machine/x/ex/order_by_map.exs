# Generated by Mochi compiler v0.10.27 on 2025-07-17T07:51:40Z
defmodule Main do
  def main do
    # data :: list(map())
    data = [%{a: 1, b: 2}, %{a: 1, b: 1}, %{a: 0, b: 5}]
    # sorted :: list(map())
    sorted = for x <- Enum.sort_by(data, fn x -> %{a: x.a, b: x.b} end), do: x
    _print_list(sorted)
  end

  defp _print_list(v) do
    Enum.with_index(v)
    |> Enum.each(fn {it, i} ->
      if i > 0, do: IO.write(" ")
      IO.write(inspect(it))
    end)

    IO.puts("")
  end
end

Main.main()
