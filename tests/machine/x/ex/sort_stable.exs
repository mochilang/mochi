# Generated by Mochi compiler v0.10.27 on 2025-07-17T07:52:00Z
defmodule Main do
  def main do
    # items :: list(map())
    items = [%{n: 1, v: "a"}, %{n: 1, v: "b"}, %{n: 2, v: "c"}]
    # result :: list(any())
    result = for i <- Enum.sort_by(items, fn i -> i.n end), do: i.v
    _print_list(result)
  end

  defp _print_list(v) do
    Enum.with_index(v)
    |> Enum.each(fn {it, i} ->
      if i > 0, do: IO.write(" ")
      IO.write(inspect(it))
    end)

    IO.puts("")
  end
end

Main.main()
