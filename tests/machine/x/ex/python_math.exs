# Generated by Mochi compiler v0.10.26 on 2025-07-16T16:28:49Z
defmodule Main do
  @r 3
  def main do
    # area :: float()
    area = :math.pi() * :math.pow(@r, 2)
    # root :: float()
    root = :math.sqrt(49)
    # sin45 :: float()
    sin45 = :math.sin(:math.pi() / 4)
    # log_e :: float()
    log_e = :math.log(:math.exp(1))
    IO.puts(Enum.join(Enum.map(["Circle area with r =", @r, "=>", area], &inspect(&1)), " "))
    IO.puts(Enum.join(Enum.map(["Square root of 49:", root], &inspect(&1)), " "))
    IO.puts(Enum.join(Enum.map(["sin(π/4):", sin45], &inspect(&1)), " "))
    IO.puts(Enum.join(Enum.map(["log(e):", log_e], &inspect(&1)), " "))
  end
end

Main.main()
