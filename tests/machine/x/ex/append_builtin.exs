# Generated by Mochi compiler v0.10.27 on 2025-07-17T07:50:04Z
defmodule Main do
  @a [1, 2]
  def main do
    _print_list(@a ++ [3])
  end

  defp _print_list(v) do
    Enum.with_index(v)
    |> Enum.each(fn {it, i} ->
      if i > 0, do: IO.write(" ")
      IO.write(inspect(it))
    end)

    IO.puts("")
  end
end

Main.main()
