// Generated by Mochi C# compiler
using System;
using System.Collections.Generic;
using System.Linq;

public class Program
{
    public static void Main()
    {
        var items = new Dictionary<string, dynamic>[] { new Dictionary<string, dynamic> { { "cat", "a" }, { "val", 3 } }, new Dictionary<string, dynamic> { { "cat", "a" }, { "val", 1 } }, new Dictionary<string, dynamic> { { "cat", "b" }, { "val", 5 } }, new Dictionary<string, dynamic> { { "cat", "b" }, { "val", 2 } } };
        var grouped = _group_by(items, i => i["cat"]).Select(g => new Dictionary<string, dynamic> { { "cat", g["key"] }, { "total", Enumerable.Sum(g.Select(x => x["val"]).Select(_tmp2 => Convert.ToDouble(_tmp2))) } }).OrderBy(g => (-Enumerable.Sum(g.Select(x => x["val"]).Select(_tmp3 => Convert.ToDouble(_tmp3))))).ToList();
        Console.WriteLine(grouped);
    }
    static List<_Group> _group_by(IEnumerable<dynamic> src, Func<dynamic, dynamic> keyfn)
    {
        var groups = new Dictionary<string, _Group>();
        var order = new List<string>();
        foreach (var it in src)
        {
            var key = keyfn(it);
            var ks = Convert.ToString(key);
            if (!groups.TryGetValue(ks, out var g))
            {
                g = new _Group(key);
                groups[ks] = g;
                order.Add(ks);
            }
            g.Items.Add(it);
        }
        var res = new List<_Group>();
        foreach (var k in order) res.Add(groups[k]);
        return res;
    }

    public class _Group
    {
        public dynamic key;
        public List<dynamic> Items = new List<dynamic>();
        public _Group(dynamic k) { key = k; }
    }

}
