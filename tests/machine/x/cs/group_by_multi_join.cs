// Generated by Mochi C# compiler
using System;
using System.Collections.Generic;
using System.IO;
using System.Net.Http;
using System.Text;
using System.Web;
using System.Linq;

public class Program
{
    public static void Main()
    {
        var nations = new Dictionary<string, dynamic>[] { new Dictionary<string, dynamic> { { "id", 1L }, { "name", "A" } }, new Dictionary<string, dynamic> { { "id", 2L }, { "name", "B" } } };
        Dictionary<string, long>[] suppliers = new Dictionary<string, long>[] { new Dictionary<string, long> { { "id", 1L }, { "nation", 1L } }, new Dictionary<string, long> { { "id", 2L }, { "nation", 2L } } };
        var partsupp = new Dictionary<string, dynamic>[] { new Dictionary<string, dynamic> { { "part", 100L }, { "supplier", 1L }, { "cost", 10.000000 }, { "qty", 2L } }, new Dictionary<string, dynamic> { { "part", 100L }, { "supplier", 2L }, { "cost", 20.000000 }, { "qty", 1L } }, new Dictionary<string, dynamic> { { "part", 200L }, { "supplier", 1L }, { "cost", 5.000000 }, { "qty", 3L } } };
        var filtered = new Func<List<Dictionary<string, dynamic>>>(() =>
        {
            var _res = new List<Dictionary<string, dynamic>>();
            foreach (var ps in partsupp)
            {
                foreach (var s in suppliers)
                {
                    if (!((s["id"] == ps["supplier"]))) continue;
                    foreach (var n in nations)
                    {
                        if (!((n["id"] == s["nation"]))) continue;
                        if (!((n.name == "A"))) continue;
                        _res.Add(new Dictionary<string, dynamic> { { "part", ps["part"] }, { "value", (ps["cost"] * ps["qty"]) } });
                    }
                }
            }
            return _res;
        })();
        var grouped = _group_by(filtered, x => x["part"]).Select(g => new Dictionary<string, dynamic> { { "part", g["key"] }, { "total", _sum(new List<dynamic>(g.Select(r => r.value))) } }).ToList();
        Console.WriteLine(grouped);
    }
    static double _sum(dynamic v)
    {
        if (v == null) return 0.0;
        double _sum = 0;
        foreach (var it in v)
        {
            _sum += Convert.ToDouble(it);
        }
        return _sum;
    }

    static List<_Group> _group_by(IEnumerable<dynamic> src, Func<dynamic, dynamic> keyfn)
    {
        var groups = new Dictionary<string, _Group>();
        var order = new List<string>();
        foreach (var it in src)
        {
            var key = keyfn(it);
            var ks = Convert.ToString(key);
            if (!groups.TryGetValue(ks, out var g))
            {
                g = new _Group(key);
                groups[ks] = g;
                order.Add(ks);
            }
            g.Items.Add(it);
        }
        var res = new List<_Group>();
        foreach (var k in order) res.Add(groups[k]);
        return res;
    }

    public class _Group
    {
        public dynamic key;
        public List<dynamic> Items = new List<dynamic>();
        public _Group(dynamic k) { key = k; }
    }

}
