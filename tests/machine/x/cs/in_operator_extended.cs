// Generated by Mochi C# compiler
using System;
using System.Collections.Generic;
using System.IO;
using System.Net.Http;
using System.Text;
using System.Web;
using System.Linq;

public class Program
{
    public static void Main()
    {
        long[] xs = new long[] { 1L, 2L, 3L };
        long[] ys = new List<dynamic>(xs.Select(x => x));
        Console.WriteLine(_in(1L, ys));
        Console.WriteLine(_in(2L, ys));
        Dictionary<string, long> m = new Dictionary<string, long> { { "a", 1L } };
        Console.WriteLine(_in("a", m));
        Console.WriteLine(_in("b", m));
        string s = "hello";
        Console.WriteLine(_in("ell", s));
        Console.WriteLine(_in("foo", s));
    }
    static bool _in(dynamic item, dynamic col)
    {
        if (col is string s && item is string sub)
        {
            return s.Contains(sub);
        }
        if (col is System.Collections.IDictionary d)
        {
            return d.Contains(item);
        }
        if (col is System.Collections.IEnumerable e)
        {
            foreach (var it in e)
            {
                if (Equals(it, item)) return true;
            }
            return false;
        }
        return false;
    }

}
