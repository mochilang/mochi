// Generated by Mochi experimental C compiler
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

static int min_int(const int *arr, int n) {
    int m = arr[0];
    for (int i = 1; i < n; i++) if (arr[i] < m) m = arr[i];
    return m;
}

static int max_int(const int *arr, int n) {
    int m = arr[0];
    for (int i = 1; i < n; i++) if (arr[i] > m) m = arr[i];
    return m;
}

int main() {
    int nums[] = {3, 1, 4};
    printf("%d\n", min_int(nums, sizeof(nums)/sizeof(nums[0])));
    printf("%d\n", max_int(nums, sizeof(nums)/sizeof(nums[0])));
    return 0;
}
