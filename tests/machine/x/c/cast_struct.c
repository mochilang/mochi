// Generated by Mochi compiler v0.10.25 on 2025-07-14T02:49:27Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Todo Todo;

typedef struct Todo {
  char *title;
} Todo;
typedef struct {
  int len;
  todo_t *data;
} todo_list_t;
static todo_list_t create_todo_list(int len) {
  todo_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(todo_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  todo_t todo = (todo_t){.title = "hi"};
  {
    int first = 1;
    if (!first && strlen(todo.title) > 0)
      printf(" ");
    if (strlen(todo.title) > 0)
      printf("%s", todo.title);
    if (strlen(todo.title) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
