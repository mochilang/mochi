// Generated by Mochi compiler v0.10.24 on 2025-07-13T03:59:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
int main() {
  {
    int first = 1;
    if (!first && strlen(slice_string("mochi", 1, 4)) > 0)
      printf(" ");
    if (strlen(slice_string("mochi", 1, 4)) > 0)
      printf("%s", slice_string("mochi", 1, 4));
    if (strlen(slice_string("mochi", 1, 4)) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
