// Generated by Mochi compiler v0.10.25 on 2025-07-14T02:49:31Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int inner;
} outer_item_t;
typedef struct {
  int len;
  outer_item_t *data;
} outer_item_list_t;
static outer_item_list_t create_outer_item_list(int len) {
  outer_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(outer_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  outer_item_t outer;
} data_item_t;
typedef struct {
  int len;
  data_item_t *data;
} data_item_list_t;
static data_item_list_t create_data_item_list(int len) {
  data_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(data_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  data_item_t data = (data_item_t){.outer = (outer_item_t){.inner = 1}};
  data.outer.inner = 2;
  {
    int first = 1;
    if (!first)
      printf(" ");
    printf("%d", data.outer.inner);
    first = 0;
    printf("\n");
  }
  return 0;
}
