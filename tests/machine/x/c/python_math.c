// Generated by Mochi compiler v0.10.25 on 2025-07-14T02:49:32Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

extern double math_pi;
extern double math_e;
extern double math_sqrt(double x);
extern double math_pow(double x, double y);
extern double math_sin(double x);
extern double math_log(double x);

static double r = 3.0;

int main() {
  double area = 3.141592653589793 * (r * r);
  double root = 7;
  double sin45 = __builtin_sin(3.141592653589793 / 4.0);
  double log_e = __builtin_log(2.718281828459045);
  {
    int first = 1;
    if (!first && strlen("Circle area with r =") > 0)
      printf(" ");
    if (strlen("Circle area with r =") > 0)
      printf("%s", "Circle area with r =");
    if (strlen("Circle area with r =") > 0)
      first = 0;
    if (!first)
      printf(" ");
    printf("%.17g", r);
    first = 0;
    if (!first && strlen("=>") > 0)
      printf(" ");
    if (strlen("=>") > 0)
      printf("%s", "=>");
    if (strlen("=>") > 0)
      first = 0;
    if (!first)
      printf(" ");
    printf("%.17g", area);
    first = 0;
    printf("\n");
  }
  {
    int first = 1;
    if (!first && strlen("Square root of 49:") > 0)
      printf(" ");
    if (strlen("Square root of 49:") > 0)
      printf("%s", "Square root of 49:");
    if (strlen("Square root of 49:") > 0)
      first = 0;
    if (!first)
      printf(" ");
    printf("%.17g", root);
    first = 0;
    printf("\n");
  }
  {
    int first = 1;
    if (!first && strlen("sin(π/4):") > 0)
      printf(" ");
    if (strlen("sin(π/4):") > 0)
      printf("%s", "sin(π/4):");
    if (strlen("sin(π/4):") > 0)
      first = 0;
    if (!first)
      printf(" ");
    printf("%.17g", sin45);
    first = 0;
    printf("\n");
  }
  {
    int first = 1;
    if (!first && strlen("log(e):") > 0)
      printf(" ");
    if (strlen("log(e):") > 0)
      printf("%s", "log(e):");
    if (strlen("log(e):") > 0)
      first = 0;
    if (!first)
      printf(" ");
    printf("%.17g", log_e);
    first = 0;
    printf("\n");
  }
  return 0;
}
