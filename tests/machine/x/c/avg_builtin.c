// Generated by Mochi experimental C compiler
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

static int sum_list(const int *a, int n) {
    int s = 0;
    for (int i = 0; i < n; i++) s += a[i];
    return s;
}

static double avg_list(const int *a, int n) {
    return sum_list(a, n) / (double)n;
}

int main() {
    printf("%g\n", avg_list((int[]){1, 2, 3}, 3));
    return 0;
}
