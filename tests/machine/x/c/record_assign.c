// Generated by Mochi compiler v0.10.24 on 2025-07-13T03:59:19Z
#include <stdio.h>
#include <stdlib.h>

typedef struct Counter Counter;

typedef struct Counter {
  int n;
} Counter;
typedef struct {
  int len;
  Counter *data;
} list_Counter;
static list_Counter list_Counter_create(int len) {
  list_Counter l;
  l.len = len;
  l.data = calloc(len, sizeof(Counter));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int inc(Counter *c) { c->n = c->n + 1; }

int main() {
  Counter c = (Counter){.n = 0};
  inc(&c);
  {
    int first = 1;
    if (!first)
      printf(" ");
    printf("%d", c.n);
    first = 0;
    printf("\n");
  }
  return 0;
}
