// Generated by Mochi compiler v0.10.25 on 2025-07-14T03:02:06Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customer_id;
  int total;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int order_id;
  int order_customer_id;
  char *paired_customer_name;
  int order_total;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  customer_t tmp1_data[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"},
                            (customer_t){.id = 3, .name = "Charlie"}};
  customer_list_t tmp1 = {3, tmp1_data};
  customer_list_t customers = tmp1;
  order_t tmp2_data[] = {(order_t){.id = 100, .customer_id = 1, .total = 250},
                         (order_t){.id = 101, .customer_id = 2, .total = 125},
                         (order_t){.id = 102, .customer_id = 1, .total = 300}};
  order_list_t tmp2 = {3, tmp2_data};
  order_list_t orders = tmp2;
  result_item_list_t tmp3 = create_result_item_list(orders.len * customers.len);
  int tmp4 = 0;
  for (int o_idx = 0; o_idx < orders.len; o_idx++) {
    order_t o = orders.data[o_idx];
    for (int c_idx = 0; c_idx < customers.len; c_idx++) {
      customer_t c = customers.data[c_idx];
      tmp3.data[tmp4] = (result_item_t){.order_id = o.id,
                                        .order_customer_id = o.customer_id,
                                        .paired_customer_name = c.name,
                                        .order_total = o.total};
      tmp4++;
    }
  }
  tmp3.len = tmp4;
  result_item_list_t result = tmp3;
        printf("--- Cross Join: All order-customer pairs ---
");
	for (int tmp5 = 0; tmp5 < result.len; tmp5++) {
    result_item_t entry = result.data[tmp5];
                printf("Order %d (customerId: %d , total: $ %d ) paired with %s
", entry.order_id, entry.order_customer_id, entry.order_total, entry.paired_customer_name);
	}
	free(tmp3.data);
	return 0;
}
