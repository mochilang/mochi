// Generated by Mochi compiler v0.10.24 on 2025-07-13T03:59:21Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *_index_string(char *s, int i) {
  int len = strlen(s);
  if (i < 0)
    i += len;
  if (i < 0 || i >= len) {
    fprintf(stderr, "index out of range\n");
    exit(1);
  }
  char *buf = (char *)malloc(2);
  buf[0] = s[i];
  buf[1] = '\0';
  return buf;
}
static char *s = "mochi";

int main() {
  {
    int first = 1;
    char *tmp1 = _index_string(s, 1);
    if (!first && strlen(tmp1) > 0)
      printf(" ");
    if (strlen(tmp1) > 0)
      printf("%s", tmp1);
    if (strlen(tmp1) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
