// Generated by Mochi compiler v0.10.25 on 2025-07-14T02:49:34Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Person Person;
typedef struct Book Book;

typedef struct Person {
  char *name;
  int age;
} Person;
typedef struct {
  int len;
  person_t *data;
} person_list_t;
static person_list_t create_person_list(int len) {
  person_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(person_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct Book {
  char *title;
  Person author;
} Book;
typedef struct {
  int len;
  book_t *data;
} book_list_t;
static book_list_t create_book_list(int len) {
  book_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(book_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  book_t book =
      (Book){.title = "Go", .author = (Person){.name = "Bob", .age = 42}};
  {
    int first = 1;
    if (!first && strlen(book.author.name) > 0)
      printf(" ");
    if (strlen(book.author.name) > 0)
      printf("%s", book.author.name);
    if (strlen(book.author.name) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
