// Generated by Mochi compiler v0.10.25 on 2025-07-14T02:49:29Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
static customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customerId;
  int total;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
static order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int orderId;
  char *customerName;
  int total;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
static result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  customer_t tmp1_data[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"},
                            (customer_t){.id = 3, .name = "Charlie"}};
  customer_list_t tmp1 = {3, tmp1_data};
  customer_list_t customers = tmp1;
  order_t tmp2_data[] = {(order_t){.id = 100, .customerId = 1, .total = 250},
                         (order_t){.id = 101, .customerId = 2, .total = 125},
                         (order_t){.id = 102, .customerId = 1, .total = 300},
                         (order_t){.id = 103, .customerId = 4, .total = 80}};
  order_list_t tmp2 = {4, tmp2_data};
  order_list_t orders = tmp2;
  result_item_list_t tmp3 =
      result_item_list_t_create(orders.len * customers.len);
  int tmp4 = 0;
  for (int tmp5 = 0; tmp5 < orders.len; tmp5++) {
    order_t o = orders.data[tmp5];
    for (int tmp6 = 0; tmp6 < customers.len; tmp6++) {
      customer_t c = customers.data[tmp6];
      if (!(o.customerId == c.id)) {
        continue;
      }
      tmp3.data[tmp4] = (result_item_t){
          .orderId = o.id, .customerName = c.name, .total = o.total};
      tmp4++;
    }
  }
  tmp3.len = tmp4;
  result_item_list_t result = tmp3;
  {
    int first = 1;
    if (!first && strlen("--- Orders with customer info ---") > 0)
      printf(" ");
    if (strlen("--- Orders with customer info ---") > 0)
      printf("%s", "--- Orders with customer info ---");
    if (strlen("--- Orders with customer info ---") > 0)
      first = 0;
    printf("\n");
  }
  for (int tmp7 = 0; tmp7 < result.len; tmp7++) {
    result_item_t entry = result.data[tmp7];
    {
      int first = 1;
      if (!first && strlen("Order") > 0)
        printf(" ");
      if (strlen("Order") > 0)
        printf("%s", "Order");
      if (strlen("Order") > 0)
        first = 0;
      if (!first)
        printf(" ");
      printf("%d", entry.orderId);
      first = 0;
      if (!first && strlen("by") > 0)
        printf(" ");
      if (strlen("by") > 0)
        printf("%s", "by");
      if (strlen("by") > 0)
        first = 0;
      if (!first && strlen(entry.customerName) > 0)
        printf(" ");
      if (strlen(entry.customerName) > 0)
        printf("%s", entry.customerName);
      if (strlen(entry.customerName) > 0)
        first = 0;
      if (!first && strlen("- $") > 0)
        printf(" ");
      if (strlen("- $") > 0)
        printf("%s", "- $");
      if (strlen("- $") > 0)
        first = 0;
      if (!first)
        printf(" ");
      printf("%d", entry.total);
      first = 0;
      printf("\n");
    }
  }
  return 0;
}
