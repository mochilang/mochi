// Generated by Mochi compiler v0.10.25 on 2025-07-13T09:57:50Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Person Person;

typedef struct {
  char *name;
  char *email;
} AdultsItem;
typedef struct {
  int len;
  AdultsItem *data;
} list_AdultsItem;
static list_AdultsItem list_AdultsItem_create(int len) {
  list_AdultsItem l;
  l.len = len;
  l.data = calloc(len, sizeof(AdultsItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct Person {
  char *name;
  int age;
  char *email;
} Person;
typedef struct {
  int len;
  Person *data;
} list_Person;
static list_Person list_Person_create(int len) {
  list_Person l;
  l.len = len;
  l.data = calloc(len, sizeof(Person));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  Person arr1[] = {(Person){.name = "Alice",
                            .age = 30,
                            .email = "alice@example.com",
                            .name = "Alice",
                            .age = 30,
                            .email = "alice@example.com"},
                   (Person){.name = "Bob",
                            .age = 15,
                            .email = "bob@example.com",
                            .name = "Bob",
                            .age = 15,
                            .email = "bob@example.com"},
                   (Person){.name = "Charlie",
                            .age = 20,
                            .email = "charlie@example.com",
                            .name = "Charlie",
                            .age = 20,
                            .email = "charlie@example.com"}};
  list_Person tmp2 = {3, arr1};
  list_Person people = tmp2;
  list_AdultsItem tmp3 = list_AdultsItem_create(people.len);
  int tmp4 = 0;
  for (int tmp5 = 0; tmp5 < people.len; tmp5++) {
    Person p = people.data[tmp5];
    if (!(p.age >= 18)) {
      continue;
    }
    tmp3.data[tmp4] = (AdultsItem){.name = p.name, .email = p.email};
    tmp4++;
  }
  tmp3.len = tmp4;
  list_AdultsItem adults = tmp3;
  for (int tmp6 = 0; tmp6 < adults.len; tmp6++) {
    AdultsItem a = adults.data[tmp6];
    {
      int first = 1;
      if (!first && strlen(a.name) > 0)
        printf(" ");
      if (strlen(a.name) > 0)
        printf("%s", a.name);
      if (strlen(a.name) > 0)
        first = 0;
      if (!first && strlen(a.email) > 0)
        printf(" ");
      if (strlen(a.email) > 0)
        printf("%s", a.email);
      if (strlen(a.email) > 0)
        first = 0;
      printf("\n");
    }
  }
  return 0;
}
