// Generated by Mochi compiler v0.10.25 on 2025-07-13T07:13:15Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static int x = 2;
static char *day = "sun";
static int ok = 1;

char *classify(int n) { return (n == 0 ? "zero" : (n == 1 ? "one" : "many")); }

int main() {
  char *label =
      (x == 1 ? "one" : (x == 2 ? "two" : (x == 3 ? "three" : "unknown")));
  {
    int first = 1;
    if (!first && strlen(label) > 0)
      printf(" ");
    if (strlen(label) > 0)
      printf("%s", label);
    if (strlen(label) > 0)
      first = 0;
    printf("\n");
  }
  char *mood =
      (day == "mon" ? "tired"
                    : (day == "fri" ? "excited"
                                    : (day == "sun" ? "relaxed" : "normal")));
  {
    int first = 1;
    if (!first && strlen(mood) > 0)
      printf(" ");
    if (strlen(mood) > 0)
      printf("%s", mood);
    if (strlen(mood) > 0)
      first = 0;
    printf("\n");
  }
  char *status = (ok == 1 ? "confirmed" : (ok == 0 ? "denied" : NULL));
  {
    int first = 1;
    if (!first && strlen(status) > 0)
      printf(" ");
    if (strlen(status) > 0)
      printf("%s", status);
    if (strlen(status) > 0)
      first = 0;
    printf("\n");
  }
  {
    int first = 1;
    if (!first && strlen(classify(0)) > 0)
      printf(" ");
    if (strlen(classify(0)) > 0)
      printf("%s", classify(0));
    if (strlen(classify(0)) > 0)
      first = 0;
    printf("\n");
  }
  {
    int first = 1;
    if (!first && strlen(classify(5)) > 0)
      printf(" ");
    if (strlen(classify(5)) > 0)
      printf("%s", classify(5));
    if (strlen(classify(5)) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
