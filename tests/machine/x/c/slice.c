// Generated by Mochi experimental C compiler
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

char* substr(const char* s, int start, int end) {
    int n = end - start;
    char* out = malloc(n + 1);
    strncpy(out, s + start, n);
    out[n] = '\0';
    return out;
}

int* list_slice(const int* arr, int start, int end) {
    int n = end - start;
    int* out = malloc(sizeof(int) * n);
    for (int i = 0; i < n; i++) out[i] = arr[start + i];
    return out;
}

void print_list(const int* arr, int n) {
    for (int i = 0; i < n; i++) {
        if (i > 0) printf(" ");
        printf("%d", arr[i]);
    }
    printf("\n");
}

int main() {
print_list(list_slice((int[]){1, 2, 3}, 1, 3), (3 - 1));
print_list(list_slice((int[]){1, 2, 3}, 0, 2), (2 - 0));
printf("%s\n", substr("hello", 1, 4));
    return 0;
}
