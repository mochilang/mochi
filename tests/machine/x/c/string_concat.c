// Generated by Mochi compiler v0.10.24 on 2025-07-13T03:59:20Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
int main() {
  {
    int first = 1;
    char *tmp1 = concat_string("hello ", "world");
    if (!first && strlen(tmp1) > 0)
      printf(" ");
    if (strlen(tmp1) > 0)
      printf("%s", tmp1);
    if (strlen(tmp1) > 0)
      first = 0;
    printf("\n");
  }
  return 0;
}
