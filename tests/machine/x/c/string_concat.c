// Generated by Mochi experimental C compiler
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

char* str_concat(const char* a, const char* b) {
    size_t len = strlen(a) + strlen(b);
    char* res = malloc(len + 1);
    strcpy(res, a);
    strcat(res, b);
    return res;
}

int main() {
printf("%s\n", str_concat("hello ", "world"));
    return 0;
}
