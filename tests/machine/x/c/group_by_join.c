// Generated by Mochi compiler v0.10.25 on 2025-07-14T03:20:46Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customer_id;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *name;
  int count;
} stats_item_t;
typedef struct {
  int len;
  stats_item_t *data;
} stats_item_list_t;
stats_item_list_t create_stats_item_list(int len) {
  stats_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(stats_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int main() {
  customer_t tmp1_data[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"}};
  customer_list_t tmp1 = {2, tmp1_data};
  customer_list_t customers = tmp1;
  order_t tmp2_data[] = {(order_t){.id = 100, .customer_id = 1},
                         (order_t){.id = 101, .customer_id = 1},
                         (order_t){.id = 102, .customer_id = 2}};
  order_list_t tmp2 = {3, tmp2_data};
  order_list_t orders = tmp2;
  stats_item_list_t tmp3 = create_stats_item_list(orders.len * customers.len);
  int tmp4 = 0;
  for (int i5 = 0; i5 < orders.len; i5++) {
    order_t o = orders.data[i5];
    for (int i6 = 0; i6 < customers.len; i6++) {
      customer_t c = customers.data[i6];
      if (!(o.customer_id == c.id)) {
        continue;
      }
      int tmp7 = -1;
      for (int i8 = 0; i8 < tmp4; i8++) {
        if (strcmp(tmp3.data[i8].name, c.name) == 0) {
          tmp7 = i8;
          break;
        }
      }
      if (tmp7 == -1) {
        tmp3.data[tmp4].name = c.name;
        tmp3.data[tmp4].count = 1;
        tmp7 = tmp4;
        tmp4++;
      } else {
        tmp3.data[tmp7].count++;
      }
    }
  }
  tmp3.len = tmp4;
  stats_item_list_t stats = tmp3;
        printf("--- Orders per customer ---
");
	for (int tmp9 = 0; tmp9 < stats.len; tmp9++) {
    stats_item_t s = stats.data[tmp9];
                printf("%s orders: %d
", s.name, s.count);
	}
	return 0;
}
