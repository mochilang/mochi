// Generated by Mochi TypeScript compiler

let m: _;
let s: string;
let xs: number[];
let ys: number[];

function main(): void {
  xs = [
    1,
    2,
    3,
  ];
  ys = xs.filter((x) => ((x % 2) == 1)).map((x) => x);
  console.log(ys.includes(1));
  console.log(ys.includes(2));
  m = { "a": 1 };
  console.log(_contains(m, "a"));
  console.log(_contains(m, "b"));
  s = "hello";
  console.log(s.includes("ell"));
  console.log(s.includes("foo"));
}
function _contains(c: any, v: any): boolean {
  if (Array.isArray(c)) return c.includes(v);
  if (typeof c === "string") return c.includes(String(v));
  if (c && typeof c === "object") {
    return Object.prototype.hasOwnProperty.call(c, String(v));
  }
  return false;
}

main();
