// Generated by Mochi TypeScript compiler

let filtered: Record<string, any>[];
let grouped: Record<string, any>[];
let nations: Record<string, any>[];
let partsupp: Record<string, any>[];
let suppliers: Record<string, number>[];

function main(): void {
  nations = [
    {
      "id": 1,
      "name": "A",
    },
    {
      "id": 2,
      "name": "B",
    },
  ];
  suppliers = [
    {
      "id": 1,
      "nation": 1,
    },
    {
      "id": 2,
      "nation": 2,
    },
  ];
  partsupp = [
    {
      "part": 100,
      "supplier": 1,
      "cost": 10,
      "qty": 2,
    },
    {
      "part": 100,
      "supplier": 2,
      "cost": 20,
      "qty": 1,
    },
    {
      "part": 200,
      "supplier": 1,
      "cost": 5,
      "qty": 3,
    },
  ];
  filtered = (() => {
    const _src = partsupp;
    const _res = [];
    for (const ps of _src) {
      for (const s of suppliers) {
        if (!(_equal(s.id, ps.supplier))) continue;
        for (const n of nations) {
          if (!(_equal(n.id, s.nation))) continue;
          if (!(_equal(n.name, "A"))) continue;
          _res.push({
            "part": ps.part,
            "value": (ps.cost * ps.qty),
          });
        }
      }
    }
    return _res;
  })();
  grouped = (() => {
    const _src = filtered;
    const _map = new Map<string, any>();
    const _order: string[] = [];
    var _items = [];
    for (const x of _src) {
      const _key = x.part;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({ ...x, x: x });
    }
    let _groups = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "part": g.key,
        "total": _sum(g.items.map((r) => r.value)),
      });
    }
    return _res;
  })();
  console.log(grouped);
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _sum(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum;
}

main();
