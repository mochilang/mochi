; Generated by Mochi compiler v0.10.26 on 2025-07-16T15:36:05Z
(ns main)

(defn _sort_key [k]
  (cond
    (map? k) (pr-str (into (sorted-map) k))
    (sequential? k) (vec k)
    :else k))
(declare data sorted)

(defn -main []
  (def data [{:a 1 :b 2} {:a 1 :b 1} {:a 0 :b 5}]) ;; list of
  (def sorted (vec (->> (for [x data] x) (sort-by (fn [x] (_sort_key {:a (:a x) :b (:b x)})))))) ;; list of
  (println sorted)
)

(-main)
