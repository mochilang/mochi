; Generated by Mochi compiler v0.10.26 on 2025-07-16T15:36:31Z
(ns main)

(defn _indexString [s i]
  (let [r (vec (seq s))
        i (if (neg? i) (+ i (count r)) i)]
    (if (or (< i 0) (>= i (count r)))
      (throw (ex-info "index out of range" {}))
      (str (nth r i)))))

(declare s)

(defn -main []
  (def s "mochi") ;; string
  (println (_indexString s 1))
)

(-main)
