// Generated by Mochi compiler v0.10.24 on 2025-07-13T02:30:21Z
#include <iostream>
#include <string>
#include <vector>

struct Combo {
  decltype(n) n;
  decltype(l) l;
  decltype(b) b;
};
inline bool operator==(const Combo &a, const Combo &b) {
  return a.n == b.n && a.l == b.l && a.b == b.b;
}
inline bool operator!=(const Combo &a, const Combo &b) { return !(a == b); }
int main() {
  std::vector<int> nums = {1, 2};
  std::vector<std::string> letters = {std::string("A"), std::string("B")};
  std::vector<bool> bools = {true, false};
  auto combos = ([&]() {
    std::vector<Combo> __items;
    for (auto n : nums) {
      for (auto l : letters) {
        for (auto b : bools) {
          __items.push_back(Combo{n, l, b});
        }
      }
    }
    return __items;
  })();
  std::cout << std::string("--- Cross Join of three lists ---") << std::endl;
  for (auto c : combos) {
    {
      std::cout << c.n;
      std::cout << ' ';
      std::cout << c.l;
      std::cout << ' ';
      std::cout << c.b;
      std::cout << std::endl;
    }
  }
  return 0;
}
