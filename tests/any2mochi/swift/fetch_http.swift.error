line 13:13: cannot find 'URLRequest' in scope
  12|   guard let url = comps?.url else { return [:] }
  13|   var req = URLRequest(url: url)
    |             ^
  14|   req.httpMethod = (opts?["method"] as? String) ?? "GET"
  15|   if let b = opts?["body"], let d = try? JSONSerialization.data(withJSONObject: b) {
