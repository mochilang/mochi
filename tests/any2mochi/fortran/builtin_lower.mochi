fun main() {
  print(str_lower('HELLO'))
}
fun str_lower() {
  for i in 1..len(v) {
    if (iachar(v(i:i)) >= iachar('A'))  &&  (iachar(v(i:i)) <= iachar('Z')) {
      r(i:i) = achar(iachar(v(i:i)) + 32)
    else {
      r(i:i) = v(i:i)
    }
}
