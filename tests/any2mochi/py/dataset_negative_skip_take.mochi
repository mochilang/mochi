type Num {
  val: int
}
let items: list<Num>
let a
let b
fun main() {
  // global items
  // items = [Num(val=1), Num(val=2), Num(val=3)]
  // global a
  // a = [x.val for x in (([x for x in items])[max((-1), 0) :])[: max(2, 0)]]
  // global b
  // b = [x.val for x in (([x for x in items])[max(1, 0) :])[: max((-1), 0)]]
  // print("---a---")
  // for v in a:
  // print(v)
  // print("---b---")
  // print(len(b))
}
