Person = []
Person["__index"] = Person
fun Person["new"](o) {
  o = o || []
  setmetatable(o, Person)
  return o
}
fun .greet() {
  return "hi " + self["name"]
}
p = Person["new"]({ "name": "Ada" })
print(p["greet"]())
