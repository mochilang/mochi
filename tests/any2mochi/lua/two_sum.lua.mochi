fun twoSum(nums, target) {
  let n = len(nums)
  for i = 0; i <= n - 1; i += 1 {
    for j = __add(i, 1); j <= n - 1; j += 1 {
      if __eq(__add(__index(nums, i), __index(nums, j)), target) {
        return [i, j]
      }
    }
  }
  return [-1, -1]
}
result = twoSum([2, 7, 11, 15], 9)
print(__index(result, 0))
print(__index(result, 1))
