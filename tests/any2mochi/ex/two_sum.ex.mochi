fun twoSum(nums, target) {
  try do
  let n = length(nums)
  for i <- 0..(n - 1) do
  for j <- (i + 1)..(n - 1) do
  let if ((nums[i] + nums[j]) = = target) do
  throw {:return, [i, j]}
}
fun main() {
  let result = twoSum([2, 7, 11, 15], 9)
  print(result[0])
  print(result[1])
}
main()
