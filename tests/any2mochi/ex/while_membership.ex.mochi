fun main() {
  let set = %{}
  let _ = set
  let {set} = Enum.reduce([1, 2, 3], {set}, fn n, {set} ->
  let set = Map.put(set, n, true)
  {set}
  end)
  let _ = set
  let i = 1
  let _ = i
  let count = 0
  let _ = count
  let t1 = fn t1, count, i ->
  try do
  if (if is_map(set), do: Map.has_key?(set, i), else: Enum.member?(set, i)) do
  let i = (i + 1)
  let count = (count + 1)
  t1.(t1, count, i)
  else
  {:ok, count, i}
}
main()
