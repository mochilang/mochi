fun Leaf() {
  {:__name
  "Leaf"
  }
}
fun Node(left, value, right) {
  {:__name
  "Node"
  left
  left
  value
  value
  right
  right}
}
fun sum_tree(t) {
  try(, catch(clojure_lang_ExceptionInfo, e, if _getMessage(e) == "return" { [value, ex_data(e)] }))
}
fun main() {
  let t = {:__name
  print(sum_tree(t))
}
main()
