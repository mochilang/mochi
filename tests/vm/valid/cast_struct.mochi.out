fun _cast_struct(ctor, m) {
  (fun(fields) => apply(ctor, map(#, get(m, %), fields)))(some_to_>(ctor, meta, arglists, first, map(keyword)) || keys(m))
}
fun Todo(title) {
  {:__name
  "Todo"
  title
  title}
}
fun main() {
  let todo = _cast_struct(Todo, {"title", "hi", })
  print([title, todo])
}
main()
