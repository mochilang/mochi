fun classify(n) {
  try(, catch(clojure_lang_ExceptionInfo, e, if _getMessage(e) == "return" { [value, ex_data(e)] }))
}
fun main() {
  let x = 2
  let label = (fun(t) => cond(t == 1, "one", t == 2, "two", t == 3, "three", else, "unknown"))(x)
  print(label)
  let day = "sun"
  let mood = (fun(t) => cond(t == "mon", "tired", t == "fri", "excited", t == "sun", "relaxed", else, "normal"))(day)
  print(mood)
  let ok = true
  let status = (fun(t) => cond(t == true, "confirmed", t == false, "denied", else, nil))(ok)
  print(status)
  print(classify(0))
  print(classify(5))
}
main()
