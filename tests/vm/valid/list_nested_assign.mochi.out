fun _indexList(xs, i) {
  (fun(idx) => if idx < 0 || idx >= count(xs) {  } else { nth(xs, idx) })(if neg_p(i) { i + count(xs) } else { i })
}
fun main() {
  let matrix = 
  let matrix = assoc_in(matrix, [1, 0], 5)
  print(_indexList(_indexList(matrix, 1), 0))
}
main()
