// Generated by Mochi TypeScript compiler

type StoreSale = {
  ss_ticket_number: number;
  ss_item_sk: number;
  ss_customer_sk: number;
  ss_store_sk: number;
  ss_net_paid: number;
};

type StoreReturn = {
  sr_ticket_number: number;
  sr_item_sk: number;
};

type Store = {
  s_store_sk: number;
  s_store_name: string;
  s_market_id: number;
  s_state: string;
  s_zip: string;
};

type Item = {
  i_item_sk: number;
  i_color: string;
  i_current_price: number;
  i_manager_id: number;
  i_units: string;
  i_size: string;
};

type Customer = {
  c_customer_sk: number;
  c_first_name: string;
  c_last_name: string;
  c_current_addr_sk: number;
  c_birth_country: string;
};

type CustomerAddress = {
  ca_address_sk: number;
  ca_state: string;
  ca_country: string;
  ca_zip: string;
};

let avg_paid: number;
let customer: Record<string, any>[];
let customer_address: Record<string, any>[];
let item: Record<string, any>[];
let result: Record<string, any>[];
let ssales: Record<string, any>[];
let store: Record<string, any>[];
let store_returns: Record<string, number>[];
let store_sales: Record<string, any>[];

function test_TPCDS_Q24_customer_net_paid(): void {
  if (
    !(_equal(result, [
      {
        "c_last_name": "Smith",
        "c_first_name": "Ann",
        "s_store_name": "Store1",
        "paid": 100,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  store_sales = [
    {
      "ss_ticket_number": 1,
      "ss_item_sk": 1,
      "ss_customer_sk": 1,
      "ss_store_sk": 1,
      "ss_net_paid": 100,
    },
    {
      "ss_ticket_number": 2,
      "ss_item_sk": 2,
      "ss_customer_sk": 2,
      "ss_store_sk": 1,
      "ss_net_paid": 50,
    },
  ];
  store_returns = [
    {
      "sr_ticket_number": 1,
      "sr_item_sk": 1,
    },
    {
      "sr_ticket_number": 2,
      "sr_item_sk": 2,
    },
  ];
  store = [
    {
      "s_store_sk": 1,
      "s_store_name": "Store1",
      "s_market_id": 5,
      "s_state": "CA",
      "s_zip": "12345",
    },
  ];
  item = [
    {
      "i_item_sk": 1,
      "i_color": "RED",
      "i_current_price": 10,
      "i_manager_id": 1,
      "i_units": "EA",
      "i_size": "M",
    },
    {
      "i_item_sk": 2,
      "i_color": "BLUE",
      "i_current_price": 20,
      "i_manager_id": 2,
      "i_units": "EA",
      "i_size": "L",
    },
  ];
  customer = [
    {
      "c_customer_sk": 1,
      "c_first_name": "Ann",
      "c_last_name": "Smith",
      "c_current_addr_sk": 1,
      "c_birth_country": "Canada",
    },
    {
      "c_customer_sk": 2,
      "c_first_name": "Bob",
      "c_last_name": "Jones",
      "c_current_addr_sk": 2,
      "c_birth_country": "USA",
    },
  ];
  customer_address = [
    {
      "ca_address_sk": 1,
      "ca_state": "CA",
      "ca_country": "USA",
      "ca_zip": "12345",
    },
    {
      "ca_address_sk": 2,
      "ca_state": "CA",
      "ca_country": "USA",
      "ca_zip": "54321",
    },
  ];
  ssales = (() => {
    const _src = store_sales;
    let _items = _query(_src, [
      {
        items: store_returns,
        on: (
          ss,
          sr,
        ) => (_equal(ss.ss_ticket_number, sr.sr_ticket_number) &&
          _equal(ss.ss_item_sk, sr.sr_item_sk)),
      },
      {
        items: store,
        on: (ss, sr, s) => (_equal(ss.ss_store_sk, s.s_store_sk)),
      },
      {
        items: item,
        on: (ss, sr, s, i) => (_equal(ss.ss_item_sk, i.i_item_sk)),
      },
      {
        items: customer,
        on: (ss, sr, s, i, c) => (_equal(ss.ss_customer_sk, c.c_customer_sk)),
      },
      {
        items: customer_address,
        on: (
          ss,
          sr,
          s,
          i,
          c,
          ca,
        ) => (_equal(c.c_current_addr_sk, ca.ca_address_sk)),
      },
    ], {
      select: (ss, sr, s, i, c, ca) => [ss, sr, s, i, c, ca],
      where: (
        ss,
        sr,
        s,
        i,
        c,
        ca,
      ) => ((!_equal(c.c_birth_country, String(ca.ca_country).toUpperCase()) &&
        _equal(s.s_zip, ca.ca_zip)) && _equal(s.s_market_id, 5)),
    });
    const _map = new Map<string, any>();
    const _order: string[] = [];
    for (const _r of _items) {
      const [ss, sr, s, i, c, ca] = _r;
      const _key = {
        "last": c.c_last_name,
        "first": c.c_first_name,
        "store_name": s.s_store_name,
        "color": i.i_color,
      };
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({
        ...ss,
        ...sr,
        ...s,
        ...i,
        ...c,
        ...ca,
        ss: ss,
        sr: sr,
        s: s,
        i: i,
        c: c,
        ca: ca,
      });
    }
    let _itemsG = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _itemsG) {
      _res.push({
        "c_last_name": g.key.last,
        "c_first_name": g.key.first,
        "s_store_name": g.key.store_name,
        "color": g.key.color,
        "netpaid": _sum(g.items.map((x) => x.ss_net_paid)),
      });
    }
    return _res;
  })();
  avg_paid = _avg(ssales.map((x) => x.netpaid));
  result = (() => {
    const _src = ssales;
    var _items = [];
    for (const x of _src) {
      if (!(_equal(x.color, "RED") && (x.netpaid > (0.05 * avg_paid)))) {
        continue;
      }
      _items.push(x);
    }
    let _pairs = _items.map((it) => {
      const x = it;
      return {
        item: it,
        key: [
          x.c_last_name,
          x.c_first_name,
          x.s_store_name,
        ],
      };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _items = _pairs.map((p) => p.item);
    const _res = [];
    for (const x of _items) {
      _res.push({
        "c_last_name": x.c_last_name,
        "c_first_name": x.c_first_name,
        "s_store_name": x.s_store_name,
        "paid": x.netpaid,
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q24_customer_net_paid();
}
function _avg(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum / list.length;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

function _sum(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum;
}

main();
