// Generated by Mochi TypeScript compiler

type StoreSale = {
  ss_sold_date_sk: number;
  ss_item_sk: number;
  ss_customer_sk: number;
  ss_ticket_number: number;
  ss_quantity: number;
  ss_store_sk: number;
};

type StoreReturn = {
  sr_returned_date_sk: number;
  sr_customer_sk: number;
  sr_item_sk: number;
  sr_ticket_number: number;
  sr_return_quantity: number;
};

type CatalogSale = {
  cs_sold_date_sk: number;
  cs_item_sk: number;
  cs_bill_customer_sk: number;
  cs_quantity: number;
};

type DateDim = {
  d_date_sk: number;
  d_quarter_name: string;
};

type Store = {
  s_store_sk: number;
  s_state: string;
};

type Item = {
  i_item_sk: number;
  i_item_id: string;
  i_item_desc: string;
};

let catalog_sales: Record<string, number>[];
let date_dim: Record<string, any>[];
let item: Record<string, any>[];
let joined: Record<string, any>[];
let result: Record<string, any>[];
let store: Record<string, any>[];
let store_returns: Record<string, number>[];
let store_sales: Record<string, number>[];

function test_TPCDS_Q17_stats(): void {
  if (
    !(_equal(result, [
      {
        "i_item_id": "I1",
        "i_item_desc": "Item 1",
        "s_state": "CA",
        "store_sales_quantitycount": 1,
        "store_sales_quantityave": 10,
        "store_sales_quantitystdev": 0,
        "store_sales_quantitycov": 0,
        "store_returns_quantitycount": 1,
        "store_returns_quantityave": 2,
        "store_returns_quantitystdev": 0,
        "store_returns_quantitycov": 0,
        "catalog_sales_quantitycount": 1,
        "catalog_sales_quantityave": 5,
        "catalog_sales_quantitystdev": 0,
        "catalog_sales_quantitycov": 0,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  store_sales = [
    {
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
      "ss_customer_sk": 1,
      "ss_ticket_number": 1,
      "ss_quantity": 10,
      "ss_store_sk": 1,
    },
  ];
  store_returns = [
    {
      "sr_returned_date_sk": 2,
      "sr_customer_sk": 1,
      "sr_item_sk": 1,
      "sr_ticket_number": 1,
      "sr_return_quantity": 2,
    },
  ];
  catalog_sales = [
    {
      "cs_sold_date_sk": 3,
      "cs_item_sk": 1,
      "cs_bill_customer_sk": 1,
      "cs_quantity": 5,
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_quarter_name": "1998Q1",
    },
    {
      "d_date_sk": 2,
      "d_quarter_name": "1998Q2",
    },
    {
      "d_date_sk": 3,
      "d_quarter_name": "1998Q3",
    },
  ];
  store = [
    {
      "s_store_sk": 1,
      "s_state": "CA",
    },
  ];
  item = [
    {
      "i_item_sk": 1,
      "i_item_id": "I1",
      "i_item_desc": "Item 1",
    },
  ];
  joined = (() => {
    const _src = store_sales;
    return _query(_src, [
      {
        items: store_returns,
        on: (
          ss,
          sr,
        ) => (((ss.ss_customer_sk == sr.sr_customer_sk) &&
          (ss.ss_item_sk == sr.sr_item_sk)) &&
          (ss.ss_ticket_number == sr.sr_ticket_number)),
      },
      {
        items: catalog_sales,
        on: (
          ss,
          sr,
          cs,
        ) => ((sr.sr_customer_sk == cs.cs_bill_customer_sk) &&
          (sr.sr_item_sk == cs.cs_item_sk)),
      },
      {
        items: date_dim,
        on: (
          ss,
          sr,
          cs,
          d1,
        ) => (_equal(ss.ss_sold_date_sk, d1.d_date_sk) &&
          _equal(d1.d_quarter_name, "1998Q1")),
      },
      {
        items: date_dim,
        on: (
          ss,
          sr,
          cs,
          d1,
          d2,
        ) => (_equal(sr.sr_returned_date_sk, d2.d_date_sk) && [
          "1998Q1",
          "1998Q2",
          "1998Q3",
        ].includes(d2.d_quarter_name)),
      },
      {
        items: date_dim,
        on: (
          ss,
          sr,
          cs,
          d1,
          d2,
          d3,
        ) => (_equal(cs.cs_sold_date_sk, d3.d_date_sk) && [
          "1998Q1",
          "1998Q2",
          "1998Q3",
        ].includes(d3.d_quarter_name)),
      },
      {
        items: store,
        on: (
          ss,
          sr,
          cs,
          d1,
          d2,
          d3,
          s,
        ) => (_equal(ss.ss_store_sk, s.s_store_sk)),
      },
      {
        items: item,
        on: (
          ss,
          sr,
          cs,
          d1,
          d2,
          d3,
          s,
          i,
        ) => (_equal(ss.ss_item_sk, i.i_item_sk)),
      },
    ], {
      select: (ss, sr, cs, d1, d2, d3, s, i) => ({
        "qty": ss.ss_quantity,
        "ret": sr.sr_return_quantity,
        "csq": cs.cs_quantity,
        "i_item_id": i.i_item_id,
        "i_item_desc": i.i_item_desc,
        "s_state": s.s_state,
      }),
    });
  })();
  result = (() => {
    const _src = joined;
    const _map = new Map<string, any>();
    const _order: string[] = [];
    var _items = [];
    for (const j of _src) {
      const _key = {
        "i_item_id": j.i_item_id,
        "i_item_desc": j.i_item_desc,
        "s_state": j.s_state,
      };
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({ ...j, j: j });
    }
    let _groups = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "i_item_id": g.key.i_item_id,
        "i_item_desc": g.key.i_item_desc,
        "s_state": g.key.s_state,
        "store_sales_quantitycount": _count(g.items.map((_) => _)),
        "store_sales_quantityave": _avg(g.items.map((x) => x.qty)),
        "store_sales_quantitystdev": 0,
        "store_sales_quantitycov": 0,
        "store_returns_quantitycount": _count(g.items.map((_) => _)),
        "store_returns_quantityave": _avg(g.items.map((x) => x.ret)),
        "store_returns_quantitystdev": 0,
        "store_returns_quantitycov": 0,
        "catalog_sales_quantitycount": _count(g.items.map((_) => _)),
        "catalog_sales_quantityave": _avg(g.items.map((x) => x.csq)),
        "catalog_sales_quantitystdev": 0,
        "catalog_sales_quantitycov": 0,
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q17_stats();
}
function _avg(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum / list.length;
}

function _count(v: any): number {
  if (Array.isArray(v)) return v.length;
  if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) return (v as any).items.length;
    if (Array.isArray((v as any).Items)) return (v as any).Items.length;
  }
  return 0;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

main();
