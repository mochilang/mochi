// Generated by Mochi TypeScript compiler

let date_dim: Record<string, number>[];
let joined: Record<string, any>[];
let month: number;
let result: Record<string, any>[];
let store: Record<string, any>[];
let store_returns: Record<string, number>[];
let store_sales: Record<string, number>[];
let year: number;

function test_TPCDS_Q50_simplified(): void {
  if (
    !(_equal(result, [
      {
        "s_store_name": "Main",
        "d30": 1,
        "d31_60": 1,
        "d61_90": 1,
        "d91_120": 1,
        "d_gt_120": 1,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  store_sales = [
    {
      "ticket": 1,
      "item": 101,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 2,
      "item": 102,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 3,
      "item": 103,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 4,
      "item": 104,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 5,
      "item": 105,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
  ];
  store_returns = [
    {
      "ticket": 1,
      "item": 101,
      "returned": 16,
      "customer": 1,
    },
    {
      "ticket": 2,
      "item": 102,
      "returned": 46,
      "customer": 1,
    },
    {
      "ticket": 3,
      "item": 103,
      "returned": 76,
      "customer": 1,
    },
    {
      "ticket": 4,
      "item": 104,
      "returned": 111,
      "customer": 1,
    },
    {
      "ticket": 5,
      "item": 105,
      "returned": 151,
      "customer": 1,
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_year": 2001,
      "d_moy": 7,
    },
    {
      "d_date_sk": 16,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 46,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 76,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 111,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 151,
      "d_year": 2001,
      "d_moy": 8,
    },
  ];
  store = [
    {
      "s_store_sk": 1,
      "s_store_name": "Main",
      "s_company_id": 1,
      "s_street_number": "1",
      "s_street_name": "Main",
      "s_street_type": "St",
      "s_suite_number": "100",
      "s_city": "City",
      "s_county": "County",
      "s_state": "CA",
      "s_zip": "12345",
    },
  ];
  year = 2001;
  month = 8;
  joined = (() => {
    const _src = store_sales;
    return _query(_src, [
      {
        items: store_returns,
        on: (
          ss,
          sr,
        ) => (((ss.ticket == sr.ticket) && (ss.item == sr.item)) &&
          (ss.customer == sr.customer)),
      },
      { items: date_dim, on: (ss, sr, d1) => (ss.sold == d1.d_date_sk) },
      {
        items: date_dim,
        on: (
          ss,
          sr,
          d1,
          d2,
        ) => (((sr.returned == d2.d_date_sk) && (d2.d_year == year)) &&
          (d2.d_moy == month)),
      },
      {
        items: store,
        on: (ss, sr, d1, d2, s) => (_equal(ss.store, s.s_store_sk)),
      },
    ], {
      select: (ss, sr, d1, d2, s) => ({
        "s": s,
        "diff": (sr.returned - ss.sold),
      }),
    });
  })();
  result = (() => {
    const _src = joined;
    const _map = new Map<string, any>();
    const _order: string[] = [];
    var _items = [];
    for (const j of _src) {
      const _key = j.s;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({ ...j, j: j });
    }
    let _groups = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "s_store_name": g.key.s_store_name,
        "d30": _count(g.items.filter((x) => (x.diff <= 30)).map((x) => 1)),
        "d31_60": _count(
          g.items.filter((x) => ((x.diff > 30) && (x.diff <= 60))).map((x) =>
            1
          ),
        ),
        "d61_90": _count(
          g.items.filter((x) => ((x.diff > 60) && (x.diff <= 90))).map((x) =>
            1
          ),
        ),
        "d91_120": _count(
          g.items.filter((x) => ((x.diff > 90) && (x.diff <= 120))).map((x) =>
            1
          ),
        ),
        "d_gt_120": _count(g.items.filter((x) => (x.diff > 120)).map((x) => 1)),
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q50_simplified();
}
function _count(v: any): number {
  if (Array.isArray(v)) return v.length;
  if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) return (v as any).items.length;
    if (Array.isArray((v as any).Items)) return (v as any).Items.length;
  }
  return 0;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

main();
