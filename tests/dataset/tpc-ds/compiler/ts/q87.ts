// Generated by Mochi TypeScript compiler

let catalog_customers: string[];
let catalog_sales: Record<string, string>[];
let result: number;
let store_customers: any[];
let store_only: any[];
let store_sales: Record<string, any>[];
let web_customers: any[];
let web_sales: any[];

function test_TPCDS_Q87_sample(): void {
  if (!(result == 87)) throw new Error("expect failed");
}

function main(): void {
  store_sales = [
    {
      "cust": "A",
      "price": 5,
    },
    {
      "cust": "B",
      "price": 30,
    },
    {
      "cust": "C",
      "price": 57,
    },
  ];
  catalog_sales = [{ "cust": "A" }];
  web_sales = [];
  store_customers = store_sales.map((s) => s.cust);
  catalog_customers = catalog_sales.map((s) => s.cust);
  web_customers = web_sales.map((s) => s.cust);
  store_only = store_customers.filter(
    (c) => ((catalog_customers.filter((x) => (_equal(x, c))).map((x) => x)
      .length == 0) &&
      (web_customers.filter((x) => (_equal(x, c))).map((x) => x).length == 0))
  ).map((c) => c);
  result = _sum(
    store_sales.filter(
      (s) => (store_only.filter((x) => (_equal(x, s.cust))).map((x) => x)
        .length > 0)
    ).map((s) => s.price),
  );
  console.log(_json(result));
  test_TPCDS_Q87_sample();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _sum(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum;
}

main();
