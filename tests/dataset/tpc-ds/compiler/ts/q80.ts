// Generated by Mochi TypeScript compiler

let catalog_sales: Record<string, number>[];
let store_sales: Record<string, number>[];
let total_profit: number;
let web_sales: Record<string, number>[];

function test_TPCDS_Q80_sample(): void {
  if (!(total_profit == 80)) throw new Error("expect failed");
}

function main(): void {
  store_sales = [
    {
      "price": 20,
      "ret": 5,
    },
    {
      "price": 10,
      "ret": 2,
    },
    {
      "price": 5,
      "ret": 0,
    },
  ];
  catalog_sales = [
    {
      "price": 15,
      "ret": 3,
    },
    {
      "price": 8,
      "ret": 1,
    },
  ];
  web_sales = [
    {
      "price": 25,
      "ret": 5,
    },
    {
      "price": 15,
      "ret": 8,
    },
    {
      "price": 8,
      "ret": 2,
    },
  ];
  total_profit =
    (_sum(store_sales.map((s) => (s.price - s.ret))) +
      _sum(catalog_sales.map((c) => (c.price - c.ret)))) +
    _sum(web_sales.map((w) => (w.price - w.ret)));
  console.log(_json(total_profit));
  test_TPCDS_Q80_sample();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _sum(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum;
}

main();
