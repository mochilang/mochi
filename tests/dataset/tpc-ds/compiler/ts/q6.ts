// Generated by Mochi TypeScript compiler

let customer: Record<string, number>[];
let customer_address: Record<string, any>[];
let date_dim: Record<string, number>[];
let item: Record<string, any>[];
let result: Record<string, any>[];
let store_sales: Record<string, number>[];
let target_month_seq: number;

function test_TPCDS_Q6_result(): void {
  if (
    !(_equal(result, [
      {
        "state": "CA",
        "cnt": 10,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  customer_address = [
    {
      "ca_address_sk": 1,
      "ca_state": "CA",
      "ca_zip": "12345",
    },
  ];
  customer = [
    {
      "c_customer_sk": 1,
      "c_current_addr_sk": 1,
    },
  ];
  store_sales = [
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
    {
      "ss_customer_sk": 1,
      "ss_sold_date_sk": 1,
      "ss_item_sk": 1,
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_year": 1999,
      "d_moy": 5,
      "d_month_seq": 120,
    },
  ];
  item = [
    {
      "i_item_sk": 1,
      "i_category": "A",
      "i_current_price": 100,
    },
    {
      "i_item_sk": 2,
      "i_category": "A",
      "i_current_price": 50,
    },
  ];
  target_month_seq = _max(
    date_dim.filter((d) => ((d.d_year == 1999) && (d.d_moy == 5))).map((d) =>
      d.d_month_seq
    ),
  );
  result = (() => {
    const _src = customer_address;
    let _items = _query(_src, [
      {
        items: customer,
        on: (a, c) => (_equal(a.ca_address_sk, c.c_current_addr_sk)),
      },
      {
        items: store_sales,
        on: (a, c, s) => (c.c_customer_sk == s.ss_customer_sk),
      },
      {
        items: date_dim,
        on: (a, c, s, d) => (s.ss_sold_date_sk == d.d_date_sk),
      },
      {
        items: item,
        on: (a, c, s, d, i) => (_equal(s.ss_item_sk, i.i_item_sk)),
      },
    ], {
      select: (a, c, s, d, i) => [a, c, s, d, i],
      where: (
        a,
        c,
        s,
        d,
        i,
      ) => ((d.d_month_seq == target_month_seq) &&
        (i.i_current_price >
          (1.2 *
            _avg(
              item.filter((j) => (_equal(j.i_category, i.i_category))).map(
                (j) => j.i_current_price
              ),
            )))),
    });
    const _map = new Map<string, any>();
    const _order: string[] = [];
    for (const _r of _items) {
      const [a, c, s, d, i] = _r;
      const _key = a.ca_state;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({
        ...a,
        ...c,
        ...s,
        ...d,
        ...i,
        a: a,
        c: c,
        s: s,
        d: d,
        i: i,
      });
    }
    let _itemsG = _order.map((k) => _map.get(k)!);
    let _pairs = _itemsG.map((it) => {
      const g = it;
      return {
        item: it,
        key: [
          _count(g),
          g.key,
        ],
      };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _itemsG = _pairs.map((p) => p.item);
    {
      const _n = 100;
      if (_n < _itemsG.length) _itemsG = _itemsG.slice(0, _n);
    }
    const _res = [];
    for (const g of _itemsG) {
      _res.push({
        "state": g.key,
        "cnt": _count(g),
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q6_result();
}
function _avg(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum / list.length;
}

function _count(v: any): number {
  if (Array.isArray(v)) return v.length;
  if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) return (v as any).items.length;
    if (Array.isArray((v as any).Items)) return (v as any).Items.length;
  }
  return 0;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _max(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m = Number(list[0]);
  for (const n of list) {
    const num = Number(n);
    if (num > m) m = num;
  }
  return m;
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

main();
