// Generated by Mochi TypeScript compiler

let avg_by_state: Record<string, any>[];
let customer: Record<string, any>[];
let customer_address: Record<string, any>[];
let customer_total_return: Record<string, any>[];
let date_dim: Record<string, number>[];
let result: Record<string, any>[];
let web_returns: Record<string, any>[];

function test_TPCDS_Q30_simplified(): void {
  if (
    !(_equal(result, [
      {
        "c_customer_id": "C1",
        "c_first_name": "John",
        "c_last_name": "Doe",
        "ctr_total_return": 150,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  web_returns = [
    {
      "wr_returning_customer_sk": 1,
      "wr_returned_date_sk": 1,
      "wr_return_amt": 100,
      "wr_returning_addr_sk": 1,
    },
    {
      "wr_returning_customer_sk": 2,
      "wr_returned_date_sk": 1,
      "wr_return_amt": 30,
      "wr_returning_addr_sk": 2,
    },
    {
      "wr_returning_customer_sk": 1,
      "wr_returned_date_sk": 1,
      "wr_return_amt": 50,
      "wr_returning_addr_sk": 1,
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_year": 2000,
    },
  ];
  customer_address = [
    {
      "ca_address_sk": 1,
      "ca_state": "CA",
    },
    {
      "ca_address_sk": 2,
      "ca_state": "CA",
    },
  ];
  customer = [
    {
      "c_customer_sk": 1,
      "c_customer_id": "C1",
      "c_first_name": "John",
      "c_last_name": "Doe",
      "c_current_addr_sk": 1,
    },
    {
      "c_customer_sk": 2,
      "c_customer_id": "C2",
      "c_first_name": "Jane",
      "c_last_name": "Smith",
      "c_current_addr_sk": 2,
    },
  ];
  customer_total_return = (() => {
    const _src = web_returns;
    let _items = _query(_src, [
      {
        items: date_dim,
        on: (wr, d) => (_equal(wr.wr_returned_date_sk, d.d_date_sk)),
      },
      {
        items: customer_address,
        on: (wr, d, ca) => (_equal(wr.wr_returning_addr_sk, ca.ca_address_sk)),
      },
    ], {
      select: (wr, d, ca) => [wr, d, ca],
      where: (wr, d, ca) => ((d.d_year == 2000) && _equal(ca.ca_state, "CA")),
    });
    const _map = new Map<string, any>();
    const _order: string[] = [];
    for (const _r of _items) {
      const [wr, d, ca] = _r;
      const _key = {
        "cust": wr.wr_returning_customer_sk,
        "state": ca.ca_state,
      };
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({ ...wr, ...d, ...ca, wr: wr, d: d, ca: ca });
    }
    let _itemsG = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _itemsG) {
      _res.push({
        "ctr_customer_sk": g.key.cust,
        "ctr_state": g.key.state,
        "ctr_total_return": _sum(g.items.map((x) => x.wr_return_amt)),
      });
    }
    return _res;
  })();
  avg_by_state = (() => {
    const _src = customer_total_return;
    const _map = new Map<string, any>();
    const _order: string[] = [];
    var _items = [];
    for (const ctr of _src) {
      const _key = ctr.ctr_state;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({ ...ctr, ctr: ctr });
    }
    let _groups = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "state": g.key,
        "avg_return": _avg(g.items.map((x) => x.ctr_total_return)),
      });
    }
    return _res;
  })();
  result = (() => {
    const _src = customer_total_return;
    return _query(_src, [
      {
        items: avg_by_state,
        on: (ctr, avg) => (_equal(ctr.ctr_state, avg.state)),
      },
      {
        items: customer,
        on: (ctr, avg, c) => (_equal(ctr.ctr_customer_sk, c.c_customer_sk)),
      },
    ], {
      select: (ctr, avg, c) => ({
        "c_customer_id": c.c_customer_id,
        "c_first_name": c.c_first_name,
        "c_last_name": c.c_last_name,
        "ctr_total_return": ctr.ctr_total_return,
      }),
      where: (ctr, avg, c) => (ctr.ctr_total_return > (avg.avg_return * 1.2)),
    });
  })();
  console.log(_json(result));
  test_TPCDS_Q30_simplified();
}
function _avg(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum / list.length;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

function _sum(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum;
}

main();
