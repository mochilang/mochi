// Generated by Mochi TypeScript compiler

type CatalogSale = {
  cs_sold_date_sk: number;
  cs_item_sk: number;
  cs_bill_cdemo_sk: number;
  cs_promo_sk: number;
  cs_quantity: number;
  cs_list_price: number;
  cs_coupon_amt: number;
  cs_sales_price: number;
};

type CustomerDemo = {
  cd_demo_sk: number;
  cd_gender: string;
  cd_marital_status: string;
  cd_education_status: string;
};

type DateDim = {
  d_date_sk: number;
  d_year: number;
};

type Item = {
  i_item_sk: number;
  i_item_id: string;
};

type Promotion = {
  p_promo_sk: number;
  p_channel_email: string;
  p_channel_event: string;
};

let catalog_sales: Record<string, any>[];
let customer_demographics: Record<string, any>[];
let date_dim: Record<string, number>[];
let item: Record<string, any>[];
let promotion: Record<string, any>[];
let result: Record<string, any>[];

function test_TPCDS_Q26_demographic_averages(): void {
  if (
    !(_equal(result, [
      {
        "i_item_id": "ITEM1",
        "agg1": 10,
        "agg2": 100,
        "agg3": 5,
        "agg4": 95,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  catalog_sales = [
    {
      "cs_sold_date_sk": 1,
      "cs_item_sk": 1,
      "cs_bill_cdemo_sk": 1,
      "cs_promo_sk": 1,
      "cs_quantity": 10,
      "cs_list_price": 100,
      "cs_coupon_amt": 5,
      "cs_sales_price": 95,
    },
    {
      "cs_sold_date_sk": 1,
      "cs_item_sk": 2,
      "cs_bill_cdemo_sk": 2,
      "cs_promo_sk": 2,
      "cs_quantity": 5,
      "cs_list_price": 50,
      "cs_coupon_amt": 2,
      "cs_sales_price": 48,
    },
  ];
  customer_demographics = [
    {
      "cd_demo_sk": 1,
      "cd_gender": "M",
      "cd_marital_status": "S",
      "cd_education_status": "College",
    },
    {
      "cd_demo_sk": 2,
      "cd_gender": "F",
      "cd_marital_status": "M",
      "cd_education_status": "High School",
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_year": 2000,
    },
  ];
  item = [
    {
      "i_item_sk": 1,
      "i_item_id": "ITEM1",
    },
    {
      "i_item_sk": 2,
      "i_item_id": "ITEM2",
    },
  ];
  promotion = [
    {
      "p_promo_sk": 1,
      "p_channel_email": "N",
      "p_channel_event": "Y",
    },
    {
      "p_promo_sk": 2,
      "p_channel_email": "Y",
      "p_channel_event": "N",
    },
  ];
  result = (() => {
    const _src = catalog_sales;
    let _items = _query(_src, [
      {
        items: customer_demographics,
        on: (cs, cd) => (_equal(cs.cs_bill_cdemo_sk, cd.cd_demo_sk)),
      },
      {
        items: date_dim,
        on: (cs, cd, d) => (_equal(cs.cs_sold_date_sk, d.d_date_sk)),
      },
      {
        items: item,
        on: (cs, cd, d, i) => (_equal(cs.cs_item_sk, i.i_item_sk)),
      },
      {
        items: promotion,
        on: (cs, cd, d, i, p) => (_equal(cs.cs_promo_sk, p.p_promo_sk)),
      },
    ], {
      select: (cs, cd, d, i, p) => [cs, cd, d, i, p],
      where: (
        cs,
        cd,
        d,
        i,
        p,
      ) => ((((_equal(cd.cd_gender, "M") &&
        _equal(cd.cd_marital_status, "S")) &&
        _equal(cd.cd_education_status, "College")) &&
        (_equal(p.p_channel_email, "N") || _equal(p.p_channel_event, "N"))) &&
        (d.d_year == 2000)),
    });
    const _map = new Map<string, any>();
    const _order: string[] = [];
    for (const _r of _items) {
      const [cs, cd, d, i, p] = _r;
      const _key = i.i_item_id;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
        _order.push(_ks);
      }
      _g.items.push({
        ...cs,
        ...cd,
        ...d,
        ...i,
        ...p,
        cs: cs,
        cd: cd,
        d: d,
        i: i,
        p: p,
      });
    }
    let _itemsG = _order.map((k) => _map.get(k)!);
    const _res = [];
    for (const g of _itemsG) {
      _res.push({
        "i_item_id": g.key,
        "agg1": _avg(g.items.map((x) => x.cs_quantity)),
        "agg2": _avg(g.items.map((x) => x.cs_list_price)),
        "agg3": _avg(g.items.map((x) => x.cs_coupon_amt)),
        "agg4": _avg(g.items.map((x) => x.cs_sales_price)),
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q26_demographic_averages();
}
function _avg(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let sum = 0;
  for (const n of list) sum += Number(n);
  return sum / list.length;
}

function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _query(src: any[], joins: any[], opts: any): any {
  let items = src.map((v) => [v]);
  for (const j of joins) {
    const joined: any[] = [];
    if (j.right && j.left) {
      const matched: boolean[] = new Array(j.items.length).fill(false);
      for (const left of items) {
        let m = false;
        for (let ri = 0; ri < j.items.length; ri++) {
          const right = j.items[ri];
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          matched[ri] = true;
          joined.push([...left, right]);
        }
        if (!m) joined.push([...left, null]);
      }
      for (let ri = 0; ri < j.items.length; ri++) {
        if (!matched[ri]) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, j.items[ri]]);
        }
      }
    } else if (j.right) {
      for (const right of j.items) {
        let m = false;
        for (const left of items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (!m) {
          const undef = Array(items[0]?.length || 0).fill(null);
          joined.push([...undef, right]);
        }
      }
    } else {
      for (const left of items) {
        let m = false;
        for (const right of j.items) {
          let keep = true;
          if (left.some((v: any) => v === null) || right === null) {
            keep = false;
          } else if (j.on) keep = j.on(...left, right);
          if (!keep) continue;
          m = true;
          joined.push([...left, right]);
        }
        if (j.left && !m) joined.push([...left, null]);
      }
    }
    items = joined;
  }
  if (opts.where) items = items.filter((r) => opts.where(...r));
  if (opts.sortKey) {
    let pairs = items.map((it) => ({ item: it, key: opts.sortKey(...it) }));
    pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    items = pairs.map((p) => p.item);
  }
  if (opts.skip !== undefined) {
    const n = opts.skip;
    items = n < items.length ? items.slice(n) : [];
  }
  if (opts.take !== undefined) {
    const n = opts.take;
    if (n < items.length) items = items.slice(0, n);
  }
  const res = [];
  for (const r of items) res.push(opts.select(...r));
  return res;
}

main();
