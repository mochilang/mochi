#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.26 on 2025-07-15T06:31:59Z
% q79.erl - generated from q79.mochi

main(_) ->
    Date_dim = [#{d_date_sk => 1, d_dow => 1, d_year => 1999}],
    Store = [#{s_store_sk => 1, s_city => "CityA", s_number_employees => 250}],
    Household_demographics = [#{hd_demo_sk => 1, hd_dep_count => 2, hd_vehicle_count => 1}],
    Store_sales = [#{ss_sold_date_sk => 1, ss_store_sk => 1, ss_ticket_number => 1, ss_customer_sk => 1, ss_hdemo_sk => 1, ss_coupon_amt => 5, ss_net_profit => 10}],
    Customer = [#{c_customer_sk => 1, c_last_name => "Smith", c_first_name => "Alice"}],
    Agg = [#{key => Key0, amt => lists:sum([maps:get(ss_coupon_amt, maps:get(ss, X)) || X <- Val0]), profit => lists:sum([maps:get(ss_net_profit, maps:get(ss, X)) || X <- Val0])} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{#{ticket => maps:get(ss_ticket_number, Ss), customer_sk => maps:get(ss_customer_sk, Ss), city => maps:get(s_city, S)}, #{ss => Ss, d => D, s => S, hd => Hd}} || Ss <- Store_sales, D <- Date_dim, S <- Store, Hd <- Household_demographics, (maps:get(d_date_sk, D) == maps:get(ss_sold_date_sk, Ss)), (maps:get(s_store_sk, S) == maps:get(ss_store_sk, Ss)), (maps:get(hd_demo_sk, Hd) == maps:get(ss_hdemo_sk, Ss)), (((((((maps:get(hd_dep_count, Hd) == 2) orelse (maps:get(hd_vehicle_count, Hd) > 1))) andalso (maps:get(d_dow, D) == 1)) andalso ((((maps:get(d_year, D) == 1998) orelse (maps:get(d_year, D) == 1999)) orelse (maps:get(d_year, D) == 2000)))) andalso (maps:get(s_number_employees, S) >= 200)) andalso (maps:get(s_number_employees, S) =< 295))]))],
    Result = [V || {_, V} <- lists:keysort(1, [{[maps:get(c_last_name, C), maps:get(c_first_name, C), maps:get(city, maps:get(key, A)), maps:get(profit, A)], #{c_last_name => maps:get(c_last_name, C), c_first_name => maps:get(c_first_name, C), s_city => maps:get(city, maps:get(key, A)), ss_ticket_number => maps:get(ticket, maps:get(key, A)), amt => maps:get(amt, A), profit => maps:get(profit, A)}} || A <- Agg, C <- Customer, (maps:get(c_customer_sk, C) == maps:get(customer_sk, maps:get(key, A)))])],
    mochi_json(Result),
    (case (Result == [#{c_last_name => "Smith", c_first_name => "Alice", s_city => "CityA", ss_ticket_number => 1, amt => 5, profit => 10}]) of true -> ok; _ -> erlang:error(test_failed) end).

mochi_escape_json([]) -> [];
mochi_escape_json([H|T]) ->
    E = case H of
        $\ -> "\\";
        $" -> "\"";
        _ -> [H]
    end,
    E ++ mochi_escape_json(T).

mochi_to_json(true) -> "true";
mochi_to_json(false) -> "false";
mochi_to_json(V) when is_integer(V); is_float(V) -> lists:flatten(io_lib:format("~p", [V]));
mochi_to_json(V) when is_binary(V) -> "\"" ++ mochi_escape_json(binary_to_list(V)) ++ "\"";
mochi_to_json(V) when is_list(V), (V =:= [] orelse is_integer(hd(V))) -> "\"" ++ mochi_escape_json(V) ++ "\"";
mochi_to_json(V) when is_list(V) -> "[" ++ lists:join(",", [mochi_to_json(X) || X <- V]) ++ "]";
mochi_to_json(V) when is_map(V) -> "{" ++ lists:join(",", ["\"" ++ atom_to_list(K) ++ "\":" ++ mochi_to_json(Val) || {K,Val} <- maps:to_list(V)]) ++ "}".

mochi_json(V) -> io:format("~s
", [mochi_to_json(V)]).
