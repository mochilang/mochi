#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.26 on 2025-07-15T06:31:41Z
% q59.erl - generated from q59.mochi

main(_) ->
    Sales_year1 = [#{week => 1, store => 1, amount => 100}],
    Sales_year2 = [#{week => 53, store => 1, amount => 150}],
    Join = [#{s_store_id1 => maps:get(store, Y1), ratio => (maps:get(amount, Y2) / maps:get(amount, Y1))} || Y1 <- Sales_year1, Y2 <- Sales_year2, ((maps:get(store, Y1) == maps:get(store, Y2)) andalso (maps:get(week, Y1) == (maps:get(week, Y2) - 52)))],
    Result = Join,
    mochi_json(Result),
    (case (Result == [#{s_store_id1 => 1, ratio => 1.5}]) of true -> ok; _ -> erlang:error(test_failed) end).

mochi_escape_json([]) -> [];
mochi_escape_json([H|T]) ->
    E = case H of
        $\ -> "\\";
        $" -> "\"";
        _ -> [H]
    end,
    E ++ mochi_escape_json(T).

mochi_to_json(true) -> "true";
mochi_to_json(false) -> "false";
mochi_to_json(V) when is_integer(V); is_float(V) -> lists:flatten(io_lib:format("~p", [V]));
mochi_to_json(V) when is_binary(V) -> "\"" ++ mochi_escape_json(binary_to_list(V)) ++ "\"";
mochi_to_json(V) when is_list(V), (V =:= [] orelse is_integer(hd(V))) -> "\"" ++ mochi_escape_json(V) ++ "\"";
mochi_to_json(V) when is_list(V) -> "[" ++ lists:join(",", [mochi_to_json(X) || X <- V]) ++ "]";
mochi_to_json(V) when is_map(V) -> "{" ++ lists:join(",", ["\"" ++ atom_to_list(K) ++ "\":" ++ mochi_to_json(Val) || {K,Val} <- maps:to_list(V)]) ++ "}".

mochi_json(V) -> io:format("~s
", [mochi_to_json(V)]).
