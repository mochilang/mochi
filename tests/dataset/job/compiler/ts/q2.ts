// Generated by Mochi compiler v0.10.25 on 2025-07-13T11:58:05Z
// Source: /workspace/mochi/tests/dataset/job/q2.mochi

let company_name: Record<string, any>[];
let keyword: Record<string, any>[];
let movie_companies: { [key: string]: number }[];
let movie_keyword: { [key: string]: number }[];
let result: any;
let title: Record<string, any>[];
let titles: any[];

function test_Q2_finds_earliest_title_for_German_companies_with_character_keyword(): void {
  if (!(_equal(result, "Der Film"))) throw new Error("expect failed");
}

function main(): void {
  company_name = [
    {
      "id": 1,
      "country_code": "[de]",
    },
    {
      "id": 2,
      "country_code": "[us]",
    },
  ];
  keyword = [
    {
      "id": 1,
      "keyword": "character-name-in-title",
    },
    {
      "id": 2,
      "keyword": "other",
    },
  ];
  movie_companies = [
    {
      "movie_id": 100,
      "company_id": 1,
    },
    {
      "movie_id": 200,
      "company_id": 2,
    },
  ];
  movie_keyword = [
    {
      "movie_id": 100,
      "keyword_id": 1,
    },
    {
      "movie_id": 200,
      "keyword_id": 2,
    },
  ];
  title = [
    {
      "id": 100,
      "title": "Der Film",
    },
    {
      "id": 200,
      "title": "Other Movie",
    },
  ];
  titles = (() => {
    const _src = company_name;
    const _res = [];
    for (const cn of _src) {
      for (const mc of movie_companies) {
        if (!(_equal(mc.company_id, cn.id))) continue;
        for (const t of title) {
          if (!(_equal(mc.movie_id, t.id))) continue;
          for (const mk of movie_keyword) {
            if (!(_equal(mk.movie_id, t.id))) continue;
            for (const k of keyword) {
              if (!(_equal(mk.keyword_id, k.id))) continue;
              if (
                !((_equal(cn.country_code, "[de]") &&
                  _equal(k.keyword, "character-name-in-title")) &&
                  (mc.movie_id == mk.movie_id))
              ) continue;
              _res.push(t.title);
            }
          }
        }
      }
    }
    return _res;
  })();
  result = _min(titles);
  console.log(_json(result));
  test_Q2_finds_earliest_title_for_German_companies_with_character_keyword();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v), null, 2);
}

function _min(v: any): any {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m: any = list[0];
  if (typeof m === "string") {
    for (const s of list) if (typeof s === "string" && s < m) m = s;
    return m;
  }
  let mv = Number(m);
  for (const n of list) {
    const num = Number(n);
    if (num < mv) mv = num;
  }
  return mv;
}

main();
