// Generated by Mochi compiler v0.10.25 on 2025-07-13T07:34:51Z
import 'dart:convert';

var part = [{'p_partkey': 1, 'p_brand': 'Brand#23', 'p_container': 'MED BOX'}, {'p_partkey': 2, 'p_brand': 'Brand#77', 'p_container': 'LG JAR'}];

var lineitem = [{'l_partkey': 1, 'l_quantity': 1, 'l_extendedprice': 100}, {'l_partkey': 1, 'l_quantity': 10, 'l_extendedprice': 1000}, {'l_partkey': 1, 'l_quantity': 20, 'l_extendedprice': 2000}, {'l_partkey': 2, 'l_quantity': 5, 'l_extendedprice': 500}];

var brand = 'Brand#23';

var container = 'MED BOX';

var filtered = (() {
  var _q0 = <dynamic>[];
  for (var l in lineitem) {
    for (var p in part) {
      if (!(p['p_partkey'] == l['l_partkey'])) continue;
      if (!(((p['p_brand'] == brand) && (p['p_container'] == container) && (l['l_quantity'].toString().compareTo((0.2 * (() { var _t2 = (() {
  var _q1 = <dynamic>[];
  for (var x in lineitem) {
    if (!(x['l_partkey'] == p['p_partkey'])) continue;
    _q1.add(x['l_quantity']);
  }
  return _q1;
})(); return (_t2.isEmpty ? 0 : _t2.reduce((a, b) => a + b) / _t2.length); })()).toString()) < 0)))) continue;
      _q0.add(l['l_extendedprice']);
    }
  }
  return _q0;
})();

var result = filtered.reduce((a, b) => a + b) / 7;

void main() {
  print(jsonEncode(result));
}
