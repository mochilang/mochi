// Generated by Mochi compiler v0.10.25 on 2025-07-13T16:56:06Z
import 'dart:io';
import 'dart:convert';

bool _in(dynamic item, dynamic col) {
  if (col is Map) return col.containsKey(item);
  if (col is Iterable || col is String) return col.contains(item);
  return false;
}

var customer = [{'c_custkey': 1}, {'c_custkey': 2}, {'c_custkey': 3}];

var orders = [{'o_orderkey': 100, 'o_custkey': 1, 'o_comment': 'fast delivery'}, {'o_orderkey': 101, 'o_custkey': 1, 'o_comment': 'no comment'}, {'o_orderkey': 102, 'o_custkey': 2, 'o_comment': 'special requests only'}];

var per_customer = (() {
  var _q0 = <dynamic>[];
  for (var c in customer) {
    _q0.add({'c_count': (() {
  var _q1 = <dynamic>[];
  for (var o in orders) {
    if (!(((o['o_custkey'] == c['c_custkey'] && (!(_in('special', o['o_comment']))) && (!(_in('requests', o['o_comment']))))) ?? false)) continue;
    _q1.add(o);
  }
  return _q1;
})().length});
  }
  return _q0;
})();

var grouped = (() {
  var _q2 = <dynamic>[];
  var _g3 = <String, List<dynamic>>{};
  for (var x in per_customer) {
    var _k4 = x['c_count'];
    var _k4_s = jsonEncode(_k4);
    _g3.putIfAbsent(_k4_s, () => <dynamic>[]).add(x);
  }
  for (var entry in _g3.entries) {
    var g = entry.value;
    var _k4 = jsonDecode(entry.key);
    _q2.add([-(_k4 as num), {'c_count': _k4, 'custdist': g.length}]);
  }
  _q2.sort((a,b) => (a[0] as Comparable).compareTo(b[0]));
  _q2 = [for (var x in _q2) x[1]];
  return _q2;
})();

void main() {
  print(jsonEncode(grouped));
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

num _sum(dynamic v) {
    Iterable<dynamic>? list;
    if (v is Iterable) list = v;
    else if (v is Map && v['items'] is Iterable) list = (v['items'] as Iterable);
    else if (v is Map && v['Items'] is Iterable) list = (v['Items'] as Iterable);
    else { try { var it = (v as dynamic).items; if (it is Iterable) list = it; } catch (_) {} }
    if (list == null) return 0;
    num s = 0;
    for (var n in list) s += (n as num);
    return s;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
