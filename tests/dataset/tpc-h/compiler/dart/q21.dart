// Generated by Mochi compiler v0.10.25 on 2025-07-13T16:56:08Z
import 'dart:io';
import 'dart:convert';

var nation = [{'n_nationkey': 1, 'n_name': 'SAUDI ARABIA'}, {'n_nationkey': 2, 'n_name': 'FRANCE'}];

var supplier = [{'s_suppkey': 100, 's_name': 'Desert Trade', 's_nationkey': 1}, {'s_suppkey': 200, 's_name': 'Euro Goods', 's_nationkey': 2}];

var orders = [{'o_orderkey': 500, 'o_orderstatus': 'F'}, {'o_orderkey': 600, 'o_orderstatus': 'O'}];

var lineitem = [{'l_orderkey': 500, 'l_suppkey': 100, 'l_receiptdate': '1995-04-15', 'l_commitdate': '1995-04-10'}, {'l_orderkey': 500, 'l_suppkey': 200, 'l_receiptdate': '1995-04-12', 'l_commitdate': '1995-04-12'}, {'l_orderkey': 600, 'l_suppkey': 100, 'l_receiptdate': '1995-05-01', 'l_commitdate': '1995-04-25'}];

var result = (() {
  var _q0 = <dynamic>[];
  var _g1 = <String, List<dynamic>>{};
  for (var s in supplier) {
    for (var l1 in lineitem) {
      if (!(s['s_suppkey'] == l1['l_suppkey'])) continue;
      for (var o in orders) {
        if (!(o['o_orderkey'] == l1['l_orderkey'])) continue;
        for (var n in nation) {
          if (!(n['n_nationkey'] == s['s_nationkey'])) continue;
          if (!(o['o_orderstatus'] == 'F' && l1['l_receiptdate'].toString().compareTo(l1['l_commitdate'].toString()) > 0 && n['n_name'] == 'SAUDI ARABIA' && (!(() {
  var _q2 = <dynamic>[];
  for (var x in lineitem) {
    if (!(x['l_orderkey'] == l1['l_orderkey'] && x['l_suppkey'] != l1['l_suppkey'] && x['l_receiptdate'].toString().compareTo(x['l_commitdate'].toString()) > 0)) continue;
    _q2.add(x);
  }
  return _q2;
})().isNotEmpty))) continue;
          var _k3 = s['s_name'];
          var _k3_s = jsonEncode(_k3);
          _g1.putIfAbsent(_k3_s, () => <dynamic>[]).add({'s': s, 'l1': l1, 'o': o, 'n': n});
        }
      }
    }
  }
  for (var entry in _g1.entries) {
    var g = entry.value;
    var _k3 = jsonDecode(entry.key);
    _q0.add([[-g.length, _k3], {'s_name': _k3, 'numwait': g.length}]);
  }
  _q0.sort((a,b) => (jsonEncode(a[0]) as Comparable).compareTo(jsonEncode(b[0])));
  _q0 = [for (var x in _q0) x[1]];
  return _q0;
})();

void main() {
  print(jsonEncode(result));
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

num _sum(dynamic v) {
    Iterable<dynamic>? list;
    if (v is Iterable) list = v;
    else if (v is Map && v['items'] is Iterable) list = (v['items'] as Iterable);
    else if (v is Map && v['Items'] is Iterable) list = (v['Items'] as Iterable);
    else { try { var it = (v as dynamic).items; if (it is Iterable) list = it; } catch (_) {} }
    if (list == null) return 0;
    num s = 0;
    for (var n in list) s += (n as num);
    return s;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
