//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-13T18:09:25Z

package main

import (
	"encoding/json"
	"fmt"
	"mochi/runtime/data"
	"reflect"
	"sort"
	"time"
)

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q21_returns_Saudi_suppliers_who_caused_unique_delivery_delays() {
	expect(_equal(result, []v{v{
		S_name:  "Desert Trade",
		Numwait: 1,
	}}))
}

var nation []NationItem
var supplier []SupplierItem
var orders []OrdersItem
var lineitem []LineitemItem
var result []Result

func main() {
	nation = _convSlice[v, NationItem]([]NationItem{NationItem{
		1,
		"SAUDI ARABIA",
	}, NationItem{
		2,
		"FRANCE",
	}})
	supplier = _convSlice[v, SupplierItem]([]SupplierItem{SupplierItem{
		100,
		"Desert Trade",
		1,
	}, SupplierItem{
		200,
		"Euro Goods",
		2,
	}})
	orders = _convSlice[v, OrdersItem]([]OrdersItem{OrdersItem{
		500,
		"F",
	}, OrdersItem{
		600,
		"O",
	}})
	lineitem = _convSlice[v, LineitemItem]([]LineitemItem{LineitemItem{
		500,
		100,
		"1995-04-15",
		"1995-04-10",
	}, LineitemItem{
		500,
		200,
		"1995-04-12",
		"1995-04-12",
	}, LineitemItem{
		600,
		100,
		"1995-05-01",
		"1995-04-25",
	}})
	result = _convSlice[v, Result](func() []Result {
		groups := map[string]*data.Group{}
		order := []string{}
		for _, s := range supplier {
			for _, l1 := range lineitem {
				if !(s.S_suppkey == l1.L_suppkey) {
					continue
				}
				for _, o := range orders {
					if !(o.O_orderkey == l1.L_orderkey) {
						continue
					}
					for _, n := range nation {
						if !(n.N_nationkey == s.S_nationkey) {
							continue
						}
						if (((o.O_orderstatus == "F") && (l1.L_receiptdate > l1.L_commitdate)) && (n.N_name == "SAUDI ARABIA")) && (!len(func() []LineitemItem {
							results := []LineitemItem{}
							for _, x := range lineitem {
								if ((x.L_orderkey == l1.L_orderkey) && (x.L_suppkey != l1.L_suppkey)) && (x.L_receiptdate > x.L_commitdate) {
									if ((x.L_orderkey == l1.L_orderkey) && (x.L_suppkey != l1.L_suppkey)) && (x.L_receiptdate > x.L_commitdate) {
										results = append(results, x)
									}
								}
							}
							return results
						}()) > 0) {
							key := s.S_name
							ks := fmt.Sprint(key)
							g, ok := groups[ks]
							if !ok {
								g = &data.Group{Key: key}
								groups[ks] = g
								order = append(order, ks)
							}
							_item := map[string]any{}
							_item["s_suppkey"] = s.S_suppkey
							_item["s_name"] = s.S_name
							_item["s_nationkey"] = s.S_nationkey
							_item["s"] = s
							_item["l_orderkey"] = l1.L_orderkey
							_item["l_suppkey"] = l1.L_suppkey
							_item["l_receiptdate"] = l1.L_receiptdate
							_item["l_commitdate"] = l1.L_commitdate
							_item["l1"] = l1
							_item["o_orderkey"] = o.O_orderkey
							_item["o_orderstatus"] = o.O_orderstatus
							_item["o"] = o
							_item["n_nationkey"] = n.N_nationkey
							_item["n_name"] = n.N_name
							_item["n"] = n
							g.Items = append(g.Items, _item)
						}
					}
				}
			}
		}
		items := []*data.Group{}
		for _, ks := range order {
			items = append(items, groups[ks])
		}
		type pair struct {
			item *data.Group
			key  any
		}
		pairs := make([]pair, len(items))
		for idx, it := range items {
			g := it
			pairs[idx] = pair{item: it, key: _toAnySlice([]any{-len(g.Items), g.Key})}
		}
		sort.Slice(pairs, func(i, j int) bool {
			a, b := pairs[i].key, pairs[j].key
			switch av := a.(type) {
			case int:
				switch bv := b.(type) {
				case int:
					return av < bv
				case float64:
					return float64(av) < bv
				}
			case float64:
				switch bv := b.(type) {
				case int:
					return av < float64(bv)
				case float64:
					return av < bv
				}
			case string:
				bs, _ := b.(string)
				return av < bs
			}
			return fmt.Sprint(a) < fmt.Sprint(b)
		})
		for idx, p := range pairs {
			items[idx] = p.item
		}
		results := []Result{}
		for _, g := range items {
			results = append(results, Result{
				g.Key,
				len(g.Items),
			})
		}
		return results
	}())
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_Q21_returns_Saudi_suppliers_who_caused_unique_delivery_delays()
}

func _convSlice[T any, U any](s []T) []U {
	out := make([]U, len(s))
	for i, v := range s {
		out[i] = any(v).(U)
	}
	return out
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _toAnySlice[T any](s []T) []any {
	out := make([]any, len(s))
	for i, v := range s {
		out[i] = v
	}
	return out
}
