//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-13T18:09:23Z

package main

import (
	"encoding/json"
	"fmt"
	"mochi/runtime/data"
	"reflect"
	"sort"
	"strings"
	"time"
)

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers() {
	expect(_equal(result, []v{v{
		N_name:  "JAPAN",
		Revenue: 950,
	}, v{
		N_name:  "INDIA",
		Revenue: 720,
	}}))
}

var region []RegionItem
var nation []NationItem
var customer []CustomerItem
var supplier []SupplierItem
var orders []OrdersItem
var lineitem []LineitemItem
var asia_nations []NationItem
var local_customer_supplier_orders []Local_customer_supplier_orders
var result []Result

func main() {
	region = _convSlice[v, RegionItem]([]RegionItem{RegionItem{
		0,
		"ASIA",
	}, RegionItem{
		1,
		"EUROPE",
	}})
	nation = _convSlice[v, NationItem]([]NationItem{NationItem{
		10,
		0,
		"JAPAN",
	}, NationItem{
		20,
		0,
		"INDIA",
	}, NationItem{
		30,
		1,
		"FRANCE",
	}})
	customer = _convSlice[v, CustomerItem]([]CustomerItem{CustomerItem{
		1,
		10,
	}, CustomerItem{
		2,
		20,
	}})
	supplier = _convSlice[v, SupplierItem]([]SupplierItem{SupplierItem{
		100,
		10,
	}, SupplierItem{
		200,
		20,
	}})
	orders = _convSlice[v, OrdersItem]([]OrdersItem{OrdersItem{
		1000,
		1,
		"1994-03-15",
	}, OrdersItem{
		2000,
		2,
		"1994-06-10",
	}, OrdersItem{
		3000,
		2,
		"1995-01-01",
	}})
	lineitem = _convSlice[v, LineitemItem]([]LineitemItem{LineitemItem{
		1000,
		100,
		1000.0,
		0.05,
	}, LineitemItem{
		2000,
		200,
		800.0,
		0.1,
	}, LineitemItem{
		3000,
		200,
		900.0,
		0.05,
	}})
	asia_nations = func() []NationItem {
		results := []NationItem{}
		for _, r := range region {
			if r.R_name == "ASIA" {
				for _, n := range nation {
					if !(n.N_regionkey == r.R_regionkey) {
						continue
					}
					results = append(results, n)
				}
			}
		}
		return results
	}()
	local_customer_supplier_orders = _convSlice[v, Local_customer_supplier_orders](func() []Local_customer_supplier_orders {
		results := []Local_customer_supplier_orders{}
		for _, c := range customer {
			for _, n := range asia_nations {
				if !(c.C_nationkey == n.N_nationkey) {
					continue
				}
				for _, o := range orders {
					if !(o.O_custkey == c.C_custkey) {
						continue
					}
					for _, l := range lineitem {
						if !(l.L_orderkey == o.O_orderkey) {
							continue
						}
						for _, s := range supplier {
							if !(s.S_suppkey == l.L_suppkey) {
								continue
							}
							if ((o.O_orderdate >= "1994-01-01") && (o.O_orderdate < "1995-01-01")) && (s.S_nationkey == c.C_nationkey) {
								if ((o.O_orderdate >= "1994-01-01") && (o.O_orderdate < "1995-01-01")) && (s.S_nationkey == c.C_nationkey) {
									results = append(results, Local_customer_supplier_orders{
										n.N_name,
										(l.L_extendedprice * (float64(1) - l.L_discount)),
									})
								}
							}
						}
					}
				}
			}
		}
		return results
	}())
	result = _convSlice[v, Result](func() []Result {
		groups := map[string]*data.Group{}
		order := []string{}
		for _, r := range local_customer_supplier_orders {
			key := r.Nation
			ks := fmt.Sprint(key)
			g, ok := groups[ks]
			if !ok {
				g = &data.Group{Key: key}
				groups[ks] = g
				order = append(order, ks)
			}
			_item := map[string]any{}
			_item["nation"] = r.Nation
			_item["revenue"] = r.Revenue
			_item["r"] = r
			g.Items = append(g.Items, _item)
		}
		items := []*data.Group{}
		for _, ks := range order {
			items = append(items, groups[ks])
		}
		type pair struct {
			item *data.Group
			key  any
		}
		pairs := make([]pair, len(items))
		for idx, it := range items {
			g := it
			pairs[idx] = pair{item: it, key: -_sum(func() []any {
				results := []any{}
				for _, x := range g.Items {
					results = append(results, _toAnyMap(x)["revenue"])
				}
				return results
			}())}
		}
		sort.Slice(pairs, func(i, j int) bool {
			a, b := pairs[i].key, pairs[j].key
			switch av := a.(type) {
			case int:
				switch bv := b.(type) {
				case int:
					return av < bv
				case float64:
					return float64(av) < bv
				}
			case float64:
				switch bv := b.(type) {
				case int:
					return av < float64(bv)
				case float64:
					return av < bv
				}
			case string:
				bs, _ := b.(string)
				return av < bs
			}
			return fmt.Sprint(a) < fmt.Sprint(b)
		})
		for idx, p := range pairs {
			items[idx] = p.item
		}
		results := []Result{}
		for _, g := range items {
			results = append(results, Result{
				g.Key,
				_sum(func() []any {
					results := []any{}
					for _, x := range g.Items {
						results = append(results, _toAnyMap(x)["revenue"])
					}
					return results
				}()),
			})
		}
		return results
	}())
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers()
}

func _convSlice[T any, U any](s []T) []U {
	out := make([]U, len(s))
	for i, v := range s {
		out[i] = any(v).(U)
	}
	return out
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _sum(v any) float64 {
	var items []any
	if g, ok := v.(*data.Group); ok {
		items = g.Items
	} else {
		switch s := v.(type) {
		case []any:
			items = s
		case []int:
			items = []any{}
			for _, v := range s {
				items = append(items, v)
			}
		case []float64:
			items = []any{}
			for _, v := range s {
				items = append(items, v)
			}
		case []string, []bool:
			panic("sum() expects numbers")
		default:
			panic("sum() expects list or group")
		}
	}
	var sum float64
	for _, it := range items {
		switch n := it.(type) {
		case int:
			sum += float64(n)
		case int64:
			sum += float64(n)
		case float64:
			sum += n
		default:
			panic("sum() expects numbers")
		}
	}
	return sum
}

func _toAnyMap(m any) map[string]any {
	switch v := m.(type) {
	case map[string]any:
		return v
	case map[string]string:
		out := make(map[string]any, len(v))
		for k, vv := range v {
			out[k] = vv
		}
		return out
	default:
		rv := reflect.ValueOf(v)
		if rv.Kind() == reflect.Struct {
			out := make(map[string]any, rv.NumField())
			rt := rv.Type()
			for i := 0; i < rv.NumField(); i++ {
				name := rt.Field(i).Name
				if tag := rt.Field(i).Tag.Get("json"); tag != "" {
					comma := strings.Index(tag, ",")
					if comma >= 0 {
						tag = tag[:comma]
					}
					if tag != "-" {
						name = tag
					}
				}
				out[name] = rv.Field(i).Interface()
			}
			return out
		}
		return nil
	}
}
