//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-13T18:09:23Z

package main

import (
	"encoding/json"
	"fmt"
	"mochi/runtime/data"
	"reflect"
	"sort"
	"strings"
	"time"
)

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q3_returns_revenue_per_order_with_correct_priority() {
	expect(_equal(order_line_join, []v{v{
		L_orderkey:     100,
		Revenue:        ((1000.0 * 0.95) + 500.0),
		O_orderdate:    "1995-03-14",
		O_shippriority: 1,
	}}))
}

var customer []CustomerItem
var orders []OrdersItem
var lineitem []LineitemItem
var cutoff string
var segment string
var building_customers []CustomerItem
var valid_orders []OrdersItem
var valid_lineitems []LineitemItem
var order_line_join []Order_line_join

func main() {
	customer = _convSlice[v, CustomerItem]([]CustomerItem{CustomerItem{
		1,
		"BUILDING",
	}, CustomerItem{
		2,
		"AUTOMOBILE",
	}})
	orders = _convSlice[v, OrdersItem]([]OrdersItem{OrdersItem{
		100,
		1,
		"1995-03-14",
		1,
	}, OrdersItem{
		200,
		2,
		"1995-03-10",
		2,
	}})
	lineitem = _convSlice[v, LineitemItem]([]LineitemItem{LineitemItem{
		100,
		1000.0,
		0.05,
		"1995-03-16",
	}, LineitemItem{
		100,
		500.0,
		0.0,
		"1995-03-20",
	}, LineitemItem{
		200,
		1000.0,
		0.1,
		"1995-03-14",
	}})
	cutoff = "1995-03-15"
	segment = "BUILDING"
	building_customers = func() []CustomerItem {
		results := []CustomerItem{}
		for _, c := range customer {
			if c.C_mktsegment == segment {
				if c.C_mktsegment == segment {
					results = append(results, c)
				}
			}
		}
		return results
	}()
	valid_orders = func() []OrdersItem {
		results := []OrdersItem{}
		for _, o := range orders {
			if o.O_orderdate < cutoff {
				for _, c := range building_customers {
					if !(o.O_custkey == c.C_custkey) {
						continue
					}
					results = append(results, o)
				}
			}
		}
		return results
	}()
	valid_lineitems = func() []LineitemItem {
		results := []LineitemItem{}
		for _, l := range lineitem {
			if l.L_shipdate > cutoff {
				if l.L_shipdate > cutoff {
					results = append(results, l)
				}
			}
		}
		return results
	}()
	order_line_join = _convSlice[v, Order_line_join](func() []Order_line_join {
		groups := map[string]*data.Group{}
		order := []string{}
		for _, o := range valid_orders {
			for _, l := range valid_lineitems {
				if !(l.L_orderkey == o.O_orderkey) {
					continue
				}
				key := v{
					O_orderkey:     o.O_orderkey,
					O_orderdate:    o.O_orderdate,
					O_shippriority: o.O_shippriority,
				}
				ks := fmt.Sprint(key)
				g, ok := groups[ks]
				if !ok {
					g = &data.Group{Key: key}
					groups[ks] = g
					order = append(order, ks)
				}
				_item := map[string]any{}
				_item["o_orderkey"] = o.O_orderkey
				_item["o_custkey"] = o.O_custkey
				_item["o_orderdate"] = o.O_orderdate
				_item["o_shippriority"] = o.O_shippriority
				_item["o"] = o
				_item["l_orderkey"] = l.L_orderkey
				_item["l_extendedprice"] = l.L_extendedprice
				_item["l_discount"] = l.L_discount
				_item["l_shipdate"] = l.L_shipdate
				_item["l"] = l
				g.Items = append(g.Items, _item)
			}
		}
		items := []*data.Group{}
		for _, ks := range order {
			items = append(items, groups[ks])
		}
		type pair struct {
			item *data.Group
			key  any
		}
		pairs := make([]pair, len(items))
		for idx, it := range items {
			g := it
			pairs[idx] = pair{item: it, key: _toAnySlice([]any{-_sum(func() []any {
				results := []any{}
				for _, r := range g.Items {
					results = append(results, ((_toAnyMap(_toAnyMap(r)["l"])["l_extendedprice"]).(float64) * (float64(1) - (_toAnyMap(_toAnyMap(r)["l"])["l_discount"]).(float64)).(float64)))
				}
				return results
			}()), _toAnyMap(g.Key)["o_orderdate"]})}
		}
		sort.Slice(pairs, func(i, j int) bool {
			a, b := pairs[i].key, pairs[j].key
			switch av := a.(type) {
			case int:
				switch bv := b.(type) {
				case int:
					return av < bv
				case float64:
					return float64(av) < bv
				}
			case float64:
				switch bv := b.(type) {
				case int:
					return av < float64(bv)
				case float64:
					return av < bv
				}
			case string:
				bs, _ := b.(string)
				return av < bs
			}
			return fmt.Sprint(a) < fmt.Sprint(b)
		})
		for idx, p := range pairs {
			items[idx] = p.item
		}
		results := []Order_line_join{}
		for _, g := range items {
			results = append(results, Order_line_join{
				_toAnyMap(g.Key)["o_orderkey"],
				_sum(func() []any {
					results := []any{}
					for _, r := range g.Items {
						results = append(results, ((_toAnyMap(_toAnyMap(r)["l"])["l_extendedprice"]).(float64) * (float64(1) - (_toAnyMap(_toAnyMap(r)["l"])["l_discount"]).(float64)).(float64)))
					}
					return results
				}()),
				_toAnyMap(g.Key)["o_orderdate"],
				_toAnyMap(g.Key)["o_shippriority"],
			})
		}
		return results
	}())
	func() { b, _ := json.Marshal(order_line_join); fmt.Println(string(b)) }()
	test_Q3_returns_revenue_per_order_with_correct_priority()
}

func _convSlice[T any, U any](s []T) []U {
	out := make([]U, len(s))
	for i, v := range s {
		out[i] = any(v).(U)
	}
	return out
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _sum(v any) float64 {
	var items []any
	if g, ok := v.(*data.Group); ok {
		items = g.Items
	} else {
		switch s := v.(type) {
		case []any:
			items = s
		case []int:
			items = []any{}
			for _, v := range s {
				items = append(items, v)
			}
		case []float64:
			items = []any{}
			for _, v := range s {
				items = append(items, v)
			}
		case []string, []bool:
			panic("sum() expects numbers")
		default:
			panic("sum() expects list or group")
		}
	}
	var sum float64
	for _, it := range items {
		switch n := it.(type) {
		case int:
			sum += float64(n)
		case int64:
			sum += float64(n)
		case float64:
			sum += n
		default:
			panic("sum() expects numbers")
		}
	}
	return sum
}

func _toAnyMap(m any) map[string]any {
	switch v := m.(type) {
	case map[string]any:
		return v
	case map[string]string:
		out := make(map[string]any, len(v))
		for k, vv := range v {
			out[k] = vv
		}
		return out
	default:
		rv := reflect.ValueOf(v)
		if rv.Kind() == reflect.Struct {
			out := make(map[string]any, rv.NumField())
			rt := rv.Type()
			for i := 0; i < rv.NumField(); i++ {
				name := rt.Field(i).Name
				if tag := rt.Field(i).Tag.Get("json"); tag != "" {
					comma := strings.Index(tag, ",")
					if comma >= 0 {
						tag = tag[:comma]
					}
					if tag != "-" {
						name = tag
					}
				}
				out[name] = rv.Field(i).Interface()
			}
			return out
		}
		return nil
	}
}

func _toAnySlice[T any](s []T) []any {
	out := make([]any, len(s))
	for i, v := range s {
		out[i] = v
	}
	return out
}
