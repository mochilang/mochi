// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *prefix = "green";
static char *start_date = "1995-01-01";
static char *end_date = "1996-12-31";

typedef struct {
  char *nation;
  char *o_year;
  double profit;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  char *n_name;
} NationItem;
typedef struct {
  int len;
  NationItem *data;
} list_NationItem;
static list_NationItem list_NationItem_create(int len) {
  list_NationItem l;
  l.len = len;
  l.data = calloc(len, sizeof(NationItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  int s_nationkey;
} SupplierItem;
typedef struct {
  int len;
  SupplierItem *data;
} list_SupplierItem;
static list_SupplierItem list_SupplierItem_create(int len) {
  list_SupplierItem l;
  l.len = len;
  l.data = calloc(len, sizeof(SupplierItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_name;
} PartItem;
typedef struct {
  int len;
  PartItem *data;
} list_PartItem;
static list_PartItem list_PartItem_create(int len) {
  list_PartItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int ps_partkey;
  int ps_suppkey;
  double ps_supplycost;
} PartsuppItem;
typedef struct {
  int len;
  PartsuppItem *data;
} list_PartsuppItem;
static list_PartsuppItem list_PartsuppItem_create(int len) {
  list_PartsuppItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartsuppItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  char *o_orderdate;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_partkey;
  int l_suppkey;
  int l_quantity;
  double l_extendedprice;
  double l_discount;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int nation;
  char *o_year;
  double profit;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q9_computes_profit_for_green_parts_by_nation_and_year_result;
static void test_Q9_computes_profit_for_green_parts_by_nation_and_year() {
  double revenue = 1000.0 * 0.9;
  double cost = 5 * 10.0;
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] =
      (TmpItem){.nation = "BRAZIL", .o_year = "1995", .profit = revenue - cost};
  int tmp2 = 1;
  if (test_Q9_computes_profit_for_green_parts_by_nation_and_year_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q9_computes_profit_for_green_parts_by_nation_and_year_result.len;
         i3++) {
      if (test_Q9_computes_profit_for_green_parts_by_nation_and_year_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  NationItem tmp4_data[] = {(NationItem){.n_nationkey = 1, .n_name = "BRAZIL"},
                            (NationItem){.n_nationkey = 2, .n_name = "CANADA"}};
  list_NationItem tmp4 = {2, tmp4_data};
  list_NationItem nation = tmp4;
  SupplierItem tmp5_data[] = {
      (SupplierItem){.s_suppkey = 100, .s_nationkey = 1},
      (SupplierItem){.s_suppkey = 200, .s_nationkey = 2}};
  list_SupplierItem tmp5 = {2, tmp5_data};
  list_SupplierItem supplier = tmp5;
  PartItem tmp6_data[] = {
      (PartItem){.p_partkey = 1000, .p_name = "green metal box"},
      (PartItem){.p_partkey = 2000, .p_name = "red plastic crate"}};
  list_PartItem tmp6 = {2, tmp6_data};
  list_PartItem part = tmp6;
  PartsuppItem tmp7_data[] = {
      (PartsuppItem){
          .ps_partkey = 1000, .ps_suppkey = 100, .ps_supplycost = 10.0},
      (PartsuppItem){
          .ps_partkey = 1000, .ps_suppkey = 200, .ps_supplycost = 8.0}};
  list_PartsuppItem tmp7 = {2, tmp7_data};
  list_PartsuppItem partsupp = tmp7;
  OrdersItem tmp8_data[] = {
      (OrdersItem){.o_orderkey = 1, .o_orderdate = "1995-02-10"},
      (OrdersItem){.o_orderkey = 2, .o_orderdate = "1997-01-01"}};
  list_OrdersItem tmp8 = {2, tmp8_data};
  list_OrdersItem orders = tmp8;
  LineitemItem tmp9_data[] = {(LineitemItem){.l_orderkey = 1,
                                             .l_partkey = 1000,
                                             .l_suppkey = 100,
                                             .l_quantity = 5,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1},
                              (LineitemItem){.l_orderkey = 2,
                                             .l_partkey = 1000,
                                             .l_suppkey = 200,
                                             .l_quantity = 10,
                                             .l_extendedprice = 800.0,
                                             .l_discount = 0.05}};
  list_LineitemItem tmp9 = {2, tmp9_data};
  list_LineitemItem lineitem = tmp9;
  list_ResultItem result = (list_ResultItem){0, NULL};
  printf("[");
  for (int i10 = 0; i10 < result.len; i10++) {
    if (i10 > 0)
      printf(",");
    ResultItem it = result.data[i10];
    printf("{");
    _json_string("nation");
    printf(":");
    _json_int(it.nation);
    printf(",");
    _json_string("o_year");
    printf(":");
    _json_string(it.o_year);
    printf(",");
    _json_string("profit");
    printf(":");
    _json_float(it.profit);
    printf("}");
  }
  printf("]");
  test_Q9_computes_profit_for_green_parts_by_nation_and_year_cost = cost;
  test_Q9_computes_profit_for_green_parts_by_nation_and_year_result = result;
  test_Q9_computes_profit_for_green_parts_by_nation_and_year_revenue = revenue;
  test_Q9_computes_profit_for_green_parts_by_nation_and_year();
  return 0;
}
