// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _min_float(list_float v) {
  if (v.len == 0)
    return 0;
  double m = v.data[0];
  for (int i = 1; i < v.len; i++)
    if (v.data[i] < m)
      m = v.data[i];
  return m;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  double s_acctbal;
  char *s_name;
  char *n_name;
  int p_partkey;
  char *p_mfgr;
  char *s_address;
  char *s_phone;
  char *s_comment;
  double ps_supplycost;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int r_regionkey;
  char *r_name;
} RegionItem;
typedef struct {
  int len;
  RegionItem *data;
} list_RegionItem;
static list_RegionItem list_RegionItem_create(int len) {
  list_RegionItem l;
  l.len = len;
  l.data = calloc(len, sizeof(RegionItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  int n_regionkey;
  char *n_name;
} NationItem;
typedef struct {
  int len;
  NationItem *data;
} list_NationItem;
static list_NationItem list_NationItem_create(int len) {
  list_NationItem l;
  l.len = len;
  l.data = calloc(len, sizeof(NationItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  int s_nationkey;
  char *s_phone;
  double s_acctbal;
  char *s_comment;
} SupplierItem;
typedef struct {
  int len;
  SupplierItem *data;
} list_SupplierItem;
static list_SupplierItem list_SupplierItem_create(int len) {
  list_SupplierItem l;
  l.len = len;
  l.data = calloc(len, sizeof(SupplierItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_type;
  int p_size;
  char *p_mfgr;
} PartItem;
typedef struct {
  int len;
  PartItem *data;
} list_PartItem;
static list_PartItem list_PartItem_create(int len) {
  list_PartItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int ps_partkey;
  int ps_suppkey;
  double ps_supplycost;
} PartsuppItem;
typedef struct {
  int len;
  PartsuppItem *data;
} list_PartsuppItem;
static list_PartsuppItem list_PartsuppItem_create(int len) {
  list_PartsuppItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartsuppItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  SupplierItem s;
  NationItem n;
} Europe_suppliersItem;
typedef struct {
  int len;
  Europe_suppliersItem *data;
} list_Europe_suppliersItem;
static list_Europe_suppliersItem list_Europe_suppliersItem_create(int len) {
  list_Europe_suppliersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(Europe_suppliersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  double s_acctbal;
  char *s_name;
  char *n_name;
  int p_partkey;
  char *p_mfgr;
  char *s_address;
  char *s_phone;
  char *s_comment;
  double ps_supplycost;
} Target_partsuppItem;
typedef struct {
  int len;
  Target_partsuppItem *data;
} list_Target_partsuppItem;
static list_Target_partsuppItem list_Target_partsuppItem_create(int len) {
  list_Target_partsuppItem l;
  l.len = len;
  l.data = calloc(len, sizeof(Target_partsuppItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result;
static void
test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part() {
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.s_acctbal = 1000.0,
                           .s_name = "BestSupplier",
                           .n_name = "FRANCE",
                           .p_partkey = 1000,
                           .p_mfgr = "M1",
                           .s_address = "123 Rue",
                           .s_phone = "123",
                           .s_comment = "Fast and reliable",
                           .ps_supplycost = 10.0};
  int tmp2 = 1;
  if (test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
          .len != tmp1.len) {
    tmp2 = 0;
  } else {
    for (
        int i3 = 0;
        i3 <
        test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
            .len;
        i3++) {
      if (test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  RegionItem tmp4_data[] = {(RegionItem){.r_regionkey = 1, .r_name = "EUROPE"},
                            (RegionItem){.r_regionkey = 2, .r_name = "ASIA"}};
  list_RegionItem tmp4 = {2, tmp4_data};
  list_RegionItem region = tmp4;
  NationItem tmp5_data[] = {
      (NationItem){.n_nationkey = 10, .n_regionkey = 1, .n_name = "FRANCE"},
      (NationItem){.n_nationkey = 20, .n_regionkey = 2, .n_name = "CHINA"}};
  list_NationItem tmp5 = {2, tmp5_data};
  list_NationItem nation = tmp5;
  SupplierItem tmp6_data[] = {(SupplierItem){.s_suppkey = 100,
                                             .s_name = "BestSupplier",
                                             .s_address = "123 Rue",
                                             .s_nationkey = 10,
                                             .s_phone = "123",
                                             .s_acctbal = 1000.0,
                                             .s_comment = "Fast and reliable"},
                              (SupplierItem){.s_suppkey = 200,
                                             .s_name = "AltSupplier",
                                             .s_address = "456 Way",
                                             .s_nationkey = 20,
                                             .s_phone = "456",
                                             .s_acctbal = 500.0,
                                             .s_comment = "Slow"}};
  list_SupplierItem tmp6 = {2, tmp6_data};
  list_SupplierItem supplier = tmp6;
  PartItem tmp7_data[] = {(PartItem){.p_partkey = 1000,
                                     .p_type = "LARGE BRASS",
                                     .p_size = 15,
                                     .p_mfgr = "M1"},
                          (PartItem){.p_partkey = 2000,
                                     .p_type = "SMALL COPPER",
                                     .p_size = 15,
                                     .p_mfgr = "M2"}};
  list_PartItem tmp7 = {2, tmp7_data};
  list_PartItem part = tmp7;
  PartsuppItem tmp8_data[] = {
      (PartsuppItem){
          .ps_partkey = 1000, .ps_suppkey = 100, .ps_supplycost = 10.0},
      (PartsuppItem){
          .ps_partkey = 1000, .ps_suppkey = 200, .ps_supplycost = 15.0}};
  list_PartsuppItem tmp8 = {2, tmp8_data};
  list_PartsuppItem partsupp = tmp8;
  list_NationItem tmp9 = list_NationItem_create(region.len * nation.len);
  int tmp10 = 0;
  for (int tmp11 = 0; tmp11 < region.len; tmp11++) {
    RegionItem r = region.data[tmp11];
    for (int tmp12 = 0; tmp12 < nation.len; tmp12++) {
      NationItem n = nation.data[tmp12];
      if (!(n.n_regionkey == r.r_regionkey)) {
        continue;
      }
      if (!((strcmp(r.r_name, "EUROPE") == 0))) {
        continue;
      }
      tmp9.data[tmp10] = n;
      tmp10++;
    }
  }
  tmp9.len = tmp10;
  list_NationItem europe_nations = tmp9;
  list_Europe_suppliersItem tmp13 =
      list_Europe_suppliersItem_create(supplier.len * europe_nations.len);
  int tmp14 = 0;
  for (int tmp15 = 0; tmp15 < supplier.len; tmp15++) {
    SupplierItem s = supplier.data[tmp15];
    for (int tmp16 = 0; tmp16 < europe_nations.len; tmp16++) {
      NationItem n = europe_nations.data[tmp16];
      if (!(s.s_nationkey == n.n_nationkey)) {
        continue;
      }
      tmp13.data[tmp14] = (Europe_suppliersItem){.s = s, .n = n};
      tmp14++;
    }
  }
  tmp13.len = tmp14;
  list_Europe_suppliersItem europe_suppliers = tmp13;
  list_PartItem tmp17 = list_PartItem_create(part.len);
  int tmp18 = 0;
  for (int tmp19 = 0; tmp19 < part.len; tmp19++) {
    PartItem p = part.data[tmp19];
    if (!(p.p_size == 15 && p.p_type == "LARGE BRASS")) {
      continue;
    }
    tmp17.data[tmp18] = p;
    tmp18++;
  }
  tmp17.len = tmp18;
  list_PartItem target_parts = tmp17;
  list_Target_partsuppItem tmp20 = list_Target_partsuppItem_create(
      partsupp.len * target_parts.len * europe_suppliers.len);
  int tmp21 = 0;
  for (int tmp22 = 0; tmp22 < partsupp.len; tmp22++) {
    PartsuppItem ps = partsupp.data[tmp22];
    for (int tmp23 = 0; tmp23 < target_parts.len; tmp23++) {
      PartItem p = target_parts.data[tmp23];
      if (!(ps.ps_partkey == p.p_partkey)) {
        continue;
      }
      for (int tmp24 = 0; tmp24 < europe_suppliers.len; tmp24++) {
        Europe_suppliersItem s = europe_suppliers.data[tmp24];
        if (!(ps.ps_suppkey == s.s.s_suppkey)) {
          continue;
        }
        tmp20.data[tmp21] =
            (Target_partsuppItem){.s_acctbal = s.s.s_acctbal,
                                  .s_name = s.s.s_name,
                                  .n_name = s.n.n_name,
                                  .p_partkey = p.p_partkey,
                                  .p_mfgr = p.p_mfgr,
                                  .s_address = s.s.s_address,
                                  .s_phone = s.s.s_phone,
                                  .s_comment = s.s.s_comment,
                                  .ps_supplycost = ps.ps_supplycost};
        tmp21++;
      }
    }
  }
  tmp20.len = tmp21;
  list_Target_partsuppItem target_partsupp = tmp20;
  list_float tmp25 = list_float_create(target_partsupp.len);
  int tmp26 = 0;
  for (int tmp27 = 0; tmp27 < target_partsupp.len; tmp27++) {
    Target_partsuppItem x = target_partsupp.data[tmp27];
    tmp25.data[tmp26] = x.ps_supplycost;
    tmp26++;
  }
  tmp25.len = tmp26;
  list_float costs = tmp25;
  double min_cost = _min_float(costs);
  list_Target_partsuppItem tmp28 =
      list_Target_partsuppItem_create(target_partsupp.len);
  double *tmp31 = (double *)malloc(sizeof(double) * target_partsupp.len);
  int tmp29 = 0;
  for (int tmp30 = 0; tmp30 < target_partsupp.len; tmp30++) {
    Target_partsuppItem x = target_partsupp.data[tmp30];
    if (!(x.ps_supplycost == min_cost)) {
      continue;
    }
    tmp28.data[tmp29] = x;
    tmp31[tmp29] = (-x.s_acctbal);
    tmp29++;
  }
  tmp28.len = tmp29;
  for (int i34 = 0; i34 < tmp29 - 1; i34++) {
    for (int i35 = i34 + 1; i35 < tmp29; i35++) {
      if (tmp31[i34] > tmp31[i35]) {
        double tmp32 = tmp31[i34];
        tmp31[i34] = tmp31[i35];
        tmp31[i35] = tmp32;
        Target_partsuppItem tmp33 = tmp28.data[i34];
        tmp28.data[i34] = tmp28.data[i35];
        tmp28.data[i35] = tmp33;
      }
    }
  }
  list_Target_partsuppItem result = tmp28;
  printf("[");
  for (int i36 = 0; i36 < result.len; i36++) {
    if (i36 > 0)
      printf(",");
    Target_partsuppItem it = result.data[i36];
    printf("{");
    _json_string("s_acctbal");
    printf(":");
    _json_float(it.s_acctbal);
    printf(",");
    _json_string("s_name");
    printf(":");
    _json_string(it.s_name);
    printf(",");
    _json_string("n_name");
    printf(":");
    _json_string(it.n_name);
    printf(",");
    _json_string("p_partkey");
    printf(":");
    _json_int(it.p_partkey);
    printf(",");
    _json_string("p_mfgr");
    printf(":");
    _json_string(it.p_mfgr);
    printf(",");
    _json_string("s_address");
    printf(":");
    _json_string(it.s_address);
    printf(",");
    _json_string("s_phone");
    printf(":");
    _json_string(it.s_phone);
    printf(",");
    _json_string("s_comment");
    printf(":");
    _json_string(it.s_comment);
    printf(",");
    _json_string("ps_supplycost");
    printf(":");
    _json_float(it.ps_supplycost);
    printf("}");
  }
  printf("]");
  test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result =
      result;
  test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part();
  return 0;
}
