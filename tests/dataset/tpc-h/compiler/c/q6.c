// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
  int l_quantity;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_Q6_calculates_revenue_from_qualified_lineitems_result;
static void test_Q6_calculates_revenue_from_qualified_lineitems() {
  if (!(test_Q6_calculates_revenue_from_qualified_lineitems_result ==
        ((1000.0 * 0.06) + (500.0 * 0.07)))) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  LineitemItem tmp1_data[] = {(LineitemItem){.l_extendedprice = 1000.0,
                                             .l_discount = 0.06,
                                             .l_shipdate = "1994-02-15",
                                             .l_quantity = 10},
                              (LineitemItem){.l_extendedprice = 500.0,
                                             .l_discount = 0.07,
                                             .l_shipdate = "1994-03-10",
                                             .l_quantity = 23},
                              (LineitemItem){.l_extendedprice = 400.0,
                                             .l_discount = 0.04,
                                             .l_shipdate = "1994-04-10",
                                             .l_quantity = 15},
                              (LineitemItem){.l_extendedprice = 200.0,
                                             .l_discount = 0.06,
                                             .l_shipdate = "1995-01-01",
                                             .l_quantity = 5}};
  list_LineitemItem tmp1 = {4, tmp1_data};
  list_LineitemItem lineitem = tmp1;
  list_float tmp2 = list_float_create(lineitem.len);
  int tmp3 = 0;
  for (int tmp4 = 0; tmp4 < lineitem.len; tmp4++) {
    LineitemItem l = lineitem.data[tmp4];
    if (!(((strcmp(l.l_shipdate, "1994-01-01") >= 0)) &&
          ((strcmp(l.l_shipdate, "1995-01-01") < 0)) &&
          (l.l_discount >= 0.05) && (l.l_discount <= 0.07) &&
          (l.l_quantity < 24))) {
      continue;
    }
    tmp2.data[tmp3] = l.l_extendedprice * l.l_discount;
    tmp3++;
  }
  tmp2.len = tmp3;
  double result = _sum_float(tmp2);
  _json_float(result);
  test_Q6_calculates_revenue_from_qualified_lineitems_result = result;
  test_Q6_calculates_revenue_from_qualified_lineitems();
  return 0;
}
