// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1995-01-01";
static char *end_date = "1996-12-31";
static char *nation1 = "FRANCE";
static char *nation2 = "GERMANY";

typedef struct {
  char *supp_nation;
  char *cust_nation;
  char *l_year;
  double revenue;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  char *n_name;
} NationItem;
typedef struct {
  int len;
  NationItem *data;
} list_NationItem;
static list_NationItem list_NationItem_create(int len) {
  list_NationItem l;
  l.len = len;
  l.data = calloc(len, sizeof(NationItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  int s_nationkey;
} SupplierItem;
typedef struct {
  int len;
  SupplierItem *data;
} list_SupplierItem;
static list_SupplierItem list_SupplierItem_create(int len) {
  list_SupplierItem l;
  l.len = len;
  l.data = calloc(len, sizeof(SupplierItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  int c_nationkey;
} CustomerItem;
typedef struct {
  int len;
  CustomerItem *data;
} list_CustomerItem;
static list_CustomerItem list_CustomerItem_create(int len) {
  list_CustomerItem l;
  l.len = len;
  l.data = calloc(len, sizeof(CustomerItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_suppkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int supp_nation;
  int cust_nation;
  int l_year;
  double revenue;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year_result;
static void test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year() {
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.supp_nation = "FRANCE",
                           .cust_nation = "GERMANY",
                           .l_year = "1995",
                           .revenue = 900.0};
  int tmp2 = 1;
  if (test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year_result.len;
         i3++) {
      if (test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  NationItem tmp4_data[] = {
      (NationItem){.n_nationkey = 1, .n_name = "FRANCE"},
      (NationItem){.n_nationkey = 2, .n_name = "GERMANY"}};
  list_NationItem tmp4 = {2, tmp4_data};
  list_NationItem nation = tmp4;
  SupplierItem tmp5_data[] = {
      (SupplierItem){.s_suppkey = 100, .s_nationkey = 1}};
  list_SupplierItem tmp5 = {1, tmp5_data};
  list_SupplierItem supplier = tmp5;
  CustomerItem tmp6_data[] = {
      (CustomerItem){.c_custkey = 200, .c_nationkey = 2}};
  list_CustomerItem tmp6 = {1, tmp6_data};
  list_CustomerItem customer = tmp6;
  OrdersItem tmp7_data[] = {(OrdersItem){.o_orderkey = 1000, .o_custkey = 200}};
  list_OrdersItem tmp7 = {1, tmp7_data};
  list_OrdersItem orders = tmp7;
  LineitemItem tmp8_data[] = {(LineitemItem){.l_orderkey = 1000,
                                             .l_suppkey = 100,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1,
                                             .l_shipdate = "1995-06-15"},
                              (LineitemItem){.l_orderkey = 1000,
                                             .l_suppkey = 100,
                                             .l_extendedprice = 800.0,
                                             .l_discount = 0.05,
                                             .l_shipdate = "1997-01-01"}};
  list_LineitemItem tmp8 = {2, tmp8_data};
  list_LineitemItem lineitem = tmp8;
  list_ResultItem result = (list_ResultItem){0, NULL};
  printf("[");
  for (int i9 = 0; i9 < result.len; i9++) {
    if (i9 > 0)
      printf(",");
    ResultItem it = result.data[i9];
    printf("{");
    _json_string("supp_nation");
    printf(":");
    _json_int(it.supp_nation);
    printf(",");
    _json_string("cust_nation");
    printf(":");
    _json_int(it.cust_nation);
    printf(",");
    _json_string("l_year");
    printf(":");
    _json_int(it.l_year);
    printf(",");
    _json_string("revenue");
    printf(":");
    _json_float(it.revenue);
    printf("}");
  }
  printf("]");
  test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year_result = result;
  test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year();
  return 0;
}
