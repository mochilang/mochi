// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  char *key;
  list_int items;
} _GroupString;
typedef struct {
  int len;
  int cap;
  _GroupString *data;
} list_group_string;
static list_group_string _group_by_string(list_string src) {
  list_group_string res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    char *key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (strcmp(res.data[j].key, key) == 0) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data =
            (_GroupString *)realloc(res.data, sizeof(_GroupString) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupString *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = i;
  }
  return res;
}
static char *start_date = "1993-07-01";
static char *end_date = "1993-08-01";

typedef struct {
  char *o_orderpriority;
  int order_count;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *o_orderpriority;
  int order_count;
} TmpItem1;
typedef struct {
  int len;
  TmpItem1 *data;
} list_TmpItem1;
static list_TmpItem1 list_TmpItem1_create(int len) {
  list_TmpItem1 l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem1));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  char *o_orderdate;
  char *o_orderpriority;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  char *l_commitdate;
  char *l_receiptdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *o_orderpriority;
  int order_count;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q4_returns_count_of_orders_with_late_lineitems_in_range_result;
static void test_Q4_returns_count_of_orders_with_late_lineitems_in_range() {
  list_int tmp1 = list_int_create(2);
  tmp1.data[0] = (TmpItem){.o_orderpriority = "1-URGENT", .order_count = 1};
  tmp1.data[1] = (TmpItem1){.o_orderpriority = "2-HIGH", .order_count = 1};
  int tmp2 = 1;
  if (test_Q4_returns_count_of_orders_with_late_lineitems_in_range_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q4_returns_count_of_orders_with_late_lineitems_in_range_result
             .len;
         i3++) {
      if (test_Q4_returns_count_of_orders_with_late_lineitems_in_range_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  OrdersItem tmp4_data[] = {(OrdersItem){.o_orderkey = 1,
                                         .o_orderdate = "1993-07-01",
                                         .o_orderpriority = "1-URGENT"},
                            (OrdersItem){.o_orderkey = 2,
                                         .o_orderdate = "1993-07-15",
                                         .o_orderpriority = "2-HIGH"},
                            (OrdersItem){.o_orderkey = 3,
                                         .o_orderdate = "1993-08-01",
                                         .o_orderpriority = "3-NORMAL"}};
  list_OrdersItem tmp4 = {3, tmp4_data};
  list_OrdersItem orders = tmp4;
  LineitemItem tmp5_data[] = {(LineitemItem){.l_orderkey = 1,
                                             .l_commitdate = "1993-07-10",
                                             .l_receiptdate = "1993-07-12"},
                              (LineitemItem){.l_orderkey = 1,
                                             .l_commitdate = "1993-07-12",
                                             .l_receiptdate = "1993-07-10"},
                              (LineitemItem){.l_orderkey = 2,
                                             .l_commitdate = "1993-07-20",
                                             .l_receiptdate = "1993-07-25"},
                              (LineitemItem){.l_orderkey = 3,
                                             .l_commitdate = "1993-08-02",
                                             .l_receiptdate = "1993-08-01"},
                              (LineitemItem){.l_orderkey = 3,
                                             .l_commitdate = "1993-08-05",
                                             .l_receiptdate = "1993-08-10"}};
  list_LineitemItem tmp5 = {5, tmp5_data};
  list_LineitemItem lineitem = tmp5;
  list_OrdersItem tmp6 = list_OrdersItem_create(orders.len);
  int tmp7 = 0;
  for (int tmp8 = 0; tmp8 < orders.len; tmp8++) {
    OrdersItem o = orders.data[tmp8];
    if (!((strcmp(o.o_orderdate, start_date) >= 0) &&
          o.o_orderdate < end_date)) {
      continue;
    }
    tmp6.data[tmp7] = o;
    tmp7++;
  }
  tmp6.len = tmp7;
  list_OrdersItem date_filtered_orders = tmp6;
  list_LineitemItem tmp9 = list_LineitemItem_create(lineitem.len);
  int tmp10 = 0;
  for (int tmp11 = 0; tmp11 < lineitem.len; tmp11++) {
    LineitemItem l = lineitem.data[tmp11];
    if (!(l.l_orderkey == o.o_orderkey && l.l_commitdate < l.l_receiptdate)) {
      continue;
    }
    tmp9.data[tmp10] = l;
    tmp10++;
  }
  tmp9.len = tmp10;
  list_OrdersItem tmp12 = list_OrdersItem_create(date_filtered_orders.len);
  int tmp13 = 0;
  for (int tmp14 = 0; tmp14 < date_filtered_orders.len; tmp14++) {
    OrdersItem o = date_filtered_orders.data[tmp14];
    if (!(tmp9.len > 0)) {
      continue;
    }
    tmp12.data[tmp13] = o;
    tmp13++;
  }
  tmp12.len = tmp13;
  list_OrdersItem late_orders = tmp12;
  list_OrdersItem tmp15 = list_OrdersItem_create(late_orders.len);
  list_string tmp16 = list_string_create(late_orders.len);
  int tmp17 = 0;
  for (int i18 = 0; i18 < late_orders.len; i18++) {
    OrdersItem o = late_orders.data[i18];
    tmp15.data[tmp17] = o;
    tmp16.data[tmp17] = o.o_orderpriority;
    tmp17++;
  }
  tmp15.len = tmp17;
  tmp16.len = tmp17;
  list_group_string tmp19 = _group_by_string(tmp16);
  list_ResultItem tmp20 = list_ResultItem_create(tmp19.len);
  char **tmp22 = (char **)malloc(sizeof(char *) * tmp19.len);
  int tmp21 = 0;
  for (int gi = 0; gi < tmp19.len; gi++) {
    _GroupString _gp = tmp19.data[gi];
    list_OrdersItem tmp23 = list_OrdersItem_create(_gp.items.len);
    for (int i24 = 0; i24 < _gp.items.len; i24++) {
      tmp23.data[i24] = tmp15.data[_gp.items.data[i24]];
    }
    tmp23.len = _gp.items.len;
    struct {
      char *key;
      list_OrdersItem items;
    } g = {_gp.key, tmp23};
    tmp20.data[tmp21] =
        (ResultItem){.o_orderpriority = g.key, .order_count = g.items.len};
    tmp22[tmp21] = g.key;
    tmp21++;
  }
  tmp20.len = tmp21;
  for (int i27 = 0; i27 < tmp21 - 1; i27++) {
    for (int i28 = i27 + 1; i28 < tmp21; i28++) {
      if (strcmp(tmp22[i27], tmp22[i28]) > 0) {
        char *tmp25 = tmp22[i27];
        tmp22[i27] = tmp22[i28];
        tmp22[i28] = tmp25;
        ResultItem tmp26 = tmp20.data[i27];
        tmp20.data[i27] = tmp20.data[i28];
        tmp20.data[i28] = tmp26;
      }
    }
  }
  list_ResultItem result = tmp20;
  printf("[");
  for (int i29 = 0; i29 < result.len; i29++) {
    if (i29 > 0)
      printf(",");
    ResultItem it = result.data[i29];
    printf("{");
    _json_string("o_orderpriority");
    printf(":");
    _json_string(it.o_orderpriority);
    printf(",");
    _json_string("order_count");
    printf(":");
    _json_int(it.order_count);
    printf("}");
  }
  printf("]");
  test_Q4_returns_count_of_orders_with_late_lineitems_in_range_result = result;
  test_Q4_returns_count_of_orders_with_late_lineitems_in_range();
  return 0;
}
