// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  char *l_shipmode;
  int high_line_count;
  int low_line_count;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  char *o_orderpriority;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  char *l_shipmode;
  char *l_commitdate;
  char *l_receiptdate;
  char *l_shipdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *l_shipmode;
  double high_line_count;
  double low_line_count;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q12_counts_lineitems_by_ship_mode_and_priority_result;
static void test_Q12_counts_lineitems_by_ship_mode_and_priority() {
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){
      .l_shipmode = "MAIL", .high_line_count = 1, .low_line_count = 0};
  int tmp2 = 1;
  if (test_Q12_counts_lineitems_by_ship_mode_and_priority_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 < test_Q12_counts_lineitems_by_ship_mode_and_priority_result.len;
         i3++) {
      if (test_Q12_counts_lineitems_by_ship_mode_and_priority_result.data[i3] !=
          tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  OrdersItem tmp4_data[] = {
      (OrdersItem){.o_orderkey = 1, .o_orderpriority = "1-URGENT"},
      (OrdersItem){.o_orderkey = 2, .o_orderpriority = "3-MEDIUM"}};
  list_OrdersItem tmp4 = {2, tmp4_data};
  list_OrdersItem orders = tmp4;
  LineitemItem tmp5_data[] = {(LineitemItem){.l_orderkey = 1,
                                             .l_shipmode = "MAIL",
                                             .l_commitdate = "1994-02-10",
                                             .l_receiptdate = "1994-02-15",
                                             .l_shipdate = "1994-02-05"},
                              (LineitemItem){.l_orderkey = 2,
                                             .l_shipmode = "SHIP",
                                             .l_commitdate = "1994-03-01",
                                             .l_receiptdate = "1994-02-28",
                                             .l_shipdate = "1994-02-27"}};
  list_LineitemItem tmp5 = {2, tmp5_data};
  list_LineitemItem lineitem = tmp5;
  list_ResultItem result = (list_ResultItem){0, NULL};
  printf("[");
  for (int i6 = 0; i6 < result.len; i6++) {
    if (i6 > 0)
      printf(",");
    ResultItem it = result.data[i6];
    printf("{");
    _json_string("l_shipmode");
    printf(":");
    _json_string(it.l_shipmode);
    printf(",");
    _json_string("high_line_count");
    printf(":");
    _json_float(it.high_line_count);
    printf(",");
    _json_string("low_line_count");
    printf(":");
    _json_float(it.low_line_count);
    printf("}");
  }
  printf("]");
  test_Q12_counts_lineitems_by_ship_mode_and_priority_result = result;
  test_Q12_counts_lineitems_by_ship_mode_and_priority();
  return 0;
}
