// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static int contains_string(char *s, char *sub) {
  return strstr(s, sub) != NULL;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1995-09-01";
static char *end_date = "1995-10-01";

typedef struct {
  int p_partkey;
  char *p_type;
} PartItem;
typedef struct {
  int len;
  PartItem *data;
} list_PartItem;
static list_PartItem list_PartItem_create(int len) {
  list_PartItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_partkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int is_promo;
  double revenue;
} FilteredItem;
typedef struct {
  int len;
  FilteredItem *data;
} list_FilteredItem;
static list_FilteredItem list_FilteredItem_create(int len) {
  list_FilteredItem l;
  l.len = len;
  l.data = calloc(len, sizeof(FilteredItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_Q14_calculates_promo_revenue_percent_in_1995_09_result;
static void test_Q14_calculates_promo_revenue_percent_in_1995_09() {
  double promo = 1000.0 * 0.9;
  double total = 900 + 800.0;
  double expected = 100.0 * promo / total;
  if (!(test_Q14_calculates_promo_revenue_percent_in_1995_09_result ==
        expected)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  PartItem tmp1_data[] = {
      (PartItem){.p_partkey = 1, .p_type = "PROMO LUXURY"},
      (PartItem){.p_partkey = 2, .p_type = "STANDARD BRASS"}};
  list_PartItem tmp1 = {2, tmp1_data};
  list_PartItem part = tmp1;
  LineitemItem tmp2_data[] = {(LineitemItem){.l_partkey = 1,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1,
                                             .l_shipdate = "1995-09-05"},
                              (LineitemItem){.l_partkey = 2,
                                             .l_extendedprice = 800.0,
                                             .l_discount = 0.0,
                                             .l_shipdate = "1995-09-20"},
                              (LineitemItem){.l_partkey = 1,
                                             .l_extendedprice = 500.0,
                                             .l_discount = 0.2,
                                             .l_shipdate = "1995-10-02"}};
  list_LineitemItem tmp2 = {3, tmp2_data};
  list_LineitemItem lineitem = tmp2;
  list_FilteredItem tmp3 = list_FilteredItem_create(lineitem.len * part.len);
  int tmp4 = 0;
  for (int tmp5 = 0; tmp5 < lineitem.len; tmp5++) {
    LineitemItem l = lineitem.data[tmp5];
    for (int tmp6 = 0; tmp6 < part.len; tmp6++) {
      PartItem p = part.data[tmp6];
      if (!(p.p_partkey == l.l_partkey)) {
        continue;
      }
      if (!((strcmp(l.l_shipdate, start_date) >= 0) &&
            l.l_shipdate < end_date)) {
        continue;
      }
      tmp3.data[tmp4] =
          (FilteredItem){.is_promo = contains_string(p.p_type, "PROMO"),
                         .revenue = l.l_extendedprice * (1 - l.l_discount)};
      tmp4++;
    }
  }
  tmp3.len = tmp4;
  list_FilteredItem filtered = tmp3;
  list_float tmp7 = list_float_create(filtered.len);
  int tmp8 = 0;
  for (int tmp9 = 0; tmp9 < filtered.len; tmp9++) {
    FilteredItem x = filtered.data[tmp9];
    if (!(x.is_promo)) {
      continue;
    }
    tmp7.data[tmp8] = x.revenue;
    tmp8++;
  }
  tmp7.len = tmp8;
  double promo_sum = _sum_float(tmp7);
  list_float tmp10 = list_float_create(filtered.len);
  int tmp11 = 0;
  for (int tmp12 = 0; tmp12 < filtered.len; tmp12++) {
    FilteredItem x = filtered.data[tmp12];
    tmp10.data[tmp11] = x.revenue;
    tmp11++;
  }
  tmp10.len = tmp11;
  double total_sum = _sum_float(tmp10);
  double result = 100.0 * promo_sum / total_sum;
  _json_float(result);
  test_Q14_calculates_promo_revenue_percent_in_1995_09_expected = expected;
  test_Q14_calculates_promo_revenue_percent_in_1995_09_promo = promo;
  test_Q14_calculates_promo_revenue_percent_in_1995_09_result = result;
  test_Q14_calculates_promo_revenue_percent_in_1995_09_total = total;
  test_Q14_calculates_promo_revenue_percent_in_1995_09();
  return 0;
}
