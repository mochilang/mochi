// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static double _max_float(list_float v) {
  if (v.len == 0)
    return 0;
  double m = v.data[0];
  for (int i = 1; i < v.len; i++)
    if (v.data[i] > m)
      m = v.data[i];
  return m;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  int key;
  list_int items;
} _GroupInt;
typedef struct {
  int len;
  int cap;
  _GroupInt *data;
} list_group_int;
static list_group_int _group_by_int(list_int src) {
  list_group_int res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    int key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (res.data[j].key == key) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data = (_GroupInt *)realloc(res.data, sizeof(_GroupInt) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupInt *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = src.data[i];
  }
  return res;
}
static char *start_date = "1996-01-01";
static char *end_date = "1996-04-01";

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
  double total_revenue;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
} SupplierItem;
typedef struct {
  int len;
  SupplierItem *data;
} list_SupplierItem;
static list_SupplierItem list_SupplierItem_create(int len) {
  list_SupplierItem l;
  l.len = len;
  l.data = calloc(len, sizeof(SupplierItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_suppkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int supplier_no;
  double total_revenue;
} Revenue0Item;
typedef struct {
  int len;
  Revenue0Item *data;
} list_Revenue0Item;
static list_Revenue0Item list_Revenue0Item_create(int len) {
  list_Revenue0Item l;
  l.len = len;
  l.data = calloc(len, sizeof(Revenue0Item));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
  double total_revenue;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result;
static void test_Q15_returns_top_revenue_supplier_s__for_Q1_1996() {
  double rev = 1000.0 * 0.9 + 500.0;
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.s_suppkey = 100,
                           .s_name = "Best Supplier",
                           .s_address = "123 Market St",
                           .s_phone = "123-456",
                           .total_revenue = rev};
  int tmp2 = 1;
  if (test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 < test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result.len;
         i3++) {
      if (test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  SupplierItem tmp4_data[] = {(SupplierItem){.s_suppkey = 100,
                                             .s_name = "Best Supplier",
                                             .s_address = "123 Market St",
                                             .s_phone = "123-456"},
                              (SupplierItem){.s_suppkey = 200,
                                             .s_name = "Second Supplier",
                                             .s_address = "456 Elm St",
                                             .s_phone = "987-654"}};
  list_SupplierItem tmp4 = {2, tmp4_data};
  list_SupplierItem supplier = tmp4;
  LineitemItem tmp5_data[] = {(LineitemItem){.l_suppkey = 100,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1,
                                             .l_shipdate = "1996-01-15"},
                              (LineitemItem){.l_suppkey = 100,
                                             .l_extendedprice = 500.0,
                                             .l_discount = 0.0,
                                             .l_shipdate = "1996-03-20"},
                              (LineitemItem){.l_suppkey = 200,
                                             .l_extendedprice = 800.0,
                                             .l_discount = 0.05,
                                             .l_shipdate = "1995-12-30"}};
  list_LineitemItem tmp5 = {3, tmp5_data};
  list_LineitemItem lineitem = tmp5;
  list_LineitemItem tmp6 = list_LineitemItem_create(lineitem.len);
  int tmp7_data[lineitem.len];
  list_int tmp7 = {0, tmp7_data};
  int tmp8 = 0;
  for (int i9 = 0; i9 < lineitem.len; i9++) {
    LineitemItem l = lineitem.data[i9];
    if (!((strcmp(l.l_shipdate, start_date) >= 0) && l.l_shipdate < end_date)) {
      continue;
    }
    tmp6.data[tmp8] = l;
    tmp7.data[tmp8] = l.l_suppkey;
    tmp8++;
  }
  tmp6.len = tmp8;
  tmp7.len = tmp8;
  list_group_int tmp10 = _group_by_int(tmp7);
  list_Revenue0Item tmp11 = list_Revenue0Item_create(tmp10.len);
  int tmp12 = 0;
  for (int gi = 0; gi < tmp10.len; gi++) {
    _GroupInt _gp = tmp10.data[gi];
    list_LineitemItem tmp13 = list_LineitemItem_create(_gp.items.len);
    for (int i14 = 0; i14 < _gp.items.len; i14++) {
      tmp13.data[i14] = tmp6.data[_gp.items.data[i14]];
    }
    tmp13.len = _gp.items.len;
    struct {
      int key;
      list_LineitemItem items;
    } g = {_gp.key, tmp13};
    list_float tmp15 = list_float_create(g.items.len);
    int tmp16 = 0;
    for (int i17 = 0; i17 < g.items.len; i17++) {
      LineitemItem x = g.items.data[i17];
      tmp15.data[tmp16] = x.l_extendedprice * (1 - x.l_discount);
      tmp16++;
    }
    tmp15.len = tmp16;
    tmp11.data[tmp12] = (Revenue0Item){.supplier_no = g.key,
                                       .total_revenue = _sum_float(tmp15)};
    tmp12++;
  }
  tmp11.len = tmp12;
  list_Revenue0Item revenue0 = tmp11;
  list_float tmp18 = list_float_create(revenue0.len);
  int tmp19 = 0;
  for (int tmp20 = 0; tmp20 < revenue0.len; tmp20++) {
    Revenue0Item x = revenue0.data[tmp20];
    tmp18.data[tmp19] = x.total_revenue;
    tmp19++;
  }
  tmp18.len = tmp19;
  list_float revenues = tmp18;
  double max_revenue = _max_float(revenues);
  list_ResultItem tmp21 = list_ResultItem_create(supplier.len * revenue0.len);
  int tmp22 = 0;
  for (int tmp23 = 0; tmp23 < supplier.len; tmp23++) {
    SupplierItem s = supplier.data[tmp23];
    for (int tmp24 = 0; tmp24 < revenue0.len; tmp24++) {
      Revenue0Item r = revenue0.data[tmp24];
      if (!(s.s_suppkey == r.supplier_no)) {
        continue;
      }
      if (!(r.total_revenue == max_revenue)) {
        continue;
      }
      tmp21.data[tmp22] = (ResultItem){.s_suppkey = s.s_suppkey,
                                       .s_name = s.s_name,
                                       .s_address = s.s_address,
                                       .s_phone = s.s_phone,
                                       .total_revenue = r.total_revenue};
      tmp22++;
    }
  }
  tmp21.len = tmp22;
  list_ResultItem result = tmp21;
  printf("[");
  for (int i25 = 0; i25 < result.len; i25++) {
    if (i25 > 0)
      printf(",");
    ResultItem it = result.data[i25];
    printf("{");
    _json_string("s_suppkey");
    printf(":");
    _json_int(it.s_suppkey);
    printf(",");
    _json_string("s_name");
    printf(":");
    _json_string(it.s_name);
    printf(",");
    _json_string("s_address");
    printf(":");
    _json_string(it.s_address);
    printf(",");
    _json_string("s_phone");
    printf(":");
    _json_string(it.s_phone);
    printf(",");
    _json_string("total_revenue");
    printf(":");
    _json_float(it.total_revenue);
    printf("}");
  }
  printf("]");
  test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result = result;
  test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_rev = rev;
  test_Q15_returns_top_revenue_supplier_s__for_Q1_1996();
  return 0;
}
