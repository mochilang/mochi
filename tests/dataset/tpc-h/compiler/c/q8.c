// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1995-01-01";
static char *end_date = "1996-12-31";
static char *target_type = "ECONOMY ANODIZED STEEL";
static char *target_nation = "BRAZIL";

typedef struct {
  char *o_year;
  double mkt_share;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int r_regionkey;
  char *r_name;
} RegionItem;
typedef struct {
  int len;
  RegionItem *data;
} list_RegionItem;
static list_RegionItem list_RegionItem_create(int len) {
  list_RegionItem l;
  l.len = len;
  l.data = calloc(len, sizeof(RegionItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  int n_regionkey;
  char *n_name;
} NationItem;
typedef struct {
  int len;
  NationItem *data;
} list_NationItem;
static list_NationItem list_NationItem_create(int len) {
  list_NationItem l;
  l.len = len;
  l.data = calloc(len, sizeof(NationItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  int c_nationkey;
} CustomerItem;
typedef struct {
  int len;
  CustomerItem *data;
} list_CustomerItem;
static list_CustomerItem list_CustomerItem_create(int len) {
  list_CustomerItem l;
  l.len = len;
  l.data = calloc(len, sizeof(CustomerItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
  char *o_orderdate;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_suppkey;
  int l_partkey;
  double l_extendedprice;
  double l_discount;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
} SupplierItem;
typedef struct {
  int len;
  SupplierItem *data;
} list_SupplierItem;
static list_SupplierItem list_SupplierItem_create(int len) {
  list_SupplierItem l;
  l.len = len;
  l.data = calloc(len, sizeof(SupplierItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_type;
} PartItem;
typedef struct {
  int len;
  PartItem *data;
} list_PartItem;
static list_PartItem list_PartItem_create(int len) {
  list_PartItem l;
  l.len = len;
  l.data = calloc(len, sizeof(PartItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *o_year;
  double mkt_share;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result;
static void test_Q8_returns_correct_market_share_for_BRAZIL_in_1995() {
  double numerator = 1000.0 * 0.9;
  double denominator = numerator + (500.0 * 0.95);
  double share = numerator / denominator;
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.o_year = "1995", .mkt_share = share};
  int tmp2 = 1;
  if (test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result.len;
         i3++) {
      if (test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  RegionItem tmp4_data[] = {
      (RegionItem){.r_regionkey = 0, .r_name = "AMERICA"}};
  list_RegionItem tmp4 = {1, tmp4_data};
  list_RegionItem region = tmp4;
  NationItem tmp5_data[] = {
      (NationItem){.n_nationkey = 10, .n_regionkey = 0, .n_name = "BRAZIL"},
      (NationItem){.n_nationkey = 20, .n_regionkey = 0, .n_name = "CANADA"}};
  list_NationItem tmp5 = {2, tmp5_data};
  list_NationItem nation = tmp5;
  CustomerItem tmp6_data[] = {
      (CustomerItem){.c_custkey = 1, .c_nationkey = 10},
      (CustomerItem){.c_custkey = 2, .c_nationkey = 20}};
  list_CustomerItem tmp6 = {2, tmp6_data};
  list_CustomerItem customer = tmp6;
  OrdersItem tmp7_data[] = {
      (OrdersItem){
          .o_orderkey = 100, .o_custkey = 1, .o_orderdate = "1995-04-10"},
      (OrdersItem){
          .o_orderkey = 200, .o_custkey = 2, .o_orderdate = "1995-07-15"}};
  list_OrdersItem tmp7 = {2, tmp7_data};
  list_OrdersItem orders = tmp7;
  LineitemItem tmp8_data[] = {(LineitemItem){.l_orderkey = 100,
                                             .l_suppkey = 1000,
                                             .l_partkey = 5000,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1},
                              (LineitemItem){.l_orderkey = 200,
                                             .l_suppkey = 2000,
                                             .l_partkey = 5000,
                                             .l_extendedprice = 500.0,
                                             .l_discount = 0.05}};
  list_LineitemItem tmp8 = {2, tmp8_data};
  list_LineitemItem lineitem = tmp8;
  SupplierItem tmp9_data[] = {(SupplierItem){.s_suppkey = 1000},
                              (SupplierItem){.s_suppkey = 2000}};
  list_SupplierItem tmp9 = {2, tmp9_data};
  list_SupplierItem supplier = tmp9;
  PartItem tmp10_data[] = {
      (PartItem){.p_partkey = 5000, .p_type = "ECONOMY ANODIZED STEEL"},
      (PartItem){.p_partkey = 6000, .p_type = "SMALL BRASS"}};
  list_PartItem tmp10 = {2, tmp10_data};
  list_PartItem part = tmp10;
  list_ResultItem result = (list_ResultItem){0, NULL};
  printf("[");
  for (int i11 = 0; i11 < result.len; i11++) {
    if (i11 > 0)
      printf(",");
    ResultItem it = result.data[i11];
    printf("{");
    _json_string("o_year");
    printf(":");
    _json_string(it.o_year);
    printf(",");
    _json_string("mkt_share");
    printf(":");
    _json_float(it.mkt_share);
    printf("}");
  }
  printf("]");
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_denominator =
      denominator;
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_numerator = numerator;
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result = result;
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_share = share;
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995();
  return 0;
}
