// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1993-10-01";
static char *end_date = "1994-01-01";

typedef struct {
  int c_custkey;
  char *c_name;
  double revenue;
  double c_acctbal;
  char *n_name;
  char *c_address;
  char *c_phone;
  char *c_comment;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  char *n_name;
} NationItem;
typedef struct {
  int len;
  NationItem *data;
} list_NationItem;
static list_NationItem list_NationItem_create(int len) {
  list_NationItem l;
  l.len = len;
  l.data = calloc(len, sizeof(NationItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  char *c_name;
  double c_acctbal;
  int c_nationkey;
  char *c_address;
  char *c_phone;
  char *c_comment;
} CustomerItem;
typedef struct {
  int len;
  CustomerItem *data;
} list_CustomerItem;
static list_CustomerItem list_CustomerItem_create(int len) {
  list_CustomerItem l;
  l.len = len;
  l.data = calloc(len, sizeof(CustomerItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
  char *o_orderdate;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  char *l_returnflag;
  double l_extendedprice;
  double l_discount;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  int c_name;
  double revenue;
  int c_acctbal;
  int n_name;
  int c_address;
  int c_phone;
  int c_comment;
} ResultItem;
typedef struct {
  int len;
  ResultItem *data;
} list_ResultItem;
static list_ResultItem list_ResultItem_create(int len) {
  list_ResultItem l;
  l.len = len;
  l.data = calloc(len, sizeof(ResultItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q10_returns_customer_revenue_from_returned_items_result;
static void test_Q10_returns_customer_revenue_from_returned_items() {
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.c_custkey = 1,
                           .c_name = "Alice",
                           .revenue = 1000.0 * 0.9,
                           .c_acctbal = 100.0,
                           .n_name = "BRAZIL",
                           .c_address = "123 St",
                           .c_phone = "123-456",
                           .c_comment = "Loyal"};
  int tmp2 = 1;
  if (test_Q10_returns_customer_revenue_from_returned_items_result.len !=
      tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 < test_Q10_returns_customer_revenue_from_returned_items_result.len;
         i3++) {
      if (test_Q10_returns_customer_revenue_from_returned_items_result
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  NationItem tmp4_data[] = {(NationItem){.n_nationkey = 1, .n_name = "BRAZIL"}};
  list_NationItem tmp4 = {1, tmp4_data};
  list_NationItem nation = tmp4;
  CustomerItem tmp5_data[] = {(CustomerItem){.c_custkey = 1,
                                             .c_name = "Alice",
                                             .c_acctbal = 100.0,
                                             .c_nationkey = 1,
                                             .c_address = "123 St",
                                             .c_phone = "123-456",
                                             .c_comment = "Loyal"}};
  list_CustomerItem tmp5 = {1, tmp5_data};
  list_CustomerItem customer = tmp5;
  OrdersItem tmp6_data[] = {
      (OrdersItem){
          .o_orderkey = 1000, .o_custkey = 1, .o_orderdate = "1993-10-15"},
      (OrdersItem){
          .o_orderkey = 2000, .o_custkey = 1, .o_orderdate = "1994-01-02"}};
  list_OrdersItem tmp6 = {2, tmp6_data};
  list_OrdersItem orders = tmp6;
  LineitemItem tmp7_data[] = {(LineitemItem){.l_orderkey = 1000,
                                             .l_returnflag = "R",
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1},
                              (LineitemItem){.l_orderkey = 2000,
                                             .l_returnflag = "N",
                                             .l_extendedprice = 500.0,
                                             .l_discount = 0.0}};
  list_LineitemItem tmp7 = {2, tmp7_data};
  list_LineitemItem lineitem = tmp7;
  list_ResultItem result = (list_ResultItem){0, NULL};
  printf("[");
  for (int i8 = 0; i8 < result.len; i8++) {
    if (i8 > 0)
      printf(",");
    ResultItem it = result.data[i8];
    printf("{");
    _json_string("c_custkey");
    printf(":");
    _json_int(it.c_custkey);
    printf(",");
    _json_string("c_name");
    printf(":");
    _json_int(it.c_name);
    printf(",");
    _json_string("revenue");
    printf(":");
    _json_float(it.revenue);
    printf(",");
    _json_string("c_acctbal");
    printf(":");
    _json_int(it.c_acctbal);
    printf(",");
    _json_string("n_name");
    printf(":");
    _json_int(it.n_name);
    printf(",");
    _json_string("c_address");
    printf(":");
    _json_int(it.c_address);
    printf(",");
    _json_string("c_phone");
    printf(":");
    _json_int(it.c_phone);
    printf(",");
    _json_string("c_comment");
    printf(":");
    _json_int(it.c_comment);
    printf("}");
  }
  printf("]");
  test_Q10_returns_customer_revenue_from_returned_items_result = result;
  test_Q10_returns_customer_revenue_from_returned_items();
  return 0;
}
