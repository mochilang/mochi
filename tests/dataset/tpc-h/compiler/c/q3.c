// Generated by Mochi compiler v0.10.25 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *cutoff = "1995-03-15";
static char *segment = "BUILDING";

typedef struct {
  int l_orderkey;
  double revenue;
  char *o_orderdate;
  int o_shippriority;
} TmpItem;
typedef struct {
  int len;
  TmpItem *data;
} list_TmpItem;
static list_TmpItem list_TmpItem_create(int len) {
  list_TmpItem l;
  l.len = len;
  l.data = calloc(len, sizeof(TmpItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  char *c_mktsegment;
} CustomerItem;
typedef struct {
  int len;
  CustomerItem *data;
} list_CustomerItem;
static list_CustomerItem list_CustomerItem_create(int len) {
  list_CustomerItem l;
  l.len = len;
  l.data = calloc(len, sizeof(CustomerItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
  char *o_orderdate;
  int o_shippriority;
} OrdersItem;
typedef struct {
  int len;
  OrdersItem *data;
} list_OrdersItem;
static list_OrdersItem list_OrdersItem_create(int len) {
  list_OrdersItem l;
  l.len = len;
  l.data = calloc(len, sizeof(OrdersItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} LineitemItem;
typedef struct {
  int len;
  LineitemItem *data;
} list_LineitemItem;
static list_LineitemItem list_LineitemItem_create(int len) {
  list_LineitemItem l;
  l.len = len;
  l.data = calloc(len, sizeof(LineitemItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  double revenue;
  int o_orderdate;
  int o_shippriority;
} Order_line_joinItem;
typedef struct {
  int len;
  Order_line_joinItem *data;
} list_Order_line_joinItem;
static list_Order_line_joinItem list_Order_line_joinItem_create(int len) {
  list_Order_line_joinItem l;
  l.len = len;
  l.data = calloc(len, sizeof(Order_line_joinItem));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q3_returns_revenue_per_order_with_correct_priority_order_line_join;
static void test_Q3_returns_revenue_per_order_with_correct_priority() {
  list_int tmp1 = list_int_create(1);
  tmp1.data[0] = (TmpItem){.l_orderkey = 100,
                           .revenue = 1000.0 * 0.95 + 500.0,
                           .o_orderdate = "1995-03-14",
                           .o_shippriority = 1};
  int tmp2 = 1;
  if (test_Q3_returns_revenue_per_order_with_correct_priority_order_line_join
          .len != tmp1.len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q3_returns_revenue_per_order_with_correct_priority_order_line_join
             .len;
         i3++) {
      if (test_Q3_returns_revenue_per_order_with_correct_priority_order_line_join
              .data[i3] != tmp1.data[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  CustomerItem tmp4_data[] = {
      (CustomerItem){.c_custkey = 1, .c_mktsegment = "BUILDING"},
      (CustomerItem){.c_custkey = 2, .c_mktsegment = "AUTOMOBILE"}};
  list_CustomerItem tmp4 = {2, tmp4_data};
  list_CustomerItem customer = tmp4;
  OrdersItem tmp5_data[] = {(OrdersItem){.o_orderkey = 100,
                                         .o_custkey = 1,
                                         .o_orderdate = "1995-03-14",
                                         .o_shippriority = 1},
                            (OrdersItem){.o_orderkey = 200,
                                         .o_custkey = 2,
                                         .o_orderdate = "1995-03-10",
                                         .o_shippriority = 2}};
  list_OrdersItem tmp5 = {2, tmp5_data};
  list_OrdersItem orders = tmp5;
  LineitemItem tmp6_data[] = {(LineitemItem){.l_orderkey = 100,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.05,
                                             .l_shipdate = "1995-03-16"},
                              (LineitemItem){.l_orderkey = 100,
                                             .l_extendedprice = 500.0,
                                             .l_discount = 0.0,
                                             .l_shipdate = "1995-03-20"},
                              (LineitemItem){.l_orderkey = 200,
                                             .l_extendedprice = 1000.0,
                                             .l_discount = 0.1,
                                             .l_shipdate = "1995-03-14"}};
  list_LineitemItem tmp6 = {3, tmp6_data};
  list_LineitemItem lineitem = tmp6;
  list_CustomerItem tmp7 = list_CustomerItem_create(customer.len);
  int tmp8 = 0;
  for (int tmp9 = 0; tmp9 < customer.len; tmp9++) {
    CustomerItem c = customer.data[tmp9];
    if (!((strcmp(c.c_mktsegment, segment) == 0))) {
      continue;
    }
    tmp7.data[tmp8] = c;
    tmp8++;
  }
  tmp7.len = tmp8;
  list_CustomerItem building_customers = tmp7;
  list_OrdersItem tmp10 =
      list_OrdersItem_create(orders.len * building_customers.len);
  int tmp11 = 0;
  for (int tmp12 = 0; tmp12 < orders.len; tmp12++) {
    OrdersItem o = orders.data[tmp12];
    for (int tmp13 = 0; tmp13 < building_customers.len; tmp13++) {
      CustomerItem c = building_customers.data[tmp13];
      if (!(o.o_custkey == c.c_custkey)) {
        continue;
      }
      if (!((strcmp(o.o_orderdate, cutoff) < 0))) {
        continue;
      }
      tmp10.data[tmp11] = o;
      tmp11++;
    }
  }
  tmp10.len = tmp11;
  list_OrdersItem valid_orders = tmp10;
  list_LineitemItem tmp14 = list_LineitemItem_create(lineitem.len);
  int tmp15 = 0;
  for (int tmp16 = 0; tmp16 < lineitem.len; tmp16++) {
    LineitemItem l = lineitem.data[tmp16];
    if (!((strcmp(l.l_shipdate, cutoff) > 0))) {
      continue;
    }
    tmp14.data[tmp15] = l;
    tmp15++;
  }
  tmp14.len = tmp15;
  list_LineitemItem valid_lineitems = tmp14;
  list_Order_line_joinItem order_line_join =
      (list_Order_line_joinItem){0, NULL};
  printf("[");
  for (int i17 = 0; i17 < order_line_join.len; i17++) {
    if (i17 > 0)
      printf(",");
    Order_line_joinItem it = order_line_join.data[i17];
    printf("{");
    _json_string("l_orderkey");
    printf(":");
    _json_int(it.l_orderkey);
    printf(",");
    _json_string("revenue");
    printf(":");
    _json_float(it.revenue);
    printf(",");
    _json_string("o_orderdate");
    printf(":");
    _json_int(it.o_orderdate);
    printf(",");
    _json_string("o_shippriority");
    printf(":");
    _json_int(it.o_shippriority);
    printf("}");
  }
  printf("]");
  test_Q3_returns_revenue_per_order_with_correct_priority_order_line_join =
      order_line_join;
  test_Q3_returns_revenue_per_order_with_correct_priority();
  return 0;
}
