// Generated by Mochi compiler v0.10.25 on 2025-07-13T14:00:17Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
struct Auto1: Equatable {
    var o_orderdate: String
    var o_orderkey: Int
    var o_orderpriority: String
}

struct Auto2: Equatable {
    var l_commitdate: String
    var l_orderkey: Int
    var l_receiptdate: String
}

struct Auto3: Equatable {
    var o_orderpriority: String
    var order_count: Int
}

var orders = [Auto1(o_orderdate: "1993-07-01", o_orderkey: 1, o_orderpriority: "1-URGENT"), Auto1(o_orderdate: "1993-07-15", o_orderkey: 2, o_orderpriority: "2-HIGH"), Auto1(o_orderdate: "1993-08-01", o_orderkey: 3, o_orderpriority: "3-NORMAL")]
var lineitem = [Auto2(l_commitdate: "1993-07-10", l_orderkey: 1, l_receiptdate: "1993-07-12"), Auto2(l_commitdate: "1993-07-12", l_orderkey: 1, l_receiptdate: "1993-07-10"), Auto2(l_commitdate: "1993-07-20", l_orderkey: 2, l_receiptdate: "1993-07-25"), Auto2(l_commitdate: "1993-08-02", l_orderkey: 3, l_receiptdate: "1993-08-01"), Auto2(l_commitdate: "1993-08-05", l_orderkey: 3, l_receiptdate: "1993-08-10")]
let start_date = "1993-07-01"
let end_date = "1993-08-01"
var date_filtered_orders = orders.filter { o in o.o_orderdate >= start_date && o.o_orderdate < end_date }
var late_orders = date_filtered_orders.filter { o in lineitem.contains { l in l.l_orderkey == o.o_orderkey && l.l_commitdate < l.l_receiptdate } != nil }
var result = { () -> [Any] in
    var _groups: [String:[Auto1]] = [:]
    for o in late_orders {
        let _k = o.o_orderpriority
        _groups[_k, default: []].append(o)
    }
    var _tmp: [(key: String, items: [Auto1])] = []
    for (k, v) in _groups {
        _tmp.append((key: k, items: v))
    }
    _tmp.sort { String(describing: $0.key) < String(describing: $1.key) }
    return _tmp.map { g in ["o_orderpriority": g.key, "order_count": g.items.count] }
}()
_json(result)
expect(_equal(result, [Auto3(o_orderpriority: "1-URGENT", order_count: 1), Auto3(o_orderpriority: "2-HIGH", order_count: 1)]))
