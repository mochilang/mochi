// Generated by Mochi compiler v0.10.25 on 2025-07-13T14:00:17Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
class _Group {
    var key: Any
    var Items: [Any] = []
    init(_ k: Any) { self.key = k }
}

func _keyStr(_ v: Any) -> String {
    if let data = try? JSONSerialization.data(withJSONObject: v, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        return s
    }
    return String(describing: v)
}
func _sum<T: BinaryInteger>(_ arr: [T]) -> Double {
    var sum = 0.0
    for v in arr { sum += Double(v) }
    return sum
}
func _sum<T: BinaryFloatingPoint>(_ arr: [T]) -> Double {
    var sum = 0.0
    for v in arr { sum += Double(v) }
    return sum
}
struct Auto1: Equatable {
    var r_name: String
    var r_regionkey: Int
}

struct Auto2: Equatable {
    var n_name: String
    var n_nationkey: Int
    var n_regionkey: Int
}

struct Auto3: Equatable {
    var c_custkey: Int
    var c_nationkey: Int
}

struct Auto4: Equatable {
    var s_nationkey: Int
    var s_suppkey: Int
}

struct Auto5: Equatable {
    var o_custkey: Int
    var o_orderdate: String
    var o_orderkey: Int
}

struct Auto6: Equatable {
    var l_discount: Double
    var l_extendedprice: Double
    var l_orderkey: Int
    var l_suppkey: Int
}

struct Auto7: Equatable {
    var n_name: String
    var revenue: Int
}

var region = [Auto1(r_name: "ASIA", r_regionkey: 0), Auto1(r_name: "EUROPE", r_regionkey: 1)]
var nation = [Auto2(n_name: "JAPAN", n_nationkey: 10, n_regionkey: 0), Auto2(n_name: "INDIA", n_nationkey: 20, n_regionkey: 0), Auto2(n_name: "FRANCE", n_nationkey: 30, n_regionkey: 1)]
var customer = [Auto3(c_custkey: 1, c_nationkey: 10), Auto3(c_custkey: 2, c_nationkey: 20)]
var supplier = [Auto4(s_nationkey: 10, s_suppkey: 100), Auto4(s_nationkey: 20, s_suppkey: 200)]
var orders = [Auto5(o_custkey: 1, o_orderdate: "1994-03-15", o_orderkey: 1000), Auto5(o_custkey: 2, o_orderdate: "1994-06-10", o_orderkey: 2000), Auto5(o_custkey: 2, o_orderdate: "1995-01-01", o_orderkey: 3000)]
var lineitem = [Auto6(l_discount: 0.05, l_extendedprice: 1000.0, l_orderkey: 1000, l_suppkey: 100), Auto6(l_discount: 0.1, l_extendedprice: 800.0, l_orderkey: 2000, l_suppkey: 200), Auto6(l_discount: 0.05, l_extendedprice: 900.0, l_orderkey: 3000, l_suppkey: 200)]
var asia_nations = ({
	var _res: [Auto2] = []
	for r in region {
		for n in nation {
			if !(n.n_regionkey == r.r_regionkey) { continue }
			if !(r.r_name == "ASIA") { continue }
			_res.append(n)
		}
	}
	return _res
}())
var local_customer_supplier_orders = ({
	var _res: [[String:Any]] = []
	for c in customer {
		for n in asia_nations {
			if !(c.c_nationkey == n.n_nationkey) { continue }
			for o in orders {
				if !(o.o_custkey == c.c_custkey) { continue }
				for l in lineitem {
					if !(l.l_orderkey == o.o_orderkey) { continue }
					for s in supplier {
						if !(s.s_suppkey == l.l_suppkey) { continue }
						if !(o.o_orderdate >= "1994-01-01" && o.o_orderdate < "1995-01-01" && s.s_nationkey == c.c_nationkey) { continue }
						_res.append(["nation": n.n_name, "revenue": l.l_extendedprice * Double((Double(1) - l.l_discount))])
					}
				}
			}
		}
	}
	return _res
}())
var result = { () -> [Any] in
    var _groups: [AnyHashable:[[String:Any]]] = [:]
    for r in local_customer_supplier_orders {
        let _k = r["nation"]!
        _groups[_k as! AnyHashable, default: []].append(r)
    }
    var _tmp: [(key: AnyHashable, items: [[String:Any]])] = []
    for (k, v) in _groups {
        _tmp.append((key: k, items: v))
    }
    _tmp.sort { _sum($0.items.map { x in x["revenue"]! }) > _sum($1.items.map { x in x["revenue"]! }) }
    return _tmp.map { g in ["n_name": g.key, "revenue": _sum(g.items.map { x in x["revenue"]! })] }
}()
_json(result)
expect(_equal(result, [Auto7(n_name: "JAPAN", revenue: 950), Auto7(n_name: "INDIA", revenue: 720)]))
