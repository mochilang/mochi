let customers = [alist->hash-table([cons("id", 1), cons("name", "Alice")]), alist->hash-table([cons("id", 2), cons("name", "Bob")]), alist->hash-table([cons("id", 3), cons("name", "Charlie")])]
let orders = [alist->hash-table([cons("id", 100), cons("customerId", 1), cons("total", 250)]), alist->hash-table([cons("id", 101), cons("customerId", 2), cons("total", 125)]), alist->hash-table([cons("id", 102), cons("customerId", 1), cons("total", 300)])]
let result = let(begin(for-each(lambda(o(), for-each(lambda(c(), set!(res14, append(res14, [alist->hash-table([cons("orderId", hash-table-ref(o, "id")), cons("orderCustomerId", hash-table-ref(o, "customerId")), cons("pairedCustomerName", hash-table-ref(c, "name")), cons("orderTotal", hash-table-ref(o, "total"))])]))), customers)), orders), res14))
print(to-str("--- Cross Join: All order-customer pairs ---"))
print("")
