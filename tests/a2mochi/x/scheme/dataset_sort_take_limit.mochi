let products = [alist->hash-table([cons("name", "Laptop"), cons("price", 1500)]), alist->hash-table([cons("name", "Smartphone"), cons("price", 900)]), alist->hash-table([cons("name", "Tablet"), cons("price", 600)]), alist->hash-table([cons("name", "Monitor"), cons("price", 300)]), alist->hash-table([cons("name", "Keyboard"), cons("price", 100)]), alist->hash-table([cons("name", "Mouse"), cons("price", 50)]), alist->hash-table([cons("name", "Headphones"), cons("price", 200)])]
let expensive = let(begin(for-each(lambda(p(), set!(res4, append(res4, [p]))), products), res4))
print("--- Top products (excluding most expensive)\n   ---")
print("")
