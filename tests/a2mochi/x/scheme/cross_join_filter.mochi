let nums = [1, 2, 3]
let letters = ["A", "B"]
let pairs = let(begin(for-each(lambda(n(), for-each(lambda(l(), if((modulo(n, 2) == 0), set!(res2, append(res2, [alist->hash-table([cons("n", n), cons("l", l)])])), quote(nil))), letters)), nums), res2))
print("--- Even pairs ---")
print("")
