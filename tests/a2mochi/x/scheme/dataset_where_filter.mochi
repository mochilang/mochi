let people = [alist->hash-table([cons("name", "Alice"), cons("age", 30)]), alist->hash-table([cons("name", "Bob"), cons("age", 15)]), alist->hash-table([cons("name", "Charlie"), cons("age", 65)]), alist->hash-table([cons("name", "Diana"), cons("age", 45)])]
let adults = let(begin(for-each(lambda(person(), if((hash-table-ref(person, "age") >= 18), set!(res5, append(res5, [alist->hash-table([cons("name", hash-table-ref(person, "name")), cons("age", hash-table-ref(person, "age")), cons("is_senior", (hash-table-ref(person, "age") >= 60))])])), quote(nil))), people), res5))
print("--- Adults ---")
print("")
