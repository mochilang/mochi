let people = [alist_to_hash_table([cons("name", "Alice"), cons("age", 30)]), alist_to_hash_table([cons("name", "Bob"), cons("age", 15)]), alist_to_hash_table([cons("name", "Charlie"), cons("age", 65)]), alist_to_hash_table([cons("name", "Diana"), cons("age", 45)])]
let adults = begin(for_each(lambda(person(), if((hash_table_ref(person, "age") >= 18), set_bang(res5, append(res5, alist_to_hash_table([cons("name", hash_table_ref(person, "name")), cons("age", hash_table_ref(person, "age")), cons("is_senior", (hash_table_ref(person, "age") >= 60))]))), quote(nil))), people), res5)
print("--- Adults ---")
