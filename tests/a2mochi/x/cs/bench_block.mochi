# Generated by a2mochi cs v0.10.48 on 2025-07-29 18:05:06 GMT+7
let seededNow: bool = false
let nowSeed: int = 0
fun _now(): int {
  if !seededNow {
    var s = GetEnvironmentVariable(Environment, "MOCHI_NOW_SEED")
    if TryParse(long, s, unknown) {
      nowSeed = v
      seededNow = true
    }
  }
  if seededNow {
    nowSeed = (((nowSeed * 1664525) + 1013904223) % 2147483647)
    return nowSeed
  }
  return (ToUnixTimeMilliseconds(DateTimeOffset.UtcNow) % int.MaxValue)
}
fun _mem(): int {
  return GetTotalMemory(GC, false)
}
var __memStart = _mem()
var __start = _now()
var n = 1000
var s = 0
for i_0 in 1..n {
  s = (s + i_0)
}
var __end = _now()
var __memEnd = _mem()
var __dur = ((__end - __start) / 1000)
var __memDiff = (__memEnd - __memStart)
print(Serialize(JsonSerializer, {"name": "simple", "duration_us": __dur, "memory_bytes": __memDiff}, JsonSerializerOptions {WriteIndented: true}))
