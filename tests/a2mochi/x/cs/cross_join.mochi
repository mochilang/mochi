# Generated by a2mochi cs v0.10.48 on 2025-07-29 18:05:09 GMT+7
type Customer {
  id: int
  name: string
  fun ToString(): string {
  }
}
type Order {
  id: int
  customerId: int
  total: int
  fun ToString(): string {
  }
}
type OResult {
  orderId: int
  orderCustomerId: int
  pairedCustomerName: string
  orderTotal: int
  fun ToString(): string {
  }
}
let customers: list<any> = [Customer {id: 1, name: "Alice"}, Customer {id: 2, name: "Bob"}, Customer {id: 3, name: "Charlie"}]
let orders: list<any> = [Order {id: 100, customerId: 1, total: 250}, Order {id: 101, customerId: 2, total: 125}, Order {id: 102, customerId: 1, total: 300}]
let result: list<any> = ToArray(unknown)
print("--- Cross Join: All order-customer pairs ---")
for entry in result {
  print("Order", entry.orderId, "(customerId:", entry.orderCustomerId, ", total: $", entry.orderTotal, ") paired with", entry.pairedCustomerName)
}
