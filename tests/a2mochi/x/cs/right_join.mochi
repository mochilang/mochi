# Generated by a2mochi cs v0.10.48 on 2025-07-29 18:05:40 GMT+7
type Customer {
  id: int
  name: string
  fun ToString(): string {
  }
}
type Order {
  id: int
  customerId: int
  total: int
  fun ToString(): string {
  }
}
type OResult {
  customerName: string
  order: any
  fun ToString(): string {
  }
}
let customers: list<any> = [Customer {id: 1, name: "Alice"}, Customer {id: 2, name: "Bob"}, Customer {id: 3, name: "Charlie"}, Customer {id: 4, name: "Diana"}]
let orders: list<any> = [Order {id: 100, customerId: 1, total: 250}, Order {id: 101, customerId: 2, total: 125}, Order {id: 102, customerId: 1, total: 300}]
let result: list<any> = ToArray(unknown)
print("--- Right Join using syntax ---")
for entry in result {
  if entry.order {
    print(("[" + (["Customer", entry.customerName, "has order", entry.order.id, "- $", entry.order.total] + "]")))
  } else {
    print(("[" + (["Customer", entry.customerName, "has no orders"] + "]")))
  }
}
