(program
  (let prefix (string fore))
  (let s1 (string forest))
  (call print
    (binary ==
      (call substr
        (selector s1)
        (int 0)
        (call len (selector prefix))
      )
      (selector prefix)
    )
  )
  (let s2 (string desert))
  (call print
    (binary ==
      (call substr
        (selector s2)
        (int 0)
        (call len (selector prefix))
      )
      (selector prefix)
    )
  )
)
