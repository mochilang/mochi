type People {
  name: string
  age: int
}
var people = [People("Alice", 30), People("Bob", 25)]
for _row in people {
  var _tmp = if hasattr(_row, "__dataclass_fields__") then (_row) else _row
  print((_tmp))
}
