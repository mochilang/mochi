//go:build ignore

// Generated by Mochi v0.10.62 on 2025-08-09 16:28:40 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _toFloat(v any) float64 {
	switch t := v.(type) {
	case int:
		return float64(t)
	case int64:
		return float64(t)
	case float64:
		return t
	default:
		return 0
	}
}

type Point struct {
	X int `json:"x"`
	Y int `json:"y"`
}

func abs_int(n int) int {
	if n < 0 {
		return (0 - n)
	}
	return n
}

func round_int(x float64) int {
	return func() int { n, _ := strconv.Atoi(fmt.Sprint((x + 0.5))); return n }()
}

func digital_differential_analyzer_line(p1 Point, p2 Point) []Point {
	var dx int = (p2.X - p1.X)
	_ = dx
	var dy int = (p2.Y - p1.Y)
	_ = dy
	abs_dx := abs_int(dx)
	_ = abs_dx
	_ = abs_dx
	abs_dy := abs_int(dy)
	_ = abs_dy
	_ = abs_dy
	steps := func() any {
		if _toFloat(abs_dx) > _toFloat(abs_dy) {
			return abs_dx
		} else {
			return abs_dy
		}
	}()
	_ = steps
	x_increment := (float64(dx) / float64(steps))
	_ = x_increment
	y_increment := (float64(dy) / float64(steps))
	_ = y_increment
	var coordinates []Point = []Point{}
	_ = coordinates
	var x float64 = float64(p1.X)
	_ = x
	var y float64 = float64(p1.Y)
	_ = y
	var i int = 0
	_ = i
	for i < steps {
		x = (x + _toFloat(x_increment))
		y = (y + _toFloat(y_increment))
		point := Point{
			X: round_int(x),
			Y: round_int(y),
		}
		_ = point
		coordinates = append(coordinates, Point(point))
		i = (i + 1)
	}
	return coordinates
}

func mochiMain() {
	result := digital_differential_analyzer_line(Point{
		X: 1,
		Y: 1,
	}, Point{
		X: 4,
		Y: 4,
	})
	_ = result
	fmt.Println(result)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
