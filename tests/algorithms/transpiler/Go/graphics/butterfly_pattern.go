//go:build ignore

// Generated by Mochi v0.10.61 on 2025-08-09 10:28:07 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		if vv, ok := v.(int); ok {
			return vv
		}
		if vv, ok := v.(float64); ok {
			return int(vv)
		}
		return v.(int)
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	return s[idx]
}

func repeat_char(ch string, count int) string {
	var result string = ""
	_ = result
	var i int = 0
	_ = i
	for i < count {
		result = (result + ch)
		i = (i + 1)
	}
	return result
}

func butterfly_pattern(n int) string {
	var lines []string = []string{}
	_ = lines
	var i int = 1
	_ = i
	for i < n {
		left := repeat_char("*", i)
		_ = left
		mid := repeat_char(" ", ((2 * (n - i)) - 1))
		_ = mid
		right := repeat_char("*", i)
		_ = right
		lines = append(lines, fmt.Sprint(((fmt.Sprint(left) + fmt.Sprint(mid)) + fmt.Sprint(right))))
		i = (i + 1)
	}
	lines = append(lines, repeat_char("*", ((2*n)-1)))
	var j int = (n - 1)
	_ = j
	for j > 0 {
		left := repeat_char("*", j)
		_ = left
		mid := repeat_char(" ", ((2 * (n - j)) - 1))
		_ = mid
		right := repeat_char("*", j)
		_ = right
		lines = append(lines, fmt.Sprint(((fmt.Sprint(left) + fmt.Sprint(mid)) + fmt.Sprint(right))))
		j = (j - 1)
	}
	var out string = ""
	_ = out
	var k int = 0
	_ = k
	for k < len(lines) {
		if k > 0 {
			out = (out + "\n")
		}
		out = (out + _index(lines, k))
		k = (k + 1)
	}
	return out
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(butterfly_pattern(3))
		fmt.Println(butterfly_pattern(5))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
