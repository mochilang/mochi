//go:build ignore

// Generated by Mochi v0.10.64 on 2025-08-12 12:35:59 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _mod(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func isqrt(n int) int {
	var r int = 0
	_ = r
	for ((r + 1) * (r + 1)) <= n {
		r = (r + 1)
	}
	return r
}

func is_prime(number int) bool {
	if (1 < number) && (number < 4) {
		return true
	} else {
		if ((number < 2) || (_mod(number, 2) == 0)) || (_mod(number, 3) == 0) {
			return false
		}
	}
	var limit int = isqrt(number)
	_ = limit
	var i int = 5
	_ = i
	for i <= limit {
		if (_mod(number, i) == 0) || (_mod(number, (i+2)) == 0) {
			return false
		}
		i = (i + 6)
	}
	return true
}

func solution(nth int) int {
	var count int = 0
	_ = count
	var number int = 1
	_ = number
	for (count != nth) && (number < 3) {
		number = (number + 1)
		if is_prime(number) {
			count = (count + 1)
		}
	}
	for count != nth {
		number = (number + 2)
		if is_prime(number) {
			count = (count + 1)
		}
	}
	return number
}

func mochiMain() {
	fmt.Println(("solution() = " + fmt.Sprint(solution(10001))))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
