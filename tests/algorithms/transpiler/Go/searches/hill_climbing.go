//go:build ignore

// Generated by Mochi v0.10.63 on 2025-08-11 16:29:15 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		if vv, ok := v.(int); ok {
			return vv
		}
		if vv, ok := v.(float64); ok {
			return int(vv)
		}
		return v.(int)
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	return s[idx]
}

func _len(v any) int {
	switch t := v.(type) {
	case string:
		return len([]rune(t))
	case []any:
		return len(t)
	case []string:
		return len(t)
	case []int:
		return len(t)
	case []float64:
		return len(t)
	case map[string]any:
		return len(t)
	case map[int]any:
		return len(t)
	case map[any]any:
		return len(t)
	default:
		return 0
	}
}

func _toFloat(v any) float64 {
	switch t := v.(type) {
	case int:
		return float64(t)
	case int64:
		return float64(t)
	case float64:
		return t
	default:
		return 0
	}
}

type SearchProblem struct {
	X    float64                        `json:"x"`
	Y    float64                        `json:"y"`
	Step float64                        `json:"step"`
	F    func(float64, float64) float64 `json:"f"`
}

func score(sp SearchProblem) float64 {
	return sp.F(sp.X, sp.Y)
}

func neighbors(sp SearchProblem) []SearchProblem {
	var s float64 = sp.Step
	_ = s
	_ = s
	return []SearchProblem{SearchProblem{
		X:    (sp.X - s),
		Y:    (sp.Y - s),
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    (sp.X - s),
		Y:    sp.Y,
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    (sp.X - s),
		Y:    (sp.Y + s),
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    sp.X,
		Y:    (sp.Y - s),
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    sp.X,
		Y:    (sp.Y + s),
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    (sp.X + s),
		Y:    (sp.Y - s),
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    (sp.X + s),
		Y:    sp.Y,
		Step: s,
		F:    sp.F,
	}, SearchProblem{
		X:    (sp.X + s),
		Y:    (sp.Y + s),
		Step: s,
		F:    sp.F,
	}}
}

func equal_state(a SearchProblem, b SearchProblem) bool {
	return ((a.X == b.X) && (a.Y == b.Y))
}

func contains_state(lst []SearchProblem, sp SearchProblem) bool {
	var i int = 0
	_ = i
	for i < len(lst) {
		if equal_state(_index(lst, i), sp) {
			return true
		}
		i = (i + 1)
	}
	return false
}

func hill_climbing(sp SearchProblem, find_max bool, max_x float64, min_x float64, max_y float64, min_y float64, max_iter int) SearchProblem {
	var current SearchProblem = sp
	_ = current
	var visited []SearchProblem = []SearchProblem{}
	_ = visited
	var iterations int = 0
	_ = iterations
	var solution_found bool = false
	_ = solution_found
	for (solution_found == false) && (iterations < max_iter) {
		visited = append(visited, current)
		iterations = (iterations + 1)
		current_score := score(current)
		_ = current_score
		neighs := neighbors(current)
		_ = neighs
		var max_change float64 = (0 - 1000000000000000000.0)
		_ = max_change
		var min_change float64 = 1000000000000000000.0
		_ = min_change
		var next SearchProblem = current
		_ = next
		var improved bool = false
		_ = improved
		var i int = 0
		_ = i
		for i < _len(neighs) {
			n := _index(func(v any) []int {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]int); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []int{}
					}
					out := make([]int, len(arr))
					for i, x := range arr {
						out[i] = x.(int)
					}
					return out
				}
				return v.([]int)
			}(neighs), i)
			_ = n
			i = (i + 1)
			if contains_state(func(v any) []SearchProblem {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]SearchProblem); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []SearchProblem{}
					}
					out := make([]SearchProblem, len(arr))
					for i, x := range arr {
						out[i] = x.(SearchProblem)
					}
					return out
				}
				return v.([]SearchProblem)
			}(visited), SearchProblem(n)) {
				continue
			}
			if (((_toFloat(SearchProblem(n).X) > max_x) || (_toFloat(SearchProblem(n).X) < min_x)) || (_toFloat(SearchProblem(n).Y) > max_y)) || (_toFloat(SearchProblem(n).Y) < min_y) {
				continue
			}
			change := (score(SearchProblem(n)) - _toFloat(current_score))
			_ = change
			if find_max {
				if (_toFloat(change) > max_change) && (_toFloat(change) > 0.0) {
					max_change = change
					next = n
					improved = true
				}
			} else {
				if (_toFloat(change) < min_change) && (_toFloat(change) < 0.0) {
					min_change = change
					next = n
					improved = true
				}
			}
		}
		if improved {
			current = next
		} else {
			solution_found = true
		}
	}
	return current
}

func test_f1(x float64, y float64) float64 {
	return ((x * x) + (y * y))
}

func mochiMain() {
	prob1 := SearchProblem{
		X:    3.0,
		Y:    4.0,
		Step: 1.0,
		F:    test_f1,
	}
	_ = prob1
	local_min1 := hill_climbing(SearchProblem(prob1), false, 1000000000.0, (0 - 1000000000.0), 1000000000.0, (0 - 1000000000.0), 10000)
	_ = local_min1
	_ = local_min1
	fmt.Println(fmt.Sprint(int(score(SearchProblem(local_min1)))))
	prob2 := SearchProblem{
		X:    12.0,
		Y:    47.0,
		Step: 1.0,
		F:    test_f1,
	}
	_ = prob2
	local_min2 := hill_climbing(SearchProblem(prob2), false, 100.0, 5.0, 50.0, (0 - 5.0), 10000)
	_ = local_min2
	_ = local_min2
	fmt.Println(fmt.Sprint(int(score(SearchProblem(local_min2)))))
	prob3 := SearchProblem{
		X:    3.0,
		Y:    4.0,
		Step: 1.0,
		F:    test_f1,
	}
	_ = prob3
	local_max := hill_climbing(SearchProblem(prob3), true, 1000000000.0, (0 - 1000000000.0), 1000000000.0, (0 - 1000000000.0), 1000)
	_ = local_max
	_ = local_max
	fmt.Println(fmt.Sprint(int(score(SearchProblem(local_max)))))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
