//go:build ignore

// Generated by Mochi v0.10.63 on 2025-08-11 15:36:14 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		if vv, ok := v.(int); ok {
			return vv
		}
		if vv, ok := v.(float64); ok {
			return int(vv)
		}
		return v.(int)
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	return s[idx]
}

func _len(v any) int {
	switch t := v.(type) {
	case string:
		return len([]rune(t))
	case []any:
		return len(t)
	case []string:
		return len(t)
	case []int:
		return len(t)
	case []float64:
		return len(t)
	case map[string]any:
		return len(t)
	case map[int]any:
		return len(t)
	case map[any]any:
		return len(t)
	default:
		return 0
	}
}

func split_words(s string) []string {
	var words []string = []string{}
	_ = words
	var current string = ""
	_ = current
	var i int = 0
	_ = i
	for i < len(s) {
		ch := string(_index([]rune(s), i))
		_ = ch
		if fmt.Sprint(ch) == " " {
			if len(current) > 0 {
				words = append(words, current)
				current = ""
			}
		} else {
			current = (current + fmt.Sprint(ch))
		}
		i = (i + 1)
	}
	if len(current) > 0 {
		words = append(words, current)
	}
	return words
}

func reverse_words(input_str string) string {
	words := split_words(input_str)
	_ = words
	var res string = ""
	_ = res
	var i int = (_len(words) - 1)
	_ = i
	for i >= 0 {
		res = (res + fmt.Sprint(_index(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}(words), i)))
		if i > 0 {
			res = (res + " ")
		}
		i = (i - 1)
	}
	return res
}

func mochiMain() {
	fmt.Println(reverse_words("I love Python"))
	fmt.Println(reverse_words("I     Love          Python"))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
