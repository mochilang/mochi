//go:build ignore

// Generated by Mochi v0.10.63 on 2025-08-11 15:36:15 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"strings"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		if vv, ok := v.(int); ok {
			return vv
		}
		if vv, ok := v.(float64); ok {
			return int(vv)
		}
		return v.(int)
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	return s[idx]
}

func _len(v any) int {
	switch t := v.(type) {
	case string:
		return len([]rune(t))
	case []any:
		return len(t)
	case []string:
		return len(t)
	case []int:
		return len(t)
	case []float64:
		return len(t)
	case map[string]any:
		return len(t)
	case map[int]any:
		return len(t)
	case map[any]any:
		return len(t)
	default:
		return 0
	}
}

func split(s string, sep string) []string {
	var res []string = []string{}
	_ = res
	var current string = ""
	_ = current
	var i int = 0
	_ = i
	for i < len(s) {
		ch := _substr(s, i, (i + 1))
		_ = ch
		if fmt.Sprint(ch) == sep {
			res = append(res, current)
			current = ""
		} else {
			current = (current + fmt.Sprint(ch))
		}
		i = (i + 1)
	}
	res = append(res, current)
	return res
}

func capitalize(word string) string {
	if len(word) == 0 {
		return ""
	}
	first := strings.ToUpper(_substr(word, 0, 1))
	_ = first
	_ = first
	rest := _substr(word, 1, len(word))
	_ = rest
	_ = rest
	return fmt.Sprint((fmt.Sprint(first) + fmt.Sprint(rest)))
}

func snake_to_camel_case(input_str string, use_pascal bool) string {
	words := split(input_str, "_")
	_ = words
	var result string = ""
	_ = result
	var index int = 0
	_ = index
	if !use_pascal {
		if _len(words) > 0 {
			result = _index(func(v any) []int {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]int); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []int{}
					}
					out := make([]int, len(arr))
					for i, x := range arr {
						out[i] = x.(int)
					}
					return out
				}
				return v.([]int)
			}(words), 0)
			index = 1
		}
	}
	for index < _len(words) {
		word := _index(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}(words), index)
		_ = word
		result = (result + capitalize(word))
		index = (index + 1)
	}
	return result
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(snake_to_camel_case("some_random_string", false))
		fmt.Println(snake_to_camel_case("some_random_string", true))
		fmt.Println(snake_to_camel_case("some_random_string_with_numbers_123", false))
		fmt.Println(snake_to_camel_case("some_random_string_with_numbers_123", true))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
