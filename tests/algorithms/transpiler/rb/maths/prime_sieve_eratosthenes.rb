# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def prime_sieve_eratosthenes(num)
    if num <= 0
      panic("Input must be a positive integer")
    end
    primes = [].clone
    i = 0.clone
    while i <= num
      primes = (primes << (true)).clone
      i = _add(i, 1).clone
    end
    p = 2.clone
    while p * p <= num
      if primes[p]
        j = p * p.clone
        while j <= num
          primes[j] = false
          j = _add(j, p).clone
        end
      end
      p = _add(p, 1).clone
    end
    result = [].clone
    k = 2.clone
    while k <= num
      if primes[k]
        result = (result << (k)).clone
      end
      k = _add(k, 1).clone
    end
    return result
  end
  def list_eq(a, b)
    if a.length != b.length
      return false
    end
    i = 0.clone
    while i < a.length
      if a[i] != b[i]
        return false
      end
      i = _add(i, 1).clone
    end
    return true
  end
  def test_prime_sieve_eratosthenes()
    if !list_eq(prime_sieve_eratosthenes(10), [2, 3, 5, 7])
      panic("test 10 failed")
    end
    if !list_eq(prime_sieve_eratosthenes(20), [2, 3, 5, 7, 11, 13, 17, 19])
      panic("test 20 failed")
    end
    if !list_eq(prime_sieve_eratosthenes(2), [2])
      panic("test 2 failed")
    end
    if prime_sieve_eratosthenes(1).length != 0
      panic("test 1 failed")
    end
  end
  def main()
    test_prime_sieve_eratosthenes()
    puts(((x = prime_sieve_eratosthenes(20)); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
