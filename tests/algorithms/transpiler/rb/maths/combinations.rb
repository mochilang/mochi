# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def combinations(n, k)
    if k < 0 || n < k
      panic("Please enter positive integers for n and k where n >= k")
    end
    res = 1.clone
    i = 0.clone
    while i < k
      res = res * (n - i).clone
      res = res / (_add(i, 1)).clone
      i = _add(i, 1).clone
    end
    return res
  end
  puts(_add("The number of five-card hands possible from a standard fifty-two card deck is: ", _str(combinations(52, 5))))
  puts("")
  puts(_add(_add("If a class of 40 students must be arranged into groups of 4 for group projects, there are ", _str(combinations(40, 4))), " ways to arrange them."))
  puts("")
  puts(_add(_add("If 10 teams are competing in a Formula One race, there are ", _str(combinations(10, 3))), " ways that first, second and third place can be awarded."))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
