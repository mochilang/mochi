# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def recursive_lucas_number(n)
    if n == 0
      return 2
    end
    if n == 1
      return 1
    end
    return _add(recursive_lucas_number(n - 1), recursive_lucas_number(n - 2))
  end
  def dynamic_lucas_number(n)
    a = 2.clone
    b = 1.clone
    i = 0.clone
    while i < n
      next_ = _add(a, b)
      a = b.clone
      b = next_.clone
      i = _add(i, 1).clone
    end
    return a
  end
  puts(_str(recursive_lucas_number(1)))
  puts(_str(recursive_lucas_number(20)))
  puts(_str(recursive_lucas_number(0)))
  puts(_str(recursive_lucas_number(5)))
  puts(_str(dynamic_lucas_number(1)))
  puts(_str(dynamic_lucas_number(20)))
  puts(_str(dynamic_lucas_number(0)))
  puts(_str(dynamic_lucas_number(25)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
