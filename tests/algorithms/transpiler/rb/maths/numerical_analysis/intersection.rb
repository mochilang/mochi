# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def abs_float(x)
    if x < 0.0
      return -x
    end
    return x
  end
  def intersection(function, x0, x1)
    x_n = x0.clone
    x_n1 = x1.clone
    while true
      if x_n == x_n1 || function.call(x_n1) == function.call(x_n)
        panic("float division by zero, could not find root")
      end
      numerator = function.call(x_n1)
      denominator = (function.call(x_n1) - function.call(x_n)) / (x_n1 - x_n)
      x_n2 = x_n1 - numerator / denominator
      if abs_float(x_n2 - x_n1) < 1e-05
        return x_n2
      end
      x_n = x_n1.clone
      x_n1 = x_n2.clone
    end
  end
  def f(x)
    return x * x * x - 2.0 * x - 5.0
  end
  def main()
    puts(_str(intersection(method(:f), 3.0, 3.5)))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
