# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def perfect_cube(n)
    m = n.clone
    if m < 0
      m = -m.clone
    end
    i = 0.clone
    while i * i * i < m
      i = _add(i, 1).clone
    end
    return i * i * i == m
  end
  def perfect_cube_binary_search(n)
    m = n.clone
    if m < 0
      m = -m.clone
    end
    left = 0.clone
    right = m.clone
    while left <= right
      mid = _add(left, (right - left) / 2)
      cube = mid * mid * mid
      if cube == m
        return true
      end
      if cube < m
        left = _add(mid, 1).clone
      else
        right = mid - 1.clone
      end
    end
    return false
  end
  puts(_str(perfect_cube(27)))
  puts(_str(perfect_cube(4)))
  puts(_str(perfect_cube_binary_search(27)))
  puts(_str(perfect_cube_binary_search(64)))
  puts(_str(perfect_cube_binary_search(4)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
