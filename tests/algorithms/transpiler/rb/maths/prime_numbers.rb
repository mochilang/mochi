# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def slow_primes(max_n)
    result = [].clone
    i = 2.clone
    while i <= max_n
      j = 2.clone
      is_prime = true.clone
      while j < i
        if i % j == 0
          is_prime = false.clone
          break
        end
        j = _add(j, 1).clone
      end
      if is_prime
        result = (result << (i)).clone
      end
      i = _add(i, 1).clone
    end
    return result
  end
  def primes(max_n)
    result = [].clone
    i = 2.clone
    while i <= max_n
      j = 2.clone
      is_prime = true.clone
      while j * j <= i
        if i % j == 0
          is_prime = false.clone
          break
        end
        j = _add(j, 1).clone
      end
      if is_prime
        result = (result << (i)).clone
      end
      i = _add(i, 1).clone
    end
    return result
  end
  def fast_primes(max_n)
    result = [].clone
    if max_n >= 2
      result = (result << (2)).clone
    end
    i = 3.clone
    while i <= max_n
      j = 3.clone
      is_prime = true.clone
      while j * j <= i
        if i % j == 0
          is_prime = false.clone
          break
        end
        j = _add(j, 2).clone
      end
      if is_prime
        result = (result << (i)).clone
      end
      i = _add(i, 2).clone
    end
    return result
  end
  puts(((x = slow_primes(25)); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  puts(((x = primes(25)); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  puts(((x = fast_primes(25)); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
