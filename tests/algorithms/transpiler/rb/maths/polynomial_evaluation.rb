# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def pow_float(base, exponent)
    exp = exponent.clone
    result = 1.0.clone
    i = 0.clone
    while i < exp
      result = result * base.clone
      i = _add(i, 1).clone
    end
    return result
  end
  def evaluate_poly(poly, x)
    total = 0.0.clone
    i = 0.clone
    while i < poly.length
      total = _add(total, poly[i] * pow_float(x, i)).clone
      i = _add(i, 1).clone
    end
    return total
  end
  def horner(poly, x)
    result = 0.0.clone
    i = poly.length - 1.clone
    while i >= 0
      result = _add(result * x, poly[i]).clone
      i = i - 1.clone
    end
    return result
  end
  def test_polynomial_evaluation()
    poly = [0.0, 0.0, 5.0, 9.3, 7.0]
    x = 10.0
    if evaluate_poly(poly, x) != 79800.0
      panic("evaluate_poly failed")
    end
    if horner(poly, x) != 79800.0
      panic("horner failed")
    end
  end
  def main()
    test_polynomial_evaluation()
    poly = [0.0, 0.0, 5.0, 9.3, 7.0]
    x = 10.0
    puts(evaluate_poly(poly, x))
    puts(horner(poly, x))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
