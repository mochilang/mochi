# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def multiplicative_persistence(num)
    if num < 0
      panic("multiplicative_persistence() does not accept negative values")
    end
    steps = 0.clone
    n = num.clone
    while n >= 10
      product = 1.clone
      temp = n.clone
      while temp > 0
        digit = temp % 10
        product = product * digit.clone
        temp = temp / 10.clone
      end
      n = product.clone
      steps = _add(steps, 1).clone
    end
    return steps
  end
  def additive_persistence(num)
    if num < 0
      panic("additive_persistence() does not accept negative values")
    end
    steps = 0.clone
    n = num.clone
    while n >= 10
      total = 0.clone
      temp = n.clone
      while temp > 0
        digit = temp % 10
        total = _add(total, digit).clone
        temp = temp / 10.clone
      end
      n = total.clone
      steps = _add(steps, 1).clone
    end
    return steps
  end
  def test_persistence()
    if multiplicative_persistence(217) != 2
      panic("multiplicative_persistence failed")
    end
    if additive_persistence(199) != 3
      panic("additive_persistence failed")
    end
  end
  def main()
    test_persistence()
    puts(_str(multiplicative_persistence(217)))
    puts(_str(additive_persistence(199)))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
