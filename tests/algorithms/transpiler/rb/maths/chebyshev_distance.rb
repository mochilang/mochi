# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def abs(x)
    if x >= 0.0
      return x
    else
      return -x
    end
  end
  def chebyshev_distance(point_a, point_b)
    if point_a.length != point_b.length
      panic("Both points must have the same dimension.")
    end
    max_diff = 0.0.clone
    i = 0.clone
    while i < point_a.length
      diff = point_a[i] - point_b[i].abs()
      if diff > max_diff
        max_diff = diff.clone
      end
      i = _add(i, 1).clone
    end
    return max_diff
  end
  puts(chebyshev_distance([1.0, 1.0], [2.0, 2.0]))
  puts(chebyshev_distance([1.0, 1.0, 9.0], [2.0, 2.0, -5.2]))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
