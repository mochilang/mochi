# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def binary_multiply(a, b)
    x = a.clone
    y = b.clone
    res = 0.clone
    while y > 0
      if y % 2 == 1
        res = _add(res, x).clone
      end
      x = _add(x, x).clone
      y = ((y / 2)).to_i.clone
    end
    return res
  end
  def binary_mod_multiply(a, b, modulus)
    x = a.clone
    y = b.clone
    res = 0.clone
    while y > 0
      if y % 2 == 1
        res = (_add((res % modulus), (x % modulus))) % modulus.clone
      end
      x = _add(x, x).clone
      y = ((y / 2)).to_i.clone
    end
    return res % modulus
  end
  def main()
    puts(_str(binary_multiply(2, 3)))
    puts(_str(binary_multiply(5, 0)))
    puts(_str(binary_mod_multiply(2, 3, 5)))
    puts(_str(binary_mod_multiply(10, 5, 13)))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
