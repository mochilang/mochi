# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def remove_digit(num)
    n = num.clone
    if n < 0
      n = -n.clone
    end
    max_val = 0.clone
    divisor = 1.clone
    while divisor <= n
      higher = n / (divisor * 10)
      lower = n % divisor
      candidate = _add(higher * divisor, lower)
      if candidate > max_val
        max_val = candidate.clone
      end
      divisor = divisor * 10.clone
    end
    return max_val
  end
  def test_remove_digit()
    if remove_digit(152) != 52
      panic("remove_digit(152) failed")
    end
    if remove_digit(6385) != 685
      panic("remove_digit(6385) failed")
    end
    if remove_digit(-11) != 1
      panic("remove_digit(-11) failed")
    end
    if remove_digit(2222222) != 222222
      panic("remove_digit(2222222) failed")
    end
  end
  def main()
    test_remove_digit()
    puts(remove_digit(152))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
