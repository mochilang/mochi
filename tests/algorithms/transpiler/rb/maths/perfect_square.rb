# Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:17 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def perfect_square(num)
    if num < 0
      return false
    end
    i = 0.clone
    while i * i <= num
      if i * i == num
        return true
      end
      i = _add(i, 1).clone
    end
    return false
  end
  def perfect_square_binary_search(n)
    if n < 0
      return false
    end
    left = 0.clone
    right = n.clone
    while left <= right
      mid = (_add(left, right)) / 2
      sq = mid * mid
      if sq == n
        return true
      end
      if sq > n
        right = mid - 1.clone
      else
        left = _add(mid, 1).clone
      end
    end
    return false
  end
  puts(_str(perfect_square(9)))
  puts(_str(perfect_square(10)))
  puts(_str(perfect_square_binary_search(16)))
  puts(_str(perfect_square_binary_search(10)))
  puts(_str(perfect_square_binary_search(-1)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
