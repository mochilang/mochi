# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def is_prime(number)
    if number > 1 && number < 4
      return true
    end
    if number < 2 || number % 2 == 0 || number % 3 == 0
      return false
    end
    i = 5.clone
    while i * i <= number
      if number % i == 0 || number % (_add(i, 2)) == 0
        return false
      end
      i = _add(i, 6).clone
    end
    return true
  end
  def solution(n)
    num = n.clone
    if num <= 0
      puts("Parameter n must be greater than or equal to one.")
      return 0
    end
    if is_prime(num)
      return num
    end
    while num % 2 == 0
      num = num / 2.clone
      if is_prime(num)
        return num
      end
    end
    max_number = 1.clone
    i = 3.clone
    while i * i <= num
      if num % i == 0
        if is_prime(num / i)
          max_number = num / i.clone
          break
        else
          if is_prime(i)
            max_number = i.clone
          end
        end
      end
      i = _add(i, 2).clone
    end
    return max_number
  end
  def main()
    result = solution(600851475143)
    puts(_add("solution() = ", _str(result)))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
