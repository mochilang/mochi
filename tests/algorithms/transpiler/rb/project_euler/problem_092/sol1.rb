# Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:09 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def next_number(number)
    n = number.clone
    total = 0.clone
    while n > 0
      d = n % 10
      total = _add(total, d * d).clone
      n = n / 10.clone
    end
    return total
  end
  def chain(number)
    n = number.clone
    while !_eq(n, 1) && !_eq(n, 89)
      n = next_number(n).clone
    end
    return _eq(n, 1)
  end
  def solution(limit)
    count = 0.clone
    i = 1.clone
    while i < limit
      if !chain(i)
        count = _add(count, 1).clone
      end
      i = _add(i, 1).clone
    end
    return count
  end
  puts(_str(next_number(44)))
  puts(_str(next_number(10)))
  puts(_str(next_number(32)))
  puts(_str(chain(10)))
  puts(_str(chain(58)))
  puts(_str(chain(1)))
  puts(_str(solution(100)))
  puts(_str(solution(1000)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
