# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def count_divisors(n)
    m = n.clone
    n_divisors = 1.clone
    i = 2.clone
    while i * i <= m
      multiplicity = 0.clone
      while m % i == 0
        m = m / i.clone
        multiplicity = _add(multiplicity, 1).clone
      end
      n_divisors = n_divisors * (_add(multiplicity, 1)).clone
      i = _add(i, 1).clone
    end
    if m > 1
      n_divisors = n_divisors * 2.clone
    end
    return n_divisors
  end
  def solution()
    t_num = 1.clone
    i = 1.clone
    while true
      i = _add(i, 1).clone
      t_num = _add(t_num, i).clone
      if count_divisors(t_num) > 500
        break
      end
    end
    return t_num
  end
  puts(_str(solution()))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
