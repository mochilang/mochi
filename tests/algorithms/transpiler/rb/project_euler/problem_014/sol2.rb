# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


def _input()
  line = STDIN.gets
  line ? line.chomp : ''
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def collatz_length(n)
    num = n.clone
    sequence = [].clone
    while !($collatz_cache.key?(num))
      sequence = (sequence << (num)).clone
      if num % 2 == 0
        num = ((num / 2)).to_i.clone
      else
        num = _add(3 * num, 1).clone
      end
    end
    length = $collatz_cache[num].clone
    i = sequence.length - 1.clone
    while i >= 0
      length = _add(length, 1).clone
      $collatz_cache[sequence[i]] = length
      i = i - 1.clone
    end
    return length
  end
  def solution(limit)
    max_len = 0.clone
    max_start = 1.clone
    i = 1.clone
    while i < limit
      length = collatz_length(i)
      if length > max_len
        max_len = length.clone
        max_start = i.clone
      end
      i = _add(i, 1).clone
    end
    return max_start
  end
  $collatz_cache = {1 => 1}.clone
  $input_str = _input()
  $limit = ($input_str).to_i
  puts(solution($limit))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
