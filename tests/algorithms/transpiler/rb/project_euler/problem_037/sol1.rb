# Generated by Mochi transpiler v0.10.60 on 2025-08-08 10:32 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def is_prime(number)
    if 1 < number && number < 4
      return true
    end
    if number < 2 || _eq(number % 2, 0) || _eq(number % 3, 0)
      return false
    end
    i = 5.clone
    while i * i <= number
      if _eq(number % i, 0) || _eq(number % (_add(i, 2)), 0)
        return false
      end
      i = _add(i, 6).clone
    end
    return true
  end
  def list_truncated_nums(n)
    str_num = _str(n)
    list_nums = [n].clone
    i = 1.clone
    length = str_num.length
    while i < length
      right = (str_num[i...length]).to_i
      left = (str_num[0...length - i]).to_i
      list_nums = (list_nums << (right)).clone
      list_nums = (list_nums << (left)).clone
      i = _add(i, 1).clone
    end
    return list_nums
  end
  def validate(n)
    s = _str(n)
    length = s.length
    if length > 3
      last3 = (s[length - 3...length]).to_i
      first3 = (s[0...3]).to_i
      if !(is_prime(last3) && is_prime(first3))
        return false
      end
    end
    return true
  end
  def compute_truncated_primes(count)
    list_truncated_primes = [].clone
    num = 13.clone
    while !_eq(list_truncated_primes.length, count)
      if validate(num)
        list_nums = list_truncated_nums(num)
        all_prime = true.clone
        j = 0.clone
        while j < list_nums.length
          if !is_prime(list_nums[j])
            all_prime = false.clone
            break
          end
          j = _add(j, 1).clone
        end
        if all_prime
          list_truncated_primes = (list_truncated_primes << (num)).clone
        end
      end
      num = _add(num, 2).clone
    end
    return list_truncated_primes
  end
  def solution()
    primes = compute_truncated_primes(11)
    total = 0.clone
    i = 0.clone
    while i < primes.length
      total = _add(total, primes[i]).clone
      i = _add(i, 1).clone
    end
    return total
  end
  puts(_add("sum(compute_truncated_primes(11)) = ", _str(solution())))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
