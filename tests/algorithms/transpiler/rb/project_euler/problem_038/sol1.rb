# Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:09 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def is_9_pandigital(n)
    digits = [].clone
    i = 0.clone
    while i < 10
      digits = (digits << (0)).clone
      i = _add(i, 1).clone
    end
    count = 0.clone
    x = n.clone
    while x > 0
      digit = x % 10
      if _eq(digit, 0)
        return false
      end
      if _eq(digits[digit], 1)
        return false
      end
      digits[digit] = 1
      x = x / 10.clone
      count = _add(count, 1).clone
    end
    return _eq(count, 9) && _eq(digits[1], 1) && _eq(digits[2], 1) && _eq(digits[3], 1) && _eq(digits[4], 1) && _eq(digits[5], 1) && _eq(digits[6], 1) && _eq(digits[7], 1) && _eq(digits[8], 1) && _eq(digits[9], 1)
  end
  def solution()
    base_num = 9999.clone
    while base_num >= 5000
      candidate = 100002 * base_num
      if is_9_pandigital(candidate)
        return candidate
      end
      base_num = base_num - 1.clone
    end
    base_num = 333.clone
    while base_num >= 100
      candidate = 1002003 * base_num
      if is_9_pandigital(candidate)
        return candidate
      end
      base_num = base_num - 1.clone
    end
    return 0
  end
  puts(_add("solution() = ", _str(solution())))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
