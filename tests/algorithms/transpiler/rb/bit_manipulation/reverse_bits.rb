# Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:30 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def get_reverse_bit_string(number)
    bit_string = ""
    n = number
    i = 0
    while i < 32
      bit_string = _add(bit_string, (n % 2).to_s)
      n = n / 2
      i = _add(i, 1)
    end
    return bit_string
  end
  def reverse_bit(number)
    if number < 0
      panic("the value of input must be positive")
    end
    n = number
    result = 0
    i = 1
    while i <= 32
      result = result * 2
      end_bit = n % 2
      n = n / 2
      result = _add(result, end_bit)
      i = _add(i, 1)
    end
    return get_reverse_bit_string(result)
  end
  puts(reverse_bit(25))
  puts(reverse_bit(37))
  puts(reverse_bit(21))
  puts(reverse_bit(58))
  puts(reverse_bit(0))
  puts(reverse_bit(256))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
