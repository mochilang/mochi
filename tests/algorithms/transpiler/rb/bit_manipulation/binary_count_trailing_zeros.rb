# Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:11 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def binary_count_trailing_zeros(a)
    if a < 0
      panic("Input value must be a non-negative integer")
    end
    if a == 0
      return 0
    end
    n = a
    count = 0
    while n % 2 == 0
      count = _add(count, 1)
      n = n / 2
    end
    return count
  end
  puts((binary_count_trailing_zeros(25)).to_s)
  puts((binary_count_trailing_zeros(36)).to_s)
  puts((binary_count_trailing_zeros(16)).to_s)
  puts((binary_count_trailing_zeros(58)).to_s)
  puts((binary_count_trailing_zeros(4294967296)).to_s)
  puts((binary_count_trailing_zeros(0)).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
