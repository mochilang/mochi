# Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:11 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def to_binary4(n)
    result = ""
    x = n
    while x > 0
      result = _add((x % 2).to_s, result)
      x = x / 2
    end
    while result.length < 4
      result = _add("0", result)
    end
    return result
  end
  def binary_coded_decimal(number)
    n = number
    if n < 0
      n = 0
    end
    digits = (n).to_s
    out = "0b"
    i = 0
    while i < digits.length
      d = digits[i]
      d_int = (d).to_i
      out = _add(out, to_binary4(d_int))
      i = _add(i, 1)
    end
    return out
  end
  puts(binary_coded_decimal(-2))
  puts(binary_coded_decimal(-1))
  puts(binary_coded_decimal(0))
  puts(binary_coded_decimal(3))
  puts(binary_coded_decimal(2))
  puts(binary_coded_decimal(12))
  puts(binary_coded_decimal(987))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
