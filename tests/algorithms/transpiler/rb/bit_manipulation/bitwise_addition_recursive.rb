# Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:30 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def bitwise_xor(a, b)
    result = 0
    bit = 1
    x = a
    y = b
    while x > 0 || y > 0
      ax = x % 2
      by = y % 2
      if (_add(ax, by)) % 2 == 1
        result = _add(result, bit)
      end
      x = x / 2
      y = y / 2
      bit = bit * 2
    end
    return result
  end
  def bitwise_and(a, b)
    result = 0
    bit = 1
    x = a
    y = b
    while x > 0 && y > 0
      if x % 2 == 1 && y % 2 == 1
        result = _add(result, bit)
      end
      x = x / 2
      y = y / 2
      bit = bit * 2
    end
    return result
  end
  def bitwise_addition_recursive(number, other_number)
    if number < 0 || other_number < 0
      panic("Both arguments MUST be non-negative!")
    end
    bitwise_sum = bitwise_xor(number, other_number)
    carry = bitwise_and(number, other_number)
    if carry == 0
      return bitwise_sum
    end
    return bitwise_addition_recursive(bitwise_sum, carry * 2)
  end
  puts((bitwise_addition_recursive(4, 5)).to_s)
  puts((bitwise_addition_recursive(8, 9)).to_s)
  puts((bitwise_addition_recursive(0, 4)).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
