# Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:45 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def set_at_int(xs, idx, value)
    res = [].clone
    i = 0.clone
    while i < xs.length
      if i == idx
        res = (res << (value)).clone
      else
        res = (res << (xs[i])).clone
      end
      i = _add(i, 1).clone
    end
    return res
  end
  def comp_and_swap(arr, i, j, dir)
    res = arr.clone
    xi = arr[i]
    xj = arr[j]
    if (dir == 1 && xi > xj) || (dir == 0 && xi < xj)
      res = set_at_int(res, i, xj).clone
      res = set_at_int(res, j, xi).clone
    end
    return res
  end
  def bitonic_merge(arr, low, length, dir)
    res = arr.clone
    if length > 1
      mid = length / 2
      k = low.clone
      while k < _add(low, mid)
        res = comp_and_swap(res, k, _add(k, mid), dir).clone
        k = _add(k, 1).clone
      end
      res = bitonic_merge(res, low, mid, dir).clone
      res = bitonic_merge(res, _add(low, mid), mid, dir).clone
    end
    return res
  end
  def bitonic_sort(arr, low, length, dir)
    res = arr.clone
    if length > 1
      mid = length / 2
      res = bitonic_sort(res, low, mid, 1).clone
      res = bitonic_sort(res, _add(low, mid), mid, 0).clone
      res = bitonic_merge(res, low, length, dir).clone
    end
    return res
  end
  def main()
    data = [12, 34, 92, -23, 0, -121, -167, 145]
    asc = bitonic_sort(data, 0, data.length, 1)
    puts(((x = asc); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    desc = bitonic_merge(asc, 0, asc.length, 0)
    puts(((x = desc); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
