# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def pow10(exp)
    result = 1.0.clone
    i = 0.clone
    while i < exp
      result = result * 10.0.clone
      i = _add(i, 1).clone
    end
    return result
  end
  def maximum_kinetic_energy(frequency, work_function, in_ev)
    if frequency < 0.0
      panic("Frequency can't be negative.")
    end
    energy = (in_ev ? $PLANCK_CONSTANT_EVS * frequency - work_function : $PLANCK_CONSTANT_JS * frequency - work_function)
    if energy > 0.0
      return energy
    end
    return 0.0
  end
  $PLANCK_CONSTANT_JS = 6.6261 / pow10(34)
  $PLANCK_CONSTANT_EVS = 4.1357 / pow10(15)
  puts(_str(maximum_kinetic_energy(1000000.0, 2.0, false)))
  puts(_str(maximum_kinetic_energy(1000000.0, 2.0, true)))
  puts(_str(maximum_kinetic_energy(10000000000000000.0, 2.0, true)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
