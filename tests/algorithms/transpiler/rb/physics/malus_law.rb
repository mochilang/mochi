# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def _mod(x, m)
    return x - (((x / m).to_i).to_f) * m
  end
  def cos(x)
    y = _mod(_add(x, $PI), $TWO_PI) - $PI
    y2 = y * y
    y4 = y2 * y2
    y6 = y4 * y2
    return _add(1.0 - y2 / 2.0, y4 / 24.0) - y6 / 720.0
  end
  def radians(deg)
    return deg * $PI / 180.0
  end
  def abs_val(x)
    if x < 0.0
      return -x
    end
    return x
  end
  def malus_law(initial_intensity, angle)
    if initial_intensity < 0.0
      panic("The value of intensity cannot be negative")
    end
    if angle < 0.0 || angle > 360.0
      panic("In Malus Law, the angle is in the range 0-360 degrees")
    end
    theta = radians(angle)
    c = cos(theta)
    return initial_intensity * (c * c)
  end
  def test_malus_law()
    if abs_val(malus_law(10.0, 45.0) - 5.0) > 0.01
      panic("malus_law test1 failed")
    end
    if abs_val(malus_law(100.0, 60.0) - 25.0) > 0.01
      panic("malus_law test2 failed")
    end
    if abs_val(malus_law(50.0, 150.0) - 37.5) > 0.01
      panic("malus_law test3 failed")
    end
    if abs_val(malus_law(75.0, 270.0) - 0.0) > 0.01
      panic("malus_law test4 failed")
    end
    if abs_val(malus_law(100.0, 180.0) - 100.0) > 0.01
      panic("malus_law test5 failed")
    end
    if abs_val(malus_law(100.0, 360.0) - 100.0) > 0.01
      panic("malus_law test6 failed")
    end
  end
  def main()
    test_malus_law()
    puts(_str(malus_law(100.0, 60.0)))
  end
  $PI = 3.141592653589793
  $TWO_PI = 6.283185307179586
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
