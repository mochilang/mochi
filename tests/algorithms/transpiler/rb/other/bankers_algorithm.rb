# Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:16 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def processes_resource_summation(alloc)
    resources = alloc[0].length
    sums = [].clone
    i = 0.clone
    while i < resources
      total = 0.clone
      j = 0.clone
      while j < alloc.length
        total = _add(total, alloc[j][i]).clone
        j = _add(j, 1).clone
      end
      sums = (sums << (total)).clone
      i = _add(i, 1).clone
    end
    return sums
  end
  def available_resources(claim, alloc_sum)
    avail = [].clone
    i = 0.clone
    while i < claim.length
      avail = (avail << (claim[i] - alloc_sum[i])).clone
      i = _add(i, 1).clone
    end
    return avail
  end
  def need(max, alloc)
    needs = [].clone
    i = 0.clone
    while i < max.length
      row = [].clone
      j = 0.clone
      while j < max[0].length
        row = (row << (max[i][j] - alloc[i][j])).clone
        j = _add(j, 1).clone
      end
      needs = (needs << (row)).clone
      i = _add(i, 1).clone
    end
    return needs
  end
  def pretty_print(claim, alloc, max)
    puts("         Allocated Resource Table")
    i = 0.clone
    while i < alloc.length
      row = alloc[i].clone
      line = _add(_add("P", _str(_add(i, 1))), "       ").clone
      j = 0.clone
      while j < row.length
        line = _add(line, _str(row[j])).clone
        if j < row.length - 1
          line = _add(line, "        ").clone
        end
        j = _add(j, 1).clone
      end
      puts(line)
      puts("")
      i = _add(i, 1).clone
    end
    puts("         System Resource Table")
    i = 0.clone
    while i < max.length
      row = max[i].clone
      line = _add(_add("P", _str(_add(i, 1))), "       ").clone
      j = 0.clone
      while j < row.length
        line = _add(line, _str(row[j])).clone
        if j < row.length - 1
          line = _add(line, "        ").clone
        end
        j = _add(j, 1).clone
      end
      puts(line)
      puts("")
      i = _add(i, 1).clone
    end
    usage = "".clone
    i = 0.clone
    while i < claim.length
      if i > 0
        usage = _add(usage, " ").clone
      end
      usage = _add(usage, _str(claim[i])).clone
      i = _add(i, 1).clone
    end
    alloc_sum = processes_resource_summation(alloc).clone
    avail = available_resources(claim, alloc_sum).clone
    avail_str = "".clone
    i = 0.clone
    while i < avail.length
      if i > 0
        avail_str = _add(avail_str, " ").clone
      end
      avail_str = _add(avail_str, _str(avail[i])).clone
      i = _add(i, 1).clone
    end
    puts(_add("Current Usage by Active Processes: ", usage))
    puts(_add("Initial Available Resources:       ", avail_str))
  end
  def bankers_algorithm(claim, alloc, max)
    need_list = need(max, alloc).clone
    alloc_sum = processes_resource_summation(alloc).clone
    avail = available_resources(claim, alloc_sum).clone
    puts("__________________________________________________")
    puts("")
    finished = [].clone
    i = 0.clone
    while i < need_list.length
      finished = (finished << (false)).clone
      i = _add(i, 1).clone
    end
    remaining = need_list.length.clone
    while remaining > 0
      safe = false.clone
      p = 0.clone
      while p < need_list.length
        if !finished[p]
          exec = true.clone
          r = 0.clone
          while r < avail.length
            if need_list[p][r] > avail[r]
              exec = false.clone
              break
            end
            r = _add(r, 1).clone
          end
          if exec
            safe = true.clone
            puts(_add(_add("Process ", _str(_add(p, 1))), " is executing."))
            r = 0.clone
            while r < avail.length
              avail[r] = _add(avail[r], alloc[p][r])
              r = _add(r, 1).clone
            end
            avail_str = "".clone
            r = 0.clone
            while r < avail.length
              if r > 0
                avail_str = _add(avail_str, " ").clone
              end
              avail_str = _add(avail_str, _str(avail[r])).clone
              r = _add(r, 1).clone
            end
            puts(_add("Updated available resource stack for processes: ", avail_str))
            puts("The process is in a safe state.")
            puts("")
            finished[p] = true
            remaining = remaining - 1.clone
          end
        end
        p = _add(p, 1).clone
      end
      if !safe
        puts("System in unsafe state. Aborting...")
        puts("")
        break
      end
    end
  end
  State = Struct.new(:claim, :alloc, :max, keyword_init: true)
  $claim_vector = [8, 5, 9, 7].clone
  $allocated_resources_table = [[2, 0, 1, 1], [0, 1, 2, 1], [4, 0, 0, 3], [0, 2, 1, 0], [1, 0, 3, 0]].clone
  $maximum_claim_table = [[3, 2, 1, 4], [0, 2, 5, 2], [5, 1, 0, 5], [1, 5, 3, 0], [3, 0, 3, 3]].clone
  pretty_print($claim_vector, $allocated_resources_table, $maximum_claim_table)
  bankers_algorithm($claim_vector, $allocated_resources_table, $maximum_claim_table)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
