# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def make_list(len, value)
    arr = []
    i = 0
    while i < len
      arr = (arr << (value))
      i = _add(i, 1)
    end
    return arr
  end
  def min_int(a, b)
    if a < b
      return a
    end
    return b
  end
  def min_steps_to_one(number)
    if number <= 0
      return 0
    end
    table = make_list(_add(number, 1), _add(number, 1))
    table[1] = 0
    i = 1
    while i < number
      table[_add(i, 1)] = min_int(table[_add(i, 1)], _add(table[i], 1))
      if i * 2 <= number
        table[i * 2] = min_int(table[i * 2], _add(table[i], 1))
      end
      if i * 3 <= number
        table[i * 3] = min_int(table[i * 3], _add(table[i], 1))
      end
      i = _add(i, 1)
    end
    return table[number]
  end
  puts((min_steps_to_one(10)).to_s)
  puts((min_steps_to_one(15)).to_s)
  puts((min_steps_to_one(6)).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
