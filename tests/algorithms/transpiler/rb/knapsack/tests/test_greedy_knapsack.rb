# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def calc_profit(profit, weight, max_weight)
    if profit.length != weight.length
      return CalcResult.new(ok: false, value: 0.0, error: "The length of profit and weight must be same.")
    end
    if max_weight <= 0
      return CalcResult.new(ok: false, value: 0.0, error: "max_weight must greater than zero.")
    end
    i = 0.clone
    while i < profit.length
      if profit[i] < 0
        return CalcResult.new(ok: false, value: 0.0, error: "Profit can not be negative.")
      end
      if weight[i] < 0
        return CalcResult.new(ok: false, value: 0.0, error: "Weight can not be negative.")
      end
      i = _add(i, 1).clone
    end
    used = [].clone
    j = 0.clone
    while j < profit.length
      used = (used << (false)).clone
      j = _add(j, 1).clone
    end
    limit = 0.clone
    gain = 0.0.clone
    while limit < max_weight
      max_ratio = -1.0.clone
      idx = 0 - 1.clone
      k = 0.clone
      while k < profit.length
        if !used[k]
          ratio = ((profit[k]).to_f) / ((weight[k]).to_f)
          if ratio > max_ratio
            max_ratio = ratio.clone
            idx = k.clone
          end
        end
        k = _add(k, 1).clone
      end
      if idx == 0 - 1
        break
      end
      used[idx] = true
      if max_weight - limit >= weight[idx]
        limit = _add(limit, weight[idx]).clone
        gain = _add(gain, ((profit[idx]).to_f)).clone
      else
        gain = _add(gain, ((((max_weight - limit)).to_f) / ((weight[idx]).to_f)) * ((profit[idx]).to_f)).clone
        break
      end
    end
    return CalcResult.new(ok: true, value: gain, error: "")
  end
  def test_sorted()
    profit = [10, 20, 30, 40, 50, 60]
    weight = [2, 4, 6, 8, 10, 12]
    res = calc_profit(profit, weight, 100)
    return res.ok && res.value == 210.0
  end
  def test_negative_max_weight()
    profit = [10, 20, 30, 40, 50, 60]
    weight = [2, 4, 6, 8, 10, 12]
    res = calc_profit(profit, weight, -15)
    return !res.ok && res.error == "max_weight must greater than zero."
  end
  def test_negative_profit_value()
    profit = [10, -20, 30, 40, 50, 60]
    weight = [2, 4, 6, 8, 10, 12]
    res = calc_profit(profit, weight, 15)
    return !res.ok && res.error == "Profit can not be negative."
  end
  def test_negative_weight_value()
    profit = [10, 20, 30, 40, 50, 60]
    weight = [2, -4, 6, -8, 10, 12]
    res = calc_profit(profit, weight, 15)
    return !res.ok && res.error == "Weight can not be negative."
  end
  def test_null_max_weight()
    profit = [10, 20, 30, 40, 50, 60]
    weight = [2, 4, 6, 8, 10, 12]
    res = calc_profit(profit, weight, 0)
    return !res.ok && res.error == "max_weight must greater than zero."
  end
  def test_unequal_list_length()
    profit = [10, 20, 30, 40, 50]
    weight = [2, 4, 6, 8, 10, 12]
    res = calc_profit(profit, weight, 100)
    return !res.ok && res.error == "The length of profit and weight must be same."
  end
  CalcResult = Struct.new(:ok, :value, :error, keyword_init: true)
  puts((test_sorted() ? 'true' : 'false'))
  puts((test_negative_max_weight() ? 'true' : 'false'))
  puts((test_negative_profit_value() ? 'true' : 'false'))
  puts((test_negative_weight_value() ? 'true' : 'false'))
  puts((test_null_max_weight() ? 'true' : 'false'))
  puts((test_unequal_list_length() ? 'true' : 'false'))
  puts((true ? 'true' : 'false'))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
