# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def knapsack(weights, values, number_of_items, max_weight, index)
    if index == number_of_items
      return 0
    end
    ans1 = knapsack(weights, values, number_of_items, max_weight, _add(index, 1))
    ans2 = 0.clone
    if weights[index] <= max_weight
      ans2 = _add(values[index], knapsack(weights, values, number_of_items, max_weight - weights[index], _add(index, 1))).clone
    end
    if ans1 > ans2
      return ans1
    end
    return ans2
  end
  def main()
    w1 = [1, 2, 4, 5]
    v1 = [5, 4, 8, 6]
    r1 = knapsack(w1, v1, 4, 5, 0)
    puts(_str(r1))
    w2 = [3, 4, 5]
    v2 = [10, 9, 8]
    r2 = knapsack(w2, v2, 3, 25, 0)
    puts(_str(r2))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
