# Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:45 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def palindromic_string(input_string)
    max_length = 0.clone
    new_input_string = "".clone
    output_string = "".clone
    n = input_string.length
    i = 0.clone
    while i < n - 1
      new_input_string = _add(_add(new_input_string, input_string[i..._add(i, 1)]), "|").clone
      i = _add(i, 1).clone
    end
    new_input_string = _add(new_input_string, input_string[n - 1...n]).clone
    left = 0.clone
    right = 0.clone
    length = [].clone
    i = 0.clone
    m = new_input_string.length
    while i < m
      length = (length << (1)).clone
      i = _add(i, 1).clone
    end
    start = 0.clone
    j = 0.clone
    while j < m
      k = 1.clone
      if j <= right
        mirror = _add(left, right) - j
        k = length[mirror] / 2.clone
        diff = _add(right - j, 1)
        if diff < k
          k = diff.clone
        end
        if k < 1
          k = 1.clone
        end
      end
      while j - k >= 0 && _add(j, k) < m && new_input_string[_add(j, k)..._add(_add(j, k), 1)] == new_input_string[j - k..._add(j - k, 1)]
        k = _add(k, 1).clone
      end
      length[j] = 2 * k - 1
      if _add(j, k) - 1 > right
        left = _add(j - k, 1).clone
        right = _add(j, k) - 1.clone
      end
      if length[j] > max_length
        max_length = length[j].clone
        start = j.clone
      end
      j = _add(j, 1).clone
    end
    s = new_input_string[start - max_length / 2..._add(_add(start, max_length / 2), 1)]
    idx = 0.clone
    while idx < s.length
      ch = s[idx..._add(idx, 1)]
      if ch != "|"
        output_string = _add(output_string, ch).clone
      end
      idx = _add(idx, 1).clone
    end
    return output_string
  end
  def main()
    puts(palindromic_string("abbbaba"))
    puts(palindromic_string("ababa"))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
