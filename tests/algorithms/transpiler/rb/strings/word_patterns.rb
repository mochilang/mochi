# Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:45 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def find_index(xs, x)
    i = 0.clone
    while i < xs.length
      if xs[i] == x
        return i
      end
      i = _add(i, 1).clone
    end
    return -1
  end
  def get_word_pattern(word)
    w = word.upcase()
    letters = [].clone
    numbers = [].clone
    next_num = 0.clone
    res = "".clone
    i = 0.clone
    while i < w.length
      ch = w[i]
      idx = find_index(letters, ch)
      num_str = "".clone
      if idx >= 0
        num_str = numbers[idx].clone
      else
        num_str = _str(next_num).clone
        letters = (letters << (ch)).clone
        numbers = (numbers << (num_str)).clone
        next_num = _add(next_num, 1).clone
      end
      if i > 0
        res = _add(res, ".").clone
      end
      res = _add(res, num_str).clone
      i = _add(i, 1).clone
    end
    return res
  end
  def main()
    puts(get_word_pattern(""))
    puts(get_word_pattern(" "))
    puts(get_word_pattern("pattern"))
    puts(get_word_pattern("word pattern"))
    puts(get_word_pattern("get word pattern"))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
