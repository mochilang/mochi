# Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:45 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def string_to_chars(s)
    chars = [].clone
    i = 0.clone
    while i < s.length
      chars = (chars << (s[i..._add(i, 1)])).clone
      i = _add(i, 1).clone
    end
    return chars
  end
  def join_chars(chars)
    res = "".clone
    i = 0.clone
    while i < chars.length
      res = _add(res, chars[i]).clone
      i = _add(i, 1).clone
    end
    return res
  end
  def insert_at(chars, index, ch)
    res = [].clone
    i = 0.clone
    while i < index
      res = (res << (chars[i])).clone
      i = _add(i, 1).clone
    end
    res = (res << (ch)).clone
    while i < chars.length
      res = (res << (chars[i])).clone
      i = _add(i, 1).clone
    end
    return res
  end
  def remove_at(chars, index)
    res = [].clone
    i = 0.clone
    while i < chars.length
      if i != index
        res = (res << (chars[i])).clone
      end
      i = _add(i, 1).clone
    end
    return res
  end
  def make_matrix_int(rows, cols, init)
    matrix = [].clone
    (0...rows).each do |_|
      row = [].clone
      (0...cols).each do |unused2|
        row = (row << (init)).clone
      end
      matrix = (matrix << (row)).clone
    end
    return matrix
  end
  def make_matrix_string(rows, cols, init)
    matrix = [].clone
    (0...rows).each do |_|
      row = [].clone
      (0...cols).each do |unused2|
        row = (row << (init)).clone
      end
      matrix = (matrix << (row)).clone
    end
    return matrix
  end
  def compute_transform_tables(source_string, destination_string, copy_cost, replace_cost, delete_cost, insert_cost)
    source_seq = string_to_chars(source_string)
    dest_seq = string_to_chars(destination_string)
    m = source_seq.length
    n = dest_seq.length
    costs = make_matrix_int(_add(m, 1), _add(n, 1), 0).clone
    ops = make_matrix_string(_add(m, 1), _add(n, 1), "0").clone
    i = 1.clone
    while i <= m
      costs[i][0] = i * delete_cost
      ops[i][0] = _add("D", source_seq[i - 1])
      i = _add(i, 1).clone
    end
    j = 1.clone
    while j <= n
      costs[0][j] = j * insert_cost
      ops[0][j] = _add("I", dest_seq[j - 1])
      j = _add(j, 1).clone
    end
    i = 1.clone
    while i <= m
      j = 1.clone
      while j <= n
        if source_seq[i - 1] == dest_seq[j - 1]
          costs[i][j] = _add(costs[i - 1][j - 1], copy_cost)
          ops[i][j] = _add("C", source_seq[i - 1])
        else
          costs[i][j] = _add(costs[i - 1][j - 1], replace_cost)
          ops[i][j] = _add(_add("R", source_seq[i - 1]), dest_seq[j - 1])
        end
        if _add(costs[i - 1][j], delete_cost) < costs[i][j]
          costs[i][j] = _add(costs[i - 1][j], delete_cost)
          ops[i][j] = _add("D", source_seq[i - 1])
        end
        if _add(costs[i][j - 1], insert_cost) < costs[i][j]
          costs[i][j] = _add(costs[i][j - 1], insert_cost)
          ops[i][j] = _add("I", dest_seq[j - 1])
        end
        j = _add(j, 1).clone
      end
      i = _add(i, 1).clone
    end
    return TransformTables.new(costs: costs, ops: ops)
  end
  def assemble_transformation(ops, i, j)
    if i == 0 && j == 0
      return []
    end
    op = ops[i][j]
    kind = op[0...1]
    if kind == "C" || kind == "R"
      seq = assemble_transformation(ops, i - 1, j - 1).clone
      seq = (seq << (op)).clone
      return seq
    else
      if kind == "D"
        seq = assemble_transformation(ops, i - 1, j).clone
        seq = (seq << (op)).clone
        return seq
      else
        seq = assemble_transformation(ops, i, j - 1).clone
        seq = (seq << (op)).clone
        return seq
      end
    end
  end
  def main()
    copy_cost = -1
    replace_cost = 1
    delete_cost = 2
    insert_cost = 2
    src = "Python"
    dst = "Algorithms"
    tables = compute_transform_tables(src, dst, copy_cost, replace_cost, delete_cost, insert_cost)
    operations = tables.ops
    m = operations.length
    n = operations[0].length
    sequence = assemble_transformation(operations, m - 1, n - 1).clone
    string_list = string_to_chars(src).clone
    idx = 0.clone
    cost = 0.clone
    k = 0.clone
    while k < sequence.length
      puts(join_chars(string_list))
      op = sequence[k]
      kind = op[0...1]
      if kind == "C"
        cost = _add(cost, copy_cost).clone
      else
        if kind == "R"
          string_list[idx] = op[2...3]
          cost = _add(cost, replace_cost).clone
        else
          if kind == "D"
            string_list = remove_at(string_list, idx).clone
            cost = _add(cost, delete_cost).clone
          else
            string_list = insert_at(string_list, idx, op[1...2]).clone
            cost = _add(cost, insert_cost).clone
          end
        end
      end
      idx = _add(idx, 1).clone
      k = _add(k, 1).clone
    end
    puts(join_chars(string_list))
    puts(_add("Cost: ", _str(cost)))
  end
  TransformTables = Struct.new(:costs, :ops, keyword_init: true)
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
