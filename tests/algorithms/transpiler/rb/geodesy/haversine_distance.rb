# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def to_radians(deg)
    return deg * $PI / 180.0
  end
  def sin_taylor(x)
    term = x
    sum = x
    i = 1
    while i < 10
      k1 = 2.0 * ((i).to_f)
      k2 = _add(k1, 1.0)
      term = -term * x * x / (k1 * k2)
      sum = _add(sum, term)
      i = _add(i, 1)
    end
    return sum
  end
  def cos_taylor(x)
    term = 1.0
    sum = 1.0
    i = 1
    while i < 10
      k1 = 2.0 * ((i).to_f) - 1.0
      k2 = 2.0 * ((i).to_f)
      term = -term * x * x / (k1 * k2)
      sum = _add(sum, term)
      i = _add(i, 1)
    end
    return sum
  end
  def tan_approx(x)
    return sin_taylor(x) / cos_taylor(x)
  end
  def sqrtApprox(x)
    guess = x / 2.0
    i = 0
    while i < 20
      guess = (_add(guess, x / guess)) / 2.0
      i = _add(i, 1)
    end
    return guess
  end
  def atanApprox(x)
    if x > 1.0
      return $PI / 2.0 - x / (_add(x * x, 0.28))
    end
    if x < (-1.0)
      return -$PI / 2.0 - x / (_add(x * x, 0.28))
    end
    return x / (_add(1.0, 0.28 * x * x))
  end
  def atan2Approx(y, x)
    if x > 0.0
      val = atanApprox(y / x)
      return val
    end
    if x < 0.0
      if y >= 0.0
        return _add(atanApprox(y / x), $PI)
      end
      return atanApprox(y / x) - $PI
    end
    if y > 0.0
      return $PI / 2.0
    end
    if y < 0.0
      return -$PI / 2.0
    end
    return 0.0
  end
  def asinApprox(x)
    denom = sqrtApprox(1.0 - x * x)
    res = atan2Approx(x, denom)
    return res
  end
  def haversine_distance(lat1, lon1, lat2, lon2)
    flattening = ($AXIS_A - $AXIS_B) / $AXIS_A
    phi_1 = atanApprox((1.0 - flattening) * tan_approx(to_radians(lat1)))
    phi_2 = atanApprox((1.0 - flattening) * tan_approx(to_radians(lat2)))
    lambda_1 = to_radians(lon1)
    lambda_2 = to_radians(lon2)
    sin_sq_phi = sin_taylor((phi_2 - phi_1) / 2.0)
    sin_sq_lambda = sin_taylor((lambda_2 - lambda_1) / 2.0)
    sin_sq_phi = sin_sq_phi * sin_sq_phi
    sin_sq_lambda = sin_sq_lambda * sin_sq_lambda
    h_value = sqrtApprox(_add(sin_sq_phi, cos_taylor(phi_1) * cos_taylor(phi_2) * sin_sq_lambda))
    return 2.0 * $RADIUS * asinApprox(h_value)
  end
  $PI = 3.141592653589793
  $AXIS_A = 6378137.0
  $AXIS_B = 6.356752314245e+06
  $RADIUS = 6378137.0
  $SAN_FRANCISCO = [37.774856, -122.424227]
  $YOSEMITE = [37.864742, -119.537521]
  puts(_str(haversine_distance($SAN_FRANCISCO[0], $SAN_FRANCISCO[1], $YOSEMITE[0], $YOSEMITE[1])))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
