# Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:11 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def contains(words, target)
        __tmp1 = words
    if __tmp1.respond_to?(:keys) && !__tmp1.is_a?(String)
      __tmp1 = __tmp1.keys
    end
    __tmp1.each do |w|
      if w == target
        return true
      end
    end
    return false
  end
  def backtrack(s, word_dict, start)
    if start == s.length
      return true
    end
    end_ = _add(start, 1)
    while end_ <= s.length
      substr = s[start...end_]
      if word_dict.include?(substr) && backtrack(s, word_dict, end_)
        return true
      end
      end_ = _add(end_, 1)
    end
    return false
  end
  def word_break(s, word_dict)
    return backtrack(s, word_dict, 0)
  end
  puts((word_break("leetcode", ["leet", "code"])).to_s)
  puts((word_break("applepenapple", ["apple", "pen"])).to_s)
  puts((word_break("catsandog", ["cats", "dog", "sand", "and", "cat"])).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
