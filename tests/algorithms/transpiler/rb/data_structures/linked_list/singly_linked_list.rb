# Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:43 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def empty_list()
    return SinglyLinkedList.new(data: [])
  end
  def length(list)
    return list.data.length
  end
  def is_empty(list)
    return list.data.length == 0
  end
  def to_string(list)
    if list.data.length == 0
      return ""
    end
    s = (list.data[0]).to_s
    i = 1
    while i < list.data.length
      s = _add(_add(s, " -> "), (list.data[i]).to_s)
      i = _add(i, 1)
    end
    return s
  end
  def insert_nth(list, index, value)
    if index < 0 || index > list.data.length
      panic("index out of range")
    end
    res = []
    i = 0
    while i < index
      res = (res << list.data[i])
      i = _add(i, 1)
    end
    res = (res << value)
    while i < list.data.length
      res = (res << list.data[i])
      i = _add(i, 1)
    end
    return SinglyLinkedList.new(data: res)
  end
  def insert_head(list, value)
    return insert_nth(list, 0, value)
  end
  def insert_tail(list, value)
    return insert_nth(list, list.data.length, value)
  end
  def delete_nth(list, index)
    if index < 0 || index >= list.data.length
      panic("index out of range")
    end
    res = []
    val = 0
    i = 0
    while i < list.data.length
      if i == index
        val = list.data[i]
      else
        res = (res << list.data[i])
      end
      i = _add(i, 1)
    end
    return DeleteResult.new(list: SinglyLinkedList.new(data: res), value: val)
  end
  def delete_head(list)
    return delete_nth(list, 0)
  end
  def delete_tail(list)
    return delete_nth(list, list.data.length - 1)
  end
  def get_item(list, index)
    if index < 0 || index >= list.data.length
      panic("index out of range")
    end
    return list.data[index]
  end
  def set_item(list, index, value)
    if index < 0 || index >= list.data.length
      panic("index out of range")
    end
    res = []
    i = 0
    while i < list.data.length
      if i == index
        res = (res << value)
      else
        res = (res << list.data[i])
      end
      i = _add(i, 1)
    end
    return SinglyLinkedList.new(data: res)
  end
  def reverse_list(list)
    res = []
    i = list.data.length - 1
    while i >= 0
      res = (res << list.data[i])
      i = i - 1
    end
    return SinglyLinkedList.new(data: res)
  end
  def main()
    lst = empty_list()
    i = 1
    while i <= 5
      lst = insert_tail(lst, i)
      i = _add(i, 1)
    end
    puts(to_string(lst))
    lst = insert_head(lst, 0)
    puts(to_string(lst))
    del = delete_head(lst)
    lst = del.list
    puts((del.value).to_s)
    del = delete_tail(lst)
    lst = del.list
    puts((del.value).to_s)
    del = delete_nth(lst, 2)
    lst = del.list
    puts((del.value).to_s)
    lst = set_item(lst, 1, 99)
    puts((get_item(lst, 1)).to_s)
    lst = reverse_list(lst)
    puts(to_string(lst))
  end
  SinglyLinkedList = Struct.new(:data, keyword_init: true)
  DeleteResult = Struct.new(:list, :value, keyword_init: true)
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
