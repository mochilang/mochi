# Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:43 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def create_queue(capacity)
    arr = []
    i = 0
    while i < capacity
      arr = (arr << 0)
      i = _add(i, 1)
    end
    return CircularQueue.new(data: arr, front: 0, rear: 0, size: 0, capacity: capacity)
  end
  def length(q)
    return q.size
  end
  def is_empty(q)
    return q.size == 0
  end
  def front(q)
    if is_empty(q)
      return 0
    end
    return q.data[q.front]
  end
  def enqueue(q, value)
    if q.size >= q.capacity
      panic("QUEUE IS FULL")
    end
    arr = q.data
    arr[q.rear] = value
    q["data"] = arr
    q["rear"] = (_add(q.rear, 1)) % q.capacity
    q["size"] = _add(q.size, 1)
    return q
  end
  def dequeue(q)
    if q.size == 0
      panic("UNDERFLOW")
    end
    value = q.data[q.front]
    arr2 = q.data
    arr2[q.front] = 0
    q["data"] = arr2
    q["front"] = (_add(q.front, 1)) % q.capacity
    q["size"] = q.size - 1
    return DequeueResult.new(queue: q, value: value)
  end
  def main()
    q = create_queue(5)
    puts((is_empty(q) ? 'true' : 'false'))
    q = enqueue(q, 10)
    puts((is_empty(q) ? 'true' : 'false'))
    q = enqueue(q, 20)
    q = enqueue(q, 30)
    puts(front(q))
    r = dequeue(q)
    q = r.queue
    puts(r.value)
    puts(front(q))
    puts(length(q))
  end
  CircularQueue = Struct.new(:data, :front, :rear, :size, :capacity, keyword_init: true)
  DequeueResult = Struct.new(:queue, :value, keyword_init: true)
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
