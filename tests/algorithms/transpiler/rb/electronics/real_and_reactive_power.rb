# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def sqrt(x)
    if x <= 0.0
      return 0.0
    end
    guess = x
    i = 0
    while i < 10
      guess = (_add(guess, x / guess)) / 2.0
      i = _add(i, 1)
    end
    return guess
  end
  def real_power(apparent_power, power_factor)
    if power_factor < 0.0 - 1.0 || power_factor > 1.0
      panic("power_factor must be a valid float value between -1 and 1.")
    end
    return apparent_power * power_factor
  end
  def reactive_power(apparent_power, power_factor)
    if power_factor < 0.0 - 1.0 || power_factor > 1.0
      panic("power_factor must be a valid float value between -1 and 1.")
    end
    return apparent_power * sqrt(1.0 - power_factor * power_factor)
  end
  puts((real_power(100.0, 0.9)).to_s)
  puts((real_power(0.0, 0.8)).to_s)
  puts((real_power(100.0, -0.9)).to_s)
  puts((reactive_power(100.0, 0.9)).to_s)
  puts((reactive_power(0.0, 0.8)).to_s)
  puts((reactive_power(100.0, -0.9)).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
