# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def resistor_parallel(resistors)
    sum = 0.0
    i = 0
    while i < resistors.length
      r = resistors[i]
      if r <= 0.0
        panic(_add(_add("Resistor at index ", (i).to_s), " has a negative or zero value!"))
      end
      sum = _add(sum, 1.0 / r)
      i = _add(i, 1)
    end
    return 1.0 / sum
  end
  def resistor_series(resistors)
    sum = 0.0
    i = 0
    while i < resistors.length
      r = resistors[i]
      if r < 0.0
        panic(_add(_add("Resistor at index ", (i).to_s), " has a negative value!"))
      end
      sum = _add(sum, r)
      i = _add(i, 1)
    end
    return sum
  end
  def main()
    resistors = [3.21389, 2.0, 3.0]
    puts(_add("Parallel: ", (resistor_parallel(resistors)).to_s))
    puts(_add("Series: ", (resistor_series(resistors)).to_s))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
