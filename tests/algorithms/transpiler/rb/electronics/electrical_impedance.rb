# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def sqrtApprox(x)
    if x <= 0.0
      return 0.0
    end
    guess = x / 2.0
    i = 0
    while i < 20
      guess = (_add(guess, x / guess)) / 2.0
      i = _add(i, 1)
    end
    return guess
  end
  def electrical_impedance(resistance, reactance, impedance)
    zero_count = 0
    if resistance == 0.0
      zero_count = _add(zero_count, 1)
    end
    if reactance == 0.0
      zero_count = _add(zero_count, 1)
    end
    if impedance == 0.0
      zero_count = _add(zero_count, 1)
    end
    if zero_count != 1
      panic("One and only one argument must be 0")
    end
    if resistance == 0.0
      value = sqrtApprox(impedance * impedance - reactance * reactance)
      return {"resistance" => value}
    else
      if reactance == 0.0
        value = sqrtApprox(impedance * impedance - resistance * resistance)
        return {"reactance" => value}
      else
        if impedance == 0.0
          value = sqrtApprox(_add(resistance * resistance, reactance * reactance))
          return {"impedance" => value}
        else
          panic("Exactly one argument must be 0")
        end
      end
    end
  end
  puts(electrical_impedance(3.0, 4.0, 0.0))
  puts(electrical_impedance(0.0, 4.0, 5.0))
  puts(electrical_impedance(3.0, 0.0, 5.0))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
