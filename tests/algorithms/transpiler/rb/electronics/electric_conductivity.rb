# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def electric_conductivity(conductivity, electron_conc, mobility)
    zero_count = 0
    if conductivity == 0.0
      zero_count = _add(zero_count, 1)
    end
    if electron_conc == 0.0
      zero_count = _add(zero_count, 1)
    end
    if mobility == 0.0
      zero_count = _add(zero_count, 1)
    end
    if zero_count != 1
      panic("You cannot supply more or less than 2 values")
    end
    if conductivity < 0.0
      panic("Conductivity cannot be negative")
    end
    if electron_conc < 0.0
      panic("Electron concentration cannot be negative")
    end
    if mobility < 0.0
      panic("mobility cannot be negative")
    end
    if conductivity == 0.0
      return Result.new(kind: "conductivity", value: mobility * electron_conc * $ELECTRON_CHARGE)
    end
    if electron_conc == 0.0
      return Result.new(kind: "electron_conc", value: conductivity / (mobility * $ELECTRON_CHARGE))
    end
    return Result.new(kind: "mobility", value: conductivity / (electron_conc * $ELECTRON_CHARGE))
  end
  $ELECTRON_CHARGE = 1.6021e-19
  Result = Struct.new(:kind, :value, keyword_init: true)
  $r1 = electric_conductivity(25.0, 100.0, 0.0)
  $r2 = electric_conductivity(0.0, 1600.0, 200.0)
  $r3 = electric_conductivity(1000.0, 0.0, 1200.0)
  puts(_add(_add($r1.kind, " "), ($r1.value).to_s))
  puts(_add(_add($r2.kind, " "), ($r2.value).to_s))
  puts(_add(_add($r3.kind, " "), ($r3.value).to_s))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
