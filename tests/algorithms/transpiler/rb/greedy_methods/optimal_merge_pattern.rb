# Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:14 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def index_of_min(xs)
    min_idx = 0.clone
    i = 1.clone
    while i < xs.length
      if xs[i] < xs[min_idx]
        min_idx = i.clone
      end
      i = _add(i, 1).clone
    end
    return min_idx
  end
  def remove_at(xs, idx)
    res = [].clone
    i = 0.clone
    while i < xs.length
      if i != idx
        res = (res << (xs[i])).clone
      end
      i = _add(i, 1).clone
    end
    return res
  end
  def optimal_merge_pattern(files)
    arr = files.clone
    optimal_merge_cost = 0.clone
    while arr.length > 1
      temp = 0.clone
      k = 0.clone
      while k < 2
        min_idx = index_of_min(arr)
        temp = _add(temp, arr[min_idx]).clone
        arr = remove_at(arr, min_idx).clone
        k = _add(k, 1).clone
      end
      arr = (arr << (temp)).clone
      optimal_merge_cost = _add(optimal_merge_cost, temp).clone
    end
    return optimal_merge_cost
  end
  puts(optimal_merge_pattern([2, 3, 4]))
  puts(optimal_merge_pattern([5, 10, 20, 30, 30]))
  puts(optimal_merge_pattern([8, 8, 8, 8, 8]))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
