# Generated by Mochi transpiler v0.10.59 on 2025-08-06 23:55 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def dot(a, b)
    s = 0.clone
    i = 0.clone
    while i < a.length
      s = _add(s, a[i] * b[i]).clone
      i = _add(i, 1).clone
    end
    return s
  end
  def new_svc(lr, lambda, epochs)
    return SVC.new(weights: [], bias: 0, lr: lr, lambda: lambda, epochs: epochs)
  end
  def fit(model, xs, ys)
    n_features = xs[0].length
    w = [].clone
    i = 0.clone
    while i < n_features
      w = (w << (0)).clone
      i = _add(i, 1).clone
    end
    b = 0.clone
    epoch = 0.clone
    while epoch < model.epochs
      j = 0.clone
      while j < xs.length
        x = xs[j]
        y = (ys[j]).to_f
        prod = _add(dot(w, x), b)
        if y * prod < 1
          k = 0.clone
          while k < w.length
            w[k] = _add(w[k], model.lr * (y * x[k] - 2 * model.lambda * w[k]))
            k = _add(k, 1).clone
          end
          b = _add(b, model.lr * y).clone
        else
          k = 0.clone
          while k < w.length
            w[k] = w[k] - model.lr * (2 * model.lambda * w[k])
            k = _add(k, 1).clone
          end
        end
        j = _add(j, 1).clone
      end
      epoch = _add(epoch, 1).clone
    end
    return SVC.new(weights: w, bias: b, lr: model.lr, lambda: model.lambda, epochs: model.epochs)
  end
  def predict(model, x)
    s = _add(dot(model.weights, x), model.bias)
    if s >= 0
      return 1
    else
      return -1
    end
  end
  SVC = Struct.new(:weights, :bias, :lr, :lambda, :epochs, keyword_init: true)
  $xs = [[0, 1], [0, 2], [1, 1], [1, 2]]
  $ys = [1, 1, -1, -1]
  $base = new_svc(0.01, 0.01, 1000)
  $model = fit($base, $xs, $ys)
  puts(predict($model, [0, 1]))
  puts(predict($model, [1, 1]))
  puts(predict($model, [2, 2]))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
