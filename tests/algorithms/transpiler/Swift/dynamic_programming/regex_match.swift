// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:32:27 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func recursive_match(_ text: String, _ pattern: String) -> Bool {
        if (Int(((pattern).count)) == 0) {
            return (Int(((text).count)) == 0)
        }
        if (Int(((text).count)) == 0) {
            if ((Int(((pattern).count)) >= 2) && (String(describing: String(Array(String(describing: (pattern as! String)))[(Int(((pattern).count)) &- 1)..<Int(((pattern).count))])) == "*")) {
                return recursive_match((text as! String), String(describing: String(Array(String(describing: (pattern as! String)))[0..<(Int(((pattern).count)) &- 2)])))
            }
            return false
        }
        let last_text = String(describing: String(Array(String(describing: (text as! String)))[(Int(((text).count)) &- 1)..<Int(((text).count))]))
        let last_pattern = String(describing: String(Array(String(describing: (pattern as! String)))[(Int(((pattern).count)) &- 1)..<Int(((pattern).count))]))
        if ((last_text == last_pattern) || (last_pattern == ".")) {
            return recursive_match(String(describing: String(Array(String(describing: (text as! String)))[0..<(Int(((text).count)) &- 1)])), String(describing: String(Array(String(describing: (pattern as! String)))[0..<(Int(((pattern).count)) &- 1)])))
        }
        if (last_pattern == "*") {
            if recursive_match(String(describing: String(Array(String(describing: (text as! String)))[0..<(Int(((text).count)) &- 1)])), (pattern as! String)) {
                return true
            }
            return recursive_match((text as! String), String(describing: String(Array(String(describing: (pattern as! String)))[0..<(Int(((pattern).count)) &- 2)])))
        }
        return false
    }
    func dp_match(_ text: String, _ pattern: String) -> Bool {
        let m = Int(((text).count))
        let n = Int(((pattern).count))
        var dp: [[Bool?]] = ([] as! [[Bool?]])
        var i: Int = 0
        while (i <= m) {
            var row: [Bool?] = ([] as! [Bool?])
            var j: Int = 0
            while (j <= n) {
                row = (_append(row, false) as! [Bool?])
                j = Int((j &+ 1))
            }
            dp = (_append(dp, row) as! [[Bool?]])
            i = Int((i &+ 1))
        }
        dp[0][0] = true
        var j: Int = 1
        while (j <= n) {
            if ((String(describing: String(Array(String(describing: (pattern as! String)))[(j &- 1)..<j])) == "*") && (j >= 2)) {
                if (dp[0][(j &- 2)] as! Bool) {
                    dp[0][j] = true
                }
            }
            j = Int((j &+ 1))
        }
        i = 1
        while (i <= m) {
            j = 1
            while (j <= n) {
                let p_char = String(describing: String(Array(String(describing: (pattern as! String)))[(j &- 1)..<j]))
                let t_char = String(describing: String(Array(String(describing: (text as! String)))[(i &- 1)..<i]))
                if ((p_char == ".") || (p_char == t_char)) {
                    if (dp[(i &- 1)][(j &- 1)] as! Bool) {
                        dp[i][j] = true
                    }
                } else if (p_char == "*") {
                    if (j >= 2) {
                        if (dp[i][(j &- 2)] as! Bool) {
                            dp[i][j] = true
                        }
                        let prev_p = String(describing: String(Array(String(describing: (pattern as! String)))[(j &- 2)..<(j &- 1)]))
                        if ((prev_p == ".") || (prev_p == t_char)) {
                            if (dp[(i &- 1)][j] as! Bool) {
                                dp[i][j] = true
                            }
                        }
                    }
                } else {
                    dp[i][j] = false
                }
                
                j = Int((j &+ 1))
            }
            i = Int((i &+ 1))
        }
        return (dp[m][n] as! Bool)
    }
    func print_bool(_ b: Bool) {
        if b {
            print(_p(true))
        } else {
            print(_p(false))
        }
    }
    _ = print_bool(recursive_match("abc", "a.c"))
    _ = print_bool(recursive_match("abc", "af*.c"))
    _ = print_bool(recursive_match("abc", "a.c*"))
    _ = print_bool(recursive_match("abc", "a.c*d"))
    _ = print_bool(recursive_match("aa", ".*"))
    _ = print_bool(dp_match("abc", "a.c"))
    _ = print_bool(dp_match("abc", "af*.c"))
    _ = print_bool(dp_match("abc", "a.c*"))
    _ = print_bool(dp_match("abc", "a.c*d"))
    _ = print_bool(dp_match("aa", ".*"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
