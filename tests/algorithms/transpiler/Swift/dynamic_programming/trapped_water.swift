// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:32:37 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_list(_ len: Int, _ value: Int) -> [Int?] {
        var arr: [Int?] = ([] as! [Int?])
        var i: Int = 0
        while (i < len) {
            arr = (_append(arr, value) as! [Int?])
            i = Int((i &+ 1))
        }
        return arr
    }
    func trapped_rainwater(_ heights: [Int?]) -> Int {
        if (Int(((heights).count)) == 0) {
            return 0
        }
        var i: Int = 0
        while (i < Int(((heights).count))) {
            if ((heights[i] as? Int ?? 0) < 0) {
                _ = fatalError("No height can be negative")
            }
            i = Int((i &+ 1))
        }
        let length: Int = Int(((heights).count))
        var left_max: [Int?] = (make_list(length, 0) as! [Int?])
        left_max = _set(left_max, 0, (heights[0] as? Int ?? 0))
        i = 1
        while (i < length) {
            if ((heights[i] as? Int ?? 0) > (left_max[(i &- 1)] as? Int ?? 0)) {
                left_max = _set(left_max, i, (heights[i] as? Int ?? 0))
            } else {
                left_max = _set(left_max, i, (left_max[(i &- 1)] as? Int ?? 0))
            }
            i = Int((i &+ 1))
        }
        var right_max: [Int?] = (make_list(length, 0) as! [Int?])
        let last: Int = (length &- 1)
        right_max = _set(right_max, last, (heights[last] as? Int ?? 0))
        i = Int((last &- 1))
        while (i >= 0) {
            if ((heights[i] as? Int ?? 0) > (right_max[(i &+ 1)] as? Int ?? 0)) {
                right_max = _set(right_max, i, (heights[i] as? Int ?? 0))
            } else {
                right_max = _set(right_max, i, (right_max[(i &+ 1)] as? Int ?? 0))
            }
            i = Int((i &- 1))
        }
        var total: Int = 0
        i = 0
        while (i < length) {
            let left = (left_max[i] as? Int ?? 0)
            let right = (right_max[i] as? Int ?? 0)
            let smaller = Int(((left < right) ? left : right))
            total = Int((total &+ Int((smaller &- (heights[i] as? Int ?? 0)))))
            i = Int((i &+ 1))
        }
        return total
    }
    print(_p(String(describing: Int(trapped_rainwater(([0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1] as! [Int?]))))))
    print(_p(String(describing: Int(trapped_rainwater(([7, 1, 5, 3, 6, 4] as! [Int?]))))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
