// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:31:43 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func reverse(_ s: String) -> String {
        var result: String = ""
        var i: Int = (Int(((s).count)) &- 1)
        while (i >= 0) {
            result = ((result + String(Array(s)[i..<(i &+ 1)])) as! String)
            i = Int((i &- 1))
        }
        return result
    }
    func max_int(_ a: Int, _ b: Int) -> Int {
        if (a > b) {
            return a
        }
        return b
    }
    func longest_palindromic_subsequence(_ s: String) -> Int {
        let rev = String(describing: reverse((s as! String)))
        let n = Int(((s).count))
        let m = Int(((rev).count))
        var dp: [[Int?]] = ([] as! [[Int?]])
        var i: Int = 0
        while (i <= n) {
            var row: [Int?] = ([] as! [Int?])
            var j: Int = 0
            while (j <= m) {
                row = (_append(row, 0) as! [Int?])
                j = Int((j &+ 1))
            }
            dp = (_append(dp, row) as! [[Int?]])
            i = Int((i &+ 1))
        }
        i = 1
        while (i <= n) {
            var j: Int = 1
            while (j <= m) {
                let a_char = String(Array(s)[(i &- 1)..<i])
                let b_char = String(Array(rev)[(j &- 1)..<j])
                if (a_char == b_char) {
                    dp[i][j] = Int((1 &+ (dp[(i &- 1)][(j &- 1)] as? Int ?? 0)))
                } else {
                    dp[i][j] = Int(max_int((dp[(i &- 1)][j] as? Int ?? 0), (dp[i][(j &- 1)] as? Int ?? 0)))
                }
                j = Int((j &+ 1))
            }
            i = Int((i &+ 1))
        }
        return (dp[n][m] as? Int ?? 0)
    }
    print(_p(String(describing: Int(longest_palindromic_subsequence("bbbab")))))
    print(_p(String(describing: Int(longest_palindromic_subsequence("bbabcbcab")))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
