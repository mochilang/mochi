// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:31:36 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func longest_common_substring(_ text1: String, _ text2: String) -> String {
        if ((Int(((text1).count)) == 0) || (Int(((text2).count)) == 0)) {
            return ""
        }
        let m = Int(((text1).count))
        let n = Int(((text2).count))
        var dp: [[Int?]] = ([] as! [[Int?]])
        var i: Int = 0
        while (i < (m &+ 1)) {
            var row: [Int?] = ([] as! [Int?])
            var j: Int = 0
            while (j < (n &+ 1)) {
                row = (_append(row, 0) as! [Int?])
                j = Int((j &+ 1))
            }
            dp = (_append(dp, row) as! [[Int?]])
            i = Int((i &+ 1))
        }
        var end_pos: Int = 0
        var max_len: Int = 0
        var ii: Int = 1
        while (ii <= m) {
            var jj: Int = 1
            while (jj <= n) {
                if (String(describing: String(Array(String(describing: (text1 as! String)))[(ii &- 1)..<ii])) == String(describing: String(Array(String(describing: (text2 as! String)))[(jj &- 1)..<jj]))) {
                    dp[ii][jj] = Int((1 &+ (dp[(ii &- 1)][(jj &- 1)] as? Int ?? 0)))
                    if ((dp[ii][jj] as? Int ?? 0) > max_len) {
                        max_len = (dp[ii][jj] as? Int ?? 0)
                        end_pos = (ii as! Int)
                    }
                }
                jj = Int((jj &+ 1))
            }
            ii = Int((ii &+ 1))
        }
        return String(describing: String(Array(String(describing: (text1 as! String)))[(end_pos &- max_len)..<end_pos]))
    }
    print(_p(String(describing: longest_common_substring("abcdef", "xabded"))))
    print(_p("\n"))
    print(_p(String(describing: longest_common_substring("zxabcdezy", "yzabcdezx"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
