// Generated by Mochi transpiler v0.10.64 on 2025-08-12 16:34:49 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let G: Double = 0.000000000066743
    let C: Double = 299792458.0
    let PI: Double = 3.141592653589793
    func pow10(_ n: Int) -> Double {
        var result = 1.0
        var i = 0
        while (i < n) {
            result = Double((result * 10.0))
            i = _int((i &+ 1))
        }
        return result
    }
    func sqrt(_ x: Double) -> Double {
        if (x <= 0.0) {
            return 0.0
        }
        var guess = x
        var i = 0
        while (i < 20) {
            guess = Double((Double((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func abs(_ x: Double) -> Double {
        if (x < 0.0) {
            return -x
        }
        return x
    }
    func capture_radii(_ target_body_radius: Double, _ target_body_mass: Double, _ projectile_velocity: Double) -> Double {
        if (target_body_mass < 0.0) {
            _ = fatalError("Mass cannot be less than 0")
        }
        if (target_body_radius < 0.0) {
            _ = fatalError("Radius cannot be less than 0")
        }
        if (projectile_velocity > C) {
            _ = fatalError("Cannot go beyond speed of light")
        }
        let escape_velocity_squared = (Double(((2.0 * G) * target_body_mass)) / target_body_radius)
        let denom = (projectile_velocity * projectile_velocity)
        let capture_radius = (target_body_radius * Double(sqrt(Double((1.0 + (escape_velocity_squared / denom))))))
        return capture_radius
    }
    func capture_area(_ capture_radius: Double) -> Double {
        if (capture_radius < 0.0) {
            _ = fatalError("Cannot have a capture radius less than 0")
        }
        let sigma = ((PI * capture_radius) * capture_radius)
        return sigma
    }
    func run_tests() {
        let r = Double(capture_radii(Double((6.957 * Double(pow10(8)))), Double((1.99 * Double(pow10(30)))), 25000.0))
        if (Double(abs(Double((r - (1.720959069143714 * Double(pow10(10))))))) > 1.0) {
            _ = fatalError("capture_radii failed")
        }
        let a = Double(capture_area(Double(r)))
        if (Double(abs(Double((a - (9.304455331801812 * Double(pow10(20))))))) > 1.0) {
            _ = fatalError("capture_area failed")
        }
    }
    func main() {
        _ = run_tests()
        let r = Double(capture_radii(Double((6.957 * Double(pow10(8)))), Double((1.99 * Double(pow10(30)))), 25000.0))
        print(_p(_p(r)))
        print(_p(_p(Double(capture_area(Double(r))))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
