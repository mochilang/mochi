// Generated by Mochi transpiler v0.10.64 on 2025-08-12 10:13:57 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let PI: Double = 3.141592653589793
    let TWO_PI: Double = 6.283185307179586
    let g: Double = 9.80665
    func _mod(_ x: Double, _ m: Double) -> Double {
        return (x - (Double(_int((x / m))) * m))
    }
    func sin(_ x: Double) -> Double {
        let y = (Double(_mod(Double((x + PI)), Double(TWO_PI))) - PI)
        let y2 = (y * y)
        let y3 = (y2 * y)
        let y5 = (y3 * y2)
        let y7 = (y5 * y2)
        return (((y - (y3 / 6.0)) + (y5 / 120.0)) - (y7 / 5040.0))
    }
    func deg_to_rad(_ deg: Double) -> Double {
        return ((deg * PI) / 180.0)
    }
    func floor(_ x: Double) -> Double {
        var i = Int(x)
        if (Double(i) > x) {
            i = _int((i &- 1))
        }
        return Double(i)
    }
    func pow10(_ n: Int) -> Double {
        var result = 1.0
        var i = 0
        while (i < n) {
            result = Double((result * 10.0))
            i = _int((i &+ 1))
        }
        return result
    }
    func round(_ x: Double, _ n: Int) -> Double {
        let m = Double(pow10(n))
        let y = Double(floor(Double(((x * m) + 0.5))))
        return (y / m)
    }
    func check_args(_ init_velocity: Double, _ angle: Double) {
        if ((angle > 90.0) || (angle < 1.0)) {
            _ = fatalError("Invalid angle. Range is 1-90 degrees.")
        }
        if (init_velocity < 0.0) {
            _ = fatalError("Invalid velocity. Should be a positive number.")
        }
    }
    func horizontal_distance(_ init_velocity: Double, _ angle: Double) -> Double {
        _ = check_args(Double(init_velocity), Double(angle))
        let radians = Double(deg_to_rad(Double((2.0 * angle))))
        return Double(round(Double((Double(((init_velocity * init_velocity) * Double(sin(Double(radians))))) / g)), 2))
    }
    func max_height(_ init_velocity: Double, _ angle: Double) -> Double {
        _ = check_args(Double(init_velocity), Double(angle))
        let radians = Double(deg_to_rad(Double(angle)))
        let s = Double(sin(Double(radians)))
        return Double(round(Double((Double((((init_velocity * init_velocity) * s) * s)) / Double((2.0 * g)))), 2))
    }
    func total_time(_ init_velocity: Double, _ angle: Double) -> Double {
        _ = check_args(Double(init_velocity), Double(angle))
        let radians = Double(deg_to_rad(Double(angle)))
        return Double(round(Double((Double(((2.0 * init_velocity) * Double(sin(Double(radians))))) / g)), 2))
    }
    let v0 = 25.0
    let angle = 20.0
    print(_p(Double(horizontal_distance(Double(v0), Double(angle)))))
    print(_p(Double(max_height(Double(v0), Double(angle)))))
    print(_p(Double(total_time(Double(v0), Double(angle)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
