// Generated by Mochi transpiler v0.10.64 on 2025-08-12 10:14:05 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func focal_length_of_lens(_ object_distance_from_lens: Double, _ image_distance_from_lens: Double) -> Double {
        if ((object_distance_from_lens == 0.0) || (image_distance_from_lens == 0.0)) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / Double((Double((1.0 / image_distance_from_lens)) - Double((1.0 / object_distance_from_lens)))))
    }
    func object_distance(_ focal_length_of_lens: Double, _ image_distance_from_lens: Double) -> Double {
        if ((image_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / Double((Double((1.0 / image_distance_from_lens)) - Double((1.0 / focal_length_of_lens)))))
    }
    func image_distance(_ focal_length_of_lens: Double, _ object_distance_from_lens: Double) -> Double {
        if ((object_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / Double((Double((1.0 / object_distance_from_lens)) + Double((1.0 / focal_length_of_lens)))))
    }
    print(_p(_p(Double(focal_length_of_lens(10.0, 4.0)))))
    print(_p(_p(Double(focal_length_of_lens(2.7, 5.8)))))
    print(_p(_p(Double(object_distance(10.0, 40.0)))))
    print(_p(_p(Double(object_distance(6.2, 1.5)))))
    print(_p(_p(Double(image_distance(50.0, 40.0)))))
    print(_p(_p(Double(image_distance(5.3, 7.9)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
