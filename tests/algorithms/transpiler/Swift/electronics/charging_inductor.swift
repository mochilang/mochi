// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:32:20 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func expApprox(_ x: Double) -> Double {
        if (x < 0.0) {
            return (1.0 / Double(expApprox(Double(-x))))
        }
        if (x > 1.0) {
            let half = Double(expApprox(Double((x / 2.0))))
            return (half * half)
        }
        var sum: Double = 1.0
        var term: Double = 1.0
        var n: Int = 1
        while (n < 20) {
            term = Double(((term * x) / Double(n)))
            sum = Double((sum + term))
            n = Int((n &+ 1))
        }
        return sum
    }
    func floor(_ x: Double) -> Double {
        var i: Int = Int(x)
        if (Double(i) > x) {
            i = Int((i &- 1))
        }
        return Double(i)
    }
    func pow10(_ n: Int) -> Double {
        var result: Double = 1.0
        var i: Int = 0
        while (i < n) {
            result = Double((result * 10.0))
            i = Int((i &+ 1))
        }
        return result
    }
    func round(_ x: Double, _ n: Int) -> Double {
        let m = Double(pow10(n))
        return (Double(floor(Double(((x * m) + 0.5)))) / m)
    }
    func charging_inductor(_ source_voltage: Double, _ resistance: Double, _ inductance: Double, _ time: Double) -> Double {
        if (source_voltage <= 0.0) {
            _ = fatalError("Source voltage must be positive.")
        }
        if (resistance <= 0.0) {
            _ = fatalError("Resistance must be positive.")
        }
        if (inductance <= 0.0) {
            _ = fatalError("Inductance must be positive.")
        }
        let exponent = (Double((-time * resistance)) / inductance)
        let current = ((source_voltage / resistance) * Double((1.0 - Double(expApprox(Double(exponent))))))
        return Double(round(Double(current), 3))
    }
    print(_p(Double(charging_inductor(5.8, 1.5, 2.3, 2.0))))
    print(_p(Double(charging_inductor(8.0, 5.0, 3.0, 2.0))))
    print(_p(Double(charging_inductor(8.0, Double((5.0 * Double(pow10(2)))), 3.0, 2.0))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
