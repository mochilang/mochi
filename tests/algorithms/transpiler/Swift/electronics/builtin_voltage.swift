// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:30:53 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func pow10(_ n: Int) -> Double {
        var result: Double = 1.0
        var i: Int = 0
        while (i < n) {
            result = Double((result * 10.0))
            i = Int((i &+ 1))
        }
        return result
    }
    let BOLTZMANN: Double = (1.380649 / Double(pow10(23)))
    let ELECTRON_VOLT: Double = (1.602176634 / Double(pow10(19)))
    let TEMPERATURE: Double = 300.0
    func ln_series(_ x: Double) -> Double {
        let t = (Double((x - 1.0)) / Double((x + 1.0)))
        var term: Double = t
        var sum: Double = 0.0
        var n: Int = 1
        while (n <= 19) {
            sum = Double((sum + (term / Double(n))))
            term = Double(((term * t) * t))
            n = Int((n &+ 2))
        }
        return (2.0 * sum)
    }
    func ln(_ x: Double) -> Double {
        var y: Double = x
        var k: Int = 0
        while (y >= 10.0) {
            y = Double((y / 10.0))
            k = Int((k &+ 1))
        }
        while (y < 1.0) {
            y = Double((y * 10.0))
            k = Int((k &- 1))
        }
        return (Double(ln_series(Double(y))) + (Double(k) * Double(ln_series(10.0))))
    }
    func builtin_voltage(_ donor_conc: Double, _ acceptor_conc: Double, _ intrinsic_conc: Double) -> Double {
        if (donor_conc <= 0.0) {
            _ = fatalError("Donor concentration should be positive")
        }
        if (acceptor_conc <= 0.0) {
            _ = fatalError("Acceptor concentration should be positive")
        }
        if (intrinsic_conc <= 0.0) {
            _ = fatalError("Intrinsic concentration should be positive")
        }
        if (donor_conc <= intrinsic_conc) {
            _ = fatalError("Donor concentration should be greater than intrinsic concentration")
        }
        if (acceptor_conc <= intrinsic_conc) {
            _ = fatalError("Acceptor concentration should be greater than intrinsic concentration")
        }
        return (((BOLTZMANN * TEMPERATURE) * Double(ln(Double((Double((donor_conc * acceptor_conc)) / Double((intrinsic_conc * intrinsic_conc))))))) / ELECTRON_VOLT)
    }
    print(_p(String(describing: Double(builtin_voltage(Double(pow10(17)), Double(pow10(17)), Double(pow10(10)))))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
