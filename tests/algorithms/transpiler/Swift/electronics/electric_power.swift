// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:32:44 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
struct Result: Codable {
    var name: String
    var value: Double
    init() {
        self.name = ""
        self.value = 0
    }
    init(name: String, value: Double) {
        self.name = name
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func absf(_ x: Double) -> Double {
        if (x < 0.0) {
            return -x
        }
        return x
    }
    func pow10(_ n: Int) -> Double {
        var p: Double = 1.0
        var i: Int = 0
        while (i < n) {
            p = Double((p * 10.0))
            i = Int((i &+ 1))
        }
        return p
    }
    func round_to(_ x: Double, _ n: Int) -> Double {
        let m = Double(pow10(n))
        return (Double(floor(((x * m) + 0.5))) / m)
    }
    func electric_power(_ voltage: Double, _ current: Double, _ power: Double) -> Result {
        var zeros: Int = 0
        if (voltage == 0.0) {
            zeros = Int((zeros &+ 1))
        }
        if (current == 0.0) {
            zeros = Int((zeros &+ 1))
        }
        if (power == 0.0) {
            zeros = Int((zeros &+ 1))
        }
        if (zeros != 1) {
            _ = fatalError("Exactly one argument must be 0")
        } else if (power < 0.0) {
            _ = fatalError("Power cannot be negative in any electrical/electronics system")
        } else if (voltage == 0.0) {
            return Result(name: "voltage", value: (power / current))
        } else if (current == 0.0) {
            return Result(name: "current", value: (power / voltage))
        } else if (power == 0.0) {
            let p = Double(absf(Double((voltage * current))))
            return Result(name: "power", value: Double(round_to(Double(p), 2)))
        } else {
            _ = fatalError("Unhandled case")
        }
        
        
        
        
    }
    func str_result(_ r: Result) -> String {
        return (((("Result(name='" + r.name) + "', value=") + String(describing: r.value)) + ")")
    }
    let r1 = electric_power(0.0, 2.0, 5.0)
    print(_p(String(describing: str_result((r1 as! Result)))))
    let r2 = electric_power(2.0, 2.0, 0.0)
    print(_p(String(describing: str_result((r2 as! Result)))))
    let r3 = electric_power(Double(-2.0), 3.0, 0.0)
    print(_p(String(describing: str_result((r3 as! Result)))))
    let r4 = electric_power(2.2, 2.2, 0.0)
    print(_p(String(describing: str_result((r4 as! Result)))))
    let r5 = electric_power(2.0, 0.0, 6.0)
    print(_p(String(describing: str_result((r5 as! Result)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
