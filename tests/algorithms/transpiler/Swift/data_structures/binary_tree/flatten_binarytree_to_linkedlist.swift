// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:38:31 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    var node_data: [Int] = ([0] as! [Int])
    var left_child: [Int] = ([0] as! [Int])
    var right_child: [Int] = ([0] as! [Int])
    func new_node(_ value: Int) -> Int {
        node_data = (_append(node_data, value) as! [Int])
        left_child = (_append(left_child, 0) as! [Int])
        right_child = (_append(right_child, 0) as! [Int])
        return (Int(((node_data).count)) &- 1)
    }
    func build_tree() -> Int {
        let root = Int(new_node(1))
        let n2 = Int(new_node(2))
        let n5 = Int(new_node(5))
        let n3 = Int(new_node(3))
        let n4 = Int(new_node(4))
        let n6 = Int(new_node(6))
        left_child = _set(left_child, root, Int(n2))
        right_child = _set(right_child, root, Int(n5))
        left_child = _set(left_child, n2, Int(n3))
        right_child = _set(right_child, n2, Int(n4))
        right_child = _set(right_child, n5, Int(n6))
        return root
    }
    func flatten(_ root: Int) -> [Int] {
        if (root == 0) {
            return ([] as! [Int])
        }
        var res: [Int] = ([(node_data[root] as? Int ?? 0)] as! [Int])
        let left_vals: [Int] = (flatten((left_child[root] as? Int ?? 0)) as! [Int])
        let right_vals: [Int] = (flatten((right_child[root] as? Int ?? 0)) as! [Int])
        var i: Int = 0
        while (i < Int(((left_vals).count))) {
            res = (_append(res, (left_vals[i] as? Int ?? 0)) as! [Int])
            i = Int((i &+ 1))
        }
        i = 0
        while (i < Int(((right_vals).count))) {
            res = (_append(res, (right_vals[i] as? Int ?? 0)) as! [Int])
            i = Int((i &+ 1))
        }
        return res
    }
    func display(_ values: [Int]) -> Void {
        var s: String = ""
        var i: Int = 0
        while (i < Int(((values).count))) {
            if (i == 0) {
                s = String(describing: (values[i] as? Int ?? 0))
            } else {
                s = (((s + " ") + String(describing: (values[i] as? Int ?? 0))) as! String)
            }
            i = Int((i &+ 1))
        }
        print(_p(s))
    }
    print(_p("Flattened Linked List:"))
    let root = Int(build_tree())
    let vals: [Int] = (flatten(root) as! [Int])
    _ = display((vals as! [Int]))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
