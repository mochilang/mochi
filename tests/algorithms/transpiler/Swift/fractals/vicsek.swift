// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:55:16 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func repeat_char(_ c: String, _ count: Int) -> String {
        var s: String = ""
        var i: Int = 0
        while (i < count) {
            s = ((s + c) as! String)
            i = Int((i &+ 1))
        }
        return s
    }
    func vicsek(_ order: Int) -> [String] {
        if (order == 0) {
            return (["#"] as! [String])
        }
        let prev: [String] = (vicsek((order &- 1)) as! [String])
        let size = Int(((prev).count))
        let blank = String(describing: repeat_char(" ", size))
        var result: [String] = ([] as! [String])
        var i: Int = 0
        while (i < size) {
            result = (_append(result, ((blank + (prev[i] as! String)) + blank)) as! [String])
            i = Int((i &+ 1))
        }
        i = 0
        while (i < size) {
            result = (_append(result, (((prev[i] as! String) + (prev[i] as! String)) + (prev[i] as! String))) as! [String])
            i = Int((i &+ 1))
        }
        i = 0
        while (i < size) {
            result = (_append(result, ((blank + (prev[i] as! String)) + blank)) as! [String])
            i = Int((i &+ 1))
        }
        return result
    }
    func print_pattern(_ pattern: [String]) -> Void {
        var i: Int = 0
        while (i < Int(((pattern).count))) {
            print(_p((pattern[i] as! String)))
            i = Int((i &+ 1))
        }
    }
    func main() -> Void {
        let depth = 3
        let pattern: [String] = (vicsek(depth) as! [String])
        _ = print_pattern((pattern as! [String]))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
