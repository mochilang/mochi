// Generated by Mochi transpiler v0.10.61 on 2025-08-08 15:53:08 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let MOD = 4294967296
    let ASCII = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~"
    func ord(_ ch: String) -> Int {
        var i: Int = 0
        while (i < _int(((ASCII).count))) {
            if (String(Array(ASCII)[i..<(i &+ 1)]) == ch) {
                return (32 &+ i)
            }
            i = _int((i &+ 1))
        }
        return 0
    }
    func to_little_endian(_ s: String) -> String {
        if (_int(((s).count)) != 32) {
            _ = fatalError("Input must be of length 32")
        }
        return (((String(Array(s)[24..<32]) + String(Array(s)[16..<24])) + String(Array(s)[8..<16])) + String(Array(s)[0..<8]))
    }
    func int_to_bits(_ n: Int, _ width: Int) -> String {
        var bits: String = ""
        var num: Int = n
        while (num > 0) {
            bits = ((_p(((num % 2 + 2) % 2)) + bits) as! String)
            num = _int((num / 2))
        }
        while (_int(((bits).count)) < width) {
            bits = (("0" + bits) as! String)
        }
        if (_int(((bits).count)) > width) {
            bits = (String(Array(bits)[(_int(((bits).count)) &- width)..<_int(((bits).count))]) as! String)
        }
        return bits
    }
    func bits_to_int(_ bits: String) -> Int {
        var num: Int = 0
        var i: Int = 0
        while (i < _int(((bits).count))) {
            if (String(Array(bits)[i..<(i &+ 1)]) == "1") {
                num = _int(((num &* 2) &+ 1))
            } else {
                num = _int((num &* 2))
            }
            i = _int((i &+ 1))
        }
        return num
    }
    func to_hex(_ n: Int) -> String {
        let digits = "0123456789abcdef"
        if (n == 0) {
            return "0"
        }
        var num: Int = n
        var s: String = ""
        while (num > 0) {
            let d = ((num % 16 + 16) % 16)
            s = ((String(Array(digits)[d..<(d &+ 1)]) + s) as! String)
            num = _int((num / 16))
        }
        return s
    }
    func reformat_hex(_ i: Int) -> String {
        if (i < 0) {
            _ = fatalError("Input must be non-negative")
        }
        var hex: String = String(describing: to_hex(i))
        while (_int(((hex).count)) < 8) {
            hex = (("0" + hex) as! String)
        }
        if (_int(((hex).count)) > 8) {
            hex = (String(Array(hex)[(_int(((hex).count)) &- 8)..<_int(((hex).count))]) as! String)
        }
        var le: String = ""
        var j: Int = (_int(((hex).count)) &- 2)
        while (j >= 0) {
            le = ((le + String(Array(hex)[j..<(j &+ 2)])) as! String)
            j = _int((j &- 2))
        }
        return le
    }
    func preprocess(_ message: String) -> String {
        var bit_string: String = ""
        var i: Int = 0
        while (i < _int(((message).count))) {
            let ch = String(Array(message)[i..<(i &+ 1)])
            bit_string = ((bit_string + String(describing: int_to_bits(_int(ord((ch as! String))), 8))) as! String)
            i = _int((i &+ 1))
        }
        let start_len = String(describing: int_to_bits(_int(((bit_string).count)), 64))
        bit_string = ((bit_string + "1") as! String)
        while (((_int(((bit_string).count)) % 512 + 512) % 512) != 448) {
            bit_string = ((bit_string + "0") as! String)
        }
        bit_string = (((bit_string + String(describing: to_little_endian((String(Array(start_len)[32..<64]) as! String)))) + String(describing: to_little_endian((String(Array(start_len)[0..<32]) as! String)))) as! String)
        return bit_string
    }
    func get_block_words(_ bit_string: String) -> [[Int]] {
        if (((_int(((bit_string).count)) % 512 + 512) % 512) != 0) {
            _ = fatalError("Input must have length that's a multiple of 512")
        }
        var blocks: [[Int]] = ([] as! [[Int]])
        var pos: Int = 0
        while (pos < _int(((bit_string).count))) {
            var block: [Int] = ([] as! [Int])
            var i: Int = 0
            while (i < 512) {
                let part = String(Array(bit_string)[(pos &+ i)..<((pos &+ i) &+ 32)])
                let word = _int(bits_to_int(String(describing: to_little_endian((part as! String)))))
                block = (_append(block, word) as! [Int])
                i = _int((i &+ 32))
            }
            blocks = (_append(blocks, block) as! [[Int]])
            pos = _int((pos &+ 512))
        }
        return blocks
    }
    func bit_and(_ a: Int, _ b: Int) -> Int {
        var x: Int = a
        var y: Int = b
        var res: Int = 0
        var bit: Int = 1
        var i: Int = 0
        while (i < 32) {
            if ((((x % 2 + 2) % 2) == 1) && (((y % 2 + 2) % 2) == 1)) {
                res = _int((res &+ bit))
            }
            x = _int((x / 2))
            y = _int((y / 2))
            bit = _int((bit &* 2))
            i = _int((i &+ 1))
        }
        return res
    }
    func bit_or(_ a: Int, _ b: Int) -> Int {
        var x: Int = a
        var y: Int = b
        var res: Int = 0
        var bit: Int = 1
        var i: Int = 0
        while (i < 32) {
            let abit = ((x % 2 + 2) % 2)
            let bbit = ((y % 2 + 2) % 2)
            if ((abit == 1) || (bbit == 1)) {
                res = _int((res &+ bit))
            }
            x = _int((x / 2))
            y = _int((y / 2))
            bit = _int((bit &* 2))
            i = _int((i &+ 1))
        }
        return res
    }
    func bit_xor(_ a: Int, _ b: Int) -> Int {
        var x: Int = a
        var y: Int = b
        var res: Int = 0
        var bit: Int = 1
        var i: Int = 0
        while (i < 32) {
            let abit = ((x % 2 + 2) % 2)
            let bbit = ((y % 2 + 2) % 2)
            if (((_int((abit &+ bbit)) % 2 + 2) % 2) == 1) {
                res = _int((res &+ bit))
            }
            x = _int((x / 2))
            y = _int((y / 2))
            bit = _int((bit &* 2))
            i = _int((i &+ 1))
        }
        return res
    }
    func not_32(_ i: Int) -> Int {
        if (i < 0) {
            _ = fatalError("Input must be non-negative")
        }
        return (4294967295 &- i)
    }
    func sum_32(_ a: Int, _ b: Int) -> Int {
        return ((_int((a &+ b)) % MOD + MOD) % MOD)
    }
    func lshift(_ num: Int, _ k: Int) -> Int {
        var result: Int = ((num % MOD + MOD) % MOD)
        var i: Int = 0
        while (i < k) {
            result = _int(((_int((result &* 2)) % MOD + MOD) % MOD))
            i = _int((i &+ 1))
        }
        return result
    }
    func rshift(_ num: Int, _ k: Int) -> Int {
        var result: Int = num
        var i: Int = 0
        while (i < k) {
            result = _int((result / 2))
            i = _int((i &+ 1))
        }
        return result
    }
    func left_rotate_32(_ i: Int, _ shift: Int) -> Int {
        if (i < 0) {
            _ = fatalError("Input must be non-negative")
        }
        if (shift < 0) {
            _ = fatalError("Shift must be non-negative")
        }
        let left = _int(lshift(i, shift))
        let right = _int(rshift(i, (32 &- shift)))
        return ((_int((left &+ right)) % MOD + MOD) % MOD)
    }
    func md5_me(_ message: String) -> String {
        let bit_string = String(describing: preprocess((message as! String)))
        let added_consts: [Int] = ([3614090360, 3905402710, 606105819, 3250441966, 4118548399, 1200080426, 2821735955, 4249261313, 1770035416, 2336552879, 4294925233, 2304563134, 1804603682, 4254626195, 2792965006, 1236535329, 4129170786, 3225465664, 643717713, 3921069994, 3593408605, 38016083, 3634488961, 3889429448, 568446438, 3275163606, 4107603335, 1163531501, 2850285829, 4243563512, 1735328473, 2368359562, 4294588738, 2272392833, 1839030562, 4259657740, 2763975236, 1272893353, 4139469664, 3200236656, 681279174, 3936430074, 3572445317, 76029189, 3654602809, 3873151461, 530742520, 3299628645, 4096336452, 1126891415, 2878612391, 4237533241, 1700485571, 2399980690, 4293915773, 2240044497, 1873313359, 4264355552, 2734768916, 1309151649, 4149444226, 3174756917, 718787259, 3951481745] as! [Int])
        let shift_amounts: [Int] = ([7, 12, 17, 22, 7, 12, 17, 22, 7, 12, 17, 22, 7, 12, 17, 22, 5, 9, 14, 20, 5, 9, 14, 20, 5, 9, 14, 20, 5, 9, 14, 20, 4, 11, 16, 23, 4, 11, 16, 23, 4, 11, 16, 23, 4, 11, 16, 23, 6, 10, 15, 21, 6, 10, 15, 21, 6, 10, 15, 21, 6, 10, 15, 21] as! [Int])
        var a0: Int = 1732584193
        var b0: Int = 4023233417
        var c0: Int = 2562383102
        var d0: Int = 271733878
        let blocks: [[Int]] = (get_block_words((bit_string as! String)) as! [[Int]])
        var bi: Int = 0
        while (bi < _int(((blocks).count))) {
            let block: [Int] = (blocks[bi] as! [Int])
            var a: Int = a0
            var b: Int = b0
            var c: Int = c0
            var d: Int = d0
            var i: Int = 0
            while (i < 64) {
                var f: Int = 0
                var g: Int = 0
                if (i <= 15) {
                    f = _int(bit_xor(d, _int(bit_and(b, _int(bit_xor(c, d))))))
                    g = Int(i)
                } else if (i <= 31) {
                    f = _int(bit_xor(c, _int(bit_and(d, _int(bit_xor(b, c))))))
                    g = _int(((_int(((5 &* i) &+ 1)) % 16 + 16) % 16))
                } else if (i <= 47) {
                    f = _int(bit_xor(_int(bit_xor(b, c)), d))
                    g = _int(((_int(((3 &* i) &+ 5)) % 16 + 16) % 16))
                } else {
                    f = _int(bit_xor(c, _int(bit_or(b, _int(not_32(d))))))
                    g = _int(((_int((7 &* i)) % 16 + 16) % 16))
                }
                
                
                f = _int(sum_32(f, a))
                f = _int(sum_32(f, (added_consts[i] as? Int ?? 0)))
                f = _int(sum_32(f, (block[g] as? Int ?? 0)))
                let rotated = _int(left_rotate_32(f, (shift_amounts[i] as? Int ?? 0)))
                let new_b = _int(sum_32(b, rotated))
                a = Int(d)
                d = Int(c)
                c = Int(b)
                b = Int(new_b)
                i = _int((i &+ 1))
            }
            a0 = _int(sum_32(a0, a))
            b0 = _int(sum_32(b0, b))
            c0 = _int(sum_32(c0, c))
            d0 = _int(sum_32(d0, d))
            bi = _int((bi &+ 1))
        }
        let digest = (((String(describing: reformat_hex(a0)) + String(describing: reformat_hex(b0))) + String(describing: reformat_hex(c0))) + String(describing: reformat_hex(d0)))
        return digest
    }
    do {
        assert((String(describing: md5_me("")) == "d41d8cd98f00b204e9800998ecf8427e"))
    }
    do {
        assert((String(describing: md5_me("The quick brown fox jumps over the lazy dog")) == "9e107d9d372bb6826bd81d3542a419d6"))
    }
    do {
        assert((String(describing: md5_me("The quick brown fox jumps over the lazy dog.")) == "e4d909c290d0fb1ca068ffaddf22cbd0"))
    }
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
