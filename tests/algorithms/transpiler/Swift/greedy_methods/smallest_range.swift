// Generated by Mochi transpiler v0.10.61 on 2025-08-08 15:52:50 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct HeapItem: Codable {
    var value: Int
    var list_idx: Int
    var elem_idx: Int
    init() {
        self.value = 0
        self.list_idx = 0
        self.elem_idx = 0
    }
    init(value: Int, list_idx: Int, elem_idx: Int) {
        self.value = value
        self.list_idx = list_idx
        self.elem_idx = elem_idx
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let INF = 1000000000
    func smallest_range(_ nums: [[Int]]) -> [Int] {
        var heap: [HeapItem] = ([] as! [HeapItem])
        var current_max: Int = -INF
        var i: Int = 0
        while (i < _int(((nums).count))) {
            let first_val = (nums[i][0] as? Int ?? 0)
            heap = (_append(heap, HeapItem(value: first_val, list_idx: i, elem_idx: 0)) as! [HeapItem])
            if (first_val > current_max) {
                current_max = Int(first_val)
            }
            i = _int((i &+ 1))
        }
        var best: [Int] = ([-INF, INF] as! [Int])
        while (_int(((heap).count)) > 0) {
            var min_idx: Int = 0
            var j: Int = 1
            while (j < _int(((heap).count))) {
                let hj = (heap[j] as! HeapItem)
                let hmin = (heap[min_idx] as! HeapItem)
                if (hj.value < hmin.value) {
                    min_idx = Int(j)
                }
                j = _int((j &+ 1))
            }
            let item = (heap[min_idx] as! HeapItem)
            var new_heap: [HeapItem] = ([] as! [HeapItem])
            var k: Int = 0
            while (k < _int(((heap).count))) {
                if (k != min_idx) {
                    new_heap = (_append(new_heap, (heap[k] as! HeapItem)) as! [HeapItem])
                }
                k = _int((k &+ 1))
            }
            heap = (new_heap as! [HeapItem])
            let current_min = item.value
            if ((current_max &- current_min) < ((best[1] as? Int ?? 0) &- (best[0] as? Int ?? 0))) {
                best = ([current_min, current_max] as! [Int])
            }
            if (item.elem_idx == (_int((((nums[item.list_idx] as! [Int])).count)) &- 1)) {
                break
            }
            let next_val = (nums[item.list_idx][(item.elem_idx &+ 1)] as? Int ?? 0)
            heap = (_append(heap, HeapItem(value: next_val, list_idx: item.list_idx, elem_idx: (item.elem_idx &+ 1))) as! [HeapItem])
            if (next_val > current_max) {
                current_max = Int(next_val)
            }
        }
        return best
    }
    func list_to_string(_ arr: [Int]) -> String {
        var s: String = "["
        var i: Int = 0
        while (i < _int(((arr).count))) {
            s = ((s + _p((arr[i] as? Int ?? 0))) as! String)
            if (i < (_int(((arr).count)) &- 1)) {
                s = ((s + ", ") as! String)
            }
            i = _int((i &+ 1))
        }
        return (s + "]")
    }
    func main() {
        let result1: [Int] = (smallest_range(([([4, 10, 15, 24, 26] as! [Int]), ([0, 9, 12, 20] as! [Int]), ([5, 18, 22, 30] as! [Int])] as! [[Int]])) as! [Int])
        print(_p(String(describing: list_to_string((result1 as! [Int])))))
        let result2: [Int] = (smallest_range(([([1, 2, 3] as! [Int]), ([1, 2, 3] as! [Int]), ([1, 2, 3] as! [Int])] as! [[Int]])) as! [Int])
        print(_p(String(describing: list_to_string((result2 as! [Int])))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
