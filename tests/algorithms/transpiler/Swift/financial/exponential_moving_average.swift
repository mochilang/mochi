// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:54:55 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func exponential_moving_average(_ stock_prices: [Double], _ window_size: Int) -> [Double] {
        if (window_size <= 0) {
            _ = fatalError("window_size must be > 0")
        }
        let alpha = (2.0 / Double((1.0 + Double(window_size))))
        var moving_average: Double = 0.0
        var result: [Double] = ([] as! [Double])
        var i: Int = 0
        while (i < Int(((stock_prices).count))) {
            let price = Double(stock_prices[i])
            if (i <= window_size) {
                if (i == 0) {
                    moving_average = Double(price)
                } else {
                    moving_average = Double((Double((moving_average + price)) * 0.5))
                }
            } else {
                moving_average = Double(((alpha * price) + (Double((1.0 - alpha)) * moving_average)))
            }
            result = (_append(result, moving_average) as! [Double])
            i = Int((i &+ 1))
        }
        return result
    }
    let stock_prices: [Double] = ([2.0, 5.0, 3.0, 8.2, 6.0, 9.0, 10.0] as! [Double])
    let window_size = 3
    var result: [Double] = (exponential_moving_average((stock_prices as! [Double]), window_size) as! [Double])
    print(_p("[" + result.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
