// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:55:02 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct SMAValue: Codable {
    var value: Double
    var ok: Bool
    init() {
        self.value = 0
        self.ok = false
    }
    init(value: Double, ok: Bool) {
        self.value = value
        self.ok = ok
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func simple_moving_average(_ data: [Double], _ window_size: Int) -> [SMAValue] {
        if (window_size < 1) {
            _ = fatalError("Window size must be a positive integer")
        }
        var result: [SMAValue] = ([] as! [SMAValue])
        var window_sum: Double = 0.0
        var i: Int = 0
        while (i < Int(((data).count))) {
            window_sum = Double((window_sum + Double(data[i])))
            if (i >= window_size) {
                window_sum = Double((window_sum - Double(data[(i &- window_size)])))
            }
            if (i >= (window_size &- 1)) {
                let avg = (window_sum / Double(window_size))
                result = (_append(result, SMAValue(value: avg, ok: true)) as! [SMAValue])
            } else {
                result = (_append(result, SMAValue(value: 0.0, ok: false)) as! [SMAValue])
            }
            i = Int((i &+ 1))
        }
        return result
    }
    let data: [Double] = ([10.0, 12.0, 15.0, 13.0, 14.0, 16.0, 18.0, 17.0, 19.0, 21.0] as! [Double])
    let window_size = 3
    let sma_values: [SMAValue] = (simple_moving_average((data as! [Double]), window_size) as! [SMAValue])
    var idx: Int = 0
    while (idx < Int(((sma_values).count))) {
        let item = (sma_values[idx] as! SMAValue)
        if item.ok {
            print(_p(((("Day " + _p((idx &+ 1))) + ": ") + _p(item.value))))
        } else {
            print(_p((("Day " + _p((idx &+ 1))) + ": Not enough data for SMA")))
        }
        idx = Int((idx &+ 1))
    }
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
