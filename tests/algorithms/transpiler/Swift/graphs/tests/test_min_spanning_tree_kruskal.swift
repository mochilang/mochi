// Generated by Mochi transpiler v0.10.60 on 2025-08-08 10:45:52 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func sort_edges(_ edges: [[Int]]) -> [[Int]] {
        var es: [[Int]] = edges
        var i: Int = 0
        while (i < _int(((es).count))) {
            var j: Int = 0
            while (j < ((_int(((es).count)) &- i) &- 1)) {
                if ((es[j][2] as? Int ?? 0) > (es[(j &+ 1)][2] as? Int ?? 0)) {
                    let tmp: [Int] = (es[j] as! [Int])
                    es = _set(es, j, (es[(j &+ 1)] as! [Int]))
                    es = _set(es, (j &+ 1), (tmp as! [Int]))
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return es
    }
    func find(_ parent: [Int], _ x: Int) -> Int {
        var r: Int = x
        while ((parent[r] as? Int ?? 0) != r) {
            r = (parent[r] as? Int ?? 0)
        }
        return r
    }
    func kruskal(_ n: Int, _ edges: [[Int]]) -> [[Int]] {
        var parent: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < n) {
            parent = (_append(parent, i) as! [Int])
            i = _int((i &+ 1))
        }
        var sorted: [[Int]] = (sort_edges((edges as! [[Int]])) as! [[Int]])
        var mst: [[Int]] = ([] as! [[Int]])
        var e: Int = 0
        while (e < _int(((sorted).count))) {
            if (_int(((mst).count)) == (n &- 1)) {
                break
            }
            let edge: [Int] = (sorted[e] as! [Int])
            e = _int((e &+ 1))
            let u = (edge[0] as? Int ?? 0)
            let v = (edge[1] as? Int ?? 0)
            let w = (edge[2] as? Int ?? 0)
            let ru = _int(find((parent as! [Int]), u))
            let rv = _int(find((parent as! [Int]), v))
            if (ru != rv) {
                parent = _set(parent, ru, Int(rv))
                mst = (_append(mst, ([u, v, w] as! [Int])) as! [[Int]])
            }
        }
        return mst
    }
    func edges_equal(_ a: [[Int]], _ b: [[Int]]) -> Bool {
        if (_int(((a).count)) != _int(((b).count))) {
            return false
        }
        var i: Int = 0
        while (i < _int(((a).count))) {
            let e1: [Int] = (a[i] as! [Int])
            let e2: [Int] = (b[i] as! [Int])
            if ((((e1[0] as? Int ?? 0) != (e2[0] as? Int ?? 0)) || ((e1[1] as? Int ?? 0) != (e2[1] as? Int ?? 0))) || ((e1[2] as? Int ?? 0) != (e2[2] as? Int ?? 0))) {
                return false
            }
            i = _int((i &+ 1))
        }
        return true
    }
    func main() {
        let num_nodes = 9
        let edges: [[Int]] = ([([0, 1, 4] as! [Int]), ([0, 7, 8] as! [Int]), ([1, 2, 8] as! [Int]), ([7, 8, 7] as! [Int]), ([7, 6, 1] as! [Int]), ([2, 8, 2] as! [Int]), ([8, 6, 6] as! [Int]), ([2, 3, 7] as! [Int]), ([2, 5, 4] as! [Int]), ([6, 5, 2] as! [Int]), ([3, 5, 14] as! [Int]), ([3, 4, 9] as! [Int]), ([5, 4, 10] as! [Int]), ([1, 7, 11] as! [Int])] as! [[Int]])
        let expected: [[Int]] = ([([7, 6, 1] as! [Int]), ([2, 8, 2] as! [Int]), ([6, 5, 2] as! [Int]), ([0, 1, 4] as! [Int]), ([2, 5, 4] as! [Int]), ([2, 3, 7] as! [Int]), ([0, 7, 8] as! [Int]), ([3, 4, 9] as! [Int])] as! [[Int]])
        let result: [[Int]] = (kruskal(num_nodes, (edges as! [[Int]])) as! [[Int]])
        let sorted_result: [[Int]] = (sort_edges((result as! [[Int]])) as! [[Int]])
        let sorted_expected: [[Int]] = (sort_edges((expected as! [[Int]])) as! [[Int]])
        print(_p("[" + sorted_result.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
        if edges_equal((sorted_expected as! [[Int]]), (sorted_result as! [[Int]])) {
            print(_p(true))
        } else {
            print(_p(false))
        }
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
