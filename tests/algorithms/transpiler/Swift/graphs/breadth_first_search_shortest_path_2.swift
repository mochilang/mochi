// Generated by Mochi transpiler v0.10.60 on 2025-08-08 10:43:25 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func contains(_ xs: [String], _ x: String) -> Bool {
        var i: Int = 0
        while (i < _int(((xs).count))) {
            if ((xs[i] as! String) == x) {
                return true
            }
            i = _int((i &+ 1))
        }
        return false
    }
    func contains_key(_ m: [String: [String]], _ key: String) -> Bool {
        for k in m.keys.sorted() {
            if (k == key) {
                return true
            }
        }
        return false
    }
    func bfs_shortest_path(_ graph: [String: [String]], _ start: String, _ goal: String) -> [String] {
        var explored: [String] = ([] as! [String])
        var queue: [[String]] = ([([start] as! [String])] as! [[String]])
        if (start == goal) {
            return ([start] as! [String])
        }
        while (_int(((queue).count)) > 0) {
            let path: [String] = (queue[0] as! [String])
            queue = (Array(queue[1..<_int(((queue).count))]) as! [[String]])
            let node = (path[(_int(((path).count)) &- 1)] as! String)
            if (!((explored as! [String]).contains((node as! String)))) {
                let neighbours: [String] = (graph[String(String(describing: node))] as! [String])
                var i: Int = 0
                while (i < _int(((neighbours).count))) {
                    let neighbour = (neighbours[i] as! String)
                    var new_path: [String] = path
                    new_path = (_append(new_path, neighbour) as! [String])
                    queue = (_append(queue, new_path) as! [[String]])
                    if (neighbour == goal) {
                        return new_path
                    }
                    i = _int((i &+ 1))
                }
                explored = (_append(explored, node) as! [String])
            }
        }
        return ([] as! [String])
    }
    func bfs_shortest_path_distance(_ graph: [String: [String]], _ start: String, _ target: String) -> Int {
        if ((contains_key((graph as! [String: [String]]), (start as! String)) == false) || (contains_key((graph as! [String: [String]]), (target as! String)) == false)) {
            return -1
        }
        if (start == target) {
            return 0
        }
        var queue: [String] = ([start] as! [String])
        var visited: [String] = ([start] as! [String])
        var dist: [String: Int] = ([:] as! [String: Int])
        dist = _set(dist, String(describing: start), 0)
        dist = _set(dist, String(describing: target), _int(-1))
        while (_int(((queue).count)) > 0) {
            let node = (queue[0] as! String)
            queue = (Array(queue[1..<_int(((queue).count))]) as! [String])
            if (node == target) {
                if (((dist[String(String(describing: target))] as? Int ?? 0) == _int(-1)) || ((dist[String(String(describing: node))] as? Int ?? 0) < (dist[String(String(describing: target))] as? Int ?? 0))) {
                    dist = _set(dist, String(describing: target), (dist[String(String(describing: node))] as? Int ?? 0))
                }
            }
            let adj: [String] = (graph[String(String(describing: node))] as! [String])
            var i: Int = 0
            while (i < _int(((adj).count))) {
                let next = (adj[i] as! String)
                if (!((visited as! [String]).contains((next as! String)))) {
                    visited = (_append(visited, next) as! [String])
                    queue = (_append(queue, next) as! [String])
                    dist = _set(dist, String(describing: next), _int(((dist[String(String(describing: node))] as? Int ?? 0) &+ 1)))
                }
                i = _int((i &+ 1))
            }
        }
        return (dist[String(String(describing: target))] as? Int ?? 0)
    }
    let demo_graph: [String: [String]] = (["A": (["B", "C", "E"] as! [String]), "B": (["A", "D", "E"] as! [String]), "C": (["A", "F", "G"] as! [String]), "D": (["B"] as! [String]), "E": (["A", "B", "D"] as! [String]), "F": (["C"] as! [String]), "G": (["C"] as! [String])] as! [String: [String]])
    do {
        assert(((bfs_shortest_path((demo_graph as! [String: [String]]), "G", "D") as! [String]) == (["G", "C", "A", "B", "D"] as! [String])))
    }
    do {
        assert(((bfs_shortest_path((demo_graph as! [String: [String]]), "G", "G") as! [String]) == (["G"] as! [String])))
    }
    do {
        assert((_int((((bfs_shortest_path((demo_graph as! [String: [String]]), "G", "Unknown") as! [String])).count)) == 0))
    }
    do {
        assert((_int(bfs_shortest_path_distance((demo_graph as! [String: [String]]), "G", "D")) == 4))
    }
    do {
        assert((_int(bfs_shortest_path_distance((demo_graph as! [String: [String]]), "A", "A")) == 0))
    }
    do {
        assert((_int(bfs_shortest_path_distance((demo_graph as! [String: [String]]), "A", "Unknown")) == _int(-1)))
    }
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
