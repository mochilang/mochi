// Generated by Mochi transpiler v0.10.59 on 2025-08-07 08:56:39 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func sqrtApprox(_ x: Double) -> Double {
        var guess: Double = (x / 2.0)
        var i: Int = 0
        while (i < 20) {
            guess = Double((Double((guess + (x / guess))) / 2.0))
            i = Int((i &+ 1))
        }
        return guess
    }
    func rand_float() -> Double {
        return (Double(((Int(_now()) % 1000000 + 1000000) % 1000000)) / 1000000.0)
    }
    func pow_float(_ base: Double, _ exp: Double) -> Double {
        var result: Double = 1.0
        var i: Int = 0
        let e = Int(exp)
        while (i < e) {
            result = Double((result * base))
            i = Int((i &+ 1))
        }
        return result
    }
    func distance(_ city1: [Int], _ city2: [Int]) -> Double {
        let dx = Double(((city1[0] as? Int ?? 0) &- (city2[0] as? Int ?? 0)))
        let dy = Double(((city1[1] as? Int ?? 0) &- (city2[1] as? Int ?? 0)))
        return Double(sqrtApprox(Double(((dx * dx) + (dy * dy)))))
    }
    func choose_weighted(_ options: [Int], _ weights: [Double]) -> Int {
        var total: Double = 0.0
        var i: Int = 0
        while (i < Int(((weights).count))) {
            total = Double((total + Double(weights[i])))
            i = Int((i &+ 1))
        }
        let r = (Double(rand_float()) * total)
        var accum: Double = 0.0
        i = 0
        while (i < Int(((weights).count))) {
            accum = Double((accum + Double(weights[i])))
            if (r <= accum) {
                return (options[i] as? Int ?? 0)
            }
            i = Int((i &+ 1))
        }
        return (options[(Int(((options).count)) &- 1)] as? Int ?? 0)
    }
    func city_select(_ pheromone: [[Double]], _ current: Int, _ unvisited: [Int], _ alpha: Double, _ beta: Double, _ cities: [Int: [Int]]) -> Int {
        var probs: [Double] = ([] as! [Double])
        var i: Int = 0
        while (i < Int(((unvisited).count))) {
            let city = (unvisited[i] as? Int ?? 0)
            let dist = Double(distance((cities[city] as! [Int]), (cities[current] as! [Int])))
            let trail = Double(pheromone[city][current])
            let prob = (Double(pow_float(Double(trail), Double(alpha))) * Double(pow_float(Double((1.0 / dist)), Double(beta))))
            probs = (_append(probs, prob) as! [Double])
            i = Int((i &+ 1))
        }
        return Int(choose_weighted((unvisited as! [Int]), (probs as! [Double])))
    }
    func pheromone_update(_ pheromone: inout [[Double]], _ cities: [Int: [Int]], _ evaporation: Double, _ ants_route: [[Int]], _ q: Double) -> [[Double]] {
        let n = Int(((pheromone).count))
        var i: Int = 0
        while (i < n) {
            var j: Int = 0
            while (j < n) {
                pheromone[i][j] = Double((Double(pheromone[i][j]) * evaporation))
                j = Int((j &+ 1))
            }
            i = Int((i &+ 1))
        }
        var a: Int = 0
        while (a < Int(((ants_route).count))) {
            let route: [Int] = (ants_route[a] as! [Int])
            var total: Double = 0.0
            var r: Int = 0
            while (r < (Int(((route).count)) &- 1)) {
                total = Double((total + Double(distance((cities[(route[r] as? Int ?? 0)] as! [Int]), (cities[(route[(r &+ 1)] as? Int ?? 0)] as! [Int])))))
                r = Int((r &+ 1))
            }
            let delta = (q / total)
            r = 0
            while (r < (Int(((route).count)) &- 1)) {
                let u = (route[r] as? Int ?? 0)
                let v = (route[(r &+ 1)] as? Int ?? 0)
                pheromone[u][v] = Double((Double(pheromone[u][v]) + delta))
                pheromone[v][u] = Double(pheromone[u][v])
                r = Int((r &+ 1))
            }
            a = Int((a &+ 1))
        }
        return pheromone
    }
    func remove_value(_ lst: [Int], _ val: Int) -> [Int] {
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < Int(((lst).count))) {
            if ((lst[i] as? Int ?? 0) != val) {
                res = (_append(res, (lst[i] as? Int ?? 0)) as! [Int])
            }
            i = Int((i &+ 1))
        }
        return res
    }
    func ant_colony(_ cities: [Int: [Int]], _ ants_num: Int, _ iterations: Int, _ evaporation: Double, _ alpha: Double, _ beta: Double, _ q: Double) {
        let n = Int(((cities).count))
        var pheromone: [[Double]] = ([] as! [[Double]])
        var i: Int = 0
        while (i < n) {
            var row: [Double] = ([] as! [Double])
            var j: Int = 0
            while (j < n) {
                row = (_append(row, 1.0) as! [Double])
                j = Int((j &+ 1))
            }
            pheromone = (_append(pheromone, row) as! [[Double]])
            i = Int((i &+ 1))
        }
        var best_path: [Int] = ([] as! [Int])
        var best_distance: Double = 1000000000.0
        var iter: Int = 0
        while (iter < iterations) {
            var ants_route: [[Int]] = ([] as! [[Int]])
            var k: Int = 0
            while (k < ants_num) {
                var route: [Int] = ([0] as! [Int])
                var unvisited: [Int] = ([] as! [Int])
                for key in cities.keys.sorted() {
                    if (key != 0) {
                        unvisited = (_append(unvisited, key) as! [Int])
                    }
                }
                var current: Int = 0
                while (Int(((unvisited).count)) > 0) {
                    let next_city = Int(city_select((pheromone as! [[Double]]), current, (unvisited as! [Int]), Double(alpha), Double(beta), (cities as! [Int: [Int]])))
                    route = (_append(route, next_city) as! [Int])
                    unvisited = (remove_value((unvisited as! [Int]), next_city) as! [Int])
                    current = Int(next_city)
                }
                route = (_append(route, 0) as! [Int])
                ants_route = (_append(ants_route, route) as! [[Int]])
                k = Int((k &+ 1))
            }
            pheromone = (pheromone_update(&pheromone, (cities as! [Int: [Int]]), Double(evaporation), (ants_route as! [[Int]]), Double(q)) as! [[Double]])
            var a: Int = 0
            while (a < Int(((ants_route).count))) {
                let route: [Int] = (ants_route[a] as! [Int])
                var dist: Double = 0.0
                var r: Int = 0
                while (r < (Int(((route).count)) &- 1)) {
                    dist = Double((dist + Double(distance((cities[(route[r] as? Int ?? 0)] as! [Int]), (cities[(route[(r &+ 1)] as? Int ?? 0)] as! [Int])))))
                    r = Int((r &+ 1))
                }
                if (dist < best_distance) {
                    best_distance = Double(dist)
                    best_path = (route as! [Int])
                }
                a = Int((a &+ 1))
            }
            iter = Int((iter &+ 1))
        }
        print(_p(("best_path = " + "[" + best_path.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]")))
        print(_p(("best_distance = " + _p(best_distance))))
    }
    let cities: [Int: [Int]] = ([0: ([0, 0] as! [Int]), 1: ([0, 5] as! [Int]), 2: ([3, 8] as! [Int]), 3: ([8, 10] as! [Int]), 4: ([12, 8] as! [Int]), 5: ([12, 4] as! [Int]), 6: ([8, 0] as! [Int]), 7: ([6, 2] as! [Int])] as! [Int: [Int]])
    _ = ant_colony((cities as! [Int: [Int]]), 10, 20, 0.7, 1.0, 5.0, 10.0)
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
