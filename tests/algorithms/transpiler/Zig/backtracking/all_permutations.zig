// Generated by Mochi Zig transpiler on 2025-08-06 18:11 +0700
const std = @import("std");
const Value = union(enum) { Null: void, Int: i64, Float: f64, Str: []const u8, Bool: bool, List: [][]i64, StrList: [][]const u8, };

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var sequence_var: []Value = &[_]Value{};
var sequence_2_var: []Value = &[_]Value{};

fn repeat_bool(times: i64) []bool {
    var res: []bool = &[_]bool{};
    var i: i64 = 0;
    while (i < times) {
        res = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, res)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return res;
}

fn set_bool(xs: []bool, idx: i64, value: bool) []bool {
    var res_1: []bool = &[_]bool{};
    var i_1: i64 = 0;
    while (i_1 < @as(i64, @intCast(xs.len))) {
        if (i_1 == idx) {
            res_1 = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, res_1)) catch |err| handleError(err); _tmp.append(value) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        } else {
            res_1 = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, res_1)) catch |err| handleError(err); _tmp.append(xs[@intCast(i_1)]) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
        i_1 = i_1 + 1;
    }
    return res_1;
}

fn create_state_space_tree(sequence_param: []Value, current: []Value, index: i64, used: []bool) void {
    if (index == @as(i64, @intCast(sequence_param.len))) {
        std.debug.print("{s}\n", .{_str(current)});
        return;
    }
    var i_2: i64 = 0;
    while (i_2 < @as(i64, @intCast(sequence_param.len))) {
        if (!(used[@intCast(i_2)])) {
            const next_current: []Value = blk: { var _tmp = std.ArrayList(Value).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const Value, current)) catch |err| handleError(err); _tmp.append(sequence_param[@intCast(i_2)]) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
            const next_used: []bool = set_bool(used, i_2, true);
            create_state_space_tree(sequence_param, next_current, index + 1, next_used);
        }
        i_2 = i_2 + 1;
    }
}

fn generate_all_permutations(sequence_param: []Value) void {
    const used_1: []bool = repeat_bool(@as(i64, @intCast(sequence_param.len)));
    create_state_space_tree(sequence_param, &[_]Value{}, 0, used_1);
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        sequence_var = blk0: { var _tmp0 = std.heap.page_allocator.alloc(Value, 4) catch unreachable; _tmp0[0] = Value{.Int = 3}; _tmp0[1] = Value{.Int = 1}; _tmp0[2] = Value{.Int = 2}; _tmp0[3] = Value{.Int = 4}; break :blk0 _tmp0; };
        sequence_2_var = blk1: { var _tmp1 = std.heap.page_allocator.alloc(Value, 3) catch unreachable; _tmp1[0] = Value{.Str = "A"}; _tmp1[1] = Value{.Str = "B"}; _tmp1[2] = Value{.Str = "C"}; break :blk1 _tmp1; };
        generate_all_permutations(sequence_var);
        generate_all_permutations(sequence_2_var);
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    if (@TypeOf(v) == Value) {
        return switch (v) {
            .Int => std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v.Int}) catch unreachable,
            .Float => std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v.Float}) catch unreachable,
            .Str => v.Str,
            .Bool => if (v.Bool) "true" else "false",
            .List => _str(v.List),
            .StrList => _str(v.StrList),
            else => "null",
        };
    }
    const info = @typeInfo(@TypeOf(v));
    if (info == .Pointer and info.Pointer.size == .Slice) {
        var out = std.ArrayList(u8).init(std.heap.page_allocator);
        defer out.deinit();
        out.append('[') catch unreachable;
        for (v, 0..) |e, i| {
            if (i != 0) { out.append(' ') catch unreachable; }
            const s = _str(e);
            out.appendSlice(s) catch unreachable;
        }
        out.append(']') catch unreachable;
        return out.toOwnedSlice() catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
