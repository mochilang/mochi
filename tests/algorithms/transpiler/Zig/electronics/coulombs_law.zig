// Generated by Mochi Zig transpiler on 2025-08-13 07:12 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const COULOMBS_CONSTANT_var: f64 = 8988000000.0;

fn abs(x_param: f64) f64 {
    var x_var: f64 = x_param;
    x_var = x_var;
    if (x_var < 0.0) {
        return @as(f64, @floatFromInt(0)) - x_var;
    }
    return x_var;
}

fn sqrtApprox(x_param_1: f64) f64 {
    var x_var: f64 = x_param_1;
    x_var = x_var;
    if (x_var <= 0.0) {
        return 0.0;
    }
    var guess: f64 = x_var;
    guess = guess;
    var i: i64 = 0;
    i = i;
    while (i < 20) {
        guess = (guess + x_var / guess) / 2.0;
        i = i + 1;
    }
    return guess;
}

fn coulombs_law(force_param: f64, charge1_param: f64, charge2_param: f64, distance_param: f64) std.StringHashMap(f64) {
    var force_var: f64 = force_param;
    force_var = force_var;
    var charge1_var: f64 = charge1_param;
    charge1_var = charge1_var;
    var charge2_var: f64 = charge2_param;
    charge2_var = charge2_var;
    var distance_var: f64 = distance_param;
    distance_var = distance_var;
    const charge_product: f64 = abs(charge1_var * charge2_var);
    var zero_count: i64 = 0;
    zero_count = zero_count;
    if (force_var == 0.0) {
        zero_count = zero_count + 1;
    }
    if (charge1_var == 0.0) {
        zero_count = zero_count + 1;
    }
    if (charge2_var == 0.0) {
        zero_count = zero_count + 1;
    }
    if (distance_var == 0.0) {
        zero_count = zero_count + 1;
    }
    if (zero_count != 1) {
        @panic("One and only one argument must be 0");
    }
    if (distance_var < 0.0) {
        @panic("Distance cannot be negative");
    }
    if (force_var == 0.0) {
        const f: f64 = COULOMBS_CONSTANT_var * charge_product / (distance_var * distance_var);
        return blk0: { var m = std.StringHashMap(f64).init(std.heap.page_allocator); m.put("force", f) catch unreachable; break :blk0 m; };
    }
    if (charge1_var == 0.0) {
        const c1: f64 = abs(force_var) * (distance_var * distance_var) / (COULOMBS_CONSTANT_var * charge2_var);
        return blk1: { var m_1 = std.StringHashMap(f64).init(std.heap.page_allocator); m_1.put("charge1", c1) catch unreachable; break :blk1 m_1; };
    }
    if (charge2_var == 0.0) {
        const c2: f64 = abs(force_var) * (distance_var * distance_var) / (COULOMBS_CONSTANT_var * charge1_var);
        return blk2: { var m_2 = std.StringHashMap(f64).init(std.heap.page_allocator); m_2.put("charge2", c2) catch unreachable; break :blk2 m_2; };
    }
    const d: f64 = sqrtApprox(COULOMBS_CONSTANT_var * charge_product / abs(force_var));
    return blk3: { var m_3 = std.StringHashMap(f64).init(std.heap.page_allocator); m_3.put("distance", d) catch unreachable; break :blk3 m_3; };
}

fn print_map(m_param: *const std.StringHashMap(f64)) void {
    var __mapit1 = m_param.keyIterator();
    while (__mapit1.next()) |__it0| {
        const k = __it0.*;
        std.debug.print("{s}\n", .{_concat_string(_concat_string(_concat_string(_concat_string("{\"", k), "\": "), _str(m_param.get(k).?)), "}")});
    }
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        print_map(&coulombs_law(0.0, 3.0, 5.0, 2000.0));
        print_map(&coulombs_law(10.0, 3.0, 5.0, 0.0));
        print_map(&coulombs_law(10.0, 0.0, 5.0, 2000.0));
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
