// Generated by Mochi Zig transpiler on 2025-08-12 08:38 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn dfs(u: i64, graph: [][]i64, visit_param: []bool, stack_param: []i64) []i64 {
    var visit_var: []bool = visit_param;
    visit_var = visit_var;
    var stack_var: []i64 = stack_param;
    stack_var = stack_var;
    if (visit_var[@as(usize, @intCast(u))]) {
        return stack_var;
    }
    visit_var[@as(usize, @intCast(u))] = true;
    for (graph[@as(usize, @intCast(u))]) |__it0| {
        const v = __it0;
        stack_var = dfs(v, graph, visit_var, stack_var);
    }
    stack_var = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, stack_var)) catch |err| handleError(err); _tmp.append(u) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    return stack_var;
}

fn dfs2(u: i64, reversed_graph: [][]i64, visit_param_1: []bool, component_param: []i64) []i64 {
    var visit_var: []bool = visit_param_1;
    visit_var = visit_var;
    var component_var: []i64 = component_param;
    component_var = component_var;
    if (visit_var[@as(usize, @intCast(u))]) {
        return component_var;
    }
    visit_var[@as(usize, @intCast(u))] = true;
    component_var = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, component_var)) catch |err| handleError(err); _tmp.append(u) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    for (reversed_graph[@as(usize, @intCast(u))]) |__it1| {
        const v = __it1;
        component_var = dfs2(v, reversed_graph, visit_var, component_var);
    }
    return component_var;
}

fn kosaraju(graph: [][]i64) [][]i64 {
    const n: i64 = @as(i64, @intCast(graph.len));
    var reversed_graph: [][]i64 = @constCast(&[_][]i64{})[0..];
    reversed_graph = reversed_graph;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        reversed_graph = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, reversed_graph)) catch |err| handleError(err); _tmp.append((&[_]i64{})) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    i = 0;
    while (i < n) {
        for (graph[@as(usize, @intCast(i))]) |__it2| {
            const v = __it2;
            reversed_graph[@as(usize, @intCast(v))] = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, reversed_graph[@as(usize, @intCast(v))])) catch |err| handleError(err); _tmp.append(i) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
        i = i + 1;
    }
    var visit: []bool = @constCast(&[_]bool{})[0..];
    visit = visit;
    i = 0;
    while (i < n) {
        visit = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, visit)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    var stack: []i64 = @constCast(&[_]i64{})[0..];
    stack = stack;
    i = 0;
    while (i < n) {
        if (visit[@as(usize, @intCast(i))] == false) {
            stack = dfs(i, graph, visit, stack);
        }
        i = i + 1;
    }
    i = 0;
    while (i < n) {
        visit[@as(usize, @intCast(i))] = false;
        i = i + 1;
    }
    var scc: [][]i64 = @constCast(&[_][]i64{})[0..];
    scc = scc;
    var idx: i64 = @as(i64, @intCast(stack.len)) - 1;
    idx = idx;
    while (idx >= 0) {
        const node: i64 = stack[@as(usize, @intCast(idx))];
        if (visit[@as(usize, @intCast(node))] == false) {
            var component: []i64 = @constCast(&[_]i64{})[0..];
            component = component;
            component = dfs2(node, reversed_graph, visit, component);
            scc = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, scc)) catch |err| handleError(err); _tmp.append(component) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
        idx = idx - 1;
    }
    return scc;
}

fn mochi_main() void {
    const graph: [][]i64 = @constCast(&([5][]i64{@constCast(&([1]i64{1}))[0..], @constCast(&([1]i64{2}))[0..], @constCast(&([2]i64{0, 3}))[0..], @constCast(&([1]i64{4}))[0..], &[_]i64{}}))[0..];
    const comps: [][]i64 = kosaraju(graph);
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(comps.len))) {
        std.debug.print("{d}\n", .{comps[@as(usize, @intCast(i))]});
        i = i + 1;
    }
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
