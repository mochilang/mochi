// Generated by Mochi Zig transpiler on 2025-08-12 08:38 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn topology_sort(graph: [][]i64, vert: i64, visited_param: []bool) []i64 {
    var visited_var: []bool = visited_param;
    visited_var = visited_var;
    visited_var[@as(usize, @intCast(vert))] = true;
    var order: []i64 = @constCast(&[_]i64{})[0..];
    order = order;
    for (graph[@as(usize, @intCast(vert))]) |__it0| {
        const neighbour = __it0;
        if (!(visited_var[@as(usize, @intCast(neighbour))])) {
            order = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, order)) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, topology_sort(graph, neighbour, visited_var))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    order = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, order)) catch |err| handleError(err); _tmp.append(vert) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    return order;
}

fn find_component(graph: [][]i64, vert: i64, visited_param_1: []bool) []i64 {
    var visited_var: []bool = visited_param_1;
    visited_var = visited_var;
    visited_var[@as(usize, @intCast(vert))] = true;
    var comp: []i64 = @constCast(&([1]i64{vert}))[0..];
    comp = comp;
    for (graph[@as(usize, @intCast(vert))]) |__it1| {
        const neighbour = __it1;
        if (!(visited_var[@as(usize, @intCast(neighbour))])) {
            comp = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, comp)) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, find_component(graph, neighbour, visited_var))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    return comp;
}

fn strongly_connected_components(graph: [][]i64) [][]i64 {
    const n: i64 = @as(i64, @intCast(graph.len));
    var visited: []bool = @constCast(&[_]bool{})[0..];
    visited = visited;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        visited = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, visited)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    }
    var reversed: [][]i64 = @constCast(&[_][]i64{})[0..];
    reversed = reversed;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        reversed = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, reversed)) catch |err| handleError(err); _tmp.append((&[_]i64{})) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    }
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |__it4| {
        const i: i64 = @as(i64, @intCast(__it4));
        for (graph[@as(usize, @intCast(i))]) |__it5| {
            const neighbour = __it5;
            reversed[@as(usize, @intCast(neighbour))] = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, reversed[@as(usize, @intCast(neighbour))])) catch |err| handleError(err); _tmp.append(i) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    var order: []i64 = @constCast(&[_]i64{})[0..];
    order = order;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |__it6| {
        const i: i64 = @as(i64, @intCast(__it6));
        if (!(visited[@as(usize, @intCast(i))])) {
            order = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, order)) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, topology_sort(graph, i, visited))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    visited = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        visited = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, visited)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    }
    var components: [][]i64 = @constCast(&[_][]i64{})[0..];
    components = components;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        const v: i64 = order[@as(usize, @intCast(n - i - 1))];
        if (!(visited[@as(usize, @intCast(v))])) {
            const comp: []i64 = find_component(reversed, v, visited);
            components = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, components)) catch |err| handleError(err); _tmp.append(comp) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
        i = i + 1;
    }
    return components;
}

fn mochi_main() void {
    const test_graph_1: [][]i64 = @constCast(&([5][]i64{@constCast(&([2]i64{2, 3}))[0..], @constCast(&([1]i64{0}))[0..], @constCast(&([1]i64{1}))[0..], @constCast(&([1]i64{4}))[0..], &[_]i64{}}))[0..];
    const test_graph_2: [][]i64 = @constCast(&([6][]i64{@constCast(&([3]i64{1, 2, 3}))[0..], @constCast(&([1]i64{2}))[0..], @constCast(&([1]i64{0}))[0..], @constCast(&([1]i64{4}))[0..], @constCast(&([1]i64{5}))[0..], @constCast(&([1]i64{3}))[0..]}))[0..];
    std.debug.print("{s}\n", .{_str(strongly_connected_components(test_graph_1))});
    std.debug.print("{s}\n", .{_str(strongly_connected_components(test_graph_2))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
