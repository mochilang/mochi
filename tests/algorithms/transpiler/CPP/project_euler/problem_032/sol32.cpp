// Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:48:51 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static long _parse_int_str(const std::string& s, long base) {
    if(s.empty()) return 0;
    return std::stol(s, nullptr, base);
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string join_digits(const std::vector<std::string>& xs);
int64_t digits_to_int(const std::vector<std::string>& xs);
bool contains_int(const std::vector<int64_t>& xs, int64_t value);
std::vector<std::string> remove_at(const std::vector<std::string>& xs, int64_t idx);
bool is_combination_valid(const std::vector<std::string>& comb);
std::vector<int64_t> search(const std::vector<std::string>& prefix, const std::vector<std::string>& remaining, std::vector<int64_t>& products);
int main();

std::vector<std::string> digits = std::vector<std::string>{std::string("1"), std::string("2"), std::string("3"), std::string("4"), std::string("5"), std::string("6"), std::string("7"), std::string("8"), std::string("9")};
std::vector<int64_t> products = {};
int64_t total = 0;
int64_t i = 0;

std::string join_digits(const std::vector<std::string>& xs) {
    std::string s = std::string("");
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        s = (s + _index(xs, i));
        i = (i + int64_t(1));
    }
    return s;
}

int64_t digits_to_int(const std::vector<std::string>& xs) {
    return _parse_int_str(join_digits(xs), int64_t(10));
}

bool contains_int(const std::vector<int64_t>& xs, int64_t value) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == value)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

std::vector<std::string> remove_at(const std::vector<std::string>& xs, int64_t idx) {
    std::vector<std::string> res = {};
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((i != idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

bool is_combination_valid(const std::vector<std::string>& comb) {
    int64_t prod = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(5), int64_t(9) - int64_t(5)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(5), __v.begin()+int64_t(9)); })(comb));
    int64_t mul2 = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(2) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(2)); })(comb));
    int64_t mul3 = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(2), int64_t(5) - int64_t(2)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(2), __v.begin()+int64_t(5)); })(comb));
    if (((mul2 * mul3) == prod)) {
        return true;
    }
    int64_t mul1 = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(1) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(1)); })(comb));
    int64_t mul4 = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), int64_t(5) - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+int64_t(5)); })(comb));
    return ((mul1 * mul4) == prod);
}

std::vector<int64_t> search(const std::vector<std::string>& prefix, const std::vector<std::string>& remaining, std::vector<int64_t>& products) {
    if ((remaining.size() == int64_t(0))) {
        if (is_combination_valid(prefix)) {
            int64_t p = digits_to_int(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(5), int64_t(9) - int64_t(5)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(5), __v.begin()+int64_t(9)); })(prefix));
            if (!contains_int(products, p)) {
                products = ([&]{ auto __tmp = products; __tmp.push_back(p); return __tmp; }());
            }
        }
        return products;
    }
    int64_t i = int64_t(0);
    while ((i < remaining.size())) {
        auto next_prefix = ([&]{ auto __tmp = prefix; __tmp.push_back(_index(remaining, i)); return __tmp; }());
        std::vector<std::string> next_remaining = remove_at(remaining, i);
        products = search((std::vector<std::string>)(next_prefix), next_remaining, products);
        i = (i + int64_t(1));
    }
    return products;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        products = search(std::vector<std::string>{}, digits, products);
        total = int64_t(0);
        i = int64_t(0);
        while ((i < products.size())) {
            total = (total + _index(products, i));
            i = (i + int64_t(1));
        }
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << total; return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
