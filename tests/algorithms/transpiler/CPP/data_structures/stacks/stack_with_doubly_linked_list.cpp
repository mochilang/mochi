// Generated by Mochi transpiler v0.10.60 on 2025-08-07 20:18:51 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Node;
struct Stack;
struct PopResult;
struct TopResult;

std::ostream& operator<<(std::ostream&, const Node&);
std::ostream& operator<<(std::ostream&, const Stack&);
std::ostream& operator<<(std::ostream&, const PopResult&);
std::ostream& operator<<(std::ostream&, const TopResult&);


struct Node {
    int64_t data;
    int64_t next;
    int64_t prev;
    auto operator<=>(const Node&) const = default;
};

struct Stack {
    std::vector<Node> nodes;
    int64_t head;
    auto operator<=>(const Stack&) const = default;
};

struct PopResult {
    Stack stack;
    int64_t value;
    bool ok;
    auto operator<=>(const PopResult&) const = default;
};

struct TopResult {
    int64_t value;
    bool ok;
    auto operator<=>(const TopResult&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Node& v) {
    os << '{' << "'data': "<< v.data
 << ", " << "'next': "<< v.next
 << ", " << "'prev': "<< v.prev
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Stack& v) {
    os << '{' << "'nodes': "<< "["; for(size_t i=0;i<v.nodes.size();++i){ if(i>0) os << ", "; os << v.nodes[i]; } os << "]"
 << ", " << "'head': "<< v.head
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const PopResult& v) {
    os << '{' << "'stack': "<< v.stack
 << ", " << "'value': "<< v.value
 << ", " << "'ok': "<< v.ok
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const TopResult& v) {
    os << '{' << "'value': "<< v.value
 << ", " << "'ok': "<< v.ok
 << '}';
    return os;
}

Stack empty_stack();
Stack push(const Stack& stack, int64_t value);
PopResult pop(const Stack& stack);
TopResult top(const Stack& stack);
int64_t size(const Stack& stack);
bool is_empty(const Stack& stack);
void print_stack(const Stack& stack);
int main();

Stack empty_stack() {
    return Stack{.nodes = std::vector<Node>{}, .head = (int64_t(0) - int64_t(1))};
}

Stack push(const Stack& stack, int64_t value) {
    std::vector<Node> nodes = stack.nodes;
    int idx = nodes.size();
    Node new_node = Node{.data = value, .next = stack.head, .prev = (int64_t(0) - int64_t(1))};
    nodes = ([&]{ auto& __tmp = nodes; __tmp.push_back(new_node); return __tmp; }());
    if ((stack.head != (int64_t(0) - int64_t(1)))) {
        Node head_node = ([&](const auto& __v){ auto __i = stack.head; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(nodes);
        head_node.prev = idx;
        nodes[stack.head] = head_node;
    }
    return Stack{.nodes = nodes, .head = idx};
}

PopResult pop(const Stack& stack) {
    if ((stack.head == (int64_t(0) - int64_t(1)))) {
        return PopResult{.stack = stack, .value = int64_t(0), .ok = false};
    }
    std::vector<Node> nodes = stack.nodes;
    Node head_node = ([&](const auto& __v){ auto __i = stack.head; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(nodes);
    int64_t value = head_node.data;
    int64_t next_idx = head_node.next;
    if ((next_idx != (int64_t(0) - int64_t(1)))) {
        Node next_node = ([&](const auto& __v){ auto __i = next_idx; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(nodes);
        next_node.prev = (int64_t(0) - int64_t(1));
        nodes[next_idx] = next_node;
    }
    Stack new_stack = Stack{.nodes = nodes, .head = next_idx};
    return PopResult{.stack = new_stack, .value = value, .ok = true};
}

TopResult top(const Stack& stack) {
    if ((stack.head == (int64_t(0) - int64_t(1)))) {
        return TopResult{.value = int64_t(0), .ok = false};
    }
    Node node = ([&](const auto& __v){ auto __i = stack.head; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(stack.nodes);
    return TopResult{.value = node.data, .ok = true};
}

int64_t size(const Stack& stack) {
    int64_t count = int64_t(0);
    int64_t idx = stack.head;
    while ((idx != (int64_t(0) - int64_t(1)))) {
        count = (count + int64_t(1));
        Node node = ([&](const auto& __v){ auto __i = idx; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(stack.nodes);
        idx = node.next;
    }
    return count;
}

bool is_empty(const Stack& stack) {
    return (stack.head == (int64_t(0) - int64_t(1)));
}

void print_stack(const Stack& stack) {
    std::cout << std::string("stack elements are:");
    std::cout << std::endl;
    int64_t idx = stack.head;
    std::string s = std::string("");
    while ((idx != (int64_t(0) - int64_t(1)))) {
        Node node = ([&](const auto& __v){ auto __i = idx; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(stack.nodes);
        s = ((s + ([&]{ std::ostringstream ss; ss << std::boolalpha << node.data; return ss.str(); }())) + std::string("->"));
        idx = node.next;
    }
    if ((s.size() > int64_t(0))) {
        std::cout << s;
        std::cout << std::endl;
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        Stack stack = empty_stack();
        std::cout << std::string("Stack operations using Doubly LinkedList");
        std::cout << std::endl;
        stack = push(stack, int64_t(4));
        stack = push(stack, int64_t(5));
        stack = push(stack, int64_t(6));
        stack = push(stack, int64_t(7));
        print_stack(stack);
        TopResult t = top(stack);
        if (t.ok) {
            std::cout << (std::string("Top element is ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << t.value; return ss.str(); }()));
            std::cout << std::endl;
        } else {
            std::cout << std::string("Top element is None");
            std::cout << std::endl;
        }
        std::cout << (std::string("Size of the stack is ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << size(stack); return ss.str(); }()));
        std::cout << std::endl;
        PopResult p = pop(stack);
        stack = p.stack;
        p = pop(stack);
        stack = p.stack;
        print_stack(stack);
        std::cout << (std::string("stack is empty: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << is_empty(stack); return ss.str(); }()));
        std::cout << std::endl;
    }
    return 0;
}
