// Generated by Mochi transpiler v0.10.60 on 2025-08-08 11:19:23 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<std::vector<int64_t>> sort_edges(const std::vector<std::vector<int64_t>>& edges);
int64_t find_parent(std::vector<int64_t> parent, int64_t i);
std::vector<std::vector<int64_t>> kruskal(int64_t num_nodes, const std::vector<std::vector<int64_t>>& edges);
std::string edges_to_string(const std::vector<std::vector<int64_t>>& es);
int main();

std::vector<std::vector<int64_t>> edges1 = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(3)}, std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(5)}, std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(1)}};
std::vector<std::vector<int64_t>> edges2 = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(3)}, std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(5)}, std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(1)}, std::vector<int64_t>{int64_t(0), int64_t(2), int64_t(1)}, std::vector<int64_t>{int64_t(0), int64_t(3), int64_t(2)}};
std::vector<std::vector<int64_t>> edges3 = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(3)}, std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(5)}, std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(1)}, std::vector<int64_t>{int64_t(0), int64_t(2), int64_t(1)}, std::vector<int64_t>{int64_t(0), int64_t(3), int64_t(2)}, std::vector<int64_t>{int64_t(2), int64_t(1), int64_t(1)}};

std::vector<std::vector<int64_t>> sort_edges(const std::vector<std::vector<int64_t>>& edges) {
    std::vector<std::vector<int64_t>> es = edges;
    int64_t i = int64_t(0);
    while ((i < es.size())) {
        int64_t j = int64_t(0);
        while ((j < ((es.size() - i) - int64_t(1)))) {
            if ((([&](const auto& __v){ auto __i = int64_t(2); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(([&](const auto& __v){ auto __i = j; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es)) > ([&](const auto& __v){ auto __i = int64_t(2); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(([&](const auto& __v){ auto __i = (j + int64_t(1)); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es)))) {
                std::vector<int64_t> temp = ([&](const auto& __v){ auto __i = j; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es);
                es[j] = ([&](const auto& __v){ auto __i = (j + int64_t(1)); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es);
                es[(j + int64_t(1))] = temp;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return es;
}

int64_t find_parent(std::vector<int64_t> parent, int64_t i) {
    if ((([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(parent) != i)) {
        parent[i] = find_parent(parent, ([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(parent));
    }
    return ([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(parent);
}

std::vector<std::vector<int64_t>> kruskal(int64_t num_nodes, const std::vector<std::vector<int64_t>>& edges) {
    std::vector<std::vector<int64_t>> es = sort_edges(edges);
    std::vector<int64_t> parent = {};
    int64_t i = int64_t(0);
    while ((i < num_nodes)) {
        parent = ([&]{ auto& __tmp = parent; __tmp.push_back(i); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<std::vector<int64_t>> mst = {};
    int64_t idx = int64_t(0);
    while ((idx < es.size())) {
        std::vector<int64_t> e = ([&](const auto& __v){ auto __i = idx; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es);
        int64_t pa = find_parent(parent, ([&](const auto& __v){ auto __i = int64_t(0); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(e));
        int64_t pb = find_parent(parent, ([&](const auto& __v){ auto __i = int64_t(1); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(e));
        if ((pa != pb)) {
            mst = ([&]{ auto& __tmp = mst; __tmp.push_back(e); return __tmp; }());
            parent[pa] = pb;
        }
        idx = (idx + int64_t(1));
    }
    return mst;
}

std::string edges_to_string(const std::vector<std::vector<int64_t>>& es) {
    std::string s = std::string("[");
    int64_t i = int64_t(0);
    while ((i < es.size())) {
        std::vector<int64_t> e = ([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(es);
        s = (((((((s + std::string("(")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(0); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(e); return ss.str(); }())) + std::string(", ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(1); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(e); return ss.str(); }())) + std::string(", ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(2); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(e); return ss.str(); }())) + std::string(")"));
        if ((i < (es.size() - int64_t(1)))) {
            s = (s + std::string(", "));
        }
        i = (i + int64_t(1));
    }
    s = (s + std::string("]"));
    return s;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << edges_to_string(kruskal(int64_t(4), edges1));
        std::cout << std::endl;
        std::cout << edges_to_string(kruskal(int64_t(4), edges2));
        std::cout << std::endl;
        std::cout << edges_to_string(kruskal(int64_t(4), edges3));
        std::cout << std::endl;
    }
    return 0;
}
