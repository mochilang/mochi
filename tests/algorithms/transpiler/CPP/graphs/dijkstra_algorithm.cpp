// Generated by Mochi transpiler v0.10.60 on 2025-08-08 11:18:07 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Edge;

std::ostream& operator<<(std::ostream&, const Edge&);


struct Edge {
    int64_t node;
    int64_t weight;
    auto operator<=>(const Edge&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Edge& v) {
    os << '{' << "'node': "<< v.node
 << ", " << "'weight': "<< v.weight
 << '}';
    return os;
}

std::vector<int64_t> make_int_list(int64_t n, int64_t value);
std::vector<bool> make_bool_list(int64_t n);
std::vector<int64_t> dijkstra(const std::vector<std::vector<Edge>>& graph, int64_t src);
int main();

std::vector<std::vector<Edge>> graph = std::vector<std::vector<Edge>>{std::vector<Edge>{Edge{.node = int64_t(1), .weight = int64_t(10)}, Edge{.node = int64_t(3), .weight = int64_t(5)}}, std::vector<Edge>{Edge{.node = int64_t(2), .weight = int64_t(1)}, Edge{.node = int64_t(3), .weight = int64_t(2)}}, std::vector<Edge>{Edge{.node = int64_t(4), .weight = int64_t(4)}}, std::vector<Edge>{Edge{.node = int64_t(1), .weight = int64_t(3)}, Edge{.node = int64_t(2), .weight = int64_t(9)}, Edge{.node = int64_t(4), .weight = int64_t(2)}}, std::vector<Edge>{Edge{.node = int64_t(0), .weight = int64_t(7)}, Edge{.node = int64_t(2), .weight = int64_t(6)}}};
std::vector<int64_t> dist = dijkstra(graph, int64_t(0));

std::vector<int64_t> make_int_list(int64_t n, int64_t value) {
    std::vector<int64_t> lst = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        lst = ([&]{ auto& __tmp = lst; __tmp.push_back(value); return __tmp; }());
        i = (i + int64_t(1));
    }
    return lst;
}

std::vector<bool> make_bool_list(int64_t n) {
    std::vector<bool> lst = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        lst = ([&]{ auto& __tmp = lst; __tmp.push_back(false); return __tmp; }());
        i = (i + int64_t(1));
    }
    return lst;
}

std::vector<int64_t> dijkstra(const std::vector<std::vector<Edge>>& graph, int64_t src) {
    int n = graph.size();
    std::vector<int64_t> dist = make_int_list((int64_t)(n), int64_t(1000000000));
    std::vector<bool> visited = make_bool_list((int64_t)(n));
    dist[src] = int64_t(0);
    int64_t count = int64_t(0);
    while ((count < n)) {
        int64_t u = -int64_t(1);
        int64_t min_dist = int64_t(1000000000);
        int64_t i = int64_t(0);
        while ((i < n)) {
            if ((!([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(visited) && (([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist) < min_dist))) {
                min_dist = ([&](const auto& __v){ auto __i = i; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist);
                u = i;
            }
            i = (i + int64_t(1));
        }
        if ((u < int64_t(0))) {
            break;
        }
        visited[u] = true;
        int64_t j = int64_t(0);
        while ((j < ([&](const auto& __v){ auto __i = u; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(graph).size())) {
            Edge e = ([&](const auto& __v){ auto __i = j; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(([&](const auto& __v){ auto __i = u; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(graph));
            int64_t v = e.node;
            int64_t w = e.weight;
            if (!([&](const auto& __v){ auto __i = v; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(visited)) {
                int64_t new_dist = (([&](const auto& __v){ auto __i = u; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist) + w);
                if ((new_dist < ([&](const auto& __v){ auto __i = v; if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist))) {
                    dist[v] = new_dist;
                }
            }
            j = (j + int64_t(1));
        }
        count = (count + int64_t(1));
    }
    return dist;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(0); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(1); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(2); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(3); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << ([&](const auto& __v){ auto __i = int64_t(4); if (__i < 0) __i += __v.size(); return __v[static_cast<size_t>(__i)]; })(dist); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
