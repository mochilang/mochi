// Generated by Mochi v0.10.59 on 2025-08-07 00:18:01 GMT+7

function gcd(a: number, b: number): number {
  let x: number = a;
  let y: number = b;
  while ((y != 0)) {
    let t: number = (x % y);
    x = y;
    y = t;
  }
  if ((x < 0)) {
    return -x;
  }
  return x;
}
function proper_fractions(den: number): string[] {
  if ((den < 0)) {
    _panic("The Denominator Cannot be less than 0");
  }
  let res: string[] = [];
  let n: number = 1;
  while ((n < den)) {
    if ((gcd(n, den) == 1)) {
      res.push(((_str(n) + "/") + _str(den)));
    }
    n = (n + 1);
  }
  return res;
}
function test_proper_fractions() {
  let a: string[] = proper_fractions(10);
  if ((a != ["1/10", "3/10", "7/10", "9/10"])) {
    _panic("test 10 failed");
  }
  let b: string[] = proper_fractions(5);
  if ((b != ["1/5", "2/5", "3/5", "4/5"])) {
    _panic("test 5 failed");
  }
  let c: string[] = proper_fractions(0);
  if ((c != [])) {
    _panic("test 0 failed");
  }
}
function main() {
  test_proper_fractions();
  console.log(_str(_str(proper_fractions(10))));
  console.log(_str(_str(proper_fractions(5))));
  console.log(_str(_str(proper_fractions(0))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

