// Generated by Mochi v0.10.59 on 2025-08-07 00:18:07 GMT+7

function odd_sieve(num: number): number[] {
  if ((num <= 2)) {
    return [];
  }
  if ((num == 3)) {
    return [2];
  }
  let size = (Math.trunc(num / 2) - 1);
  let sieve: boolean[] = [];
  let idx: number = 0;
  while ((idx < size)) {
    sieve.push(true);
    idx = (idx + 1);
  }
  let i: number = 3;
  while (((i * i) <= num)) {
    let s_idx: number = (Math.trunc(i / 2) - 1);
    if (sieve[(s_idx) < 0 ? sieve.length + (s_idx) : s_idx]) {
      let j: number = (i * i);
      while ((j < num)) {
        let j_idx: number = (Math.trunc(j / 2) - 1);
        sieve[(j_idx) < 0 ? sieve.length + (j_idx) : j_idx] = false;
        j = (j + (2 * i));
      }
    }
    i = (i + 2);
  }
  let primes: number[] = [2];
  let n: number = 3;
  let k: number = 0;
  while ((n < num)) {
    if (sieve[(k) < 0 ? sieve.length + (k) : k]) {
      primes.push(n);
    }
    n = (n + 2);
    k = (k + 1);
  }
  return primes;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str("[" + (odd_sieve(2)).join(' ') + "]"));
  console.log(_str("[" + (odd_sieve(3)).join(' ') + "]"));
  console.log(_str("[" + (odd_sieve(10)).join(' ') + "]"));
  console.log(_str("[" + (odd_sieve(20)).join(' ') + "]"));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

