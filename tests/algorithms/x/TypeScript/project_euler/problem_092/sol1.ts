// Generated by Mochi v0.10.59 on 2025-08-07 08:54:37 GMT+7

function next_number(_number: number): number {
  let n: number = _number;
  let total: number = 0;
  while ((n > 0)) {
    let d: number = (n % 10);
    total = (total + (d * d));
    n = Math.trunc(n / 10);
  }
  return total;
}
function chain(_number: number): boolean {
  let n: number = _number;
  while (((n != 1) && (n != 89))) {
    n = next_number(n);
  }
  return (n == 1);
}
function solution(limit: number): number {
  let count: number = 0;
  let i: number = 1;
  while ((i < limit)) {
    if (!chain(i)) {
      count = (count + 1);
    }
    i = (i + 1);
  }
  return count;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(next_number(44))));
  console.log(_str(_str(next_number(10))));
  console.log(_str(_str(next_number(32))));
  console.log(_str(_str(chain(10))));
  console.log(_str(_str(chain(58))));
  console.log(_str(_str(chain(1))));
  console.log(_str(_str(solution(100))));
  console.log(_str(_str(solution(1000))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

