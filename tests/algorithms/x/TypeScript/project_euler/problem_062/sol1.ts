// Generated by Mochi v0.10.59 on 2025-08-07 08:24:32 GMT+7

function get_digits(num: number): string {
  let cube = ((num * num) * num);
  let s: string = _str(cube);
  let counts: number[] = [];
  let j: number = 0;
  while ((j < 10)) {
    counts.push(0);
    j = (j + 1);
  }
  let i: number = 0;
  while ((i < _len(s))) {
    let d: number = Math.trunc(s[(i) < 0 ? s.length + (i) : i]);
    counts[(d) < 0 ? counts.length + (d) : d] = (counts[(d) < 0 ? counts.length + (d) : d] + 1);
    i = (i + 1);
  }
  let result: string = "";
  let d: number = 0;
  while ((d < 10)) {
    let c: number = counts[(d) < 0 ? counts.length + (d) : d];
    while ((c > 0)) {
      result = (result + _str(d));
      c = (c - 1);
    }
    d = (d + 1);
  }
  return result;
}
function solution(max_base: number): number {
  let freqs: Record<string, number[]> = {};
  let num: number = 0;
  while (true) {
    let digits = get_digits(num);
    let arr: number[] = [];
    if ((digits in freqs)) {
      arr = freqs[(digits) < 0 ? freqs.length + (digits) : digits];
    }
    arr.push(num);
    freqs[(digits) < 0 ? freqs.length + (digits) : digits] = arr;
    if ((_len(arr) == max_base)) {
      let base: number = arr[(Math.trunc(0)) < 0 ? arr.length + (Math.trunc(0)) : Math.trunc(0)];
      return ((base * base) * base);
    }
    num = (num + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(("solution() = " + _str(solution(5)))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

