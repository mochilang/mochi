// Generated by Mochi v0.10.59 on 2025-08-07 08:20:32 GMT+7

function is_leap(year: number): boolean {
  if (((((year % 4) == 0) && ((year % 100) != 0)) || ((year % 400) == 0))) {
    return true;
  }
  return false;
}
function count_sundays(): number {
  let days_per_month: number[] = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
  let day: number = 6;
  let month: number = 1;
  let year: number = 1901;
  let sundays: number = 0;
  while ((year < 2001)) {
    day = (day + 7);
    if (is_leap(year)) {
      if (((day > days_per_month[(Math.trunc((month - 1))) < 0 ? days_per_month.length + (Math.trunc((month - 1))) : Math.trunc((month - 1))]) && (month != 2))) {
        month = (month + 1);
        day = (day - days_per_month[(Math.trunc((month - 2))) < 0 ? days_per_month.length + (Math.trunc((month - 2))) : Math.trunc((month - 2))]);
      } else {
        if (((day > 29) && (month == 2))) {
          month = (month + 1);
          day = (day - 29);
        }
      }
    } else {
      if ((day > days_per_month[(Math.trunc((month - 1))) < 0 ? days_per_month.length + (Math.trunc((month - 1))) : Math.trunc((month - 1))])) {
        month = (month + 1);
        day = (day - days_per_month[(Math.trunc((month - 2))) < 0 ? days_per_month.length + (Math.trunc((month - 2))) : Math.trunc((month - 2))]);
      }
    }
    if ((month > 12)) {
      year = (year + 1);
      month = 1;
    }
    if (((year < 2001) && (day == 1))) {
      sundays = (sundays + 1);
    }
  }
  return sundays;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(count_sundays()));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

