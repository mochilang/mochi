// Generated by Mochi v0.10.59 on 2025-08-07 08:20:30 GMT+7

function solution(n: number): number {
  let ones_counts: number[] = [0, 3, 3, 5, 4, 4, 3, 5, 5, 4, 3, 6, 6, 8, 8, 7, 7, 9, 8, 8];
  let tens_counts: number[] = [0, 0, 6, 6, 5, 5, 5, 7, 6, 6];
  let count: number = 0;
  let i: number = 1;
  while ((i <= n)) {
    if ((i < 1000)) {
      if ((i >= 100)) {
        count = ((count + ones_counts[(Math.trunc(Math.trunc(i / 100))) < 0 ? ones_counts.length + (Math.trunc(Math.trunc(i / 100))) : Math.trunc(Math.trunc(i / 100))]) + 7);
        if (((i % 100) != 0)) {
          count = (count + 3);
        }
      }
      let remainder: number = (i % 100);
      if (((remainder > 0) && (remainder < 20))) {
        count = (count + ones_counts[(remainder) < 0 ? ones_counts.length + (remainder) : remainder]);
      } else {
        count = (count + ones_counts[(Math.trunc((i % 10))) < 0 ? ones_counts.length + (Math.trunc((i % 10))) : Math.trunc((i % 10))]);
        count = (count + tens_counts[(Math.trunc(Math.trunc((remainder - (i % 10)) / 10))) < 0 ? tens_counts.length + (Math.trunc(Math.trunc((remainder - (i % 10)) / 10))) : Math.trunc(Math.trunc((remainder - (i % 10)) / 10))]);
      }
    } else {
      count = ((count + ones_counts[(Math.trunc(Math.trunc(i / 1000))) < 0 ? ones_counts.length + (Math.trunc(Math.trunc(i / 1000))) : Math.trunc(Math.trunc(i / 1000))]) + 8);
    }
    i = (i + 1);
  }
  return count;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(solution(1000))));
  console.log(_str(_str(solution(5))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

