// Generated by Mochi v0.10.59 on 2025-08-07 08:19:04 GMT+7

function is_prime(_number: number): boolean {
  if (((_number > 1) && (_number < 4))) {
    return true;
  } else {
    if ((((_number < 2) || ((_number % 2) == 0)) || ((_number % 3) == 0))) {
      return false;
    }
  }
  let i: number = 5;
  while (((i * i) <= _number)) {
    if ((((_number % i) == 0) || ((_number % (i + 2)) == 0))) {
      return false;
    }
    i = (i + 6);
  }
  return true;
}
function solution(nth: number): number {
  if ((nth <= 0)) {
    error("Parameter nth must be greater than or equal to one.");
  }
  let primes: number[] = [];
  let num: number = 2;
  while ((_len(primes) < nth)) {
    if (is_prime(num)) {
      primes.push(num);
    }
    num = (num + 1);
  }
  return primes[(Math.trunc((_len(primes) - 1))) < 0 ? primes.length + (Math.trunc((_len(primes) - 1))) : Math.trunc((_len(primes) - 1))];
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let ans: number
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
ans = solution(10001)
  console.log(_str(("solution(10001) = " + _str(ans))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

