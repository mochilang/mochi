// Generated by Mochi v0.10.59 on 2025-08-07 08:54:35 GMT+7

function solution(_number: number): number {
  let partitions: number[] = [1];
  let i: number = _len(partitions);
  while (true) {
    let item: number = 0;
    let j: number = 1;
    while (true) {
      let sign: number = (((j % 2) == 0) ? -1 : 1);
      let index: number = Math.trunc((((j * j) * 3) - j) / 2);
      if ((index > i)) {
        break
      }
      item = (item + (partitions[(Math.trunc((i - index))) < 0 ? partitions.length + (Math.trunc((i - index))) : Math.trunc((i - index))] * sign));
      item = (item % _number);
      index = (index + j);
      if ((index > i)) {
        break
      }
      item = (item + (partitions[(Math.trunc((i - index))) < 0 ? partitions.length + (Math.trunc((i - index))) : Math.trunc((i - index))] * sign));
      item = (item % _number);
      j = (j + 1);
    }
    if ((item == 0)) {
      return i;
    }
    partitions.push(item);
    i = (i + 1);
  }
  return 0;
}
function main() {
  console.log(_str(_str(solution(1))));
  console.log(_str(_str(solution(9))));
  console.log(_str(_str(solution(1000000))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

