// Generated by Mochi v0.10.59 on 2025-08-07 08:24:21 GMT+7

function is_prime(_number: number): boolean {
  if (((1 < _number) && (_number < 4))) {
    return true;
  }
  if ((((_number < 2) || ((_number % 2) == 0)) || ((_number % 3) == 0))) {
    return false;
  }
  let i: number = 5;
  while (((i * i) <= _number)) {
    if ((((_number % i) == 0) || ((_number % (i + 2)) == 0))) {
      return false;
    }
    i = (i + 6);
  }
  return true;
}
function solution(ratio: number): number {
  let j: number = 3;
  let primes: number = 3;
  while (((primes / ((2 * j) - 1)) >= ratio)) {
    let i: number = (((j * j) + j) + 1);
    let limit: number = ((j + 2) * (j + 2));
    let step: number = (j + 1);
    while ((i < limit)) {
      if (is_prime(i)) {
        primes = (primes + 1);
      }
      i = (i + step);
    }
    j = (j + 2);
  }
  return j;
}
function test_solution() {
  if ((solution(0.5) != 11)) {
    _panic("solution 0.5 failed");
  }
  if ((solution(0.2) != 309)) {
    _panic("solution 0.2 failed");
  }
  if ((solution(0.111) != 11317)) {
    _panic("solution 0.111 failed");
  }
}
function main() {
  test_solution();
  console.log(_str(_str(solution(0.1))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

