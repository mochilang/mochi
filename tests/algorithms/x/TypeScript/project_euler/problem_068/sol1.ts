// Generated by Mochi v0.10.59 on 2025-08-07 08:24:39 GMT+7

function range_desc(start: number, end: number): number[] {
  let res: number[] = [];
  let i: number = start;
  while ((i >= end)) {
    res.push(i);
    i = (i - 1);
  }
  return res;
}
function range_asc(start: number, end: number): number[] {
  let res: number[] = [];
  let i: number = start;
  while ((i <= end)) {
    res.push(i);
    i = (i + 1);
  }
  return res;
}
function concat_lists(a: number[], b: number[]): number[] {
  let res: number[] = a;
  let i: number = 0;
  while ((i < _len(b))) {
    res.push(b[(i) < 0 ? b.length + (i) : i]);
    i = (i + 1);
  }
  return res;
}
function swap(xs: number[], i: number, j: number): number[] {
  let res: number[] = [];
  let k: number = 0;
  while ((k < _len(xs))) {
    if ((k == i)) {
      res.push(xs[(j) < 0 ? xs.length + (j) : j]);
    } else {
      if ((k == j)) {
        res.push(xs[(i) < 0 ? xs.length + (i) : i]);
      } else {
        res.push(xs[(k) < 0 ? xs.length + (k) : k]);
      }
    }
    k = (k + 1);
  }
  return res;
}
function generate_gon_ring(gon_side: number, perm: number[]): number[] {
  let result: number[] = [];
  result.push(perm[(Math.trunc(0)) < 0 ? perm.length + (Math.trunc(0)) : Math.trunc(0)]);
  result.push(perm[(Math.trunc(1)) < 0 ? perm.length + (Math.trunc(1)) : Math.trunc(1)]);
  result.push(perm[(Math.trunc(2)) < 0 ? perm.length + (Math.trunc(2)) : Math.trunc(2)]);
  let extended: any[] = [...perm, perm[(Math.trunc(1)) < 0 ? perm.length + (Math.trunc(1)) : Math.trunc(1)]];
  let magic_number: number = ((gon_side < 5) ? 1 : 2);
  let i: number = 1;
  while ((i < (Math.trunc(_len(extended) / 3) + magic_number))) {
    result.push(extended[(Math.trunc(((2 * i) + 1))) < 0 ? extended.length + (Math.trunc(((2 * i) + 1))) : Math.trunc(((2 * i) + 1))]);
    result.push(result[(Math.trunc(((3 * i) - 1))) < 0 ? result.length + (Math.trunc(((3 * i) - 1))) : Math.trunc(((3 * i) - 1))]);
    result.push(extended[(Math.trunc(((2 * i) + 2))) < 0 ? extended.length + (Math.trunc(((2 * i) + 2))) : Math.trunc(((2 * i) + 2))]);
    i = (i + 1);
  }
  return result;
}
function min_outer(numbers: number[]): number {
  let min_val: number = numbers[(Math.trunc(0)) < 0 ? numbers.length + (Math.trunc(0)) : Math.trunc(0)];
  let i: number = 3;
  while ((i < _len(numbers))) {
    if ((numbers[(i) < 0 ? numbers.length + (i) : i] < min_val)) {
      min_val = numbers[(i) < 0 ? numbers.length + (i) : i];
    }
    i = (i + 3);
  }
  return min_val;
}
function is_magic_gon(numbers: number[]): boolean {
  if (((_len(numbers) % 3) != 0)) {
    return false;
  }
  if ((min_outer(numbers) != numbers[(Math.trunc(0)) < 0 ? numbers.length + (Math.trunc(0)) : Math.trunc(0)])) {
    return false;
  }
  let total: number = ((numbers[(Math.trunc(0)) < 0 ? numbers.length + (Math.trunc(0)) : Math.trunc(0)] + numbers[(Math.trunc(1)) < 0 ? numbers.length + (Math.trunc(1)) : Math.trunc(1)]) + numbers[(Math.trunc(2)) < 0 ? numbers.length + (Math.trunc(2)) : Math.trunc(2)]);
  let i: number = 3;
  while ((i < _len(numbers))) {
    if ((((numbers[(i) < 0 ? numbers.length + (i) : i] + numbers[(Math.trunc((i + 1))) < 0 ? numbers.length + (Math.trunc((i + 1))) : Math.trunc((i + 1))]) + numbers[(Math.trunc((i + 2))) < 0 ? numbers.length + (Math.trunc((i + 2))) : Math.trunc((i + 2))]) != total)) {
      return false;
    }
    i = (i + 3);
  }
  return true;
}
function permute_search(nums: number[], start: number, gon_side: number, current_max: string): string {
  if ((start == _len(nums))) {
    let ring: number[] = generate_gon_ring(gon_side, nums);
    if (is_magic_gon(ring)) {
      let s: string = "";
      let k: number = 0;
      while ((k < _len(ring))) {
        s = (s + _str(ring[(k) < 0 ? ring.length + (k) : k]));
        k = (k + 1);
      }
      if ((s > current_max)) {
        return s;
      }
    }
    return current_max;
  }
  let res: string = current_max;
  let i: number = start;
  while ((i < _len(nums))) {
    let swapped: number[] = swap(nums, start, i);
    let candidate: string = permute_search(swapped, (start + 1), gon_side, res);
    if ((candidate > res)) {
      res = candidate;
    }
    i = (i + 1);
  }
  return res;
}
function solution(gon_side: number): string {
  if (((gon_side < 3) || (gon_side > 5))) {
    return "";
  }
  let small: number[] = range_desc((gon_side + 1), 1);
  let big: number[] = range_asc((gon_side + 2), (gon_side * 2));
  let numbers: number[] = concat_lists(small, big);
  let max_str: string = permute_search(numbers, 0, gon_side, "");
  return max_str;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(solution(5)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

