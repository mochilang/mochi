// Generated by Mochi v0.10.59 on 2025-08-06 21:32:13 GMT+7

function change_contrast(img: number[][], level: number): number[][] {
  let factor: number = ((259.0 * (level + 255.0)) / (255.0 * (259.0 - level)));
  let result: number[][] = [];
  let i: number = 0;
  while ((i < _len(img))) {
    let row: number[] = img[i];
    let new_row: number[] = [];
    let j: number = 0;
    while ((j < _len(row))) {
      let c: number = row[j];
      let contrasted: number[][] = Math.trunc((128.0 + (factor * (c - 128.0))));
      let clamped = ((contrasted < 0) ? 0 : ((contrasted > 255) ? 255 : contrasted));
      new_row.push(clamped);
      j = (j + 1);
    }
    result.push(new_row);
    i = (i + 1);
  }
  return result;
}
function print_image(img: number[][]) {
  let i: number = 0;
  while ((i < _len(img))) {
    let row: number[] = img[i];
    let j: number = 0;
    let line: string = "";
    while ((j < _len(row))) {
      line = ((line + _str(row[j])) + " ");
      j = (j + 1);
    }
    console.log(_str(line));
    i = (i + 1);
  }
}
let image: number[][] = [[100, 125, 150], [175, 200, 225], [50, 75, 100]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let contrasted: number[][]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str("Original image:"));
  print_image(image);
contrasted = change_contrast(image, 170)
  console.log(_str("After contrast:"));
  print_image(contrasted);
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

