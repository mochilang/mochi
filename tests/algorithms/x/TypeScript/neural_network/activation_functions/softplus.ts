// Generated by Mochi v0.10.59 on 2025-08-07 08:17:26 GMT+7

function ln(x: number): number {
  if ((x <= 0.0)) {
    _panic("ln domain error");
  }
  let y: number = ((x - 1.0) / (x + 1.0));
  let y2: number = (y * y);
  let term: number = y;
  let sum: number = 0.0;
  let k: number = 0;
  while ((k < 10)) {
    let denom = ((2 * k) + 1);
    sum = (sum + (term / denom));
    term = (term * y2);
    k = (k + 1);
  }
  return (2.0 * sum);
}
function exp(x: number): number {
  let term: number = 1.0;
  let sum: number = 1.0;
  let n: number = 1;
  while ((n < 20)) {
    term = ((term * x) / n);
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}
function softplus(vector: number[]): number[] {
  let result: number[] = [];
  let i: number = 0;
  while ((i < _len(vector))) {
    let x: number = vector[(i) < 0 ? vector.length + (i) : i];
    let value: number = ln((1.0 + exp(x)));
    result.push(value);
    i = (i + 1);
  }
  return result;
}
function main() {
  let v1: number[] = [2.3, 0.6, -2.0, -3.8];
  let v2: number[] = [-9.2, -0.3, 0.45, -4.56];
  let r1: number[] = softplus(v1);
  let r2: number[] = softplus(v2);
  console.log(_str("[" + (r1).join(' ') + "]"));
  console.log(_str("[" + (r2).join(' ') + "]"));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

