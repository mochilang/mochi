// Generated by Mochi v0.10.59 on 2025-08-07 08:17:05 GMT+7

function bubble_sort(a: number[]): number[] {
  let arr: number[] = a;
  let n: number = _len(arr);
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while (((j + 1) < (n - i))) {
      if ((arr[(j) < 0 ? arr.length + (j) : j] > arr[(Math.trunc((j + 1))) < 0 ? arr.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))])) {
        let temp: number = arr[(j) < 0 ? arr.length + (j) : j];
        arr[(j) < 0 ? arr.length + (j) : j] = arr[(Math.trunc((j + 1))) < 0 ? arr.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
        arr[((j + 1)) < 0 ? arr.length + ((j + 1)) : (j + 1)] = temp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}
function median(matrix: number[][]): number {
  let linear: number[] = [];
  let i: number = 0;
  while ((i < _len(matrix))) {
    let row: number[] = matrix[(i) < 0 ? matrix.length + (i) : i];
    let j: number = 0;
    while ((j < _len(row))) {
      linear.push(row[(j) < 0 ? row.length + (j) : j]);
      j = (j + 1);
    }
    i = (i + 1);
  }
  let sorted: number[] = bubble_sort(linear);
  let mid: number = Math.trunc((_len(sorted) - 1) / 2);
  return sorted[(mid) < 0 ? sorted.length + (mid) : mid];
}
let matrix1: number[][] = [[1, 3, 5], [2, 6, 9], [3, 6, 9]];
let matrix2: number[][] = [[1, 2, 3], [4, 5, 6]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(median(matrix1))));
  console.log(_str(_str(median(matrix2))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

