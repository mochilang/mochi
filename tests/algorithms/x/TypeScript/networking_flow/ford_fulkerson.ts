// Generated by Mochi v0.10.59 on 2025-08-07 08:17:15 GMT+7

let INF: number = 1000000000;
function breadth_first_search(graph: number[][], source: number, sink: number, parent: number[]): boolean {
  let visited: boolean[] = [];
  let i: number = 0;
  while ((i < _len(graph))) {
    visited.push(false);
    i = (i + 1);
  }
  let queue: number[] = [];
  queue.push(source);
  visited[(source) < 0 ? visited.length + (source) : source] = true;
  let head: number = 0;
  while ((head < _len(queue))) {
    let u: number = queue[(head) < 0 ? queue.length + (head) : head];
    head = (head + 1);
    let row: number[] = graph[(u) < 0 ? graph.length + (u) : u];
    let ind: number = 0;
    while ((ind < _len(row))) {
      let capacity: number = row[(ind) < 0 ? row.length + (ind) : ind];
      if (((visited[(ind) < 0 ? visited.length + (ind) : ind] == false) && (capacity > 0))) {
        queue.push(ind);
        visited[(ind) < 0 ? visited.length + (ind) : ind] = true;
        parent[(ind) < 0 ? parent.length + (ind) : ind] = u;
      }
      ind = (ind + 1);
    }
  }
  return visited[(sink) < 0 ? visited.length + (sink) : sink];
}
function ford_fulkerson(graph: number[][], source: number, sink: number): number {
  let parent: number[] = [];
  let i: number = 0;
  while ((i < _len(graph))) {
    parent.push(-1);
    i = (i + 1);
  }
  let max_flow: number = 0;
  while (breadth_first_search(graph, source, sink, parent)) {
    let path_flow: number = INF;
    let s: number = sink;
    while ((s != source)) {
      let prev: number = parent[(s) < 0 ? parent.length + (s) : s];
      let cap: number = graph[(prev) < 0 ? graph.length + (prev) : prev][(s) < 0 ? graph[(prev) < 0 ? graph.length + (prev) : prev].length + (s) : s];
      if ((cap < path_flow)) {
        path_flow = cap;
      }
      s = prev;
    }
    max_flow = (max_flow + path_flow);
    let v: number = sink;
    while ((v != source)) {
      let u: number = parent[(v) < 0 ? parent.length + (v) : v];
      graph[(u) < 0 ? graph.length + (u) : u][(v) < 0 ? graph[(u) < 0 ? graph.length + (u) : u].length + (v) : v] = (graph[(u) < 0 ? graph.length + (u) : u][(v) < 0 ? graph[(u) < 0 ? graph.length + (u) : u].length + (v) : v] - path_flow);
      graph[(v) < 0 ? graph.length + (v) : v][(u) < 0 ? graph[(v) < 0 ? graph.length + (v) : v].length + (u) : u] = (graph[(v) < 0 ? graph.length + (v) : v][(u) < 0 ? graph[(v) < 0 ? graph.length + (v) : v].length + (u) : u] + path_flow);
      v = u;
    }
    let j: number = 0;
    while ((j < _len(parent))) {
      parent[(j) < 0 ? parent.length + (j) : j] = -1;
      j = (j + 1);
    }
  }
  return max_flow;
}
let graph: number[][] = [[0, 16, 13, 0, 0, 0], [0, 0, 10, 12, 0, 0], [0, 4, 0, 0, 14, 0], [0, 0, 9, 0, 0, 20], [0, 0, 0, 7, 0, 4], [0, 0, 0, 0, 0, 0]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(ford_fulkerson(graph, 0, 5))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

