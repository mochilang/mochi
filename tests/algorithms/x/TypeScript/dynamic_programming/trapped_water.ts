// Generated by Mochi v0.10.59 on 2025-08-06 21:52:42 GMT+7

function make_list(len: number, value: number): number[] {
  let arr: number[] = [];
  let i: number = 0;
  while ((i < len)) {
    arr.push(value);
    i = (i + 1);
  }
  return arr;
}
function trapped_rainwater(heights: number[]): number {
  if ((_len(heights) == 0)) {
    return 0;
  }
  let i: number = 0;
  while ((i < _len(heights))) {
    if ((heights[(i) < 0 ? heights.length + (i) : i] < 0)) {
      _panic("No height can be negative");
    }
    i = (i + 1);
  }
  let length: number = _len(heights);
  let left_max: number[] = make_list(length, 0);
  left_max[(0) < 0 ? left_max.length + (0) : 0] = heights[(Math.trunc(0)) < 0 ? heights.length + (Math.trunc(0)) : Math.trunc(0)];
  i = 1;
  while ((i < length)) {
    if ((heights[(i) < 0 ? heights.length + (i) : i] > left_max[(Math.trunc((i - 1))) < 0 ? left_max.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))])) {
      left_max[(i) < 0 ? left_max.length + (i) : i] = heights[(i) < 0 ? heights.length + (i) : i];
    } else {
      left_max[(i) < 0 ? left_max.length + (i) : i] = left_max[(Math.trunc((i - 1))) < 0 ? left_max.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))];
    }
    i = (i + 1);
  }
  let right_max: number[] = make_list(length, 0);
  let last: number = (length - 1);
  right_max[(last) < 0 ? right_max.length + (last) : last] = heights[(last) < 0 ? heights.length + (last) : last];
  i = (last - 1);
  while ((i >= 0)) {
    if ((heights[(i) < 0 ? heights.length + (i) : i] > right_max[(Math.trunc((i + 1))) < 0 ? right_max.length + (Math.trunc((i + 1))) : Math.trunc((i + 1))])) {
      right_max[(i) < 0 ? right_max.length + (i) : i] = heights[(i) < 0 ? heights.length + (i) : i];
    } else {
      right_max[(i) < 0 ? right_max.length + (i) : i] = right_max[(Math.trunc((i + 1))) < 0 ? right_max.length + (Math.trunc((i + 1))) : Math.trunc((i + 1))];
    }
    i = (i - 1);
  }
  let total: number = 0;
  i = 0;
  while ((i < length)) {
    let left: number = left_max[(i) < 0 ? left_max.length + (i) : i];
    let right: number = right_max[(i) < 0 ? right_max.length + (i) : i];
    let smaller: number = ((left < right) ? left : right);
    total = (total + (smaller - heights[(i) < 0 ? heights.length + (i) : i]));
    i = (i + 1);
  }
  return total;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(trapped_rainwater([0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1]))));
  console.log(_str(_str(trapped_rainwater([7, 1, 5, 3, 6, 4]))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

