// Generated by Mochi v0.10.59 on 2025-08-06 21:52:33 GMT+7

function min_partitions(s: string): number {
  let n: number = _len(s);
  let cut: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    cut.push(0);
    i = (i + 1);
  }
  let pal: boolean[][] = [];
  i = 0;
  while ((i < n)) {
    let row: boolean[] = [];
    let j: number = 0;
    while ((j < n)) {
      row.push(false);
      j = (j + 1);
    }
    pal.push(row);
    i = (i + 1);
  }
  i = 0;
  while ((i < n)) {
    let mincut: number = i;
    let j: number = 0;
    while ((j <= i)) {
      if (((s[(i) < 0 ? s.length + (i) : i] == s[(j) < 0 ? s.length + (j) : j]) && (((i - j) < 2) || pal[(Math.trunc((j + 1))) < 0 ? pal.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))][(Math.trunc((i - 1))) < 0 ? pal[(Math.trunc((j + 1))) < 0 ? pal.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))].length + (Math.trunc((i - 1))) : Math.trunc((i - 1))]))) {
        pal[(j) < 0 ? pal.length + (j) : j][(i) < 0 ? pal[(j) < 0 ? pal.length + (j) : j].length + (i) : i] = true;
        if ((j == 0)) {
          mincut = 0;
        } else {
          let candidate: number = (cut[(Math.trunc((j - 1))) < 0 ? cut.length + (Math.trunc((j - 1))) : Math.trunc((j - 1))] + 1);
          if ((candidate < mincut)) {
            mincut = candidate;
          }
        }
      }
      j = (j + 1);
    }
    cut[(i) < 0 ? cut.length + (i) : i] = mincut;
    i = (i + 1);
  }
  return cut[(Math.trunc((n - 1))) < 0 ? cut.length + (Math.trunc((n - 1))) : Math.trunc((n - 1))];
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(min_partitions("aab")));
  console.log(_str(min_partitions("aaa")));
  console.log(_str(min_partitions("ababbbabbababa")));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

