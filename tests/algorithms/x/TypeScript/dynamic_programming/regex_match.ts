// Generated by Mochi v0.10.59 on 2025-08-06 21:52:35 GMT+7

function recursive_match(text: string, pattern: string): boolean {
  if ((_len(pattern) == 0)) {
    return (_len(text) == 0);
  }
  if ((_len(text) == 0)) {
    if (((_len(pattern) >= 2) && ((pattern).substring((_len(pattern) - 1), _len(pattern)) == "*"))) {
      return recursive_match(text, (pattern).substring(0, (_len(pattern) - 2)));
    }
    return false;
  }
  let last_text: string = (text).substring((_len(text) - 1), _len(text));
  let last_pattern: string = (pattern).substring((_len(pattern) - 1), _len(pattern));
  if (((last_text == last_pattern) || (last_pattern == "."))) {
    return recursive_match((text).substring(0, (_len(text) - 1)), (pattern).substring(0, (_len(pattern) - 1)));
  }
  if ((last_pattern == "*")) {
    if (recursive_match((text).substring(0, (_len(text) - 1)), pattern)) {
      return true;
    }
    return recursive_match(text, (pattern).substring(0, (_len(pattern) - 2)));
  }
  return false;
}
function dp_match(text: string, pattern: string): boolean {
  let m: number = _len(text);
  let n: number = _len(pattern);
  let dp: boolean[][] = [];
  let i: number = 0;
  while ((i <= m)) {
    let row: boolean[] = [];
    let j: number = 0;
    while ((j <= n)) {
      row.push(false);
      j = (j + 1);
    }
    dp.push(row);
    i = (i + 1);
  }
  dp[(0) < 0 ? dp.length + (0) : 0][(0) < 0 ? dp[(0) < 0 ? dp.length + (0) : 0].length + (0) : 0] = true;
  let j: number = 1;
  while ((j <= n)) {
    if ((((pattern).substring((j - 1), j) == "*") && (j >= 2))) {
      if (dp[(Math.trunc(0)) < 0 ? dp.length + (Math.trunc(0)) : Math.trunc(0)][(Math.trunc((j - 2))) < 0 ? dp[(Math.trunc(0)) < 0 ? dp.length + (Math.trunc(0)) : Math.trunc(0)].length + (Math.trunc((j - 2))) : Math.trunc((j - 2))]) {
        dp[(0) < 0 ? dp.length + (0) : 0][(j) < 0 ? dp[(0) < 0 ? dp.length + (0) : 0].length + (j) : j] = true;
      }
    }
    j = (j + 1);
  }
  i = 1;
  while ((i <= m)) {
    j = 1;
    while ((j <= n)) {
      let p_char: string = (pattern).substring((j - 1), j);
      let t_char: string = (text).substring((i - 1), i);
      if (((p_char == ".") || (p_char == t_char))) {
        if (dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(Math.trunc((j - 1))) < 0 ? dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (Math.trunc((j - 1))) : Math.trunc((j - 1))]) {
          dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = true;
        }
      } else {
        if ((p_char == "*")) {
          if ((j >= 2)) {
            if (dp[(i) < 0 ? dp.length + (i) : i][(Math.trunc((j - 2))) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (Math.trunc((j - 2))) : Math.trunc((j - 2))]) {
              dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = true;
            }
            let prev_p: string = (pattern).substring((j - 2), (j - 1));
            if (((prev_p == ".") || (prev_p == t_char))) {
              if (dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(j) < 0 ? dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (j) : j]) {
                dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = true;
              }
            }
          }
        } else {
          dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = false;
        }
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return dp[(m) < 0 ? dp.length + (m) : m][(n) < 0 ? dp[(m) < 0 ? dp.length + (m) : m].length + (n) : n];
}
function print_bool(b: boolean) {
  if (b) {
    console.log("");
  } else {
    _stdout_write("");
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _stdout_write(s: string) {
  if (typeof Deno !== 'undefined') {
    Deno.stdout.writeSync(new TextEncoder().encode(s));
  } else if (typeof process !== 'undefined') {
    process.stdout.write(s);
  } else {
    console.log(s);
  }
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  print_bool(recursive_match("abc", "a.c"));
  print_bool(recursive_match("abc", "af*.c"));
  print_bool(recursive_match("abc", "a.c*"));
  print_bool(recursive_match("abc", "a.c*d"));
  print_bool(recursive_match("aa", ".*"));
  print_bool(dp_match("abc", "a.c"));
  print_bool(dp_match("abc", "af*.c"));
  print_bool(dp_match("abc", "a.c*"));
  print_bool(dp_match("abc", "a.c*d"));
  print_bool(dp_match("aa", ".*"));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

