// Generated by Mochi v0.10.59 on 2025-08-06 21:52:23 GMT+7

function min_int(a: number, b: number): number {
  if ((a < b)) {
    return a;
  }
  return b;
}
function minimum_cost_path(matrix: number[][]): number {
  let rows: number = _len(matrix);
  let cols: number = _len(matrix[(Math.trunc(0)) < 0 ? matrix.length + (Math.trunc(0)) : Math.trunc(0)]);
  let j: number = 1;
  while ((j < cols)) {
    let row0: number[] = matrix[(Math.trunc(0)) < 0 ? matrix.length + (Math.trunc(0)) : Math.trunc(0)];
    row0[(j) < 0 ? row0.length + (j) : j] = (row0[(j) < 0 ? row0.length + (j) : j] + row0[(Math.trunc((j - 1))) < 0 ? row0.length + (Math.trunc((j - 1))) : Math.trunc((j - 1))]);
    matrix[(0) < 0 ? matrix.length + (0) : 0] = row0;
    j = (j + 1);
  }
  let i: number = 1;
  while ((i < rows)) {
    let row: number[] = matrix[(i) < 0 ? matrix.length + (i) : i];
    row[(0) < 0 ? row.length + (0) : 0] = (row[(Math.trunc(0)) < 0 ? row.length + (Math.trunc(0)) : Math.trunc(0)] + matrix[(Math.trunc((i - 1))) < 0 ? matrix.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(Math.trunc(0)) < 0 ? matrix[(Math.trunc((i - 1))) < 0 ? matrix.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (Math.trunc(0)) : Math.trunc(0)]);
    matrix[(i) < 0 ? matrix.length + (i) : i] = row;
    i = (i + 1);
  }
  i = 1;
  while ((i < rows)) {
    let row: number[] = matrix[(i) < 0 ? matrix.length + (i) : i];
    j = 1;
    while ((j < cols)) {
      let up: number = matrix[(Math.trunc((i - 1))) < 0 ? matrix.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(j) < 0 ? matrix[(Math.trunc((i - 1))) < 0 ? matrix.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (j) : j];
      let left: number = row[(Math.trunc((j - 1))) < 0 ? row.length + (Math.trunc((j - 1))) : Math.trunc((j - 1))];
      let best: number = min_int(up, left);
      row[(j) < 0 ? row.length + (j) : j] = (row[(j) < 0 ? row.length + (j) : j] + best);
      j = (j + 1);
    }
    matrix[(i) < 0 ? matrix.length + (i) : i] = row;
    i = (i + 1);
  }
  return matrix[(Math.trunc((rows - 1))) < 0 ? matrix.length + (Math.trunc((rows - 1))) : Math.trunc((rows - 1))][(Math.trunc((cols - 1))) < 0 ? matrix[(Math.trunc((rows - 1))) < 0 ? matrix.length + (Math.trunc((rows - 1))) : Math.trunc((rows - 1))].length + (Math.trunc((cols - 1))) : Math.trunc((cols - 1))];
}
let m1: number[][] = [[2, 1], [3, 1], [4, 2]];
let m2: number[][] = [[2, 1, 4], [2, 1, 3], [3, 2, 1]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(minimum_cost_path(m1))));
  console.log(_str(_str(minimum_cost_path(m2))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

