// Generated by Mochi v0.10.59 on 2025-08-06 21:52:04 GMT+7

export interface LcsResult { length: number; sequence: string }
function zeros_matrix(rows: number, cols: number): number[][] {
  let matrix: number[][] = [];
  let i: number = 0;
  while ((i <= rows)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j <= cols)) {
      row.push(0);
      j = (j + 1);
    }
    matrix.push(row);
    i = (i + 1);
  }
  return matrix;
}
function longest_common_subsequence(x: string, y: string): LcsResult {
  let m: number = _len(x);
  let n: number = _len(y);
  let dp: number[][] = zeros_matrix(m, n);
  let i: number = 1;
  while ((i <= m)) {
    let j: number = 1;
    while ((j <= n)) {
      if ((x[(Math.trunc((i - 1))) < 0 ? x.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))] == y[(Math.trunc((j - 1))) < 0 ? y.length + (Math.trunc((j - 1))) : Math.trunc((j - 1))])) {
        dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = (dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(Math.trunc((j - 1))) < 0 ? dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (Math.trunc((j - 1))) : Math.trunc((j - 1))] + 1);
      } else {
        if ((dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(j) < 0 ? dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (j) : j] > dp[(i) < 0 ? dp.length + (i) : i][(Math.trunc((j - 1))) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (Math.trunc((j - 1))) : Math.trunc((j - 1))])) {
          dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))][(j) < 0 ? dp[(Math.trunc((i - 1))) < 0 ? dp.length + (Math.trunc((i - 1))) : Math.trunc((i - 1))].length + (j) : j];
        } else {
          dp[(i) < 0 ? dp.length + (i) : i][(j) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (j) : j] = dp[(i) < 0 ? dp.length + (i) : i][(Math.trunc((j - 1))) < 0 ? dp[(i) < 0 ? dp.length + (i) : i].length + (Math.trunc((j - 1))) : Math.trunc((j - 1))];
        }
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  let seq: string = "";
  let i2: number = m;
  let j2: number = n;
  while (((i2 > 0) && (j2 > 0))) {
    if ((x[(Math.trunc((i2 - 1))) < 0 ? x.length + (Math.trunc((i2 - 1))) : Math.trunc((i2 - 1))] == y[(Math.trunc((j2 - 1))) < 0 ? y.length + (Math.trunc((j2 - 1))) : Math.trunc((j2 - 1))])) {
      seq = (x[(Math.trunc((i2 - 1))) < 0 ? x.length + (Math.trunc((i2 - 1))) : Math.trunc((i2 - 1))] + seq);
      i2 = (i2 - 1);
      j2 = (j2 - 1);
    } else {
      if ((dp[(Math.trunc((i2 - 1))) < 0 ? dp.length + (Math.trunc((i2 - 1))) : Math.trunc((i2 - 1))][(j2) < 0 ? dp[(Math.trunc((i2 - 1))) < 0 ? dp.length + (Math.trunc((i2 - 1))) : Math.trunc((i2 - 1))].length + (j2) : j2] >= dp[(i2) < 0 ? dp.length + (i2) : i2][(Math.trunc((j2 - 1))) < 0 ? dp[(i2) < 0 ? dp.length + (i2) : i2].length + (Math.trunc((j2 - 1))) : Math.trunc((j2 - 1))])) {
        i2 = (i2 - 1);
      } else {
        j2 = (j2 - 1);
      }
    }
  }
  return {"length": dp[(m) < 0 ? dp.length + (m) : m][(n) < 0 ? dp[(m) < 0 ? dp.length + (m) : m].length + (n) : n], "sequence": seq};
}
let a: string = "AGGTAB";
let b: string = "GXTXAYB";
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let res: LcsResult
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
res = longest_common_subsequence(a, b)
  console.log(_str(((("len = " + _str(res.length)) + ", sub-sequence = ") + res.sequence)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

