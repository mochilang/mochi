// Generated by Mochi v0.10.59 on 2025-08-06 23:29:21 GMT+7

function retroactive_resolution(coefficients: number[][], vector: number[][]): number[][] {
  let rows: number = _len(coefficients);
  let x: number[][] = [];
  let i: number = 0;
  while ((i < rows)) {
    let inner: number[] = [];
    inner.push(0.0);
    x.push(inner);
    i = (i + 1);
  }
  let r: number = (rows - 1);
  while ((r >= 0)) {
    let total: number = 0.0;
    let c: number = (r + 1);
    while ((c < rows)) {
      total = (total + (coefficients[(r) < 0 ? coefficients.length + (r) : r][(c) < 0 ? coefficients[(r) < 0 ? coefficients.length + (r) : r].length + (c) : c] * x[(c) < 0 ? x.length + (c) : c][(Math.trunc(0)) < 0 ? x[(c) < 0 ? x.length + (c) : c].length + (Math.trunc(0)) : Math.trunc(0)]));
      c = (c + 1);
    }
    x[(r) < 0 ? x.length + (r) : r][(Math.trunc(0)) < 0 ? x[(r) < 0 ? x.length + (r) : r].length + (Math.trunc(0)) : Math.trunc(0)] = ((vector[(r) < 0 ? vector.length + (r) : r][(Math.trunc(0)) < 0 ? vector[(r) < 0 ? vector.length + (r) : r].length + (Math.trunc(0)) : Math.trunc(0)] - total) / coefficients[(r) < 0 ? coefficients.length + (r) : r][(r) < 0 ? coefficients[(r) < 0 ? coefficients.length + (r) : r].length + (r) : r]);
    r = (r - 1);
  }
  return x;
}
function gaussian_elimination(coefficients: number[][], vector: number[][]): number[][] {
  let rows: number = _len(coefficients);
  let columns: number = _len(coefficients[(Math.trunc(0)) < 0 ? coefficients.length + (Math.trunc(0)) : Math.trunc(0)]);
  if ((rows != columns)) {
    return [];
  }
  let augmented: number[][] = [];
  let i: number = 0;
  while ((i < rows)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < columns)) {
      row.push(coefficients[(i) < 0 ? coefficients.length + (i) : i][(j) < 0 ? coefficients[(i) < 0 ? coefficients.length + (i) : i].length + (j) : j]);
      j = (j + 1);
    }
    row.push(vector[(i) < 0 ? vector.length + (i) : i][(Math.trunc(0)) < 0 ? vector[(i) < 0 ? vector.length + (i) : i].length + (Math.trunc(0)) : Math.trunc(0)]);
    augmented.push(row);
    i = (i + 1);
  }
  let row_idx: number = 0;
  while ((row_idx < (rows - 1))) {
    let pivot: number = augmented[(row_idx) < 0 ? augmented.length + (row_idx) : row_idx][(row_idx) < 0 ? augmented[(row_idx) < 0 ? augmented.length + (row_idx) : row_idx].length + (row_idx) : row_idx];
    let col: number = (row_idx + 1);
    while ((col < rows)) {
      let factor: number = (augmented[(col) < 0 ? augmented.length + (col) : col][(row_idx) < 0 ? augmented[(col) < 0 ? augmented.length + (col) : col].length + (row_idx) : row_idx] / pivot);
      let k: number = row_idx;
      while ((k < (columns + 1))) {
        augmented[(col) < 0 ? augmented.length + (col) : col][(k) < 0 ? augmented[(col) < 0 ? augmented.length + (col) : col].length + (k) : k] = (augmented[(col) < 0 ? augmented.length + (col) : col][(k) < 0 ? augmented[(col) < 0 ? augmented.length + (col) : col].length + (k) : k] - (factor * augmented[(row_idx) < 0 ? augmented.length + (row_idx) : row_idx][(k) < 0 ? augmented[(row_idx) < 0 ? augmented.length + (row_idx) : row_idx].length + (k) : k]));
        k = (k + 1);
      }
      col = (col + 1);
    }
    row_idx = (row_idx + 1);
  }
  let coeffs: number[][] = [];
  let vec: number[][] = [];
  let r: number = 0;
  while ((r < rows)) {
    let row: number[] = [];
    let c: number = 0;
    while ((c < columns)) {
      row.push(augmented[(r) < 0 ? augmented.length + (r) : r][(c) < 0 ? augmented[(r) < 0 ? augmented.length + (r) : r].length + (c) : c]);
      c = (c + 1);
    }
    coeffs.push(row);
    vec.push([augmented[(r) < 0 ? augmented.length + (r) : r][(columns) < 0 ? augmented[(r) < 0 ? augmented.length + (r) : r].length + (columns) : columns]]);
    r = (r + 1);
  }
  let x: number[][] = retroactive_resolution(coeffs, vec);
  return x;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str("[" + (gaussian_elimination([[1.0, -4.0, -2.0], [5.0, 2.0, -2.0], [1.0, -1.0, 0.0]], [[-2.0], [-3.0], [4.0]])).join(' ') + "]"));
  console.log(_str("[" + (gaussian_elimination([[1.0, 2.0], [5.0, 2.0]], [[5.0], [5.0]])).join(' ') + "]"));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

