// Generated by Mochi v0.10.59 on 2025-08-06 23:29:28 GMT+7

function contains_int(xs: number[], x: number): boolean {
  let i: number = 0;
  while ((i < _len(xs))) {
    if ((xs[(i) < 0 ? xs.length + (i) : i] == x)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function split(s: string, sep: string): string[] {
  let res: string[] = [];
  let current: string = "";
  let i: number = 0;
  while ((i < _len(s))) {
    let ch: string = (s).substring(i, (i + 1));
    if ((ch == sep)) {
      res.push(current);
      current = "";
    } else {
      current = (current + ch);
    }
    i = (i + 1);
  }
  res.push(current);
  return res;
}
function pow_int_float(base: number, exp: number): number {
  let result: number = 1.0;
  let i: number = 0;
  while ((i < exp)) {
    result = (result * base);
    i = (i + 1);
  }
  return result;
}
function points_to_polynomial(coordinates: number[][]): string {
  if ((_len(coordinates) == 0)) {
    _panic("The program cannot work out a fitting polynomial.");
  }
  let i: number = 0;
  while ((i < _len(coordinates))) {
    if ((_len(coordinates[(i) < 0 ? coordinates.length + (i) : i]) != 2)) {
      _panic("The program cannot work out a fitting polynomial.");
    }
    i = (i + 1);
  }
  let j: number = 0;
  while ((j < _len(coordinates))) {
    let k: number = (j + 1);
    while ((k < _len(coordinates))) {
      if (((coordinates[(j) < 0 ? coordinates.length + (j) : j][(Math.trunc(0)) < 0 ? coordinates[(j) < 0 ? coordinates.length + (j) : j].length + (Math.trunc(0)) : Math.trunc(0)] == coordinates[(k) < 0 ? coordinates.length + (k) : k][(Math.trunc(0)) < 0 ? coordinates[(k) < 0 ? coordinates.length + (k) : k].length + (Math.trunc(0)) : Math.trunc(0)]) && (coordinates[(j) < 0 ? coordinates.length + (j) : j][(Math.trunc(1)) < 0 ? coordinates[(j) < 0 ? coordinates.length + (j) : j].length + (Math.trunc(1)) : Math.trunc(1)] == coordinates[(k) < 0 ? coordinates.length + (k) : k][(Math.trunc(1)) < 0 ? coordinates[(k) < 0 ? coordinates.length + (k) : k].length + (Math.trunc(1)) : Math.trunc(1)]))) {
        _panic("The program cannot work out a fitting polynomial.");
      }
      k = (k + 1);
    }
    j = (j + 1);
  }
  let set_x: number[] = [];
  i = 0;
  while ((i < _len(coordinates))) {
    let x_val: number = coordinates[(i) < 0 ? coordinates.length + (i) : i][(Math.trunc(0)) < 0 ? coordinates[(i) < 0 ? coordinates.length + (i) : i].length + (Math.trunc(0)) : Math.trunc(0)];
    if (!contains_int(set_x, x_val)) {
      set_x.push(x_val);
    }
    i = (i + 1);
  }
  if ((_len(set_x) == 1)) {
    return ("x=" + _str(coordinates[(Math.trunc(0)) < 0 ? coordinates.length + (Math.trunc(0)) : Math.trunc(0)][(Math.trunc(0)) < 0 ? coordinates[(Math.trunc(0)) < 0 ? coordinates.length + (Math.trunc(0)) : Math.trunc(0)].length + (Math.trunc(0)) : Math.trunc(0)]));
  }
  if ((_len(set_x) != _len(coordinates))) {
    _panic("The program cannot work out a fitting polynomial.");
  }
  let n: number = _len(coordinates);
  let matrix: number[][] = [];
  let row: number = 0;
  while ((row < n)) {
    let line: number[] = [];
    let col: number = 0;
    while ((col < n)) {
      let power: number = pow_int_float(coordinates[(row) < 0 ? coordinates.length + (row) : row][(Math.trunc(0)) < 0 ? coordinates[(row) < 0 ? coordinates.length + (row) : row].length + (Math.trunc(0)) : Math.trunc(0)], (n - (col + 1)));
      line.push(power);
      col = (col + 1);
    }
    matrix.push(line);
    row = (row + 1);
  }
  let vector: number[] = [];
  row = 0;
  while ((row < n)) {
    vector.push(coordinates[(row) < 0 ? coordinates.length + (row) : row][(Math.trunc(1)) < 0 ? coordinates[(row) < 0 ? coordinates.length + (row) : row].length + (Math.trunc(1)) : Math.trunc(1)]);
    row = (row + 1);
  }
  let count: number = 0;
  while ((count < n)) {
    let _number: number = 0;
    while ((_number < n)) {
      if ((count != _number)) {
        let fraction = (matrix[(_number) < 0 ? matrix.length + (_number) : _number][(count) < 0 ? matrix[(_number) < 0 ? matrix.length + (_number) : _number].length + (count) : count] / matrix[(count) < 0 ? matrix.length + (count) : count][(count) < 0 ? matrix[(count) < 0 ? matrix.length + (count) : count].length + (count) : count]);
        let cc: number = 0;
        while ((cc < n)) {
          matrix[(_number) < 0 ? matrix.length + (_number) : _number][(cc) < 0 ? matrix[(_number) < 0 ? matrix.length + (_number) : _number].length + (cc) : cc] = (matrix[(_number) < 0 ? matrix.length + (_number) : _number][(cc) < 0 ? matrix[(_number) < 0 ? matrix.length + (_number) : _number].length + (cc) : cc] - (matrix[(count) < 0 ? matrix.length + (count) : count][(cc) < 0 ? matrix[(count) < 0 ? matrix.length + (count) : count].length + (cc) : cc] * fraction));
          cc = (cc + 1);
        }
        vector[(_number) < 0 ? vector.length + (_number) : _number] = (vector[(_number) < 0 ? vector.length + (_number) : _number] - (vector[(count) < 0 ? vector.length + (count) : count] * fraction));
      }
      _number = (_number + 1);
    }
    count = (count + 1);
  }
  let solution: string[] = [];
  count = 0;
  while ((count < n)) {
    let value = (vector[(count) < 0 ? vector.length + (count) : count] / matrix[(count) < 0 ? matrix.length + (count) : count][(count) < 0 ? matrix[(count) < 0 ? matrix.length + (count) : count].length + (count) : count]);
    solution.push(_str(value));
    count = (count + 1);
  }
  let solved: string = "f(x)=";
  count = 0;
  while ((count < n)) {
    let parts = solution[(count) < 0 ? solution.length + (count) : count].split("e");
    let coeff = solution[(count) < 0 ? solution.length + (count) : count];
    if ((_len(parts) > 1)) {
      coeff = ((parts[(Math.trunc(0)) < 0 ? parts.length + (Math.trunc(0)) : Math.trunc(0)] + "*10^") + parts[(Math.trunc(1)) < 0 ? parts.length + (Math.trunc(1)) : Math.trunc(1)]);
    }
    solved = ((((solved + "x^") + _str((n - (count + 1)))) + "*") + coeff);
    if (((count + 1) != n)) {
      solved = (solved + "+");
    }
    count = (count + 1);
  }
  return solved;
}
function main() {
  console.log(_str(points_to_polynomial([[1, 0], [2, 0], [3, 0]])));
  console.log(_str(points_to_polynomial([[1, 1], [2, 1], [3, 1]])));
  console.log(_str(points_to_polynomial([[1, 1], [2, 4], [3, 9]])));
  console.log(_str(points_to_polynomial([[1, 3], [2, 6], [3, 11]])));
  console.log(_str(points_to_polynomial([[1, -3], [2, -6], [3, -11]])));
  console.log(_str(points_to_polynomial([[1, 1], [1, 2], [1, 3]])));
  console.log(_str(points_to_polynomial([[1, 5], [2, 2], [3, 9]])));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

