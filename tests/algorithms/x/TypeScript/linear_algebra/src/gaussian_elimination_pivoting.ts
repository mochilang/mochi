// Generated by Mochi v0.10.59 on 2025-08-06 23:29:26 GMT+7

function panic(msg: string) {
  console.log(_str(msg));
}
function abs_float(x: number): number {
  if ((x < 0.0)) {
    return -x;
  }
  return x;
}
function copy_matrix(src: number[][]): number[][] {
  let res: number[][] = [];
  let i: number = 0;
  while ((i < _len(src))) {
    let row_src: number[] = src[(i) < 0 ? src.length + (i) : i];
    let row: number[] = [];
    let j: number = 0;
    while ((j < _len(row_src))) {
      row.push(row_src[(j) < 0 ? row_src.length + (j) : j]);
      j = (j + 1);
    }
    res.push(row);
    i = (i + 1);
  }
  return res;
}
function solve_linear_system(matrix: number[][]): number[] {
  let ab: number[][] = copy_matrix(matrix);
  let num_rows: number = _len(ab);
  let num_cols: number = (_len(ab[(Math.trunc(0)) < 0 ? ab.length + (Math.trunc(0)) : Math.trunc(0)]) - 1);
  if ((num_rows != num_cols)) {
    panic("Matrix is not square");
    return [];
  }
  let column_num: number = 0;
  while ((column_num < num_rows)) {
    let i: number = column_num;
    while ((i < num_cols)) {
      if ((abs_float(ab[(i) < 0 ? ab.length + (i) : i][(column_num) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (column_num) : column_num]) > abs_float(ab[(column_num) < 0 ? ab.length + (column_num) : column_num][(column_num) < 0 ? ab[(column_num) < 0 ? ab.length + (column_num) : column_num].length + (column_num) : column_num]))) {
        let temp: number[] = ab[(column_num) < 0 ? ab.length + (column_num) : column_num];
        ab[(column_num) < 0 ? ab.length + (column_num) : column_num] = ab[(i) < 0 ? ab.length + (i) : i];
        ab[(i) < 0 ? ab.length + (i) : i] = temp;
      }
      i = (i + 1);
    }
    if ((abs_float(ab[(column_num) < 0 ? ab.length + (column_num) : column_num][(column_num) < 0 ? ab[(column_num) < 0 ? ab.length + (column_num) : column_num].length + (column_num) : column_num]) < 1e-08)) {
      panic("Matrix is singular");
      return [];
    }
    if ((column_num != 0)) {
      i = column_num;
      while ((i < num_rows)) {
        let factor: number = (ab[(i) < 0 ? ab.length + (i) : i][(Math.trunc((column_num - 1))) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))] / ab[(Math.trunc((column_num - 1))) < 0 ? ab.length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))][(Math.trunc((column_num - 1))) < 0 ? ab[(Math.trunc((column_num - 1))) < 0 ? ab.length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))].length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))]);
        let j: number = 0;
        while ((j < _len(ab[(i) < 0 ? ab.length + (i) : i]))) {
          ab[(i) < 0 ? ab.length + (i) : i][(j) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (j) : j] = (ab[(i) < 0 ? ab.length + (i) : i][(j) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (j) : j] - (factor * ab[(Math.trunc((column_num - 1))) < 0 ? ab.length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))][(j) < 0 ? ab[(Math.trunc((column_num - 1))) < 0 ? ab.length + (Math.trunc((column_num - 1))) : Math.trunc((column_num - 1))].length + (j) : j]));
          j = (j + 1);
        }
        i = (i + 1);
      }
    }
    column_num = (column_num + 1);
  }
  let x_lst: number[] = [];
  let t: number = 0;
  while ((t < num_rows)) {
    x_lst.push(0.0);
    t = (t + 1);
  }
  column_num = (num_rows - 1);
  while ((column_num >= 0)) {
    let x: number = (ab[(column_num) < 0 ? ab.length + (column_num) : column_num][(num_cols) < 0 ? ab[(column_num) < 0 ? ab.length + (column_num) : column_num].length + (num_cols) : num_cols] / ab[(column_num) < 0 ? ab.length + (column_num) : column_num][(column_num) < 0 ? ab[(column_num) < 0 ? ab.length + (column_num) : column_num].length + (column_num) : column_num]);
    x_lst[(column_num) < 0 ? x_lst.length + (column_num) : column_num] = x;
    let i: number = (column_num - 1);
    while ((i >= 0)) {
      ab[(i) < 0 ? ab.length + (i) : i][(num_cols) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (num_cols) : num_cols] = (ab[(i) < 0 ? ab.length + (i) : i][(num_cols) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (num_cols) : num_cols] - (ab[(i) < 0 ? ab.length + (i) : i][(column_num) < 0 ? ab[(i) < 0 ? ab.length + (i) : i].length + (column_num) : column_num] * x));
      i = (i - 1);
    }
    column_num = (column_num - 1);
  }
  return x_lst;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let example_matrix: number[][]
let solution: number[]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
example_matrix = [[5.0, -5.0, -3.0, 4.0, -11.0], [1.0, -4.0, 6.0, -4.0, -10.0], [-2.0, -5.0, 4.0, -5.0, -12.0], [-3.0, -3.0, 5.0, -5.0, 8.0]]
  console.log(_str("Matrix:"));
  console.log(_str(_str(example_matrix)));
solution = solve_linear_system(example_matrix)
  console.log(_str(_str(solution)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

