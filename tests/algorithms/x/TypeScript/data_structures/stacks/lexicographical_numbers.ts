// Generated by Mochi v0.10.61 on 2025-08-08 17:04:07 GMT+7

function lexical_order(max_number: number): number[] {
  let result: number[] = [];
  let stack: number[] = [1];
  while ((_len(stack) > 0)) {
    let idx: number = (_len(stack) - 1);
    let num = stack[(()=>{const _mochi_idx = idx; return _mochi_idx < 0 ? stack.length + _mochi_idx : _mochi_idx;})()];
    stack = stack.slice(0, idx);
    if ((num > max_number)) {
      continue
    }
    result.push(num);
    if (((num % 10) != 9)) {
      stack.push((num + 1));
    }
    stack.push((num * 10));
  }
  return result;
}
function join_ints(xs: number[]): string {
  let res: string = "";
  let i: number = 0;
  while ((i < _len(xs))) {
    if ((i > 0)) {
      res = (res + " ");
    }
    res = (res + _str(xs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? xs.length + _mochi_idx : _mochi_idx;})()]));
    i = (i + 1);
  }
  return res;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(join_ints(lexical_order(13))));
  console.log(_str(_str(lexical_order(1))));
  console.log(_str(join_ints(lexical_order(20))));
  console.log(_str(join_ints(lexical_order(25))));
  console.log(_str(_str(lexical_order(12))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

