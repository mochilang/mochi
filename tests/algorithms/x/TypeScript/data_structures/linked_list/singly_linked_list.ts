// Generated by Mochi v0.10.59 on 2025-08-06 21:31:15 GMT+7

export interface SinglyLinkedList { data: number[] }
function empty_list(): SinglyLinkedList {
  return {"data": []};
}
function length(list: SinglyLinkedList): number {
  return _len(list.data);
}
function is_empty(list: SinglyLinkedList): boolean {
  return (_len(list.data) == 0);
}
function to_string(list: SinglyLinkedList): string {
  if ((_len(list.data) == 0)) {
    return "";
  }
  let s: string = _str(list.data[Math.trunc(0)]);
  let i: number = 1;
  while ((i < _len(list.data))) {
    s = ((s + " -> ") + _str(list.data[i]));
    i = (i + 1);
  }
  return s;
}
function insert_nth(list: SinglyLinkedList, index: number, value: number): SinglyLinkedList {
  if (((index < 0) || (index > _len(list.data)))) {
    _panic("index out of range");
  }
  let res: number[] = [];
  let i: number = 0;
  while ((i < index)) {
    res.push(list.data[i]);
    i = (i + 1);
  }
  res.push(value);
  while ((i < _len(list.data))) {
    res.push(list.data[i]);
    i = (i + 1);
  }
  return {"data": res};
}
function insert_head(list: SinglyLinkedList, value: number): SinglyLinkedList {
  return insert_nth(list, 0, value);
}
function insert_tail(list: SinglyLinkedList, value: number): SinglyLinkedList {
  return insert_nth(list, _len(list.data), value);
}
export interface DeleteResult { list: SinglyLinkedList; value: number }
function delete_nth(list: SinglyLinkedList, index: number): DeleteResult {
  if (((index < 0) || (index >= _len(list.data)))) {
    _panic("index out of range");
  }
  let res: number[] = [];
  let val: number = 0;
  let i: number = 0;
  while ((i < _len(list.data))) {
    if ((i == index)) {
      val = list.data[i];
    } else {
      res.push(list.data[i]);
    }
    i = (i + 1);
  }
  return {"list": {"data": res}, "value": val};
}
function delete_head(list: SinglyLinkedList): DeleteResult {
  return delete_nth(list, 0);
}
function delete_tail(list: SinglyLinkedList): DeleteResult {
  return delete_nth(list, (_len(list.data) - 1));
}
function get_item(list: SinglyLinkedList, index: number): number {
  if (((index < 0) || (index >= _len(list.data)))) {
    _panic("index out of range");
  }
  return list.data[index];
}
function set_item(list: SinglyLinkedList, index: number, value: number): SinglyLinkedList {
  if (((index < 0) || (index >= _len(list.data)))) {
    _panic("index out of range");
  }
  let res: number[] = [];
  let i: number = 0;
  while ((i < _len(list.data))) {
    if ((i == index)) {
      res.push(value);
    } else {
      res.push(list.data[i]);
    }
    i = (i + 1);
  }
  return {"data": res};
}
function reverse_list(list: SinglyLinkedList): SinglyLinkedList {
  let res: number[] = [];
  let i: number = (_len(list.data) - 1);
  while ((i >= 0)) {
    res.push(list.data[i]);
    i = (i - 1);
  }
  return {"data": res};
}
function main() {
  let lst: SinglyLinkedList = empty_list();
  let i: number = 1;
  while ((i <= 5)) {
    lst = insert_tail(lst, i);
    i = (i + 1);
  }
  console.log(_str(to_string(lst)));
  lst = insert_head(lst, 0);
  console.log(_str(to_string(lst)));
  let del: DeleteResult = delete_head(lst);
  lst = del.list;
  console.log(_str(_str(del.value)));
  del = delete_tail(lst);
  lst = del.list;
  console.log(_str(_str(del.value)));
  del = delete_nth(lst, 2);
  lst = del.list;
  console.log(_str(_str(del.value)));
  lst = set_item(lst, 1, 99);
  console.log(_str(_str(get_item(lst, 1))));
  lst = reverse_list(lst);
  console.log(_str(to_string(lst)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

