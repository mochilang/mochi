// Generated by Mochi v0.10.59 on 2025-08-07 00:03:02 GMT+7

function insertion_sort(a: number[]): number[] {
  let i: number = 1;
  while ((i < _len(a))) {
    let key: number = a[(i) < 0 ? a.length + (i) : i];
    let j: number = (i - 1);
    while (((j >= 0) && (a[(j) < 0 ? a.length + (j) : j] > key))) {
      a[((j + 1)) < 0 ? a.length + ((j + 1)) : (j + 1)] = a[(j) < 0 ? a.length + (j) : j];
      j = (j - 1);
    }
    a[((j + 1)) < 0 ? a.length + ((j + 1)) : (j + 1)] = key;
    i = (i + 1);
  }
  return a;
}
function minimum_waiting_time(queries: number[]): number {
  let n: number = _len(queries);
  if (((n == 0) || (n == 1))) {
    return 0;
  }
  let sorted: number[] = insertion_sort(queries);
  let total: number = 0;
  let i: number = 0;
  while ((i < n)) {
    total = (total + (sorted[(i) < 0 ? sorted.length + (i) : i] * ((n - i) - 1)));
    i = (i + 1);
  }
  return total;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(minimum_waiting_time([3, 2, 1, 2, 6])));
  console.log(_str(minimum_waiting_time([3, 2, 1])));
  console.log(_str(minimum_waiting_time([1, 2, 3, 4])));
  console.log(_str(minimum_waiting_time([5, 5, 5, 5])));
  console.log(_str(minimum_waiting_time([])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

