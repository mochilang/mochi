// Generated by Mochi v0.10.59 on 2025-08-07 00:02:03 GMT+7

let tree: Record<number, number[]> = {};
function dfs(start: number, visited: Record<number, boolean>): number[] {
  let size: number = 1;
  let cuts: number = 0;
  visited[(start) < 0 ? visited.length + (start) : start] = true;
  for (const v of tree[(start) < 0 ? tree.length + (start) : start]) {
    if (!(v in visited)) {
      let res: number[] = dfs(v, visited);
      size = (size + res[(Math.trunc(0)) < 0 ? res.length + (Math.trunc(0)) : Math.trunc(0)]);
      cuts = (cuts + res[(Math.trunc(1)) < 0 ? res.length + (Math.trunc(1)) : Math.trunc(1)]);
    }
  }
  if (((size % 2) == 0)) {
    cuts = (cuts + 1);
  }
  return [size, cuts];
}
function even_tree(): number {
  let visited: Record<number, boolean> = {};
  let res: number[] = dfs(1, visited);
  return (res[(Math.trunc(1)) < 0 ? res.length + (Math.trunc(1)) : Math.trunc(1)] - 1);
}
function main() {
  let edges: number[][] = [[2, 1], [3, 1], [4, 3], [5, 2], [6, 1], [7, 2], [8, 6], [9, 8], [10, 8]];
  let i: number = 0;
  while ((i < _len(edges))) {
    let u: number = edges[(i) < 0 ? edges.length + (i) : i][(Math.trunc(0)) < 0 ? edges[(i) < 0 ? edges.length + (i) : i].length + (Math.trunc(0)) : Math.trunc(0)];
    let v: number = edges[(i) < 0 ? edges.length + (i) : i][(Math.trunc(1)) < 0 ? edges[(i) < 0 ? edges.length + (i) : i].length + (Math.trunc(1)) : Math.trunc(1)];
    if (!(u in tree)) {
      tree[(u) < 0 ? tree.length + (u) : u] = [];
    }
    if (!(v in tree)) {
      tree[(v) < 0 ? tree.length + (v) : v] = [];
    }
    tree[(u) < 0 ? tree.length + (u) : u] = [...tree[(u) < 0 ? tree.length + (u) : u], v];
    tree[(v) < 0 ? tree.length + (v) : v] = [...tree[(v) < 0 ? tree.length + (v) : v], u];
    i = (i + 1);
  }
  console.log(_str(_str(even_tree())));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

