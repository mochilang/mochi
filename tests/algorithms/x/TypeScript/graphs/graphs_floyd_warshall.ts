// Generated by Mochi v0.10.59 on 2025-08-07 00:02:16 GMT+7

let INF: number = 1000000000.0;
function floyd_warshall(graph: number[][]): number[][] {
  let v: number = _len(graph);
  let dist: number[][] = [];
  let i: number = 0;
  while ((i < v)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < v)) {
      row.push(graph[(i) < 0 ? graph.length + (i) : i][(j) < 0 ? graph[(i) < 0 ? graph.length + (i) : i].length + (j) : j]);
      j = (j + 1);
    }
    dist.push(row);
    i = (i + 1);
  }
  let k: number = 0;
  while ((k < v)) {
    let i: number = 0;
    while ((i < v)) {
      let j: number = 0;
      while ((j < v)) {
        if ((((dist[(i) < 0 ? dist.length + (i) : i][(k) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (k) : k] < INF) && (dist[(k) < 0 ? dist.length + (k) : k][(j) < 0 ? dist[(k) < 0 ? dist.length + (k) : k].length + (j) : j] < INF)) && ((dist[(i) < 0 ? dist.length + (i) : i][(k) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (k) : k] + dist[(k) < 0 ? dist.length + (k) : k][(j) < 0 ? dist[(k) < 0 ? dist.length + (k) : k].length + (j) : j]) < dist[(i) < 0 ? dist.length + (i) : i][(j) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (j) : j]))) {
          dist[(i) < 0 ? dist.length + (i) : i][(j) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (j) : j] = (dist[(i) < 0 ? dist.length + (i) : i][(k) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (k) : k] + dist[(k) < 0 ? dist.length + (k) : k][(j) < 0 ? dist[(k) < 0 ? dist.length + (k) : k].length + (j) : j]);
        }
        j = (j + 1);
      }
      i = (i + 1);
    }
    k = (k + 1);
  }
  return dist;
}
function print_dist(dist: number[][]) {
  console.log(_str("\nThe shortest path matrix using Floyd Warshall algorithm\n"));
  let i: number = 0;
  while ((i < _len(dist))) {
    let j: number = 0;
    let line: string = "";
    while ((j < _len(dist[(i) < 0 ? dist.length + (i) : i]))) {
      if ((dist[(i) < 0 ? dist.length + (i) : i][(j) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (j) : j] >= (INF / 2.0))) {
        line = (line + "INF\t");
      } else {
        line = ((line + _str(Math.trunc(dist[(i) < 0 ? dist.length + (i) : i][(j) < 0 ? dist[(i) < 0 ? dist.length + (i) : i].length + (j) : j]))) + "\t");
      }
      j = (j + 1);
    }
    console.log(_str(line));
    i = (i + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let graph: number[][]
let result: number[][]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
graph = [[0.0, 5.0, INF, 10.0], [INF, 0.0, 3.0, INF], [INF, INF, 0.0, 1.0], [INF, INF, INF, 0.0]]
result = floyd_warshall(graph)
  print_dist(result);
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

