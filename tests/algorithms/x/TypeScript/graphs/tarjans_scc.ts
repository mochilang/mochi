// Generated by Mochi v0.10.59 on 2025-08-06 23:28:54 GMT+7

function tarjan(g: number[][]): number[][] {
  let n: number = _len(g);
  let stack: number[] = [];
  let on_stack: boolean[] = [];
  let index_of: number[] = [];
  let lowlink_of: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    on_stack.push(false);
    index_of.push(-1);
    lowlink_of.push(-1);
    i = (i + 1);
  }
  let components: number[][] = [];
  function strong_connect(v: number, index: number): number {
    index_of[(v) < 0 ? index_of.length + (v) : v] = index;
    lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v] = index;
    let current_index: number = (index + 1);
    stack.push(v);
    on_stack[(v) < 0 ? on_stack.length + (v) : v] = true;
    for (const w of g[(v) < 0 ? g.length + (v) : v]) {
      if ((index_of[(w) < 0 ? index_of.length + (w) : w] == -1)) {
        current_index = strong_connect(w, current_index);
        if ((lowlink_of[(w) < 0 ? lowlink_of.length + (w) : w] < lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v])) {
          lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v] = lowlink_of[(w) < 0 ? lowlink_of.length + (w) : w];
        }
      } else {
        if (on_stack[(w) < 0 ? on_stack.length + (w) : w]) {
          if ((lowlink_of[(w) < 0 ? lowlink_of.length + (w) : w] < lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v])) {
            lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v] = lowlink_of[(w) < 0 ? lowlink_of.length + (w) : w];
          }
        }
      }
    }
    if ((lowlink_of[(v) < 0 ? lowlink_of.length + (v) : v] == index_of[(v) < 0 ? index_of.length + (v) : v])) {
      let component: number[] = [];
      let w: number = stack[(Math.trunc((_len(stack) - 1))) < 0 ? stack.length + (Math.trunc((_len(stack) - 1))) : Math.trunc((_len(stack) - 1))];
      stack = stack.slice(0, (_len(stack) - 1));
      on_stack[(w) < 0 ? on_stack.length + (w) : w] = false;
      component.push(w);
      while ((w != v)) {
        w = stack[(Math.trunc((_len(stack) - 1))) < 0 ? stack.length + (Math.trunc((_len(stack) - 1))) : Math.trunc((_len(stack) - 1))];
        stack = stack.slice(0, (_len(stack) - 1));
        on_stack[(w) < 0 ? on_stack.length + (w) : w] = false;
        component.push(w);
      }
      components.push(component);
    }
    return current_index;
  }
  let v: number = 0;
  while ((v < n)) {
    if ((index_of[(v) < 0 ? index_of.length + (v) : v] == -1)) {
      strong_connect(v, 0);
    }
    v = (v + 1);
  }
  return components;
}
function create_graph(n: number, edges: number[][]): number[][] {
  let g: number[][] = [];
  let i: number = 0;
  while ((i < n)) {
    g.push([]);
    i = (i + 1);
  }
  for (const e of edges) {
    let u: number = e[(Math.trunc(0)) < 0 ? e.length + (Math.trunc(0)) : Math.trunc(0)];
    let v: number = e[(Math.trunc(1)) < 0 ? e.length + (Math.trunc(1)) : Math.trunc(1)];
    g[(u) < 0 ? g.length + (u) : u] = [...g[(u) < 0 ? g.length + (u) : u], v];
  }
  return g;
}
function main() {
  let n_vertices: number = 7;
  let source: number[] = [0, 0, 1, 2, 3, 3, 4, 4, 6];
  let target: number[] = [1, 3, 2, 0, 1, 4, 5, 6, 5];
  let edges: number[][] = [];
  let i: number = 0;
  while ((i < _len(source))) {
    edges.push([source[(i) < 0 ? source.length + (i) : i], target[(i) < 0 ? target.length + (i) : i]]);
    i = (i + 1);
  }
  let g: number[][] = create_graph(n_vertices, edges);
  console.log(_str(_str(tarjan(g))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

