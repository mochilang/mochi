// Generated by Mochi v0.10.59 on 2025-08-06 23:28:38 GMT+7

function longest_distance(graph: number[][]): number {
  let n: number = _len(graph);
  let indegree: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    indegree.push(0);
    i = (i + 1);
  }
  let long_dist: number[] = [];
  let j: number = 0;
  while ((j < n)) {
    long_dist.push(1);
    j = (j + 1);
  }
  let u: number = 0;
  while ((u < n)) {
    for (const v of graph[(u) < 0 ? graph.length + (u) : u]) {
      indegree[(v) < 0 ? indegree.length + (v) : v] = (indegree[(v) < 0 ? indegree.length + (v) : v] + 1);
    }
    u = (u + 1);
  }
  let queue: number[] = [];
  let head: number = 0;
  let k: number = 0;
  while ((k < n)) {
    if ((indegree[(k) < 0 ? indegree.length + (k) : k] == 0)) {
      queue.push(k);
    }
    k = (k + 1);
  }
  while ((head < _len(queue))) {
    let vertex: number = queue[(head) < 0 ? queue.length + (head) : head];
    head = (head + 1);
    for (const x of graph[(vertex) < 0 ? graph.length + (vertex) : vertex]) {
      indegree[(x) < 0 ? indegree.length + (x) : x] = (indegree[(x) < 0 ? indegree.length + (x) : x] - 1);
      let new_dist: number = (long_dist[(vertex) < 0 ? long_dist.length + (vertex) : vertex] + 1);
      if ((new_dist > long_dist[(x) < 0 ? long_dist.length + (x) : x])) {
        long_dist[(x) < 0 ? long_dist.length + (x) : x] = new_dist;
      }
      if ((indegree[(x) < 0 ? indegree.length + (x) : x] == 0)) {
        queue.push(x);
      }
    }
  }
  let max_len: number = long_dist[(Math.trunc(0)) < 0 ? long_dist.length + (Math.trunc(0)) : Math.trunc(0)];
  let m: number = 1;
  while ((m < n)) {
    if ((long_dist[(m) < 0 ? long_dist.length + (m) : m] > max_len)) {
      max_len = long_dist[(m) < 0 ? long_dist.length + (m) : m];
    }
    m = (m + 1);
  }
  return max_len;
}
let graph: number[][] = [[2, 3, 4], [2, 7], [5], [5, 7], [7], [6], [7], []];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(longest_distance(graph)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

