// Generated by Mochi v0.10.59 on 2025-08-07 00:02:25 GMT+7

let seed: number = 123456789;
function rand(): number {
  seed = (((seed * 1103515245) + 12345) % 2147483648);
  return seed;
}
function random(): number {
  return ((1.0 * rand()) / 2147483648.0);
}
function sqrtApprox(x: number): number {
  if ((x <= 0.0)) {
    return 0.0;
  }
  let guess: number = x;
  let i: number = 0;
  while ((i < 20)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function absf(x: number): number {
  return ((x < 0.0) ? -x : x);
}
function dot(a: number[], b: number[]): number {
  let s: number = 0.0;
  let i: number = 0;
  while ((i < _len(a))) {
    s = (s + (a[(i) < 0 ? a.length + (i) : i] * b[(i) < 0 ? b.length + (i) : i]));
    i = (i + 1);
  }
  return s;
}
function vector_scale(v: number[], s: number): number[] {
  let res: number[] = [];
  let i: number = 0;
  while ((i < _len(v))) {
    res.push((v[(i) < 0 ? v.length + (i) : i] * s));
    i = (i + 1);
  }
  return res;
}
function vector_sub(a: number[], b: number[]): number[] {
  let res: number[] = [];
  let i: number = 0;
  while ((i < _len(a))) {
    res.push((a[(i) < 0 ? a.length + (i) : i] - b[(i) < 0 ? b.length + (i) : i]));
    i = (i + 1);
  }
  return res;
}
function vector_add(a: number[], b: number[]): number[] {
  let res: number[] = [];
  let i: number = 0;
  while ((i < _len(a))) {
    res.push((a[(i) < 0 ? a.length + (i) : i] + b[(i) < 0 ? b.length + (i) : i]));
    i = (i + 1);
  }
  return res;
}
function zeros_matrix(r: number, c: number): number[][] {
  let m: number[][] = [];
  let i: number = 0;
  while ((i < r)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < c)) {
      row.push(0.0);
      j = (j + 1);
    }
    m.push(row);
    i = (i + 1);
  }
  return m;
}
export interface LanczosResult { t: number[][]; q: number[][] }
export interface EigenResult { values: number[]; vectors: number[][] }
function column(m: number[][], idx: number): number[] {
  let col: number[] = [];
  let i: number = 0;
  while ((i < _len(m))) {
    col.push(m[(i) < 0 ? m.length + (i) : i][(idx) < 0 ? m[(i) < 0 ? m.length + (i) : i].length + (idx) : idx]);
    i = (i + 1);
  }
  return col;
}
function validate_adjacency_list(graph: number[][]) {
  let i: number = 0;
  while ((i < _len(graph))) {
    let j: number = 0;
    while ((j < _len(graph[(i) < 0 ? graph.length + (i) : i]))) {
      let v: number = graph[(i) < 0 ? graph.length + (i) : i][(j) < 0 ? graph[(i) < 0 ? graph.length + (i) : i].length + (j) : j];
      if (((v < 0) || (v >= _len(graph)))) {
        _panic("Invalid neighbor");
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
}
function multiply_matrix_vector(graph: number[][], vector: number[]): number[] {
  let n: number = _len(graph);
  if ((_len(vector) != n)) {
    _panic("Vector length must match number of nodes");
  }
  let result: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    let sum: number = 0.0;
    let j: number = 0;
    while ((j < _len(graph[(i) < 0 ? graph.length + (i) : i]))) {
      let nb: number = graph[(i) < 0 ? graph.length + (i) : i][(j) < 0 ? graph[(i) < 0 ? graph.length + (i) : i].length + (j) : j];
      sum = (sum + vector[(nb) < 0 ? vector.length + (nb) : nb]);
      j = (j + 1);
    }
    result.push(sum);
    i = (i + 1);
  }
  return result;
}
function lanczos_iteration(graph: number[][], k: number): LanczosResult {
  let n: number = _len(graph);
  if (((k < 1) || (k > n))) {
    _panic("invalid number of eigenvectors");
  }
  let q: number[][] = zeros_matrix(n, k);
  let t: number[][] = zeros_matrix(k, k);
  let v: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    v.push(random());
    i = (i + 1);
  }
  let ss: number = 0.0;
  i = 0;
  while ((i < n)) {
    ss = (ss + (v[(i) < 0 ? v.length + (i) : i] * v[(i) < 0 ? v.length + (i) : i]));
    i = (i + 1);
  }
  let vnorm: number = sqrtApprox(ss);
  i = 0;
  while ((i < n)) {
    q[(i) < 0 ? q.length + (i) : i][(0) < 0 ? q[(i) < 0 ? q.length + (i) : i].length + (0) : 0] = (v[(i) < 0 ? v.length + (i) : i] / vnorm);
    i = (i + 1);
  }
  let beta: number = 0.0;
  let j: number = 0;
  while ((j < k)) {
    let w: number[] = multiply_matrix_vector(graph, column(q, j));
    if ((j > 0)) {
      w = vector_sub(w, vector_scale(column(q, (j - 1)), beta));
    }
    let alpha: number = dot(column(q, j), w);
    w = vector_sub(w, vector_scale(column(q, j), alpha));
    let ss2: number = 0.0;
    let p: number = 0;
    while ((p < n)) {
      ss2 = (ss2 + (w[(p) < 0 ? w.length + (p) : p] * w[(p) < 0 ? w.length + (p) : p]));
      p = (p + 1);
    }
    beta = sqrtApprox(ss2);
    t[(j) < 0 ? t.length + (j) : j][(j) < 0 ? t[(j) < 0 ? t.length + (j) : j].length + (j) : j] = alpha;
    if ((j < (k - 1))) {
      t[(j) < 0 ? t.length + (j) : j][((j + 1)) < 0 ? t[(j) < 0 ? t.length + (j) : j].length + ((j + 1)) : (j + 1)] = beta;
      t[((j + 1)) < 0 ? t.length + ((j + 1)) : (j + 1)][(j) < 0 ? t[((j + 1)) < 0 ? t.length + ((j + 1)) : (j + 1)].length + (j) : j] = beta;
      if ((beta > 1e-10)) {
        let wnorm: number[] = vector_scale(w, (1.0 / beta));
        let r: number = 0;
        while ((r < n)) {
          q[(r) < 0 ? q.length + (r) : r][((j + 1)) < 0 ? q[(r) < 0 ? q.length + (r) : r].length + ((j + 1)) : (j + 1)] = wnorm[(r) < 0 ? wnorm.length + (r) : r];
          r = (r + 1);
        }
      }
    }
    j = (j + 1);
  }
  return {t, q};
}
function jacobi_eigen(a_in: number[][], max_iter: number): EigenResult {
  let n: number = _len(a_in);
  let a: number[][] = a_in;
  let v: number[][] = zeros_matrix(n, n);
  let i: number = 0;
  while ((i < n)) {
    v[(i) < 0 ? v.length + (i) : i][(i) < 0 ? v[(i) < 0 ? v.length + (i) : i].length + (i) : i] = 1.0;
    i = (i + 1);
  }
  let iter: number = 0;
  while ((iter < max_iter)) {
    let p: number = 0;
    let q: number = 1;
    let max: number = absf(a[(p) < 0 ? a.length + (p) : p][(q) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (q) : q]);
    i = 0;
    while ((i < n)) {
      let j: number = (i + 1);
      while ((j < n)) {
        let val: number = absf(a[(i) < 0 ? a.length + (i) : i][(j) < 0 ? a[(i) < 0 ? a.length + (i) : i].length + (j) : j]);
        if ((val > max)) {
          max = val;
          p = i;
          q = j;
        }
        j = (j + 1);
      }
      i = (i + 1);
    }
    if ((max < 1e-08)) {
      break
    }
    let app: number = a[(p) < 0 ? a.length + (p) : p][(p) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (p) : p];
    let aqq: number = a[(q) < 0 ? a.length + (q) : q][(q) < 0 ? a[(q) < 0 ? a.length + (q) : q].length + (q) : q];
    let apq: number = a[(p) < 0 ? a.length + (p) : p][(q) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (q) : q];
    let theta: number = ((aqq - app) / (2.0 * apq));
    let t: number = (1.0 / (absf(theta) + sqrtApprox(((theta * theta) + 1.0))));
    if ((theta < 0.0)) {
      t = -t;
    }
    let c: number = (1.0 / sqrtApprox((1.0 + (t * t))));
    let s: number = (t * c);
    let tau: number = (s / (1.0 + c));
    a[(p) < 0 ? a.length + (p) : p][(p) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (p) : p] = (app - (t * apq));
    a[(q) < 0 ? a.length + (q) : q][(q) < 0 ? a[(q) < 0 ? a.length + (q) : q].length + (q) : q] = (aqq + (t * apq));
    a[(p) < 0 ? a.length + (p) : p][(q) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (q) : q] = 0.0;
    a[(q) < 0 ? a.length + (q) : q][(p) < 0 ? a[(q) < 0 ? a.length + (q) : q].length + (p) : p] = 0.0;
    let k: number = 0;
    while ((k < n)) {
      if (((k != p) && (k != q))) {
        let akp: number = a[(k) < 0 ? a.length + (k) : k][(p) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (p) : p];
        let akq: number = a[(k) < 0 ? a.length + (k) : k][(q) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (q) : q];
        a[(k) < 0 ? a.length + (k) : k][(p) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (p) : p] = (akp - (s * (akq + (tau * akp))));
        a[(p) < 0 ? a.length + (p) : p][(k) < 0 ? a[(p) < 0 ? a.length + (p) : p].length + (k) : k] = a[(k) < 0 ? a.length + (k) : k][(p) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (p) : p];
        a[(k) < 0 ? a.length + (k) : k][(q) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (q) : q] = (akq + (s * (akp - (tau * akq))));
        a[(q) < 0 ? a.length + (q) : q][(k) < 0 ? a[(q) < 0 ? a.length + (q) : q].length + (k) : k] = a[(k) < 0 ? a.length + (k) : k][(q) < 0 ? a[(k) < 0 ? a.length + (k) : k].length + (q) : q];
      }
      k = (k + 1);
    }
    k = 0;
    while ((k < n)) {
      let vkp: number = v[(k) < 0 ? v.length + (k) : k][(p) < 0 ? v[(k) < 0 ? v.length + (k) : k].length + (p) : p];
      let vkq: number = v[(k) < 0 ? v.length + (k) : k][(q) < 0 ? v[(k) < 0 ? v.length + (k) : k].length + (q) : q];
      v[(k) < 0 ? v.length + (k) : k][(p) < 0 ? v[(k) < 0 ? v.length + (k) : k].length + (p) : p] = (vkp - (s * (vkq + (tau * vkp))));
      v[(k) < 0 ? v.length + (k) : k][(q) < 0 ? v[(k) < 0 ? v.length + (k) : k].length + (q) : q] = (vkq + (s * (vkp - (tau * vkq))));
      k = (k + 1);
    }
    iter = (iter + 1);
  }
  let eigenvalues: number[] = [];
  i = 0;
  while ((i < n)) {
    eigenvalues.push(a[(i) < 0 ? a.length + (i) : i][(i) < 0 ? a[(i) < 0 ? a.length + (i) : i].length + (i) : i]);
    i = (i + 1);
  }
  return {"values": eigenvalues, "vectors": v};
}
function matmul(a: number[][], b: number[][]): number[][] {
  let rows: number = _len(a);
  let cols: number = _len(b[(Math.trunc(0)) < 0 ? b.length + (Math.trunc(0)) : Math.trunc(0)]);
  let inner: number = _len(b);
  let m: number[][] = zeros_matrix(rows, cols);
  let i: number = 0;
  while ((i < rows)) {
    let j: number = 0;
    while ((j < cols)) {
      let s: number = 0.0;
      let k: number = 0;
      while ((k < inner)) {
        s = (s + (a[(i) < 0 ? a.length + (i) : i][(k) < 0 ? a[(i) < 0 ? a.length + (i) : i].length + (k) : k] * b[(k) < 0 ? b.length + (k) : k][(j) < 0 ? b[(k) < 0 ? b.length + (k) : k].length + (j) : j]));
        k = (k + 1);
      }
      m[(i) < 0 ? m.length + (i) : i][(j) < 0 ? m[(i) < 0 ? m.length + (i) : i].length + (j) : j] = s;
      j = (j + 1);
    }
    i = (i + 1);
  }
  return m;
}
function sort_eigenpairs(vals: number[], vecs: number[][]): EigenResult {
  let n: number = _len(vals);
  let values: number[] = vals;
  let vectors: number[][] = vecs;
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < (n - 1))) {
      if ((values[(j) < 0 ? values.length + (j) : j] < values[(Math.trunc((j + 1))) < 0 ? values.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))])) {
        let tmp = values[(j) < 0 ? values.length + (j) : j];
        values[(j) < 0 ? values.length + (j) : j] = values[(Math.trunc((j + 1))) < 0 ? values.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
        values[((j + 1)) < 0 ? values.length + ((j + 1)) : (j + 1)] = tmp;
        let r: number = 0;
        while ((r < _len(vectors))) {
          let tv: number = vectors[(r) < 0 ? vectors.length + (r) : r][(j) < 0 ? vectors[(r) < 0 ? vectors.length + (r) : r].length + (j) : j];
          vectors[(r) < 0 ? vectors.length + (r) : r][(j) < 0 ? vectors[(r) < 0 ? vectors.length + (r) : r].length + (j) : j] = vectors[(r) < 0 ? vectors.length + (r) : r][(Math.trunc((j + 1))) < 0 ? vectors[(r) < 0 ? vectors.length + (r) : r].length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
          vectors[(r) < 0 ? vectors.length + (r) : r][((j + 1)) < 0 ? vectors[(r) < 0 ? vectors.length + (r) : r].length + ((j + 1)) : (j + 1)] = tv;
          r = (r + 1);
        }
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return {values, vectors};
}
function find_lanczos_eigenvectors(graph: number[][], k: number): EigenResult {
  validate_adjacency_list(graph);
  let res: LanczosResult = lanczos_iteration(graph, k);
  let eig: EigenResult = jacobi_eigen(res.t, 50);
  let sorted: EigenResult = sort_eigenpairs(eig.values, eig.vectors);
  let final_vectors: number[][] = matmul(res.q, sorted.vectors);
  return {"values": sorted.values, "vectors": final_vectors};
}
function list_to_string(arr: number[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < _len(arr))) {
    s = (s + _str(arr[(i) < 0 ? arr.length + (i) : i]));
    if ((i < (_len(arr) - 1))) {
      s = (s + ", ");
    }
    i = (i + 1);
  }
  return (s + "]");
}
function matrix_to_string(m: number[][]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < _len(m))) {
    s = (s + list_to_string(m[(i) < 0 ? m.length + (i) : i]));
    if ((i < (_len(m) - 1))) {
      s = (s + "; ");
    }
    i = (i + 1);
  }
  return (s + "]");
}
let graph: number[][] = [[1, 2], [0, 2], [0, 1]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let result: EigenResult
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = find_lanczos_eigenvectors(graph, 2)
  console.log(_str(list_to_string(result.values)));
  console.log(_str(matrix_to_string(result.vectors)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

