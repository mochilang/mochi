// Generated by Mochi v0.10.59 on 2025-08-07 00:02:50 GMT+7

function sort_edges(edges: number[][]): number[][] {
  let es: number[][] = edges;
  let i: number = 0;
  while ((i < _len(es))) {
    let j: number = 0;
    while ((j < ((_len(es) - i) - 1))) {
      if ((es[(j) < 0 ? es.length + (j) : j][(Math.trunc(2)) < 0 ? es[(j) < 0 ? es.length + (j) : j].length + (Math.trunc(2)) : Math.trunc(2)] > es[(Math.trunc((j + 1))) < 0 ? es.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))][(Math.trunc(2)) < 0 ? es[(Math.trunc((j + 1))) < 0 ? es.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))].length + (Math.trunc(2)) : Math.trunc(2)])) {
        let tmp: number[] = es[(j) < 0 ? es.length + (j) : j];
        es[(j) < 0 ? es.length + (j) : j] = es[(Math.trunc((j + 1))) < 0 ? es.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
        es[((j + 1)) < 0 ? es.length + ((j + 1)) : (j + 1)] = tmp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return es;
}
function find(parent: number[], x: number): number {
  let r: number = x;
  while ((parent[(r) < 0 ? parent.length + (r) : r] != r)) {
    r = parent[(r) < 0 ? parent.length + (r) : r];
  }
  return r;
}
function kruskal(n: number, edges: number[][]): number[][] {
  let parent: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    parent.push(i);
    i = (i + 1);
  }
  let sorted: number[][] = sort_edges(edges);
  let mst: number[][] = [];
  let e: number = 0;
  while ((e < _len(sorted))) {
    if ((_len(mst) == (n - 1))) {
      break
    }
    let edge: number[] = sorted[(e) < 0 ? sorted.length + (e) : e];
    e = (e + 1);
    let u: number = edge[(Math.trunc(0)) < 0 ? edge.length + (Math.trunc(0)) : Math.trunc(0)];
    let v: number = edge[(Math.trunc(1)) < 0 ? edge.length + (Math.trunc(1)) : Math.trunc(1)];
    let w: number = edge[(Math.trunc(2)) < 0 ? edge.length + (Math.trunc(2)) : Math.trunc(2)];
    let ru: number = find(parent, u);
    let rv: number = find(parent, v);
    if ((ru != rv)) {
      parent[(ru) < 0 ? parent.length + (ru) : ru] = rv;
      mst.push([u, v, w]);
    }
  }
  return mst;
}
function edges_equal(a: number[][], b: number[][]): boolean {
  if ((_len(a) != _len(b))) {
    return false;
  }
  let i: number = 0;
  while ((i < _len(a))) {
    let e1: number[] = a[(i) < 0 ? a.length + (i) : i];
    let e2: number[] = b[(i) < 0 ? b.length + (i) : i];
    if ((((e1[(Math.trunc(0)) < 0 ? e1.length + (Math.trunc(0)) : Math.trunc(0)] != e2[(Math.trunc(0)) < 0 ? e2.length + (Math.trunc(0)) : Math.trunc(0)]) || (e1[(Math.trunc(1)) < 0 ? e1.length + (Math.trunc(1)) : Math.trunc(1)] != e2[(Math.trunc(1)) < 0 ? e2.length + (Math.trunc(1)) : Math.trunc(1)])) || (e1[(Math.trunc(2)) < 0 ? e1.length + (Math.trunc(2)) : Math.trunc(2)] != e2[(Math.trunc(2)) < 0 ? e2.length + (Math.trunc(2)) : Math.trunc(2)]))) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
function main() {
  let num_nodes: number = 9;
  let edges: number[][] = [[0, 1, 4], [0, 7, 8], [1, 2, 8], [7, 8, 7], [7, 6, 1], [2, 8, 2], [8, 6, 6], [2, 3, 7], [2, 5, 4], [6, 5, 2], [3, 5, 14], [3, 4, 9], [5, 4, 10], [1, 7, 11]];
  let expected: number[][] = [[7, 6, 1], [2, 8, 2], [6, 5, 2], [0, 1, 4], [2, 5, 4], [2, 3, 7], [0, 7, 8], [3, 4, 9]];
  let result: number[][] = kruskal(num_nodes, edges);
  let sorted_result: number[][] = sort_edges(result);
  let sorted_expected: number[][] = sort_edges(expected);
  console.log(_str(_str(sorted_result)));
  if (edges_equal(sorted_expected, sorted_result)) {
    console.log("");
  } else {
    _stdout_write("");
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _stdout_write(s: string) {
  if (typeof Deno !== 'undefined') {
    Deno.stdout.writeSync(new TextEncoder().encode(s));
  } else if (typeof process !== 'undefined') {
    process.stdout.write(s);
  } else {
    console.log(s);
  }
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

