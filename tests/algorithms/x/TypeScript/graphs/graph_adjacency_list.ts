// Generated by Mochi v0.10.59 on 2025-08-07 00:02:11 GMT+7

export interface Graph { adj: Record<string, string[]>; directed: boolean }
function create_graph(vertices: string[], edges: string[][], directed: boolean): Graph {
  let adj: Record<string, string[]> = {};
  for (const v of vertices) {
    adj[(v) < 0 ? adj.length + (v) : v] = [];
  }
  for (const e of edges) {
    let s: string = e[(Math.trunc(0)) < 0 ? e.length + (Math.trunc(0)) : Math.trunc(0)];
    let d: string = e[(Math.trunc(1)) < 0 ? e.length + (Math.trunc(1)) : Math.trunc(1)];
    if (!(s in adj)) {
      adj[(s) < 0 ? adj.length + (s) : s] = [];
    }
    if (!(d in adj)) {
      adj[(d) < 0 ? adj.length + (d) : d] = [];
    }
    adj[(s) < 0 ? adj.length + (s) : s] = [...adj[(s) < 0 ? adj.length + (s) : s], d];
    if (!directed) {
      adj[(d) < 0 ? adj.length + (d) : d] = [...adj[(d) < 0 ? adj.length + (d) : d], s];
    }
  }
  return {adj, directed};
}
function add_vertex(graph: Graph, v: string): Graph {
  if ((v in graph.adj)) {
    _panic("vertex exists");
  }
  let adj: Record<string, string[]> = {};
  for (const k in graph.adj) {
    adj[(k) < 0 ? adj.length + (k) : k] = graph.adj[(k) < 0 ? graph.adj.length + (k) : k];
  }
  adj[(v) < 0 ? adj.length + (v) : v] = [];
  return {adj, "directed": graph.directed};
}
function remove_from_list(lst: string[], value: string): string[] {
  let res: string[] = [];
  let i: number = 0;
  while ((i < _len(lst))) {
    if ((lst[(i) < 0 ? lst.length + (i) : i] != value)) {
      res.push(lst[(i) < 0 ? lst.length + (i) : i]);
    }
    i = (i + 1);
  }
  return res;
}
function remove_key(m: Record<string, string[]>, key: string): Record<string, string[]> {
  let res: Record<string, string[]> = {};
  for (const k in m) {
    if ((k != key)) {
      res[(k) < 0 ? res.length + (k) : k] = m[(k) < 0 ? m.length + (k) : k];
    }
  }
  return res;
}
function add_edge(graph: Graph, s: string, d: string): Graph {
  if ((!(s in graph.adj) || !(d in graph.adj))) {
    _panic("vertex missing");
  }
  if (contains_edge(graph, s, d)) {
    _panic("edge exists");
  }
  let adj: Record<string, string[]> = {};
  for (const k in graph.adj) {
    adj[(k) < 0 ? adj.length + (k) : k] = graph.adj[(k) < 0 ? graph.adj.length + (k) : k];
  }
  let list_s: string[] = adj[(s) < 0 ? adj.length + (s) : s];
  list_s.push(d);
  adj[(s) < 0 ? adj.length + (s) : s] = list_s;
  if (!graph.directed) {
    let list_d: string[] = adj[(d) < 0 ? adj.length + (d) : d];
    list_d.push(s);
    adj[(d) < 0 ? adj.length + (d) : d] = list_d;
  }
  return {adj, "directed": graph.directed};
}
function remove_edge(graph: Graph, s: string, d: string): Graph {
  if ((!(s in graph.adj) || !(d in graph.adj))) {
    _panic("vertex missing");
  }
  if (!contains_edge(graph, s, d)) {
    _panic("edge missing");
  }
  let adj: Record<string, string[]> = {};
  for (const k in graph.adj) {
    adj[(k) < 0 ? adj.length + (k) : k] = graph.adj[(k) < 0 ? graph.adj.length + (k) : k];
  }
  adj[(s) < 0 ? adj.length + (s) : s] = remove_from_list(adj[(s) < 0 ? adj.length + (s) : s], d);
  if (!graph.directed) {
    adj[(d) < 0 ? adj.length + (d) : d] = remove_from_list(adj[(d) < 0 ? adj.length + (d) : d], s);
  }
  return {adj, "directed": graph.directed};
}
function remove_vertex(graph: Graph, v: string): Graph {
  if (!(v in graph.adj)) {
    _panic("vertex missing");
  }
  let adj: Record<string, string[]> = {};
  for (const k in graph.adj) {
    if ((k != v)) {
      adj[(k) < 0 ? adj.length + (k) : k] = remove_from_list(graph.adj[(k) < 0 ? graph.adj.length + (k) : k], v);
    }
  }
  return {adj, "directed": graph.directed};
}
function contains_vertex(graph: Graph, v: string): boolean {
  return (v in graph.adj);
}
function contains_edge(graph: Graph, s: string, d: string): boolean {
  if ((!(s in graph.adj) || !(d in graph.adj))) {
    _panic("vertex missing");
  }
  for (const x of graph.adj[(s) < 0 ? graph.adj.length + (s) : s]) {
    if ((x == d)) {
      return true;
    }
  }
  return false;
}
function clear_graph(graph: Graph): Graph {
  return {"adj": {}, "directed": graph.directed};
}
function to_string(graph: Graph): string {
  return _str(graph.adj);
}
function main() {
  let vertices: string[] = ["1", "2", "3", "4"];
  let edges: string[][] = [["1", "2"], ["2", "3"], ["3", "4"]];
  let g: Graph = create_graph(vertices, edges, false);
  console.log(_str(to_string(g)));
  g = add_vertex(g, "5");
  g = add_edge(g, "4", "5");
  console.log(_str(_str(contains_edge(g, "4", "5"))));
  g = remove_edge(g, "1", "2");
  g = remove_vertex(g, "3");
  console.log(_str(to_string(g)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

