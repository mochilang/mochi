// Generated by Mochi v0.10.59 on 2025-08-06 23:28:43 GMT+7

function fill_row(current_row: number[], row_above: number[]): number[] {
  current_row[(Math.trunc(0)) < 0 ? current_row.length + (Math.trunc(0)) : Math.trunc(0)] = (current_row[(Math.trunc(0)) < 0 ? current_row.length + (Math.trunc(0)) : Math.trunc(0)] + row_above[(Math.trunc(0)) < 0 ? row_above.length + (Math.trunc(0)) : Math.trunc(0)]);
  let cell_n: number = 1;
  while ((cell_n < _len(current_row))) {
    let left: number = current_row[(Math.trunc((cell_n - 1))) < 0 ? current_row.length + (Math.trunc((cell_n - 1))) : Math.trunc((cell_n - 1))];
    let up: number = row_above[(cell_n) < 0 ? row_above.length + (cell_n) : cell_n];
    if ((left < up)) {
      current_row[(cell_n) < 0 ? current_row.length + (cell_n) : cell_n] = (current_row[(cell_n) < 0 ? current_row.length + (cell_n) : cell_n] + left);
    } else {
      current_row[(cell_n) < 0 ? current_row.length + (cell_n) : cell_n] = (current_row[(cell_n) < 0 ? current_row.length + (cell_n) : cell_n] + up);
    }
    cell_n = (cell_n + 1);
  }
  return current_row;
}
function min_path_sum(grid: number[][]): number {
  if (((_len(grid) == 0) || (_len(grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)]) == 0))) {
    error("The grid does not contain the appropriate information");
  }
  let cell_n: number = 1;
  while ((cell_n < _len(grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)]))) {
    grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)][(cell_n) < 0 ? grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)].length + (cell_n) : cell_n] = (grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)][(cell_n) < 0 ? grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)].length + (cell_n) : cell_n] + grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)][(Math.trunc((cell_n - 1))) < 0 ? grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)].length + (Math.trunc((cell_n - 1))) : Math.trunc((cell_n - 1))]);
    cell_n = (cell_n + 1);
  }
  let row_above: number[] = grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)];
  let row_n: number = 1;
  while ((row_n < _len(grid))) {
    let current_row: number[] = grid[(row_n) < 0 ? grid.length + (row_n) : row_n];
    grid[(row_n) < 0 ? grid.length + (row_n) : row_n] = fill_row(current_row, row_above);
    row_above = grid[(row_n) < 0 ? grid.length + (row_n) : row_n];
    row_n = (row_n + 1);
  }
  return grid[(Math.trunc((_len(grid) - 1))) < 0 ? grid.length + (Math.trunc((_len(grid) - 1))) : Math.trunc((_len(grid) - 1))][(Math.trunc((_len(grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)]) - 1))) < 0 ? grid[(Math.trunc((_len(grid) - 1))) < 0 ? grid.length + (Math.trunc((_len(grid) - 1))) : Math.trunc((_len(grid) - 1))].length + (Math.trunc((_len(grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)]) - 1))) : Math.trunc((_len(grid[(Math.trunc(0)) < 0 ? grid.length + (Math.trunc(0)) : Math.trunc(0)]) - 1))];
}
let grid1: number[][] = [[1, 3, 1], [1, 5, 1], [4, 2, 1]];
let grid2: number[][] = [[1, 0, 5, 6, 7], [8, 9, 0, 4, 2], [4, 4, 4, 5, 1], [9, 6, 3, 1, 0], [8, 4, 3, 2, 7]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(min_path_sum(grid1))));
  console.log(_str(_str(min_path_sum(grid2))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

