// Generated by Mochi v0.10.59 on 2025-08-06 22:25:44 GMT+7

export interface Edge { src: number; dst: number; weight: number }
let INF: number = 1000000000.0;
function list_to_string(arr: number[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < _len(arr))) {
    s = (s + _str(arr[(i) < 0 ? arr.length + (i) : i]));
    if ((i < (_len(arr) - 1))) {
      s = (s + ", ");
    }
    i = (i + 1);
  }
  return (s + "]");
}
function check_negative_cycle(graph: Edge[], distance: number[], edge_count: number): boolean {
  let j: number = 0;
  while ((j < edge_count)) {
    let e: Edge = graph[(j) < 0 ? graph.length + (j) : j];
    let u: number = e.src;
    let v: number = e.dst;
    let w: number = e.weight;
    if (((distance[(u) < 0 ? distance.length + (u) : u] < INF) && ((distance[(u) < 0 ? distance.length + (u) : u] + w) < distance[(v) < 0 ? distance.length + (v) : v]))) {
      return true;
    }
    j = (j + 1);
  }
  return false;
}
function bellman_ford(graph: Edge[], vertex_count: number, edge_count: number, src: number): number[] {
  let distance: number[] = [];
  let i: number = 0;
  while ((i < vertex_count)) {
    distance.push(INF);
    i = (i + 1);
  }
  distance[(src) < 0 ? distance.length + (src) : src] = 0.0;
  let k: number = 0;
  while ((k < (vertex_count - 1))) {
    let j: number = 0;
    while ((j < edge_count)) {
      let e: Edge = graph[(j) < 0 ? graph.length + (j) : j];
      let u: number = e.src;
      let v: number = e.dst;
      let w: number = e.weight;
      if (((distance[(u) < 0 ? distance.length + (u) : u] < INF) && ((distance[(u) < 0 ? distance.length + (u) : u] + w) < distance[(v) < 0 ? distance.length + (v) : v]))) {
        distance[(v) < 0 ? distance.length + (v) : v] = (distance[(u) < 0 ? distance.length + (u) : u] + w);
      }
      j = (j + 1);
    }
    k = (k + 1);
  }
  if (check_negative_cycle(graph, distance, edge_count)) {
    _panic("Negative cycle found");
  }
  return distance;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let edges: Edge[]
let distances: number[]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
edges = [{"src": 2, "dst": 1, "weight": -10}, {"src": 3, "dst": 2, "weight": 3}, {"src": 0, "dst": 3, "weight": 5}, {"src": 0, "dst": 1, "weight": 4}]
distances = bellman_ford(edges, 4, _len(edges), 0)
  console.log(_str(list_to_string(distances)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

