// Generated by Mochi v0.10.59 on 2025-08-06 23:28:51 GMT+7

let INF: number = 1000000000;
function connect(graph: Record<number, number[][]>, a: number, b: number, w: number): Record<number, number[][]> {
  let u: number = (a - 1);
  let v: number = (b - 1);
  let g: Record<number, number[][]> = graph;
  g[u] = [...g[u], [v, w]];
  g[v] = [...g[v], [u, w]];
  return g;
}
function in_list(arr: number[], x: number): boolean {
  let i: number = 0;
  while ((i < _len(arr))) {
    if ((arr[(i) < 0 ? arr.length + (i) : i] == x)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function prim(graph: Record<number, number[][]>, s: number, n: number): number[][] {
  let dist: Record<number, number> = {};
  let parent: Record<number, number> = {};
  dist[s] = 0;
  parent[s] = -1;
  let known: number[] = [];
  let keys: number[] = [s];
  while ((_len(known) < n)) {
    let mini: number = INF;
    let u: number = -1;
    let i: number = 0;
    while ((i < _len(keys))) {
      let k = keys[(i) < 0 ? keys.length + (i) : i];
      let d: number = dist[k];
      if ((!in_list(known, k) && (d < mini))) {
        mini = d;
        u = k;
      }
      i = (i + 1);
    }
    known.push(u);
    for (const e of Object.keys(graph[u])) {
      let v: number = e[(Math.trunc(0)) < 0 ? e.length + (Math.trunc(0)) : Math.trunc(0)];
      let w: number = e[(Math.trunc(1)) < 0 ? e.length + (Math.trunc(1)) : Math.trunc(1)];
      if (!in_list(keys, v)) {
        keys.push(v);
      }
      let cur: number = ((v in dist) ? dist[v] : INF);
      if ((!in_list(known, v) && (w < cur))) {
        dist[v] = w;
        parent[v] = u;
      }
    }
  }
  let edges: number[][] = [];
  let j: number = 0;
  while ((j < _len(keys))) {
    let v: number = keys[(j) < 0 ? keys.length + (j) : j];
    if ((v != s)) {
      edges.push([(v + 1), (parent[v] + 1)]);
    }
    j = (j + 1);
  }
  return edges;
}
function sort_heap(h: number[], dist: Record<number, number>): number[] {
  let a: number[] = h;
  let i: number = 0;
  while ((i < _len(a))) {
    let j: number = 0;
    while ((j < ((_len(a) - i) - 1))) {
      let dj: number = ((a[(j) < 0 ? a.length + (j) : j] in dist) ? dist[Math.trunc(a[(j) < 0 ? a.length + (j) : j])] : INF);
      let dj1: number = ((a[(Math.trunc((j + 1))) < 0 ? a.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))] in dist) ? dist[Math.trunc(a[(Math.trunc((j + 1))) < 0 ? a.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))])] : INF);
      if ((dj > dj1)) {
        let t: number = a[(j) < 0 ? a.length + (j) : j];
        a[(j) < 0 ? a.length + (j) : j] = a[(Math.trunc((j + 1))) < 0 ? a.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
        a[(Math.trunc((j + 1))) < 0 ? a.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))] = t;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return a;
}
function prim_heap(graph: Record<number, number[][]>, s: number, n: number): number[][] {
  let dist: Record<number, number> = {};
  let parent: Record<number, number> = {};
  dist[s] = 0;
  parent[s] = -1;
  let h: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    h.push(i);
    i = (i + 1);
  }
  h = sort_heap(h, dist);
  let known: number[] = [];
  while ((_len(h) > 0)) {
    let u: number = h[(Math.trunc(0)) < 0 ? h.length + (Math.trunc(0)) : Math.trunc(0)];
    h = h.slice(1, _len(h));
    known.push(u);
    for (const e of Object.keys(graph[u])) {
      let v: number = e[(Math.trunc(0)) < 0 ? e.length + (Math.trunc(0)) : Math.trunc(0)];
      let w: number = e[(Math.trunc(1)) < 0 ? e.length + (Math.trunc(1)) : Math.trunc(1)];
      let cur: number = ((v in dist) ? dist[v] : INF);
      if ((!in_list(known, v) && (w < cur))) {
        dist[v] = w;
        parent[v] = u;
      }
    }
    h = sort_heap(h, dist);
  }
  let edges: number[][] = [];
  let j: number = 0;
  while ((j < n)) {
    if ((j != s)) {
      edges.push([(j + 1), (parent[j] + 1)]);
    }
    j = (j + 1);
  }
  return edges;
}
function print_edges(edges: number[][]) {
  let i: number = 0;
  while ((i < _len(edges))) {
    let e: number[] = edges[(i) < 0 ? edges.length + (i) : i];
    console.log(_str((((("(" + _str(e[(Math.trunc(0)) < 0 ? e.length + (Math.trunc(0)) : Math.trunc(0)])) + ", ") + _str(e[(Math.trunc(1)) < 0 ? e.length + (Math.trunc(1)) : Math.trunc(1)])) + ")")));
    i = (i + 1);
  }
}
function test_vector() {
  let x: number = 5;
  let G: Record<number, number[][]> = {};
  let i: number = 0;
  while ((i < x)) {
    G[i] = [];
    i = (i + 1);
  }
  G = connect(G, 1, 2, 15);
  G = connect(G, 1, 3, 12);
  G = connect(G, 2, 4, 13);
  G = connect(G, 2, 5, 5);
  G = connect(G, 3, 2, 6);
  G = connect(G, 3, 4, 6);
  let mst: number[][] = prim(G, 0, x);
  print_edges(mst);
  let mst_heap: number[][] = prim_heap(G, 0, x);
  print_edges(mst_heap);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  test_vector();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

