// Generated by Mochi v0.10.59 on 2025-08-07 00:16:36 GMT+7

function dot(a: number[], b: number[]): number {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(a))) {
    sum = (sum + (a[(i) < 0 ? a.length + (i) : i] * b[(i) < 0 ? b.length + (i) : i]));
    i = (i + 1);
  }
  return sum;
}
function maxf(a: number, b: number): number {
  if ((a > b)) {
    return a;
  }
  return b;
}
function minf(a: number, b: number): number {
  if ((a < b)) {
    return a;
  }
  return b;
}
function absf(x: number): number {
  if ((x >= 0.0)) {
    return x;
  }
  return (0.0 - x);
}
function predict_raw(samples: number[][], labels: number[], alphas: number[], b: number, x: number[]): number {
  let res: number = 0.0;
  let i: number = 0;
  while ((i < _len(samples))) {
    res = (res + ((alphas[(i) < 0 ? alphas.length + (i) : i] * labels[(i) < 0 ? labels.length + (i) : i]) * dot(samples[(i) < 0 ? samples.length + (i) : i], x)));
    i = (i + 1);
  }
  return (res + b);
}
function smo_train(samples: number[][], labels: number[], c: number, tol: number, max_passes: number): number[][] {
  let m: number = _len(samples);
  let alphas: number[] = [];
  let i: number = 0;
  while ((i < m)) {
    alphas.push(0.0);
    i = (i + 1);
  }
  let b: number = 0.0;
  let passes: number = 0;
  while ((passes < max_passes)) {
    let num_changed: number = 0;
    let i1: number = 0;
    while ((i1 < m)) {
      let Ei: number = (predict_raw(samples, labels, alphas, b, samples[(i1) < 0 ? samples.length + (i1) : i1]) - labels[(i1) < 0 ? labels.length + (i1) : i1]);
      if (((((labels[(i1) < 0 ? labels.length + (i1) : i1] * Ei) < (0.0 - tol)) && (alphas[(i1) < 0 ? alphas.length + (i1) : i1] < c)) || (((labels[(i1) < 0 ? labels.length + (i1) : i1] * Ei) > tol) && (alphas[(i1) < 0 ? alphas.length + (i1) : i1] > 0.0)))) {
        let i2: number = ((i1 + 1) % m);
        let Ej: number = (predict_raw(samples, labels, alphas, b, samples[(i2) < 0 ? samples.length + (i2) : i2]) - labels[(i2) < 0 ? labels.length + (i2) : i2]);
        let alpha1_old: number = alphas[(i1) < 0 ? alphas.length + (i1) : i1];
        let alpha2_old: number = alphas[(i2) < 0 ? alphas.length + (i2) : i2];
        let L: number = 0.0;
        let H: number = 0.0;
        if ((labels[(i1) < 0 ? labels.length + (i1) : i1] != labels[(i2) < 0 ? labels.length + (i2) : i2])) {
          L = maxf(0.0, (alpha2_old - alpha1_old));
          H = minf(c, ((c + alpha2_old) - alpha1_old));
        } else {
          L = maxf(0.0, ((alpha2_old + alpha1_old) - c));
          H = minf(c, (alpha2_old + alpha1_old));
        }
        if ((L == H)) {
          i1 = (i1 + 1);
          continue
        }
        let eta: number = (((2.0 * dot(samples[(i1) < 0 ? samples.length + (i1) : i1], samples[(i2) < 0 ? samples.length + (i2) : i2])) - dot(samples[(i1) < 0 ? samples.length + (i1) : i1], samples[(i1) < 0 ? samples.length + (i1) : i1])) - dot(samples[(i2) < 0 ? samples.length + (i2) : i2], samples[(i2) < 0 ? samples.length + (i2) : i2]));
        if ((eta >= 0.0)) {
          i1 = (i1 + 1);
          continue
        }
        alphas[(i2) < 0 ? alphas.length + (i2) : i2] = (alpha2_old - ((labels[(i2) < 0 ? labels.length + (i2) : i2] * (Ei - Ej)) / eta));
        if ((alphas[(i2) < 0 ? alphas.length + (i2) : i2] > H)) {
          alphas[(i2) < 0 ? alphas.length + (i2) : i2] = H;
        }
        if ((alphas[(i2) < 0 ? alphas.length + (i2) : i2] < L)) {
          alphas[(i2) < 0 ? alphas.length + (i2) : i2] = L;
        }
        if ((absf((alphas[(i2) < 0 ? alphas.length + (i2) : i2] - alpha2_old)) < 1e-05)) {
          i1 = (i1 + 1);
          continue
        }
        alphas[(i1) < 0 ? alphas.length + (i1) : i1] = (alpha1_old + ((labels[(i1) < 0 ? labels.length + (i1) : i1] * labels[(i2) < 0 ? labels.length + (i2) : i2]) * (alpha2_old - alphas[(i2) < 0 ? alphas.length + (i2) : i2])));
        let b1: number = (((b - Ei) - ((labels[(i1) < 0 ? labels.length + (i1) : i1] * (alphas[(i1) < 0 ? alphas.length + (i1) : i1] - alpha1_old)) * dot(samples[(i1) < 0 ? samples.length + (i1) : i1], samples[(i1) < 0 ? samples.length + (i1) : i1]))) - ((labels[(i2) < 0 ? labels.length + (i2) : i2] * (alphas[(i2) < 0 ? alphas.length + (i2) : i2] - alpha2_old)) * dot(samples[(i1) < 0 ? samples.length + (i1) : i1], samples[(i2) < 0 ? samples.length + (i2) : i2])));
        let b2: number = (((b - Ej) - ((labels[(i1) < 0 ? labels.length + (i1) : i1] * (alphas[(i1) < 0 ? alphas.length + (i1) : i1] - alpha1_old)) * dot(samples[(i1) < 0 ? samples.length + (i1) : i1], samples[(i2) < 0 ? samples.length + (i2) : i2]))) - ((labels[(i2) < 0 ? labels.length + (i2) : i2] * (alphas[(i2) < 0 ? alphas.length + (i2) : i2] - alpha2_old)) * dot(samples[(i2) < 0 ? samples.length + (i2) : i2], samples[(i2) < 0 ? samples.length + (i2) : i2])));
        if (((alphas[(i1) < 0 ? alphas.length + (i1) : i1] > 0.0) && (alphas[(i1) < 0 ? alphas.length + (i1) : i1] < c))) {
          b = b1;
        } else {
          if (((alphas[(i2) < 0 ? alphas.length + (i2) : i2] > 0.0) && (alphas[(i2) < 0 ? alphas.length + (i2) : i2] < c))) {
            b = b2;
          } else {
            b = ((b1 + b2) / 2.0);
          }
        }
        num_changed = (num_changed + 1);
      }
      i1 = (i1 + 1);
    }
    if ((num_changed == 0)) {
      passes = (passes + 1);
    } else {
      passes = 0;
    }
  }
  return [alphas, [b]];
}
function predict(samples: number[][], labels: number[], model: number[][], x: number[]): number {
  let alphas: number[] = model[(Math.trunc(0)) < 0 ? model.length + (Math.trunc(0)) : Math.trunc(0)];
  let b: number = model[(Math.trunc(1)) < 0 ? model.length + (Math.trunc(1)) : Math.trunc(1)][(Math.trunc(0)) < 0 ? model[(Math.trunc(1)) < 0 ? model.length + (Math.trunc(1)) : Math.trunc(1)].length + (Math.trunc(0)) : Math.trunc(0)];
  let val: number = predict_raw(samples, labels, alphas, b, x);
  if ((val >= 0.0)) {
    return 1.0;
  }
  return -1.0;
}
let samples: number[][] = [[2.0, 2.0], [1.5, 1.5], [0.0, 0.0], [0.5, 0.0]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let labels: number[]
let model: number[][]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
labels = [1.0, 1.0, -1.0, -1.0]
model = smo_train(samples, labels, 1.0, 0.001, 10)
  console.log(_str(predict(samples, labels, model, [1.5, 1.0])));
  console.log(_str(predict(samples, labels, model, [0.2, 0.1])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

