// Generated by Mochi v0.10.59 on 2025-08-07 09:45:52 GMT+7

function all_digits(s: string): boolean {
  if ((_len(s) == 0)) {
    return false;
  }
  let i: number = 0;
  while ((i < _len(s))) {
    let c: string = s[(i) < 0 ? s.length + (i) : i];
    if (((c < "0") || (c > "9"))) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
function indian_phone_validator(phone: string): boolean {
  let s: string = phone;
  if (((_len(s) >= 3) && ((s).substring(0, 3) == "+91"))) {
    s = (s).substring(3, _len(s));
    if ((_len(s) > 0)) {
      let c: string = s[(Math.trunc(0)) < 0 ? s.length + (Math.trunc(0)) : Math.trunc(0)];
      if (((c == "-") || (c == " "))) {
        s = (s).substring(1, _len(s));
      }
    }
  }
  if (((_len(s) > 0) && (s[(Math.trunc(0)) < 0 ? s.length + (Math.trunc(0)) : Math.trunc(0)] == "0"))) {
    s = (s).substring(1, _len(s));
  }
  if (((_len(s) >= 2) && ((s).substring(0, 2) == "91"))) {
    s = (s).substring(2, _len(s));
  }
  if ((_len(s) != 10)) {
    return false;
  }
  let first = s[(Math.trunc(0)) < 0 ? s.length + (Math.trunc(0)) : Math.trunc(0)];
  if (!(((first == "7") || (first == "8")) || (first == "9"))) {
    return false;
  }
  if (!all_digits(s)) {
    return false;
  }
  return true;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(indian_phone_validator("+91123456789"))));
  console.log(_str(_str(indian_phone_validator("+919876543210"))));
  console.log(_str(_str(indian_phone_validator("01234567896"))));
  console.log(_str(_str(indian_phone_validator("919876543218"))));
  console.log(_str(_str(indian_phone_validator("+91-1234567899"))));
  console.log(_str(_str(indian_phone_validator("+91-9876543218"))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

