// Generated by Mochi v0.10.59 on 2025-08-07 08:56:01 GMT+7

let LOWER: string = "abcdefghijklmnopqrstuvwxyz";
let UPPER: string = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
let DIGITS: string = "0123456789";
function is_lower(ch: string): boolean {
  let i: number = 0;
  while ((i < _len(LOWER))) {
    if ((LOWER[(i) < 0 ? LOWER.length + (i) : i] == ch)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function is_upper(ch: string): boolean {
  let i: number = 0;
  while ((i < _len(UPPER))) {
    if ((UPPER[(i) < 0 ? UPPER.length + (i) : i] == ch)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function is_digit(ch: string): boolean {
  let i: number = 0;
  while ((i < _len(DIGITS))) {
    if ((DIGITS[(i) < 0 ? DIGITS.length + (i) : i] == ch)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function is_alpha(ch: string): boolean {
  if (is_lower(ch)) {
    return true;
  }
  if (is_upper(ch)) {
    return true;
  }
  return false;
}
function is_alnum(ch: string): boolean {
  if (is_alpha(ch)) {
    return true;
  }
  if (is_digit(ch)) {
    return true;
  }
  return false;
}
function to_lower(ch: string): string {
  let i: number = 0;
  while ((i < _len(UPPER))) {
    if ((UPPER[(i) < 0 ? UPPER.length + (i) : i] == ch)) {
      return LOWER[(i) < 0 ? LOWER.length + (i) : i];
    }
    i = (i + 1);
  }
  return ch;
}
function camel_to_snake_case(input_str: string): string {
  let snake_str: string = "";
  let i: number = 0;
  let prev_is_digit: boolean = false;
  let prev_is_alpha: boolean = false;
  while ((i < _len(input_str))) {
    let ch: string = input_str[(i) < 0 ? input_str.length + (i) : i];
    if (is_upper(ch)) {
      snake_str = ((snake_str + "_") + to_lower(ch));
    } else {
      if ((prev_is_digit && is_lower(ch))) {
        snake_str = ((snake_str + "_") + ch);
      } else {
        if ((prev_is_alpha && is_digit(ch))) {
          snake_str = ((snake_str + "_") + ch);
        } else {
          if (!is_alnum(ch)) {
            snake_str = (snake_str + "_");
          } else {
            snake_str = (snake_str + ch);
          }
        }
      }
    }
    prev_is_digit = is_digit(ch);
    prev_is_alpha = is_alpha(ch);
    i = (i + 1);
  }
  if (((_len(snake_str) > 0) && (snake_str[(Math.trunc(0)) < 0 ? snake_str.length + (Math.trunc(0)) : Math.trunc(0)] == "_"))) {
    snake_str = snake_str.slice(1, _len(snake_str));
  }
  return snake_str;
}
function main() {
  console.log(_str(camel_to_snake_case("someRandomString")));
  console.log(_str(camel_to_snake_case("SomeRandomStr#ng")));
  console.log(_str(camel_to_snake_case("123SomeRandom123String123")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

