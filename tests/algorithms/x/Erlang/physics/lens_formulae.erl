#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, focal_length_of_lens/2, object_distance/2, image_distance/2]).

% Generated by Mochi transpiler v0.10.61 (c04b4c8319) on 2025-08-08 17:05 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.

focal_length_of_lens(Object_distance_from_lens, Image_distance_from_lens) ->
    try
        case ((Object_distance_from_lens == 0.0) orelse (Image_distance_from_lens == 0.0)) of
        true -> erlang:error({panic, "Invalid inputs. Enter non zero values with respect to the sign convention."});
        _ -> ok
    end,
        mochi_safe_div(1.0, (mochi_safe_div(1.0, Image_distance_from_lens) - mochi_safe_div(1.0, Object_distance_from_lens)))
    catch {return, Ret} -> Ret end.

object_distance(Focal_length_of_lens, Image_distance_from_lens_2) ->
    try
        case ((Image_distance_from_lens_2 == 0.0) orelse (Focal_length_of_lens == 0.0)) of
        true -> erlang:error({panic, "Invalid inputs. Enter non zero values with respect to the sign convention."});
        _ -> ok
    end,
        mochi_safe_div(1.0, (mochi_safe_div(1.0, Image_distance_from_lens_2) - mochi_safe_div(1.0, Focal_length_of_lens)))
    catch {return, Ret} -> Ret end.

image_distance(Focal_length_of_lens_2, Object_distance_from_lens_2) ->
    try
        case ((Object_distance_from_lens_2 == 0.0) orelse (Focal_length_of_lens_2 == 0.0)) of
        true -> erlang:error({panic, "Invalid inputs. Enter non zero values with respect to the sign convention."});
        _ -> ok
    end,
        mochi_safe_div(1.0, (mochi_safe_div(1.0, Object_distance_from_lens_2) + mochi_safe_div(1.0, Focal_length_of_lens_2)))
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [mochi_str(focal_length_of_lens(10.0, 4.0))]),
    io:format("~ts~n", [mochi_str(focal_length_of_lens(2.7, 5.8))]),
    io:format("~ts~n", [mochi_str(object_distance(10.0, 40.0))]),
    io:format("~ts~n", [mochi_str(object_distance(6.2, 1.5))]),
    io:format("~ts~n", [mochi_str(image_distance(50.0, 40.0))]),
    io:format("~ts~n", [mochi_str(image_distance(5.3, 7.9))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
