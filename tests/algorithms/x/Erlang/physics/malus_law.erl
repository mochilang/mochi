#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, mod/2, cos/1, radians/1, abs_val/1, malus_law/2, test_malus_law/0, main/0]).

% Generated by Mochi transpiler v0.10.61 (c04b4c8319) on 2025-08-08 17:05 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.

mod(X, M) ->
    try
        (X - mochi_safe_mul(float(mochi_to_int(mochi_safe_div(X, M))), M))
    catch {return, Ret} -> Ret end.

cos(X_2) ->
    try
        Y = (mod((X_2 + 3.141592653589793), 6.283185307179586) - 3.141592653589793),
        Y2 = mochi_safe_mul(Y, Y),
        Y4 = mochi_safe_mul(Y2, Y2),
        Y6 = mochi_safe_mul(Y4, Y2),
        (((1.0 - mochi_safe_div(Y2, 2.0)) + mochi_safe_div(Y4, 24.0)) - mochi_safe_div(Y6, 720.0))
    catch {return, Ret} -> Ret end.

radians(Deg) ->
    try
        mochi_safe_div(mochi_safe_mul(Deg, 3.141592653589793), 180.0)
    catch {return, Ret} -> Ret end.

abs_val(X_3) ->
    try
        (case (X_3 < 0.0) of
    true -> -X_3;
    _ -> X_3
end)
    catch {return, Ret} -> Ret end.

malus_law(Initial_intensity, Angle) ->
    try
        case (Initial_intensity < 0.0) of
        true -> erlang:error({panic, "The value of intensity cannot be negative"});
        _ -> ok
    end,
        case ((Angle < 0.0) orelse (Angle > 360.0)) of
        true -> erlang:error({panic, "In Malus Law, the angle is in the range 0-360 degrees"});
        _ -> ok
    end,
        Theta = radians(Angle),
        C = cos(Theta),
        mochi_safe_mul(Initial_intensity, mochi_safe_mul(C, C))
    catch {return, Ret} -> Ret end.

test_malus_law() ->
    try
        case (abs_val((malus_law(10.0, 45.0) - 5.0)) > 0.01) of
        true -> erlang:error({panic, "malus_law test1 failed"});
        _ -> ok
    end,
        case (abs_val((malus_law(100.0, 60.0) - 25.0)) > 0.01) of
        true -> erlang:error({panic, "malus_law test2 failed"});
        _ -> ok
    end,
        case (abs_val((malus_law(50.0, 150.0) - 37.5)) > 0.01) of
        true -> erlang:error({panic, "malus_law test3 failed"});
        _ -> ok
    end,
        case (abs_val((malus_law(75.0, 270.0) - 0.0)) > 0.01) of
        true -> erlang:error({panic, "malus_law test4 failed"});
        _ -> ok
    end,
        case (abs_val((malus_law(100.0, 180.0) - 100.0)) > 0.01) of
        true -> erlang:error({panic, "malus_law test5 failed"});
        _ -> ok
    end,
        case (abs_val((malus_law(100.0, 360.0) - 100.0)) > 0.01) of
        true -> erlang:error({panic, "malus_law test6 failed"});
        _ -> ok
    end,
        nil
    catch {return, Ret} -> Ret end.

main() ->
    try
        test_malus_law(),
        io:format("~ts~n", [mochi_str(malus_law(100.0, 60.0))]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('PI', 3.141592653589793),
    erlang:put('TWO_PI', 6.283185307179586),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
