#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, slice_without_last/1, is_balanced/1, main/0]).

% Generated by Mochi transpiler v0.10.61 (c45765bf43) on 2025-08-08 15:36 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

slice_without_last(Xs) ->
    try
        Res = [],
        I = 0,
        Fun = fun Fun_loop(I, Res, Xs) ->
    case (I < (erlang:length(Xs) - 1)) of
        true ->
            Res_2 = lists:append((case Res of nil -> []; _ -> Res end), [(case erlang:is_map(Xs) of true -> maps:get(I, Xs, nil); _ -> case I < 0 of true -> mochi_nth(erlang:length(Xs) + I + 1, Xs); _ -> mochi_nth(I + 1, Xs) end end)]),
            I_2 = (I + 1),
            Fun_loop(I_2, Res_2, Xs);
        _ -> {I, Res, Xs}
    end
end,
{I_2, Res_2, Xs} = Fun(I, Res, Xs),
        Res_2
    catch {return, Ret} -> Ret end.

is_balanced(S) ->
    try
        Stack = [],
        I_3 = 0,
        Fun_2 = fun Fun_2_loop(I_3, S, Stack) ->
    case (I_3 < erlang:length(S)) of
        true ->
            Symbol = string:substr(S, I_3 + 1, ((I_3 + 1) - I_3)),
            case maps:is_key(Symbol, erlang:get('OPEN_TO_CLOSED')) of
        true -> Stack_2 = lists:append((case Stack of nil -> []; _ -> Stack end), [Symbol]),
            Stack_5 = Stack_2,
            Top_3 = nil;
        _ -> case (((Symbol == ")") orelse (Symbol == "]")) orelse (Symbol == "}")) of
        true -> case (erlang:length(Stack) == 0) of
        true -> throw({return, false});
        _ -> ok
    end,
            Top = (case erlang:is_map(Stack) of true -> maps:get((erlang:length(Stack) - 1), Stack, nil); _ -> case (erlang:length(Stack) - 1) < 0 of true -> mochi_nth(erlang:length(Stack) + (erlang:length(Stack) - 1) + 1, Stack); _ -> mochi_nth((erlang:length(Stack) - 1) + 1, Stack) end end),
            case (maps:get(Top, erlang:get('OPEN_TO_CLOSED'), nil) /= Symbol) of
        true -> throw({return, false});
        _ -> ok
    end,
            Stack_3 = slice_without_last(Stack),
            Stack_4 = Stack_3,
            Top_2 = Top;
        _ -> Stack_4 = Stack,
            Top_2 = nil
    end,
            Stack_5 = Stack_4,
            Top_3 = Top_2
    end,
            I_4 = (I_3 + 1),
            Fun_2_loop(I_4, S, Stack_5);
        _ -> {I_3, S, Stack}
    end
end,
{I_4, S, Stack_5} = Fun_2(I_3, S, Stack),
        (erlang:length(Stack_5) == 0)
    catch {return, Ret} -> Ret end.

main() ->
    try
        io:format("~ts~n", [mochi_repr(is_balanced(""))]),
        io:format("~ts~n", [mochi_repr(is_balanced("()"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("[]"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("{}"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("()[]{}"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("(())"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("[["))]),
        io:format("~ts~n", [mochi_repr(is_balanced("([{}])"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("(()[)]"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("([)]"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("[[()]]"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("(()(()))"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("]"))]),
        io:format("~ts~n", [mochi_repr(is_balanced("Life is a bowl of cherries."))]),
        io:format("~ts~n", [mochi_repr(is_balanced("Life is a bowl of che{}ies."))]),
        io:format("~ts~n", [mochi_repr(is_balanced("Life is a bowl of che}{ies."))]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('OPEN_TO_CLOSED', #{"(" => ")", "[" => "]", "{" => "}"}),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
