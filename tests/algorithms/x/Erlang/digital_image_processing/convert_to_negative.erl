#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, convert_to_negative/1, main/0]).

% Generated by Mochi transpiler v0.10.59 (cf9c8584f7) on 2025-08-07 09:56 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

convert_to_negative(Img) ->
    try
        Result = [],
        I = 0,
        Fun_2 = fun Fun_2_loop(I, Img, Result) ->
    case (I < erlang:length(Img)) of
        true ->
            Row = [],
            J = 0,
            Fun = fun Fun_loop(I, Img, J, Result, Row) ->
    case (J < erlang:length((case erlang:is_map(Img) of true -> maps:get(I, Img, nil); _ -> lists:nth(I + 1, Img) end))) of
        true ->
            Pixel = (case erlang:is_map((case erlang:is_map(Img) of true -> maps:get(I, Img, nil); _ -> lists:nth(I + 1, Img) end)) of true -> maps:get(J, (case erlang:is_map(Img) of true -> maps:get(I, Img, nil); _ -> lists:nth(I + 1, Img) end), nil); _ -> lists:nth(J + 1, (case erlang:is_map(Img) of true -> maps:get(I, Img, nil); _ -> lists:nth(I + 1, Img) end)) end),
            R = (255 - (case erlang:is_map(Pixel) of true -> maps:get(0, Pixel, nil); _ -> lists:nth(0 + 1, Pixel) end)),
            G = (255 - (case erlang:is_map(Pixel) of true -> maps:get(1, Pixel, nil); _ -> lists:nth(1 + 1, Pixel) end)),
            B = (255 - (case erlang:is_map(Pixel) of true -> maps:get(2, Pixel, nil); _ -> lists:nth(2 + 1, Pixel) end)),
            Row_2 = lists:append((case Row of nil -> []; _ -> Row end), [[R, G, B]]),
            J_2 = (J + 1),
            Fun_loop(I, Img, J_2, Result, Row_2);
        _ -> {I, Img, J, Result, Row}
    end
end,
{I, Img, J_2, Result, Row_2} = Fun(I, Img, J, Result, Row),
            Result_2 = lists:append((case Result of nil -> []; _ -> Result end), [Row_2]),
            I_2 = (I + 1),
            Fun_2_loop(I_2, Img, Result_2);
        _ -> {I, Img, Result}
    end
end,
{I_2, Img, Result_2} = Fun_2(I, Img, Result),
        Result_2
    catch {return, Ret} -> Ret end.

main() ->
    try
        Image = [[[10, 20, 30], [0, 0, 0]], [[255, 255, 255], [100, 150, 200]]],
        Neg = convert_to_negative(Image),
        io:format("~ts~n", [mochi_repr(Neg)]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
