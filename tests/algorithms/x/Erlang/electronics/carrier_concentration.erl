#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, sqrtapprox/1, carrier_concentration/3]).

% Generated by Mochi transpiler v0.10.59 (cf9c8584f7) on 2025-08-07 09:56 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try A / B catch _:_ -> 0.0 end.

sqrtapprox(X) ->
    try
        Guess = mochi_safe_div(X, 2),
        I = 0,
        Fun = fun Fun_loop(Guess, I, X) ->
    case (I < 20) of
        true ->
            Guess_2 = mochi_safe_div((Guess + mochi_safe_div(X, Guess)), 2),
            I_2 = (I + 1),
            Fun_loop(Guess_2, I_2, X);
        _ -> {Guess, I, X}
    end
end,
{Guess_2, I_2, X} = Fun(Guess, I, X),
        Guess_2
    catch {return, Ret} -> Ret end.

carrier_concentration(Electron_conc, Hole_conc, Intrinsic_conc) ->
    try
        Zero_count = 0,
        case (Electron_conc == 0) of
        true -> Zero_count_2 = (Zero_count + 1),
            Zero_count_3 = Zero_count_2;
        _ -> Zero_count_3 = Zero_count
    end,
        case (Hole_conc == 0) of
        true -> Zero_count_4 = (Zero_count_3 + 1),
            Zero_count_5 = Zero_count_4;
        _ -> Zero_count_5 = Zero_count_3
    end,
        case (Intrinsic_conc == 0) of
        true -> Zero_count_6 = (Zero_count_5 + 1),
            Zero_count_7 = Zero_count_6;
        _ -> Zero_count_7 = Zero_count_5
    end,
        case (Zero_count_7 /= 1) of
        true -> erlang:error("You cannot supply more or less than 2 values");
        _ -> ok
    end,
        case (Electron_conc < 0) of
        true -> erlang:error("Electron concentration cannot be negative in a semiconductor");
        _ -> ok
    end,
        case (Hole_conc < 0) of
        true -> erlang:error("Hole concentration cannot be negative in a semiconductor");
        _ -> ok
    end,
        case (Intrinsic_conc < 0) of
        true -> erlang:error("Intrinsic concentration cannot be negative in a semiconductor");
        _ -> ok
    end,
        case (Electron_conc == 0) of
        true -> throw({return, #{"name" => "electron_conc", "value" => mochi_safe_div(mochi_safe_mul(Intrinsic_conc, Intrinsic_conc), Hole_conc)}});
        _ -> ok
    end,
        case (Hole_conc == 0) of
        true -> throw({return, #{"name" => "hole_conc", "value" => mochi_safe_div(mochi_safe_mul(Intrinsic_conc, Intrinsic_conc), Electron_conc)}});
        _ -> ok
    end,
        case (Intrinsic_conc == 0) of
        true -> throw({return, #{"name" => "intrinsic_conc", "value" => sqrtapprox(mochi_safe_mul(Electron_conc, Hole_conc))}});
        _ -> ok
    end,
        #{"name" => "", "value" => -1}
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('r1', carrier_concentration(25, 100, 0)),
    io:format("~ts~n", [mochi_repr(((maps:get("name", erlang:get('r1'), nil) ++ ", ") ++ mochi_str(maps:get("value", erlang:get('r1'), nil))))]),
    erlang:put('r2', carrier_concentration(0, 1600, 200)),
    io:format("~ts~n", [mochi_repr(((maps:get("name", erlang:get('r2'), nil) ++ ", ") ++ mochi_str(maps:get("value", erlang:get('r2'), nil))))]),
    erlang:put('r3', carrier_concentration(1000, 0, 1200)),
    io:format("~ts~n", [mochi_repr(((maps:get("name", erlang:get('r3'), nil) ++ ", ") ++ mochi_str(maps:get("value", erlang:get('r3'), nil))))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
