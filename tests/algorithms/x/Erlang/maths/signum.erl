#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, signum/1, test_signum/0, main/0]).

% Generated by Mochi transpiler v0.10.60 (6899ff50f4) on 2025-08-07 16:25 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S = erlang:float_to_list(V, [short]),
    case lists:suffix(".0", S) of
        true -> lists:sublist(S, length(S) - 2);
        false -> S
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

signum(Num) ->
    try
        case (Num < 0.0) of
        true -> throw({return, -1});
        _ -> ok
    end,
        case (Num > 0.0) of
        true -> throw({return, 1});
        _ -> ok
    end,
        0
    catch {return, Ret} -> Ret end.

test_signum() ->
    try
        case (signum(5.0) /= 1) of
        true -> erlang:error("signum(5) failed");
        _ -> ok
    end,
        case (signum(-5.0) /= -1) of
        true -> erlang:error("signum(-5) failed");
        _ -> ok
    end,
        case (signum(0.0) /= 0) of
        true -> erlang:error("signum(0) failed");
        _ -> ok
    end,
        case (signum(10.5) /= 1) of
        true -> erlang:error("signum(10.5) failed");
        _ -> ok
    end,
        case (signum(-10.5) /= -1) of
        true -> erlang:error("signum(-10.5) failed");
        _ -> ok
    end,
        case (signum(0.000001) /= 1) of
        true -> erlang:error("signum(1e-6) failed");
        _ -> ok
    end,
        case (signum(-0.000001) /= -1) of
        true -> erlang:error("signum(-1e-6) failed");
        _ -> ok
    end,
        case (signum(123456789.0) /= 1) of
        true -> erlang:error("signum(123456789) failed");
        _ -> ok
    end,
        case (signum(-123456789.0) /= -1) of
        true -> erlang:error("signum(-123456789) failed");
        _ -> ok
    end,
        nil
    catch {return, Ret} -> Ret end.

main() ->
    try
        test_signum(),
        io:format("~ts~n", [mochi_repr(signum(12.0))]),
        io:format("~ts~n", [mochi_repr(signum(-12.0))]),
        io:format("~ts~n", [mochi_repr(signum(0.0))]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
