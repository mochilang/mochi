#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, pop_last/1, balanced_parentheses/1]).

% Generated by Mochi transpiler v0.10.59 (9be2afe36e) on 2025-08-07 08:51 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

pop_last(Xs) ->
    try
        Res = [],
        I = 0,
        Fun = fun Fun_loop(I, Res, Xs) ->
    case (I < (erlang:length(Xs) - 1)) of
        true ->
            Res_2 = lists:append((case Res of nil -> []; _ -> Res end), [(case erlang:is_map(Xs) of true -> maps:get(I, Xs, nil); _ -> lists:nth(I + 1, Xs) end)]),
            I_2 = (I + 1),
            Fun_loop(I_2, Res_2, Xs);
        _ -> {I, Res, Xs}
    end
end,
{I_2, Res_2, Xs} = Fun(I, Res, Xs),
        Res_2
    catch {return, Ret} -> Ret end.

balanced_parentheses(S) ->
    try
        Stack = [],
        Pairs = #{"(" => ")", "[" => "]", "{" => "}"},
        I_3 = 0,
        Fun_2 = fun Fun_2_loop(I_3, Pairs, S, Stack) ->
    case (I_3 < erlang:length(S)) of
        true ->
            Ch = string:substr(S, I_3 + 1, 1),
            case maps:is_key(Ch, Pairs) of
        true -> Stack_2 = lists:append((case Stack of nil -> []; _ -> Stack end), [Ch]),
            Stack_5 = Stack_2,
            Top_3 = nil;
        _ -> case (((Ch == ")") orelse (Ch == "]")) orelse (Ch == "}")) of
        true -> case (erlang:length(Stack) == 0) of
        true -> throw({return, false});
        _ -> ok
    end,
            Top = (case erlang:is_map(Stack) of true -> maps:get((erlang:length(Stack) - 1), Stack, nil); _ -> lists:nth((erlang:length(Stack) - 1) + 1, Stack) end),
            case (maps:get(Top, Pairs, nil) /= Ch) of
        true -> throw({return, false});
        _ -> ok
    end,
            Stack_3 = pop_last(Stack),
            Stack_4 = Stack_3,
            Top_2 = Top;
        _ -> Stack_4 = Stack,
            Top_2 = nil
    end,
            Stack_5 = Stack_4,
            Top_3 = Top_2
    end,
            I_4 = (I_3 + 1),
            Fun_2_loop(I_4, Pairs, S, Stack_5);
        _ -> {I_3, Pairs, S, Stack}
    end
end,
{I_4, Pairs, S, Stack_5} = Fun_2(I_3, Pairs, S, Stack),
        (erlang:length(Stack_5) == 0)
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('tests', ["([]{})", "[()]{}{[()()]()}", "[(])", "1+2*3-4", ""]),
    erlang:put('idx', 0),
    Fun_3 = fun Fun_3_loop() ->
    case (erlang:get('idx') < erlang:length(erlang:get('tests'))) of
        true ->
            io:format("~ts~n", [mochi_repr(balanced_parentheses((case erlang:is_map(erlang:get('tests')) of true -> maps:get(erlang:get('idx'), erlang:get('tests'), nil); _ -> lists:nth(erlang:get('idx') + 1, erlang:get('tests')) end)))]),
            erlang:put('idx', (erlang:get('idx') + 1)),
            Fun_3_loop();
        _ -> {}
    end
end,
{} = Fun_3(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
