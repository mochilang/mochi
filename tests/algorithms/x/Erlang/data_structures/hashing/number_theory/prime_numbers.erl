#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, isprime/1, nextprime/3]).

% Generated by Mochi transpiler v0.10.59 (52a2f6fb79) on 2025-08-07 00:21 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_not(X) ->
    case X of
        true -> false;
        false -> true;
        nil -> true;
        _ -> false
    end.


mochi_mod(A, B) when B =/= 0 ->
    ((A rem B) + B) rem B;
mochi_mod(_, _) -> 0.

isprime(Number) ->
    try
        case (Number < 2) of
        true -> throw({return, false});
        _ -> ok
    end,
        case (Number < 4) of
        true -> throw({return, true});
        _ -> ok
    end,
        case (mochi_mod(Number, 2) == 0) of
        true -> throw({return, false});
        _ -> ok
    end,
        I = 3,
        Fun = fun Fun_loop(I, Number) ->
    case ((I * I) =< Number) of
        true ->
            case (mochi_mod(Number, I) == 0) of
        true -> throw({return, false});
        _ -> ok
    end,
            I_2 = (I + 2),
            Fun_loop(I_2, Number);
        _ -> {I, Number}
    end
end,
{I_2, Number} = Fun(I, Number),
        true
    catch {return, Ret} -> Ret end.

nextprime(Value, Factor, Desc) ->
    try
        V = (Value * Factor),
        FirstValue = V,
        Fun_2 = fun Fun_2_loop(Desc, Factor, FirstValue, V, Value) ->
    case mochi_not(isprime(V)) of
        true ->
            case Desc of
        true -> V_2 = (V - 1),
            V_4 = V_2;
        _ -> V_3 = (V + 1),
            V_4 = V_3
    end,
            Fun_2_loop(Desc, Factor, FirstValue, V_4, Value);
        _ -> {Desc, Factor, FirstValue, V, Value}
    end
end,
{Desc, Factor, FirstValue, V_4, Value} = Fun_2(Desc, Factor, FirstValue, V, Value),
        case (V_4 == FirstValue) of
        true -> case Desc of
        true -> throw({return, nextprime((V_4 - 1), 1, Desc)});
        _ -> throw({return, nextprime((V_4 + 1), 1, Desc)})
    end;
        _ -> ok
    end,
        V_4
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [mochi_repr(isprime(0))]),
    io:format("~ts~n", [mochi_repr(isprime(1))]),
    io:format("~ts~n", [mochi_repr(isprime(2))]),
    io:format("~ts~n", [mochi_repr(isprime(3))]),
    io:format("~ts~n", [mochi_repr(isprime(27))]),
    io:format("~ts~n", [mochi_repr(isprime(87))]),
    io:format("~ts~n", [mochi_repr(isprime(563))]),
    io:format("~ts~n", [mochi_repr(isprime(2999))]),
    io:format("~ts~n", [mochi_repr(isprime(67483))]),
    io:format("~ts~n", [mochi_repr(nextprime(14, 1, false))]),
    io:format("~ts~n", [mochi_repr(nextprime(14, 1, true))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
