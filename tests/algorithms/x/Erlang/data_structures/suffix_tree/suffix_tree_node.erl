#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, new_suffix_tree_node/5, empty_suffix_tree_node/0, has_key/2]).

% Generated by Mochi transpiler v0.10.59 (9be2afe36e) on 2025-08-07 08:51 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

new_suffix_tree_node(Children, Is_end_of_string, Start, End, Suffix_link) ->
    try
        #{"children" => Children, "is_end_of_string" => Is_end_of_string, "start" => Start, "end" => End, "suffix_link" => Suffix_link}
    catch {return, Ret} -> Ret end.

empty_suffix_tree_node() ->
    try
        new_suffix_tree_node(#{}, false, (0 - 1), (0 - 1), (0 - 1))
    catch {return, Ret} -> Ret end.

has_key(M, K) ->
    try
        Fun = fun Fun_loop(List, K, M) ->
    case List of
        [] -> {K, M};
        [Key|Key_rest] ->
            case (Key == K) of
        true -> throw({return, true});
        _ -> ok
    end,
            Fun_loop(Key_rest, K, M);
        _ -> {K, M}
    end
end,
{K, M} = Fun(maps:keys(M), K, M),
        false
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('root', new_suffix_tree_node(#{"a" => 1}, false, (0 - 1), (0 - 1), (0 - 1))),
    erlang:put('leaf', new_suffix_tree_node(#{}, true, 0, 2, 0)),
    erlang:put('nodes', [erlang:get('root'), erlang:get('leaf')]),
    erlang:put('root_check', (case erlang:is_map(erlang:get('nodes')) of true -> maps:get(0, erlang:get('nodes'), nil); _ -> lists:nth(0 + 1, erlang:get('nodes')) end)),
    erlang:put('leaf_check', (case erlang:is_map(erlang:get('nodes')) of true -> maps:get(1, erlang:get('nodes'), nil); _ -> lists:nth(1 + 1, erlang:get('nodes')) end)),
    io:format("~ts~n", [mochi_str(has_key(maps:get("children", erlang:get('root_check'), nil), "a"))]),
    io:format("~ts~n", [mochi_str(maps:get("is_end_of_string", erlang:get('leaf_check'), nil))]),
    io:format("~ts~n", [mochi_str(maps:get("start", erlang:get('leaf_check'), nil))]),
    io:format("~ts~n", [mochi_str(maps:get("end", erlang:get('leaf_check'), nil))]),
    io:format("~ts~n", [mochi_str(maps:get("suffix_link", erlang:get('leaf_check'), nil))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
