#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, make_symmetric_tree/0, make_asymmetric_tree/0, is_symmetric_tree/1]).

% Generated by Mochi transpiler v0.10.59 (52a2f6fb79) on 2025-08-07 00:21 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

make_symmetric_tree() ->
    try
        [[1, 1, 2], [2, 3, 4], [2, 5, 6], [3, -1, -1], [4, -1, -1], [4, -1, -1], [3, -1, -1]]
    catch {return, Ret} -> Ret end.

make_asymmetric_tree() ->
    try
        [[1, 1, 2], [2, 3, 4], [2, 5, 6], [3, -1, -1], [4, -1, -1], [3, -1, -1], [4, -1, -1]]
    catch {return, Ret} -> Ret end.

is_symmetric_tree(Tree) ->
    try
        Stack = [(case erlang:is_map((case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end)) of true -> maps:get(1, (case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end), nil); _ -> lists:nth(1 + 1, (case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end)) end), (case erlang:is_map((case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end)) of true -> maps:get(2, (case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end), nil); _ -> lists:nth(2 + 1, (case erlang:is_map(Tree) of true -> maps:get(0, Tree, nil); _ -> lists:nth(0 + 1, Tree) end)) end)],
        Fun = fun Fun_loop(Stack, Tree) ->
    case (length(Stack) >= 2) of
        true ->
            try
                Left = (case erlang:is_map(Stack) of true -> maps:get((length(Stack) - 2), Stack, nil); _ -> lists:nth((length(Stack) - 2) + 1, Stack) end),
                Right = (case erlang:is_map(Stack) of true -> maps:get((length(Stack) - 1), Stack, nil); _ -> lists:nth((length(Stack) - 1) + 1, Stack) end),
                Stack_2 = lists:sublist(Stack, 0 + 1, ((length(Stack) - 2) - 0)),
                case ((Left == -1) andalso (Right == -1)) of
        true -> throw({continue, Stack_2, Tree});
        _ -> ok
    end,
                case ((Left == -1) orelse (Right == -1)) of
        true -> throw({return, false});
        _ -> ok
    end,
                Lnode = (case erlang:is_map(Tree) of true -> maps:get(Left, Tree, nil); _ -> lists:nth(Left + 1, Tree) end),
                Rnode = (case erlang:is_map(Tree) of true -> maps:get(Right, Tree, nil); _ -> lists:nth(Right + 1, Tree) end),
                case ((case erlang:is_map(Lnode) of true -> maps:get(0, Lnode, nil); _ -> lists:nth(0 + 1, Lnode) end) /= (case erlang:is_map(Rnode) of true -> maps:get(0, Rnode, nil); _ -> lists:nth(0 + 1, Rnode) end)) of
        true -> throw({return, false});
        _ -> ok
    end,
                Stack_3 = lists:append((case Stack_2 of nil -> []; _ -> Stack_2 end), [(case erlang:is_map(Lnode) of true -> maps:get(1, Lnode, nil); _ -> lists:nth(1 + 1, Lnode) end)]),
                Stack_4 = lists:append((case Stack_3 of nil -> []; _ -> Stack_3 end), [(case erlang:is_map(Rnode) of true -> maps:get(2, Rnode, nil); _ -> lists:nth(2 + 1, Rnode) end)]),
                Stack_5 = lists:append((case Stack_4 of nil -> []; _ -> Stack_4 end), [(case erlang:is_map(Lnode) of true -> maps:get(2, Lnode, nil); _ -> lists:nth(2 + 1, Lnode) end)]),
                Stack_6 = lists:append((case Stack_5 of nil -> []; _ -> Stack_5 end), [(case erlang:is_map(Rnode) of true -> maps:get(1, Rnode, nil); _ -> lists:nth(1 + 1, Rnode) end)]),
                Fun_loop(Stack_6, Tree)
            catch
                {continue, C0, C1} -> Fun_loop(C0, C1);
                {break, B0, B1} -> {B0, B1}
            end;
        _ -> {Stack, Tree}
    end
end,
{Stack_6, Tree} = Fun(Stack, Tree),
        true
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('symmetric_tree', make_symmetric_tree()),
    erlang:put('asymmetric_tree', make_asymmetric_tree()),
    io:format("~ts~n", [mochi_str(is_symmetric_tree(erlang:get('symmetric_tree')))]),
    io:format("~ts~n", [mochi_str(is_symmetric_tree(erlang:get('asymmetric_tree')))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
