;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define PI 3.141592653589793)
(define (floor x)
  (let/ec _return (begin
(define i (int x))
(if (> (exact->inexact i) x) (let ()
(set! i (- i 1))
) (void))
(_return (exact->inexact i))
))
)
(define (modf x m)
  (let/ec _return (begin
(_return (- x (* (floor (/ x m)) m)))
))
)
(define (sin_taylor x)
  (let/ec _return (begin
(define term x)
(define sum x)
(define i 1)
(let/ec _break (let loop ()
  (if (< i 10) (let/ec _cont
    (define k1 (* 2.0 (exact->inexact i)))
    (define k2 (let ([__l k1] [__r 1.0]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! term (/ (* (* (- term) x) x) (* k1 k2)))
    (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return sum)
))
)
(define (cos_taylor x)
  (let/ec _return (begin
(define term 1.0)
(define sum 1.0)
(define i 1)
(let/ec _break (let loop ()
  (if (< i 10) (let/ec _cont
    (define k1 (- (* 2.0 (exact->inexact i)) 1.0))
    (define k2 (* 2.0 (exact->inexact i)))
    (set! term (/ (* (* (- term) x) x) (* k1 k2)))
    (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return sum)
))
)
(define (convert_to_2d x y z scale distance)
  (let/ec _return (begin
(define projected_x (* (/ (* x distance) (let ([__l z] [__r distance]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) scale))
(define projected_y (* (/ (* y distance) (let ([__l z] [__r distance]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) scale))
(_return (list projected_x projected_y))
))
)
(define (rotate x y z axis angle)
  (let/ec _return (begin
(define angle (/ (* (/ (modf angle 360.0) 450.0) 180.0) PI))
(set! angle (modf angle (* 2.0 PI)))
(if (> angle PI) (let ()
(set! angle (- angle (* 2.0 PI)))
) (void))
(if (string=? axis "z") (let ()
(define new_x (- (* x (cos_taylor angle)) (* y (sin_taylor angle))))
(define new_y (let ([__l (* y (cos_taylor angle))] [__r (* x (sin_taylor angle))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define new_z z)
(_return (list new_x new_y new_z))
) (void))
(if (string=? axis "x") (let ()
(define new_y (- (* y (cos_taylor angle)) (* z (sin_taylor angle))))
(define new_z (let ([__l (* z (cos_taylor angle))] [__r (* y (sin_taylor angle))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define new_x x)
(_return (list new_x new_y new_z))
) (void))
(if (string=? axis "y") (let ()
(define new_x (- (* x (cos_taylor angle)) (* z (sin_taylor angle))))
(define new_z (let ([__l (* z (cos_taylor angle))] [__r (* x (sin_taylor angle))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define new_y y)
(_return (list new_x new_y new_z))
) (void))
(displayln "not a valid axis, choose one of 'x', 'y', 'z'")
(_return (list 0.0 0.0 0.0))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (convert_to_2d 1.0 2.0 3.0 10.0 10.0)))
(displayln (format "~a" (rotate 1.0 2.0 3.0 "y" 90.0)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
