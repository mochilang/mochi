;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define PI 3.141592653589793)
(define AXIS_A 6378137.0)
(define AXIS_B 6.356752314245e+06)
(define RADIUS 6378137.0)
(define (to_radians deg)
  (let/ec _return (begin
(_return (/ (* deg PI) 180.0))
))
)
(define (sin_taylor x)
  (let/ec _return (begin
(define term x)
(define sum x)
(define i 1)
(let/ec _break (let loop ()
  (if (< i 10) (let/ec _cont
    (define k1 (* 2.0 (exact->inexact i)))
    (define k2 (let ([__l k1] [__r 1.0]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! term (/ (* (* (- term) x) x) (* k1 k2)))
    (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return sum)
))
)
(define (cos_taylor x)
  (let/ec _return (begin
(define term 1.0)
(define sum 1.0)
(define i 1)
(let/ec _break (let loop ()
  (if (< i 10) (let/ec _cont
    (define k1 (- (* 2.0 (exact->inexact i)) 1.0))
    (define k2 (* 2.0 (exact->inexact i)))
    (set! term (/ (* (* (- term) x) x) (* k1 k2)))
    (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return sum)
))
)
(define (tan_approx x)
  (let/ec _return (begin
(_return (/ (sin_taylor x) (cos_taylor x)))
))
)
(define (sqrtApprox x)
  (let/ec _return (begin
(define guess (/ x 2.0))
(define i 0)
(let/ec _break (let loop ()
  (if (< i 20) (let/ec _cont
    (set! guess (/ (let ([__l guess] [__r (/ x guess)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return guess)
))
)
(define (atanApprox x)
  (let/ec _return (begin
(if (> x 1.0) (let ()
(_return (- (/ PI 2.0) (/ x (let ([__l (* x x)] [__r 0.28]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (void))
(if (< x (- 1.0)) (let ()
(_return (- (/ (- PI) 2.0) (/ x (let ([__l (* x x)] [__r 0.28]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (void))
(_return (/ x (let ([__l 1.0] [__r (* (* 0.28 x) x)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (atan2Approx y x)
  (let/ec _return (begin
(if (> x 0.0) (let ()
(define val (atanApprox (/ y x)))
(_return val)
) (void))
(if (< x 0.0) (let ()
(if (>= y 0.0) (let ()
(_return (let ([__l (atanApprox (/ y x))] [__r PI]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(_return (- (atanApprox (/ y x)) PI))
) (void))
(if (> y 0.0) (let ()
(_return (/ PI 2.0))
) (void))
(if (< y 0.0) (let ()
(_return (/ (- PI) 2.0))
) (void))
(_return 0.0)
))
)
(define (asinApprox x)
  (let/ec _return (begin
(define denom (sqrtApprox (- 1.0 (* x x))))
(define res (atan2Approx x denom))
(_return res)
))
)
(define (haversine_distance lat1 lon1 lat2 lon2)
  (let/ec _return (begin
(define flattening (/ (- AXIS_A AXIS_B) AXIS_A))
(define phi_1 (atanApprox (* (- 1.0 flattening) (tan_approx (to_radians lat1)))))
(define phi_2 (atanApprox (* (- 1.0 flattening) (tan_approx (to_radians lat2)))))
(define lambda_1 (to_radians lon1))
(define lambda_2 (to_radians lon2))
(define sin_sq_phi (sin_taylor (/ (- phi_2 phi_1) 2.0)))
(define sin_sq_lambda (sin_taylor (/ (- lambda_2 lambda_1) 2.0)))
(set! sin_sq_phi (* sin_sq_phi sin_sq_phi))
(set! sin_sq_lambda (* sin_sq_lambda sin_sq_lambda))
(define h_value (sqrtApprox (let ([__l sin_sq_phi] [__r (* (* (cos_taylor phi_1) (cos_taylor phi_2)) sin_sq_lambda)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return (* (* 2.0 RADIUS) (asinApprox h_value)))
))
)
(define SAN_FRANCISCO (list 37.774856 (- 122.424227)))
(define YOSEMITE (list 37.864742 (- 119.537521)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (haversine_distance (if SAN_FRANCISCO (if (hash? SAN_FRANCISCO) (hash-ref SAN_FRANCISCO 0 #f) (list-ref SAN_FRANCISCO (int 0))) #f) (if SAN_FRANCISCO (if (hash? SAN_FRANCISCO) (hash-ref SAN_FRANCISCO 1 #f) (list-ref SAN_FRANCISCO (int 1))) #f) (if YOSEMITE (if (hash? YOSEMITE) (hash-ref YOSEMITE 0 #f) (list-ref YOSEMITE (int 0))) #f) (if YOSEMITE (if (hash? YOSEMITE) (hash-ref YOSEMITE 1 #f) (list-ref YOSEMITE (int 1))) #f))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
