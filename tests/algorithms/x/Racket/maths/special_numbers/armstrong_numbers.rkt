;; Generated by Mochi 0.10.61 on 2025-08-08 17:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (pow_int base exp)
  (let/ec _return (begin
(define result 1)
(define i 0)
(let/ec _break (let loop ()
  (if (< i exp) (begin
    (let/ec _cont
      (set! result (* result base))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (armstrong_number n)
  (let/ec _return (begin
(if (< n 1) (let ()
(_return #f)
) (void))
(define digits 0)
(define temp n)
(let/ec _break (let loop ()
  (if (> temp 0) (begin
    (let/ec _cont
      (set! temp (floor (/ temp 10)))
      (set! digits (let ([__l digits] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define total 0)
(set! temp n)
(let/ec _break (let loop ()
  (if (> temp 0) (begin
    (let/ec _cont
      (define rem (modulo temp 10))
      (set! total (let ([__l total] [__r (pow_int rem digits)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! temp (floor (/ temp 10)))
    )
    (loop)) (void))))
(_return (equal? total n))
))
)
(define (pluperfect_number n)
  (let/ec _return (begin
(if (< n 1) (let ()
(_return #f)
) (void))
(define digit_histogram (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i 10) (begin
    (let/ec _cont
      (set! digit_histogram (append digit_histogram (list 0)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define digit_total 0)
(define temp n)
(let/ec _break (let loop ()
  (if (> temp 0) (begin
    (let/ec _cont
      (define rem (modulo temp 10))
      (set! digit_histogram (list-set digit_histogram (int rem) (let ([__l (if digit_histogram (if (hash? digit_histogram) (hash-ref digit_histogram rem #f) (list-ref digit_histogram (int rem))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! digit_total (let ([__l digit_total] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! temp (floor (/ temp 10)))
    )
    (loop)) (void))))
(define total 0)
(set! i 0)
(let/ec _break (let loop ()
  (if (< i 10) (begin
    (let/ec _cont
      (if (> (if digit_histogram (if (hash? digit_histogram) (hash-ref digit_histogram i #f) (list-ref digit_histogram (int i))) #f) 0) (let ()
(set! total (let ([__l total] [__r (* (if digit_histogram (if (hash? digit_histogram) (hash-ref digit_histogram i #f) (list-ref digit_histogram (int i))) #f) (pow_int i digit_total))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (equal? total n))
))
)
(define (narcissistic_number n)
  (let/ec _return (begin
(if (< n 1) (let ()
(_return #f)
) (void))
(define digits 0)
(define temp n)
(let/ec _break (let loop ()
  (if (> temp 0) (begin
    (let/ec _cont
      (set! temp (floor (/ temp 10)))
      (set! digits (let ([__l digits] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! temp n)
(define total 0)
(let/ec _break (let loop ()
  (if (> temp 0) (begin
    (let/ec _cont
      (define rem (modulo temp 10))
      (set! total (let ([__l total] [__r (pow_int rem digits)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! temp (floor (/ temp 10)))
    )
    (loop)) (void))))
(_return (equal? total n))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (armstrong_number 371))
(displayln (armstrong_number 200))
(displayln (pluperfect_number 371))
(displayln (pluperfect_number 200))
(displayln (narcissistic_number 371))
(displayln (narcissistic_number 200))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
