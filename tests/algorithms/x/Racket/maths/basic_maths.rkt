;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (pow_int base exp)
  (let/ec _return (begin
(define result 1)
(define i 0)
(let/ec _break (let loop ()
  (if (< i exp) (let/ec _cont
    (set! result (* result base))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (prime_factors n)
  (let/ec _return (begin
(if (<= n 0) (let ()
(panic "Only positive integers have prime factors")
) (void))
(define num n)
(define pf (list))
(let/ec _break (let loop ()
  (if (equal? (modulo num 2) 0) (let/ec _cont
    (set! pf (append pf (list 2)))
    (set! num (floor (/ num 2)))
    (loop)) (void))))
(define i 3)
(let/ec _break (let loop ()
  (if (<= (* i i) num) (let/ec _cont
    (let/ec _break (let loop ()
  (if (equal? (modulo num i) 0) (let/ec _cont
    (set! pf (append pf (list i)))
    (set! num (floor (/ num i)))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(if (> num 2) (let ()
(set! pf (append pf (list num)))
) (void))
(_return pf)
))
)
(define (number_of_divisors n)
  (let/ec _return (begin
(if (<= n 0) (let ()
(panic "Only positive numbers are accepted")
) (void))
(define num n)
(define div 1)
(define temp 1)
(let/ec _break (let loop ()
  (if (equal? (modulo num 2) 0) (let/ec _cont
    (set! temp (let ([__l temp] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! num (floor (/ num 2)))
    (loop)) (void))))
(set! div (* div temp))
(define i 3)
(let/ec _break (let loop ()
  (if (<= (* i i) num) (let/ec _cont
    (set! temp 1)
    (let/ec _break (let loop ()
  (if (equal? (modulo num i) 0) (let/ec _cont
    (set! temp (let ([__l temp] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! num (floor (/ num i)))
    (loop)) (void))))
    (set! div (* div temp))
    (set! i (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(if (> num 1) (let ()
(set! div (* div 2))
) (void))
(_return div)
))
)
(define (sum_of_divisors n)
  (let/ec _return (begin
(if (<= n 0) (let ()
(panic "Only positive numbers are accepted")
) (void))
(define num n)
(define s 1)
(define temp 1)
(let/ec _break (let loop ()
  (if (equal? (modulo num 2) 0) (let/ec _cont
    (set! temp (let ([__l temp] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! num (floor (/ num 2)))
    (loop)) (void))))
(if (> temp 1) (let ()
(set! s (* s (floor (/ (- (pow_int 2 temp) 1) (- 2 1)))))
) (void))
(define i 3)
(let/ec _break (let loop ()
  (if (<= (* i i) num) (let/ec _cont
    (set! temp 1)
    (let/ec _break (let loop ()
  (if (equal? (modulo num i) 0) (let/ec _cont
    (set! temp (let ([__l temp] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! num (floor (/ num i)))
    (loop)) (void))))
    (if (> temp 1) (let ()
(set! s (* s (floor (/ (- (pow_int i temp) 1) (- i 1)))))
) (void))
    (set! i (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return s)
))
)
(define (contains arr x)
  (let/ec _return (begin
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (if (equal? (if arr (if (hash? arr) (hash-ref arr idx #f) (list-ref arr (int idx))) #f) x) (let ()
(_return #t)
) (void))
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define (unique arr)
  (let/ec _return (begin
(define result (list))
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (define v (if arr (if (hash? arr) (hash-ref arr idx #f) (list-ref arr (int idx))) #f))
    (if (not (contains result v)) (let ()
(set! result (append result (list v)))
) (void))
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (euler_phi n)
  (let/ec _return (begin
(if (<= n 0) (let ()
(panic "Only positive numbers are accepted")
) (void))
(define s n)
(define factors (unique (prime_factors n)))
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx (cond [(not factors) 0] [(string? factors) (string-length factors)] [(hash? factors) (hash-count factors)] [else (length factors)])) (let/ec _cont
    (define x (if factors (if (hash? factors) (hash-ref factors idx #f) (list-ref factors (int idx))) #f))
    (set! s (* (floor (/ s x)) (- x 1)))
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return s)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (prime_factors 100)))
(displayln (format "~a" (number_of_divisors 100)))
(displayln (format "~a" (sum_of_divisors 100)))
(displayln (format "~a" (euler_phi 100)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
