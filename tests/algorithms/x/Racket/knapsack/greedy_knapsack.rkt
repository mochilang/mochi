;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (calc_profit profit weight max_weight)
  (let/ec _return (begin
(if (not (equal? (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)]) (cond [(not weight) 0] [(string? weight) (string-length weight)] [(hash? weight) (hash-count weight)] [else (length weight)]))) (let ()
(panic "The length of profit and weight must be same.")
) (void))
(if (<= max_weight 0) (let ()
(panic "max_weight must greater than zero.")
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)])) (let/ec _cont
    (if (< (if profit (if (hash? profit) (hash-ref profit i #f) (list-ref profit (int i))) #f) 0) (let ()
(panic "Profit can not be negative.")
) (void))
    (if (< (if weight (if (hash? weight) (hash-ref weight i #f) (list-ref weight (int i))) #f) 0) (let ()
(panic "Weight can not be negative.")
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define n (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)]))
(define used (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j n) (let/ec _cont
    (set! used (append used (list #f)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define limit 0)
(define gain 0.0)
(define count 0)
(let/ec _break (let loop ()
  (if (and (< limit max_weight) (< count n)) (let/ec _cont
    (define maxRatio (- 1.0))
    (define maxIndex (- 1))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k n) (let/ec _cont
    (if (not (if used (if (hash? used) (hash-ref used k #f) (list-ref used (int k))) #f)) (let ()
(define ratio (/ (exact->inexact (if profit (if (hash? profit) (hash-ref profit k #f) (list-ref profit (int k))) #f)) (exact->inexact (if weight (if (hash? weight) (hash-ref weight k #f) (list-ref weight (int k))) #f))))
(if (> ratio maxRatio) (let ()
(set! maxRatio ratio)
(set! maxIndex k)
) (void))
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (if (< maxIndex 0) (let ()
(_break)
) (void))
    (set! used (list-set used (int maxIndex) #t))
    (if (>= (- max_weight limit) (if weight (if (hash? weight) (hash-ref weight maxIndex #f) (list-ref weight (int maxIndex))) #f)) (let ()
(set! limit (let ([__l limit] [__r (if weight (if (hash? weight) (hash-ref weight maxIndex #f) (list-ref weight (int maxIndex))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! gain (let ([__l gain] [__r (exact->inexact (if profit (if (hash? profit) (hash-ref profit maxIndex #f) (list-ref profit (int maxIndex))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! gain (let ([__l gain] [__r (* (/ (exact->inexact (- max_weight limit)) (exact->inexact (if weight (if (hash? weight) (hash-ref weight maxIndex #f) (list-ref weight (int maxIndex))) #f))) (exact->inexact (if profit (if (hash? profit) (hash-ref profit maxIndex #f) (list-ref profit (int maxIndex))) #f)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_break)
))
    (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return gain)
))
)
(define (main)
  (let/ec _return (begin
(displayln (calc_profit (list 1 2 3) (list 3 4 5) 15))
(displayln (calc_profit (list 10 9 8) (list 3 4 5) 25))
(displayln (calc_profit (list 10 9 8) (list 3 4 5) 5))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
