;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (make_conn_mock)
  (let/ec _return (begin
(_return (hash "recv_called" 0 "send_called" 0 "close_called" 0))
))
)
(define (conn_recv conn size)
  (let/ec _return (begin
(set! conn (hash-set (or conn (hash)) "recv_called" (let ([__l (if conn (hash-ref conn "recv_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return 0)
))
)
(define (conn_send conn data)
  (let/ec _return (begin
(set! conn (hash-set (or conn (hash)) "send_called" (let ([__l (if conn (hash-ref conn "send_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (conn_close conn)
  (let/ec _return (begin
(set! conn (hash-set (or conn (hash)) "close_called" (let ([__l (if conn (hash-ref conn "close_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (make_socket_mock conn)
  (let/ec _return (begin
(_return (hash "bind_called" 0 "listen_called" 0 "accept_called" 0 "shutdown_called" 0 "close_called" 0 "conn" conn))
))
)
(define (socket_bind sock)
  (let/ec _return (begin
(set! sock (hash-set (or sock (hash)) "bind_called" (let ([__l (if sock (hash-ref sock "bind_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (socket_listen sock)
  (let/ec _return (begin
(set! sock (hash-set (or sock (hash)) "listen_called" (let ([__l (if sock (hash-ref sock "listen_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (socket_accept sock)
  (let/ec _return (begin
(set! sock (hash-set (or sock (hash)) "accept_called" (let ([__l (if sock (hash-ref sock "accept_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return (if sock (hash-ref sock "conn" #f) #f))
))
)
(define (socket_shutdown sock)
  (let/ec _return (begin
(set! sock (hash-set (or sock (hash)) "shutdown_called" (let ([__l (if sock (hash-ref sock "shutdown_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (socket_close sock)
  (let/ec _return (begin
(set! sock (hash-set (or sock (hash)) "close_called" (let ([__l (if sock (hash-ref sock "close_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (make_file_mock values)
  (let/ec _return (begin
(_return (hash "read_called" 0 "data" values))
))
)
(define (file_read f size)
  (let/ec _return (begin
(if (< (if f (hash-ref f "read_called" #f) #f) (cond [(string? (if f (hash-ref f "data" #f) #f)) (string-length (if f (hash-ref f "data" #f) #f))] [(hash? (if f (hash-ref f "data" #f) #f)) (hash-count (if f (hash-ref f "data" #f) #f))] [else (length (if f (hash-ref f "data" #f) #f))])) (let ()
(define value (if (if f (hash-ref f "data" #f) #f) (if (hash? (if f (hash-ref f "data" #f) #f)) (hash-ref (if f (hash-ref f "data" #f) #f) (if f (hash-ref f "read_called" #f) #f) #f) (list-ref (if f (hash-ref f "data" #f) #f) (int (if f (hash-ref f "read_called" #f) #f)))) #f))
(set! f (hash-set (or f (hash)) "read_called" (let ([__l (if f (hash-ref f "read_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return value)
) (void))
(set! f (hash-set (or f (hash)) "read_called" (let ([__l (if f (hash-ref f "read_called" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return 0)
))
)
(define (file_open)
  (let/ec _return (begin
(_return (make_file_mock (list 1 0)))
))
)
(define (send_file sock f)
  (let/ec _return (begin
(socket_bind sock)
(socket_listen sock)
(define conn (socket_accept sock))
(define _ (conn_recv conn 1024))
(define data (file_read f 1024))
(let/ec _break (let loop ()
  (if (not (equal? data 0)) (let/ec _cont
    (conn_send conn data)
    (set! data (file_read f 1024))
    (loop)) (void))))
(conn_close conn)
(socket_shutdown sock)
(socket_close sock)
))
)
(define (test_send_file_running_as_expected)
  (let/ec _return (begin
(define conn (make_conn_mock))
(define sock (make_socket_mock conn))
(define f (file_open))
(send_file sock f)
(if (and (and (and (and (and (and (and (and (equal? (if sock (hash-ref sock "bind_called" #f) #f) 1) (equal? (if sock (hash-ref sock "listen_called" #f) #f) 1)) (equal? (if sock (hash-ref sock "accept_called" #f) #f) 1)) (equal? (if conn (hash-ref conn "recv_called" #f) #f) 1)) (>= (if f (hash-ref f "read_called" #f) #f) 1)) (equal? (if conn (hash-ref conn "send_called" #f) #f) 1)) (equal? (if conn (hash-ref conn "close_called" #f) #f) 1)) (equal? (if sock (hash-ref sock "shutdown_called" #f) #f) 1)) (equal? (if sock (hash-ref sock "close_called" #f) #f) 1)) (let ()
(_return "pass")
) (void))
(_return "fail")
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (test_send_file_running_as_expected))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
