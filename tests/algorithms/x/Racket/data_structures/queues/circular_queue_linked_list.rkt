;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (create_queue capacity)
  (let/ec _return (begin
(define data (list))
(define next (list))
(define prev (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i capacity) (let/ec _cont
    (set! data (append data (list "")))
    (set! next (append next (list (modulo (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) capacity))))
    (set! prev (append prev (list (modulo (let ([__l (- i 1)] [__r capacity]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) capacity))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "data" data "next" next "prev" prev "front" 0 "rear" 0))
))
)
(define (is_empty q)
  (let/ec _return (begin
(_return (and (equal? (if q (hash-ref q "front" #f) #f) (if q (hash-ref q "rear" #f) #f)) (string=? (if (if q (hash-ref q "data" #f) #f) (if (hash? (if q (hash-ref q "data" #f) #f)) (hash-ref (if q (hash-ref q "data" #f) #f) (if q (hash-ref q "front" #f) #f) #f) (list-ref (if q (hash-ref q "data" #f) #f) (int (if q (hash-ref q "front" #f) #f)))) #f) "")))
))
)
(define (check_can_perform q)
  (let/ec _return (begin
(if (is_empty q) (let ()
(panic "Empty Queue")
) (void))
))
)
(define (check_is_full q)
  (let/ec _return (begin
(if (equal? (if (if q (hash-ref q "next" #f) #f) (if (hash? (if q (hash-ref q "next" #f) #f)) (hash-ref (if q (hash-ref q "next" #f) #f) (if q (hash-ref q "rear" #f) #f) #f) (list-ref (if q (hash-ref q "next" #f) #f) (int (if q (hash-ref q "rear" #f) #f)))) #f) (if q (hash-ref q "front" #f) #f)) (let ()
(panic "Full Queue")
) (void))
))
)
(define (peek q)
  (let/ec _return (begin
(check_can_perform q)
(_return (if (if q (hash-ref q "data" #f) #f) (if (hash? (if q (hash-ref q "data" #f) #f)) (hash-ref (if q (hash-ref q "data" #f) #f) (if q (hash-ref q "front" #f) #f) #f) (list-ref (if q (hash-ref q "data" #f) #f) (int (if q (hash-ref q "front" #f) #f)))) #f))
))
)
(define (enqueue q value)
  (let/ec _return (begin
(check_is_full q)
(if (not (is_empty q)) (let ()
(set! q (hash-set (or q (hash)) "rear" (if (if q (hash-ref q "next" #f) #f) (if (hash? (if q (hash-ref q "next" #f) #f)) (hash-ref (if q (hash-ref q "next" #f) #f) (if q (hash-ref q "rear" #f) #f) #f) (list-ref (if q (hash-ref q "next" #f) #f) (int (if q (hash-ref q "rear" #f) #f)))) #f)))
) (void))
(define data (if q (hash-ref q "data" #f) #f))
(set! data (list-set data (int (if q (hash-ref q "rear" #f) #f)) value))
(set! q (hash-set (or q (hash)) "data" data))
(_return q)
))
)
(define (dequeue q)
  (let/ec _return (begin
(check_can_perform q)
(define data (if q (hash-ref q "data" #f) #f))
(define val (if data (if (hash? data) (hash-ref data (if q (hash-ref q "front" #f) #f) #f) (list-ref data (int (if q (hash-ref q "front" #f) #f)))) #f))
(set! data (list-set data (int (if q (hash-ref q "front" #f) #f)) ""))
(set! q (hash-set (or q (hash)) "data" data))
(if (not (equal? (if q (hash-ref q "front" #f) #f) (if q (hash-ref q "rear" #f) #f))) (let ()
(set! q (hash-set (or q (hash)) "front" (if (if q (hash-ref q "next" #f) #f) (if (hash? (if q (hash-ref q "next" #f) #f)) (hash-ref (if q (hash-ref q "next" #f) #f) (if q (hash-ref q "front" #f) #f) #f) (list-ref (if q (hash-ref q "next" #f) #f) (int (if q (hash-ref q "front" #f) #f)))) #f)))
) (void))
(_return (hash "queue" q "value" val))
))
)
(define (main)
  (let/ec _return (begin
(define q (create_queue 3))
(displayln (format "~a" (is_empty q)))
(set! q (enqueue q "a"))
(set! q (enqueue q "b"))
(displayln (peek q))
(define res (dequeue q))
(set! q (if res (hash-ref res "queue" #f) #f))
(displayln (if res (hash-ref res "value" #f) #f))
(set! res (dequeue q))
(set! q (if res (hash-ref res "queue" #f) #f))
(displayln (if res (hash-ref res "value" #f) #f))
(displayln (format "~a" (is_empty q)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
