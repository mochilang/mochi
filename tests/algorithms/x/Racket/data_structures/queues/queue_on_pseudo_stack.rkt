;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (empty_queue)
  (let/ec _return (begin
(_return (hash "stack" (list) "length" 0))
))
)
(define (put q item)
  (let/ec _return (begin
(define s (append (if q (hash-ref q "stack" #f) #f) (list item)))
(_return (hash "stack" s "length" (let ([__l (if q (hash-ref q "length" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (drop_first xs)
  (let/ec _return (begin
(define res (list))
(define i 1)
(let/ec _break (let loop ()
  (if (< i (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (set! res (append res (list (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (drop_last xs)
  (let/ec _return (begin
(define res (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (- (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)]) 1)) (let/ec _cont
    (set! res (append res (list (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (rotate q rotation)
  (let/ec _return (begin
(define s (if q (hash-ref q "stack" #f) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (and (< i rotation) (> (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 0)) (let/ec _cont
    (define temp (if s (if (hash? s) (hash-ref s 0 #f) (list-ref s (int 0))) #f))
    (set! s (drop_first s))
    (set! s (append s (list temp)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "stack" s "length" (if q (hash-ref q "length" #f) #f)))
))
)
(define (get q)
  (let/ec _return (begin
(if (equal? (if q (hash-ref q "length" #f) #f) 0) (let ()
(panic "queue empty")
) (void))
(define q1 (rotate q 1))
(define v (if (if q1 (hash-ref q1 "stack" #f) #f) (if (hash? (if q1 (hash-ref q1 "stack" #f) #f)) (hash-ref (if q1 (hash-ref q1 "stack" #f) #f) (- (if q1 (hash-ref q1 "length" #f) #f) 1) #f) (list-ref (if q1 (hash-ref q1 "stack" #f) #f) (int (- (if q1 (hash-ref q1 "length" #f) #f) 1)))) #f))
(define s (drop_last (if q1 (hash-ref q1 "stack" #f) #f)))
(define q2 (hash "stack" s "length" (if q1 (hash-ref q1 "length" #f) #f)))
(set! q2 (rotate q2 (- (if q2 (hash-ref q2 "length" #f) #f) 1)))
(set! q2 (hash "stack" (if q2 (hash-ref q2 "stack" #f) #f) "length" (- (if q2 (hash-ref q2 "length" #f) #f) 1)))
(_return (hash "queue" q2 "value" v))
))
)
(define (front q)
  (let/ec _return (begin
(define r (get q))
(define q2 (put (if r (hash-ref r "queue" #f) #f) (if r (hash-ref r "value" #f) #f)))
(set! q2 (rotate q2 (- (if q2 (hash-ref q2 "length" #f) #f) 1)))
(_return (hash "queue" q2 "value" (if r (hash-ref r "value" #f) #f)))
))
)
(define (size q)
  (let/ec _return (begin
(_return (if q (hash-ref q "length" #f) #f))
))
)
(define (to_string q)
  (let/ec _return (begin
(define s "<")
(if (> (if q (hash-ref q "length" #f) #f) 0) (let ()
(set! s (string-append s (format "~a" (if (if q (hash-ref q "stack" #f) #f) (if (hash? (if q (hash-ref q "stack" #f) #f)) (hash-ref (if q (hash-ref q "stack" #f) #f) 0 #f) (list-ref (if q (hash-ref q "stack" #f) #f) (int 0))) #f))))
(define i 1)
(let/ec _break (let loop ()
  (if (< i (if q (hash-ref q "length" #f) #f)) (let/ec _cont
    (set! s (string-append (string-append s ", ") (format "~a" (if (if q (hash-ref q "stack" #f) #f) (if (hash? (if q (hash-ref q "stack" #f) #f)) (hash-ref (if q (hash-ref q "stack" #f) #f) i #f) (list-ref (if q (hash-ref q "stack" #f) #f) (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
(set! s (string-append s ">"))
(_return s)
))
)
(define (main)
  (let/ec _return (begin
(define q (empty_queue))
(set! q (put q 1))
(set! q (put q 2))
(set! q (put q 3))
(displayln (to_string q))
(define g (get q))
(set! q (if g (hash-ref g "queue" #f) #f))
(displayln (if g (hash-ref g "value" #f) #f))
(displayln (to_string q))
(define f (front q))
(set! q (if f (hash-ref f "queue" #f) #f))
(displayln (if f (hash-ref f "value" #f) #f))
(displayln (to_string q))
(displayln (size q))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
