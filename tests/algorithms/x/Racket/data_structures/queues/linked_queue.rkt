;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_queue)
  (let/ec _return (begin
(_return (hash "nodes" (list) "front" (- 0 1) "rear" (- 0 1)))
))
)
(define (is_empty q)
  (let/ec _return (begin
(_return (equal? (if q (hash-ref q "front" #f) #f) (- 0 1)))
))
)
(define (put q item)
  (let/ec _return (begin
(define node (hash "data" item "next" (- 0 1)))
(set! q (hash-set (or q (hash)) "nodes" (append (if q (hash-ref q "nodes" #f) #f) (list node))))
(define idx (- (cond [(string? (if q (hash-ref q "nodes" #f) #f)) (string-length (if q (hash-ref q "nodes" #f) #f))] [(hash? (if q (hash-ref q "nodes" #f) #f)) (hash-count (if q (hash-ref q "nodes" #f) #f))] [else (length (if q (hash-ref q "nodes" #f) #f))]) 1))
(if (equal? (if q (hash-ref q "front" #f) #f) (- 0 1)) (let ()
(set! q (hash-set (or q (hash)) "front" idx))
(set! q (hash-set (or q (hash)) "rear" idx))
) (let ()
(define nodes (if q (hash-ref q "nodes" #f) #f))
(set! nodes (list-set nodes (if q (hash-ref q "rear" #f) #f) (hash-set (list-ref nodes (if q (hash-ref q "rear" #f) #f)) "next" idx)))
(set! q (hash-set (or q (hash)) "nodes" nodes))
(set! q (hash-set (or q (hash)) "rear" idx))
))
))
)
(define (get q)
  (let/ec _return (begin
(if (is_empty q) (let ()
(panic "dequeue from empty queue")
) (void))
(define idx (if q (hash-ref q "front" #f) #f))
(define node (if (if q (hash-ref q "nodes" #f) #f) (if (hash? (if q (hash-ref q "nodes" #f) #f)) (hash-ref (if q (hash-ref q "nodes" #f) #f) idx #f) (list-ref (if q (hash-ref q "nodes" #f) #f) (int idx))) #f))
(set! q (hash-set (or q (hash)) "front" (if node (hash-ref node "next" #f) #f)))
(if (equal? (if q (hash-ref q "front" #f) #f) (- 0 1)) (let ()
(set! q (hash-set (or q (hash)) "rear" (- 0 1)))
) (void))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (length_ q)
  (let/ec _return (begin
(define count 0)
(define idx (if q (hash-ref q "front" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? idx (- 0 1))) (let/ec _cont
    (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! idx (if (if (if q (hash-ref q "nodes" #f) #f) (if (hash? (if q (hash-ref q "nodes" #f) #f)) (hash-ref (if q (hash-ref q "nodes" #f) #f) idx #f) (list-ref (if q (hash-ref q "nodes" #f) #f) (int idx))) #f) (hash-ref (if (if q (hash-ref q "nodes" #f) #f) (if (hash? (if q (hash-ref q "nodes" #f) #f)) (hash-ref (if q (hash-ref q "nodes" #f) #f) idx #f) (list-ref (if q (hash-ref q "nodes" #f) #f) (int idx))) #f) "next" #f) #f))
    (loop)) (void))))
(_return count)
))
)
(define (to_string q)
  (let/ec _return (begin
(define res "")
(define idx (if q (hash-ref q "front" #f) #f))
(define first #t)
(let/ec _break (let loop ()
  (if (not (equal? idx (- 0 1))) (let/ec _cont
    (define node (if (if q (hash-ref q "nodes" #f) #f) (if (hash? (if q (hash-ref q "nodes" #f) #f)) (hash-ref (if q (hash-ref q "nodes" #f) #f) idx #f) (list-ref (if q (hash-ref q "nodes" #f) #f) (int idx))) #f))
    (if first (let ()
(set! res (if node (hash-ref node "data" #f) #f))
(set! first #f)
) (let ()
(set! res (string-append (string-append res " <- ") (if node (hash-ref node "data" #f) #f)))
))
    (set! idx (if node (hash-ref node "next" #f) #f))
    (loop)) (void))))
(_return res)
))
)
(define (clear q)
  (let/ec _return (begin
(set! q (hash-set (or q (hash)) "nodes" (list)))
(set! q (hash-set (or q (hash)) "front" (- 0 1)))
(set! q (hash-set (or q (hash)) "rear" (- 0 1)))
))
)
(define queue (new_queue))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (is_empty queue)))
(put queue "5")
(put queue "9")
(put queue "python")
(displayln (format "~a" (is_empty queue)))
(displayln (get queue))
(put queue "algorithms")
(displayln (get queue))
(displayln (get queue))
(displayln (get queue))
(displayln (format "~a" (is_empty queue)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
