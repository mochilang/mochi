;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_queue items)
  (let/ec _return (begin
(_return (hash "stack1" items "stack2" (list)))
))
)
(define (len_queue q)
  (let/ec _return (begin
(_return (let ([__l (cond [(string? (if q (hash-ref q "stack1" #f) #f)) (string-length (if q (hash-ref q "stack1" #f) #f))] [(hash? (if q (hash-ref q "stack1" #f) #f)) (hash-count (if q (hash-ref q "stack1" #f) #f))] [else (length (if q (hash-ref q "stack1" #f) #f))])] [__r (cond [(string? (if q (hash-ref q "stack2" #f) #f)) (string-length (if q (hash-ref q "stack2" #f) #f))] [(hash? (if q (hash-ref q "stack2" #f) #f)) (hash-count (if q (hash-ref q "stack2" #f) #f))] [else (length (if q (hash-ref q "stack2" #f) #f))])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (str_queue q)
  (let/ec _return (begin
(define items (list))
(define i (- (cond [(string? (if q (hash-ref q "stack2" #f) #f)) (string-length (if q (hash-ref q "stack2" #f) #f))] [(hash? (if q (hash-ref q "stack2" #f) #f)) (hash-count (if q (hash-ref q "stack2" #f) #f))] [else (length (if q (hash-ref q "stack2" #f) #f))]) 1))
(let/ec _break (let loop ()
  (if (>= i 0) (let/ec _cont
    (set! items (append items (list (if (if q (hash-ref q "stack2" #f) #f) (if (hash? (if q (hash-ref q "stack2" #f) #f)) (hash-ref (if q (hash-ref q "stack2" #f) #f) i #f) (list-ref (if q (hash-ref q "stack2" #f) #f) (int i))) #f))))
    (set! i (- i 1))
    (loop)) (void))))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(string? (if q (hash-ref q "stack1" #f) #f)) (string-length (if q (hash-ref q "stack1" #f) #f))] [(hash? (if q (hash-ref q "stack1" #f) #f)) (hash-count (if q (hash-ref q "stack1" #f) #f))] [else (length (if q (hash-ref q "stack1" #f) #f))])) (let/ec _cont
    (set! items (append items (list (if (if q (hash-ref q "stack1" #f) #f) (if (hash? (if q (hash-ref q "stack1" #f) #f)) (hash-ref (if q (hash-ref q "stack1" #f) #f) j #f) (list-ref (if q (hash-ref q "stack1" #f) #f) (int j))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define s "Queue((")
(define k 0)
(let/ec _break (let loop ()
  (if (< k (cond [(string? items) (string-length items)] [(hash? items) (hash-count items)] [else (length items)])) (let/ec _cont
    (set! s (string-append s (format "~a" (if items (if (hash? items) (hash-ref items k #f) (list-ref items (int k))) #f))))
    (if (< k (- (cond [(string? items) (string-length items)] [(hash? items) (hash-count items)] [else (length items)]) 1)) (let ()
(set! s (string-append s ", "))
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! s (string-append s "))"))
(_return s)
))
)
(define (put q item)
  (let/ec _return (begin
(define s1 (if q (hash-ref q "stack1" #f) #f))
(set! s1 (append s1 (list item)))
(_return (hash "stack1" s1 "stack2" (if q (hash-ref q "stack2" #f) #f)))
))
)
(define (get q)
  (let/ec _return (begin
(define s1 (if q (hash-ref q "stack1" #f) #f))
(define s2 (if q (hash-ref q "stack2" #f) #f))
(if (equal? (cond [(string? s2) (string-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 0) (let ()
(let/ec _break (let loop ()
  (if (> (cond [(string? s1) (string-length s1)] [(hash? s1) (hash-count s1)] [else (length s1)]) 0) (let/ec _cont
    (define idx (- (cond [(string? s1) (string-length s1)] [(hash? s1) (hash-count s1)] [else (length s1)]) 1))
    (define v (if s1 (if (hash? s1) (hash-ref s1 idx #f) (list-ref s1 (int idx))) #f))
    (define new_s1 (list))
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i idx) (let/ec _cont
    (set! new_s1 (append new_s1 (list (if s1 (if (hash? s1) (hash-ref s1 i #f) (list-ref s1 (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! s1 new_s1)
    (set! s2 (append s2 (list v)))
    (loop)) (void))))
) (void))
(if (equal? (cond [(string? s2) (string-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 0) (let ()
(panic "Queue is empty")
) (void))
(define idx2 (- (cond [(string? s2) (string-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 1))
(define value (if s2 (if (hash? s2) (hash-ref s2 idx2 #f) (list-ref s2 (int idx2))) #f))
(define new_s2 (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j idx2) (let/ec _cont
    (set! new_s2 (append new_s2 (list (if s2 (if (hash? s2) (hash-ref s2 j #f) (list-ref s2 (int j))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! s2 new_s2)
(_return (hash "queue" (hash "stack1" s1 "stack2" s2) "value" value))
))
)
(define q (new_queue (list 10 20 30)))
(define r1 (get q))
(define r2 (get q))
(define r3 (get q))
(define r4 (get q))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(set! q (if r1 (hash-ref r1 "queue" #f) #f))
(displayln (if r1 (hash-ref r1 "value" #f) #f))
(set! q (put q 40))
(set! q (if r2 (hash-ref r2 "queue" #f) #f))
(displayln (if r2 (hash-ref r2 "value" #f) #f))
(set! q (if r3 (hash-ref r3 "queue" #f) #f))
(displayln (if r3 (hash-ref r3 "value" #f) #f))
(displayln (len_queue q))
(set! q (if r4 (hash-ref r4 "queue" #f) #f))
(displayln (if r4 (hash-ref r4 "value" #f) #f))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
