;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_queue items)
  (let/ec _return (begin
(_return (hash "entries" items))
))
)
(define (len_queue q)
  (let/ec _return (begin
(_return (cond [(string? (if q (hash-ref q "entries" #f) #f)) (string-length (if q (hash-ref q "entries" #f) #f))] [(hash? (if q (hash-ref q "entries" #f) #f)) (hash-count (if q (hash-ref q "entries" #f) #f))] [else (length (if q (hash-ref q "entries" #f) #f))]))
))
)
(define (str_queue q)
  (let/ec _return (begin
(define s "Queue((")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if q (hash-ref q "entries" #f) #f)) (string-length (if q (hash-ref q "entries" #f) #f))] [(hash? (if q (hash-ref q "entries" #f) #f)) (hash-count (if q (hash-ref q "entries" #f) #f))] [else (length (if q (hash-ref q "entries" #f) #f))])) (let/ec _cont
    (set! s (string-append s (format "~a" (if (if q (hash-ref q "entries" #f) #f) (if (hash? (if q (hash-ref q "entries" #f) #f)) (hash-ref (if q (hash-ref q "entries" #f) #f) i #f) (list-ref (if q (hash-ref q "entries" #f) #f) (int i))) #f))))
    (if (< i (- (cond [(string? (if q (hash-ref q "entries" #f) #f)) (string-length (if q (hash-ref q "entries" #f) #f))] [(hash? (if q (hash-ref q "entries" #f) #f)) (hash-count (if q (hash-ref q "entries" #f) #f))] [else (length (if q (hash-ref q "entries" #f) #f))]) 1)) (let ()
(set! s (string-append s ", "))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! s (string-append s "))"))
(_return s)
))
)
(define (put q item)
  (let/ec _return (begin
(define e (if q (hash-ref q "entries" #f) #f))
(set! e (append e (list item)))
(_return (hash "entries" e))
))
)
(define (get q)
  (let/ec _return (begin
(if (equal? (cond [(string? (if q (hash-ref q "entries" #f) #f)) (string-length (if q (hash-ref q "entries" #f) #f))] [(hash? (if q (hash-ref q "entries" #f) #f)) (hash-count (if q (hash-ref q "entries" #f) #f))] [else (length (if q (hash-ref q "entries" #f) #f))]) 0) (let ()
(panic "Queue is empty")
) (void))
(define value (if (if q (hash-ref q "entries" #f) #f) (if (hash? (if q (hash-ref q "entries" #f) #f)) (hash-ref (if q (hash-ref q "entries" #f) #f) 0 #f) (list-ref (if q (hash-ref q "entries" #f) #f) (int 0))) #f))
(define new_entries (list))
(define i 1)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if q (hash-ref q "entries" #f) #f)) (string-length (if q (hash-ref q "entries" #f) #f))] [(hash? (if q (hash-ref q "entries" #f) #f)) (hash-count (if q (hash-ref q "entries" #f) #f))] [else (length (if q (hash-ref q "entries" #f) #f))])) (let/ec _cont
    (set! new_entries (append new_entries (list (if (if q (hash-ref q "entries" #f) #f) (if (hash? (if q (hash-ref q "entries" #f) #f)) (hash-ref (if q (hash-ref q "entries" #f) #f) i #f) (list-ref (if q (hash-ref q "entries" #f) #f) (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "queue" (hash "entries" new_entries) "value" value))
))
)
(define (rotate q rotation)
  (let/ec _return (begin
(define e (if q (hash-ref q "entries" #f) #f))
(define r 0)
(let/ec _break (let loop ()
  (if (< r rotation) (let/ec _cont
    (if (> (cond [(string? e) (string-length e)] [(hash? e) (hash-count e)] [else (length e)]) 0) (let ()
(define first (if e (if (hash? e) (hash-ref e 0 #f) (list-ref e (int 0))) #f))
(define rest (list))
(define i 1)
(let/ec _break (let loop ()
  (if (< i (cond [(string? e) (string-length e)] [(hash? e) (hash-count e)] [else (length e)])) (let/ec _cont
    (set! rest (append rest (list (if e (if (hash? e) (hash-ref e i #f) (list-ref e (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! rest (append rest (list first)))
(set! e rest)
) (void))
    (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "entries" e))
))
)
(define (get_front q)
  (let/ec _return (begin
(_return (if (if q (hash-ref q "entries" #f) #f) (if (hash? (if q (hash-ref q "entries" #f) #f)) (hash-ref (if q (hash-ref q "entries" #f) #f) 0 #f) (list-ref (if q (hash-ref q "entries" #f) #f) (int 0))) #f))
))
)
(define q (new_queue (list)))
(define res (get q))
(define front (get_front q))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (len_queue q))
(set! q (put q 10))
(set! q (put q 20))
(set! q (put q 30))
(set! q (put q 40))
(displayln (str_queue q))
(set! q (if res (hash-ref res "queue" #f) #f))
(displayln (if res (hash-ref res "value" #f) #f))
(displayln (str_queue q))
(set! q (rotate q 2))
(displayln (str_queue q))
(displayln front)
(displayln (str_queue q))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
