;; Generated by Mochi 0.10.59 on 2025-08-07 08:27 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (parent_index child_idx)
  (let/ec _return (begin
(if (> child_idx 0) (let ()
(_return (floor (/ (- child_idx 1) 2)))
) (void))
(_return (- 1))
))
)
(define (left_child_idx parent_idx)
  (let/ec _return (begin
(_return (let ([__l (* 2 parent_idx)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (right_child_idx parent_idx)
  (let/ec _return (begin
(_return (let ([__l (* 2 parent_idx)] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (max_heapify h heap_size index)
  (let/ec _return (begin
(define largest index)
(define left (left_child_idx index))
(define right (right_child_idx index))
(if (and (< left heap_size) (> (if h (if (hash? h) (hash-ref h left #f) (list-ref h (int left))) #f) (if h (if (hash? h) (hash-ref h largest #f) (list-ref h (int largest))) #f))) (let ()
(set! largest left)
) (void))
(if (and (< right heap_size) (> (if h (if (hash? h) (hash-ref h right #f) (list-ref h (int right))) #f) (if h (if (hash? h) (hash-ref h largest #f) (list-ref h (int largest))) #f))) (let ()
(set! largest right)
) (void))
(if (not (equal? largest index)) (let ()
(define temp (if h (if (hash? h) (hash-ref h index #f) (list-ref h (int index))) #f))
(set! h (list-set h (int index) (if h (if (hash? h) (hash-ref h largest #f) (list-ref h (int largest))) #f)))
(set! h (list-set h (int largest) temp))
(max_heapify h heap_size largest)
) (void))
))
)
(define (build_max_heap h)
  (let/ec _return (begin
(define heap_size (cond [(string? h) (string-length h)] [(hash? h) (hash-count h)] [else (length h)]))
(define i (- (floor (/ heap_size 2)) 1))
(let/ec _break (let loop ()
  (if (>= i 0) (let/ec _cont
    (max_heapify h heap_size i)
    (set! i (- i 1))
    (loop)) (void))))
(_return heap_size)
))
)
(define (extract_max h heap_size)
  (let/ec _return (begin
(define max_value (if h (if (hash? h) (hash-ref h 0 #f) (list-ref h (int 0))) #f))
(set! h (list-set h (int 0) (if h (if (hash? h) (hash-ref h (- heap_size 1) #f) (list-ref h (int (- heap_size 1)))) #f)))
(max_heapify h (- heap_size 1) 0)
(_return max_value)
))
)
(define (insert h heap_size value)
  (let/ec _return (begin
(if (< heap_size (cond [(string? h) (string-length h)] [(hash? h) (hash-count h)] [else (length h)])) (let ()
(set! h (list-set h (int heap_size) value))
) (let ()
(set! h (append h (list value)))
))
(set! heap_size (let ([__l heap_size] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define idx (floor (/ (- heap_size 1) 2)))
(let/ec _break (let loop ()
  (if (>= idx 0) (let/ec _cont
    (max_heapify h heap_size idx)
    (set! idx (floor (/ (- idx 1) 2)))
    (loop)) (void))))
(_return heap_size)
))
)
(define (heap_sort h heap_size)
  (let/ec _return (begin
(define size heap_size)
(define j (- size 1))
(let/ec _break (let loop ()
  (if (> j 0) (let/ec _cont
    (define temp (if h (if (hash? h) (hash-ref h 0 #f) (list-ref h (int 0))) #f))
    (set! h (list-set h (int 0) (if h (if (hash? h) (hash-ref h j #f) (list-ref h (int j))) #f)))
    (set! h (list-set h (int j) temp))
    (set! size (- size 1))
    (max_heapify h size 0)
    (set! j (- j 1))
    (loop)) (void))))
))
)
(define (heap_to_string h heap_size)
  (let/ec _return (begin
(define s "[")
(define i 0)
(let/ec _break (let loop ()
  (if (< i heap_size) (let/ec _cont
    (set! s (string-append s (format "~a" (if h (if (hash? h) (hash-ref h i #f) (list-ref h (int i))) #f))))
    (if (< i (- heap_size 1)) (let ()
(set! s (string-append s ", "))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! s (string-append s "]"))
(_return s)
))
)
(define heap (list 103.0 9.0 1.0 7.0 11.0 15.0 25.0 201.0 209.0 107.0 5.0))
(define size (build_max_heap heap))
(define m (extract_max heap size))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (heap_to_string heap size))
(set! size (- size 1))
(displayln (format "~a" m))
(displayln (heap_to_string heap size))
(set! size (insert heap size 100.0))
(displayln (heap_to_string heap size))
(heap_sort heap size)
(displayln (heap_to_string heap size))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
