;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define NIL (- 0 1))
(define seed 1)
(define (set_seed s)
  (let/ec _return (begin
(set! seed s)
))
)
(define (randint a b)
  (let/ec _return (begin
(set! seed (modulo (let ([__l (* seed 1103515245)] [__r 12345]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2147483648))
(_return (let ([__l (modulo seed (let ([__l (- b a)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))] [__r a]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (rand_bool)
  (let/ec _return (begin
(_return (equal? (randint 0 1) 1))
))
)
(define nodes (list))
(define root NIL)
(define (new_heap)
  (let/ec _return (begin
(set! nodes (list))
(set! root NIL)
))
)
(define (merge r1 r2)
  (let/ec _return (begin
(if (equal? r1 NIL) (let ()
(_return r2)
) (void))
(if (equal? r2 NIL) (let ()
(_return r1)
) (void))
(if (> (if (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) "value" 0) #f) (if (if nodes (if (hash? nodes) (hash-ref nodes r2 #f) (list-ref nodes (int r2))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes r2 #f) (list-ref nodes (int r2))) #f) "value" 0) #f)) (let ()
(define tmp r1)
(set! r1 r2)
(set! r2 tmp)
) (void))
(if (rand_bool) (let ()
(define tmp (if (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) "left" 0) #f))
(set! nodes (list-set nodes r1 (hash-set (list-ref nodes r1) "left" (if (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) "right" 0) #f))))
(set! nodes (list-set nodes r1 (hash-set (list-ref nodes r1) "right" tmp)))
) (void))
(set! nodes (list-set nodes r1 (hash-set (list-ref nodes r1) "left" (merge (if (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes r1 #f) (list-ref nodes (int r1))) #f) "left" 0) #f) r2))))
(_return r1)
))
)
(define (insert value)
  (let/ec _return (begin
(define node (hash "value" value "left" NIL "right" NIL))
(set! nodes (append nodes (list node)))
(define idx (- (cond [(string? nodes) (string-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(set! root (merge root idx))
))
)
(define (top)
  (let/ec _return (begin
(if (equal? root NIL) (let ()
(_return 0)
) (void))
(_return (if (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) "value" 0) #f))
))
)
(define (pop)
  (let/ec _return (begin
(define result (top))
(define l (if (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) "left" 0) #f))
(define r (if (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) (hash-ref (if nodes (if (hash? nodes) (hash-ref nodes root #f) (list-ref nodes (int root))) #f) "right" 0) #f))
(set! root (merge l r))
(_return result)
))
)
(define (is_empty)
  (let/ec _return (begin
(_return (equal? root NIL))
))
)
(define (to_sorted_list)
  (let/ec _return (begin
(define res (list))
(let/ec _break (let loop ()
  (if (not (is_empty)) (let/ec _cont
    (set! res (append res (list (pop))))
    (loop)) (void))))
(_return res)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(set_seed 1)
(new_heap)
(insert 2)
(insert 3)
(insert 1)
(insert 5)
(insert 1)
(insert 7)
(displayln (to_sorted_list))
(new_heap)
(insert 1)
(insert (- 1))
(insert 0)
(displayln (to_sorted_list))
(new_heap)
(insert 3)
(insert 1)
(insert 3)
(insert 7)
(displayln (pop))
(displayln (pop))
(displayln (pop))
(displayln (pop))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
