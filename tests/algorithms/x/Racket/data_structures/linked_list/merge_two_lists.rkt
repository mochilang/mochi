;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (sort_list nums)
  (let/ec _return (begin
(define arr (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? nums) (string-length nums)] [(hash? nums) (hash-count nums)] [else (length nums)])) (let/ec _cont
    (set! arr (append arr (list (if nums (if (hash? nums) (hash-ref nums i #f) (list-ref nums (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (define k (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (let/ec _break (let loop ()
  (if (< k (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (if (< (if arr (if (hash? arr) (hash-ref arr k #f) (list-ref arr (int k))) #f) (if arr (if (hash? arr) (hash-ref arr j #f) (list-ref arr (int j))) #f)) (let ()
(define tmp (if arr (if (hash? arr) (hash-ref arr j #f) (list-ref arr (int j))) #f))
(set! arr (list-set arr (int j) (if arr (if (hash? arr) (hash-ref arr k #f) (list-ref arr (int k))) #f)))
(set! arr (list-set arr (int k) tmp))
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return arr)
))
)
(define (make_sorted_linked_list ints)
  (let/ec _return (begin
(_return (hash "values" (sort_list ints)))
))
)
(define (len_sll sll)
  (let/ec _return (begin
(_return (cond [(string? (if sll (hash-ref sll "values" #f) #f)) (string-length (if sll (hash-ref sll "values" #f) #f))] [(hash? (if sll (hash-ref sll "values" #f) #f)) (hash-count (if sll (hash-ref sll "values" #f) #f))] [else (length (if sll (hash-ref sll "values" #f) #f))]))
))
)
(define (str_sll sll)
  (let/ec _return (begin
(define res "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if sll (hash-ref sll "values" #f) #f)) (string-length (if sll (hash-ref sll "values" #f) #f))] [(hash? (if sll (hash-ref sll "values" #f) #f)) (hash-count (if sll (hash-ref sll "values" #f) #f))] [else (length (if sll (hash-ref sll "values" #f) #f))])) (let/ec _cont
    (set! res (string-append res (format "~a" (if (if sll (hash-ref sll "values" #f) #f) (if (hash? (if sll (hash-ref sll "values" #f) #f)) (hash-ref (if sll (hash-ref sll "values" #f) #f) i #f) (list-ref (if sll (hash-ref sll "values" #f) #f) (int i))) #f))))
    (if (< (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (cond [(string? (if sll (hash-ref sll "values" #f) #f)) (string-length (if sll (hash-ref sll "values" #f) #f))] [(hash? (if sll (hash-ref sll "values" #f) #f)) (hash-count (if sll (hash-ref sll "values" #f) #f))] [else (length (if sll (hash-ref sll "values" #f) #f))])) (let ()
(set! res (string-append res " -> "))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (merge_lists a b)
  (let/ec _return (begin
(define combined (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if a (hash-ref a "values" #f) #f)) (string-length (if a (hash-ref a "values" #f) #f))] [(hash? (if a (hash-ref a "values" #f) #f)) (hash-count (if a (hash-ref a "values" #f) #f))] [else (length (if a (hash-ref a "values" #f) #f))])) (let/ec _cont
    (set! combined (append combined (list (if (if a (hash-ref a "values" #f) #f) (if (hash? (if a (hash-ref a "values" #f) #f)) (hash-ref (if a (hash-ref a "values" #f) #f) i #f) (list-ref (if a (hash-ref a "values" #f) #f) (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if b (hash-ref b "values" #f) #f)) (string-length (if b (hash-ref b "values" #f) #f))] [(hash? (if b (hash-ref b "values" #f) #f)) (hash-count (if b (hash-ref b "values" #f) #f))] [else (length (if b (hash-ref b "values" #f) #f))])) (let/ec _cont
    (set! combined (append combined (list (if (if b (hash-ref b "values" #f) #f) (if (hash? (if b (hash-ref b "values" #f) #f)) (hash-ref (if b (hash-ref b "values" #f) #f) i #f) (list-ref (if b (hash-ref b "values" #f) #f) (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (make_sorted_linked_list combined))
))
)
(define (main)
  (let/ec _return (begin
(define test_data_odd (list 3 9 (- 11) 0 7 5 1 (- 1)))
(define test_data_even (list 4 6 2 0 8 10 3 (- 2)))
(define sll_one (make_sorted_linked_list test_data_odd))
(define sll_two (make_sorted_linked_list test_data_even))
(define merged (merge_lists sll_one sll_two))
(displayln (format "~a" (len_sll merged)))
(displayln (str_sll merged))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
