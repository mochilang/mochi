;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_deque)
  (let/ec _return (begin
(define nodes (list))
(set! nodes (append nodes (list (hash "data" "" "prev" (- 1) "next" 1))))
(set! nodes (append nodes (list (hash "data" "" "prev" 0 "next" (- 1)))))
(_return (hash "nodes" nodes "header" 0 "trailer" 1 "size" 0))
))
)
(define (is_empty d)
  (let/ec _return (begin
(_return (equal? (if d (hash-ref d "size" #f) #f) 0))
))
)
(define (front d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "List is empty")
) (void))
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define idx (if head (hash-ref head "next" #f) #f))
(define node (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) idx #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int idx))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (back d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "List is empty")
) (void))
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define idx (if tail (hash-ref tail "prev" #f) #f))
(define node (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) idx #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int idx))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (insert d pred value succ)
  (let/ec _return (begin
(define nodes (if d (hash-ref d "nodes" #f) #f))
(define new_idx (cond [(string? nodes) (string-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]))
(set! nodes (append nodes (list (hash "data" value "prev" pred "next" succ))))
(define pred_node (if nodes (if (hash? nodes) (hash-ref nodes pred #f) (list-ref nodes (int pred))) #f))
(set! pred_node (hash-set (or pred_node (hash)) "next" new_idx))
(set! nodes (list-set nodes (int pred) pred_node))
(define succ_node (if nodes (if (hash? nodes) (hash-ref nodes succ #f) (list-ref nodes (int succ))) #f))
(set! succ_node (hash-set (or succ_node (hash)) "prev" new_idx))
(set! nodes (list-set nodes (int succ) succ_node))
(set! d (hash-set (or d (hash)) "nodes" nodes))
(set! d (hash-set (or d (hash)) "size" (let ([__l (if d (hash-ref d "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(_return d)
))
)
(define (delete d idx)
  (let/ec _return (begin
(define nodes (if d (hash-ref d "nodes" #f) #f))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (list-ref nodes (int idx))) #f))
(define pred (if node (hash-ref node "prev" #f) #f))
(define succ (if node (hash-ref node "next" #f) #f))
(define pred_node (if nodes (if (hash? nodes) (hash-ref nodes pred #f) (list-ref nodes (int pred))) #f))
(set! pred_node (hash-set (or pred_node (hash)) "next" succ))
(set! nodes (list-set nodes (int pred) pred_node))
(define succ_node (if nodes (if (hash? nodes) (hash-ref nodes succ #f) (list-ref nodes (int succ))) #f))
(set! succ_node (hash-set (or succ_node (hash)) "prev" pred))
(set! nodes (list-set nodes (int succ) succ_node))
(define val (if node (hash-ref node "data" #f) #f))
(set! d (hash-set (or d (hash)) "nodes" nodes))
(set! d (hash-set (or d (hash)) "size" (- (if d (hash-ref d "size" #f) #f) 1)))
(_return (hash "deque" d "value" val))
))
)
(define (add_first d value)
  (let/ec _return (begin
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define succ (if head (hash-ref head "next" #f) #f))
(_return (insert d (if d (hash-ref d "header" #f) #f) value succ))
))
)
(define (add_last d value)
  (let/ec _return (begin
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define pred (if tail (hash-ref tail "prev" #f) #f))
(_return (insert d pred value (if d (hash-ref d "trailer" #f) #f)))
))
)
(define (remove_first d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "remove_first from empty list")
) (void))
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define idx (if head (hash-ref head "next" #f) #f))
(_return (delete d idx))
))
)
(define (remove_last d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "remove_first from empty list")
) (void))
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (list-ref (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define idx (if tail (hash-ref tail "prev" #f) #f))
(_return (delete d idx))
))
)
(define (main)
  (let/ec _return (begin
(define d (new_deque))
(set! d (add_first d "A"))
(displayln (front d))
(set! d (add_last d "B"))
(displayln (back d))
(define r (remove_first d))
(set! d (if r (hash-ref r "deque" #f) #f))
(displayln (if r (hash-ref r "value" #f) #f))
(set! r (remove_last d))
(set! d (if r (hash-ref r "deque" #f) #f))
(displayln (if r (hash-ref r "value" #f) #f))
(displayln (format "~a" (is_empty d)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
