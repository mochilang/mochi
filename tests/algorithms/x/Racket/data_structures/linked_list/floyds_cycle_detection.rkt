;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define NULL (- 0 1))
(define (empty_list)
  (let/ec _return (begin
(_return (hash "next" (list) "head" NULL))
))
)
(define (add_node list_ value)
  (let/ec _return (begin
(define nexts (if list_ (hash-ref list_ "next" #f) #f))
(define new_index (cond [(string? nexts) (string-length nexts)] [(hash? nexts) (hash-count nexts)] [else (length nexts)]))
(set! nexts (append nexts (list NULL)))
(if (equal? (if list_ (hash-ref list_ "head" #f) #f) NULL) (let ()
(_return (hash "next" nexts "head" new_index))
) (void))
(define last (if list_ (hash-ref list_ "head" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? (if nexts (if (hash? nexts) (hash-ref nexts last #f) (list-ref nexts (int last))) #f) NULL)) (let/ec _cont
    (set! last (if nexts (if (hash? nexts) (hash-ref nexts last #f) (list-ref nexts (int last))) #f))
    (loop)) (void))))
(define new_nexts (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? nexts) (string-length nexts)] [(hash? nexts) (hash-count nexts)] [else (length nexts)])) (let/ec _cont
    (if (equal? i last) (let ()
(set! new_nexts (append new_nexts (list new_index)))
) (let ()
(set! new_nexts (append new_nexts (list (if nexts (if (hash? nexts) (hash-ref nexts i #f) (list-ref nexts (int i))) #f))))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "next" new_nexts "head" (if list_ (hash-ref list_ "head" #f) #f)))
))
)
(define (set_next list_ index next_index)
  (let/ec _return (begin
(define nexts (if list_ (hash-ref list_ "next" #f) #f))
(define new_nexts (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? nexts) (string-length nexts)] [(hash? nexts) (hash-count nexts)] [else (length nexts)])) (let/ec _cont
    (if (equal? i index) (let ()
(set! new_nexts (append new_nexts (list next_index)))
) (let ()
(set! new_nexts (append new_nexts (list (if nexts (if (hash? nexts) (hash-ref nexts i #f) (list-ref nexts (int i))) #f))))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "next" new_nexts "head" (if list_ (hash-ref list_ "head" #f) #f)))
))
)
(define (detect_cycle list_)
  (let/ec _return (begin
(if (equal? (if list_ (hash-ref list_ "head" #f) #f) NULL) (let ()
(_return #f)
) (void))
(define nexts (if list_ (hash-ref list_ "next" #f) #f))
(define slow (if list_ (hash-ref list_ "head" #f) #f))
(define fast (if list_ (hash-ref list_ "head" #f) #f))
(let/ec _break (let loop ()
  (if (and (not (equal? fast NULL)) (not (equal? (if nexts (if (hash? nexts) (hash-ref nexts fast #f) (list-ref nexts (int fast))) #f) NULL))) (let/ec _cont
    (set! slow (if nexts (if (hash? nexts) (hash-ref nexts slow #f) (list-ref nexts (int slow))) #f))
    (set! fast (if nexts (if (hash? nexts) (hash-ref nexts (if nexts (if (hash? nexts) (hash-ref nexts fast #f) (list-ref nexts (int fast))) #f) #f) (list-ref nexts (int (if nexts (if (hash? nexts) (hash-ref nexts fast #f) (list-ref nexts (int fast))) #f)))) #f))
    (if (equal? slow fast) (let ()
(_return #t)
) (void))
    (loop)) (void))))
(_return #f)
))
)
(define (main)
  (let/ec _return (begin
(define ll (empty_list))
(set! ll (add_node ll 1))
(set! ll (add_node ll 2))
(set! ll (add_node ll 3))
(set! ll (add_node ll 4))
(set! ll (set_next ll 3 1))
(displayln (detect_cycle ll))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
