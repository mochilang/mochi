;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (split_custom s sep)
  (let/ec _return (begin
(define res (list))
(define current "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])) (let/ec _cont
    (define ch (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (string=? ch sep) (let ()
(set! res (append res (list current)))
(set! current "")
) (let ()
(set! current (string-append current ch))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! res (append res (list current)))
(_return res)
))
)
(define (tokenize s)
  (let/ec _return (begin
(define parts (split_custom s " "))
(define res (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? parts) (string-length parts)] [(hash? parts) (hash-count parts)] [else (length parts)])) (let/ec _cont
    (define p (if parts (if (hash? parts) (hash-ref parts i #f) (list-ref parts (int i))) #f))
    (if (not (string=? p "")) (let ()
(set! res (append res (list p)))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (is_digit ch)
  (let/ec _return (begin
(_return (and (string>=? ch "0") (string<=? ch "9")))
))
)
(define (is_operand token)
  (let/ec _return (begin
(if (string=? token "") (let ()
(_return #f)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? token) (string-length token)] [(hash? token) (hash-count token)] [else (length token)])) (let/ec _cont
    (define ch (slice token i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (not (is_digit ch)) (let ()
(_return #f)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #t)
))
)
(define (to_int token)
  (let/ec _return (begin
(define res 0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? token) (string-length token)] [(hash? token) (hash-count token)] [else (length token)])) (let/ec _cont
    (set! res (let ([__l (* res 10)] [__r (int (slice token i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (apply_op op a b)
  (let/ec _return (begin
(if (string=? op "+") (let ()
(_return (let ([__l a] [__r b]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (string=? op "-") (let ()
(_return (- a b))
) (void))
(if (string=? op "*") (let ()
(_return (* a b))
) (void))
(if (string=? op "/") (let ()
(_return (/ a b))
) (void))
(_return 0.0)
))
)
(define (evaluate expression)
  (let/ec _return (begin
(define tokens (tokenize expression))
(define stack (list))
(define i (- (cond [(string? tokens) (string-length tokens)] [(hash? tokens) (hash-count tokens)] [else (length tokens)]) 1))
(let/ec _break (let loop ()
  (if (>= i 0) (let/ec _cont
    (define token (if tokens (if (hash? tokens) (hash-ref tokens i #f) (list-ref tokens (int i))) #f))
    (if (not (string=? token "")) (let ()
(if (is_operand token) (let ()
(set! stack (append stack (list (exact->inexact (to_int token)))))
) (let ()
(define o1 (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))
(define o2 (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2)))) #f))
(set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2)))
(define res (apply_op token o1 o2))
(set! stack (append stack (list res)))
))
) (void))
    (set! i (- i 1))
    (loop)) (void))))
(_return (if stack (if (hash? stack) (hash-ref stack 0 #f) (list-ref stack (int 0))) #f))
))
)
(define (eval_rec tokens pos)
  (let/ec _return (begin
(define token (if tokens (if (hash? tokens) (hash-ref tokens pos #f) (list-ref tokens (int pos))) #f))
(define next (let ([__l pos] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (is_operand token) (let ()
(_return (list (exact->inexact (to_int token)) (exact->inexact next)))
) (void))
(define left (eval_rec tokens next))
(define a (if left (if (hash? left) (hash-ref left 0 #f) (list-ref left (int 0))) #f))
(define p1 (int (if left (if (hash? left) (hash-ref left 1 #f) (list-ref left (int 1))) #f)))
(define right (eval_rec tokens p1))
(define b (if right (if (hash? right) (hash-ref right 0 #f) (list-ref right (int 0))) #f))
(define p2 (if right (if (hash? right) (hash-ref right 1 #f) (list-ref right (int 1))) #f))
(_return (list (apply_op token a b) p2))
))
)
(define (evaluate_recursive expression)
  (let/ec _return (begin
(define tokens (tokenize expression))
(define res (eval_rec tokens 0))
(_return (if res (if (hash? res) (hash-ref res 0 #f) (list-ref res (int 0))) #f))
))
)
(define test_expression "+ 9 * 2 6")
(define test_expression2 "/ * 10 2 + 4 1 ")
(define test_expression3 "+ * 2 3 / 8 4")
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (evaluate test_expression)))
(displayln (format "~a" (evaluate test_expression2)))
(displayln (format "~a" (evaluate_recursive test_expression3)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
