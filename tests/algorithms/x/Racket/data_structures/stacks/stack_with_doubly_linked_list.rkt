;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (empty_stack)
  (let/ec _return (begin
(_return (hash "nodes" (list) "head" (- 0 1)))
))
)
(define (push stack value)
  (let/ec _return (begin
(define nodes (if stack (hash-ref stack "nodes" #f) #f))
(define idx (cond [(string? nodes) (string-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]))
(define new_node (hash "data" value "next" (if stack (hash-ref stack "head" #f) #f) "prev" (- 0 1)))
(set! nodes (append nodes (list new_node)))
(if (not (equal? (if stack (hash-ref stack "head" #f) #f) (- 0 1))) (let ()
(define head_node (if nodes (if (hash? nodes) (hash-ref nodes (if stack (hash-ref stack "head" #f) #f) #f) (list-ref nodes (int (if stack (hash-ref stack "head" #f) #f)))) #f))
(set! head_node (hash-set (or head_node (hash)) "prev" idx))
(set! nodes (list-set nodes (int (if stack (hash-ref stack "head" #f) #f)) head_node))
) (void))
(_return (hash "nodes" nodes "head" idx))
))
)
(define (pop stack)
  (let/ec _return (begin
(if (equal? (if stack (hash-ref stack "head" #f) #f) (- 0 1)) (let ()
(_return (hash "stack" stack "value" 0 "ok" #f))
) (void))
(define nodes (if stack (hash-ref stack "nodes" #f) #f))
(define head_node (if nodes (if (hash? nodes) (hash-ref nodes (if stack (hash-ref stack "head" #f) #f) #f) (list-ref nodes (int (if stack (hash-ref stack "head" #f) #f)))) #f))
(define value (if head_node (hash-ref head_node "data" #f) #f))
(define next_idx (if head_node (hash-ref head_node "next" #f) #f))
(if (not (equal? next_idx (- 0 1))) (let ()
(define next_node (if nodes (if (hash? nodes) (hash-ref nodes next_idx #f) (list-ref nodes (int next_idx))) #f))
(set! next_node (hash-set (or next_node (hash)) "prev" (- 0 1)))
(set! nodes (list-set nodes (int next_idx) next_node))
) (void))
(define new_stack (hash "nodes" nodes "head" next_idx))
(_return (hash "stack" new_stack "value" value "ok" #t))
))
)
(define (top stack)
  (let/ec _return (begin
(if (equal? (if stack (hash-ref stack "head" #f) #f) (- 0 1)) (let ()
(_return (hash "value" 0 "ok" #f))
) (void))
(define node (if (if stack (hash-ref stack "nodes" #f) #f) (if (hash? (if stack (hash-ref stack "nodes" #f) #f)) (hash-ref (if stack (hash-ref stack "nodes" #f) #f) (if stack (hash-ref stack "head" #f) #f) #f) (list-ref (if stack (hash-ref stack "nodes" #f) #f) (int (if stack (hash-ref stack "head" #f) #f)))) #f))
(_return (hash "value" (if node (hash-ref node "data" #f) #f) "ok" #t))
))
)
(define (size stack)
  (let/ec _return (begin
(define count 0)
(define idx (if stack (hash-ref stack "head" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? idx (- 0 1))) (let/ec _cont
    (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define node (if (if stack (hash-ref stack "nodes" #f) #f) (if (hash? (if stack (hash-ref stack "nodes" #f) #f)) (hash-ref (if stack (hash-ref stack "nodes" #f) #f) idx #f) (list-ref (if stack (hash-ref stack "nodes" #f) #f) (int idx))) #f))
    (set! idx (if node (hash-ref node "next" #f) #f))
    (loop)) (void))))
(_return count)
))
)
(define (is_empty stack)
  (let/ec _return (begin
(_return (equal? (if stack (hash-ref stack "head" #f) #f) (- 0 1)))
))
)
(define (print_stack stack)
  (let/ec _return (begin
(displayln "stack elements are:")
(define idx (if stack (hash-ref stack "head" #f) #f))
(define s "")
(let/ec _break (let loop ()
  (if (not (equal? idx (- 0 1))) (let/ec _cont
    (define node (if (if stack (hash-ref stack "nodes" #f) #f) (if (hash? (if stack (hash-ref stack "nodes" #f) #f)) (hash-ref (if stack (hash-ref stack "nodes" #f) #f) idx #f) (list-ref (if stack (hash-ref stack "nodes" #f) #f) (int idx))) #f))
    (set! s (string-append (string-append s (format "~a" (if node (hash-ref node "data" #f) #f))) "->"))
    (set! idx (if node (hash-ref node "next" #f) #f))
    (loop)) (void))))
(if (> (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 0) (let ()
(displayln s)
) (void))
))
)
(define (main)
  (let/ec _return (begin
(define stack (empty_stack))
(displayln "Stack operations using Doubly LinkedList")
(set! stack (push stack 4))
(set! stack (push stack 5))
(set! stack (push stack 6))
(set! stack (push stack 7))
(print_stack stack)
(define t (top stack))
(if (if t (hash-ref t "ok" #f) #f) (let ()
(displayln (string-append "Top element is " (format "~a" (if t (hash-ref t "value" #f) #f))))
) (let ()
(displayln "Top element is None")
))
(displayln (string-append "Size of the stack is " (format "~a" (size stack))))
(define p (pop stack))
(set! stack (if p (hash-ref p "stack" #f) #f))
(set! p (pop stack))
(set! stack (if p (hash-ref p "stack" #f) #f))
(print_stack stack)
(displayln (string-append "stack is empty: " (format "~a" (is_empty stack))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
