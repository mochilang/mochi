;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (make_stack)
  (let/ec _return (begin
(_return (hash "main_queue" (list) "temp_queue" (list)))
))
)
(define (push s item)
  (let/ec _return (begin
(set! s (hash-set (or s (hash)) "temp_queue" (append (if s (hash-ref s "temp_queue" #f) #f) (list item))))
(let/ec _break (let loop ()
  (if (> (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]) 0) (let/ec _cont
    (set! s (hash-set (or s (hash)) "temp_queue" (append (if s (hash-ref s "temp_queue" #f) #f) (list (if (if s (hash-ref s "main_queue" #f) #f) (if (hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-ref (if s (hash-ref s "main_queue" #f) #f) 0 #f) (list-ref (if s (hash-ref s "main_queue" #f) #f) (int 0))) #f)))))
    (set! s (hash-set (or s (hash)) "main_queue" (slice (if s (hash-ref s "main_queue" #f) #f) 1 (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]))))
    (loop)) (void))))
(define new_main (if s (hash-ref s "temp_queue" #f) #f))
(set! s (hash-set (or s (hash)) "temp_queue" (if s (hash-ref s "main_queue" #f) #f)))
(set! s (hash-set (or s (hash)) "main_queue" new_main))
))
)
(define (pop s)
  (let/ec _return (begin
(if (equal? (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]) 0) (let ()
(panic "pop from empty stack")
) (void))
(define item (if (if s (hash-ref s "main_queue" #f) #f) (if (hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-ref (if s (hash-ref s "main_queue" #f) #f) 0 #f) (list-ref (if s (hash-ref s "main_queue" #f) #f) (int 0))) #f))
(set! s (hash-set (or s (hash)) "main_queue" (slice (if s (hash-ref s "main_queue" #f) #f) 1 (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]))))
(_return item)
))
)
(define (peek s)
  (let/ec _return (begin
(if (equal? (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]) 0) (let ()
(panic "peek from empty stack")
) (void))
(_return (if (if s (hash-ref s "main_queue" #f) #f) (if (hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-ref (if s (hash-ref s "main_queue" #f) #f) 0 #f) (list-ref (if s (hash-ref s "main_queue" #f) #f) (int 0))) #f))
))
)
(define (is_empty s)
  (let/ec _return (begin
(_return (equal? (cond [(string? (if s (hash-ref s "main_queue" #f) #f)) (string-length (if s (hash-ref s "main_queue" #f) #f))] [(hash? (if s (hash-ref s "main_queue" #f) #f)) (hash-count (if s (hash-ref s "main_queue" #f) #f))] [else (length (if s (hash-ref s "main_queue" #f) #f))]) 0))
))
)
(define stack (make_stack))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(push stack 1)
(push stack 2)
(push stack 3)
(displayln (format "~a" (peek stack)))
(displayln (format "~a" (pop stack)))
(displayln (format "~a" (peek stack)))
(displayln (format "~a" (pop stack)))
(displayln (format "~a" (pop stack)))
(displayln (format "~a" (is_empty stack)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
