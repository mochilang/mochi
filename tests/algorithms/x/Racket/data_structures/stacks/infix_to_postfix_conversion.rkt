;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define PRECEDENCES (hash "+" 1 "-" 1 "*" 2 "/" 2 "^" 3))
(define ASSOCIATIVITIES (hash "+" "LR" "-" "LR" "*" "LR" "/" "LR" "^" "RL"))
(define (precedence ch)
  (let/ec _return (begin
(if (hash-has-key? PRECEDENCES ch) (let ()
(_return (if PRECEDENCES (hash-ref PRECEDENCES ch 0) #f))
) (void))
(_return (- 1))
))
)
(define (associativity ch)
  (let/ec _return (begin
(if (hash-has-key? ASSOCIATIVITIES ch) (let ()
(_return (if ASSOCIATIVITIES (hash-ref ASSOCIATIVITIES ch "") #f))
) (void))
(_return "")
))
)
(define (balanced_parentheses expr)
  (let/ec _return (begin
(define count 0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? expr) (string-length expr)] [(hash? expr) (hash-count expr)] [else (length expr)])) (let/ec _cont
    (define ch (slice expr i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (string=? ch "(") (let ()
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (if (string=? ch ")") (let ()
(set! count (- count 1))
(if (< count 0) (let ()
(_return #f)
) (void))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (equal? count 0))
))
)
(define (is_letter ch)
  (let/ec _return (begin
(_return (or (and (string<=? "a" ch) (string<=? ch "z")) (and (string<=? "A" ch) (string<=? ch "Z"))))
))
)
(define (is_digit ch)
  (let/ec _return (begin
(_return (and (string<=? "0" ch) (string<=? ch "9")))
))
)
(define (is_alnum ch)
  (let/ec _return (begin
(_return (or (is_letter ch) (is_digit ch)))
))
)
(define (infix_to_postfix expression)
  (let/ec _return (begin
(if (not (balanced_parentheses expression)) (let ()
(panic "Mismatched parentheses")
) (void))
(define stack (list))
(define postfix (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? expression) (string-length expression)] [(hash? expression) (hash-count expression)] [else (length expression)])) (let/ec _cont
    (define ch (slice expression i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (is_alnum ch) (let ()
(set! postfix (append postfix (list ch)))
) (let ()
(if (string=? ch "(") (let ()
(set! stack (append stack (list ch)))
) (let ()
(if (string=? ch ")") (let ()
(let/ec _break (let loop ()
  (if (and (> (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 0) (not (string=? (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f) "("))) (let/ec _cont
    (set! postfix (append postfix (list (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))))
    (set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))
    (loop)) (void))))
(if (> (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 0) (let ()
(set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))
) (void))
) (let ()
(if (string=? ch " ") (void) (let ()
(let/ec _break (let loop ()
  (if #t (let/ec _cont
    (if (equal? (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 0) (let ()
(set! stack (append stack (list ch)))
(_break)
) (void))
    (define cp (precedence ch))
    (define tp (precedence (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f)))
    (if (> cp tp) (let ()
(set! stack (append stack (list ch)))
(_break)
) (void))
    (if (< cp tp) (let ()
(set! postfix (append postfix (list (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))))
(set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))
(_cont)
) (void))
    (if (string=? (associativity ch) "RL") (let ()
(set! stack (append stack (list ch)))
(_break)
) (void))
    (set! postfix (append postfix (list (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))))
    (set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))
    (loop)) (void))))
))
))
))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (> (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 0) (let/ec _cont
    (set! postfix (append postfix (list (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))))
    (set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))
    (loop)) (void))))
(define res "")
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(string? postfix) (string-length postfix)] [(hash? postfix) (hash-count postfix)] [else (length postfix)])) (let/ec _cont
    (if (> j 0) (let ()
(set! res (string-append res " "))
) (void))
    (set! res (string-append res (if postfix (if (hash? postfix) (hash-ref postfix j #f) (list-ref postfix (int j))) #f)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (main)
  (let/ec _return (begin
(define expression "a+b*(c^d-e)^(f+g*h)-i")
(displayln expression)
(displayln (infix_to_postfix expression))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
