;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (exp_approx x)
  (let/ec _return (begin
(define sum 1.0)
(define term 1.0)
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 20]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (set! term (/ (* term x) (exact->inexact i)))
      (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return sum)
))
)
(define (sigmoid vector)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not vector) 0] [(string? vector) (string-length vector)] [(hash? vector) (hash-count vector)] [else (length vector)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define v (if vector (if (hash? vector) (hash-ref vector i #f) (safe-index vector (int i))) #f))
      (define s (/ 1.0 (let ([__l 1.0] [__r (exp_approx (- v))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! result (append (or result (list)) (list s)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (swish vector beta)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not vector) 0] [(string? vector) (string-length vector)] [(hash? vector) (hash-count vector)] [else (length vector)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define v (if vector (if (hash? vector) (hash-ref vector i #f) (safe-index vector (int i))) #f))
      (define s (/ 1.0 (let ([__l 1.0] [__r (exp_approx (* (- beta) v))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! result (append (or result (list)) (list (* v s))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (sigmoid_linear_unit vector)
  (let/ec _return (begin
(_return (swish vector 1.0))
))
)
(define (approx_equal a b eps)
  (let/ec _return (begin
(define diff (if (let ([__l a] [__r b]) (> (int __l) (int __r))) (- a b) (- b a)))
(_return (let ([__l diff] [__r eps]) (< (int __l) (int __r))))
))
)
(define (approx_equal_list a b eps)
  (let/ec _return (begin
(if (not (equal? (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)]) (cond [(not b) 0] [(string? b) (string-length b)] [(hash? b) (hash-count b)] [else (length b)]))) (let ()
(_return #f)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (not (approx_equal (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f) eps)) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (test_swish)
  (let/ec _return (begin
(define v (list (- 1.0) 1.0 2.0))
(define eps 0.001)
(if (not (approx_equal_list (sigmoid v) (list 0.26894142 0.73105858 0.88079708) eps)) (let ()
(panic "sigmoid incorrect")
) (void))
(if (not (approx_equal_list (sigmoid_linear_unit v) (list (- 0.26894142) 0.73105858 1.76159416) eps)) (let ()
(panic "sigmoid_linear_unit incorrect")
) (void))
(if (not (approx_equal_list (swish v 2.0) (list (- 0.11920292) 0.88079708 1.96402758) eps)) (let ()
(panic "swish incorrect")
) (void))
(if (not (approx_equal_list (swish (list (- 2.0)) 1.0) (list (- 0.23840584)) eps)) (let ()
(panic "swish with parameter 1 incorrect")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_swish)
(displayln (to-string (sigmoid (list (- 1.0) 1.0 2.0))))
(displayln (to-string (sigmoid_linear_unit (list (- 1.0) 1.0 2.0))))
(displayln (to-string (swish (list (- 1.0) 1.0 2.0) 2.0)))
(displayln (to-string (swish (list (- 2.0)) 1.0)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
