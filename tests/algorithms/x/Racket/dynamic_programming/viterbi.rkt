;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (key state obs)
  (let/ec _return (begin
(_return (string-append (string-append state "|") obs))
))
)
(define (viterbi observations states start_p trans_p emit_p)
  (let/ec _return (begin
(if (or (equal? (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)]) 0) (equal? (cond [(string? states) (string-length states)] [(hash? states) (hash-count states)] [else (length states)]) 0)) (let ()
(panic "empty parameters")
) (void))
(define probs (hash))
(define ptrs (hash))
(define first_obs (if observations (if (hash? observations) (hash-ref observations 0 #f) (list-ref observations (int 0))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? states) (string-length states)] [(hash? states) (hash-count states)] [else (length states)])) (let/ec _cont
    (define state (if states (if (hash? states) (hash-ref states i #f) (list-ref states (int i))) #f))
    (set! probs (hash-set (or probs (hash)) (key state first_obs) (* (if start_p (hash-ref start_p state 0.0) #f) (if (if emit_p (hash-ref emit_p state (hash)) #f) (hash-ref (if emit_p (hash-ref emit_p state (hash)) #f) first_obs 0.0) #f))))
    (set! ptrs (hash-set (or ptrs (hash)) (key state first_obs) ""))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define t 1)
(let/ec _break (let loop ()
  (if (< t (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)])) (let/ec _cont
    (define obs (if observations (if (hash? observations) (hash-ref observations t #f) (list-ref observations (int t))) #f))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j (cond [(string? states) (string-length states)] [(hash? states) (hash-count states)] [else (length states)])) (let/ec _cont
    (define state (if states (if (hash? states) (hash-ref states j #f) (list-ref states (int j))) #f))
    (define max_prob (- 1.0))
    (define prev_state "")
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k (cond [(string? states) (string-length states)] [(hash? states) (hash-count states)] [else (length states)])) (let/ec _cont
    (define state0 (if states (if (hash? states) (hash-ref states k #f) (list-ref states (int k))) #f))
    (define obs0 (if observations (if (hash? observations) (hash-ref observations (- t 1) #f) (list-ref observations (int (- t 1)))) #f))
    (define prob_prev (if probs (hash-ref probs (key state0 obs0) 0.0) #f))
    (define prob (* (* prob_prev (if (if trans_p (hash-ref trans_p state0 (hash)) #f) (hash-ref (if trans_p (hash-ref trans_p state0 (hash)) #f) state 0.0) #f)) (if (if emit_p (hash-ref emit_p state (hash)) #f) (hash-ref (if emit_p (hash-ref emit_p state (hash)) #f) obs 0.0) #f)))
    (if (> prob max_prob) (let ()
(set! max_prob prob)
(set! prev_state state0)
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! probs (hash-set (or probs (hash)) (key state obs) max_prob))
    (set! ptrs (hash-set (or ptrs (hash)) (key state obs) prev_state))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! t (let ([__l t] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define path (list))
(define n 0)
(let/ec _break (let loop ()
  (if (< n (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)])) (let/ec _cont
    (set! path (append path (list "")))
    (set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define last_obs (if observations (if (hash? observations) (hash-ref observations (- (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)]) 1) #f) (list-ref observations (int (- (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)]) 1)))) #f))
(define max_final (- 1.0))
(define last_state "")
(define m 0)
(let/ec _break (let loop ()
  (if (< m (cond [(string? states) (string-length states)] [(hash? states) (hash-count states)] [else (length states)])) (let/ec _cont
    (define state (if states (if (hash? states) (hash-ref states m #f) (list-ref states (int m))) #f))
    (define prob (if probs (hash-ref probs (key state last_obs) 0.0) #f))
    (if (> prob max_final) (let ()
(set! max_final prob)
(set! last_state state)
) (void))
    (set! m (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define last_index (- (cond [(string? observations) (string-length observations)] [(hash? observations) (hash-count observations)] [else (length observations)]) 1))
(set! path (list-set path (int last_index) last_state))
(define idx last_index)
(let/ec _break (let loop ()
  (if (> idx 0) (let/ec _cont
    (define obs (if observations (if (hash? observations) (hash-ref observations idx #f) (list-ref observations (int idx))) #f))
    (define prev (if ptrs (hash-ref ptrs (key (if path (if (hash? path) (hash-ref path idx #f) (list-ref path (int idx))) #f) obs) "") #f))
    (set! path (list-set path (int (- idx 1)) prev))
    (set! idx (- idx 1))
    (loop)) (void))))
(_return path)
))
)
(define (join_words words)
  (let/ec _return (begin
(define res "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? words) (string-length words)] [(hash? words) (hash-count words)] [else (length words)])) (let/ec _cont
    (if (> i 0) (let ()
(set! res (string-append res " "))
) (void))
    (set! res (string-append res (if words (if (hash? words) (hash-ref words i #f) (list-ref words (int i))) #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define observations (list "normal" "cold" "dizzy"))
(define states (list "Healthy" "Fever"))
(define start_p (hash "Healthy" 0.6 "Fever" 0.4))
(define trans_p (hash "Healthy" (hash "Healthy" 0.7 "Fever" 0.3) "Fever" (hash "Healthy" 0.4 "Fever" 0.6)))
(define emit_p (hash "Healthy" (hash "normal" 0.5 "cold" 0.4 "dizzy" 0.1) "Fever" (hash "normal" 0.1 "cold" 0.3 "dizzy" 0.6)))
(define result (viterbi observations states start_p trans_p emit_p))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (join_words result))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
