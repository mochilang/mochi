;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define INF 1000000000)
(define (new_graph n)
  (let/ec _return (begin
(define dp (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j n) (let/ec _cont
    (if (equal? i j) (let ()
(set! row (append row (list 0)))
) (let ()
(set! row (append row (list INF)))
))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! dp (append dp (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "n" n "dp" dp))
))
)
(define (add_edge g u v w)
  (let/ec _return (begin
(define dp (if g (hash-ref g "dp" #f) #f))
(define row (if dp (if (hash? dp) (hash-ref dp u #f) (list-ref dp (int u))) #f))
(set! row (list-set row (int v) w))
(set! dp (list-set dp (int u) row))
(set! g (hash-set (or g (hash)) "dp" dp))
))
)
(define (floyd_warshall g)
  (let/ec _return (begin
(define dp (if g (hash-ref g "dp" #f) #f))
(define k 0)
(let/ec _break (let loop ()
  (if (< k (if g (hash-ref g "n" #f) #f)) (let/ec _cont
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i (if g (hash-ref g "n" #f) #f)) (let/ec _cont
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j (if g (hash-ref g "n" #f) #f)) (let/ec _cont
    (define alt (let ([__l (if (if dp (if (hash? dp) (hash-ref dp i #f) (list-ref dp (int i))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i #f) (list-ref dp (int i))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i #f) (list-ref dp (int i))) #f) k #f) (list-ref (if dp (if (hash? dp) (hash-ref dp i #f) (list-ref dp (int i))) #f) (int k))) #f)] [__r (if (if dp (if (hash? dp) (hash-ref dp k #f) (list-ref dp (int k))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp k #f) (list-ref dp (int k))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp k #f) (list-ref dp (int k))) #f) j #f) (list-ref (if dp (if (hash? dp) (hash-ref dp k #f) (list-ref dp (int k))) #f) (int j))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define row (if dp (if (hash? dp) (hash-ref dp i #f) (list-ref dp (int i))) #f))
    (if (< alt (if row (if (hash? row) (hash-ref row j #f) (list-ref row (int j))) #f)) (let ()
(set! row (list-set row (int j) alt))
(set! dp (list-set dp (int i) row))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! g (hash-set (or g (hash)) "dp" dp))
))
)
(define (show_min g u v)
  (let/ec _return (begin
(_return (if (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (list-ref (if g (hash-ref g "dp" #f) #f) (int u))) #f) (if (hash? (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (list-ref (if g (hash-ref g "dp" #f) #f) (int u))) #f)) (hash-ref (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (list-ref (if g (hash-ref g "dp" #f) #f) (int u))) #f) v #f) (list-ref (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (list-ref (if g (hash-ref g "dp" #f) #f) (int u))) #f) (int v))) #f))
))
)
(define graph (new_graph 5))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(add_edge graph 0 2 9)
(add_edge graph 0 4 10)
(add_edge graph 1 3 5)
(add_edge graph 2 3 7)
(add_edge graph 3 0 10)
(add_edge graph 3 1 2)
(add_edge graph 3 2 1)
(add_edge graph 3 4 6)
(add_edge graph 4 1 3)
(add_edge graph 4 2 4)
(add_edge graph 4 3 9)
(floyd_warshall graph)
(displayln (format "~a" (show_min graph 1 4)))
(displayln (format "~a" (show_min graph 0 3)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
