;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (score_function source_char target_char match_score mismatch_score gap_score)
  (let/ec _return (begin
(if (or (string=? source_char "-") (string=? target_char "-")) (let ()
(_return gap_score)
) (void))
(if (string=? source_char target_char) (let ()
(_return match_score)
) (void))
(_return mismatch_score)
))
)
(define (smith_waterman query subject match_score mismatch_score gap_score)
  (let/ec _return (begin
(define q (upper query))
(define s (upper subject))
(define m (cond [(string? q) (string-length q)] [(hash? q) (hash-count q)] [else (length q)]))
(define n (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]))
(define score (list))
(let/ec _break (let ([_ 0])
  (let loop ()
    (when (< _ (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define row (list))
(let/ec _break (let ([_2 0])
  (let loop ()
    (when (< _2 (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! row (append row (list 0)))
      (set! _2 (+ _2 1))
      (loop)))
))
(set! score (append score (list row)))
      (set! _ (+ _ 1))
      (loop)))
))
(let/ec _break (let ([i 1])
  (let loop ()
    (when (< i (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(let/ec _break (let ([j 1])
  (let loop ()
    (when (< j (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define qc (slice q (- i 1) i))
(define sc (slice s (- j 1) j))
(define diag (let ([__l (if (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (if (hash? (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (- j 1) #f) (list-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (int (- j 1)))) #f)] [__r (score_function qc sc match_score mismatch_score gap_score)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define delete (let ([__l (if (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (if (hash? (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (int j))) #f)] [__r gap_score]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define insert (let ([__l (if (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (if (hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (- j 1) #f) (list-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (int (- j 1)))) #f)] [__r gap_score]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define max_val 0)
(if (> diag max_val) (let ()
(set! max_val diag)
) (void))
(if (> delete max_val) (let ()
(set! max_val delete)
) (void))
(if (> insert max_val) (let ()
(set! max_val insert)
) (void))
(set! score (list-set score i (list-set (list-ref score i) j max_val)))
      (set! j (+ j 1))
      (loop)))
))
      (set! i (+ i 1))
      (loop)))
))
(_return score)
))
)
(define (traceback score query subject match_score mismatch_score gap_score)
  (let/ec _return (begin
(define q (upper query))
(define s (upper subject))
(define max_value 0)
(define i_max 0)
(define j_max 0)
(let/ec _break (let ([i 0])
  (let loop ()
    (when (< i (cond [(string? score) (string-length score)] [(hash? score) (hash-count score)] [else (length score)]))
(let/ec _break (let ([j 0])
  (let loop ()
    (when (< j (cond [(string? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (string-length (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f))] [(hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-count (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f))] [else (length (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f))]))
(if (> (if (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (if (hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (int j))) #f) max_value) (let ()
(set! max_value (if (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (if (hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (int j))) #f))
(set! i_max i)
(set! j_max j)
) (void))
      (set! j (+ j 1))
      (loop)))
))
      (set! i (+ i 1))
      (loop)))
))
(define i i_max)
(define j j_max)
(define align1 "")
(define align2 "")
(define gap_penalty (score_function "-" "-" match_score mismatch_score gap_score))
(if (or (equal? i 0) (equal? j 0)) (let ()
(_return "")
) (void))
(let/ec _break (let loop ()
  (if (and (> i 0) (> j 0)) (let/ec _cont
    (define qc (slice q (- i 1) i))
    (define sc (slice s (- j 1) j))
    (if (equal? (if (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (if (hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (int j))) #f) (let ([__l (if (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (if (hash? (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (- j 1) #f) (list-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (int (- j 1)))) #f)] [__r (score_function qc sc match_score mismatch_score gap_score)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let ()
(set! align1 (string-append qc align1))
(set! align2 (string-append sc align2))
(set! i (- i 1))
(set! j (- j 1))
) (let ()
(if (equal? (if (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (if (hash? (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score i #f) (list-ref score (int i))) #f) (int j))) #f) (let ([__l (if (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (if (hash? (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f)) (hash-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) j #f) (list-ref (if score (if (hash? score) (hash-ref score (- i 1) #f) (list-ref score (int (- i 1)))) #f) (int j))) #f)] [__r gap_penalty]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let ()
(set! align1 (string-append qc align1))
(set! align2 (string-append "-" align2))
(set! i (- i 1))
) (let ()
(set! align1 (string-append "-" align1))
(set! align2 (string-append sc align2))
(set! j (- j 1))
))
))
    (loop)) (void))))
(_return (string-append (string-append align1 "\n") align2))
))
)
(define query "HEAGAWGHEE")
(define subject "PAWHEAE")
(define score (smith_waterman query subject 1 (- 1) (- 2)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (traceback score query subject 1 (- 1) (- 2)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
