;; Generated by Mochi 0.10.62 on 2025-08-09 10:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (index_of xs x)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (begin
    (let/ec _cont
      (if (equal? (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f) x) (let ()
(_return i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (- 0 1))
))
)
(define (majority_vote votes votes_needed_to_win)
  (let/ec _return (begin
(if (< votes_needed_to_win 2) (let ()
(_return (list))
) (void))
(define candidates (list))
(define counts (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not votes) 0] [(string? votes) (string-length votes)] [(hash? votes) (hash-count votes)] [else (length votes)])) (begin
    (let/ec _cont
      (define v (if votes (if (hash? votes) (hash-ref votes i #f) (safe-index votes (int i))) #f))
      (define idx (index_of candidates v))
      (if (not (equal? idx (- 0 1))) (let ()
(set! counts (list-set counts (int idx) (let ([__l (if counts (if (hash? counts) (hash-ref counts idx #f) (safe-index counts (int idx))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (let ()
(if (< (cond [(not candidates) 0] [(string? candidates) (string-length candidates)] [(hash? candidates) (hash-count candidates)] [else (length candidates)]) (- votes_needed_to_win 1)) (let ()
(set! candidates (append (or candidates (list)) (list v)))
(set! counts (append (or counts (list)) (list 1)))
) (let ()
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not counts) 0] [(string? counts) (string-length counts)] [(hash? counts) (hash-count counts)] [else (length counts)])) (begin
    (let/ec _cont
      (set! counts (list-set counts (int j) (- (if counts (if (hash? counts) (hash-ref counts j #f) (safe-index counts (int j))) #f) 1)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define new_candidates (list))
(define new_counts (list))
(set! j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not candidates) 0] [(string? candidates) (string-length candidates)] [(hash? candidates) (hash-count candidates)] [else (length candidates)])) (begin
    (let/ec _cont
      (if (> (if counts (if (hash? counts) (hash-ref counts j #f) (safe-index counts (int j))) #f) 0) (let ()
(set! new_candidates (append (or new_candidates (list)) (list (if candidates (if (hash? candidates) (hash-ref candidates j #f) (safe-index candidates (int j))) #f))))
(set! new_counts (append (or new_counts (list)) (list (if counts (if (hash? counts) (hash-ref counts j #f) (safe-index counts (int j))) #f))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! candidates new_candidates)
(set! counts new_counts)
))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define final_counts (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not candidates) 0] [(string? candidates) (string-length candidates)] [(hash? candidates) (hash-count candidates)] [else (length candidates)])) (begin
    (let/ec _cont
      (set! final_counts (append (or final_counts (list)) (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not votes) 0] [(string? votes) (string-length votes)] [(hash? votes) (hash-count votes)] [else (length votes)])) (begin
    (let/ec _cont
      (define v (if votes (if (hash? votes) (hash-ref votes i #f) (safe-index votes (int i))) #f))
      (define idx (index_of candidates v))
      (if (not (equal? idx (- 0 1))) (let ()
(set! final_counts (list-set final_counts (int idx) (let ([__l (if final_counts (if (hash? final_counts) (hash-ref final_counts idx #f) (safe-index final_counts (int idx))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define result (list))
(set! j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not candidates) 0] [(string? candidates) (string-length candidates)] [(hash? candidates) (hash-count candidates)] [else (length candidates)])) (begin
    (let/ec _cont
      (if (> (* (if final_counts (if (hash? final_counts) (hash-ref final_counts j #f) (safe-index final_counts (int j))) #f) votes_needed_to_win) (cond [(not votes) 0] [(string? votes) (string-length votes)] [(hash? votes) (hash-count votes)] [else (length votes)])) (let ()
(set! result (append (or result (list)) (list (if candidates (if (hash? candidates) (hash-ref candidates j #f) (safe-index candidates (int j))) #f))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (main)
  (let/ec _return (begin
(define votes (list 1 2 2 3 1 3 2))
(displayln (to-string (majority_vote votes 3)))
(displayln (to-string (majority_vote votes 2)))
(displayln (to-string (majority_vote votes 4)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
