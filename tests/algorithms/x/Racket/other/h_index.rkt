;; Generated by Mochi 0.10.64 on 2025-08-12 15:27 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (subarray xs start end)
  (let/ec _return (begin
(define result (list))
(define k start)
(let/ec _break (let loop ()
  (if (let ([__l k] [__r end]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (append (or result (list)) (list (if xs (if (hash? xs) (hash-ref xs k #f) (safe-index xs (int k))) #f))))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (merge left_half right_half)
  (let/ec _return (begin
(define result (list))
(define i 0)
(define j 0)
(let/ec _break (let loop ()
  (if (and (let ([__l i] [__r (cond [(not left_half) 0] [(string? left_half) (string-length left_half)] [(hash? left_half) (hash-count left_half)] [else (length left_half)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l j] [__r (cond [(not right_half) 0] [(string? right_half) (string-length right_half)] [(hash? right_half) (hash-count right_half)] [else (length right_half)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (begin
    (let/ec _cont
      (if (let ([__l (if left_half (if (hash? left_half) (hash-ref left_half i #f) (safe-index left_half (int i))) #f)] [__r (if right_half (if (hash? right_half) (hash-ref right_half j #f) (safe-index right_half (int j))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! result (append (or result (list)) (list (if left_half (if (hash? left_half) (hash-ref left_half i #f) (safe-index left_half (int i))) #f))))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! result (append (or result (list)) (list (if right_half (if (hash? right_half) (hash-ref right_half j #f) (safe-index right_half (int j))) #f))))
(set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
    )
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not left_half) 0] [(string? left_half) (string-length left_half)] [(hash? left_half) (hash-count left_half)] [else (length left_half)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (append (or result (list)) (list (if left_half (if (hash? left_half) (hash-ref left_half i #f) (safe-index left_half (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not right_half) 0] [(string? right_half) (string-length right_half)] [(hash? right_half) (hash-count right_half)] [else (length right_half)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (append (or result (list)) (list (if right_half (if (hash? right_half) (hash-ref right_half j #f) (safe-index right_half (int j))) #f))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (merge_sort array)
  (let/ec _return (begin
(if (let ([__l (cond [(not array) 0] [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)])] [__r 1]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(_return array)
) (void))
(define middle (floor (/ (cond [(not array) 0] [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)]) 2)))
(define left_half (subarray array 0 middle))
(define right_half (subarray array middle (cond [(not array) 0] [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)])))
(define sorted_left (merge_sort left_half))
(define sorted_right (merge_sort right_half))
(_return (merge sorted_left sorted_right))
))
)
(define (h_index citations)
  (let/ec _return (begin
(define idx 0)
(let/ec _break (let loop ()
  (if (let ([__l idx] [__r (cond [(not citations) 0] [(string? citations) (string-length citations)] [(hash? citations) (hash-count citations)] [else (length citations)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (if citations (if (hash? citations) (hash-ref citations idx #f) (safe-index citations (int idx))) #f)] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "The citations should be a list of non negative integers.")
) (void))
      (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define sorted (merge_sort citations))
(define n (cond [(not sorted) 0] [(string? sorted) (string-length sorted)] [(hash? sorted) (hash-count sorted)] [else (length sorted)]))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (if sorted (if (hash? sorted) (hash-ref sorted (- (- n 1) i) #f) (safe-index sorted (int (- (- n 1) i)))) #f)] [__r i]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(_return i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return n)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (h_index (list 3 0 6 1 5))))
(displayln (to-string (h_index (list 1 3 1))))
(displayln (to-string (h_index (list 1 2 3))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
