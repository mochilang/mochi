;; Generated by Mochi 0.10.60 on 2025-08-08 10:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (index_of xs x)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (if (equal? (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f) x) (let ()
(_return i)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (- 1))
))
)
(define (remove_item xs x)
  (let/ec _return (begin
(define res (list))
(define removed #f)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (if (and (not removed) (equal? (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f) x)) (let ()
(set! removed #t)
) (let ()
(set! res (append res (list (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f))))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (stable_matching donor_pref recipient_pref)
  (let/ec _return (begin
(if (not (equal? (cond [(not donor_pref) 0] [(string? donor_pref) (string-length donor_pref)] [(hash? donor_pref) (hash-count donor_pref)] [else (length donor_pref)]) (cond [(not recipient_pref) 0] [(string? recipient_pref) (string-length recipient_pref)] [(hash? recipient_pref) (hash-count recipient_pref)] [else (length recipient_pref)]))) (let ()
(panic "unequal groups")
) (void))
(define n (cond [(not donor_pref) 0] [(string? donor_pref) (string-length donor_pref)] [(hash? donor_pref) (hash-count donor_pref)] [else (length donor_pref)]))
(define unmatched (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (set! unmatched (append unmatched (list i)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define donor_record (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (set! donor_record (append donor_record (list (- 1))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define rec_record (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (set! rec_record (append rec_record (list (- 1))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define num_donations (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (set! num_donations (append num_donations (list 0)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (> (cond [(not unmatched) 0] [(string? unmatched) (string-length unmatched)] [(hash? unmatched) (hash-count unmatched)] [else (length unmatched)]) 0) (let/ec _cont
    (define donor (if unmatched (if (hash? unmatched) (hash-ref unmatched 0 #f) (list-ref unmatched (int 0))) #f))
    (define donor_preference (if donor_pref (if (hash? donor_pref) (hash-ref donor_pref donor #f) (list-ref donor_pref (int donor))) #f))
    (define recipient (if donor_preference (if (hash? donor_preference) (hash-ref donor_preference (if num_donations (if (hash? num_donations) (hash-ref num_donations donor #f) (list-ref num_donations (int donor))) #f) #f) (list-ref donor_preference (int (if num_donations (if (hash? num_donations) (hash-ref num_donations donor #f) (list-ref num_donations (int donor))) #f)))) #f))
    (set! num_donations (list-set num_donations (int donor) (let ([__l (if num_donations (if (hash? num_donations) (hash-ref num_donations donor #f) (list-ref num_donations (int donor))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (define rec_preference (if recipient_pref (if (hash? recipient_pref) (hash-ref recipient_pref recipient #f) (list-ref recipient_pref (int recipient))) #f))
    (define prev_donor (if rec_record (if (hash? rec_record) (hash-ref rec_record recipient #f) (list-ref rec_record (int recipient))) #f))
    (if (not (equal? prev_donor (- 0 1))) (let ()
(define prev_index (index_of rec_preference prev_donor))
(define new_index (index_of rec_preference donor))
(if (> prev_index new_index) (let ()
(set! rec_record (list-set rec_record (int recipient) donor))
(set! donor_record (list-set donor_record (int donor) recipient))
(set! unmatched (append unmatched (list prev_donor)))
(set! unmatched (remove_item unmatched donor))
) (void))
) (let ()
(set! rec_record (list-set rec_record (int recipient) donor))
(set! donor_record (list-set donor_record (int donor) recipient))
(set! unmatched (remove_item unmatched donor))
))
    (loop)) (void))))
(_return donor_record)
))
)
(define donor_pref (list (list 0 1 3 2) (list 0 2 3 1) (list 1 0 2 3) (list 0 3 1 2)))
(define recipient_pref (list (list 3 1 2 0) (list 3 1 0 2) (list 0 3 1 2) (list 1 0 3 2)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (stable_matching donor_pref recipient_pref)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
