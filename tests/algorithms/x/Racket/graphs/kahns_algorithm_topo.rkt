;; Generated by Mochi 0.10.60 on 2025-08-08 10:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (topological_sort graph)
  (let/ec _return (begin
(define indegree (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)])) (let/ec _cont
    (set! indegree (append indegree (list 0)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(let/ec _break (for ([edges (hash-values graph)])
  (let/ec _cont
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define v (if edges (if (hash? edges) (hash-ref edges j #f) (list-ref edges (int j))) #f))
    (set! indegree (list-set indegree (int v) (let ([__l (if indegree (if (hash? indegree) (hash-ref indegree v #f) (list-ref indegree (int v))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
  )))
(define queue (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not indegree) 0] [(string? indegree) (string-length indegree)] [(hash? indegree) (hash-count indegree)] [else (length indegree)])) (let/ec _cont
    (if (equal? (if indegree (if (hash? indegree) (hash-ref indegree i #f) (list-ref indegree (int i))) #f) 0) (let ()
(set! queue (append queue (list i)))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define order (list))
(define head 0)
(define processed 0)
(let/ec _break (let loop ()
  (if (< head (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)])) (let/ec _cont
    (define v (if queue (if (hash? queue) (hash-ref queue head #f) (list-ref queue (int head))) #f))
    (set! head (let ([__l head] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! processed (let ([__l processed] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! order (append order (list v)))
    (define neighbors (if graph (hash-ref graph v (list)) #f))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k (cond [(not neighbors) 0] [(string? neighbors) (string-length neighbors)] [(hash? neighbors) (hash-count neighbors)] [else (length neighbors)])) (let/ec _cont
    (define nb (if neighbors (if (hash? neighbors) (hash-ref neighbors k #f) (list-ref neighbors (int k))) #f))
    (set! indegree (list-set indegree (int nb) (- (if indegree (if (hash? indegree) (hash-ref indegree nb #f) (list-ref indegree (int nb))) #f) 1)))
    (if (equal? (if indegree (if (hash? indegree) (hash-ref indegree nb #f) (list-ref indegree (int nb))) #f) 0) (let ()
(set! queue (append queue (list nb)))
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (loop)) (void))))
(if (not (equal? processed (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))) (let ()
(_return #f)
) (void))
(_return order)
))
)
(define (main)
  (let/ec _return (begin
(define graph (let ([h (make-hash)]) (hash-set! h 0 (list 1 2)) (hash-set! h 1 (list 3)) (hash-set! h 2 (list 3)) (hash-set! h 3 (list 4 5)) (hash-set! h 4 (list)) (hash-set! h 5 (list)) h))
(displayln (topological_sort graph))
(define cyclic (let ([h (make-hash)]) (hash-set! h 0 (list 1)) (hash-set! h 1 (list 2)) (hash-set! h 2 (list 0)) h))
(displayln (topological_sort cyclic))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
