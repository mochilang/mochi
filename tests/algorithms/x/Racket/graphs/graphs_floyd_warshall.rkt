;; Generated by Mochi 0.10.60 on 2025-08-08 10:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define INF 1000000000.0)
(define (floyd_warshall graph)
  (let/ec _return (begin
(define v (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))
(define dist (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i v) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j v) (let/ec _cont
    (set! row (append row (list (if (if graph (if (hash? graph) (hash-ref graph i #f) (list-ref graph (int i))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph i #f) (list-ref graph (int i))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph i #f) (list-ref graph (int i))) #f) j #f) (list-ref (if graph (if (hash? graph) (hash-ref graph i #f) (list-ref graph (int i))) #f) (int j))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! dist (append dist (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define k 0)
(let/ec _break (let loop ()
  (if (< k v) (let/ec _cont
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i v) (let/ec _cont
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j v) (let/ec _cont
    (if (and (and (< (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) k #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int k))) #f) INF) (< (if (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (int j))) #f) INF)) (< (let ([__l (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) k #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int k))) #f)] [__r (if (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (int j))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int j))) #f))) (let ()
(set! dist (list-set dist i (list-set (list-ref dist i) j (let ([__l (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) k #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int k))) #f)] [__r (if (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist k #f) (list-ref dist (int k))) #f) (int j))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return dist)
))
)
(define (print_dist dist)
  (let/ec _return (begin
(displayln "\nThe shortest path matrix using Floyd Warshall algorithm\n")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not dist) 0] [(string? dist) (string-length dist)] [(hash? dist) (hash-count dist)] [else (length dist)])) (let/ec _cont
    (define j 0)
    (define line "")
    (let/ec _break (let loop ()
  (if (< j (cond [(not (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) 0] [(string? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (string-length (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f))] [(hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-count (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f))] [else (length (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f))])) (let/ec _cont
    (if (>= (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int j))) #f) (/ INF 2.0)) (let ()
(set! line (string-append line "INF\t"))
) (let ()
(set! line (string-append (string-append line (format "~a" (int (if (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (if (hash? (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f)) (hash-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) j #f) (list-ref (if dist (if (hash? dist) (hash-ref dist i #f) (list-ref dist (int i))) #f) (int j))) #f)))) "\t"))
))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (displayln line)
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(define graph (list (list 0.0 5.0 INF 10.0) (list INF 0.0 3.0 INF) (list INF INF 0.0 1.0) (list INF INF INF 0.0)))
(define result (floyd_warshall graph))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(print_dist result)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
