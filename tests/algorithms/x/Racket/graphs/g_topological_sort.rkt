;; Generated by Mochi 0.10.60 on 2025-08-08 10:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (depth_first_search u visited graph stack)
  (let/ec _return (begin
(set! visited (list-set visited (int u) #t))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f)) 0] [(string? (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f)) (string-length (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f))] [(hash? (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f)) (hash-count (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f))] [else (length (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f))])) (let/ec _cont
    (define v (if (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f) i #f) (list-ref (if graph (if (hash? graph) (hash-ref graph u #f) (list-ref graph (int u))) #f) (int i))) #f))
    (if (not (if visited (if (hash? visited) (hash-ref visited v #f) (list-ref visited (int v))) #f)) (let ()
(set! stack (depth_first_search v visited graph stack))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! stack (append stack (list u)))
(_return stack)
))
)
(define (topological_sort graph)
  (let/ec _return (begin
(define visited (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)])) (let/ec _cont
    (set! visited (append visited (list #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define stack (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)])) (let/ec _cont
    (if (not (if visited (if (hash? visited) (hash-ref visited i #f) (list-ref visited (int i))) #f)) (let ()
(set! stack (depth_first_search i visited graph stack))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return stack)
))
)
(define (print_stack stack clothes)
  (let/ec _return (begin
(define order 1)
(define s stack)
(let/ec _break (let loop ()
  (if (> (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 0) (let/ec _cont
    (define idx (if s (if (hash? s) (hash-ref s (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1) #f) (list-ref s (int (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1)))) #f))
    (set! s (slice s 0 (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1)))
    (displayln (string-append (string-append (format "~a" order) " ") (if clothes (hash-ref clothes idx "") #f)))
    (set! order (let ([__l order] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(define (format_list xs)
  (let/ec _return (begin
(define res "[")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (set! res (string-append res (format "~a" (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f))))
    (if (< i (- (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)]) 1)) (let ()
(set! res (string-append res ", "))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! res (string-append res "]"))
(_return res)
))
)
(define (main)
  (let/ec _return (begin
(define clothes (hash 0 "underwear" 1 "pants" 2 "belt" 3 "suit" 4 "shoe" 5 "socks" 6 "shirt" 7 "tie" 8 "watch"))
(define graph (list (list 1 4) (list 2 4) (list 3) (list) (list) (list 4) (list 2 7) (list 3) (list)))
(define stack (topological_sort graph))
(displayln (format_list stack))
(print_stack stack clothes)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
