;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define G (hash "A" (hash "B" 2 "C" 5) "B" (hash "A" 2 "D" 3 "E" 1 "F" 1) "C" (hash "A" 5 "F" 3) "D" (hash "B" 3) "E" (hash "B" 4 "F" 3) "F" (hash "C" 3 "E" 3)))
(define heap (list (hash "node" "E" "cost" 0)))
(define visited (hash))
(define result (- 1))
(define G2 (hash "B" (hash "C" 1) "C" (hash "D" 1) "D" (hash "F" 1) "E" (hash "B" 1 "F" 3) "F" (hash)))
(define heap2 (list (hash "node" "E" "cost" 0)))
(define visited2 (hash))
(define result2 (- 1))
(define G3 (hash "B" (hash "C" 1) "C" (hash "D" 1) "D" (hash "F" 1) "E" (hash "B" 1 "G" 2) "F" (hash) "G" (hash "F" 1)))
(define heap3 (list (hash "node" "E" "cost" 0)))
(define visited3 (hash))
(define result3 (- 1))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (> (cond [(string? heap) (string-length heap)] [(hash? heap) (hash-count heap)] [else (length heap)]) 0) (let/ec _cont
    (define best_idx 0)
    (define i 1)
    (let/ec _break (let loop ()
  (if (< i (cond [(string? heap) (string-length heap)] [(hash? heap) (hash-count heap)] [else (length heap)])) (let/ec _cont
    (if (< (if (if heap (if (hash? heap) (hash-ref heap i #f) (list-ref heap (int i))) #f) (hash-ref (if heap (if (hash? heap) (hash-ref heap i #f) (list-ref heap (int i))) #f) "cost" #f) #f) (if (if heap (if (hash? heap) (hash-ref heap best_idx #f) (list-ref heap (int best_idx))) #f) (hash-ref (if heap (if (hash? heap) (hash-ref heap best_idx #f) (list-ref heap (int best_idx))) #f) "cost" #f) #f)) (let ()
(set! best_idx i)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define best (if heap (if (hash? heap) (hash-ref heap best_idx #f) (list-ref heap (int best_idx))) #f))
    (define new_heap (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j (cond [(string? heap) (string-length heap)] [(hash? heap) (hash-count heap)] [else (length heap)])) (let/ec _cont
    (if (not (equal? j best_idx)) (let ()
(set! new_heap (append new_heap (list (if heap (if (hash? heap) (hash-ref heap j #f) (list-ref heap (int j))) #f))))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! heap new_heap)
    (define u (if best (hash-ref best "node" #f) #f))
    (define cost (if best (hash-ref best "cost" #f) #f))
    (if (hash-has-key? visited u) (let ()
(_cont)
) (void))
    (set! visited (hash-set (or visited (hash)) u #t))
    (if (string=? u "C") (let ()
(set! result cost)
(_break)
) (void))
    (let/ec _break (for ([v (in-hash-keys (if G (hash-ref G u (hash)) #f))] #:unless (hash-has-key? visited v))
  (let/ec _cont
(define next_cost (let ([__l cost] [__r (if (if G (hash-ref G u (hash)) #f) (hash-ref (if G (hash-ref G u (hash)) #f) v 0) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! heap (append heap (list (hash "node" v "cost" next_cost))))
  )))
    (loop)) (void))))
(displayln result)
(let/ec _break (let loop ()
  (if (> (cond [(string? heap2) (string-length heap2)] [(hash? heap2) (hash-count heap2)] [else (length heap2)]) 0) (let/ec _cont
    (define best2_idx 0)
    (define i2 1)
    (let/ec _break (let loop ()
  (if (< i2 (cond [(string? heap2) (string-length heap2)] [(hash? heap2) (hash-count heap2)] [else (length heap2)])) (let/ec _cont
    (if (< (if (if heap2 (if (hash? heap2) (hash-ref heap2 i2 #f) (list-ref heap2 (int i2))) #f) (hash-ref (if heap2 (if (hash? heap2) (hash-ref heap2 i2 #f) (list-ref heap2 (int i2))) #f) "cost" #f) #f) (if (if heap2 (if (hash? heap2) (hash-ref heap2 best2_idx #f) (list-ref heap2 (int best2_idx))) #f) (hash-ref (if heap2 (if (hash? heap2) (hash-ref heap2 best2_idx #f) (list-ref heap2 (int best2_idx))) #f) "cost" #f) #f)) (let ()
(set! best2_idx i2)
) (void))
    (set! i2 (let ([__l i2] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define best2 (if heap2 (if (hash? heap2) (hash-ref heap2 best2_idx #f) (list-ref heap2 (int best2_idx))) #f))
    (define new_heap2 (list))
    (define j2 0)
    (let/ec _break (let loop ()
  (if (< j2 (cond [(string? heap2) (string-length heap2)] [(hash? heap2) (hash-count heap2)] [else (length heap2)])) (let/ec _cont
    (if (not (equal? j2 best2_idx)) (let ()
(set! new_heap2 (append new_heap2 (list (if heap2 (if (hash? heap2) (hash-ref heap2 j2 #f) (list-ref heap2 (int j2))) #f))))
) (void))
    (set! j2 (let ([__l j2] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! heap2 new_heap2)
    (define u2 (if best2 (hash-ref best2 "node" #f) #f))
    (define cost2 (if best2 (hash-ref best2 "cost" #f) #f))
    (if (hash-has-key? visited2 u2) (let ()
(_cont)
) (void))
    (set! visited2 (hash-set (or visited2 (hash)) u2 #t))
    (if (string=? u2 "F") (let ()
(set! result2 cost2)
(_break)
) (void))
    (let/ec _break (for ([v2 (in-hash-keys (if G2 (hash-ref G2 u2 (hash)) #f))] #:unless (hash-has-key? visited2 v2))
  (let/ec _cont
(define next_cost2 (let ([__l cost2] [__r (if (if G2 (hash-ref G2 u2 (hash)) #f) (hash-ref (if G2 (hash-ref G2 u2 (hash)) #f) v2 0) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! heap2 (append heap2 (list (hash "node" v2 "cost" next_cost2))))
  )))
    (loop)) (void))))
(displayln result2)
(let/ec _break (let loop ()
  (if (> (cond [(string? heap3) (string-length heap3)] [(hash? heap3) (hash-count heap3)] [else (length heap3)]) 0) (let/ec _cont
    (define best3_idx 0)
    (define i3 1)
    (let/ec _break (let loop ()
  (if (< i3 (cond [(string? heap3) (string-length heap3)] [(hash? heap3) (hash-count heap3)] [else (length heap3)])) (let/ec _cont
    (if (< (if (if heap3 (if (hash? heap3) (hash-ref heap3 i3 #f) (list-ref heap3 (int i3))) #f) (hash-ref (if heap3 (if (hash? heap3) (hash-ref heap3 i3 #f) (list-ref heap3 (int i3))) #f) "cost" #f) #f) (if (if heap3 (if (hash? heap3) (hash-ref heap3 best3_idx #f) (list-ref heap3 (int best3_idx))) #f) (hash-ref (if heap3 (if (hash? heap3) (hash-ref heap3 best3_idx #f) (list-ref heap3 (int best3_idx))) #f) "cost" #f) #f)) (let ()
(set! best3_idx i3)
) (void))
    (set! i3 (let ([__l i3] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define best3 (if heap3 (if (hash? heap3) (hash-ref heap3 best3_idx #f) (list-ref heap3 (int best3_idx))) #f))
    (define new_heap3 (list))
    (define j3 0)
    (let/ec _break (let loop ()
  (if (< j3 (cond [(string? heap3) (string-length heap3)] [(hash? heap3) (hash-count heap3)] [else (length heap3)])) (let/ec _cont
    (if (not (equal? j3 best3_idx)) (let ()
(set! new_heap3 (append new_heap3 (list (if heap3 (if (hash? heap3) (hash-ref heap3 j3 #f) (list-ref heap3 (int j3))) #f))))
) (void))
    (set! j3 (let ([__l j3] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! heap3 new_heap3)
    (define u3 (if best3 (hash-ref best3 "node" #f) #f))
    (define cost3 (if best3 (hash-ref best3 "cost" #f) #f))
    (if (hash-has-key? visited3 u3) (let ()
(_cont)
) (void))
    (set! visited3 (hash-set (or visited3 (hash)) u3 #t))
    (if (string=? u3 "F") (let ()
(set! result3 cost3)
(_break)
) (void))
    (let/ec _break (for ([v3 (in-hash-keys (if G3 (hash-ref G3 u3 (hash)) #f))] #:unless (hash-has-key? visited3 v3))
  (let/ec _cont
(define next_cost3 (let ([__l cost3] [__r (if (if G3 (hash-ref G3 u3 (hash)) #f) (hash-ref (if G3 (hash-ref G3 u3 (hash)) #f) v3 0) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! heap3 (append heap3 (list (hash "node" v3 "cost" next_cost3))))
  )))
    (loop)) (void))))
(displayln result3)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
