;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (list_contains_int xs x)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (if (equal? (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f) x) (let ()
(_return #t)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define (edge_exists edges w v)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (if (and (equal? (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 0 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 0))) #f) w) (equal? (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f) v)) (let ()
(_return #t)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define (first_key m)
  (let/ec _return (begin
(let/ec _break (for ([k (in-hash-keys m)])
  (let/ec _cont
(_return k)
  )))
(_return 0)
))
)
(define (rand_range low high)
  (let/ec _return (begin
(_return (let ([__l (modulo (now) (- high low))] [__r low]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (dg_make_graph)
  (let/ec _return (begin
(_return (hash "graph" (hash)))
))
)
(define (dg_add_pair g u v w)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))
(if (not (edge_exists edges w v)) (let ()
(set! edges (append edges (list (list w v))))
(define m (if g (hash-ref g "graph" #f) #f))
(set! m (hash-set (or m (hash)) u edges))
(set! g (hash-set (or g (hash)) "graph" m))
) (void))
) (let ()
(define m0 (if g (hash-ref g "graph" #f) #f))
(set! m0 (hash-set (or m0 (hash)) u (list (list w v))))
(set! g (hash-set (or g (hash)) "graph" m0))
))
(if (not (hash-has-key? (if g (hash-ref g "graph" #f) #f) v)) (let ()
(define m1 (if g (hash-ref g "graph" #f) #f))
(set! m1 (hash-set (or m1 (hash)) v (list)))
(set! g (hash-set (or g (hash)) "graph" m1))
) (void))
))
)
(define (dg_remove_pair g u v)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))
(define new_edges (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (if (not (equal? (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f) v)) (let ()
(set! new_edges (append new_edges (list (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define m (if g (hash-ref g "graph" #f) #f))
(set! m (hash-set (or m (hash)) u new_edges))
(set! g (hash-set (or g (hash)) "graph" m))
) (void))
))
)
(define (dg_all_nodes g)
  (let/ec _return (begin
(define res (list))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(set! res (append res (list k)))
  )))
(_return res)
))
)
(define (dg_dfs_util g node visited order d)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(set! order (append order (list node)))
(if (and (not (equal? d (- 1))) (equal? node d)) (let ()
(_return order)
) (void))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! order (dg_dfs_util g neigh visited order d))
(if (and (not (equal? d (- 1))) (equal? (if order (if (hash? order) (hash-ref order (- (cond [(not order) 0] [(string? order) (string-length order)] [(hash? order) (hash-count order)] [else (length order)]) 1) #f) (list-ref order (int (- (cond [(not order) 0] [(string? order) (string-length order)] [(hash? order) (hash-count order)] [else (length order)]) 1)))) #f) d)) (let ()
(_return order)
) (void))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return order)
))
)
(define (dg_dfs g s d)
  (let/ec _return (begin
(if (equal? s d) (let ()
(_return (list))
) (void))
(define start (if (equal? s (- 2)) (first_key (if g (hash-ref g "graph" #f) #f)) s))
(define visited (hash))
(define order (list))
(set! order (dg_dfs_util g start visited order d))
(_return order)
))
)
(define (dg_bfs g s)
  (let/ec _return (begin
(define queue (list))
(define visited (hash))
(define order (list))
(define start (if (equal? s (- 2)) (first_key (if g (hash-ref g "graph" #f) #f)) s))
(set! queue (append queue (list start)))
(set! visited (hash-set (or visited (hash)) start #t))
(let/ec _break (let loop ()
  (if (> (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)]) 0) (let/ec _cont
    (define node (if queue (if (hash? queue) (hash-ref queue 0 #f) (list-ref queue (int 0))) #f))
    (set! queue (slice queue 1 (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)])))
    (set! order (append order (list node)))
    (define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! queue (append queue (list neigh)))
(set! visited (hash-set (or visited (hash)) neigh #t))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (loop)) (void))))
(_return order)
))
)
(define (dg_in_degree g u)
  (let/ec _return (begin
(define count 0)
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) k #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int k))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (if (equal? (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f) u) (let ()
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
  )))
(_return count)
))
)
(define (dg_out_degree g u)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(_return (cond [(not (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) 0] [(string? (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) (string-length (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))] [(hash? (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) (hash-count (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))] [else (length (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))]))
) (void))
(_return 0)
))
)
(define (dg_topo_util g node visited stack)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! stack (dg_topo_util g neigh visited stack))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! stack (append stack (list node)))
(_return stack)
))
)
(define (dg_topological_sort g)
  (let/ec _return (begin
(define visited (hash))
(define stack (list))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(if (not (hash-has-key? visited k)) (let ()
(set! stack (dg_topo_util g k visited stack))
) (void))
  )))
(define res (list))
(define i (- (cond [(not stack) 0] [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1))
(let/ec _break (let loop ()
  (if (>= i 0) (let/ec _cont
    (set! res (append res (list (if stack (if (hash? stack) (hash-ref stack i #f) (list-ref stack (int i))) #f))))
    (set! i (- i 1))
    (loop)) (void))))
(_return res)
))
)
(define (dg_cycle_util g node visited rec res)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(set! rec (hash-set (or rec (hash)) node #t))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! res (dg_cycle_util g neigh visited rec res))
) (let ()
(if (if rec (hash-ref rec neigh #f) #f) (let ()
(if (not (list_contains_int res neigh)) (let ()
(set! res (append res (list neigh)))
) (void))
(if (not (list_contains_int res node)) (let ()
(set! res (append res (list node)))
) (void))
) (void))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! rec (hash-set (or rec (hash)) node #f))
(_return res)
))
)
(define (dg_cycle_nodes g)
  (let/ec _return (begin
(define visited (hash))
(define rec (hash))
(define res (list))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(if (not (hash-has-key? visited k)) (let ()
(set! res (dg_cycle_util g k visited rec res))
) (void))
  )))
(_return res)
))
)
(define (dg_has_cycle_util g node visited rec)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(set! rec (hash-set (or rec (hash)) node #t))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(if (dg_has_cycle_util g neigh visited rec) (let ()
(_return #t)
) (void))
) (let ()
(if (if rec (hash-ref rec neigh #f) #f) (let ()
(_return #t)
) (void))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! rec (hash-set (or rec (hash)) node #f))
(_return #f)
))
)
(define (dg_has_cycle g)
  (let/ec _return (begin
(define visited (hash))
(define rec (hash))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(if (not (hash-has-key? visited k)) (let ()
(if (dg_has_cycle_util g k visited rec) (let ()
(_return #t)
) (void))
) (void))
  )))
(_return #f)
))
)
(define (dg_fill_graph_randomly g c)
  (let/ec _return (begin
(define count c)
(if (equal? count (- 1)) (let ()
(set! count (rand_range 10 10010))
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i count) (let/ec _cont
    (define edge_count (rand_range 1 103))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j edge_count) (let/ec _cont
    (define n (rand_range 0 count))
    (if (not (equal? n i)) (let ()
(dg_add_pair g i n 1)
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(define (dg_dfs_time g s e)
  (let/ec _return (begin
(define begin (now))
(dg_dfs g s e)
(define end (now))
(_return (- end begin))
))
)
(define (dg_bfs_time g s)
  (let/ec _return (begin
(define begin (now))
(dg_bfs g s)
(define end (now))
(_return (- end begin))
))
)
(define (g_make_graph)
  (let/ec _return (begin
(_return (hash "graph" (hash)))
))
)
(define (g_add_pair g u v w)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))
(if (not (edge_exists edges w v)) (let ()
(set! edges (append edges (list (list w v))))
(define m (if g (hash-ref g "graph" #f) #f))
(set! m (hash-set (or m (hash)) u edges))
(set! g (hash-set (or g (hash)) "graph" m))
) (void))
) (let ()
(define m0 (if g (hash-ref g "graph" #f) #f))
(set! m0 (hash-set (or m0 (hash)) u (list (list w v))))
(set! g (hash-set (or g (hash)) "graph" m0))
))
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) v) (let ()
(define edges2 (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) v #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int v))) #f))
(if (not (edge_exists edges2 w u)) (let ()
(set! edges2 (append edges2 (list (list w u))))
(define m2 (if g (hash-ref g "graph" #f) #f))
(set! m2 (hash-set (or m2 (hash)) v edges2))
(set! g (hash-set (or g (hash)) "graph" m2))
) (void))
) (let ()
(define m3 (if g (hash-ref g "graph" #f) #f))
(set! m3 (hash-set (or m3 (hash)) v (list (list w u))))
(set! g (hash-set (or g (hash)) "graph" m3))
))
))
)
(define (g_remove_pair g u v)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))
(define new_edges (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (if (not (equal? (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f) v)) (let ()
(set! new_edges (append new_edges (list (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define m (if g (hash-ref g "graph" #f) #f))
(set! m (hash-set (or m (hash)) u new_edges))
(set! g (hash-set (or g (hash)) "graph" m))
) (void))
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) v) (let ()
(define edges2 (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) v #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int v))) #f))
(define new_edges2 (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not edges2) 0] [(string? edges2) (string-length edges2)] [(hash? edges2) (hash-count edges2)] [else (length edges2)])) (let/ec _cont
    (if (not (equal? (if (if edges2 (if (hash? edges2) (hash-ref edges2 j #f) (list-ref edges2 (int j))) #f) (if (hash? (if edges2 (if (hash? edges2) (hash-ref edges2 j #f) (list-ref edges2 (int j))) #f)) (hash-ref (if edges2 (if (hash? edges2) (hash-ref edges2 j #f) (list-ref edges2 (int j))) #f) 1 #f) (list-ref (if edges2 (if (hash? edges2) (hash-ref edges2 j #f) (list-ref edges2 (int j))) #f) (int 1))) #f) u)) (let ()
(set! new_edges2 (append new_edges2 (list (if edges2 (if (hash? edges2) (hash-ref edges2 j #f) (list-ref edges2 (int j))) #f))))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define m2 (if g (hash-ref g "graph" #f) #f))
(set! m2 (hash-set (or m2 (hash)) v new_edges2))
(set! g (hash-set (or g (hash)) "graph" m2))
) (void))
))
)
(define (g_all_nodes g)
  (let/ec _return (begin
(define res (list))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(set! res (append res (list k)))
  )))
(_return res)
))
)
(define (g_dfs_util g node visited order d)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(set! order (append order (list node)))
(if (and (not (equal? d (- 1))) (equal? node d)) (let ()
(_return order)
) (void))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! order (g_dfs_util g neigh visited order d))
(if (and (not (equal? d (- 1))) (equal? (if order (if (hash? order) (hash-ref order (- (cond [(not order) 0] [(string? order) (string-length order)] [(hash? order) (hash-count order)] [else (length order)]) 1) #f) (list-ref order (int (- (cond [(not order) 0] [(string? order) (string-length order)] [(hash? order) (hash-count order)] [else (length order)]) 1)))) #f) d)) (let ()
(_return order)
) (void))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return order)
))
)
(define (g_dfs g s d)
  (let/ec _return (begin
(if (equal? s d) (let ()
(_return (list))
) (void))
(define start (if (equal? s (- 2)) (first_key (if g (hash-ref g "graph" #f) #f)) s))
(define visited (hash))
(define order (list))
(set! order (g_dfs_util g start visited order d))
(_return order)
))
)
(define (g_bfs g s)
  (let/ec _return (begin
(define queue (list))
(define visited (hash))
(define order (list))
(define start (if (equal? s (- 2)) (first_key (if g (hash-ref g "graph" #f) #f)) s))
(set! queue (append queue (list start)))
(set! visited (hash-set (or visited (hash)) start #t))
(let/ec _break (let loop ()
  (if (> (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)]) 0) (let/ec _cont
    (define node (if queue (if (hash? queue) (hash-ref queue 0 #f) (list-ref queue (int 0))) #f))
    (set! queue (slice queue 1 (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)])))
    (set! order (append order (list node)))
    (define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! queue (append queue (list neigh)))
(set! visited (hash-set (or visited (hash)) neigh #t))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (loop)) (void))))
(_return order)
))
)
(define (g_degree g u)
  (let/ec _return (begin
(if (hash-has-key? (if g (hash-ref g "graph" #f) #f) u) (let ()
(_return (cond [(not (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) 0] [(string? (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) (string-length (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))] [(hash? (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f)) (hash-count (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))] [else (length (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) u #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int u))) #f))]))
) (void))
(_return 0)
))
)
(define (g_cycle_util g node visited parent res)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(set! res (g_cycle_util g neigh visited node res))
) (let ()
(if (not (equal? neigh parent)) (let ()
(if (not (list_contains_int res neigh)) (let ()
(set! res (append res (list neigh)))
) (void))
(if (not (list_contains_int res node)) (let ()
(set! res (append res (list node)))
) (void))
) (void))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (g_cycle_nodes g)
  (let/ec _return (begin
(define visited (hash))
(define res (list))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(if (not (hash-has-key? visited k)) (let ()
(set! res (g_cycle_util g k visited (- 1) res))
) (void))
  )))
(_return res)
))
)
(define (g_has_cycle_util g node visited parent)
  (let/ec _return (begin
(set! visited (hash-set (or visited (hash)) node #t))
(define edges (if (if g (hash-ref g "graph" #f) #f) (if (hash? (if g (hash-ref g "graph" #f) #f)) (hash-ref (if g (hash-ref g "graph" #f) #f) node #f) (list-ref (if g (hash-ref g "graph" #f) #f) (int node))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define neigh (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? visited neigh)) (let ()
(if (g_has_cycle_util g neigh visited node) (let ()
(_return #t)
) (void))
) (let ()
(if (not (equal? neigh parent)) (let ()
(_return #t)
) (void))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define (g_has_cycle g)
  (let/ec _return (begin
(define visited (hash))
(let/ec _break (for ([k (in-hash-keys (if g (hash-ref g "graph" #f) #f))])
  (let/ec _cont
(if (not (hash-has-key? visited k)) (let ()
(if (g_has_cycle_util g k visited (- 1)) (let ()
(_return #t)
) (void))
) (void))
  )))
(_return #f)
))
)
(define (g_fill_graph_randomly g c)
  (let/ec _return (begin
(define count c)
(if (equal? count (- 1)) (let ()
(set! count (rand_range 10 10010))
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i count) (let/ec _cont
    (define edge_count (rand_range 1 103))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j edge_count) (let/ec _cont
    (define n (rand_range 0 count))
    (if (not (equal? n i)) (let ()
(g_add_pair g i n 1)
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(define (g_dfs_time g s e)
  (let/ec _return (begin
(define begin (now))
(g_dfs g s e)
(define end (now))
(_return (- end begin))
))
)
(define (g_bfs_time g s)
  (let/ec _return (begin
(define begin (now))
(g_bfs g s)
(define end (now))
(_return (- end begin))
))
)
(define (main)
  (let/ec _return (begin
(define dg (dg_make_graph))
(dg_add_pair dg 0 1 5)
(dg_add_pair dg 0 2 3)
(dg_add_pair dg 1 3 2)
(dg_add_pair dg 2 3 4)
(displayln (format "~a" (dg_dfs dg (- 2) (- 1))))
(displayln (format "~a" (dg_bfs dg (- 2))))
(displayln (format "~a" (dg_in_degree dg 3)))
(displayln (format "~a" (dg_out_degree dg 0)))
(displayln (format "~a" (dg_topological_sort dg)))
(displayln (format "~a" (dg_has_cycle dg)))
(define ug (g_make_graph))
(g_add_pair ug 0 1 1)
(g_add_pair ug 1 2 1)
(g_add_pair ug 2 0 1)
(displayln (format "~a" (g_dfs ug (- 2) (- 1))))
(displayln (format "~a" (g_bfs ug (- 2))))
(displayln (format "~a" (g_degree ug 1)))
(displayln (format "~a" (g_has_cycle ug)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
