;; Generated by Mochi 0.10.60 on 2025-08-07 16:04 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define tree (hash))
(define (dfs start visited)
  (let/ec _return (begin
(define size 1)
(define cuts 0)
(set! visited (hash-set (or visited (hash)) start #t))
(let/ec _break (for ([v (if tree (hash-ref tree start (list)) #f)])
  (let/ec _cont
(if (not (hash-has-key? visited v)) (let ()
(define res (dfs v visited))
(set! size (let ([__l size] [__r (if res (if (hash? res) (hash-ref res 0 #f) (list-ref res (int 0))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! cuts (let ([__l cuts] [__r (if res (if (hash? res) (hash-ref res 1 #f) (list-ref res (int 1))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
  )))
(if (equal? (modulo size 2) 0) (let ()
(set! cuts (let ([__l cuts] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(_return (list size cuts))
))
)
(define (even_tree)
  (let/ec _return (begin
(define visited (hash))
(define res (dfs 1 visited))
(_return (- (if res (if (hash? res) (hash-ref res 1 #f) (list-ref res (int 1))) #f) 1))
))
)
(define (main)
  (let/ec _return (begin
(define edges (list (list 2 1) (list 3 1) (list 4 3) (list 5 2) (list 6 1) (list 7 2) (list 8 6) (list 9 8) (list 10 8)))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])) (let/ec _cont
    (define u (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 0 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 0))) #f))
    (define v (if (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) 1 #f) (list-ref (if edges (if (hash? edges) (hash-ref edges i #f) (list-ref edges (int i))) #f) (int 1))) #f))
    (if (not (hash-has-key? tree u)) (let ()
(set! tree (hash-set (or tree (hash)) u (list)))
) (void))
    (if (not (hash-has-key? tree v)) (let ()
(set! tree (hash-set (or tree (hash)) v (list)))
) (void))
    (set! tree (hash-set (or tree (hash)) u (append (if tree (hash-ref tree u (list)) #f) (list v))))
    (set! tree (hash-set (or tree (hash)) v (append (if tree (hash-ref tree v (list)) #f) (list u))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln (format "~a" (even_tree)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
