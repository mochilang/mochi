;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define NUM_PRIMES 100)
(define (generate_primes limit)
  (let/ec _return (begin
(define is_prime (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r limit]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (set! is_prime (append (or is_prime (list)) (list #t)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! is_prime (list-set is_prime (int 0) #f))
(set! is_prime (list-set is_prime (int 1) #f))
(set! i 2)
(let/ec _break (let loop ()
  (if (let ([__l (* i i)] [__r limit]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (if is_prime (if (hash? is_prime) (hash-ref is_prime i #f) (safe-index is_prime (int i))) #f) (let ()
(define j (* i i))
(let/ec _break (let loop ()
  (if (let ([__l j] [__r limit]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (set! is_prime (list-set is_prime (int j) #f))
      (set! j (let ([__l j] [__r i]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define primes (list))
(set! i 2)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r limit]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (if is_prime (if (hash? is_prime) (hash-ref is_prime i #f) (safe-index is_prime (int i))) #f) (let ()
(set! primes (append (or primes (list)) (list i)))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return primes)
))
)
(define primes (generate_primes NUM_PRIMES))
(define (contains xs value)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (equal? (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f) value) (let ()
(_return #t)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define partition_cache (let ([h (make-hash)]) h))
(define (partition n)
  (let/ec _return (begin
(if (let ([__l n] [__r 0]) (< (int __l) (int __r))) (let ()
(_return (list))
) (void))
(if (equal? n 0) (let ()
(_return (list 1))
) (void))
(if (hash-has-key? partition_cache n) (let ()
(_return (if partition_cache (hash-ref partition_cache n (list)) #f))
) (void))
(define ret (list))
(let/ec _break (for ([prime primes] #:unless (let ([__l prime] [__r n]) (> (int __l) (int __r))))
  (let/ec _cont
(define subs (partition (- n prime)))
(let/ec _break (for ([sub subs])
  (let/ec _cont
(define prod (* sub prime))
(if (not (contains ret prod)) (let ()
(set! ret (append (or ret (list)) (list prod)))
) (void))
  )))
  )))
(if (hash? partition_cache) (hash-set! partition_cache n ret) (begin (set! partition_cache (make-hash)) (hash-set! partition_cache n ret)))
(_return ret)
))
)
(define (solution threshold)
  (let/ec _return (begin
(define number_to_partition 1)
(let/ec _break (let loop ()
  (if (let ([__l number_to_partition] [__r NUM_PRIMES]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define parts (partition number_to_partition))
      (if (let ([__l (cond [(not parts) 0] [(string? parts) (string-length parts)] [(hash? parts) (hash-count parts)] [else (length parts)])] [__r threshold]) (> (int __l) (int __r))) (let ()
(_return number_to_partition)
) (void))
      (set! number_to_partition (let ([__l number_to_partition] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return 0)
))
)
(define result (solution 5000))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append "solution() = " (to-string result)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
