;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (prime_sieve limit)
  (let/ec _return (begin
(if (let ([__l limit] [__r 2]) (<= (int __l) (int __r))) (let ()
(_return (list))
) (void))
(define is_prime (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r limit]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! is_prime (append (or is_prime (list)) (list #t)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! is_prime (list-set is_prime (int 0) #f))
(set! is_prime (list-set is_prime (int 1) #f))
(define p 3)
(let/ec _break (let loop ()
  (if (let ([__l (* p p)] [__r limit]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define index (* p 2))
      (let/ec _break (let loop ()
  (if (let ([__l index] [__r limit]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! is_prime (list-set is_prime (int index) #f))
      (set! index (let ([__l index] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! p (let ([__l p] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define primes (list 2))
(define n 3)
(let/ec _break (let loop ()
  (if (let ([__l n] [__r limit]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (if is_prime (if (hash? is_prime) (hash-ref is_prime n #f) (safe-index is_prime (int n))) #f) (let ()
(set! primes (append (or primes (list)) (list n)))
) (void))
      (set! n (let ([__l n] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return primes)
))
)
(define (solution ceiling)
  (let/ec _return (begin
(define primes (prime_sieve ceiling))
(define prime_map (let ([h (make-hash)]) h))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not primes) 0] [(string? primes) (string-length primes)] [(hash? primes) (hash-count primes)] [else (length primes)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (hash? prime_map) (hash-set! prime_map (if primes (if (hash? primes) (hash-ref primes i #f) (safe-index primes (int i))) #f) #t) (begin (set! prime_map (make-hash)) (hash-set! prime_map (if primes (if (hash? primes) (hash-ref primes i #f) (safe-index primes (int i))) #f) #t)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define prefix (list 0))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not primes) 0] [(string? primes) (string-length primes)] [(hash? primes) (hash-count primes)] [else (length primes)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! prefix (append (or prefix (list)) (list (let ([__l (if prefix (if (hash? prefix) (hash-ref prefix i #f) (safe-index prefix (int i))) #f)] [__r (if primes (if (hash? primes) (hash-ref primes i #f) (safe-index primes (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define max_len 0)
(let/ec _break (let loop ()
  (if (and (let ([__l max_len] [__r (cond [(not prefix) 0] [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)])]) (< (int __l) (int __r))) (let ([__l (if prefix (if (hash? prefix) (hash-ref prefix max_len #f) (safe-index prefix (int max_len))) #f)] [__r ceiling]) (< (int __l) (int __r)))) (begin
    (let/ec _cont
      (set! max_len (let ([__l max_len] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define L max_len)
(let/ec _break (let loop ()
  (if (let ([__l L] [__r 0]) (> (int __l) (int __r))) (begin
    (let/ec _cont
      (define start 0)
      (let/ec _break (let loop ()
  (if (let ([__l (let ([__l start] [__r L]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (cond [(not primes) 0] [(string? primes) (string-length primes)] [(hash? primes) (hash-count primes)] [else (length primes)])]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (define s (- (if prefix (if (hash? prefix) (hash-ref prefix (let ([__l start] [__r L]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index prefix (int (let ([__l start] [__r L]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f) (if prefix (if (hash? prefix) (hash-ref prefix start #f) (safe-index prefix (int start))) #f)))
      (if (let ([__l s] [__r ceiling]) (>= (int __l) (int __r))) (let ()
(_break)
) (void))
      (if (if prime_map (hash-ref prime_map s #f) #f) (let ()
(_return s)
) (void))
      (set! start (let ([__l start] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! L (- L 1))
    )
    (loop)) (void))))
(_return 0)
))
)
(define ans (solution 1000000))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append "solution() = " (to-string ans)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
