;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define DIGIT_FACTORIALS (list 1 1 2 6 24 120 720 5040 40320 362880))
(define cache_sum_digit_factorials (let ([h (make-hash)]) (hash-set! h 145 145) h))
(define chain_length_cache (let ([h (make-hash)]) (hash-set! h 145 0) (hash-set! h 169 3) (hash-set! h 36301 3) (hash-set! h 1454 3) (hash-set! h 871 2) (hash-set! h 45361 2) (hash-set! h 872 2) h))
(define (sum_digit_factorials n)
  (let/ec _return (begin
(if (hash-has-key? cache_sum_digit_factorials n) (let ()
(_return (if cache_sum_digit_factorials (hash-ref cache_sum_digit_factorials n 0) #f))
) (void))
(define m n)
(define ret 0)
(if (equal? m 0) (let ()
(set! ret (if DIGIT_FACTORIALS (if (hash? DIGIT_FACTORIALS) (hash-ref DIGIT_FACTORIALS 0 #f) (safe-index DIGIT_FACTORIALS (int 0))) #f))
) (void))
(let/ec _break (let loop ()
  (if (let ([__l m] [__r 0]) (> (int __l) (int __r))) (begin
    (let/ec _cont
      (define digit (modulo m 10))
      (set! ret (let ([__l ret] [__r (if DIGIT_FACTORIALS (if (hash? DIGIT_FACTORIALS) (hash-ref DIGIT_FACTORIALS digit #f) (safe-index DIGIT_FACTORIALS (int digit))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! m (floor (/ m 10)))
    )
    (loop)) (void))))
(if (hash? cache_sum_digit_factorials) (hash-set! cache_sum_digit_factorials n ret) (begin (set! cache_sum_digit_factorials (make-hash)) (hash-set! cache_sum_digit_factorials n ret)))
(_return ret)
))
)
(define (chain_length n)
  (let/ec _return (begin
(if (hash-has-key? chain_length_cache n) (let ()
(_return (if chain_length_cache (hash-ref chain_length_cache n 0) #f))
) (void))
(define chain (list))
(define seen (let ([h (make-hash)]) h))
(define current n)
(let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (if (hash-has-key? chain_length_cache current) (let ()
(define known (if chain_length_cache (hash-ref chain_length_cache current 0) #f))
(define total known)
(define i (- (cond [(not chain) 0] [(string? chain) (string-length chain)] [(hash? chain) (hash-count chain)] [else (length chain)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (>= (int __l) (int __r))) (begin
    (let/ec _cont
      (set! total (let ([__l total] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (if (hash? chain_length_cache) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) total) (begin (set! chain_length_cache (make-hash)) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) total)))
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return (if chain_length_cache (hash-ref chain_length_cache n 0) #f))
) (void))
      (if (hash-has-key? seen current) (let ()
(define loop_start (if seen (hash-ref seen current 0) #f))
(define loop_len (- (cond [(not chain) 0] [(string? chain) (string-length chain)] [(hash? chain) (hash-count chain)] [else (length chain)]) loop_start))
(define i (- (cond [(not chain) 0] [(string? chain) (string-length chain)] [(hash? chain) (hash-count chain)] [else (length chain)]) 1))
(define ahead 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (>= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (let ([__l i] [__r loop_start]) (>= (int __l) (int __r))) (let ()
(if (hash? chain_length_cache) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) loop_len) (begin (set! chain_length_cache (make-hash)) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) loop_len)))
) (let ()
(if (hash? chain_length_cache) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) (let ([__l loop_len] [__r (let ([__l ahead] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! chain_length_cache (make-hash)) (hash-set! chain_length_cache (if chain (if (hash? chain) (hash-ref chain i #f) (safe-index chain (int i))) #f) (let ([__l loop_len] [__r (let ([__l ahead] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
))
      (set! ahead (let ([__l ahead] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return (if chain_length_cache (hash-ref chain_length_cache n 0) #f))
) (void))
      (if (hash? seen) (hash-set! seen current (cond [(not chain) 0] [(string? chain) (string-length chain)] [(hash? chain) (hash-count chain)] [else (length chain)])) (begin (set! seen (make-hash)) (hash-set! seen current (cond [(not chain) 0] [(string? chain) (string-length chain)] [(hash? chain) (hash-count chain)] [else (length chain)]))))
      (set! chain (append (or chain (list)) (list current)))
      (set! current (sum_digit_factorials current))
    )
    (loop)) (void))))
))
)
(define (solution num_terms max_start)
  (let/ec _return (begin
(define count 0)
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r max_start]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (equal? (chain_length i) num_terms) (let ()
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return count)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append "solution() = " (to-string (solution 60 1000))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
