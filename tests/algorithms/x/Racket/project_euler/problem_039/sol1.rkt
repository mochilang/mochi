;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (int_sqrt n)
  (let/ec _return (begin
(define low 0)
(define high n)
(let/ec _break (let loop ()
  (if (let ([__l low] [__r high]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (define mid (floor (/ (let ([__l low] [__r high]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
      (define sq (* mid mid))
      (if (equal? sq n) (let ()
(_return mid)
) (void))
      (if (let ([__l sq] [__r n]) (< (int __l) (int __r))) (let ()
(set! low (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! high (- mid 1))
))
    )
    (loop)) (void))))
(_return high)
))
)
(define (pythagorean_triple max_perimeter)
  (let/ec _return (begin
(define triplets (let ([h (make-hash)]) h))
(define base 1)
(let/ec _break (let loop ()
  (if (let ([__l base] [__r max_perimeter]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (define perpendicular base)
      (let/ec _break (let loop ()
  (if (let ([__l perpendicular] [__r max_perimeter]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (define hyp_sq (let ([__l (* base base)] [__r (* perpendicular perpendicular)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define hyp (int_sqrt hyp_sq))
      (if (equal? (* hyp hyp) hyp_sq) (let ()
(define perimeter (let ([__l (let ([__l base] [__r perpendicular]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r hyp]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (let ([__l perimeter] [__r max_perimeter]) (<= (int __l) (int __r))) (let ()
(if (hash-has-key? triplets perimeter) (let ()
(if (hash? triplets) (hash-set! triplets perimeter (let ([__l (if triplets (hash-ref triplets perimeter 0) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! triplets (make-hash)) (hash-set! triplets perimeter (let ([__l (if triplets (hash-ref triplets perimeter 0) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (let ()
(if (hash? triplets) (hash-set! triplets perimeter 1) (begin (set! triplets (make-hash)) (hash-set! triplets perimeter 1)))
))
) (void))
) (void))
      (set! perpendicular (let ([__l perpendicular] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! base (let ([__l base] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return triplets)
))
)
(define (max_perimeter counts)
  (let/ec _return (begin
(define best_p 0)
(define max_count 0)
(let/ec _break (for ([p (in-hash-keys counts)])
  (let/ec _cont
(define count (if counts (hash-ref counts p 0) #f))
(if (or (let ([__l count] [__r max_count]) (> (int __l) (int __r))) (and (equal? count max_count) (let ([__l p] [__r best_p]) (> (int __l) (int __r))))) (let ()
(set! max_count count)
(set! best_p p)
) (void))
  )))
(_return best_p)
))
)
(define (solution n)
  (let/ec _return (begin
(define triplets (pythagorean_triple n))
(_return (max_perimeter triplets))
))
)
(define (main)
  (let/ec _return (begin
(define s200 (max_perimeter (pythagorean_triple 200)))
(define s100 (max_perimeter (pythagorean_triple 100)))
(displayln (to-string s100))
(displayln (to-string s200))
(define best (max_perimeter (pythagorean_triple 1000)))
(displayln (to-string best))
(displayln (string-append (string-append "Perimeter " (to-string best)) " has maximum solutions"))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
