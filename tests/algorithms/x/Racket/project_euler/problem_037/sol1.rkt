;; Generated by Mochi 0.10.64 on 2025-08-12 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (is_prime number)
  (let/ec _return (begin
(if (and (let ([__l 1] [__r number]) (< (int __l) (int __r))) (let ([__l number] [__r 4]) (< (int __l) (int __r)))) (let ()
(_return #t)
) (void))
(if (or (or (let ([__l number] [__r 2]) (< (int __l) (int __r))) (equal? (modulo number 2) 0)) (equal? (modulo number 3) 0)) (let ()
(_return #f)
) (void))
(define i 5)
(let/ec _break (let loop ()
  (if (let ([__l (* i i)] [__r number]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (or (equal? (modulo number i) 0) (equal? (modulo number (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) 0)) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 6]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (list_truncated_nums n)
  (let/ec _return (begin
(define str_num (to-string n))
(define list_nums (list n))
(define i 1)
(define length_ (cond [(not str_num) 0] [(string? str_num) (string-length str_num)] [(hash? str_num) (hash-count str_num)] [else (length str_num)]))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r length_]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define right (int (slice str_num i length_)))
      (define left (int (slice str_num 0 (- length_ i))))
      (set! list_nums (append (or list_nums (list)) (list right)))
      (set! list_nums (append (or list_nums (list)) (list left)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return list_nums)
))
)
(define (validate n)
  (let/ec _return (begin
(define s (to-string n))
(define length_ (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]))
(if (let ([__l length_] [__r 3]) (> (int __l) (int __r))) (let ()
(define last3 (int (slice s (- length_ 3) length_)))
(define first3 (int (slice s 0 3)))
(if (not (and (is_prime last3) (is_prime first3))) (let ()
(_return #f)
) (void))
) (void))
(_return #t)
))
)
(define (compute_truncated_primes count)
  (let/ec _return (begin
(define list_truncated_primes (list))
(define num 13)
(let/ec _break (let loop ()
  (if (not (equal? (cond [(not list_truncated_primes) 0] [(string? list_truncated_primes) (string-length list_truncated_primes)] [(hash? list_truncated_primes) (hash-count list_truncated_primes)] [else (length list_truncated_primes)]) count)) (begin
    (let/ec _cont
      (if (validate num) (let ()
(define list_nums (list_truncated_nums num))
(define all_prime #t)
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not list_nums) 0] [(string? list_nums) (string-length list_nums)] [(hash? list_nums) (hash-count list_nums)] [else (length list_nums)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (not (is_prime (if list_nums (if (hash? list_nums) (hash-ref list_nums j #f) (safe-index list_nums (int j))) #f))) (let ()
(set! all_prime #f)
(_break)
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if all_prime (let ()
(set! list_truncated_primes (append (or list_truncated_primes (list)) (list num)))
) (void))
) (void))
      (set! num (let ([__l num] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return list_truncated_primes)
))
)
(define (solution)
  (let/ec _return (begin
(define primes (compute_truncated_primes 11))
(define total 0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not primes) 0] [(string? primes) (string-length primes)] [(hash? primes) (hash-count primes)] [else (length primes)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! total (let ([__l total] [__r (if primes (if (hash? primes) (hash-ref primes i #f) (safe-index primes (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return total)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append "sum(compute_truncated_primes(11)) = " (to-string (solution))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
