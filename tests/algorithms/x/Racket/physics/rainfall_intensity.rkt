;; Generated by Mochi 0.10.62 on 2025-08-09 10:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (exp_approx x)
  (let/ec _return (begin
(define y x)
(define is_neg #f)
(if (< x 0.0) (let ()
(set! is_neg #t)
(set! y (- x))
) (void))
(define term 1.0)
(define sum 1.0)
(define n 1)
(let/ec _break (let loop ()
  (if (< n 30) (begin
    (let/ec _cont
      (set! term (/ (* term y) (exact->inexact n)))
      (set! sum (let ([__l sum] [__r term]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if is_neg (let ()
(_return (/ 1.0 sum))
) (void))
(_return sum)
))
)
(define (ln_series x)
  (let/ec _return (begin
(define t (/ (- x 1.0) (let ([__l x] [__r 1.0]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(define term t)
(define sum 0.0)
(define n 1)
(let/ec _break (let loop ()
  (if (<= n 19) (begin
    (let/ec _cont
      (set! sum (let ([__l sum] [__r (/ term (exact->inexact n))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! term (* (* term t) t))
      (set! n (let ([__l n] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (* 2.0 sum))
))
)
(define (ln x)
  (let/ec _return (begin
(define y x)
(define k 0)
(let/ec _break (let loop ()
  (if (>= y 10.0) (begin
    (let/ec _cont
      (set! y (/ y 10.0))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (< y 1.0) (begin
    (let/ec _cont
      (set! y (* y 10.0))
      (set! k (- k 1))
    )
    (loop)) (void))))
(_return (let ([__l (ln_series y)] [__r (* (exact->inexact k) (ln_series 10.0))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (powf base exponent)
  (let/ec _return (begin
(_return (exp_approx (* exponent (ln base))))
))
)
(define (rainfall_intensity coefficient_k coefficient_a coefficient_b coefficient_c return_period duration)
  (let/ec _return (begin
(if (<= coefficient_k 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(if (<= coefficient_a 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(if (<= coefficient_b 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(if (<= coefficient_c 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(if (<= return_period 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(if (<= duration 0.0) (let ()
(panic "All parameters must be positive.")
) (void))
(define numerator (* coefficient_k (powf return_period coefficient_a)))
(define denominator (powf (let ([__l duration] [__r coefficient_b]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) coefficient_c))
(_return (/ numerator denominator))
))
)
(define r1 (rainfall_intensity 1000.0 0.2 11.6 0.81 10.0 60.0))
(define r2 (rainfall_intensity 1000.0 0.2 11.6 0.81 10.0 30.0))
(define r3 (rainfall_intensity 1000.0 0.2 11.6 0.81 5.0 60.0))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string r1))
(displayln (to-string r2))
(displayln (to-string r3))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
