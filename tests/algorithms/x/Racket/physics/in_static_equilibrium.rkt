;; Generated by Mochi 0.10.62 on 2025-08-09 10:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define PI 3.141592653589793)
(define TWO_PI 6.283185307179586)
(define (_mod x m)
  (let/ec _return (begin
(_return (- x (* (exact->inexact (int (/ x m))) m)))
))
)
(define (sin_approx x)
  (let/ec _return (begin
(define y (- (_mod (let ([__l x] [__r PI]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) TWO_PI) PI))
(define y2 (* y y))
(define y3 (* y2 y))
(define y5 (* y3 y2))
(define y7 (* y5 y2))
(_return (- (let ([__l (- y (/ y3 6.0))] [__r (/ y5 120.0)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (/ y7 5040.0)))
))
)
(define (cos_approx x)
  (let/ec _return (begin
(define y (- (_mod (let ([__l x] [__r PI]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) TWO_PI) PI))
(define y2 (* y y))
(define y4 (* y2 y2))
(define y6 (* y4 y2))
(_return (- (let ([__l (- 1.0 (/ y2 2.0))] [__r (/ y4 24.0)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (/ y6 720.0)))
))
)
(define (polar_force magnitude angle radian_mode)
  (let/ec _return (begin
(define theta (if radian_mode angle (/ (* angle PI) 180.0)))
(_return (list (* magnitude (cos_approx theta)) (* magnitude (sin_approx theta))))
))
)
(define (abs_float x)
  (let/ec _return (begin
(if (< x 0.0) (let ()
(_return (- x))
) (let ()
(_return x)
))
))
)
(define (in_static_equilibrium forces location eps)
  (let/ec _return (begin
(define sum_moments 0.0)
(define i 0)
(define n (cond [(not forces) 0] [(string? forces) (string-length forces)] [(hash? forces) (hash-count forces)] [else (length forces)]))
(let/ec _break (let loop ()
  (if (< i n) (begin
    (let/ec _cont
      (define r (if location (if (hash? location) (hash-ref location i #f) (safe-index location (int i))) #f))
      (define f (if forces (if (hash? forces) (hash-ref forces i #f) (safe-index forces (int i))) #f))
      (define moment (- (* (if r (if (hash? r) (hash-ref r 0 #f) (safe-index r (int 0))) #f) (if f (if (hash? f) (hash-ref f 1 #f) (safe-index f (int 1))) #f)) (* (if r (if (hash? r) (hash-ref r 1 #f) (safe-index r (int 1))) #f) (if f (if (hash? f) (hash-ref f 0 #f) (safe-index f (int 0))) #f))))
      (set! sum_moments (let ([__l sum_moments] [__r moment]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (< (abs_float sum_moments) eps))
))
)
(define forces1 (list (list 1.0 1.0) (list (- 1.0) 2.0)))
(define location1 (list (list 1.0 0.0) (list 10.0 0.0)))
(define forces2 (list (polar_force 718.4 150.0 #f) (polar_force 879.54 45.0 #f) (polar_force 100.0 (- 90.0) #f)))
(define location2 (list (list 0.0 0.0) (list 0.0 0.0) (list 0.0 0.0)))
(define forces3 (list (polar_force (* 30.0 9.81) 15.0 #f) (polar_force 215.0 135.0 #f) (polar_force 264.0 60.0 #f)))
(define location3 (list (list 0.0 0.0) (list 0.0 0.0) (list 0.0 0.0)))
(define forces4 (list (list 0.0 (- 2000.0)) (list 0.0 (- 1200.0)) (list 0.0 15600.0) (list 0.0 (- 12400.0))))
(define location4 (list (list 0.0 0.0) (list 6.0 0.0) (list 10.0 0.0) (list 12.0 0.0)))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (in_static_equilibrium forces1 location1 0.1)))
(displayln (to-string (in_static_equilibrium forces2 location2 0.1)))
(displayln (to-string (in_static_equilibrium forces3 location3 0.1)))
(displayln (to-string (in_static_equilibrium forces4 location4 0.1)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
