;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (sqrt x)
  (let/ec _return (begin
(if (<= x 0.0) (let ()
(_return 0.0)
) (void))
(define guess x)
(define i 0)
(let/ec _break (let loop ()
  (if (< i 10) (let/ec _cont
    (set! guess (/ (let ([__l guess] [__r (/ x guess)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return guess)
))
)
(define (euclidean a b)
  (let/ec _return (begin
(define sum 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])) (let/ec _cont
    (define diff (- (if a (if (hash? a) (hash-ref a i #f) (list-ref a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (list-ref b (int i))) #f)))
    (set! sum (let ([__l sum] [__r (* diff diff)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define res (sqrt sum))
(_return res)
))
)
(define (similarity_search dataset value_array)
  (let/ec _return (begin
(define dim (cond [(not (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f)) 0] [(string? (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f)) (string-length (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f))] [(hash? (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f)) (hash-count (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f))] [else (length (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f))]))
(if (not (equal? dim (cond [(not (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f)) 0] [(string? (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f)) (string-length (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f))] [(hash? (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f)) (hash-count (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f))] [else (length (if value_array (if (hash? value_array) (hash-ref value_array 0 #f) (list-ref value_array (int 0))) #f))]))) (let ()
(_return (list))
) (void))
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not value_array) 0] [(string? value_array) (string-length value_array)] [(hash? value_array) (hash-count value_array)] [else (length value_array)])) (let/ec _cont
    (define value (if value_array (if (hash? value_array) (hash-ref value_array i #f) (list-ref value_array (int i))) #f))
    (define dist (euclidean value (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f)))
    (define vec (if dataset (if (hash? dataset) (hash-ref dataset 0 #f) (list-ref dataset (int 0))) #f))
    (define j 1)
    (let/ec _break (let loop ()
  (if (< j (cond [(not dataset) 0] [(string? dataset) (string-length dataset)] [(hash? dataset) (hash-count dataset)] [else (length dataset)])) (let/ec _cont
    (define d (euclidean value (if dataset (if (hash? dataset) (hash-ref dataset j #f) (list-ref dataset (int j))) #f)))
    (if (< d dist) (let ()
(set! dist d)
(set! vec (if dataset (if (hash? dataset) (hash-ref dataset j #f) (list-ref dataset (int j))) #f))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define nb (let ([h (make-hash)]) (hash-set! h "vector" vec) (hash-set! h "distance" dist) h))
    (set! result (append result (list nb)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (cosine_similarity a b)
  (let/ec _return (begin
(define dot 0.0)
(define norm_a 0.0)
(define norm_b 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])) (let/ec _cont
    (set! dot (let ([__l dot] [__r (* (if a (if (hash? a) (hash-ref a i #f) (list-ref a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (list-ref b (int i))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! norm_a (let ([__l norm_a] [__r (* (if a (if (hash? a) (hash-ref a i #f) (list-ref a (int i))) #f) (if a (if (hash? a) (hash-ref a i #f) (list-ref a (int i))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! norm_b (let ([__l norm_b] [__r (* (if b (if (hash? b) (hash-ref b i #f) (list-ref b (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (list-ref b (int i))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(if (or (equal? norm_a 0.0) (equal? norm_b 0.0)) (let ()
(_return 0.0)
) (void))
(_return (/ dot (* (sqrt norm_a) (sqrt norm_b))))
))
)
(define dataset (list (list 0.0 0.0 0.0) (list 1.0 1.0 1.0) (list 2.0 2.0 2.0)))
(define value_array (list (list 0.0 0.0 0.0) (list 0.0 0.0 1.0)))
(define neighbors (similarity_search dataset value_array))
(define k 0)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (< k (cond [(not neighbors) 0] [(string? neighbors) (string-length neighbors)] [(hash? neighbors) (hash-count neighbors)] [else (length neighbors)])) (let/ec _cont
    (define n (if neighbors (if (hash? neighbors) (hash-ref neighbors k #f) (list-ref neighbors (int k))) #f))
    (displayln (string-append (string-append (string-append (string-append "[" (format "~a" (if n (hash-ref n "vector" #f) #f))) ", ") (format "~a" (if n (hash-ref n "distance" #f) #f))) "]"))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln (format "~a" (cosine_similarity (list 1.0 2.0) (list 6.0 32.0))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
