// Generated by Mochi v0.10.59 on 2025-08-07 12:47:08 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def bubble_sort(nums: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var arr: ArrayBuffer[BigInt] = nums
  var n: BigInt = BigInt((arr).size)
  var i: BigInt = BigInt(0)
  while (i < n) {
    var j: BigInt = BigInt(0)
    while (j < n - BigInt(1)) {
      val a: BigInt = arr((j.toInt).toInt)
      val b: BigInt = arr(((j + BigInt(1)).toInt).toInt)
      if (a > b) {
        arr((j).toInt) = b
        arr((j + BigInt(1)).toInt) = a
      }
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return arr
}

def median(nums: ArrayBuffer[BigInt]): Double = {
  val sorted_list: ArrayBuffer[BigInt] = bubble_sort(nums)
  val length: BigInt = BigInt((sorted_list).size)
  val mid_index: BigInt = length / BigInt(2)
  if (length % BigInt(2) == BigInt(0)) {
    return (sorted_list((mid_index.toInt).toInt) + sorted_list(((mid_index - BigInt(1)).toInt).toInt)).toString.toDouble / 2.0
  } else {
    return (sorted_list((mid_index.toInt).toInt)).toString.toDouble
  }
  return 0.0
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(median(ArrayBuffer(BigInt(0)))))
    println(String.valueOf(median(ArrayBuffer(BigInt(4), BigInt(1), BigInt(3), BigInt(2)))))
    println(String.valueOf(median(ArrayBuffer(BigInt(2), BigInt(70), BigInt(6), BigInt(50), BigInt(20), BigInt(8), BigInt(4)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
