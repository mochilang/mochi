// Generated by Mochi v0.10.59 on 2025-08-07 11:23:38 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def floyd_warshall(graph: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  val v: BigInt = BigInt((graph).size)
  var dist: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer[ArrayBuffer[Double]]()
  var i: BigInt = BigInt(0)
  while (i < v) {
    var row: ArrayBuffer[Double] = ArrayBuffer[Double]()
    var j: BigInt = BigInt(0)
    while (j < v) {
      row = row :+ graph((i.toInt).toInt)((j.toInt).toInt)
      j = j + BigInt(1)
    }
    dist = dist :+ row
    i = i + BigInt(1)
  }
  var k: BigInt = BigInt(0)
  while (k < v) {
    i = BigInt(0)
    while (i < v) {
      var j: BigInt = BigInt(0)
      while (j < v) {
        if (((dist((i.toInt).toInt)((k.toInt).toInt) < INF && dist((k.toInt).toInt)((j.toInt).toInt) < INF).asInstanceOf[Boolean] && dist((i.toInt).toInt)((k.toInt).toInt) + dist((k.toInt).toInt)((j.toInt).toInt) < dist((i.toInt).toInt)((j.toInt).toInt)).asInstanceOf[Boolean]) {
          dist((i).toInt)((j).toInt) = dist((i.toInt).toInt)((k.toInt).toInt) + dist((k.toInt).toInt)((j.toInt).toInt)
        }
        j = j + BigInt(1)
      }
      i = i + BigInt(1)
    }
    k = k + BigInt(1)
  }
  return dist
}

def print_dist(dist: ArrayBuffer[ArrayBuffer[Double]]): Any = {
  println("\nThe shortest path matrix using Floyd Warshall algorithm\n")
  var i: BigInt = BigInt(0)
  while (i < BigInt((dist).size)) {
    var j: BigInt = BigInt(0)
    var line: String = ""
    while (j < BigInt((dist((i.toInt).toInt)).size)) {
      if (dist((i.toInt).toInt)((j.toInt).toInt) >= INF / 2.0) {
        line = line + "INF\t"
      } else {
        line = line + String.valueOf(BigInt((dist((i.toInt).toInt)((j.toInt).toInt)).toString.toDouble.toInt)) + "\t"
      }
      j = j + BigInt(1)
    }
    println(line)
    i = i + BigInt(1)
  }
}

val INF: Double = 1000000000.0

val graph: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 5.0, INF, 10.0), ArrayBuffer(INF, 0.0, 3.0, INF), ArrayBuffer(INF, INF, 0.0, 1.0), ArrayBuffer(INF, INF, INF, 0.0))

val result: ArrayBuffer[ArrayBuffer[Double]] = floyd_warshall(graph)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    print_dist(result)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
