// Generated by Mochi v0.10.59 on 2025-08-07 00:50:31 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class Queue(var stack1: ArrayBuffer[BigInt], var stack2: ArrayBuffer[BigInt])

case class GetResult(var queue: Queue, var value: BigInt)

def new_queue(items: ArrayBuffer[BigInt]): Queue = {
  return Queue(items, ArrayBuffer())
}

def len_queue(q: Queue): BigInt = {
  return BigInt((q.stack1).size) + BigInt((q.stack2).size)
}

def str_queue(q: Queue): String = {
  var items: ArrayBuffer[BigInt] = ArrayBuffer()
  var i: BigInt = BigInt((q.stack2).size) - 1
  while (i >= 0) {
    items = items :+ q.stack2((i.toInt).toInt)
    i = i - 1
  }
  var j: BigInt = 0
  while (j < BigInt((q.stack1).size)) {
    items = items :+ q.stack1((j.toInt).toInt)
    j = j + 1
  }
  var s: String = "Queue(("
  var k: BigInt = 0
  while (k < BigInt((items).size)) {
    s = s + String.valueOf(items((k.toInt).toInt))
    if (k < BigInt((items).size) - 1) {
      s = s + ", "
    }
    k = k + 1
  }
  s = s + "))"
  return s
}

def put(q: Queue, item: BigInt): Queue = {
  var s1: ArrayBuffer[BigInt] = q.stack1
  s1 = s1 :+ item
  return Queue(s1, q.stack2)
}

def get(q: Queue): GetResult = {
  var s1: ArrayBuffer[BigInt] = q.stack1
  var s2: ArrayBuffer[BigInt] = q.stack2
  if (BigInt((s2).size) == 0) {
    while (BigInt((s1).size) > 0) {
      val idx: BigInt = BigInt((s1).size) - 1
      val v: BigInt = s1((idx.toInt).toInt)
      var new_s1: ArrayBuffer[BigInt] = ArrayBuffer()
      var i: BigInt = 0
      while (i < idx) {
        new_s1 = new_s1 :+ s1((i.toInt).toInt)
        i = i + 1
      }
      s1 = new_s1
      s2 = s2 :+ v
    }
  }
  if (BigInt((s2).size) == 0) {
    throw new RuntimeException(String.valueOf("Queue is empty"))
  }
  val idx2: BigInt = BigInt((s2).size) - 1
  val value: BigInt = s2((idx2.toInt).toInt)
  var new_s2: ArrayBuffer[BigInt] = ArrayBuffer()
  var j: BigInt = 0
  while (j < idx2) {
    new_s2 = new_s2 :+ s2((j.toInt).toInt)
    j = j + 1
  }
  s2 = new_s2
  return GetResult(Queue(s1, s2), value)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var q: Queue = new_queue(ArrayBuffer(10, 20, 30))
    val r1: GetResult = get(q)
    q = r1.queue
    println(r1.value)
    q = put(q, 40)
    val r2: GetResult = get(q)
    q = r2.queue
    println(r2.value)
    val r3: GetResult = get(q)
    q = r3.queue
    println(r3.value)
    println(len_queue(q))
    val r4: GetResult = get(q)
    q = r4.queue
    println(r4.value)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
