// Generated by Mochi v0.10.59 on 2025-08-07 00:50:15 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class FixedPriorityQueue(var queues: ArrayBuffer[ArrayBuffer[BigInt]])

case class FPQDequeueResult(var queue: FixedPriorityQueue, var value: BigInt)

case class ElementPriorityQueue(var queue: ArrayBuffer[BigInt])

case class EPQDequeueResult(var queue: ElementPriorityQueue, var value: BigInt)

def panic(msg: String): Any = {
  println(msg)
}

def fpq_new(): FixedPriorityQueue = {
  return FixedPriorityQueue(ArrayBuffer(ArrayBuffer(), ArrayBuffer(), ArrayBuffer()))
}

def fpq_enqueue(fpq: FixedPriorityQueue, priority: BigInt, data: BigInt): FixedPriorityQueue = {
  if ((priority < 0 || priority >= BigInt((fpq.asInstanceOf[FixedPriorityQueue].queues).size)).asInstanceOf[Boolean]) {
    throw new RuntimeException(String.valueOf("Valid priorities are 0, 1, and 2"))
    return fpq
  }
  if (BigInt((fpq.asInstanceOf[FixedPriorityQueue].queues((priority.toInt).toInt)).size) >= 100) {
    throw new RuntimeException(String.valueOf("Maximum queue size is 100"))
    return fpq
  }
  var qs: ArrayBuffer[ArrayBuffer[BigInt]] = fpq.asInstanceOf[FixedPriorityQueue].queues
  qs((priority).toInt) = qs((priority.toInt).toInt) :+ data
  fpq.asInstanceOf[FixedPriorityQueue].queues = qs
  return fpq
}

def fpq_dequeue(fpq: FixedPriorityQueue): FPQDequeueResult = {
  var qs: ArrayBuffer[ArrayBuffer[BigInt]] = fpq.asInstanceOf[FixedPriorityQueue].queues
  var i: BigInt = 0
  while (i < BigInt((qs).size)) {
    val q: ArrayBuffer[BigInt] = qs((i.toInt).toInt)
    if (BigInt((q).size) > 0) {
      val `val`: BigInt = q((0.toInt).toInt)
      var new_q: ArrayBuffer[BigInt] = ArrayBuffer()
      var j: BigInt = 1
      while (j < BigInt((q).size)) {
        new_q = new_q :+ q((j.toInt).toInt)
        j = j + 1
      }
      qs((i).toInt) = new_q
      fpq.asInstanceOf[FixedPriorityQueue].queues = qs
      return FPQDequeueResult(fpq, `val`)
    }
    i = i + 1
  }
  throw new RuntimeException(String.valueOf("All queues are empty"))
  return FPQDequeueResult(fpq, 0)
}

def fpq_to_string(fpq: FixedPriorityQueue): String = {
  var lines: ArrayBuffer[String] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((fpq.asInstanceOf[FixedPriorityQueue].queues).size)) {
    var q_str: String = "["
    var q: ArrayBuffer[BigInt] = fpq.asInstanceOf[FixedPriorityQueue].queues((i.toInt).toInt)
    var j: BigInt = 0
    while (j < BigInt((q).size)) {
      if (j > 0) {
        q_str = q_str + ", "
      }
      q_str = q_str + String.valueOf(q((j.toInt).toInt))
      j = j + 1
    }
    q_str = q_str + "]"
    lines = lines :+ "Priority " + String.valueOf(i) + ": " + q_str
    i = i + 1
  }
  var res: String = ""
  i = 0
  while (i < BigInt((lines).size)) {
    if (i > 0) {
      res = res + "\n"
    }
    res = res + lines((i.toInt).toInt)
    i = i + 1
  }
  return res
}

def epq_new(): ElementPriorityQueue = {
  return ElementPriorityQueue(ArrayBuffer())
}

def epq_enqueue(epq: ElementPriorityQueue, data: BigInt): ElementPriorityQueue = {
  if (BigInt((epq.queue).size) >= 100) {
    throw new RuntimeException(String.valueOf("Maximum queue size is 100"))
    return epq
  }
  epq.queue = epq.queue :+ data
  return epq
}

def epq_dequeue(epq: ElementPriorityQueue): EPQDequeueResult = {
  if (BigInt((epq.queue).size) == 0) {
    throw new RuntimeException(String.valueOf("The queue is empty"))
    return EPQDequeueResult(epq, 0)
  }
  var min_val: BigInt = epq.queue((0.toInt).toInt)
  var idx: BigInt = 0
  var i: BigInt = 1
  while (i < BigInt((epq.queue).size)) {
    val v: BigInt = epq.queue((i.toInt).toInt)
    if (v < min_val) {
      min_val = v
      idx = i
    }
    i = i + 1
  }
  var new_q: ArrayBuffer[BigInt] = ArrayBuffer()
  i = 0
  while (i < BigInt((epq.queue).size)) {
    if (i != idx) {
      new_q = new_q :+ epq.queue((i.toInt).toInt)
    }
    i = i + 1
  }
  epq.queue = new_q
  return EPQDequeueResult(epq, min_val)
}

def epq_to_string(epq: ElementPriorityQueue): String = {
  return String.valueOf(epq.queue)
}

def fixed_priority_queue(): Any = {
  var fpq: FixedPriorityQueue = fpq_new()
  fpq = fpq_enqueue(fpq, 0, 10)
  fpq = fpq_enqueue(fpq, 1, 70)
  fpq = fpq_enqueue(fpq, 0, 100)
  fpq = fpq_enqueue(fpq, 2, 1)
  fpq = fpq_enqueue(fpq, 2, 5)
  fpq = fpq_enqueue(fpq, 1, 7)
  fpq = fpq_enqueue(fpq, 2, 4)
  fpq = fpq_enqueue(fpq, 1, 64)
  fpq = fpq_enqueue(fpq, 0, 128)
  println(fpq_to_string(fpq))
  var res: FPQDequeueResult = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  println(fpq_to_string(fpq))
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
  res = fpq_dequeue(fpq)
  fpq = res.queue
  println(res.value)
}

def element_priority_queue(): Any = {
  var epq: ElementPriorityQueue = epq_new()
  epq = epq_enqueue(epq, 10)
  epq = epq_enqueue(epq, 70)
  epq = epq_enqueue(epq, 100)
  epq = epq_enqueue(epq, 1)
  epq = epq_enqueue(epq, 5)
  epq = epq_enqueue(epq, 7)
  epq = epq_enqueue(epq, 4)
  epq = epq_enqueue(epq, 64)
  epq = epq_enqueue(epq, 128)
  println(epq_to_string(epq))
  var res: EPQDequeueResult = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  println(epq_to_string(epq))
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
  res = epq_dequeue(epq)
  epq = res.queue
  println(res.value)
}

def main(): Any = {
  fixed_priority_queue()
  element_priority_queue()
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
