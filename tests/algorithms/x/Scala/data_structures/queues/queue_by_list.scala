// Generated by Mochi v0.10.59 on 2025-08-07 00:50:23 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class Queue(var entries: ArrayBuffer[BigInt])

case class GetResult(var queue: Queue, var value: BigInt)

def new_queue(items: ArrayBuffer[BigInt]): Queue = {
  return Queue(items)
}

def len_queue(q: Queue): BigInt = {
  return BigInt((q.entries).size)
}

def str_queue(q: Queue): String = {
  var s: String = "Queue(("
  var i: BigInt = 0
  while (i < BigInt((q.entries).size)) {
    s = s + String.valueOf(q.entries((i.toInt).toInt))
    if (i < BigInt((q.entries).size) - 1) {
      s = s + ", "
    }
    i = i + 1
  }
  s = s + "))"
  return s
}

def put(q: Queue, item: BigInt): Queue = {
  var e: ArrayBuffer[BigInt] = q.entries
  e = e :+ item
  return Queue(e)
}

def get(q: Queue): GetResult = {
  if (BigInt((q.entries).size) == 0) {
    throw new RuntimeException(String.valueOf("Queue is empty"))
  }
  val value: BigInt = q.entries((0.toInt).toInt)
  var new_entries: ArrayBuffer[BigInt] = ArrayBuffer()
  var i: BigInt = 1
  while (i < BigInt((q.entries).size)) {
    new_entries = new_entries :+ q.entries((i.toInt).toInt)
    i = i + 1
  }
  return GetResult(Queue(new_entries), value)
}

def rotate(q: Queue, rotation: BigInt): Queue = {
  var e: ArrayBuffer[BigInt] = q.entries
  var r: BigInt = 0
  while (r < rotation) {
    if (BigInt((e).size) > 0) {
      val first: BigInt = e((0.toInt).toInt)
      var rest: ArrayBuffer[BigInt] = ArrayBuffer()
      var i: BigInt = 1
      while (i < BigInt((e).size)) {
        rest = rest :+ e((i.toInt).toInt)
        i = i + 1
      }
      rest = rest :+ first
      e = rest
    }
    r = r + 1
  }
  return Queue(e)
}

def get_front(q: Queue): BigInt = {
  return q.entries((0.toInt).toInt)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var q: Queue = new_queue(ArrayBuffer())
    println(len_queue(q))
    q = put(q, 10)
    q = put(q, 20)
    q = put(q, 30)
    q = put(q, 40)
    println(str_queue(q))
    val res: GetResult = get(q)
    q = res.queue
    println(res.value)
    println(str_queue(q))
    q = rotate(q, 2)
    println(str_queue(q))
    val front: BigInt = get_front(q)
    println(front)
    println(str_queue(q))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
