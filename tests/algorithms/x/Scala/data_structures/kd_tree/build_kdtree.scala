// Generated by Mochi v0.10.59 on 2025-08-07 00:46:59 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class KDNode(var point: ArrayBuffer[Double], var left: BigInt, var right: BigInt)

def sort_points(points: ArrayBuffer[ArrayBuffer[Double]], axis: BigInt): ArrayBuffer[ArrayBuffer[Double]] = {
  var arr: ArrayBuffer[ArrayBuffer[Double]] = points
  var i: BigInt = 0
  while (i < BigInt((arr).size)) {
    var j: BigInt = 0
    while (j < BigInt((arr).size) - 1) {
      if (arr((j.toInt).toInt)((axis.toInt).toInt) > arr(((j + 1).toInt).toInt)((axis.toInt).toInt)) {
        val tmp: ArrayBuffer[Double] = arr((j.toInt).toInt)
        arr((j).toInt) = arr(((j + 1).toInt).toInt)
        arr((j + 1).toInt) = tmp
      }
      j = j + 1
    }
    i = i + 1
  }
  return arr
}

def build_kdtree(points: ArrayBuffer[ArrayBuffer[Double]], depth: BigInt): BigInt = {
  if (BigInt((points).size) == 0) {
    return 0 - 1
  }
  val k: BigInt = BigInt((points((0.toInt).toInt)).size)
  val axis: BigInt = depth % k
  val sorted: ArrayBuffer[ArrayBuffer[Double]] = sort_points(points, axis)
  val median_idx: BigInt = BigInt((sorted).size) / 2
  val left_points: ArrayBuffer[ArrayBuffer[Double]] = sorted.slice((0).toInt, (median_idx).toInt)
  val right_points: ArrayBuffer[ArrayBuffer[Double]] = sorted.slice((median_idx + 1).toInt, (BigInt((sorted).size)).toInt)
  var idx: BigInt = BigInt((tree).size)
  tree = tree :+ KDNode(sorted((median_idx.toInt).toInt), 0 - 1, 0 - 1)
  val left_idx: BigInt = build_kdtree(left_points, depth + 1)
  val right_idx: BigInt = build_kdtree(right_points, depth + 1)
  var node: KDNode = tree((idx.toInt).toInt)
  node.asInstanceOf[KDNode].left = left_idx
  node.asInstanceOf[KDNode].right = right_idx
  tree((idx).toInt) = node
  return idx
}

var tree: ArrayBuffer[KDNode] = ArrayBuffer()

val pts: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(2.0, 3.0), ArrayBuffer(5.0, 4.0), ArrayBuffer(9.0, 6.0), ArrayBuffer(4.0, 7.0), ArrayBuffer(8.0, 1.0), ArrayBuffer(7.0, 2.0))

val root: BigInt = build_kdtree(pts, 0)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(tree))
    println(root)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
