// Generated by Mochi v0.10.59 on 2025-08-07 00:53:03 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class Node(var children: scala.collection.mutable.Map[String,BigInt], var is_leaf: Boolean)

case class Trie(var nodes: ArrayBuffer[Node])

def new_trie(): Trie = {
  return Trie(ArrayBuffer(Node(scala.collection.mutable.Map(), false)))
}

def remove_key(m: scala.collection.mutable.Map[String,BigInt], k: String): scala.collection.mutable.Map[String,BigInt] = {
  var out: scala.collection.mutable.Map[String,BigInt] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,BigInt]]
  for (key <- m.keys) {
    if (key != k) {
      out.update(key, m.getOrElse(key.toString, null).asInstanceOf[BigInt])
    }
  }
  return out
}

def insert(trie: Trie, word: String): Any = {
  var nodes: ArrayBuffer[Node] = trie.nodes
  var curr: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((word).size)) {
    val ch = word.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    var child_idx: BigInt = -1
    val children: scala.collection.mutable.Map[String,BigInt] = nodes((curr.toInt).toInt).children
    if ((children.contains(ch)).asInstanceOf[Boolean]) {
      child_idx = children.getOrElse(ch.toString, null).asInstanceOf[BigInt]
    } else {
      val new_node: Node = Node(scala.collection.mutable.Map(), false)
      nodes = nodes :+ new_node
      child_idx = BigInt((nodes).size) - 1
      var new_children: scala.collection.mutable.Map[String,BigInt] = children
      new_children.update(ch, child_idx)
      var node: Node = nodes((curr.toInt).toInt)
      node.asInstanceOf[Node].children = new_children
      nodes((curr).toInt) = node
    }
    curr = child_idx
    i = i + 1
  }
  var node: Node = nodes((curr.toInt).toInt)
  node.asInstanceOf[Node].is_leaf = true
  nodes((curr).toInt) = node
  trie.nodes = nodes
}

def insert_many(trie: Trie, words: ArrayBuffer[String]): Any = {
  for (w <- words) {
    insert(trie, w)
  }
}

def find(trie: Trie, word: String): Boolean = {
  val nodes: ArrayBuffer[Node] = trie.nodes
  var curr: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((word).size)) {
    val ch = word.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    val children: scala.collection.mutable.Map[String,BigInt] = nodes((curr.toInt).toInt).children
    if (!(children.contains(ch)).asInstanceOf[Boolean]) {
      return false
    }
    curr = children.getOrElse(ch.toString, null).asInstanceOf[BigInt]
    i = i + 1
  }
  val node: Node = nodes((curr.toInt).toInt)
  return node.asInstanceOf[Node].is_leaf
}

def delete(trie: Trie, word: String): Any = {
  var nodes: ArrayBuffer[Node] = trie.nodes
  def _delete(idx: BigInt, pos: BigInt): Boolean = {
    if (pos == BigInt((word).size)) {
      var node: Node = nodes((idx.toInt).toInt)
      if (node.asInstanceOf[Node].is_leaf == false) {
        return false
      }
      node.asInstanceOf[Node].is_leaf = false
      nodes((idx).toInt) = node
      return BigInt((node.asInstanceOf[Node].children).size) == 0
    }
    var node: Node = nodes((idx.toInt).toInt)
    val children: scala.collection.mutable.Map[String,BigInt] = node.asInstanceOf[Node].children
    val ch = word.slice((pos.toInt).toInt, (pos.toInt).toInt + 1)
    if (!(children.contains(ch)).asInstanceOf[Boolean]) {
      return false
    }
    val child_idx: BigInt = children.getOrElse(ch.toString, null).asInstanceOf[BigInt]
    val should_delete = _delete(child_idx, pos + 1)
    node = nodes((idx.toInt).toInt)
    if (should_delete.asInstanceOf[Boolean]) {
      var new_children: scala.collection.mutable.Map[String,BigInt] = remove_key(node.asInstanceOf[Node].children, ch)
      node.asInstanceOf[Node].children = new_children
      nodes((idx).toInt) = node
      return (BigInt((new_children).size) == 0 && node.asInstanceOf[Node].is_leaf == false).asInstanceOf[Boolean]
    }
    nodes((idx).toInt) = node
    return false
  }
  _delete(0, 0)
  trie.nodes = nodes
  return null
}

def print_words(trie: Trie): Any = {
  def dfs(idx: BigInt, word: String): Any = {
    val node: Node = trie.nodes((idx.toInt).toInt)
    if (node.asInstanceOf[Node].is_leaf) {
      println(word)
    }
    for (key <- node.asInstanceOf[Node].children.keys) {
      dfs(node.asInstanceOf[Node].children.getOrElse(key.toString, null).asInstanceOf[BigInt], word + key)
    }
  }
  dfs(0, "")
}

def test_trie(): Boolean = {
  val words: ArrayBuffer[String] = ArrayBuffer("banana", "bananas", "bandana", "band", "apple", "all", "beast")
  val trie: Trie = new_trie()
  insert_many(trie, words)
  var ok: Boolean = true
  for (w <- words) {
    ok = (ok && find(trie, w)).asInstanceOf[Boolean]
  }
  ok = (ok && find(trie, "banana")).asInstanceOf[Boolean]
  var t: Boolean = find(trie, "bandanas")
  ok = (ok && t == false).asInstanceOf[Boolean]
  var t2: Boolean = find(trie, "apps")
  ok = (ok && t2 == false).asInstanceOf[Boolean]
  ok = (ok && find(trie, "apple")).asInstanceOf[Boolean]
  ok = (ok && find(trie, "all")).asInstanceOf[Boolean]
  delete(trie, "all")
  var t3: Boolean = find(trie, "all")
  ok = (ok && t3 == false).asInstanceOf[Boolean]
  delete(trie, "banana")
  var t4: Boolean = find(trie, "banana")
  ok = (ok && t4 == false).asInstanceOf[Boolean]
  ok = (ok && find(trie, "bananas")).asInstanceOf[Boolean]
  return ok
}

def print_results(msg: String, passes: Boolean): Any = {
  if (passes) {
    println(msg + " works!")
  } else {
    println(msg + " doesn't work :(")
  }
}

val trie: Trie = new_trie()

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    print_results("Testing trie functionality", test_trie())
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
