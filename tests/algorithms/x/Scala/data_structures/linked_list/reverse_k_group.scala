// Generated by Mochi v0.10.59 on 2025-08-07 00:49:06 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class LinkedList(var data: ArrayBuffer[BigInt])

def to_string(list: LinkedList): String = {
  if (BigInt((list.asInstanceOf[LinkedList].data).size) == 0) {
    return ""
  }
  var s: String = String.valueOf(list.asInstanceOf[LinkedList].data((0.toInt).toInt))
  var i: BigInt = 1
  while (i < BigInt((list.asInstanceOf[LinkedList].data).size)) {
    s = s + " -> " + String.valueOf(list.asInstanceOf[LinkedList].data((i.toInt).toInt))
    i = i + 1
  }
  return s
}

def reverse_k_nodes(list: LinkedList, k: BigInt): LinkedList = {
  if (k <= 1) {
    return list
  }
  var res: ArrayBuffer[BigInt] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((list.asInstanceOf[LinkedList].data).size)) {
    var j: BigInt = 0
    var group: ArrayBuffer[BigInt] = ArrayBuffer()
    while (j < k && i + j < BigInt((list.asInstanceOf[LinkedList].data).size)) {
      group = group :+ list.asInstanceOf[LinkedList].data(((i + j).toInt).toInt)
      j = j + 1
    }
    if (BigInt((group).size) == k) {
      var g: BigInt = k - 1
      while (g >= 0) {
        res = res :+ group((g.toInt).toInt)
        g = g - 1
      }
    } else {
      var g: BigInt = 0
      while (g < BigInt((group).size)) {
        res = res :+ group((g.toInt).toInt)
        g = g + 1
      }
    }
    i = i + k
  }
  return LinkedList(res)
}

def main(): Any = {
  var ll: LinkedList = LinkedList(ArrayBuffer(1, 2, 3, 4, 5))
  println("Original Linked List: " + to_string(ll))
  var k: BigInt = 2
  ll = reverse_k_nodes(ll, k)
  println("After reversing groups of size " + String.valueOf(k) + ": " + to_string(ll))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
