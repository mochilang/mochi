// Generated by Mochi v0.10.59 on 2025-08-07 00:55:55 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def permute(k: BigInt, arr: ArrayBuffer[BigInt], _res: ArrayBuffer[ArrayBuffer[BigInt]]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var res: ArrayBuffer[ArrayBuffer[BigInt]] = _res
  if (k == 1) {
    val copy: ArrayBuffer[BigInt] = arr.slice((0).toInt, (BigInt((arr).size)).toInt)
    return res :+ copy
  }
  res = permute(k - 1, arr, res)
  var i: BigInt = 0
  while (i < k - 1) {
    if (k % 2 == 0) {
      val temp: BigInt = arr((i.toInt).toInt)
      arr((i).toInt) = arr(((k - 1).toInt).toInt)
      arr((k - 1).toInt) = temp
    } else {
      val temp: BigInt = arr((0.toInt).toInt)
      arr((0).toInt) = arr(((k - 1).toInt).toInt)
      arr((k - 1).toInt) = temp
    }
    res = permute(k - 1, arr, res)
    i = i + 1
  }
  return res
}

def heaps(arr: ArrayBuffer[BigInt]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  if (BigInt((arr).size) <= 1) {
    return ArrayBuffer(arr.slice((0).toInt, (BigInt((arr).size)).toInt))
  }
  var res: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
  res = permute(BigInt((arr).size), arr, res)
  return res
}

def main(): Any = {
  val perms: ArrayBuffer[ArrayBuffer[BigInt]] = heaps(ArrayBuffer(1, 2, 3))
  println(perms)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
