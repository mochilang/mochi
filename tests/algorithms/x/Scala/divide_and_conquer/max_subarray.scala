// Generated by Mochi v0.10.59 on 2025-08-07 00:56:30 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class Result(var start: BigInt, var end: BigInt, var sum: Double)

def max_cross_sum(arr: ArrayBuffer[Double], low: BigInt, mid: BigInt, high: BigInt): Result = {
  var left_sum: Double = -1000000000000000000.0
  var max_left: BigInt = -1
  var sum: Double = 0.0
  var i: BigInt = mid
  while (i >= low) {
    sum = sum + arr((i.toInt).toInt)
    if (sum > left_sum) {
      left_sum = sum
      max_left = i
    }
    i = i - 1
  }
  var right_sum: Double = -1000000000000000000.0
  var max_right: BigInt = -1
  sum = 0.0
  i = mid + 1
  while (i <= high) {
    sum = sum + arr((i.toInt).toInt)
    if (sum > right_sum) {
      right_sum = sum
      max_right = i
    }
    i = i + 1
  }
  return Result(max_left, max_right, left_sum + right_sum)
}

def max_subarray(arr: ArrayBuffer[Double], low: BigInt, high: BigInt): Result = {
  if (BigInt((arr).size) == 0) {
    return Result(-1, -1, 0.0)
  }
  if (low == high) {
    return Result(low, high, arr((low.toInt).toInt))
  }
  val mid: BigInt = (low + high) / 2
  val left: Result = max_subarray(arr, low, mid)
  val right: Result = max_subarray(arr, mid + 1, high)
  val cross: Result = max_cross_sum(arr, low, mid, high)
  if ((left.asInstanceOf[Result].sum >= right.asInstanceOf[Result].sum && left.asInstanceOf[Result].sum >= cross.asInstanceOf[Result].sum).asInstanceOf[Boolean]) {
    return left
  }
  if ((right.asInstanceOf[Result].sum >= left.asInstanceOf[Result].sum && right.asInstanceOf[Result].sum >= cross.asInstanceOf[Result].sum).asInstanceOf[Boolean]) {
    return right
  }
  return cross
}

def show(res: Result): Any = {
  println("[" + String.valueOf(res.asInstanceOf[Result].start) + ", " + String.valueOf(res.asInstanceOf[Result].end) + ", " + String.valueOf(res.asInstanceOf[Result].sum) + "]")
}

def main(): Any = {
  val nums1: ArrayBuffer[Double] = ArrayBuffer(-2.0, 1.0, -3.0, 4.0, -1.0, 2.0, 1.0, -5.0, 4.0)
  val res1: Result = max_subarray(nums1, 0, BigInt((nums1).size) - 1)
  show(res1)
  val nums2: ArrayBuffer[Double] = ArrayBuffer(2.0, 8.0, 9.0)
  val res2: Result = max_subarray(nums2, 0, BigInt((nums2).size) - 1)
  show(res2)
  val nums3: ArrayBuffer[Double] = ArrayBuffer(0.0, 0.0)
  val res3: Result = max_subarray(nums3, 0, BigInt((nums3).size) - 1)
  show(res3)
  val nums4: ArrayBuffer[Double] = ArrayBuffer(-1.0, 0.0, 1.0)
  val res4: Result = max_subarray(nums4, 0, BigInt((nums4).size) - 1)
  show(res4)
  val nums5: ArrayBuffer[Double] = ArrayBuffer(-2.0, -3.0, -1.0, -4.0, -6.0)
  val res5: Result = max_subarray(nums5, 0, BigInt((nums5).size) - 1)
  show(res5)
  val nums6: ArrayBuffer[Double] = ArrayBuffer()
  val res6: Result = max_subarray(nums6, 0, 0)
  show(res6)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
