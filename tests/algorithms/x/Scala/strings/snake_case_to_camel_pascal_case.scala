// Generated by Mochi v0.10.63 on 2025-08-11 15:39:42 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def split(s: String, sep: String): ArrayBuffer[String] = {
  var res: ArrayBuffer[String] = ArrayBuffer[String]()
  var current: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((s).size)) {
    val ch: String = s.slice((i).toInt, (i + BigInt(1)).toInt)
    if (ch == sep) {
      res = res :+ (current)
      current = ""
    } else {
      current = current + ch
    }
    i = i + BigInt(1)
  }
  res = res :+ (current)
  return res
}

def capitalize(word: String): String = {
  if (BigInt((word).size) == BigInt(0)) {
    return ""
  }
  val first = word.slice((BigInt(0)).toInt, (BigInt(1)).toInt).toUpperCase()
  val rest: String = word.slice((BigInt(1)).toInt, (BigInt((word).size)).toInt)
  return first + rest
}

def snake_to_camel_case(input_str: String, use_pascal: Boolean): String = {
  val words = ArrayBuffer(input_str.split("_"): _*)
  var result: String = ""
  var index: BigInt = BigInt(0)
  if (!use_pascal) {
    if (BigInt((words).size) > BigInt(0)) {
      result = words.lift((BigInt(0).toInt).toInt).getOrElse("")
      index = BigInt(1)
    }
  }
  while (index < BigInt((words).size)) {
    val word: String = words.lift((index.toInt).toInt).getOrElse("")
    result = result + capitalize(word)
    index = index + BigInt(1)
  }
  return result
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(snake_to_camel_case("some_random_string", false))
    println(snake_to_camel_case("some_random_string", true))
    println(snake_to_camel_case("some_random_string_with_numbers_123", false))
    println(snake_to_camel_case("some_random_string_with_numbers_123", true))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
