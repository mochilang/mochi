// Generated by Mochi v0.10.63 on 2025-08-11 15:39:31 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def split(s: String, sep: String): ArrayBuffer[String] = {
  var res: ArrayBuffer[String] = ArrayBuffer[String]()
  var current: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((s).size)) {
    val ch: String = s.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    if (ch == sep) {
      res = res :+ (current)
      current = ""
    } else {
      current = current + ch
    }
    i = i + BigInt(1)
  }
  res = res :+ (current)
  return res
}

def join_with_space(xs: ArrayBuffer[String]): String = {
  var s: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size)) {
    s = s + xs.lift((i.toInt).toInt).getOrElse("")
    if (i + BigInt(1) < BigInt((xs).size)) {
      s = s + " "
    }
    i = i + BigInt(1)
  }
  return s
}

def reverse_str(s: String): String = {
  var res: String = ""
  var i: BigInt = BigInt((s).size) - BigInt(1)
  while (i >= BigInt(0)) {
    res = res + s.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    i = i - BigInt(1)
  }
  return res
}

def reverse_letters(sentence: String, length: BigInt): String = {
  val words = ArrayBuffer(sentence.split(" "): _*)
  var result: ArrayBuffer[String] = ArrayBuffer[String]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((words).size)) {
    val word: String = words.lift((i.toInt).toInt).getOrElse("")
    if (BigInt((word).size) > length) {
      result = result :+ (reverse_str(word))
    } else {
      result = result :+ (word)
    }
    i = i + BigInt(1)
  }
  return join_with_space(result)
}

def test_reverse_letters(): Any = {
  if (reverse_letters("Hey wollef sroirraw", BigInt(3)) != "Hey fellow warriors") {
    throw new RuntimeException(String.valueOf("test1 failed"))
  }
  if (reverse_letters("nohtyP is nohtyP", BigInt(2)) != "Python is Python") {
    throw new RuntimeException(String.valueOf("test2 failed"))
  }
  if (reverse_letters("1 12 123 1234 54321 654321", BigInt(0)) != "1 21 321 4321 12345 123456") {
    throw new RuntimeException(String.valueOf("test3 failed"))
  }
  if (reverse_letters("racecar", BigInt(0)) != "racecar") {
    throw new RuntimeException(String.valueOf("test4 failed"))
  }
}

def main(): Any = {
  test_reverse_letters()
  println(reverse_letters("Hey wollef sroirraw", BigInt(3)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
