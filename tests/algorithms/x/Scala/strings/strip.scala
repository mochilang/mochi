// Generated by Mochi v0.10.63 on 2025-08-11 15:39:58 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def contains(chars: String, ch: String): Boolean = {
  var i: BigInt = BigInt(0)
  while (i < BigInt((chars).size)) {
    if (chars.slice((i.toInt).toInt, (i.toInt).toInt + 1) == ch) {
      return true
    }
    i = i + BigInt(1)
  }
  return false
}

def substring(s: String, start: BigInt, end: BigInt): String = {
  var res: String = ""
  var i: BigInt = start
  while (i < end) {
    res = res + s.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    i = i + BigInt(1)
  }
  return res
}

def strip_chars(user_string: String, characters: String): String = {
  var start: BigInt = BigInt(0)
  var end: BigInt = BigInt((user_string).size)
  while (start < end && (characters.contains(user_string.slice((start.toInt).toInt, (start.toInt).toInt + 1))).asInstanceOf[Boolean]) {
    start = start + BigInt(1)
  }
  while (end > start && (characters.contains(user_string.slice(((end - BigInt(1)).toInt).toInt, ((end - BigInt(1)).toInt).toInt + 1))).asInstanceOf[Boolean]) {
    end = end - BigInt(1)
  }
  return user_string.slice((start).toInt, (end).toInt)
}

def strip(user_string: String): String = {
  return strip_chars(user_string, " \t\n\r")
}

def test_strip(): Any = {
  if (strip("   hello   ") != "hello") {
    throw new RuntimeException(String.valueOf("test1 failed"))
  }
  if (strip_chars("...world...", ".") != "world") {
    throw new RuntimeException(String.valueOf("test2 failed"))
  }
  if (strip_chars("123hello123", "123") != "hello") {
    throw new RuntimeException(String.valueOf("test3 failed"))
  }
  if (strip("") != "") {
    throw new RuntimeException(String.valueOf("test4 failed"))
  }
}

def main(): Any = {
  test_strip()
  println(strip("   hello   "))
  println(strip_chars("...world...", "."))
  println(strip_chars("123hello123", "123"))
  println(strip(""))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
