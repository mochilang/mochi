// Generated by Mochi v0.10.59 on 2025-08-07 00:53:49 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def abs(x: Double): Double = {
  if (x < 0.0) {
    return -x
  }
  return x
}

def sqrtApprox(x: Double): Double = {
  if (x <= 0.0) {
    return 0.0
  }
  var guess: Double = x
  var i: BigInt = 0
  while (i < 10) {
    guess = (guess + x / guess) / 2.0
    i = i + 1
  }
  return guess
}

def expApprox(x: Double): Double = {
  var term: Double = 1.0
  var sum: Double = 1.0
  var n: BigInt = 1
  while (n < 10) {
    term = term * x / n.toString.toDouble
    sum = sum + term
    n = n + 1
  }
  return sum
}

def vec_gaussian(mat: ArrayBuffer[ArrayBuffer[Double]], variance: Double): ArrayBuffer[ArrayBuffer[Double]] = {
  var i: BigInt = 0
  var out: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  while (i < BigInt((mat).size)) {
    var row: ArrayBuffer[Double] = ArrayBuffer()
    var j: BigInt = 0
    while (j < BigInt((mat((i.toInt).toInt)).size)) {
      val v: Double = mat((i.toInt).toInt)((j.toInt).toInt)
      val e: Double = -(v * v) / (2.0 * variance)
      row = (row ++ ArrayBuffer(expApprox(e))).asInstanceOf[ArrayBuffer[Double]]
      j = j + 1
    }
    out = (out ++ ArrayBuffer(row)).asInstanceOf[ArrayBuffer[ArrayBuffer[Double]]]
    i = i + 1
  }
  return out
}

def get_slice(img: ArrayBuffer[ArrayBuffer[Double]], x: BigInt, y: BigInt, kernel_size: BigInt): ArrayBuffer[ArrayBuffer[Double]] = {
  val half: BigInt = kernel_size / 2
  var i: BigInt = x - half
  var slice: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  while (i <= x + half) {
    var row: ArrayBuffer[Double] = ArrayBuffer()
    var j: BigInt = y - half
    while (j <= y + half) {
      row = (row ++ ArrayBuffer(img((i.toInt).toInt)((j.toInt).toInt))).asInstanceOf[ArrayBuffer[Double]]
      j = j + 1
    }
    slice = (slice ++ ArrayBuffer(row)).asInstanceOf[ArrayBuffer[ArrayBuffer[Double]]]
    i = i + 1
  }
  return slice
}

def get_gauss_kernel(kernel_size: BigInt, spatial_variance: Double): ArrayBuffer[ArrayBuffer[Double]] = {
  var arr: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < kernel_size) {
    var row: ArrayBuffer[Double] = ArrayBuffer()
    var j: BigInt = 0
    while (j < kernel_size) {
      val di: Double = (i - kernel_size / 2).toString.toDouble
      val dj: Double = (j - kernel_size / 2).toString.toDouble
      val dist: Double = sqrtApprox(di * di + dj * dj)
      row = (row ++ ArrayBuffer(dist)).asInstanceOf[ArrayBuffer[Double]]
      j = j + 1
    }
    arr = (arr ++ ArrayBuffer(row)).asInstanceOf[ArrayBuffer[ArrayBuffer[Double]]]
    i = i + 1
  }
  return vec_gaussian(arr, spatial_variance)
}

def elementwise_sub(mat: ArrayBuffer[ArrayBuffer[Double]], value: Double): ArrayBuffer[ArrayBuffer[Double]] = {
  var res: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((mat).size)) {
    var row: ArrayBuffer[Double] = ArrayBuffer()
    var j: BigInt = 0
    while (j < BigInt((mat((i.toInt).toInt)).size)) {
      row = (row ++ ArrayBuffer(mat((i.toInt).toInt)((j.toInt).toInt) - value)).asInstanceOf[ArrayBuffer[Double]]
      j = j + 1
    }
    res = (res ++ ArrayBuffer(row)).asInstanceOf[ArrayBuffer[ArrayBuffer[Double]]]
    i = i + 1
  }
  return res
}

def elementwise_mul(a: ArrayBuffer[ArrayBuffer[Double]], b: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  var res: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((a).size)) {
    var row: ArrayBuffer[Double] = ArrayBuffer()
    var j: BigInt = 0
    while (j < BigInt((a((i.toInt).toInt)).size)) {
      row = (row ++ ArrayBuffer(a((i.toInt).toInt)((j.toInt).toInt) * b((i.toInt).toInt)((j.toInt).toInt))).asInstanceOf[ArrayBuffer[Double]]
      j = j + 1
    }
    res = (res ++ ArrayBuffer(row)).asInstanceOf[ArrayBuffer[ArrayBuffer[Double]]]
    i = i + 1
  }
  return res
}

def matrix_sum(mat: ArrayBuffer[ArrayBuffer[Double]]): Double = {
  var total: Double = 0.0
  var i: BigInt = 0
  while (i < BigInt((mat).size)) {
    var j: BigInt = 0
    while (j < BigInt((mat((i.toInt).toInt)).size)) {
      total = total + mat((i.toInt).toInt)((j.toInt).toInt)
      j = j + 1
    }
    i = i + 1
  }
  return total
}

def bilateral_filter(img: ArrayBuffer[ArrayBuffer[Double]], spatial_variance: Double, intensity_variance: Double, kernel_size: BigInt): Double = {
  val gauss_ker: ArrayBuffer[ArrayBuffer[Double]] = get_gauss_kernel(kernel_size, spatial_variance)
  val img_s: ArrayBuffer[ArrayBuffer[Double]] = img
  val center: Double = img_s(((kernel_size / 2).toInt).toInt)(((kernel_size / 2).toInt).toInt)
  val img_i: ArrayBuffer[ArrayBuffer[Double]] = elementwise_sub(img_s, center)
  val img_ig: ArrayBuffer[ArrayBuffer[Double]] = vec_gaussian(img_i, intensity_variance)
  val weights: ArrayBuffer[ArrayBuffer[Double]] = elementwise_mul(gauss_ker, img_ig)
  val vals: ArrayBuffer[ArrayBuffer[Double]] = elementwise_mul(img_s, weights)
  val sum_weights: Double = matrix_sum(weights)
  var `val`: Double = 0.0
  if (sum_weights != 0.0) {
    `val` = matrix_sum(vals) / sum_weights
  }
  return `val`
}

val PI: Double = 3.141592653589793

val img: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.2, 0.3, 0.4), ArrayBuffer(0.3, 0.4, 0.5), ArrayBuffer(0.4, 0.5, 0.6))

val result: Double = bilateral_filter(img, 1.0, 1.0, 3)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(result)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
