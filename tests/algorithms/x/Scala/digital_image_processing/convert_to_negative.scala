// Generated by Mochi v0.10.59 on 2025-08-07 00:53:26 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def convert_to_negative(img: ArrayBuffer[ArrayBuffer[ArrayBuffer[BigInt]]]): ArrayBuffer[ArrayBuffer[ArrayBuffer[BigInt]]] = {
  var result: ArrayBuffer[ArrayBuffer[ArrayBuffer[BigInt]]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((img).size)) {
    var row: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
    var j: BigInt = 0
    while (j < BigInt((img((i.toInt).toInt)).size)) {
      val pixel: ArrayBuffer[BigInt] = img((i.toInt).toInt)((j.toInt).toInt)
      val r: BigInt = 255 - pixel((0.toInt).toInt)
      val g: BigInt = 255 - pixel((1.toInt).toInt)
      val b: BigInt = 255 - pixel((2.toInt).toInt)
      row = row :+ ArrayBuffer(BigInt(r.toString.toDouble.toInt), BigInt(g.toString.toDouble.toInt), BigInt(b.toString.toDouble.toInt))
      j = j + 1
    }
    result = result :+ row
    i = i + 1
  }
  return result
}

def main(): Any = {
  val image: ArrayBuffer[ArrayBuffer[ArrayBuffer[BigInt]]] = ArrayBuffer(ArrayBuffer(ArrayBuffer(10, 20, 30), ArrayBuffer(0, 0, 0)), ArrayBuffer(ArrayBuffer(255, 255, 255), ArrayBuffer(100, 150, 200)))
  val neg: ArrayBuffer[ArrayBuffer[ArrayBuffer[BigInt]]] = convert_to_negative(image)
  println(neg)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
