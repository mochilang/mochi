-- Generated by Mochi v0.10.59 on 2025-08-07 08:10 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function enforce_args(n, prices)
    if (n < 0) then
      panic("n must be non-negative")
    end
    if (n > (function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' then
          local l = utf8.len(v)
          if l then return l end
          return #v
        elseif type(v) == 'table' then
            return #v
          else
            return 0
          end
        end)(prices)) then
          panic("price list is shorter than n")
        end
      end
      function bottom_up_cut_rod(n, prices)
        enforce_args(n, prices)
        local max_rev = nil
        local i = 0
        while (i <= n) do
          if (i == 0) then
            max_rev = (function(lst, item)
            local res = {table.unpack(lst or {})}
            table.insert(res, item)
            return res
          end)(max_rev, 0)
        else
          max_rev = (function(lst, item)
          local res = {table.unpack(lst or {})}
          table.insert(res, item)
          return res
        end)(max_rev, (-2147483648))
      end
      i = (i + 1)
    end
    local length = 1
    while (length <= n) do
      local best = max_rev[length + 1]
      local j = 1
      while (j <= length) do
        local candidate = (prices[(j - 1) + 1] + max_rev[(length - j) + 1])
        if (candidate > best) then
          best = candidate
        end
        j = (j + 1)
      end
      max_rev[length + 1] = best
      length = (length + 1)
    end
    return max_rev[n + 1]
  end
  prices = {1, 5, 8, 9, 10, 17, 17, 20, 24, 30}
  print(bottom_up_cut_rod(4, prices))
  print(bottom_up_cut_rod(10, prices))
  local _bench_end = os.clock()
  collectgarbage()
  local _bench_end_mem = collectgarbage('count') * 1024
  local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
  local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
  print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
