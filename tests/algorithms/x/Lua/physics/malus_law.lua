-- Generated by Mochi v0.10.59 on 2025-08-07 11:58 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _panic(msg)
io.stderr:write(tostring(msg))
os.exit(1)
end

local function _str(v)
if type(v) == 'number' then
  local s = tostring(v)
  s = string.gsub(s, '%.0+$', '')
  return s
end
return tostring(v)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function _mod(x, m)
    return (x - (math.floor(tonumber((x / m)) or 0) * m))
  end
  function cos(x)
    local y = (_mod((x + PI), TWO_PI) - PI)
    local y2 = (y * y)
    local y4 = (y2 * y2)
    local y6 = (y4 * y2)
    return (((1 - (y2 / 2)) + (y4 / 24)) - (y6 / 720))
  end
  function radians(deg)
    return ((deg * PI) / 180)
  end
  function abs_val(x)
    if (x < 0) then
      return (-x)
    end
    return x
  end
  function malus_law(initial_intensity, angle)
    if (initial_intensity < 0) then
      _panic("The value of intensity cannot be negative")
    end
    if ((angle < 0) or (angle > 360)) then
      _panic("In Malus Law, the angle is in the range 0-360 degrees")
    end
    local theta = radians(angle)
    local c = cos(theta)
    return (initial_intensity * (c * c))
  end
  function test_malus_law()
    if (abs_val((malus_law(10, 45) - 5)) > 0.01) then
      _panic("malus_law test1 failed")
    end
    if (abs_val((malus_law(100, 60) - 25)) > 0.01) then
      _panic("malus_law test2 failed")
    end
    if (abs_val((malus_law(50, 150) - 37.5)) > 0.01) then
      _panic("malus_law test3 failed")
    end
    if (abs_val((malus_law(75, 270) - 0)) > 0.01) then
      _panic("malus_law test4 failed")
    end
    if (abs_val((malus_law(100, 180) - 100)) > 0.01) then
      _panic("malus_law test5 failed")
    end
    if (abs_val((malus_law(100, 360) - 100)) > 0.01) then
      _panic("malus_law test6 failed")
    end
  end
  function main()
    test_malus_law()
    print((((type(_str(malus_law(100, 60))) == "table")) and (
    (function(v)
    local function encode(x)
    if type(x) == "table" then
      if x.__name and x.__order then
        local parts = {x.__name, " {"}
        for i, k in ipairs(x.__order) do
          if i > 1 then parts[#parts+1] = ", " end
          parts[#parts+1] = k .. " = " .. encode(x[k])
        end
        parts[#parts+1] = "}"
        return table.concat(parts)
      elseif #x > 0 then
          local allTables = true
          for _, v in ipairs(x) do
            if type(v) ~= "table" then allTables = false break end
          end
          local parts = {}
          if not allTables then parts[#parts+1] = "[" end
          for i, val in ipairs(x) do
            parts[#parts+1] = encode(val)
            if i < #x then parts[#parts+1] = " " end
          end
          if not allTables then parts[#parts+1] = "]" end
          return table.concat(parts)
        else
          local keys = {}
          for k in pairs(x) do if k ~= "__name" and k ~= "__order" then table.insert(keys, k) end end
          table.sort(keys, function(a,b) return tostring(a) > tostring(b) end)
          local parts = {"{"}
          for i, k in ipairs(keys) do
            parts[#parts+1] = "'" .. tostring(k) .. "': " .. encode(x[k])
            if i < #keys then parts[#parts+1] = ", " end
          end
          parts[#parts+1] = "}"
          return table.concat(parts)
        end
      elseif type(x) == "string" then
          return '"' .. x .. '"'
        else
          return tostring(x)
        end
      end
      return encode(v)
    end)(_str(malus_law(100, 60)))) or (_str(malus_law(100, 60)))))
  end
  PI = 3.141592653589793
  TWO_PI = 6.283185307179586
  main()
  local _bench_end = os.clock()
  collectgarbage()
  local _bench_end_mem = collectgarbage('count') * 1024
  local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
  local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
  print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
