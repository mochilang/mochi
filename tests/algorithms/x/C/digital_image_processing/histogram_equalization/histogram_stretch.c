// Generated by Mochi 0.10.32 on 2025-08-11 14:57 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_list_len;
size_t histogram_stretch_len;
size_t *histogram_stretch_lens;

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long img_0[3] = {52LL, 55LL, 61LL};
long long img_1[3] = {59LL, 79LL, 61LL};
long long img_2[3] = {85LL, 76LL, 62LL};
long long *img_init[3] = {img_0, img_1, img_2};
long long **img = img_init;
size_t img_len = 3;
size_t img_lens_init[3] = {3, 3, 3};
size_t *img_lens = img_lens_init;
size_t img_lens_len = 3;
long long **result = NULL;
size_t result_len = 0;
size_t *result_lens = NULL;
size_t result_lens_len = 0;

long long * make_list(long long n, long long value);
long long * * histogram_stretch(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len);
void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len);
int main(void);

long long * make_list(long long n, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        res = list_append_long_long(res, &res_len, value);
        i = i + 1LL;
    }
    return make_list_len = res_len, res;
}

long long * * histogram_stretch(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len) {
    long long height = image_len;
    long long width = image_lens[0LL];
    long long *hist = make_list(256LL, 0LL);
    size_t hist_len = make_list_len;
    long long i = 0LL;
    while (i < height) {
        long long j = 0LL;
        while (j < width) {
            long long val = image[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? image_lens[i] + _mochi_idx : _mochi_idx;})];
            hist[(int)(val)] = hist[(int)({long long _mochi_idx = val; _mochi_idx < 0 ? hist_len + _mochi_idx : _mochi_idx;})] + 1LL;
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long *mapping = make_list(256LL, 0LL);
    size_t mapping_len = make_list_len;
    long long cumulative = 0LL;
    long long total = height * width;
    long long h = 0LL;
    while (h < 256LL) {
        cumulative = cumulative + hist[(int)({long long _mochi_idx = h; _mochi_idx < 0 ? hist_len + _mochi_idx : _mochi_idx;})];
        mapping[(int)(h)] = (255LL * cumulative) / total;
        h = h + 1LL;
    }
    i = 0LL;
    while (i < height) {
        long long j = 0LL;
        while (j < width) {
            long long val = image[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? image_lens[i] + _mochi_idx : _mochi_idx;})];
            image[(int)(i)][(int)(j)] = mapping[(int)({long long _mochi_idx = val; _mochi_idx < 0 ? mapping_len + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return histogram_stretch_lens = image_lens, histogram_stretch_len = image_len, image;
}

void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len) {
    long long i = 0LL;
    while (i < image_len) {
        puts(str_list_int(image[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})], image_lens[i]));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        result = histogram_stretch(img, img_len, img_lens, img_len);
        result_len = histogram_stretch_len;
        result_lens = histogram_stretch_lens;
        print_image(result, result_len, result_lens, result_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
