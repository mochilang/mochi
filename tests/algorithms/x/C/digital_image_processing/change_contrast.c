// Generated by Mochi 0.10.32 on 2025-08-09 10:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t change_contrast_len;
size_t *change_contrast_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long image_0[3] = {100LL, 125LL, 150LL};
long long image_1[3] = {175LL, 200LL, 225LL};
long long image_2[3] = {50LL, 75LL, 100LL};
long long *image_init[3] = {image_0, image_1, image_2};
long long **image = image_init;
size_t image_len = 3;
size_t image_lens_init[3] = {3, 3, 3};
size_t *image_lens = image_lens_init;
size_t image_lens_len = 3;
long long **contrasted = NULL;
size_t contrasted_len = 0;
size_t *contrasted_lens = NULL;
size_t contrasted_lens_len = 0;

long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level);
void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
int main(void);

long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level) {
    double factor = (259.0 * ((double)(level) + 255.0)) / (255.0 * (259.0 - (double)(level)));
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)(i)];
        size_t row_len = 0;
        long long *new_row = NULL;
        size_t new_row_len = 0;
        long long j = 0LL;
        while (j < row_len) {
            long long c = row[(int)(j)];
            long long contrasted = 128.0 + (factor * ((double)(c) - 128.0));
            long long clamped = (contrasted < 0LL ? 0LL : (contrasted > 255LL ? 255LL : contrasted));
            new_row = list_append_long_long(new_row, &new_row_len, clamped);
            j = j + 1LL;
        }
        result = list_append_long_longptr(result, &result_len, new_row);
        result_lens = list_append_szt(result_lens, &result_lens_len, new_row_len);
        i = i + 1LL;
    }
    return change_contrast_lens = result_lens, change_contrast_len = result_len, result;
}

void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)(i)];
        size_t row_len = 0;
        long long j = 0LL;
        const char* line = "";
        while (j < row_len) {
            line = str_concat(str_concat(line, str_int(row[(int)(j)])), " ");
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts("Original image:");
        print_image(image, image_len, image_lens, image_len);
        contrasted = change_contrast(image, image_len, image_lens, image_len, 170LL);
        contrasted_len = change_contrast_len;
        contrasted_lens = change_contrast_lens;
        puts("After contrast:");
        print_image(contrasted, contrasted_len, contrasted_lens, contrasted_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
