// Generated by Mochi 0.10.59 on 2025-08-06 23:23 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class LinkedList {
    public long[] data;
    public override string ToString() => $"LinkedList {{data = {data}}}";
}
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static LinkedList empty_list() {
        return new LinkedList{data = new long[]{}};
    }

    public static LinkedList append_value(LinkedList list_0, long value_1) {
        long[] d_2 = list_0.data;
        d_2 = (Enumerable.ToArray(Enumerable.Append<long>(d_2, value_1)));
        return new LinkedList{data = d_2};
    }

    public static LinkedList extend_list(LinkedList list_3, long[] items_4) {
        LinkedList result_5 = list_3;
        long i_6 = 0;
        while ((i_6 < items_4.Length)) {
            result_5 = Program.append_value(result_5, items_4[(int)(i_6)]);
            i_6 = (i_6 + 1);
        };
        return result_5;
    }

    public static string to_string(LinkedList list_7) {
        if ((list_7.data.Length == 0)) {
            return "";
        };
        string s_8 = _fmtStr(list_7.data[(int)(0)]);
        long i_9 = 1;
        while ((i_9 < list_7.data.Length)) {
            s_8 = ((s_8 + " -> ") + _fmtStr(list_7.data[(int)(i_9)]));
            i_9 = (i_9 + 1);
        };
        return s_8;
    }

    public static LinkedList make_linked_list(long[] items_10) {
        if ((items_10.Length == 0)) {
            throw new Exception("The Elements List is empty");
        };
        LinkedList ll_11 = Program.empty_list();
        ll_11 = Program.extend_list(ll_11, items_10);
        return ll_11;
    }

    public static string in_reverse(LinkedList list_12) {
        if ((list_12.data.Length == 0)) {
            return "";
        };
        long i_13 = (list_12.data.Length - 1);
        string s_14 = _fmtStr(list_12.data[(int)(i_13)]);
        i_13 = (i_13 - 1);
        while ((i_13 >= 0)) {
            s_14 = ((s_14 + " <- ") + _fmtStr(list_12.data[(int)(i_13)]));
            i_13 = (i_13 - 1);
        };
        return s_14;
    }

    public static void main() {
        LinkedList linked_list_15 = Program.make_linked_list(new long[]{14, 52, 14, 12, 43});
        Console.WriteLine(Program._fmtTop(("Linked List:  " + Program.to_string(linked_list_15))));
        Console.WriteLine(Program._fmtTop(("Reverse List: " + Program.in_reverse(linked_list_15))));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
