// Generated by Mochi 0.10.59 on 2025-08-06 23:23 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class SortedLinkedList {
    public long[] values;
    public override string ToString() => $"SortedLinkedList {{values = {values}}}";
}
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static long[] sort_list(long[] nums_0) {
        long[] arr_1 = new long[]{};
        long i_2 = 0;
        while ((i_2 < nums_0.Length)) {
            arr_1 = (Enumerable.ToArray(Enumerable.Append<long>(arr_1, nums_0[(int)(i_2)])));
            i_2 = (i_2 + 1);
        };
        long j_3 = 0;
        while ((j_3 < arr_1.Length)) {
            long k_4 = (j_3 + 1);
            while ((k_4 < arr_1.Length)) {
                if ((arr_1[(int)(k_4)] < arr_1[(int)(j_3)])) {
                    long tmp_5 = arr_1[(int)(j_3)];
                    arr_1[j_3] = arr_1[(int)(k_4)];
                    arr_1[k_4] = tmp_5;
                }
                k_4 = (k_4 + 1);
            }
            j_3 = (j_3 + 1);
        };
        return arr_1;
    }

    public static SortedLinkedList make_sorted_linked_list(long[] ints_6) {
        return new SortedLinkedList{values = Program.sort_list(ints_6)};
    }

    public static long len_sll(SortedLinkedList sll_7) {
        return sll_7.values.Length;
    }

    public static string str_sll(SortedLinkedList sll_8) {
        string res_9 = "";
        long i_10 = 0;
        while ((i_10 < sll_8.values.Length)) {
            res_9 = (res_9 + _fmtStr(sll_8.values[(int)(i_10)]));
            if (((i_10 + 1) < sll_8.values.Length)) {
                res_9 = (res_9 + " -> ");
            }
            i_10 = (i_10 + 1);
        };
        return res_9;
    }

    public static SortedLinkedList merge_lists(SortedLinkedList a_11, SortedLinkedList b_12) {
        long[] combined_13 = new long[]{};
        long i_14 = 0;
        while ((i_14 < a_11.values.Length)) {
            combined_13 = (Enumerable.ToArray(Enumerable.Append<long>(combined_13, a_11.values[(int)(i_14)])));
            i_14 = (i_14 + 1);
        };
        i_14 = 0;
        while ((i_14 < b_12.values.Length)) {
            combined_13 = (Enumerable.ToArray(Enumerable.Append<long>(combined_13, b_12.values[(int)(i_14)])));
            i_14 = (i_14 + 1);
        };
        return Program.make_sorted_linked_list(combined_13);
    }

    public static void main() {
        long[] test_data_odd_15 = new long[]{3, 9, -11, 0, 7, 5, 1, -1};
        long[] test_data_even_16 = new long[]{4, 6, 2, 0, 8, 10, 3, -2};
        SortedLinkedList sll_one_17 = Program.make_sorted_linked_list(test_data_odd_15);
        SortedLinkedList sll_two_18 = Program.make_sorted_linked_list(test_data_even_16);
        SortedLinkedList merged_19 = Program.merge_lists(sll_one_17, sll_two_18);
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.len_sll(merged_19))));
        Console.WriteLine(Program._fmtTop(Program.str_sll(merged_19)));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
