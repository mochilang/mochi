// Generated by Mochi 0.10.59 on 2025-08-06 22:16 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static long[] A_0 = new long[]{};
    static long N_1 = 0;
    static long[] st_2 = new long[]{};
    static long NEG_INF_23 = -1000000000;
    public static long left_child(long idx_3) {
        return (idx_3 * 2);
    }

    public static long right_child(long idx_4) {
        return ((idx_4 * 2) + 1);
    }

    public static void build(long idx_5, long left_6, long right_7) {
        if ((left_6 == right_7)) {
            st_2[idx_5] = A_0[(int)(left_6)];
        } else {
            long mid_8 = ((left_6 + right_7) / 2);
            Program.build(Program.left_child(idx_5), left_6, mid_8);
            Program.build(Program.right_child(idx_5), (mid_8 + 1), right_7);
            long left_val_9 = st_2[(int)(Program.left_child(idx_5))];
            long right_val_10 = st_2[(int)(Program.right_child(idx_5))];
            st_2[idx_5] = ((left_val_9 > right_val_10) ? left_val_9 : right_val_10);
        };
    }

    public static bool update_recursive(long idx_11, long left_12, long right_13, long a_14, long b_15, long val_16) {
        if (((right_13 < a_14) || (left_12 > b_15))) {
            return true;
        };
        if ((left_12 == right_13)) {
            st_2[idx_11] = val_16;
            return true;
        };
        long mid_17 = ((left_12 + right_13) / 2);
        Program.update_recursive(Program.left_child(idx_11), left_12, mid_17, a_14, b_15, val_16);
        Program.update_recursive(Program.right_child(idx_11), (mid_17 + 1), right_13, a_14, b_15, val_16);
        long left_val_18 = st_2[(int)(Program.left_child(idx_11))];
        long right_val_19 = st_2[(int)(Program.right_child(idx_11))];
        st_2[idx_11] = ((left_val_18 > right_val_19) ? left_val_18 : right_val_19);
        return true;
    }

    public static bool update(long a_20, long b_21, long val_22) {
        return Program.update_recursive(1, 0, (N_1 - 1), (a_20 - 1), (b_21 - 1), val_22);
    }

    public static long query_recursive(long idx_24, long left_25, long right_26, long a_27, long b_28) {
        if (((right_26 < a_27) || (left_25 > b_28))) {
            return NEG_INF_23;
        };
        if (((left_25 >= a_27) && (right_26 <= b_28))) {
            return st_2[(int)(idx_24)];
        };
        long mid_29 = ((left_25 + right_26) / 2);
        long q1_30 = Program.query_recursive(Program.left_child(idx_24), left_25, mid_29, a_27, b_28);
        long q2_31 = Program.query_recursive(Program.right_child(idx_24), (mid_29 + 1), right_26, a_27, b_28);
        return ((q1_30 > q2_31) ? q1_30 : q2_31);
    }

    public static long query(long a_32, long b_33) {
        return Program.query_recursive(1, 0, (N_1 - 1), (a_32 - 1), (b_33 - 1));
    }

    public static void show_data() {
        long i_34 = 0;
        long[] show_list_35 = new long[]{};
        while ((i_34 < N_1)) {
            show_list_35 = (Enumerable.ToArray(Enumerable.Append<long>(show_list_35, Program.query((i_34 + 1), (i_34 + 1)))));
            i_34 = (i_34 + 1);
        };
        Console.WriteLine(Program._fmtTop(show_list_35));
    }

    public static void main() {
        A_0 = new long[]{1, 2, -4, 7, 3, -5, 6, 11, -20, 9, 14, 15, 5, 2, -8};
        N_1 = A_0.Length;
        long i_36 = 0;
        while ((i_36 < (4 * N_1))) {
            st_2 = (Enumerable.ToArray(Enumerable.Append<long>(st_2, 0)));
            i_36 = (i_36 + 1);
        };
        if ((N_1 > 0)) {
            Program.build(1, 0, (N_1 - 1));
        };
        Console.WriteLine(Program._fmtTop(Program.query(4, 6)));
        Console.WriteLine(Program._fmtTop(Program.query(7, 11)));
        Console.WriteLine(Program._fmtTop(Program.query(7, 12)));
        Program.update(1, 3, 111);
        Console.WriteLine(Program._fmtTop(Program.query(1, 15)));
        Program.update(7, 8, 235);
        Program.show_data();
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
