// Generated by Mochi 0.10.59 on 2025-08-07 08:53 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static Dictionary<long, long[]> graph_19 = new Dictionary<long, long[]>{{0, new long[]{1, 3}}, {1, new long[]{0, 3}}, {2, new long[]{0, 3, 4}}, {3, new long[]{0, 1, 2}}, {4, new long[]{2, 3}}};
    static long[] cover_20 = Program.matching_min_vertex_cover(graph_19);
    public static bool contains(long[] xs_0, long v_1) {
        foreach (long x_2 in xs_0) {
            if ((x_2 == v_1)) {
                return true;
            }
        };
        return false;
    }

    public static long[][] get_edges(Dictionary<long, long[]> graph_3) {
        long n_4 = graph_3.Count;
        long[][] edges_5 = new long[][]{};
        for (var i_6 = 0; i_6 < n_4; i_6++) {
            foreach (long j_7 in (graph_3.ContainsKey(i_6) ? graph_3[i_6] : null)) {
                edges_5 = (Enumerable.ToArray(Enumerable.Append<long[]>(edges_5, new long[]{i_6, j_7})));
            }
        };
        return edges_5;
    }

    public static long[] matching_min_vertex_cover(Dictionary<long, long[]> graph_8) {
        long[] chosen_9 = new long[]{};
        long[][] edges_10 = Program.get_edges(graph_8);
        while ((edges_10.Length > 0)) {
            long idx_11 = (edges_10.Length - 1);
            long[] e_12 = edges_10[(int)(idx_11)];
            edges_10 = edges_10.Skip((int)(0)).Take((int)((idx_11 - 0))).ToArray();
            long u_13 = e_12[(int)(0)];
            long v_14 = e_12[(int)(1)];
            if ((!Program.contains(chosen_9, u_13))) {
                chosen_9 = (Enumerable.ToArray(Enumerable.Append<long>(chosen_9, u_13)));
            }
            if ((!Program.contains(chosen_9, v_14))) {
                chosen_9 = (Enumerable.ToArray(Enumerable.Append<long>(chosen_9, v_14)));
            }
            long[][] filtered_15 = new long[][]{};
            foreach (long[] edge_16 in edges_10) {
                long a_17 = edge_16[(int)(0)];
                long b_18 = edge_16[(int)(1)];
                if (((((a_17 != u_13) && (b_18 != u_13)) && (a_17 != v_14)) && (b_18 != v_14))) {
                    filtered_15 = (Enumerable.ToArray(Enumerable.Append<long[]>(filtered_15, edge_16)));
                }
            }
            edges_10 = filtered_15;
        };
        return chosen_9;
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(_fmtStr(cover_20)));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
