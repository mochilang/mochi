// Generated by Mochi 0.10.59 on 2025-08-07 00:02 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Result {
    public string kind;
    public double value;
    public override string ToString() => $"Result {{kind = \"{kind}\", value = {value.ToString("0.0")}}}";
}
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static double ELECTRON_CHARGE_0 = 1.6021e-19;
    static Result r1_5 = Program.electric_conductivity(25.0, 100.0, 0.0);
    static Result r2_6 = Program.electric_conductivity(0.0, 1600.0, 200.0);
    static Result r3_7 = Program.electric_conductivity(1000.0, 0.0, 1200.0);
    public static Result electric_conductivity(double conductivity_1, double electron_conc_2, double mobility_3) {
        long zero_count_4 = 0;
        if ((conductivity_1 == 0.0)) {
            zero_count_4 = (zero_count_4 + 1);
        };
        if ((electron_conc_2 == 0.0)) {
            zero_count_4 = (zero_count_4 + 1);
        };
        if ((mobility_3 == 0.0)) {
            zero_count_4 = (zero_count_4 + 1);
        };
        if ((zero_count_4 != 1)) {
            throw new Exception("You cannot supply more or less than 2 values");
        };
        if ((conductivity_1 < 0.0)) {
            throw new Exception("Conductivity cannot be negative");
        };
        if ((electron_conc_2 < 0.0)) {
            throw new Exception("Electron concentration cannot be negative");
        };
        if ((mobility_3 < 0.0)) {
            throw new Exception("mobility cannot be negative");
        };
        if ((conductivity_1 == 0.0)) {
            return new Result{kind = "conductivity", value = ((mobility_3 * electron_conc_2) * ELECTRON_CHARGE_0)};
        };
        if ((electron_conc_2 == 0.0)) {
            return new Result{kind = "electron_conc", value = (conductivity_1 / (mobility_3 * ELECTRON_CHARGE_0))};
        };
        return new Result{kind = "mobility", value = (conductivity_1 / (electron_conc_2 * ELECTRON_CHARGE_0))};
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(((r1_5.kind + " ") + _fmtStr(r1_5.value))));
            Console.WriteLine(Program._fmtTop(((r2_6.kind + " ") + _fmtStr(r2_6.value))));
            Console.WriteLine(Program._fmtTop(((r3_7.kind + " ") + _fmtStr(r3_7.value))));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
