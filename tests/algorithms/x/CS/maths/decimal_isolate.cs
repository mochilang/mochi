// Generated by Mochi 0.10.59 on 2025-08-07 09:39 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static double floor(double x_0) {
        long i_1 = Convert.ToInt64(x_0);
        if ((Convert.ToDouble(i_1) > x_0)) {
            i_1 = (i_1 - 1);
        };
        return Convert.ToDouble(i_1);
    }

    public static double pow10(long n_2) {
        double p_3 = 1.0;
        long i_4 = 0;
        while ((i_4 < n_2)) {
            p_3 = (p_3 * 10.0);
            i_4 = (i_4 + 1);
        };
        return p_3;
    }

    public static double round(double x_5, long n_6) {
        double m_7 = Program.pow10(n_6);
        return (Program.floor(((x_5 * m_7) + 0.5)) / m_7);
    }

    public static double decimal_isolate(double number_8, long digit_amount_9) {
        long whole_10 = Convert.ToInt64(number_8);
        double frac_11 = (number_8 - Convert.ToDouble(whole_10));
        if ((digit_amount_9 > 0)) {
            return Program.round(frac_11, digit_amount_9);
        };
        return frac_11;
    }

    public static void main() {
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(1.53, 0))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(35.345, 1))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(35.345, 2))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(35.345, 3))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(-14.789, 3))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(0.0, 2))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(-14.123, 1))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(-14.123, 2))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.decimal_isolate(-14.123, 3))));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
