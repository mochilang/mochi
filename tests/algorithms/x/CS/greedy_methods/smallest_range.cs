// Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class HeapItem {
    public long value;
    public long list_idx;
    public long elem_idx;
    public override string ToString() => $"HeapItem {{value = {value}, list_idx = {list_idx}, elem_idx = {elem_idx}}}";
}
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static long INF_0 = 1000000000;
    public static long[] smallest_range(long[][] nums_1) {
        HeapItem[] heap_2 = new HeapItem[]{};
        long current_max_3 = -INF_0;
        long i_4 = 0;
        while ((i_4 < nums_1.Length)) {
            long first_val_5 = nums_1[(int)(i_4)][(int)(0)];
            heap_2 = (Enumerable.ToArray(Enumerable.Append<HeapItem>(heap_2, new HeapItem{value = first_val_5, list_idx = i_4, elem_idx = 0})));
            if ((first_val_5 > current_max_3)) {
                current_max_3 = first_val_5;
            }
            i_4 = (i_4 + 1);
        };
        long[] best_6 = new long[]{-INF_0, INF_0};
        while ((heap_2.Length > 0)) {
            long min_idx_7 = 0;
            long j_8 = 1;
            while ((j_8 < heap_2.Length)) {
                HeapItem hj_9 = heap_2[(int)(j_8)];
                HeapItem hmin_10 = heap_2[(int)(min_idx_7)];
                if ((hj_9.value < hmin_10.value)) {
                    min_idx_7 = j_8;
                }
                j_8 = (j_8 + 1);
            }
            HeapItem item_11 = heap_2[(int)(min_idx_7)];
            HeapItem[] new_heap_12 = new HeapItem[]{};
            long k_13 = 0;
            while ((k_13 < heap_2.Length)) {
                if ((k_13 != min_idx_7)) {
                    new_heap_12 = (Enumerable.ToArray(Enumerable.Append<HeapItem>(new_heap_12, heap_2[(int)(k_13)])));
                }
                k_13 = (k_13 + 1);
            }
            heap_2 = new_heap_12;
            long current_min_14 = item_11.value;
            if (((current_max_3 - current_min_14) < (best_6[(int)(1)] - best_6[(int)(0)]))) {
                best_6 = new long[]{current_min_14, current_max_3};
            }
            if ((item_11.elem_idx == (nums_1[(int)(item_11.list_idx)].Length - 1))) {
                break;
            }
            long next_val_15 = nums_1[(int)(item_11.list_idx)][(int)((item_11.elem_idx + 1))];
            heap_2 = (Enumerable.ToArray(Enumerable.Append<HeapItem>(heap_2, new HeapItem{value = next_val_15, list_idx = item_11.list_idx, elem_idx = (item_11.elem_idx + 1)})));
            if ((next_val_15 > current_max_3)) {
                current_max_3 = next_val_15;
            }
        };
        return best_6;
    }

    public static string list_to_string(long[] arr_16) {
        string s_17 = "[";
        long i_18 = 0;
        while ((i_18 < arr_16.Length)) {
            s_17 = (s_17 + _fmtStr(arr_16[(int)(i_18)]));
            if ((i_18 < (arr_16.Length - 1))) {
                s_17 = (s_17 + ", ");
            }
            i_18 = (i_18 + 1);
        };
        return (s_17 + "]");
    }

    public static void main() {
        long[] result1_19 = Program.smallest_range(new long[][]{new long[]{4, 10, 15, 24, 26}, new long[]{0, 9, 12, 20}, new long[]{5, 18, 22, 30}});
        Console.WriteLine(Program._fmtTop(Program.list_to_string(result1_19)));
        long[] result2_20 = Program.smallest_range(new long[][]{new long[]{1, 2, 3}, new long[]{1, 2, 3}, new long[]{1, 2, 3}});
        Console.WriteLine(Program._fmtTop(Program.list_to_string(result2_20)));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
