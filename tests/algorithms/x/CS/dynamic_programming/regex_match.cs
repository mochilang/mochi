// Generated by Mochi 0.10.59 on 2025-08-07 00:02 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static bool recursive_match(string text_0, string pattern_1) {
        if ((pattern_1.Length == 0)) {
            return (text_0.Length == 0);
        };
        if ((text_0.Length == 0)) {
            if (((pattern_1.Length >= 2) && (_substr(pattern_1, (pattern_1.Length - 1), pattern_1.Length) == "*"))) {
                return Program.recursive_match(text_0, _substr(pattern_1, 0, (pattern_1.Length - 2)));
            }
            return false;
        };
        string last_text_2 = _substr(text_0, (text_0.Length - 1), text_0.Length);
        string last_pattern_3 = _substr(pattern_1, (pattern_1.Length - 1), pattern_1.Length);
        if (((last_text_2 == last_pattern_3) || (last_pattern_3 == "."))) {
            return Program.recursive_match(_substr(text_0, 0, (text_0.Length - 1)), _substr(pattern_1, 0, (pattern_1.Length - 1)));
        };
        if ((last_pattern_3 == "*")) {
            if (Program.recursive_match(_substr(text_0, 0, (text_0.Length - 1)), pattern_1)) {
                return true;
            }
            return Program.recursive_match(text_0, _substr(pattern_1, 0, (pattern_1.Length - 2)));
        };
        return false;
    }

    public static bool dp_match(string text_4, string pattern_5) {
        long m_6 = text_4.Length;
        long n_7 = pattern_5.Length;
        bool[][] dp_8 = new bool[][]{};
        long i_9 = 0;
        while ((i_9 <= m_6)) {
            bool[] row_10 = new bool[]{};
            long j_11 = 0;
            while ((j_11 <= n_7)) {
                row_10 = (Enumerable.ToArray(Enumerable.Append<bool>(row_10, false)));
                j_11 = (j_11 + 1);
            }
            dp_8 = (Enumerable.ToArray(Enumerable.Append<bool[]>(dp_8, row_10)));
            i_9 = (i_9 + 1);
        };
        dp_8[(int)(0)][0] = true;
        long j_12 = 1;
        while ((j_12 <= n_7)) {
            if (((_substr(pattern_5, (j_12 - 1), j_12) == "*") && (j_12 >= 2))) {
                if (dp_8[(int)(0)][(int)((j_12 - 2))]) {
                    dp_8[(int)(0)][j_12] = true;
                }
            }
            j_12 = (j_12 + 1);
        };
        i_9 = 1;
        while ((i_9 <= m_6)) {
            j_12 = 1;
            while ((j_12 <= n_7)) {
                string p_char_13 = _substr(pattern_5, (j_12 - 1), j_12);
                string t_char_14 = _substr(text_4, (i_9 - 1), i_9);
                if (((p_char_13 == ".") || (p_char_13 == t_char_14))) {
                    if (dp_8[(int)((i_9 - 1))][(int)((j_12 - 1))]) {
                        dp_8[(int)(i_9)][j_12] = true;
                    }
                } else if ((p_char_13 == "*")) {
                    if ((j_12 >= 2)) {
                        if (dp_8[(int)(i_9)][(int)((j_12 - 2))]) {
                            dp_8[(int)(i_9)][j_12] = true;
                        }
                        string prev_p_15 = _substr(pattern_5, (j_12 - 2), (j_12 - 1));
                        if (((prev_p_15 == ".") || (prev_p_15 == t_char_14))) {
                            if (dp_8[(int)((i_9 - 1))][(int)(j_12)]) {
                                dp_8[(int)(i_9)][j_12] = true;
                            }
                        }
                    }
                } else {
                    dp_8[(int)(i_9)][j_12] = false;
                }
                j_12 = (j_12 + 1);
            }
            i_9 = (i_9 + 1);
        };
        return dp_8[(int)(m_6)][(int)(n_7)];
    }

    public static void print_bool(bool b_16) {
        if (b_16) {
            Console.WriteLine(Program._fmtTop(true));
        } else {
            Console.WriteLine(Program._fmtTop(false));
        };
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.print_bool(Program.recursive_match("abc", "a.c"));
            Program.print_bool(Program.recursive_match("abc", "af*.c"));
            Program.print_bool(Program.recursive_match("abc", "a.c*"));
            Program.print_bool(Program.recursive_match("abc", "a.c*d"));
            Program.print_bool(Program.recursive_match("aa", ".*"));
            Program.print_bool(Program.dp_match("abc", "a.c"));
            Program.print_bool(Program.dp_match("abc", "af*.c"));
            Program.print_bool(Program.dp_match("abc", "a.c*"));
            Program.print_bool(Program.dp_match("abc", "a.c*d"));
            Program.print_bool(Program.dp_match("aa", ".*"));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
