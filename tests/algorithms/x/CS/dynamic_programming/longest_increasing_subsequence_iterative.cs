// Generated by Mochi 0.10.59 on 2025-08-06 23:23 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static long[] copy_list(long[] xs_0) {
        long[] res_1 = new long[]{};
        long i_2 = 0;
        while ((i_2 < xs_0.Length)) {
            res_1 = (Enumerable.ToArray(Enumerable.Append<long>(res_1, xs_0[(int)(i_2)])));
            i_2 = (i_2 + 1);
        };
        return res_1;
    }

    public static long[] longest_subsequence(long[] arr_3) {
        long n_4 = arr_3.Length;
        long[][] lis_5 = new long[][]{};
        long i_6 = 0;
        while ((i_6 < n_4)) {
            long[] single_7 = new long[]{};
            single_7 = (Enumerable.ToArray(Enumerable.Append<long>(single_7, arr_3[(int)(i_6)])));
            lis_5 = (Enumerable.ToArray(Enumerable.Append<long[]>(lis_5, single_7)));
            i_6 = (i_6 + 1);
        };
        i_6 = 1;
        while ((i_6 < n_4)) {
            long prev_8 = 0;
            while ((prev_8 < i_6)) {
                if (((arr_3[(int)(prev_8)] <= arr_3[(int)(i_6)]) && ((lis_5[(int)(prev_8)].Length + 1) > lis_5[(int)(i_6)].Length))) {
                    long[] temp_9 = Program.copy_list(lis_5[(int)(prev_8)]);
                    long[] temp2_10 = (Enumerable.ToArray(Enumerable.Append<long>(temp_9, arr_3[(int)(i_6)])));
                    lis_5[i_6] = temp2_10;
                }
                prev_8 = (prev_8 + 1);
            }
            i_6 = (i_6 + 1);
        };
        long[] result_11 = new long[]{};
        i_6 = 0;
        while ((i_6 < n_4)) {
            if ((lis_5[(int)(i_6)].Length > result_11.Length)) {
                result_11 = lis_5[(int)(i_6)];
            }
            i_6 = (i_6 + 1);
        };
        return result_11;
    }

    public static void main() {
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{10, 22, 9, 33, 21, 50, 41, 60, 80}))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{4, 8, 7, 5, 1, 12, 2, 3, 9}))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{9, 8, 7, 6, 5, 7}))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{28, 26, 12, 23, 35, 39}))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{1, 1, 1}))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.longest_subsequence(new long[]{}))));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
