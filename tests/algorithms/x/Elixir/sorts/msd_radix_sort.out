warning: variable "num" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 26 │       {length, num} = try do
    │                ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:26:16: Main.get_bit_length/1

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 56 │       {i, max_len} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:56:8: Main.max_bit_length/1

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 80 │       {i, n} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:80:8: Main.get_bit/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 115 │       {i, ones, zeros} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:115:8: Main._msd_radix_sort/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 134 │       {i, res} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:134:8: Main._msd_radix_sort/2

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 162 │       i = try do
     │       ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/sorts/msd_radix_sort.exs:162:7: Main.msd_radix_sort/1

[1 4 12 40 100]
[]
[80 123 123 345]
[1 45 1209 540402 834598]