run: exit status 1
    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 29 │       {i, res} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:29:8: Main.pow2/1

    warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 67 │       {adj, i, lvl, ptr, q} = try do
    │             ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:67:13: Main.new_dinic/1

    warning: variable "g" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 89 │       g = Map.put(g, :adj, adj)
    │       ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:89:7: Main.add_edge/5

     warning: variable "e" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 107 │           {adj_all, adj_v, e, g} = if Enum.at(g.lvl, to, 0) == Enum.at(g.lvl, v, 0) + 1 do
     │                            ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:107:28: Main.dfs/4

     warning: variable "g" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 144 │       g = Map.put(g, :adj, adj_all)
     │       ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:144:7: Main.dfs/4

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 136 │       {adj_all, adj_v, g, i, ptr} = try do
     │                           ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:136:27: Main.dfs/4

     warning: variable "lvl_inner" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 198 │                       {g, lvl_inner, q, qe} = if Enum.at(lvl_inner, to, []) == 0 && residual >= threshold do
     │                           ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:198:27: Main.max_flow/3

     warning: variable "j" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 213 │                   {g, j, q, qe} = try do
     │                       ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:213:23: Main.max_flow/3

     warning: variable "v" is unused (if the variable is not meant to be used, prefix it with an underscore)
     │
 188 │                   v = Enum.at(q, qi, [])
     │                   ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:188:19: Main.max_flow/3

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 172 │               {i, lvl, ptr} = try do
     │                ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:172:16: Main.max_flow/3

     warning: variable "p" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 240 │               {flow, p} = try do
     │                      ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:240:22: Main.max_flow/3

     warning: variable "q" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 224 │               {g, q, qe, qi} = try do
     │                   ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:224:19: Main.max_flow/3

     warning: variable "qe" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 224 │               {g, q, qe, qi} = try do
     │                      ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:224:22: Main.max_flow/3

     warning: variable "qi" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 224 │               {g, q, qe, qi} = try do
     │                          ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:224:26: Main.max_flow/3

     warning: variable "g" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 266 │       {flow, g, l} = try do
     │              ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:266:14: Main.max_flow/3

     warning: variable "l" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 266 │       {flow, g, l} = try do
     │                 ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:266:17: Main.max_flow/3

     error: undefined variable "v"
     │
 296 │         while_fun_10.(while_fun_10, v)
     │                                     ^
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:296:37: Main.main/0

     warning: variable "v" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 327 │     v = try do
     │     ~
     │
     └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs:327:5: Main.main/0

** (CompileError) /workspace/mochi/tests/algorithms/x/Elixir/graphs/dinic.exs: cannot compile module Main (errors have been logged)

