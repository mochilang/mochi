# Code generated by Mochi transpiler.
# Version 0.10.61, generated on 2025-08-08 17:42 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


def _str(v):
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    return str(v)

def centripetal(mass, velocity, radius):
    if mass < 0.0:
        panic("The mass of the body cannot be negative")
    if radius <= 0.0:
        panic("The radius is always a positive non zero integer")
    return mass * velocity * velocity / radius
def floor(x):
    i = int(x)
    if (float(i)) > x:
        i = i - 1
    return float(i)
def pow10(n):
    p = 1.0
    i = 0
    while i < n:
        p = p * 10.0
        i = i + 1
    return p
def round(x, n):
    m = pow10(n)
    return floor(x * m + 0.5) / m
def show(mass, velocity, radius):
    f = centripetal(mass, velocity, radius)
    print(_str(round(f, 2)))
show(15.5, -30.0, 10.0)
show(10.0, 15.0, 5.0)
show(20.0, -50.0, 15.0)
show(12.25, 40.0, 25.0)
show(50.0, 100.0, 50.0)
