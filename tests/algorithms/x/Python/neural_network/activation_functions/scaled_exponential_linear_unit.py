# Code generated by Mochi transpiler.
# Version 0.10.61, generated on 2025-08-08 17:42 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


def _append(lst, v):
    return (lst or []) + [v]

def exp(x):
    term = 1.0
    sum = 1.0
    n = 1
    while n < 20:
        term = term * x / (float(n))
        sum = sum + term
        n = n + 1
    return sum
def scaled_exponential_linear_unit(vector, alpha, lambda_):
    result = []
    i = 0
    while i < len(vector):
        x = vector[i]
        y = (lambda_ * x if x > 0.0 else lambda_ * alpha * (exp(x) - 1.0))
        result = _append(result, y)
        i = i + 1
    return result
print(scaled_exponential_linear_unit([1.3, 3.7, 2.4], 1.6732, 1.0507))
print(scaled_exponential_linear_unit([1.3, 4.7, 8.2], 1.6732, 1.0507))
