# Code generated by Mochi transpiler.
# Version 0.10.66, generated on 2025-08-16 11:48 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


def _str(v):
    if isinstance(v, float):
        if abs(v - round(v)) < 1e-9:
            return str(float(round(v)))
        return format(v, ".15g")
    return str(v)

def abs_float(x):
    if x < 0.0:
        return -x
    return x
def average_absolute_deviation(nums):
    if len(nums) == 0:
        panic("List is empty")
    sum_ = 0
    for x in nums:
        sum_ = sum_ + x
    n = float(len(nums))
    mean = (float(sum_)) / n
    dev_sum = 0.0
    for x in nums:
        dev_sum = dev_sum + abs_float((float(x)) - mean)
    return dev_sum / n
print(_str(average_absolute_deviation([0])))
print(_str(average_absolute_deviation([4, 1, 3, 2])))
print(_str(average_absolute_deviation([2, 70, 6, 50, 20, 8, 4, 0])))
print(_str(average_absolute_deviation([-20, 0, 30, 15])))

