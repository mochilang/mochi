(* Generated by Mochi transpiler v0.10.63 on 2025-08-11 15:32 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec parse_int s =
  let __ret = ref 0 in
  (try
  let value = ref (0) in
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  let c = String.make 1 (String.get (s) !i) in
  value := ((!value * 10) + int_of_string (c));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!value) : int); raise Return
  with Return -> !__ret)

and is_polish_national_id id =
  let __ret = ref false in
  (try
  if (String.length (id) = 0) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  if ((let __s = id in let __st = 0 in let __len = (1 - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") = "-") then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let input_int = parse_int (id) in
  if ((input_int < 10100000) || (input_int > 99923199999)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let month = parse_int ((let __s = id in let __st = 2 in let __len = (4 - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) in
  if not (((((((month >= 1) && (month <= 12)) || ((month >= 21) && (month <= 32))) || ((month >= 41) && (month <= 52))) || ((month >= 61) && (month <= 72))) || ((month >= 81) && (month <= 92)))) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let day = parse_int ((let __s = id in let __st = 4 in let __len = (6 - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) in
  if ((day < 1) || (day > 31)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let multipliers = ref ([1; 3; 7; 9; 1; 3; 7; 9; 1; 3]) in
  let subtotal = ref (0) in
  let i = ref (0) in
  (try while (!i < List.length (!multipliers)) do
    try
  let digit = parse_int ((let __s = id in let __st = !i in let __len = ((!i + 1) - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) in
  subtotal := (!subtotal + (((digit * List.nth (!multipliers) (!i)) mod 10 + 10) mod 10));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let checksum = (10 - ((!subtotal mod 10 + 10) mod 10)) in
  __ret := (Obj.magic ((checksum = ((input_int mod 10 + 10) mod 10))) : bool); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((string_of_bool (is_polish_national_id ("02070803628"))));
  print_endline ((string_of_bool (is_polish_national_id ("02150803629"))));
  print_endline ((string_of_bool (is_polish_national_id ("02075503622"))));
  print_endline ((string_of_bool (is_polish_national_id ("-99012212349"))));
  print_endline ((string_of_bool (is_polish_national_id ("990122123499999"))));
  print_endline ((string_of_bool (is_polish_national_id ("02070803621"))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
