(* Generated by Mochi transpiler v0.10.70 on 2025-08-22 23:09 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic r : int) = 0
  else
    match tag r with
    | 0 -> __is_list (field r 1)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _num_primes = 100
let rec generate_primes limit =
  let __ret = ref ([] : (int) list) in
  (try
  let limit = (Obj.magic limit : int) in
  let is_prime = ref (([] : (bool) list)) in
  let i = ref (0) in
  (try while (!i <= limit) do
    try
  is_prime := (Obj.magic ((List.append (!is_prime) [(Obj.magic (true) : bool)])) : bool list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  is_prime := (List.mapi (fun __i __x -> if __i = 0 then false else __x) (!is_prime));
  is_prime := (List.mapi (fun __i __x -> if __i = 1 then false else __x) (!is_prime));
  i := 2;
  (try while ((!i * !i) <= limit) do
    try
  if (let __l = !is_prime in let __i = !i in if __i < 0 then false else match List.nth_opt __l __i with Some v -> (Obj.magic v : bool) | None -> false) then (
  let j = ref ((!i * !i)) in
  (try while (!j <= limit) do
    try
  is_prime := (List.mapi (fun __i __x -> if __i = !j then false else __x) (!is_prime));
  j := (!j + !i);
    with Continue -> ()
  done with Break -> ());
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let primes = ref (([] : (int) list)) in
  i := 2;
  (try while (!i <= limit) do
    try
  if (let __l = !is_prime in let __i = !i in if __i < 0 then false else match List.nth_opt __l __i with Some v -> (Obj.magic v : bool) | None -> false) then (
  primes := (Obj.magic ((List.append (!primes) [(Obj.magic (!i) : int)])) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!primes) : int list); raise Return
  with Return -> !__ret)


let primes = ref (generate_primes (Obj.repr (_num_primes)))
let rec contains xs value =
  let __ret = ref false in
  (try
  let xs = (Obj.magic xs : int list) in
  let value = (Obj.magic value : int) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  if ((let __l = xs in let __i = !i in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) = value) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)


let partition_cache = ref ([] : (string * int list) list)
let rec partition n =
  let __ret = ref ([] : (int) list) in
  (try
  let n = (Obj.magic n : int) in
  if (n < 0) then (
  __ret := (Obj.magic ([]) : int list); raise Return
  );
  if (n = 0) then (
  __ret := (Obj.magic ([1]) : int list); raise Return
  );
  if (List.mem_assoc (__str (Obj.repr (n))) !partition_cache) then (
  __ret := (Obj.magic ((match List.assoc_opt (__str (Obj.repr (n))) (!partition_cache) with Some v -> (Obj.magic v : int list) | None -> [])) : int list); raise Return
  );
  let ret = ref (([] : (int) list)) in
  (try List.iter (fun prime ->
    try
  if (prime > n) then (
  raise Continue;
  );
  let subs = ref (partition (Obj.repr ((n - prime)))) in
  (try List.iter (fun sub ->
    try
  let prod = (sub * prime) in
  if not (contains (!ret) (Obj.repr (prod))) then (
  ret := (Obj.magic ((List.append (!ret) [(Obj.magic (prod) : int)])) : int list);
  );
    with Continue -> ()) (!subs) with Break -> ());
    with Continue -> ()) (!primes) with Break -> ());
  partition_cache := ((__str (Obj.repr (n)), !ret) :: List.remove_assoc (__str (Obj.repr (n))) (!partition_cache));
  __ret := (Obj.magic (!ret) : int list); raise Return
  with Return -> !__ret)

and solution threshold =
  let __ret = ref 0 in
  (try
  let threshold = (Obj.magic threshold : int) in
  let number_to_partition = ref (1) in
  (try while (!number_to_partition < _num_primes) do
    try
  let parts = ref (partition (Obj.repr (!number_to_partition))) in
  if (List.length (!parts) > threshold) then (
  __ret := (Obj.magic (!number_to_partition) : int); raise Return
  );
  number_to_partition := (!number_to_partition + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (0) : int); raise Return
  with Return -> !__ret)


let result = solution (Obj.repr (5000))
let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  print_endline (("solution() = " ^ (string_of_int (Obj.magic (result) : int))));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
