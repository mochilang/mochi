(* Generated by Mochi transpiler v0.10.64 on 2025-08-12 09:13 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec dense_to_one_hot labels num_classes =
  let __ret = ref ([] : (int list) list) in
  (try
  let labels = (Obj.magic labels : int list) in
  let num_classes = (Obj.magic num_classes : int) in
  let result = ref (([] : (int list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (labels)) do
    try
  let row = ref (([] : (int) list)) in
  let j = ref (0) in
  (try while (!j < num_classes) do
    try
  if (!j = (let __l = labels in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  row := (Obj.magic (List.append (!row) [(Obj.magic (1) : int)]) : int list);
  ) else (
  row := (Obj.magic (List.append (!row) [(Obj.magic (0) : int)]) : int list);
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  result := (Obj.magic (List.append (!result) [(Obj.magic (!row) : int list)]) : int list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list list); raise Return
  with Return -> !__ret)

and new_dataset images labels =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let images = (Obj.magic images : int list list) in
  let labels = (Obj.magic labels : int list list) in
  __ret := (Obj.magic ([(__str ("images"), Obj.repr (images)); (__str ("labels"), Obj.repr (labels)); (__str ("num_examples"), Obj.repr (List.length (images))); (__str ("index_in_epoch"), Obj.repr (0)); (__str ("epochs_completed"), Obj.repr (0))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and next_batch ds batch_size =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let ds = (Obj.magic ds : (string * Obj.t) list) in
  let batch_size = (Obj.magic batch_size : int) in
  let start = (Obj.obj (List.assoc (__str ("index_in_epoch")) (ds) : Obj.t) : int) in
  if ((start + batch_size) > (Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int)) then (
  let rest = ((Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int) - start) in
  let images_rest = ref (List.of_seq (Seq.take ((Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int) - start) (Seq.drop start (List.to_seq ((Obj.obj (List.assoc (__str ("images")) (ds) : Obj.t) : int list list)))))) in
  let labels_rest = ref (List.of_seq (Seq.take ((Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int) - start) (Seq.drop start (List.to_seq ((Obj.obj (List.assoc (__str ("labels")) (ds) : Obj.t) : int list list)))))) in
  let new_index = (batch_size - rest) in
  let images_new = ref (List.of_seq (Seq.take (new_index - 0) (Seq.drop 0 (List.to_seq ((Obj.obj (List.assoc (__str ("images")) (ds) : Obj.t) : int list list)))))) in
  let labels_new = ref (List.of_seq (Seq.take (new_index - 0) (Seq.drop 0 (List.to_seq ((Obj.obj (List.assoc (__str ("labels")) (ds) : Obj.t) : int list list)))))) in
  let batch_images = ref ((!images_rest @ !images_new)) in
  let batch_labels = ref ((!labels_rest @ !labels_new)) in
  let new_ds = [(__str ("images"), Obj.repr ((Obj.obj (List.assoc (__str ("images")) (ds) : Obj.t) : int list list))); (__str ("labels"), Obj.repr ((Obj.obj (List.assoc (__str ("labels")) (ds) : Obj.t) : int list list))); (__str ("num_examples"), Obj.repr ((Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int))); (__str ("index_in_epoch"), Obj.repr (new_index)); (__str ("epochs_completed"), Obj.repr (((Obj.obj (List.assoc (__str ("epochs_completed")) (ds) : Obj.t) : int) + 1)))] in
  __ret := (Obj.magic ([(__str ("dataset"), Obj.repr (new_ds)); (__str ("images"), Obj.repr (!batch_images)); (__str ("labels"), Obj.repr (!batch_labels))]) : (string * Obj.t) list); raise Return
  ) else (
  let end_ = (start + batch_size) in
  let batch_images = ref (List.of_seq (Seq.take (end_ - start) (Seq.drop start (List.to_seq ((Obj.obj (List.assoc (__str ("images")) (ds) : Obj.t) : int list list)))))) in
  let batch_labels = ref (List.of_seq (Seq.take (end_ - start) (Seq.drop start (List.to_seq ((Obj.obj (List.assoc (__str ("labels")) (ds) : Obj.t) : int list list)))))) in
  let new_ds = [(__str ("images"), Obj.repr ((Obj.obj (List.assoc (__str ("images")) (ds) : Obj.t) : int list list))); (__str ("labels"), Obj.repr ((Obj.obj (List.assoc (__str ("labels")) (ds) : Obj.t) : int list list))); (__str ("num_examples"), Obj.repr ((Obj.obj (List.assoc (__str ("num_examples")) (ds) : Obj.t) : int))); (__str ("index_in_epoch"), Obj.repr (end_)); (__str ("epochs_completed"), Obj.repr ((Obj.obj (List.assoc (__str ("epochs_completed")) (ds) : Obj.t) : int)))] in
  __ret := (Obj.magic ([(__str ("dataset"), Obj.repr (new_ds)); (__str ("images"), Obj.repr (!batch_images)); (__str ("labels"), Obj.repr (!batch_labels))]) : (string * Obj.t) list); raise Return
  );
    !__ret
  with Return -> !__ret)

and read_data_sets train_images train_labels_raw test_images test_labels_raw validation_size num_classes =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let train_images = (Obj.magic train_images : int list list) in
  let train_labels_raw = (Obj.magic train_labels_raw : int list) in
  let test_images = (Obj.magic test_images : int list list) in
  let test_labels_raw = (Obj.magic test_labels_raw : int list) in
  let validation_size = (Obj.magic validation_size : int) in
  let num_classes = (Obj.magic num_classes : int) in
  let train_labels = ref (dense_to_one_hot (train_labels_raw) (Obj.repr (num_classes))) in
  let test_labels = ref (dense_to_one_hot (test_labels_raw) (Obj.repr (num_classes))) in
  let validation_images = ref (List.of_seq (Seq.take (validation_size - 0) (Seq.drop 0 (List.to_seq (train_images))))) in
  let validation_labels = ref (List.of_seq (Seq.take (validation_size - 0) (Seq.drop 0 (List.to_seq (!train_labels))))) in
  let train_images_rest = ref (List.of_seq (Seq.take (List.length (train_images) - validation_size) (Seq.drop validation_size (List.to_seq (train_images))))) in
  let train_labels_rest = ref (List.of_seq (Seq.take (List.length (!train_labels) - validation_size) (Seq.drop validation_size (List.to_seq (!train_labels))))) in
  let train = new_dataset (!train_images_rest) (!train_labels_rest) in
  let validation = new_dataset (!validation_images) (!validation_labels) in
  let testset = new_dataset (test_images) (!test_labels) in
  __ret := (Obj.magic ([(__str ("train"), Obj.repr (train)); (__str ("validation"), Obj.repr (validation)); (__str ("test_ds"), Obj.repr (testset))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let train_images = ref ([[0; 1]; [1; 2]; [2; 3]; [3; 4]; [4; 5]]) in
  let train_labels_raw = ref ([0; 1; 2; 3; 4]) in
  let test_images = ref ([[5; 6]; [6; 7]]) in
  let test_labels_raw = ref ([5; 6]) in
  let data = read_data_sets (!train_images) (!train_labels_raw) (!test_images) (!test_labels_raw) (Obj.repr (2)) (Obj.repr (10)) in
  let ds = ref ((Obj.obj (List.assoc (__str ("train")) (data) : Obj.t) : ( string * Obj.t ) list)) in
  let res = ref (next_batch ((Obj.magic !ds : (string * Obj.t) list)) (Obj.repr (2))) in
  ds := (Obj.magic (Obj.obj (List.assoc (__str ("dataset")) (!res) : Obj.t) : ( string * Obj.t ) list) : (string * Obj.t) list);
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("images")) (!res) : Obj.t) : int list list)) : Obj.t)));
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("labels")) (!res) : Obj.t) : int list list)) : Obj.t)));
  res := next_batch ((Obj.magic !ds : (string * Obj.t) list)) (Obj.repr (2));
  ds := (Obj.magic (Obj.obj (List.assoc (__str ("dataset")) (!res) : Obj.t) : ( string * Obj.t ) list) : (string * Obj.t) list);
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("images")) (!res) : Obj.t) : int list list)) : Obj.t)));
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("labels")) (!res) : Obj.t) : int list list)) : Obj.t)));
  res := next_batch ((Obj.magic !ds : (string * Obj.t) list)) (Obj.repr (2));
  ds := (Obj.magic (Obj.obj (List.assoc (__str ("dataset")) (!res) : Obj.t) : ( string * Obj.t ) list) : (string * Obj.t) list);
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("images")) (!res) : Obj.t) : int list list)) : Obj.t)));
  print_endline ((__str (Obj.magic ((Obj.obj (List.assoc (__str ("labels")) (!res) : Obj.t) : int list list)) : Obj.t)));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
