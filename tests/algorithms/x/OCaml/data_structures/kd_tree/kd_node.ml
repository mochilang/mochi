(* Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:06 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let rec make_kd_node point left right =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let left = (Obj.magic left : int) in
  let right = (Obj.magic right : int) in
  __ret := (Obj.magic ([(__str ("point"), Obj.repr (point)); (__str ("left"), Obj.repr (left)); (__str ("right"), Obj.repr (right))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)


let nodes = ref (([] : ((string * Obj.t) list) list))
let () =
  let mem_start = _mem () in
  let start = _now () in
  nodes := (List.append (!nodes) [(Obj.magic (make_kd_node ([2.0; 3.0]) (Obj.repr (1)) (Obj.repr (2))) : (string * Obj.t) list)]);
  nodes := (List.append (!nodes) [(Obj.magic (make_kd_node ([1.0; 5.0]) (Obj.repr (-(1))) (Obj.repr (-(1)))) : (string * Obj.t) list)]);
  nodes := (List.append (!nodes) [(Obj.magic (make_kd_node ([4.0; 2.0]) (Obj.repr (-(1))) (Obj.repr (-(1)))) : (string * Obj.t) list)]);
  let root = List.nth (!nodes) (0) in
  let left_child = List.nth (!nodes) (1) in
  let right_child = List.nth (!nodes) (2) in
  print_endline ((__str ((Obj.obj (List.assoc (__str ("point")) (root) : Obj.t) : float list))));
  print_endline ((string_of_int ((Obj.obj (List.assoc (__str ("left")) (root) : Obj.t) : int))));
  print_endline ((string_of_int ((Obj.obj (List.assoc (__str ("right")) (root) : Obj.t) : int))));
  print_endline ((__str ((Obj.obj (List.assoc (__str ("point")) (left_child) : Obj.t) : float list))));
  print_endline ((__str ((Obj.obj (List.assoc (__str ("point")) (right_child) : Obj.t) : float list))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()