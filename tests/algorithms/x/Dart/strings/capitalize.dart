// Generated by Mochi transpiler
dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String lowercase = "abcdefghijklmnopqrstuvwxyz";
String uppercase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
int index_of(String s, String c) {
  int i = 0;
  while (i < s.length) {
    if (_substr(s, i, i + 1) == c) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String capitalize(String sentence) {
  if (sentence.length == 0) {
    return "";
  }
  String first = _substr(sentence, 0, 1);
  int idx = index_of(lowercase, first);
  String capital = (idx >= 0 ? _substr(uppercase, idx, idx + 1) : first);
  return capital + _substr(sentence, 1, sentence.length);
}

void main() {
  print(capitalize("hello world"));
  print(capitalize("123 hello world"));
  print(capitalize(" hello world"));
  print(capitalize("a"));
  print(capitalize(""));
}
