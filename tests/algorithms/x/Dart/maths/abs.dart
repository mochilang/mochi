// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

double abs_val(double _num) {
  if (_num < 0.0) {
    return -_num;
  }
  return _num;
}

int abs_min(List<int> x) {
  if (x.length == 0) {
    _error("abs_min() arg is an empty sequence");
  }
  int j = x[0];
  int idx = 0;
  while (idx < x.length) {
    int i = x[idx];
    if (abs_val((i).toDouble()) < abs_val((j).toDouble())) {
    j = i;
  }
    idx = idx + 1;
  }
  return j;
}

int abs_max(List<int> x) {
  if (x.length == 0) {
    _error("abs_max() arg is an empty sequence");
  }
  int j = x[0];
  int idx = 0;
  while (idx < x.length) {
    int i = x[idx];
    if (abs_val((i).toDouble()) > abs_val((j).toDouble())) {
    j = i;
  }
    idx = idx + 1;
  }
  return j;
}

int abs_max_sort(List<int> x) {
  if (x.length == 0) {
    _error("abs_max_sort() arg is an empty sequence");
  }
  List<int> arr = <int>[];
  int i = 0;
  while (i < x.length) {
    arr = [...arr, x[i]];
    i = i + 1;
  }
  int n = arr.length;
  int a = 0;
  while (a < n) {
    int b = 0;
    while (b < n - a - 1) {
    if (abs_val((arr[b]).toDouble()) > abs_val((arr[b + 1]).toDouble())) {
    int temp = arr[b];
    while (arr.length <= b) { arr.add(0); } arr[b] = arr[b + 1];
    while (arr.length <= b + 1) { arr.add(0); } arr[b + 1] = temp;
  }
    b = b + 1;
  }
    a = a + 1;
  }
  return arr[n - 1];
}

void test_abs_val() {
  if (abs_val(0.0) != 0.0) {
    _error("abs_val(0) failed");
  }
  if (abs_val(34.0) != 34.0) {
    _error("abs_val(34) failed");
  }
  if (abs_val(-100000000000.0) != 100000000000.0) {
    _error("abs_val large failed");
  }
  List<int> a = [-3, -1, 2, -11];
  if (abs_max(a) != -11) {
    _error("abs_max failed");
  }
  if (abs_max_sort(a) != -11) {
    _error("abs_max_sort failed");
  }
  if (abs_min(a) != -1) {
    _error("abs_min failed");
  }
}

void _main() {
  test_abs_val();
  print(abs_val(-34.0));
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
