// Generated by Mochi transpiler
String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

int parse_decimal(String s) {
  int value = 0;
  int i = 0;
  while (i < s.length) {
    String c = s.substring(i, i + 1);
    if (c.compareTo("0") < 0 || c.compareTo("9") > 0) {
    throw Exception("invalid literal");
  }
    value = value * 10 + ((c).codeUnitAt(0));
    i = i + 1;
  }
  return value;
}

String zeller_day(String date_input) {
  Map<int, String> days = {0: "Sunday", 1: "Monday", 2: "Tuesday", 3: "Wednesday", 4: "Thursday", 5: "Friday", 6: "Saturday"};
  if (date_input.length != 10) {
    throw Exception("Must be 10 characters long");
  }
  int m = parse_decimal(_substr(date_input, 0, 2));
  if (m <= 0 || m >= 13) {
    throw Exception("Month must be between 1 - 12");
  }
  String sep1 = date_input.substring(2, 2 + 1);
  if (sep1 != "-" && sep1 != "/") {
    throw Exception("Date separator must be '-' or '/'");
  }
  int d = parse_decimal(_substr(date_input, 3, 5));
  if (d <= 0 || d >= 32) {
    throw Exception("Date must be between 1 - 31");
  }
  String sep2 = date_input.substring(5, 5 + 1);
  if (sep2 != "-" && sep2 != "/") {
    throw Exception("Date separator must be '-' or '/'");
  }
  int y = parse_decimal(_substr(date_input, 6, 10));
  if (y <= 45 || y >= 8500) {
    throw Exception("Year out of range. There has to be some sort of limit...right?");
  }
  int year = y;
  int month = m;
  if (month <= 2) {
    year = year - 1;
    month = month + 12;
  }
  int c = year ~/ 100;
  int k = year % 100;
  int t = 2.6 * (month as double) - 5.39 as int;
  int u = c ~/ 4;
  int v = k ~/ 4;
  int x = d + k;
  int z = t + u + v + x;
  int w = z - 2 * c;
  int f = w % 7;
  if (f < 0) {
    f = f + 7;
  }
  return ((days[f] ?? ""))!;
}

String zeller(String date_input) {
  String day = zeller_day(date_input);
  return "Your date " + date_input + ", is a " + day + "!";
}

void test_zeller() {
  List<String> inputs = ["01-31-2010", "02-01-2010", "11-26-2024", "07-04-1776"];
  List<String> expected = ["Sunday", "Monday", "Tuesday", "Thursday"];
  int i = 0;
  while (i < inputs.length) {
    String res = zeller_day(inputs[i]);
    if (res != expected[i]) {
    throw Exception("zeller test failed");
  }
    i = i + 1;
  }
}

void _main() {
  test_zeller();
  print(zeller("01-31-2010"));
}

void _start() {
  _main();
}

void main() => _start();
