// Generated by Mochi transpiler
String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

int mod_pow(int base, int exponent, int modulus) {
  int result = 1;
  int b = base % modulus;
  int e = exponent;
  while (e > 0) {
    if (e % 2 == 1) {
    result = result * b % modulus;
  }
    b = b * b % modulus;
    e = e ~/ 2;
  }
  return result;
}

double pow_float(double base, int exponent) {
  int exp = exponent;
  double result = 1.0;
  if (exp < 0) {
    exp = -exp;
  }
  int i = 0;
  while (i < exp) {
    result = result * base;
    i = i + 1;
  }
  if (exponent < 0) {
    result = 1.0 / result;
  }
  return result;
}

String hex_digit(int n) {
  if (n < 10) {
    return (n).toString();
  }
  List<String> letters = ["a", "b", "c", "d", "e", "f"];
  return letters[n - 10];
}

double floor_float(double x) {
  int i = x as int;
  if ((i as double) > x) {
    i = i - 1;
  }
  return i as double;
}

double subsum(int digit_pos_to_extract, int denominator_addend, int precision) {
  double total = 0.0;
  int sum_index = 0;
  while (sum_index < digit_pos_to_extract + precision) {
    int denominator = 8 * sum_index + denominator_addend;
    if (sum_index < digit_pos_to_extract) {
    int exponent = digit_pos_to_extract - 1 - sum_index;
    int exponential_term = mod_pow(16, exponent, denominator);
    total = total + (exponential_term as double) / (denominator as double);
  } else {
    int exponent = digit_pos_to_extract - 1 - sum_index;
    double exponential_term = pow_float(16.0, exponent);
    total = total + exponential_term / (denominator as double);
  }
    sum_index = sum_index + 1;
  }
  return total;
}

String bailey_borwein_plouffe(int digit_position, int precision) {
  if (digit_position <= 0) {
    throw Exception("Digit position must be a positive integer");
  }
  if (precision < 0) {
    throw Exception("Precision must be a nonnegative integer");
  }
  double sum_result = 4.0 * subsum(digit_position, 1, precision) - 2.0 * subsum(digit_position, 4, precision) - 1.0 * subsum(digit_position, 5, precision) - 1.0 * subsum(digit_position, 6, precision);
  double fraction = sum_result - floor_float(sum_result);
  int digit = fraction * 16.0 as int;
  String hd = hex_digit(digit);
  return hd;
}

String digits = "";
int i = 1;
void main() {
  while (i <= 10) {
    digits = digits + bailey_borwein_plouffe(i, 1000);
    i = i + 1;
  }
  print(digits);
  print(bailey_borwein_plouffe(5, 10000));
}
