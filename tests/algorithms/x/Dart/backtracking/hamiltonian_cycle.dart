// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

void _set(List l, int i, dynamic v) { while (l.length <= i) { l.add(v); } l[i] = v; }

bool valid_connection(List<List<int>> graph, int next_ver, int curr_ind, List<int> path) {
  if (graph[path[curr_ind - 1]][next_ver] == 0) {
    return false;
  }
  for (int v in path) {
    if (v == next_ver) {
    return false;
  }
  }
  return true;
}

bool util_hamilton_cycle(List<List<int>> graph, List<int> path, int curr_ind) {
  if (curr_ind == graph.length) {
    return graph[path[curr_ind - 1]][path[0]] == 1;
  }
  int next_ver = 0;
  while (next_ver < graph.length) {
    if (valid_connection(graph, next_ver, curr_ind, path)) {
    _set(path, curr_ind, next_ver);
    if (util_hamilton_cycle(graph, path, curr_ind + 1)) {
    return true;
  };
    _set(path, curr_ind, -1);
  }
    next_ver = next_ver + 1;
  }
  return false;
}

List<int> hamilton_cycle(List<List<int>> graph, int start_index) {
  List<int> path = <int>[];
  int i = 0;
  while (i < graph.length + 1) {
    _set(path, i, -1);
    i = i + 1;
  }
  _set(path, 0, start_index);
  int last = path.length - 1;
  _set(path, last, start_index);
  if (util_hamilton_cycle(graph, path, 1)) {
    return path;
  }
  return ([] as List).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
