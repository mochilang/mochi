// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

int dp_count(List<int> s, int n) {
  if (n < 0) {
    return 0;
  }
  List<int> table = <int>[];
  int i = 0;
  while (i <= n) {
    table = [...table, 0];
    i = i + 1;
  }
  while (table.length <= 0) { table.add(0); } table[0] = 1;
  int idx = 0;
  while (idx < s.length) {
    int coin_val = s[idx];
    int j = coin_val;
    while (j <= n) {
    while (table.length <= j) { table.add(0); } table[j] = table[j] + table[j - coin_val];
    j = j + 1;
  }
    idx = idx + 1;
  }
  return table[n];
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(dp_count([1, 2, 3], 4));
  print(dp_count([1, 2, 3], 7));
  print(dp_count([2, 5, 3, 6], 10));
  print(dp_count([10], 99));
  print(dp_count([4, 5, 6], 0));
  print(dp_count([1, 2, 3], -5));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
