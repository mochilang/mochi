// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

List<double> exponential_moving_average(List<double> stock_prices, int window_size) {
  if (window_size <= 0) {
    throw Exception("window_size must be > 0");
  }
  double alpha = 2.0 / (1.0 + (window_size as double));
  double moving_average = 0.0;
  List<double> result = <double>[];
  int i = 0;
  while (i < stock_prices.length) {
    double price = stock_prices[i];
    if (i <= window_size) {
    if (i == 0) {
    moving_average = price;
  } else {
    moving_average = (moving_average + price) * 0.5;
  };
  } else {
    moving_average = alpha * price + (1.0 - alpha) * moving_average;
  }
    result = [...result, moving_average];
    i = i + 1;
  }
  return result;
}

List<double> stock_prices = [2.0, 5.0, 3.0, 8.2, 6.0, 9.0, 10.0];
int window_size = 3;
List<double> result = exponential_moving_average(stock_prices, window_size);
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print((result).toString());
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
