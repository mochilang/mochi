// Generated by Mochi transpiler
String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

List<String> units = ["km/h", "m/s", "mph", "knot"];
List<double> speed_chart = [1.0, 3.6, 1.609344, 1.852];
List<double> speed_chart_inverse = [1.0, 0.277777778, 0.621371192, 0.539956803];
int index_of(List<String> arr, String value) {
  int i = 0;
  while (i < arr.length) {
    if (arr[i] == value) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String units_string(List<String> arr) {
  String s = "";
  int i = 0;
  while (i < arr.length) {
    if (i > 0) {
    s = s + ", ";
  }
    s = s + arr[i];
    i = i + 1;
  }
  return s;
}

double round3(double x) {
  double y = x * 1000.0 + 0.5;
  int z = y as int;
  double zf = z as double;
  return zf / 1000.0;
}

double convert_speed(double speed, String unit_from, String unit_to) {
  int from_index = index_of(units, unit_from);
  int to_index = index_of(units, unit_to);
  if (from_index < 0 || to_index < 0) {
    String msg = "Incorrect 'from_type' or 'to_type' value: " + unit_from + ", " + unit_to + "\nValid values are: " + units_string(units);
    throw Exception(msg);
  }
  double result = speed * speed_chart[from_index] * speed_chart_inverse[to_index];
  double r = round3(result);
  return r;
}

void main() {
  print((convert_speed(100.0, "km/h", "m/s")).toString());
  print((convert_speed(100.0, "km/h", "mph")).toString());
  print((convert_speed(100.0, "km/h", "knot")).toString());
  print((convert_speed(100.0, "m/s", "km/h")).toString());
  print((convert_speed(100.0, "m/s", "mph")).toString());
  print((convert_speed(100.0, "m/s", "knot")).toString());
  print((convert_speed(100.0, "mph", "km/h")).toString());
  print((convert_speed(100.0, "mph", "m/s")).toString());
  print((convert_speed(100.0, "mph", "knot")).toString());
  print((convert_speed(100.0, "knot", "km/h")).toString());
  print((convert_speed(100.0, "knot", "m/s")).toString());
  print((convert_speed(100.0, "knot", "mph")).toString());
}
