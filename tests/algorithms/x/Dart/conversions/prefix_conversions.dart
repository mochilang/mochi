// Generated by Mochi transpiler
String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

Map<String, int> SI_UNITS = {"yotta": 24, "zetta": 21, "exa": 18, "peta": 15, "tera": 12, "giga": 9, "mega": 6, "kilo": 3, "hecto": 2, "deca": 1, "deci": -1, "centi": -2, "milli": -3, "micro": -6, "nano": -9, "pico": -12, "femto": -15, "atto": -18, "zepto": -21, "yocto": -24};
Map<String, int> BINARY_UNITS = {"yotta": 8, "zetta": 7, "exa": 6, "peta": 5, "tera": 4, "giga": 3, "mega": 2, "kilo": 1};
double pow(double base, int exp) {
  if (exp == 0) {
    return 1.0;
  }
  int e = exp;
  if (e < 0) {
    e = -e;
  }
  double result = 1.0;
  int i = 0;
  while (i < e) {
    result = result * base;
    i = i + 1;
  }
  if (exp < 0) {
    return 1.0 / result;
  }
  return result;
}

double convert_si_prefix(double known_amount, String known_prefix, String unknown_prefix) {
  String kp = known_prefix.toLowerCase();
  String up = unknown_prefix.toLowerCase();
  if (!SI_UNITS.containsKey(kp)) {
    throw Exception("unknown prefix: " + known_prefix);
  }
  if (!SI_UNITS.containsKey(up)) {
    throw Exception("unknown prefix: " + unknown_prefix);
  }
  num diff = SI_UNITS[kp]! - SI_UNITS[up]!;
  return known_amount * pow(10.0, (diff).toInt());
}

double convert_binary_prefix(double known_amount, String known_prefix, String unknown_prefix) {
  String kp = known_prefix.toLowerCase();
  String up = unknown_prefix.toLowerCase();
  if (!BINARY_UNITS.containsKey(kp)) {
    throw Exception("unknown prefix: " + known_prefix);
  }
  if (!BINARY_UNITS.containsKey(up)) {
    throw Exception("unknown prefix: " + unknown_prefix);
  }
  double diff = (BINARY_UNITS[kp]! - BINARY_UNITS[up]!) * 10;
  return known_amount * pow(2.0, diff);
}

void main() {
  print((convert_si_prefix(1.0, "giga", "mega")).toString());
  print((convert_si_prefix(1.0, "mega", "giga")).toString());
  print((convert_si_prefix(1.0, "kilo", "kilo")).toString());
  print((convert_binary_prefix(1.0, "giga", "mega")).toString());
  print((convert_binary_prefix(1.0, "mega", "giga")).toString());
  print((convert_binary_prefix(1.0, "kilo", "kilo")).toString());
}
