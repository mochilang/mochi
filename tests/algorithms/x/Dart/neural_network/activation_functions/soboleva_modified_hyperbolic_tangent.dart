// Generated by Mochi transpiler
import 'dart:convert';

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}


void _json(dynamic v) {
  print(jsonEncode(v));
}

double exp(double x) {
  double term = 1.0;
  double sum = 1.0;
  int n = 1;
  while (n < 20) {
    term = term * x / n.toDouble();
    sum = sum + term;
    n = n + 1;
  }
  return sum;
}

List<double> soboleva_modified_hyperbolic_tangent(List<double> vector, double a_value, double b_value, double c_value, double d_value) {
  List<double> result = <double>[];
  int i = 0;
  while (i < vector.length) {
    double x = vector[i];
    double numerator = exp(a_value * x) - exp(-b_value * x);
    double denominator = exp(c_value * x) + exp(-d_value * x);
    result = (result..add(numerator / denominator));
    i = i + 1;
  }
  return result;
}

void _main() {
  List<double> vector = [5.4, -2.4, 6.3, -5.23, 3.27, 0.56];
  List<double> res = soboleva_modified_hyperbolic_tangent(vector, 0.2, 0.4, 0.6, 0.8);
  _json(res);
}

void _start() {
  _main();
}

void main() => _start();
