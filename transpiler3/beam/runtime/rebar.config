{erl_opts, [debug_info]}.
{deps, []}.

%% OTP 27 is the minimum version. The compiler checks this at startup.
{minimum_otp_vsn, "27"}.

{profiles, [
  {test, [
    {erl_opts, [debug_info]},
    {deps, []}
  ]}
]}.
