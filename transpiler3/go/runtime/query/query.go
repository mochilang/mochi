// Package query backs Mochi's `from` / `where` / `group_by` /
// `order_by` / `join` query syntax with type-parameterised Go
// helpers.
//
// Phase 7 introduces the full implementation. Phase 0 ships
// an empty package so the emitter can import the path.
package query
