// Generated by Mochi TypeScript compiler

let example1: Node = {__name: "Node", left: {__name: "Node", left: {__name: "Leaf"}, value: 9, right: {__name: "Leaf"}}, value: 3, right: {__name: "Node", left: {__name: "Node", left: {__name: "Leaf"}, value: 15, right: {__name: "Leaf"}}, value: 20, right: {__name: "Node", left: {__name: "Leaf"}, value: 7, right: {__name: "Leaf"}}}}

function levelOrderBottom(root: Tree) : Array<Array<number>> {
	if ((() => {
	const _t = root;
	if (_t.__name === "Leaf") { return true }
	return false
})()) {
		return []
	}
	let result: Array<Array<number>> = []
	let queue: Array<any> = [root]
	while ((queue.length > 0)) {
		let level: Array<number> = []
		let next: Array<any> = []
		for (const node of queue) {
			if ((() => {
	const _t = node;
	if (_t.__name === "Leaf") { return false }
	return true
})()) {
				level = level.concat([node.value])
				if ((() => {
	const _t = node.left;
	if (_t.__name === "Leaf") { return false }
	return true
})()) {
					next = next.concat([node.left])
				}
				if ((() => {
	const _t = node.right;
	if (_t.__name === "Leaf") { return false }
	return true
})()) {
					next = next.concat([node.right])
				}
			}
		}
		result = [level].concat(result)
		queue = next
	}
	return result
}

function example_1(): void {
	if (!(_equal(levelOrderBottom(example1), [[15, 7], [9, 20], [3]]))) { throw new Error('expect failed') }
}

function single_node(): void {
	if (!(_equal(levelOrderBottom({__name: "Node", left: {__name: "Leaf"}, value: 1, right: {__name: "Leaf"}}), [[1]]))) { throw new Error('expect failed') }
}

function empty(): void {
	if (!(_equal(levelOrderBottom({__name: "Leaf"}), []))) { throw new Error('expect failed') }
}

function main(): void {
	type Leaf = {
		__name: "Leaf";
	}
	
	type Node = {
		__name: "Node";
		left: any;
		value: number;
		right: any;
	}
	
	type Tree = Leaf | Node
	example_1()
	single_node()
	empty()
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}

main()

