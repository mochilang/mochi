//go:build ignore

package main

import (
	"encoding/json"
	"fmt"
	"strconv"
	"time"
)

// line 1
func matmul(a [][]int, b [][]int) [][]int {
	var n int = len(a)
	var m int = len(b[0])
	var p int = len(b)
	var result [][]int = [][]int{}
	for i := 0; i < n; i++ {
		var row []int = []int{}
		for j := 0; j < m; j++ {
			var sum int = 0
			for k := 0; k < p; k++ {
				sum = (sum + (a[i][k] * b[k][j]))
			}
			row = append(append([]int{}, row...), []int{sum}...)
		}
		result = append(append([][]int{}, result...), [][]int{row}...)
	}
	return result
}

var a [][]int
var b [][]int

func main() {
	a = [][]int{}
	b = [][]int{}
	var size int = 10
	var repeat int = 10
	for i := 0; i < size; i++ {
		var row []int = []int{}
		for j := 0; j < size; j++ {
			row = append(append([]int{}, row...), []int{(i + j)}...)
		}
		a = append(append([][]int{}, a...), [][]int{row}...)
	}
	for i := 0; i < size; i++ {
		var row []int = []int{}
		for j := 0; j < size; j++ {
			row = append(append([]int{}, row...), []int{(i * j)}...)
		}
		b = append(append([][]int{}, b...), [][]int{row}...)
	}
	var last [][]int = [][]int{}
	var start int64 = time.Now().UnixNano()
	for i := 0; i < repeat; i++ {
		last = matmul(a, b)
	}
	var end int64 = time.Now().UnixNano()
	var duration float64 = _cast[float64]((int64((int64(end) - int64(start))) / int64(1000)))
	func() {
		b, _ := json.Marshal(map[string]any{"duration_us": duration, "output": last[0][0]})
		fmt.Println(string(b))
	}()
}

func _cast[T any](v any) T {
	if tv, ok := v.(T); ok {
		return tv
	}
	var out T
	switch any(out).(type) {
	case int:
		switch vv := v.(type) {
		case int:
			return any(vv).(T)
		case float64:
			return any(int(vv)).(T)
		case float32:
			return any(int(vv)).(T)
		case string:
			n, _ := strconv.Atoi(vv)
			return any(n).(T)
		}
	case float64:
		switch vv := v.(type) {
		case int:
			return any(float64(vv)).(T)
		case float64:
			return any(vv).(T)
		case float32:
			return any(float64(vv)).(T)
		}
	case float32:
		switch vv := v.(type) {
		case int:
			return any(float32(vv)).(T)
		case float64:
			return any(float32(vv)).(T)
		case float32:
			return any(vv).(T)
		}
	}
	if m, ok := v.(map[any]any); ok {
		v = _convertMapAny(m)
	}
	data, err := json.Marshal(v)
	if err != nil {
		panic(err)
	}
	if err := json.Unmarshal(data, &out); err != nil {
		panic(err)
	}
	return out
}

func _convertMapAny(m map[any]any) map[string]any {
	out := make(map[string]any, len(m))
	for k, v := range m {
		key := fmt.Sprint(k)
		if sub, ok := v.(map[any]any); ok {
			out[key] = _convertMapAny(sub)
		} else {
			out[key] = v
		}
	}
	return out
}
