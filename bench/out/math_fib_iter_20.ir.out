func main (regs=19)
  // let n = 20
  Const        r0, 20
  // let repeat = 1000
  Const        r1, 1000
  // var last = 0
  Const        r2, 0
  Move         r3, r2
  // let start = now()
  Now          r4
  // for i in 0..repeat {
  Move         r5, r2
L1:
  Less         r6, r5, r1
  JumpIfFalse  r6, L0
  // last = fib(n)
  Move         r7, r0
  Call         r3, fib, r7
  // for i in 0..repeat {
  Const        r9, 1
  Add          r5, r5, r9
  Jump         L1
L0:
  // let duration = (now() - start) / 1000
  Now          r11
  SubInt       r12, r11, r4
  DivInt       r13, r12, r1
  // "duration_us": duration,
  Const        r14, "duration_us"
  // "output": last,
  Const        r15, "output"
  // "duration_us": duration,
  Move         r16, r13
  // "output": last,
  Move         r17, r3
  // json({
  MakeMap      r18, 2, r14
  JSON         r18
  Return       r0

  // fun fib(n: int): int {
func fib (regs=10)
  // var a = 0
  Const        r1, 0
  Move         r2, r1
  // var b = 1
  Const        r4, 1
  // for i in 0..n {
  Move         r5, r1
L1:
  Less         r6, r5, r0
  JumpIfFalse  r6, L0
  // a = b
  Move         r2, r4
  // for i in 0..n {
  Const        r8, 1
  Add          r5, r5, r8
  Jump         L1
L0:
  // return a
  Return       r2
  Return       r0
