func main (regs=10)
  // let n = 20
  Const        r0, 20
L1:
  // let repeat = 1000
  Const        r1, 1000
  // var last = 0
  Const        r2, 0
  // let start = now()
  Now          r3
L0:
  // for i in 0..repeat {
  Const        r4, 0
  Less         r5, r4, r1
  JumpIfFalse  r5, L0
  // last = fact_rec(n)
  Move         r1, r0
  Call         r2, fact_rec, r1
  // for i in 0..repeat {
  Const        r5, 1
  Add          r4, r4, r5
  Jump         L1
  // let duration = (now() - start) / 1000
  Now          r4
  SubInt       r5, r4, r3
  Const        r1, 1000
  DivInt       r3, r5, r1
  // "duration_us": duration,
  Const        r4, "duration_us"
  // "output": last,
  Const        r5, "output"
  // "duration_us": duration,
  Move         r6, r4
  Move         r7, r3
  // "output": last,
  Move         r8, r5
  Move         r9, r2
  // json({
  MakeMap      r1, 0, r6
  JSON         r1
  Return       r0

  // fun fact_rec(n: int): int {
func fact_rec (regs=3)
  // if n == 0 {
  Const        r1, 0
  Equal        r2, r0, r1
L0:
  JumpIfFalse  r2, L0
  // return 1
  Const        r1, 1
  Return       r1
  // return n * fact_rec(n - 1)
  Const        r2, 1
  Sub          r1, r0, r2
  Call         r2, fact_rec, r1
  Mul          r1, r0, r2
  Return       r1
