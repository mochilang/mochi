func main (regs=2)
  // let size = 30
  Const        r0, 30
L0:
  // for j in 0..size {
  Jump         L0

  // fun matmul(a: list<list<int>>, b: list<list<int>>): list<list<int>> {
func matmul (regs=9)
  // let n = len(a)
  Len          r2, r0
  // let m = len(b[0])
  Const        r3, 0
  Index        r4, r1, r3
  Len          r5, r4
L3:
  // let p = len(b)
  Len          r4, r1
  // var result: list<list<int>> = []
  Const        r1, []
  // for i in 0..n {
  Move         r6, r3
L4:
  Less         r7, r6, r2
L0:
  JumpIfFalse  r7, L0
  // var row: list<int> = []
  Const        r7, []
  // for j in 0..m {
  Move         r2, r3
  Less         r8, r2, r5
  JumpIfFalse  r8, L1
  // for k in 0..p {
  Less         r8, r3, r4
  JumpIfFalse  r8, L2
L2:
  // row = row + [sum]
  MakeList     r4, 1, r8
  Add          r7, r7, r4
  // for j in 0..m {
  Const        r4, 1
  Add          r2, r2, r4
  Jump         L3
L1:
  // result = result + [row]
  Move         r4, r7
  MakeList     r7, 1, r4
  Add          r1, r1, r7
  // for i in 0..n {
  Const        r7, 1
  Add          r6, r6, r7
  Jump         L4
  // return result
  Return       r1
